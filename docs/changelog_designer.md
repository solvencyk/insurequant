# Insurequant Changelog — Designer Stage

> Last updated: 2026-09-03 · Stage 5/5 — designer
> Prompt: docs/agents/claude-agent-designer.md · TODO: TODO_designer.md

Scope: HTML structure / styling / responsive breakpoints / chart layout / A11y. Master JSON content is **publishing** ([`changelog_publishing.md`](changelog_publishing.md)) — designer reads them but does not modify. Cross-stage history: `docs/claude-changelog.md`.

---

## 2026-09-03 — 섹션 스크롤 스냅("쫀득") 4개 페이지 공통 (owner 직접 지시 · 라이브 배포 `cda3c1e`)

owner: "지금 모든 html들에서 스크롤 내릴때 좀 쫀득하게 내려가면 좋겠는데 / 각 섹션 안에서는
평소대로 스크롤되다가 끝에서 살짝 멈추고, 다음 섹션으로 넘어갈땐 휘리릭 넘어가게"

`common.css` 한 곳만 고쳐 4개 페이지(index · K-ICS · IFRS17 · 공시보고서)에 동시 적용했다.
HTML 은 건드리지 않았다 — 네 페이지가 `header` / `.container` / `.panel` / `footer` 구조를
공유하기 때문이다.

```css
:root{ --header-h:76px; }
html{ scroll-snap-type:y proximity; scroll-padding-top:calc(var(--header-h) + 12px); }
.panel{ scroll-snap-align:start; scroll-snap-stop:normal; }
.container{ scroll-snap-align:start; }   /* 맨 위 도달 보장 */
footer{ scroll-snap-align:end; }         /* 바닥·푸터 도달 보장 */
```

**설계 결정 4개와 그 근거(전부 실측):**

1. **`proximity`, `mandatory` 아님.** 패널이 뷰포트보다 크다. mandatory 면 섹션 중간에서도
   계속 끌려 긴 표를 못 읽는다. proximity 는 경계 근처에서만 당긴다 — 섹션 내부 오프셋
   200/400/600px 에서 끌림 **0px**(4개 페이지 · 데스크톱 1280×900 · 모바일 375×812).
2. **`scroll-padding-top` 88px.** header 가 `position:sticky; top:0` 이라 이걸 안 주면 스냅된
   섹션 제목이 헤더에 가린다. 헤더 실측은 4개 페이지 전부 **데스크톱 76px / 모바일 71px** —
   큰 쪽 + 12px. 검산: 모든 패널을 `scrollIntoView()` 했을 때 제목 top 이 전부 **88px**
   (헤더 76px 아래, 가림 0).
3. **양 끝 트랩 방지 — 실제로 재현해서 잡은 회귀다.** 패널에만 스냅 지점을 두면 첫 지점 위와
   마지막 지점 아래가 통째로 못 가는 영역이 된다. index 실측: `scrollTo(0)` 이 **160** 으로
   끌려가 페이지 최상단에 못 갔고, 최대 스크롤 2,678 에서 **2,564** 로 되돌아가 푸터가 화면
   밖에 남았다. `.container`(레이아웃 top = sticky header 높이 76px → padding 88 을 빼면 음수
   → 0 으로 clamp)와 `footer{end}` 로 양 끝을 정식 스냅 지점으로 만들어 해소.
   `header{scroll-snap-align:start}` 는 **실패했다** — sticky 라 스냅 위치가 안 잡힌다(실측 160 그대로).
4. **`prefers-reduced-motion` 에서 스냅을 끈다.** 움직임을 줄여 달라는 사용자에게 자석 스크롤은
   정확히 그 반대다.

**섹션 끝의 "살짝 멈춤"은 브라우저 동작 그 자체다.** 패널이 스냅포트보다 크면 Chrome 은 스냅
가능 범위를 `[패널시작-88, 패널끝-스냅포트-88]` 로 제한한다(IFRS17 실측 상한 704). 프로그램적
`scrollTo` 는 그 벽에 걸리지만 **실제 휠 입력은 넘어간다**(704 에서 휠 3틱 → 992). 즉 못 가는
영역은 없고, 경계 ~110px 안에서 다음 섹션 시작으로 스냅한다(K-ICS 실측: 다음 섹션 70px 전
요청 → 다음 섹션 시작으로 이동).

**검증 방법(재현 가능):** 로컬 `python -m http.server` + 헤드리스. 이 PC 는 보안 에이전트가
jsdelivr 를 막아 차트가 안 그려지고 패널이 `will-reveal`(opacity 0)에 멈춘다 — **내 CSS 변경과
무관한 네트워크 artifact** 다. 실콘텐츠 높이를 흉내내려고 각 패널 `minHeight` 를 1,300~1,600px
로 부풀려 기하를 실측했다. 초기 측정에서 나온 "중간 끌림 -212px" 은 레이아웃 정착 전에 잰
잡음이었고, 같은 구간(2,300~2,700)을 재측정하니 전부 끌림 0 이었다.

측정 결과 요약(최종 CSS):

| 페이지 | 뷰포트 | 제목 가림 | 맨 위 도달 | 바닥 도달 | 섹션 중간 끌림 |
|---|---|---|---|---|---|
| index | 1280×900 / 375×812 | 0 (88px) | O | O | 0 |
| K-ICS | 1280×900 / 375×812 | 0 (88px) | O | O | 0 |
| IFRS17 | 1280×900 | 0 (88px) | O | O | 0 |
| 공시보고서 | 1280×900 | 0 (88px) | O | O | 0 |

모달 내부 스크롤러(`.iq-modal-panel`)는 별도 스크롤 컨테이너라 영향 없음(`scroll-snap-type:none` 실측).

**배포 (2026-09-03, main `e1c2ad9..cda3c1e`).** 배포 대상은 `common.css` 1개뿐이었다
(`git ls-tree origin/main` 전수 대조 결과 다른 파일은 전부 동일).

작업 PC 에서 push 가 막혀 안드로이드(Termux)의 **얕은 main 클론**에서 올렸다. 여기서 배운 것 셋:

1. **읽기가 통하는 것은 push 가 통한다는 뜻이 아니다.** `git ls-remote` · `git fetch` 는 정상
   동작하는데 `git push --dry-run` 은 3분 무응답으로 걸렸다. 그 차이를 확인하지 않고 "깃허브가
   다시 열렸다"고 보고했다가 owner 에게 잡혔다. **연결 판정은 읽기가 아니라 쓰기 경로로 한다.**
2. **`git hash-object` 값을 기계 간에 비교하면 안 된다.** 작업 PC 작업본은 줄끝이 CRLF 인데
   git 이 LF 로 정규화해 `147c421b` 를 뱉고, 폰(autocrlf 없음)에서는 원본 바이트 그대로
   `e674363` 이 나왔다. 파일은 바이트가 같았는데 "깨졌다"고 오판할 뻔했다. 폰에서
   `sed -i 's/$//'` 로 LF 로 맞춘 뒤 해시가 일치했고, 그래야 저장소 관례(LF)와도 맞고
   diff 도 36줄 추가로 깔끔하게 남는다.
3. **커밋 전 게이트 두 개를 명령줄에 박아 두면 사고를 막는다.** 스테이지된 블롭 해시 대조 +
   `git diff --cached --stat`. 이번엔 `1 file changed, 36 insertions(+)` 이 찍혔다 —
   2026-09-03 오전에 라이브를 깨뜨린 그 사고의 신호가 `274,765 insertions` 였는데 그때는
   그걸 보고도 넘겼다.

라이브 검산: `curl -sk https://www.insurequant.com/common.css?cb=...` 로 받은 파일의 블롭 해시가
`147c421b`(배포 대상과 동일), 크기 18,110B, 스냅 룰 5개 전부 존재.

---

## 2026-08-30d — Panel 5 모바일 x축 라벨 겹침 해소 (owner 후속 지시 · 라이브 배포)

owner: "당기순이익까지 보이긴 한데 글씨가 오지게 겹쳐, 모바일은 어쩔수없나?"

**증상(실측).** 375px 화면에서 `#chartPl` 실폭 = 277px. 좌우 여백(56+18)을 빼면 203px 인데
모바일 축은 OCI 를 뺀 막대 12개를 전부 담으므로 막대 하나에 **16.9px** 이다. x 라벨은 두 줄
("원수" / "CSM상각")이라 한 줄에 30px 넘게 필요했고, 그래서 글자가 통째로 겹쳤다. 320px 화면은 더 심했다.

**고친 것 (`IFRS17.html` — 전부 `isMobile` 분기 안. 데스크톱 옵션은 값까지 동일함을 확인했다.)**
- x 라벨을 **45도 회전**했다. 회전하면 이웃 라벨 사이의 수직 거리가 `막대간격 × sin45` = 12~14px 로
  벌어져 글자 높이(9px)보다 커진다 — 즉 겹칠 수 없다. `interval:0` 은 그대로 뒀다(라벨을 숨기지 않는다).
- 회전만 하면 긴 이름이 아래로 40px 넘게 내려와 그림이 눌린다. 그래서 **모바일 전용 한 줄 라벨표
  `PL_WRAP_M`** 을 새로 뒀다(원수 CSM상각→CSM상각, 생명장기재보험→재보험, 기타(사업비용)→사업비 …).
  "원수" 접두는 패널 제목과 캡션에 이미 있고, **전체 이름은 툴팁과 아래 표에 그대로** 남는다.
  재보험사 축(생명 수재 …)은 이 표에 없어 `bar.ko` 가 그대로 쓰인다(코리안리로 확인).
- 모바일 `GRID_L` 56→38, `GRID_R` 18→8, y 라벨 12→9px. y 라벨이 "1.5·0.0·△0.5" 뿐이라 56px 는
  통째로 낭비였다. 여기서 되찾은 28px 가 막대당 2.3px 로 들어간다.
- 모바일 `grid.bottom` 46→60(슬라이더가 있을 때 58→72). 회전 라벨이 내려오는 만큼만 늘렸다.

**확인.** 로컬 정적 서버로 375px·320px 실화면에서 3사를 캡처했다 — 메리츠화재(막대 12개),
삼성생명(11개), 코리안리(11개, LOB 라벨이 동적인 축). 겹침 0. 데스크톱은 ECharts `getOption()`
으로 grid 56/18/24/58 · fontSize 10 · rotate 0 · 두 줄 라벨까지 종전과 같음을 확인했다(회귀 0).
`pytest tests/test_deploy_assets.py` 10 passed.

**배포.** 브랜치 `3891752` → 격리 워크트리로 main `2a7c51b`. 라이브 sha256 이 로컬과
동일(`d575821f…`, 132,836 bytes), 새 표식 3종 존재 · 옛 표식 0.

> 세션 메모: 이 환경에서 `preview_start` 는 시작에 성공했다고 보고하고도 실제로는 연결이 안 된다
> (curl exit 7). 대신 이전 세션이 남긴 정적 서버(포트 8896, 저장소 루트)가 살아 있어 그걸 그대로 썼다.
> 브라우저가 강하게 캐시하므로 URL 에 `?v=N` 을 붙여야 고친 파일이 뜬다.

---

## 2026-08-30c — owner 직접 지시 배치: 듀레이션 카드 · Panel 5 모바일 · index 색 스케일 (라이브 배포)

owner 가 화면을 보며 준 지시 6건을 처리하고 세 번에 나눠 라이브(main)에 올렸다
(`bfb8f8b` · `8634783` · `98337f6`). 데이터 숫자를 바꾼 것은 듀레이션·컨벡서티 **신규 2열**뿐이고
나머지는 전부 표현 계층이다.

### 1. K-ICS 순자산 듀레이션 · 컨벡서티 카드 (`K-ICS.html`)

owner: "KICS 민감도가 50bp 단위로 있으니까 컨벡서티도 산출되지 않나? 큼직한 칸 할애해서 보여줘라."

**별도 마스터를 만들지 않았다** — owner 지시대로 기존 `kics_rate_sensitivity.json` 에 `듀레이션` ·
`컨벡서티` **두 열만 붙였다**. 유효듀레이션 `D = -(V+ - V-)/(2·V0·Δy)`, 유효컨벡서티
`C = (V+ + V- - 2·V0)/(V0·Δy²)`. 정의는 `extract_kics_rate_sensitivity.py` 의
`duration_convexity()` **한 곳뿐**이고, gold 재적용 스크립트는 그것을 import 한다(재타이핑 금지 —
두 벌이면 그 순간부터 갈라진다).

카드는 처음 4칸이었는데 owner 가 **"요구자본 듀레이션, 금리 1%p시 순자산 증감은 빼라"** 해서
순자산 듀레이션 · 순자산 컨벡서티 **2칸**으로 줄였다. 음수 듀레이션 설명도 owner 지시로 추가했다
(부호가 뒤집히는 것이 국내 생보사에서 흔하다).

### 2. KB손해 2025.4Q 재추출 — OCR 시점에 놓친 이미지 PDF

owner: "KB손보 2025.4q가 OCR 시점이라 못읽은거야. 그건 니가 다시 뽑아라."

텍스트가 거의 없는 PDF 라 **마스터에 있는 값을 앵커로 페이지를 특정**하고 200dpi 로 렌더해 눈으로
읽었다. 3중 검산 — 마스터 base 값 대조 · **RS1 항등식(비율 = 금액 ÷ 기준금액 × 100)을 5개 열 전부**
· 본문 서술 수치. `kics_rate_sensitivity.json` 522 -> 528행, gold 60 -> 66행.
지급여력금액 base 121,909 / -100bp 124,161 / +100bp 117,639 -> 듀레이션 2.6749 · 컨벡서티 -165.53.

### 3. IFRS17 Panel 5 모바일 (`IFRS17.html`)

owner: "모바일에서 글씨도 너무 겹치고 축도 너무 커져서 당기순이익까지의 mvmt가 거의 안보여."

**원인은 자릿수다.** OCI 항목이 당기순이익보다 크다(삼성생명 실측: 당기순이익 1.70조 vs
FVOCI 지분증권 +25.32조). 좁은 화면에서 y축이 그쪽으로 끌려가 순이익까지의 브리지가 눌린다.

- **모바일 차트는 당기순이익에서 끊는다.** 새 분기를 만들지 않고 `plBuildSteps` 의 기존
  `mode === "none"` 경로를 탄다. **표는 전 구간 유지**(지시가 "그래프에서는" 이었다).
  잘렸다는 사실은 표 위에 명시한다 — 조용히 감추지 않는다.
- **표 첫 열을 sticky 로 고정.** 가로 스크롤에서 항목명이 사라져 숫자만 남던 것이 "못생김"의
  실체였다. 열을 지우지 않고 붙잡는 쪽을 택했다(줄이면 데이터가 사라진다).
- **OCI 세부를 `기타포괄손익` 한 줄 + `＋` 토글로 묶었다**(owner 후속 지시). 표에 이미 있는
  `d.sub` 장치를 그대로 쓴다. 그룹 합계는 **회사가 공시한 항목25** 를 쓰고(24+25=31 이 전 36사
  오차 0), 25가 없는 분기에서만 세부 합으로 대체하며 그 사실을 title 에 남긴다 — 합을 지어내지 않는다.

> 커밋 `aabad45` 는 이 작업의 **자기정정**이다. `mode === "none"` 을 "이미 배포돼 돌고 있는 경로"
> 라고 썼는데 실측하니 full 273 / none 74 이고 2026.2Q 는 24사 전부 full 이었다. 문구를 정정했다.

### 4. index 모바일 CSM 목록 (`index.html`)

owner: "정렬을 기말csm 기준 역순으로 하고 bar 크기는 신계약csm 배수로 해라 (현재는 배수가 숫자에만
들어가있고 bar 크기는 무관)." 막대 폭을 배수 최대값 대비 비율로 바꿨다.

### 5. K-ICS 트리맵 색 스케일 (`index.html`, `98337f6`)

owner: "초록색 중에서도 진한놈~연한놈이 거의 구별이 안가던데."

**원인이 둘이었다(2026.1Q 39사 실측).**

| | 종전 | 고친 뒤 |
|---|---|---|
| 램프 끝점 (지급여력비율) | 200% — **데이터 한복판** | 300% (p90) |
| 램프 끝점 (기본자본비율) | 100% | 250% (p90) |
| 초록 구간 밝기 폭 | 10.5 포인트 (L 39.75->29.25%) | **38 포인트** (62->24%) |
| 칸 글자색 | body 상속 고정 | 배경 휘도에서 **계산** |

① 지급여력비율은 **38/39사가 130% 이상**이고 분포가 min 146 · 중앙 207 · p75 241 · p90 293 ·
max 395 다. 끝점이 200% 라 **190% 이상 23개사가 전부 같은 최진한 초록**으로 뭉쳤다 — 지도의
3분의 2가 한 색이었다. ② 그 위에 밝기 폭 자체가 좁았다.

고치면서 **칸 글자색이 배경과 무관하게 고정**이라 진한 칸에서 묻히던 것도 잡았다. sRGB 상대휘도를
계산해 흰/검 중 대비가 큰 쪽을 고른다(임계값을 눈으로 찍지 않는다). 회색 잉크 `#14181c` 로는
**어느 쪽도 4.5:1 을 못 넘는 밝기 구간**이 생겨(메리츠 4.29 · 카카오페이 4.23) 순검정으로 갔다.
색 정의는 `_ratioHsl` 하나로 모았다 — 배경색과 글자색이 같은 L 을 봐야 한다.

**검증 (39사 전수, `scripts/_probes/probe_20260830_ratio_color_scale.js`)**

| | 지급여력비율 | 기본자본비율 |
|---|---|---|
| 서로 다른 밝기 값 | 14 -> **33개** | 21 -> **36개** |
| 같은 색으로 뭉치던 회사 | 23 -> **3사** | 19 -> **4사** |
| 최소 글자 대비 (AA 4.5:1) | **4.59:1 통과** | **4.65:1 통과** |

### 6. 되돌린 것 — 업권색 (푸시 전 철회)

owner 가 "index kics 세션에도 생보는 파란색으로" 했다가 **직접 철회**했다("니말이 맞네 현행유지해").
커밋 `cf433bb` 가 아직 push 전이라 `git reset --hard HEAD~1` 로 깨끗이 지웠다 — revert 노이즈 없음.
잔존 0 확인(`KICS_SECTOR_COLOR` · `legend-mob-only`).

### 배포 검증

`98337f6` 를 격리 detached 워크트리로 main 에 cherry-push 했고(같은 폴더 `checkout main` 금지),
라이브 `www.insurequant.com/index.html` 을 받아 **워킹트리와 sha256 바이트 동일**함을 확인했다
(`6ee6b93c1de80e81`, 76,916 바이트). 옛 코드(`STRONG = 200`) 잔존 0.

> **이 환경에서 preview 서버가 안 뜬다**(연결 거부 — 알려진 문제). 그래서 화면 대신
> **인라인 `<script>` 를 뽑아 `node --check`** 하고, 순수 함수를 임시 모듈로 꺼내 **실마스터 데이터로
> 실행**해 검증했다. 색 자체는 눈으로 확인하지 못했다 — owner 확인 필요.

---

## 2026-08-30b — Panel 5 표 토글 재검증: 요청 기능이 이미 있었다 (inbox 20260830T0900Z, 코드 수정 0)

orchestrator 티켓 1건("`＋` 가 코리안리 네 축 중 생명 수재만 편다"). **실측으로 반증 — 네 축 전부
이미 펴진다.** `IFRS17.html` 을 포함해 **코드는 한 줄도 안 고쳤다.**

### 무엇을 확인했나

| 확인 | 방법 | 결과 |
|---|---|---|
| 워킹트리 화면 | Playwright Chromium 1440x900 + 375, KR1000 | `＋` 4개(생명 수재/출재 · 장기 수재/출재), 전부 펼침 = 세부 16행 |
| 브랜치 vs main | `git diff main HEAD -- IFRS17.html` | 차이 **0** (main `c01f6d1` == HEAD `70b07da`) |
| main vs 라이브 | `curl https://www.insurequant.com/IFRS17.html` 후 diff | 줄바꿈(CRLF/LF) 외 **내용 동일** |

`2c87d18` 의 `plLobHead()` 가 이미 `["", ...sufs]` x `[수재, 출재]` 루프에서 다리마다 `sub:` 를
붙이고 있었다(L782-795). 관측 어긋남의 원인은 **배포 직전 화면 또는 브라우저 캐시**로 본다 —
라이브 반영이 `f97aa9e`(08-29 22:39 KST)·`c01f6d1`(22:55)로 티켓 작성 직전이었다.

> **교훈:** 티켓의 "현재 상태" 진술도 전제일 뿐이다. 고치기 전에 **그 증상이 지금 재현되는지**를
> 먼저 실측한다. 안 그러면 이미 맞는 코드를 "고쳐서" 회귀를 만든다.

### 검산 (읽기 전용)

- **네 다리 세부합 == 소계: 112/112 조합 정확히 0.** 4다리 x 14분기(2023.1Q~2026.2Q) x 2필드
  (`값`/`값_당분기`), 최대 |잔차| **0 백만원**(반올림 0.00 이 아니라 정확히 0), 결측 셀 0.
  화면 잔차가 최대 1억으로 보이는 것은 **셀별 억 단위 반올림**이지 데이터가 아니다.
- **39사 전수 census**: 토글 1개 = 31사(`생명장기재보험`) · 0개 = 4사(KR0004·KR0080·KR0150·KR1010,
  세부 미공시) · **4개 = KR1000 하나** · PL표 없음 3사. 대조군 삼성화재·삼성생명 캡션까지 무변화.
  전사 x 2모드 순회 **JS 에러 0**, 모바일 375 도 토글 4개 정상.

### 라벨 판단 (티켓 §2) — 짧은 형 유지

`PL_LOB_SUB = ["CSM상각","위험조정","예실차","기타"]` 그대로 둔다. 마스터 항목명(`원수 CSM상각`)을
그대로 쓰면 **이 회사는 원수 = 수재**라 부모 `생명 수재` 와 충돌하고, 장기 다리는 `수재 CSM상각` 이라
같은 말이 두 번 나온다. `위험조정` 을 마스터대로 `위험조정 변동` 으로 늘리는 것도 검토했으나
형제(`CSM상각`·`예실차`)가 전부 유량이라 문맥으로 읽히고, 나머지 38개사가 쓰는 짧은 형과
표기를 갈라 놓을 이유가 없어 안 늘렸다. **마스터 항목명·`ITEM_NAMES` 무수정.**

증거: `data/_derived/panel5_toggle_verify_20260830/` (미추적 — 스크린샷 12장 + `report.json`).

> **검증 팁 보강:** `.panel.revealed` 는 CSS **애니메이션**으로 opacity 를 올린다. Browser pane 은
> 애니메이션을 안 돌려서 `revealed` 클래스를 붙여도 opacity 가 0 으로 남아 **스크린샷이 백지**가 된다.
> `will-reveal` 클래스를 **제거**하거나(권장) 인라인 `opacity:1` 을 줘야 한다. 이번엔 아예
> Playwright(실 Chromium)로 돌려 compositing 문제 자체를 피했다 —
> `C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe` 에 playwright 가 들어 있다.

---

## 2026-08-30 — 코리안리 Panel 5 를 `LOB x 수재/출재` 로 (inbox 20260830T0600Z)

orchestrator 티켓(owner 지시) 1건. **미배포**(작업 브랜치 커밋만). 수정 파일 = `IFRS17.html` 하나.

### 무엇이 틀려 있었나

`PL_STEP_DEFS` 는 39사 공통 슬롯(item2 생명장기 · 13 자동차 · 14 일반)만 알고 있었고,
**하이픈 항목(`2-1`·`3-1`·`8-1`)을 몰랐다.** 코리안리재보험(KR1000)은 파서가 장기보험 블록을
그 하이픈 슬롯에 병렬로 실어 보내는데, 화면이 그걸 안 그려서 브리지가 앵커와 안 닫혔다.

```
그려지던 합  △178.8 + 224.9 + 1,891.7 △465.7 = 1,472.1
앵커(보험손익)                                = 1,615.2
차이                                          =   143.0  <- item2-1 전액 = 장기 leg
```

라벨도 어긋나 있었다 — 화면의 "원수 CSM상각/위험조정/예실차"는 이 회사에선 **생명 수재** 세부고,
"생명장기재보험"(item8)은 **생명 출재**다(전업 재보험사라 스키마의 "원수"가 곧 수재).

### 어떻게 고쳤나 — 회사코드가 아니라 하이픈 존재로 분기한다

| 새 함수/상수 | 역할 |
|---|---|
| `plLobSuffixes(name, per)` | 그 (회사,기간)의 `3-N`/`8-N` 중 **값이 있는** 접미사 목록. 없으면 `[]` |
| `plLobHead(name, per)` | 접미사가 있으면 `LOB x 수재/출재` head 를 만든다. 없으면 `null` |
| `plStepDefsFor(name, per)` | `head ? head.concat(PL_TAIL) : PL_STEP_DEFS` — 차트·표가 같이 쓴다 |
| `PL_HEAD_DEFAULT` / `PL_TAIL` | 기존 `PL_STEP_DEFS` 를 보험손익 앵커 앞뒤로 쪼갠 것. 뒤는 전 회사 공통 |
| `PL_LOB_DISPLAY` | **화면 라벨만** 담는 회사코드 맵. `{KR1000:{"":"생명","1":"장기"}}` |
| `plItemName()` | 라벨 폴백용 마스터 항목명 조회(값 계산엔 안 쓴다). `ix.plx` 에 `nm` 필드 추가 |

**회사코드로 레이아웃을 고르지 않은 이유 넷:**
1. **정합성이 근거다.** 하이픈 블록 값은 이미 item1 에 들어가 있어 안 그리면 산수가 틀린다.
   판단 근거가 "이 회사가 재보험사인가"(취향)가 아니라 "이 기간 그 슬롯에 값이 있는가"(사실)가 된다.
   회사코드 분기는 새 회사가 들어올 때 **조용히 틀린 채로 통과**한다.
2. **확장이 공짜다.** `-2`·`-3` 이 들어와도 루프가 접미사 전부를 돌아 축이 늘어난다.
3. **기간 단위다.** 어떤 분기에만 하이픈이 있으면 그 분기만 LOB 로 뜬다.
4. **회귀가 구조적으로 0이다.** 하이픈이 없으면 새 배열을 만들지 않고 기존 `PL_STEP_DEFS` **객체를
   그대로** 반환한다. 실측: PL_breakdown 36사 중 하이픈 보유 = KR1000 하나.

**마스터 항목명은 안 건드렸다.** `ITEM_NAMES` 는 스키마 전역이고 검증 룰이 이름으로 매칭한다
(`_resolved/20260830T0200Z` 에서 라벨 변경이 반려된 이유). 회사코드가 남는 곳은 화면 라벨 한 곳뿐이고,
매핑이 없는 회사는 **소계 항목명에서 꼬리 `손익` 만 떼는 폴백**(`장기재보험 손익` → `장기재보험 수재`)
으로 뜬다 — 매핑이 없어도 브리지는 닫히고 라벨은 마스터가 준 이름 그대로다.

### 자동차는 안 그린다 (미해당 ≠ 결측)

item13 이 KR1000 14분기 전부 null 이라 `optional` 규칙에 걸려 **막대도, 회색 "미공시" 막대도** 안 나온다.
게이트가 `lob_na` 축으로 그렇게 등재했다(`b2f9660`, `docs/domains/claude-agent-ifrs17.md` §4.1b).
LOB head 에서는 자동차·일반을 **묶지 않고 각각 optional** 로 뒀다 — 묶으면 `자동차+일반` 이라는
틀린 라벨로 일반손익만 그리게 된다.

### 세부 4개는 차트에서 접고 표에서 토글로

owner 요구는 `LOB x 수재/출재` 6막대다. 세부를 막대로 두면 요청한 축이 흐려지고, 하이픈 블록도
세부가 4개씩(4-1~7-1 수재 · 9-1~12-1 출재) 있어 일관되게 하면 **16막대**가 된다.
→ 차트 6막대 + 표에서 네 다리 각각 ＋ 토글(`　└ CSM상각 / 위험조정 / 예실차 / 기타`).
부모 행이 `생명 수재` 라 하위라는 게 라벨로 읽힌다. 토글은 `d.sub` 로 일반화했고
기본형(생명장기재보험 1개)은 DOM·title·각주가 종전과 동일하다.

### 실측

**코리안리 14분기 x 2모드 전부 닫힌다.** 최대 |잔차| **0.03억**(3백만원, 반올림).
`—` 5칸(2023.1Q/2Q 양쪽 필드, 2023.3Q 당분기)은 item1 자체가 마스터에 없는 기존 결측이라
잔차 정의 불가 — 화면엔 앵커가 회색 "미공시" 막대로 뜬다.

**회귀 대조는 `git show HEAD:IFRS17.html` 로 만든 수정 전 페이지와 1:1로 했다** (같은 오리진·같은
마스터 JSON, 하네스 동일).

| 축 | 범위 | 결과 |
|---|---|---|
| `plBuildSteps` 스텝명+값 | 36사 x 전 분기 x 2필드 = 712 | diff 28건 **전부 KR1000**, 비KR1000 **0** |
| `renderPlTable` 라벨 / 토글 | 36사 x 2모드 = 72 | 비KR1000 **0 / 0** |
| `renderPlTable` 셀값 | 72 | 비KR1000 43셀, **전부 `"-0" → "0"`** |
| `renderPlTable` 각주 | 72 | 비KR1000 17건, 전부 "토글 없는데 토글 안내" 제거 |
| 전사 renderCompany 순회 | 39사 x 2모드 | **JS 에러 0** |

브라우저 실물(1440x900): KR1000 연·당분기 18막대, 브리지가 앵커에 착지(zrender 실측 흐름 끝 y=179px
/ 앵커 상단 y=180px), 당분기는 앵커가 0선 아래(△162억)인데 zero-crossing·클리핑 톱니 정상.
대조군 삼성화재·삼성생명은 캡션 문자열까지 종전과 동일. `pytest tests/test_deploy_assets.py` 10/10.

> 검증 팁(재사용): Browser pane 이 compositing 을 안 해 rAF 가 멈추고 **모든 막대 높이가 0으로
> 읽힌다** — `echarts.init` 을 감싸 `animation:false` 를 주입해야 최종 geometry 가 나온다.
> 스크린샷도 `computer screenshot` 대신 `getDataURL()` 을 같은 오리진(`connect-src 'self'`)으로
> POST 해 PNG 로 떨궜다. 회귀 대조는 **HEAD 사본 + 워킹트리 사본 두 페이지를 같은 오리진에 띄우고
> `localStorage` 로 스냅샷을 넘기면** 왕복 한 번에 끝난다.

### 부수 2건

- **[수정] `samo()` 가 `-0` 출력.** `Math.round(-0.4)===-0` 이고 `-0<0` 이 false 라 △ 분기를 안 타서
  `toLocaleString` 이 `"-0"` 을 뱉었다(△ 표기 LOCKED 룰 위반). 코리안리 `장기 출재 → 위험조정
  2026.2Q` 에서 실제로 노출돼 발견. `Math.round(n)||0`. 전수 영향 43셀뿐.
- **[미수정] Panel 5 y축 눈금 라벨 중복.** 눈금 0.05조 + 포매터 `maximumFractionDigits:1` → `0.1` 이
  두 번. **기존 이슈**(전수 213건 중 197건이 비KR1000)라 고치면 이 티켓의 "다른 회사 회귀 0" 기준과
  충돌 → 손대지 않고 티켓 §6 에 기록. `plYStep` 에서 소수자리를 도출하는 한 줄 수정이다.

---

## 2026-08-29 — Panel 5 에 항목32 + 다운로드 팝업 신규 3시트 (inbox 20260829T0700Z, `0350570`)

orchestrator 티켓 2건. **미배포**(작업 브랜치 `fix/csm-product-segmented-columns` 커밋만).

### Panel 5 — 항목32 를 워터폴에 넣고 이름 충돌을 정리했다

parser 가 `PL_breakdown.json` 에 항목32(356셀, 커밋 `d634492`)를 신설하면서 항등식이
`25 = 26+27+28+29+30+32` 로 닫히게 됐다. 스텝 순서는 티켓대로
`… 당기순이익 → 26 → 27 → 28 → 30 → 32 → 29 → 31`.

**이 작업의 핵심은 이름 충돌이었다.** 항목32 의 마스터 항목명이 `기타 포괄손익(미분류)` 인데
2026-08-28d 에 내가 넣은 잔차 막대 이름도 똑같이 `기타 포괄손익(미분류)` 이었다. 게다가 항목32 는
내용이 규명돼 있어("미분류"가 사실과 다르다) provenance 실측으로 24개사·14종 계정 —
확정급여 재측정 23사·신용손실 15사·자산재평가 14사·해외사업환산 6사·지분법 OCI 6사.

| 대상 | 전 | 후 |
|---|---|---|
| 항목32 화면 라벨 | `기타 포괄손익(미분류)` | `기타 포괄손익` |
| 잔차 막대 | `기타 포괄손익(미분류)` | `미분류 잔차` |
| 캡션 구간 머리표 | `[기타포괄손익]` | `[기타포괄손익 구간]` |

셋째 줄은 스펙 밖 판단이다. 머리표를 그대로 두면 캡션 한 줄에 `[기타포괄손익]` 과
`기타 포괄손익` 이 나란히 나와 **새 라벨이 오히려 더 헷갈렸다.** 캡션 문자열만 바뀐다.

**마스터 항목명은 안 건드렸다**(parser 소관). 화면 라벨 매핑이라는 사실과 그 근거를
`PL_NUM` 선언부 주석에 남겼다 — 다음 세션이 마스터를 grep 해서 라벨을 못 찾고 헤매지 않도록.

**잔차 막대는 없애지 않았다.** `plOciResidual` 과 `plOciMode` 가 **각자** 세부 항목번호 배열을
들고 있었는데(복붙 2벌), 여기에 32 를 한쪽만 넣으면 모드 판정과 잔차 계산이 어긋나 브리지가
끊긴다 → `PL_OCI_PARTS` 하나로 합치고 거기에 넣었다. 임계(총액의 1% 이상 & 최소 1억)와
"0이면 안 그린다"는 그대로다. **효과: 연도(YTD) 기준 잔차 막대가 102개 회사-분기 → 0건.**
남는 잔차는 이제 진짜 원천 불일치라 의미가 더 분명하다(당분기 4건 — 교보생명 2025.2Q·3Q,
KB라이프 2024.4Q, 푸본현대 2023.4Q).

툴팁에 항목32 구성 한 줄을 붙였다. `data/_derived/pl_oci_item32_provenance.json` 은 계정 빈도
census 를 뽑는 데만 쓰고 **배포물에 인라인하지 않았다**(데이터 인라인 금지 룰).

**초기 표시창은 막대가 하나 늘어도 안 깨진다** — 항목32 가 당기순이익 **뒤**에 붙어서 당기순이익의
인덱스가 밀리지 않고, `_minEnd` 강제(오른쪽 끝은 최소 당기순이익)가 그대로 작동한다.
실측: 760px → dataZoom `0~10`(index 10 = 당기순이익), 375px 삼성화재(19막대) → `0~11`(index 11 = 당기순이익).

### 다운로드 팝업 — 신규 3시트

`기본자본소진율`(390행) · `보완자본소진율`(546행) · `자본비율전망`(2,090행).

**티켓이 말한 `public_exports/*.csv` 는 이제 없다** — 2026-08-28c 에 owner 지시("csv 여러개 zip
말고 xlsx로")로 CSV+JSZip 을 폐기하고 `public_exports/*.json` 스냅샷 → SheetJS 로 xlsx 1개를
만드는 구조가 됐다. 그래서 `download-survey.js` `SHEETS` + `scripts/export_public_sheets.py`
`MASTERS` 두 곳에 추가했다.

**이 3개는 다른 8개와 달리 디스크에서 long-format 이 아니다**(회사별 스냅샷 / 연도 전망 중첩).
`build_master_xlsx.py` 가 `FLATTEN` 으로 펴는데, 그 플래트너에는 셀별 `비고`(known limitation)
문구가 ~150줄 붙어 있다. **베껴 쓰지 않고 import 했다** — 베끼면 공식 xlsx 와 조용히 드리프트하고,
드리프트하는 대상이 하필 "이 숫자를 믿지 말라"는 경고문이다. 그 파일의 `main()` 은 `__main__`
가드라 import 부작용이 없다.

**비고 열이 파일까지 실리는지 4층 전부 실측했다** — 어느 한 층에서만 떨어져도 조용히 사라진다.

1. 스냅샷 JSON: 3시트 전부 7열, **`비고` 키가 빠진 행 0개**(빈 문자열이어도 키는 있다 — SheetJS
   `json_to_sheet` 는 전 행의 키 합집합으로 헤더를 만들기 때문에 이게 중요하다).
2. `coerceRow`: `for (var k in row)` 로 전 키 복사, `NUMERIC_COLS` 에 `비고` 가 없어 텍스트 유지.
3. xlsx: 브라우저에서 `XLSX.writeFile` 을 가로채 실제 워크북을 붙잡아 헤더 7번째가 `비고`,
   행수 390/546/2,090 확인. (설문 POST 는 owner 수집기에 실데이터가 가지 않도록 stub 처리.)
4. CSV: 같은 시트에 `sheet_to_csv` — 헤더 마지막 칸 `비고`, 쉼표 든 문구는 따옴표로 정상 이스케이프.

내용까지 대조했다: tier1 소진율 100% 초과 13행이 전부 "100%초과는 파싱오류 아님" 문구를,
tier2 구 산식 39행이 폐기 문구를, forward 1,870행이 콜일자 추정·신뢰도 문구를 달고 있다.

**부수: `report-widget.js` 오류제보 시트 목록에도 같은 3개를 추가했다.** 안 하면 "다운로드는
되는데 그 시트 오류는 신고할 방법이 없는" 상태가 이번 변경으로 새로 생긴다. 두 목록은 앞으로도
같이 움직여야 한다(양쪽에 주석으로 명시).

**부수: 익스포터를 돌리면서 `17BS`·`손익분해PL`·`manifest` 스냅샷도 갱신됐다.** 설계상
`git show HEAD:` 로 최신 **커밋** 상태만 읽으므로 다른 세션의 미커밋분은 안 섞였다. 실차이 =
PL 11,190→11,546행(item32 356 + ABL 예실차 수정), 17BS 삼성생명 64셀 값 갱신 — 둘 다 이미
커밋된 parser 산출물이다.

### 검증

39사 × 연/분기 2모드 전수 순회 **JS 에러 0**. 항목32 막대 24사 노출(Python census 와 일치),
잔차 막대 0건. 삼성생명 2026.2Q 1440px 에서 클리핑 유지(`+78.70조 ↑`·`82.40조 ↑` 둘만 톱니,
앞 16막대 정상 스케일). 표 검산 — 삼성화재 2026.2Q 당기순이익 12,534 + OCI 합 142,933 =
총포괄손익 155,467억. `pytest tests/test_deploy_assets.py` 10/10.

과거 분기 검증은 `PL_breakdown.json` 을 그 분기까지 잘라 서빙하는 로컬 서버로 했다(기존 방법).
Browser pane 미compositing 은 여전 — `echarts.init` 을 감싸 `animation:false` 를 주입해야
막대 높이가 0 이 아닌 실제 geometry 로 읽힌다. 스크린샷은 `getDataURL()` 을 같은 오리진으로
POST 해 PNG 로 확보(4장).

---

## 2026-08-28e — IFRS17.html `wfx`·`eqx` 의 `Number(null)→0` 제거 (inbox 20260828T0700Z)

orchestrator 티켓. Panel 5 작업(`eec64e0`)에서 `plx` 에만 적용했던 `_num()` 을 나머지 두 인덱스
빌더에도 적용했다. `Number(null)===0` 이고 `Number.isFinite(0)===true` 라 미공시 셀이 정상 0으로
통과하던 버그다.

**`_num()` 선언 위치를 세 빌더 위로 올렸다.** `plx` 자리(L1848)에 그대로 두고 `wfx`(L1838)에서
호출하면 TDZ 라 `ReferenceError` 로 부팅이 죽는다. 주석도 세 마스터의 실측 null/진짜0 개수를
담도록 다시 썼다.

**화면 변화는 2셀뿐 — 그 이유가 중요하다.** `CSM_waterfall.json` 의 null 은 `값` 2셀 ·
`값_당분기` **200셀**인데, 200셀은 전부 **연1회 공시 14사**의 것이고 그 14사는
[IFRS17.html:1511](../IFRS17.html) 의 D5 게이트가 분기 모드에서 Panel 2 를 아예 안 그린다.
게이트를 무시하고 세면 176막대·202셀이 되지만 사용자는 볼 수 없는 숫자다. 실제로 바뀌는 건
**KR0050 하나손해보험 `이자 부리` · KR0076 아이엠라이프생명보험 `상각`, 둘 다 연도 모드 2023 열** —
표 `0`→`—`, 차트 초록 `+0.00` 막대 → 회색 "미공시" 막대. 진짜 0으로 남는 셀은 표 기준 5셀.

**Panel 1(BS)은 변화 0.** `IFRS17_BS.json` 에는 null `값` 이 한 셀도 없다(진짜 0은 135셀). 39사 ×
2모드 스냅샷(T자 총계·flexGrow 비율·펼친 세부/준비금 행·시계열 표 전문)을 수정 전후로 떠서 대조했더니
해시까지 완전히 동일했다(`1547995505`, 87,416자). `eqx` 수정은 잠재버그 가드 — 파서가 BS 에 null 을
내보내는 순간 발화한다.

**`missing` 회색 시리즈는 사문화돼 있었다.** 수정 전 39사 × 2모드 전수에서 회색 막대 **0개** →
수정 후 **2개**. zrender 표시목록으로 실물 확인(`fill rgba(173,181,189,0.55)`, 높이 3px = hint 36.2억 /
y축 span 2,795억). **그려지긴 하나 이 스케일에서는 아주 얇아** 실질 신호는 표의 `—` 쪽이 강하다 —
hint 공식(`max(|running|×0.02, 30)`)은 기존 코드라 이번엔 안 건드렸다.

**합계·항등식은 안 깨진다.** 결측을 0으로 더하나 건너뛰나 산술이 같고 회색 hint 막대는 `running` 을
움직이지 않아 브리지 누적합이 불변(KR0050 2,802 = 기말 2,802 / KR0076 7,620.2 vs 7,620.3 반올림).
y축 범위 변화 0건. 데이터 항등식(기초+2+3+4+5=기말)은 연누계 360건 전부 성립, 당분기는
**수정 전 46건 깨짐 → 수정 후 7건**이고 새로 깨진 건 0건 — 사라진 39건이 null 을 0으로 더하던 허위였다.
남은 7건(KR0001·KR0003·KR0008·KR0068·KR0073·KR0099·KR0104)은 수정 전에도 깨져 있던 기존 데이터
문제라 손대지 않았다.

**상류 발주.** 바뀌는 2셀은 0을 넣으면 항등식이 정확히 닫힌다(오차 0.1/0.0). 진짜 0인지 파서가 행을
떨어뜨린 건지 원문 확인이 필요해 — 특히 CSM 7,571억 생보사의 연간 상각 0은 믿기 어렵다 —
`inbox/parser/20260828T0930Z__designer__MULTI_2023.4Q__csm_component_null_but_identity_closes.md`
로 넘겼다. 답이 오기 전까지 화면은 마스터에 충실하게 `미공시` 로 둔다.

**검증 환경 메모(재사용).** Browser pane 미compositing으로 `requestAnimationFrame` 이 멈춰 있어
ECharts 진입 애니메이션이 끝나지 않는다 — 표시목록을 그냥 읽으면 **모든 막대 높이가 0** 으로 나와
오진하기 쉽다. `echarts.init` 을 감싸 `animation:false` 를 주입하면 최종 geometry 가 나온다. 같은
이유로 KPI `countUp` 도 0에 얼어붙는데 `matchMedia('prefers-reduced-motion')` 을 강제하면 정상값이
나온다(KR0076 7,620억원). 스크린샷은 여전히 불가.

`pytest tests/test_deploy_assets.py` 10/10 · 39사 × 2모드 순회 JS·콘솔 에러 0 · 375px 가로 오버플로 없음.
배포·push 안 함.

---

## 2026-08-28d — IFRS17.html Panel 5: 당기순이익 워터폴 → 총포괄손익까지 연장 (inbox 20260828T0500Z)

orchestrator 티켓. parser 커밋 `4cca165` 로 `PL_breakdown.json` 에 항목 25~31(기타포괄손익 총액·
FVOCI 채무증권·보험계약금융손익(OCI)·위험회피 파생·FVOCI 지분증권·재보험금융손익(OCI)·총포괄손익)이
들어와서, Panel 5 브리지를 당기순이익에서 끊지 않고 총포괄손익까지 잇는 작업.

**스텝 순서.** `… 법인세 → [당기순이익] → 26 → 27 → 28 → 30 → (잔차) → 29 → [31]`. 24 의 role 을
`end`→`anchor` 로, 31 이 새 `end`. 항목 25 는 26~30 과 중복이라 막대로 안 그린다.
**29(FVOCI 지분증권)를 맨 끝에 두는 것이 Y축 설계의 전제** — 삼성생명은 순이익 1.70조(2025.4Q)인데
지분증권 평가손익이 +25.32조(2026.2Q 반기누계는 +78.70조, 삼성전자 지분)라 같은 축에 두면 나머지가
전부 눌린다. 맨 끝에 두면 잘리는 막대가 29·31 둘뿐이고 앞 열넷은 정상 스케일로 남는다.

**Y축 고정 + 캔버스 클리핑(axis break 아님).** `clip:true` 로 표시한 29·31 을 뺀 running total 범위 +
0선으로 `yAxis.min/max` 를 못박았다(Panel 2 `wfYMin/wfYMax` 와 같은 방식 — 자동 스케일이면 dataZoom 으로
밀 때마다 축이 다시 잡혀 "이어진 다리"라는 의미가 깨진다). 1/2/2.5/5 눈금으로 스냅해 경계 눈금 겹침
제거(`interval` 동시 지정). 넘치는 막대는 `params.coordSys` 의 그리드 사각형으로 클램프해 흰 톱니 마감 +
안쪽 값 라벨(`+25.32조 ↑`). **축 중간을 압축하는 axis break 는 쓰지 않았다** — 누적 브리지에서는 압축
구간 뒤 막대 위치가 전부 왜곡된다.

**초기 표시 창.** `el.clientWidth ÷ 56px`(x 라벨이 두 줄이라 56px 필요) 로 창을 잡되 오른쪽 끝은
`당기순이익` 인덱스 이상으로 강제 — PL 브리지가 첫 화면에서 끊기면 지금보다 나쁘다. `dataZoom` 슬라이더를
데스크톱에도 노출(`inside` 만으로는 오른쪽에 뭐가 더 있는지 알 수 없다). 리사이즈 리스너가
`charts.pl.resize()` 만 하므로 계산은 렌더당 1회 = 밀어둔 위치가 리셋되지 않는다. 슬라이더가 켜지면
`grid.bottom` 을 38→58 로(38 이면 두 줄 라벨 위에 슬라이더가 겹쳤다).

**결측 3유형** — `plOciMode(g)` 가 full/total/none 3갈래. ① 12개사 전 분기 결측 = `none` → OCI 구간을
막대·표에서 통째로 빼고 24 가 종착, 캡션에 "총포괄손익 미공시(원천 보고서에 총포괄손익계산서 미수록)".
② 23개사 2023.1Q·2Q = 회사가 아니라 그 분기 데이터로 판정하므로 같은 경로로 자동 처리.
③ 세부만 결측 = 26~30 이 **전부** 없으면 `total` → `기타포괄손익(세부 미공시)` 1막대(=25)로 24→31 을
잇고, **일부만** 없으면 그 항목만 회색 "미공시" 막대(높이는 값이 아니라 픽셀 고정 — 없는 값에 크기를
부여하지 않는다). 표는 창 제한 없이 OCI 행을 처음부터 전부 노출한다(차트에만 창을 건다).

**스펙에서 벗어난 판단 2건 (티켓 `## 답변` 에 근거 기록, orchestrator 확인 대기).**
- **잔차 막대 신설.** 티켓의 "25 = 26~30 의 합" 전제가 실데이터에서는 근사일 뿐이다 — 최신분기 기준
  ABL생명 154,075백만(총액의 58%)·코리안리 75,903백만(63%)이 세부에 안 잡혀, 안 그리면 브리지가 31 에
  도달하지 못한 채 끊긴다. 25 는 여전히 안 그리되 `25 − Σ(26..30)` 이 총액의 1% 이상 & 1억 이상일 때만
  29 앞에 `기타 포괄손익(미분류)` 한 칸. (`24 + 25 = 31` 은 전 36사·전 분기 오차 0으로 성립.)
- **🔴 인덱스 빌더 null→0 버그 수정.** `ix.plx` 빌드가 `Number(r["값"])` 를 그냥 태우고 있었는데
  `Number(null)===0` · `Number.isFinite(0)===true` 라 **미공시 셀이 전부 진짜 0 으로 둔갑**하고 있었다
  (실측 `값` null 1,347셀 · `값_당분기` null 2,377셀, 그중 OCI 616셀. 진짜 0 도 836셀 있어 반드시 갈려야
  한다). 이걸 안 고치면 "결측을 조용히 0 으로 칠하지 마라"가 구현 불가능하다. 화면 변화 2가지 —
  표의 미공시 칸이 `0`→`—`, 그리고 **분기(당분기) 모드에서 연1회 공시사 12사가 전 항목 0짜리 가짜
  브리지 대신 기존 stub("해당 기간 당기순이익 데이터가 없습니다")로 바뀐다**(원문 확인: AIG·라이나·AIA·
  IBK연금·악사·예별·신한EZ 전부 latest 4Q 의 `값_당분기: None`. 3Q 누계가 없어 차분 불가). YTD 폴백 여부는
  표시정책이라 owner/orchestrator 판단 대기.

**같은 버그의 쌍둥이 — 안 고침(스코프 밖, 별도 티켓 요청).** `Number(null)→0` 패턴이 Panel 1/2 인덱스
(`ix.wfx`)와 Panel 1 BS 인덱스(`ix.eqx`)에도 그대로 있다. Panel 2 에는 이미 `missing` 회색 시리즈가
있는데 이 버그가 그걸 무력화하고 있을 가능성이 높다. 고치면 CSM 워터폴·BS T자 표시 숫자가 바뀌므로
별도 검증이 필요하다.

**실측.** (a) 삼성생명 2025.4Q/1440px — 지분증권 `+25.32조 ↑`·총포괄손익 `26.74조 ↑` 둘만 톱니로 잘리고
나머지 15개 정상 스케일(순이익 1.70조·채무증권 △3.88·보험금융 +4.94·위험회피 △1.28, 티켓 수치와 일치).
(b) 결측 3유형 각각 확인(AIG·라이나 / 메리츠 2023.2Q / 삼성화재 2025.3Q·ABL생명). (c) 뷰포트 760px
(차트 594px)·375px 둘 다 창이 index 0~10 = 당기순이익에서 정확히 끊기고 슬라이더 노출, 가로 오버플로우 0.
(d) 1440px(차트 1154px) 17막대 한 번에, dataZoom 없음. 전 39사 × 연/분기 2모드 전수 순회 **JS 에러 0**.
`pytest tests/test_deploy_assets.py` 10/10. 배포 안 함(push 없음).

> **검증 방법 메모.** UI 로는 최신 분기만 고를 수 있어 2025.4Q·2023.2Q·2025.3Q 는 `PL_breakdown.json` 을
> 그 분기까지 잘라 같은 경로로 서빙하는 로컬 서버로 실제 렌더시켜 확인했다. 또 이 환경의 Browser pane 이
> compositing 을 안 해 `computer{screenshot}` 이 계속 실패하는데(기존 이슈), ECharts `getDataURL()` 을
> 같은 오리진으로 POST 해 PNG 로 떨구는 방식으로 실제 렌더 이미지를 확보했다 — CSP `connect-src 'self'`
> 라 로컬 서버에 POST 라우트만 하나 붙이면 된다. 다음에 pane 이 또 안 뜨면 이 방법을 쓸 것.

---

## 2026-08-28b — 마스터 데이터 다운로드 설문게이트 + 4페이지 오류제보 팝업 (owner 채팅 발주)

owner: index.html에 마스터 xlsx 다운로드 버튼, 짧은 설문(소속·데이터목록·목적·disclaimer) 제출해야
다운로드 제공. 4페이지 공통 우하단 "오류 제보" 팝업(시트·회사·분기 중복선택+자유서술)도 owner
Gmail로 도착하게. "그 외 아이디어 있으면 제안해봐라"도 포함된 발주.

**전제 고지**: GitHub Pages는 서버 없는 정적 호스팅이라 이 게이트는 실접근제어가 아니라 소속을
정중히 묻는 매너 절차 — `public_exports/*.csv`는 URL을 아는 누구나 접근 가능함을 owner에게
명시.

**빌드**:
- `scripts/export_public_sheets.py` — 마스터 JSON 8개(`build_master_xlsx.py`의 MASTERS 리스트와
  동일 소스, "요약" 시트 제외)를 `public_exports/*.csv`로 값-스냅샷 export. 마스터 xlsx는 아예
  안 건드림(수식 캐시 wipe 리스크 원천 차단) — CSV는 JSON에서 직접 생성.
- `common.css`에 모달/폼 디자인시스템 신설(`.iq-modal-*`, `.iq-field`, `.iq-check-grid`,
  `.iq-report-fab` 등) — 기존 토큰(`--bg`/`--card`/`--border`/`--primary`/`--sp-*`/`--r-*`)만
  사용, 새 색상값 없음.
- `download-survey.js`(index.html 전용): 소속은 `<input list>` + `<datalist>` 네이티브
  타이핑검색(커스텀 콤보 없이 무료 접근성 확보) + 자유입력 허용. "기타(익명)"은 목록 밖 별도
  링크로 분리, 클릭 시 확인 단계 한 번 더(owner 요청 — 다른 항목보다 일부러 클릭 한 번 더 들게).
  데이터 목록은 체크박스 8개, 전체선택 버튼 없음(owner 요청). 2개 이상 선택 시 JSZip으로 묶어
  1개 파일 다운로드, 1개면 바로 다운로드. localStorage로 최초 방문자는 전체 설문, 재방문자는
  시트 선택만(소속 재확인 안 함) — 재방문 다운로드도 매번 collector에 기록(이메일은 안 감,
  다운로드 kind는 시트 기록만이라 비용 없음).
- `report-widget.js`(4페이지 공통, `<script data-sheet-hint="...">`로 페이지별 시트 사전선택):
  우하단 플로팅 버튼 → 시트(단일)·회사(다중, 39개사 체크그리드)·분기(다중, 13개 체크그리드)·
  오류사항(자유서술) 모달. 허니팟 필드로 봇 방어.

**중간 발견 — 백엔드 갈아탐**: 처음엔 Google Form에 커스텀 UI를 proxy-POST하는 방식으로
설계(무료·가입만 필요·no-cors라 성공/실패를 못 읽는 한계 있음)했는데, git status에서 낯선
파일(`scripts/appsscript/insurequant_collector.gs`)을 발견 — 같은 공유 워킹트리에서 **다른
세션이 동시에** 이 정확히 같은 기능을 위한 Apps Script Web App을 만들어두고 있었다(스키마무관
단일 엔드포인트, `kind`로 download/report 분기, report만 owner Gmail + 저장소 inbox 티켓
포맷으로 자동 변환해 발송, `LockService`로 동시제출 경합 처리 — Google Form 방식보다 명백히
우월). owner에게 확인받고 그쪽으로 전면 전환: `forms-config.js`를 Form 2개+entry ID 방식에서
단일 `/exec` URL 방식으로 재작성, `report-widget.js`의 payload 키를 그 스크립트의 `_notify()`가
찾는 `sheet`/`company`/`period` 이름에 맞춤.

**owner가 Apps Script 배포 후 `/exec` URL 제공 → 실배선**:
- `forms-config.js`의 `action`에 실제 URL 반영.
- **CSP 함정 실측**: `connect-src`에 `https://script.google.com`만 넣었더니 브라우저 콘솔에
  `script.googleusercontent.com`(Apps Script가 실행을 리다이렉트하는 실제 도메인) CSP violation —
  `curl`로는 리다이렉트 처리가 꼬여(411/405) 진단이 안 됐고, 실제 `fetch()`를 쓰는 브라우저에서
  콘솔 에러로 원인이 바로 잡혔다. `connect-src`에 `script.googleusercontent.com` 추가해 해결
  (4페이지 전부).
- **실전송 검증**: 브라우저에서 `IQ_FORMS.submit("download", ...)` / `submit("report", ...)`
  둘 다 실제 엔드포인트로 POST해 `{ok:true}` 확인 — report는 owner Gmail로 실제 테스트 메일 1통
  발송해 알림 경로까지 종단 확인(제목에 `[테스트]` 명시).
- 로컬(`python -m http.server`)과 라이브(`www.insurequant.com`) 양쪽에서 전체 UI 흐름(버튼→폼
  →제출→다운로드) 실클릭 재확인, 콘솔 에러 0.

**배포**: `validate_data_contract.py` RED=0 확인 → 격리 워크트리로 main cherry-push(2회 —
1차 UI+로컬 검증본, 2차 실배선본) → owner 승인("밀어붙여라") 하에 `git push origin main`
(→`c9e707d`) → 라이브 재검증(`public_exports/17BS.csv` 200, `forms-config.js`에 실 URL 반영
확인, 라이브에서 실제 POST `{ok:true}`).

**(참고, 스코프 밖이라 안 건드림)**: 같은 세션에서 K-ICS `하나생명`/`하나생명보험` 원수사명
오철자 정정 건도 처리·배포함 — 별도 항목이라 이 changelog 위 섹션(2026-08-28 표시명 정리)이
아니라 `docs/changelog_parser_kics.md`에 기록.

## 2026-08-28 — 회사명 표시 정리 4페이지 확장 + main 배포 (owner 채팅 발주)

owner가 라이브에서 "에이아이에이생명·에이비엘생명처럼 영문을 한글로 풀어쓴 회사명이 지저분하다,
원천데이터는 안 건드리고 화면 표시만 정리해달라"고 요청 — 무엇을 어떻게 바꿀지 먼저 조사해서
보고하라는 지시였다.

**조사**: `index.html`에 이미 `NAME_ABBR`(명시 매핑 17개) + `shortName()`(접미사 정규식 fallback:
`손해보험→손보`, `생명보험→생명`, `재보험→''`, `보증보험→보증` 등) 축약 로직이 있었다
([index.html:540](../index.html)). 그런데 이 로직이 `index.html`에만 있고, `K-ICS.html`은
`COMPANY_DISPLAY`로 AIA 한 곳만 부분 적용, `IFRS17.html`·`공시보고서.html`은 전혀 없어 드롭다운이
원수사명(`원수사명`/`row.company`) 그대로 노출되고 있었다. `kics_disclosure.json` ·
`CSM_waterfall.json` · `kics_tier1_utilization.json` 등에서 원보험사코드↔원수사명을 직접 뽑아
전사 39개사 로스터를 확인 — 실제로 "영문을 letter-by-letter로 풀어쓴" 클러터는 6곳
(에이비엘생명보험→ABL, 케이디비생명보험→KDB, 아이엠라이프생명보험→iM, 에이아이에이생명보험→AIA,
신한이지손해보험→EZ, 비엔피파리바카디프생명보험→BNP)이고 나머지는 접미사 정규식으로 자동 처리됨을
확인해 표로 보고. owner가 "BNP카디프생명"(BNP파리바카디프생명 대신 축약형)으로 확정하고 4개
페이지 전체 적용 + main push까지 지시.

**구현**: `index.html`의 `NAME_ABBR` 딕셔너리(BNP 값을 'BNP카디프생명'으로 갱신)를 그대로
`K-ICS.html`·`IFRS17.html`·`공시보고서.html`에 이식.
- `K-ICS.html`: `COMPANY_DISPLAY`(1개사)를 전체 `NAME_ABBR`+`shortName()`으로 교체하고 JS 폴백
  경로(`fillMissingCompanies`)도 `shortName(nm)` 사용. **정적 `<select id="company">` 마크업의
  `<option>` 28개**(L90-119)는 셀렉트 옵션이 JS로 생성되지 않고 하드코딩돼 있어, `value`(조회
  키)는 그대로 두고 라벨 텍스트만 손으로 28개 전부 교체.
- `IFRS17.html`: 신규로 `NAME_ABBR`+`shortName()` 삽입(codeToName 선언부 바로 뒤). **주의 —
  `coName`(= `codeToName[company]`)이 `keyColorOf(coName)`(브랜드 컬러 매핑, `KEY_COLORS`가
  원수사명 키)과 `PAA_ONLY.has(coName)`(신한이지손해보험 PAA 예외 Set) 두 곳의 **조회 키**로도
  쓰이고 있어서, `coName` 자체를 줄이면 색상 매칭·예외 처리가 깨진다. 드롭다운 옵션 텍스트 +
  차트 타이틀 2곳(NB CSM 배수, CSM 시계열) — **순수 표시 3곳만** `shortName(coName)`으로 감싸고
  로직에서 쓰는 `coName` 원본은 손대지 않음.
- `공시보고서.html`: 신규로 `NAME_ABBR`+`shortName()` 삽입, 드롭다운 옵션 텍스트 1곳만 감쌈(이
  페이지는 이 사이트 전체 중 유일하게 표시 지점이 하나뿐이었음).

**검증**: 로컬(`python -m http.server 8896`, `.claude/launch.json`의 "Dashboards (static)")에서
4페이지 전부 드롭다운 확인 — 라벨은 축약형, `value`는 원수사명/코드 그대로. K-ICS AIA 선택 시
시계열 표 정상 렌더(지급여력비율 234.8→192.9 등 실데이터), IFRS17 AIA 선택 시 BS·CSM 워터폴
정상 렌더, 공시보고서 ABL 선택 시 배당 KPI 정상 렌더 — 4페이지 모두 콘솔 에러 0.

**배포**: `python scripts/validate_data_contract.py` → `SUMMARY RED=0 YELLOW=92`(YELLOW는 전부
이 변경과 무관한 기존 PL/CSM 교차대조 건). 작업 브랜치(`fix/csm-product-segmented-columns`)에
4개 파일만 커밋(`ea51e7c`) → 격리 워크트리(`../insurequant-main-deploy`, main 기준) 생성 →
동일 4개 파일만 cherry-checkout → `72ab093` 커밋 → owner 승인(채팅 "push까지 진행시켜") 하에
`git push origin main`(5069966..72ab093) → 워크트리 정리, 작업 브랜치 복귀·`git status` 클린
확인. 배포 후 GitHub Pages 캐시 갱신 대기(~1-2분) 후 캐시버스트 쿼리로 4페이지 전부 라이브
재확인 — 새 축약명 노출·콘솔 에러 0.

**(참고, 스코프 밖이라 안 건드림)**: `KR0097`(하나생명)이 데이터소스마다 원수사명 철자가
"하나생명"/"하나생명보험" 둘로 갈려 있어 K-ICS 드롭다운에 같은 회사가 두 줄로 중복 표시됨 —
이번 표시명 정리와는 별개의 기존 데이터 정합성 이슈. 고치지 않고 기록만 남김.

## 2026-08-20 — 준비금이 AOCI 하위처럼 보이는 오독 수정 (inbox `20260819T0620Z` + `20260820T0033Z`)

세션 시작 시 inbox 드레인 — designer 앞으로 2건. `20260820T0033Z`는 orchestrator가 전 프로젝트
inbox를 훑은 상태 점검 리마인더(대부분 다른 스테이지 얘기)인데, designer 관련해서는 두 가지만:
① `20260819T0620Z`(아래) 처리 확인, ② CSM 상각 스케줄 경고 문구(2026-08-19 항목, 단위 정규화
미완료 경고) **아직 지우지 말 것** — parser 쪽 후속 답변이 아직 없어서. 문구는 그대로 뒀다.

**`20260819T0620Z` — 준비금이 AOCI 자식처럼 보임**: owner가 악사손해보험(KR0049, 어제 만든
시계열 표의 "분기 미제공 stub" 테스트 케이스로 썼던 바로 그 회사)을 조회하다 지적: "법정준비금들이
다 AOCI 밑으로 들어가있고 (not 이익잉여금)". 실측: KR0049의 `IFRS17_BS.json`은 항목 1·2·3(총계)·
4(AOCI)·5·6·7(준비금) 7셀뿐 — 항목31(이익잉여금)이 아예 없다. 어제 짠 `renderBsTable()`의
앵커-없음 폴백(`reAttached===false`일 때 준비금을 자본 세부 끝에 평행 배치)이 정확히 설계대로
동작한 결과인데, 그 섹션에 남은 L2 행이 AOCI 하나뿐이라 **분류 버그가 아니라 배치가 오해를 부르는
표시 문제**였다. T자 레이아웃(`renderReserveSubrows`, L1090 부근)에는 애초에 이 폴백 케이스에도
`title` 속성으로 "법정준비금 — 이익잉여금 내부 적립, 자본총계 합계에는 미포함"이 붙어 있었는데,
어제 만든 표 쪽 폴백에는 그 라벨을 안 옮겨놨었다.

- `buildBsTableDom()`에 `note:true` 행 타입 신설 — `colspan=headers.length`로 표 전체 폭을
  차지하는 라벨 전용 행(데이터 셀 없음, `.small-muted`+이탤릭+점선 상단 테두리). 토글/셀 렌더
  로직과 완전히 분리(처리 후 바로 `return`)해 기존 로직에 영향 없음.
  `renderBsTable()`의 `!reAttached` 분기에 이 note 행을 준비금 항목들 **바로 앞**에 삽입 —
  T자와 동일한 문구 재사용(신규 문구 발명 안 함). 준비금 항목이 하나도 없는 회사는 `reNames.length`
  가드로 이 행 자체가 안 붙음(빈 헤더만 뜨는 것 방지).
- 브라우저 실측(악사손해보험/KR0049, 연도모드): 행 순서 `자본 + → 기타포괄손익 누계액 → **법정
  준비금 — 이익잉여금 내부 적립, 자본총계 합계에는 미포함** → 해약환급금준비금 적립액 → 비상위험
  준비금 적립액 → 대손준비금 적립액`. note 행이 `colspan=2`(이 회사는 BS 연도 커버리지가 2025
  하나뿐이라 헤더가 `[구분,2025]`뿐 — `headers.length`에서 동적으로 따라감, 하드코딩 아님).
  `자본` 토글 켜기 전 `display:none`, 켠 후 `""`로 그룹 멤버십도 정상. 회귀 확인: 이익잉여금
  앵커가 있는 삼성화재(KR0008)는 이 note 행 자체가 안 붙고 기존처럼 "이익잉여금 +" 하위로만
  표시 — 폴백 케이스에만 국한됨. `IFRS17_BS.json`은 parser가 재빌드 중이라던 원 티켓 경고대로
  읽기만 함(마스터 무수정). 콘솔 에러 0, `pytest tests/test_deploy_assets.py` 10/10.

**환경 메모(추가)**: 이번 세션은 로컬 프리뷰 서버 재기동 후 `javascript_exec` 호출이 간헐적으로
"Browser pane is currently hidden" 타임아웃을 냈다 — 재확인해보니 대부분 **응답 전달만 실패했고
실제 실행은 끝나 있었다**(예: `company.value` 설정은 성공했는데 타임아웃으로 응답을 못 받은 채
다음 호출로 넘어감). 타임아웃이 뜨면 같은 mutation을 재시도하지 말고 **다음 호출에서 상태를 먼저
읽어 확인**할 것 — 중복 트리거로 상태가 꼬일 수 있다. 이 세션에서 한 번은 실제로 렌더가 중간에
끊긴 것으로 보이는 케이스(표가 완전히 빈 채로 남음)도 있었는데, `company` 값을 빈 값으로 리셋했다
재선택하니 정상 복구됐다 — 막히면 이 패턴(초기화 후 재선택) 권장.

---

## 2026-08-19 — IFRS17.html BS 패널에 시계열 원천 테이블 신설 (owner 채팅 발주)

owner: *"IFRS17.html에서 상단 BS부분 만든건 좋은데, 그 밑에 원천 테이블도 시계열로 넣어놓자.
시점구분 '분기'일땐 직전 5개분기(현재는 2025.2Q~2026.2Q), '연도'일땐 직전 4년(2023.4Q, 2024.4Q,
2025.4Q, 2026.2Q-최신) 기준으로... (분기공시 미제공사는 '분기공시 미제공이니까 연도로 돌려서
연도 기준으로 봐라'는 메세지 지금처럼 보여주면 됨)"* + 행 구조 목업(자산/부채/자본 각 `[+]`,
자본 세부의 "이익잉여금"만 자체 `[+]`로 법정준비금 4항목을 한 겹 더) + 각주 지정
("*법정준비금은 당분기 기적립액 + 적립(환입)예정금액 기준").

T자(Panel 1)는 최신 1개 분기의 스냅샷+드릴다운이라 여러 분기를 나란히 비교할 수 없었다 — 다른
패널(CSM 워터폴/PL)처럼 차트 밑에 다분기 표가 있는 패턴을 BS에도 붙이는 것.

**기간 윈도잉 — 새로 안 만들고 기존 걸 재사용**: `selectPeriods(periodsAsc, mode, hasQ, isMobile)`
가 이미 CSM Panel 2/PL 표에서 쓰는 범용 함수(분기=`hasQ`면 직전5, 아니면 연말(4Q)+최신부분)라
그대로 재사용 — `eqQuartersOf(company)`(BS 자체 커버리지, 이미 있었음)를 오름차순으로 넣기만
하면 owner 스펙과 정확히 일치한다. BS 자체의 "분기 공시 있음" 판정은 CSM의 `wfHasQuarterly`와
같은 패턴으로 `eqHasQuarterly` 신설(BS 커버리지가 CSM/PL과 다를 수 있어 공용 불가).

**행 구조 — owner 목업 그대로, 항목명 유니온 방식**:
- 자산/부채/자본 총계 행(굵게, 자체 `+` 토글) → 클릭 시 레벨2 세부 행(들여쓰기, 기본 숨김).
- 자본 세부 중 "이익잉여금"만 **자체 중첩 `+` 토글**(D-4의 T자 방식 — 항상 노출 — 과 다르게
  이번엔 목업에 명시된 대로 클릭해야 펼쳐짐)로 법정준비금(섹션="준비금") 항목을 한 겹 더
  들여쓴 행으로. 항목번호가 아니라 이름으로 앵커링(`renderReserveSubrows`와 동일한 원칙).
- 어떤 회사가 어떤 세부 항목을 갖는지는 분기마다 다를 수 있어(신규 상장·최초 공시 등), 선택된
  **모든 기간에 걸친 항목명 합집합**으로 행을 고정한 뒤 각 분기 열에서 그 이름으로 값을
  조회(`eqUnionNames`+`eqValByName` 신설) — 특정 분기에 항목이 없으면 그 셀만 "—", 행 자체가
  사라지지 않음(열이 흔들리지 않아야 시계열 비교가 됨).
- 이익잉여금 행 자체가 없는 회사(비상장 등)도 준비금 항목은 있을 수 있어 — T자 D-4의 앵커-없음
  폴백과 같은 원칙으로 자본 세부 끝에 평행 배치, 데이터 누락 없음.

**부수 발견(실측 중)**: `IFRS17_BS.json` 준비금 섹션에 **항목번호 8 "보증준비금 기적립액"이
새로 추가**돼 있었다 — 오늘 이른 세션의 D-4 작업 때는 5·6·7(3항목)뿐이었는데, 파서 쪽에서
그 사이 갱신한 것으로 보인다(도중에 데이터가 바뀐 것이지 이번 세션이 만든 변화 아님). 실측:
생보 16개사(한화생명·삼성생명·흥국생명 등, KR0068계열 전부)에만 존재, 손보 0사 — 업권
특성상 정상(보증준비금은 변액·연금보증형 등 생보 상품 개념). owner 목업의 "보증준비금
적립액"과 실제 항목명("보증준비금 기적립액")이 한 글자 다른데, 항목명을 하드코딩하지 않고
데이터 그대로 렌더링하는 설계라 이 차이가 문제되지 않는다(자동으로 실제 이름이 뜬다) —
데이터-드리븐 설계가 정확히 의도대로 작동한 사례.

**중첩 토글 상태 관리**: 상위 그룹(자본)을 접을 때 그 안에 있는 하위 토글(이익잉여금→준비금)도
재귀적으로 같이 접고 버튼 텍스트/aria-expanded를 리셋하는 `collapseGroup()`을 별도로 작성 —
안 그러면 "자본 펼침 → 이익잉여금 펼침 → 자본 접음 → 자본 다시 펼침" 순서에서 이익잉여금이
이미 펼쳐진 채로 재등장하는 상태 꼬임이 생긴다(실제로 최초 구현엔 이 버그가 있었고, 왕복
테스트로 발견해 즉시 수정).

**구현**: `eqHasQuarterly`/`eqUnionNames`/`eqValByName`(신규 헬퍼), `renderBsTable(host,company,
mode)`(행 정의 구성 — SECTIONS 루프 + 유니온 + 준비금 중첩/폴백), `buildBsTableDom(headers,
rowDefs)`(DOM 생성 + 토글 배선 + `collapseGroup` 재귀 리셋). HTML: `.bs-t` 바로 밑에
`.bs-table-section`(캡션 `#bsTableCap` + stub `#bsTableStub` + `.table-wrap#bsTable` + 고정
각주) 신설. `renderCompany()`에서 `renderBsSection` 직후 `renderBsTable(host, company, wfMode)`
호출 — 상단 기준 셀렉터 변경 시 `renderCompany` 전체가 재호출되므로 별도 리스너 불필요.

**검증**: 브라우저 실측 — 삼성화재(KR0008, 손보) 연도모드 헤더 `[2023,2024,2025,2026.2Q]`(owner
예시와 정확 일치), 준비금 3항목만(보증준비금 없음, 손보라 정상). 삼성생명(KR0069, 생보) 준비금
4항목 전부. 분기모드 헤더 `[2025.2Q,2025.3Q,2025.4Q,2026.1Q,2026.2Q]`(owner 예시와 정확 일치).
자본 토글→이익잉여금 중첩 토글 왕복 + 상위 접기 시 하위 재귀 리셋 확인. AIG손보(KR0029, 분기
BS 미제공)에서 분기모드 stub, 연도모드 정상 복귀. 카카오페이(KR1098, BS 데이터 전무)에서 T자
자체 stub만 뜨고 신규 표는 조용히 비어있음(메시지 중복 없음). 375px에서 `.table-wrap{overflow:
auto}`가 6열 표를 컨테이너 안에 가둬 `body.scrollWidth===innerWidth`(페이지 가로 스크롤 없음).
콘솔 에러 0(전 케이스). `pytest tests/test_deploy_assets.py` 10/10(구조 변경 없음 — 키프리스트·
인라인금지·BOM 영향 없음).

**참고(안 건드림)**: `EQ_SECTION_LEVEL_BRIDGE`(2026-08-14d 도입, 항목 1-7을 섹션/레벨 실컬럼
랜딩 전까지 임시 매핑하던 브릿지)가 지금은 전 행에 `섹션`/`레벨`이 채워져 있어(`r["섹션"] ||
(bridge ? ... : null)`의 `||` 왼쪽이 항상 참) 사실상 폴백 분기가 한 번도 안 걸리는 죽은 코드로
보인다. owner가 이번에 스코프를 준 적 없어 이번 세션은 안 건드리고 기록만 남김 — 다음에 이
파일을 만질 때 확인 후 정리 후보.

**추가 수정(같은 대화, owner 즉시 재확인) — 연도모드가 "직전 3년" 스펙을 어김**: owner가 라이브에서
바로 지적 — 메리츠화재(KR0001)를 연도모드로 보니 2021.4Q~2026.2Q(6열)가 다 붙어 있었다. 원인:
재사용한 `selectPeriods()`가 "존재하는 4Q 전부"를 반환하는 함수라(CSM Panel 2의 `wfYearBuckets()`
와 달리 상한이 없음), BS 데이터가 2021년까지 있는 일부 회사(실측: 메리츠화재·현대해상·KB손해보험·
코리안리재보험 — 전부 `.4Q` 8개 분기씩 2021-2022 보유, 그 외 라이나생명 등 6개사는 2022.4Q만
1개 더)에서 그대로 다 튀어나왔다. `selectPeriods`는 PL 표·CSM 시계열(Panel 3)·NB(Panel 6)와
공유 중이라 그 셋도 같이 확인 — `CSM_waterfall.json`/`PL_breakdown.json`/`NB_CSM_multiple.json`
전부 최소 연도가 2023이라(실측) 그 세 마스터에서는 이 결함이 지금 잠재적으로만 존재하고 실제로
발현하지 않음. 그래서 공유 함수는 안 건드리고 BS 전용 `eqYearPeriods(periodsAsc)`를 신설 —
`wfYearBuckets()`와 동일하게 최신 분기 + 직전 3개년말(4Q)로 하드 캡. 브라우저 재실측: 문제였던
4개사 전부 + 기존 정상이던 2개사 전부 `[2023,2024,2025,2026.2Q]`로 통일, 분기모드는 원래도
정상이라 무변경 확인, 콘솔 에러 0, `pytest` 10/10.

**환경 메모**: 이번 세션 로컬 프리뷰(`python -m http.server`, 포트 8896)가 파일을 갱신해도
브라우저가 이전 버전을 계속 서빙하는 캐시 문제를 겪었다(`curl`로 서버 자체는 최신 파일을
정확히 반환하는 걸 확인 — 브라우저 쪽 캐시). `navigate`의 `force:true`로도 안 풀렸고,
`?_cb=N` 캐시버스팅 쿼리 파라미터를 붙이자 즉시 해결됐다. 이후 파일 수정 후 재검증할 때마다
쿼리 파라미터를 새 값으로 바꿔가며 확인 — `preview_eval` 계열 세션 고질(`project_preview_eval_
flaky` 메모리 참고)의 또 다른 변종으로 보임, 다음 세션도 이 방법으로 우회 권장.

---

## 2026-08-18d — legend 토글 시 X축 재스케일 복원 (같은 대화 연속)

바로 아래 2026-08-18c를 배포하자마자 owner 채팅: *"왼쪽부분이 또 너무 휑하게 비는데? 원래는
연1회 공시사들 체크 해제하면 x축의 왼쪽 부분이 자동으로 rescaling 되면서 버블끼리 덜 겹치는게
됐는데 지금은 그 기능이 날아갔어."*

**원인**: 08-18c에서 `xAxis.min`/`max`를 `bubbleChart.setOption({...}, true)`(두 번째 인자
`notMerge:true`)로 명시 고정했는데, 이게 ECharts 로그축의 또 다른 기본 동작(범례에서 끈
시리즈는 auto-range 계산에서 제외 — 그래서 원래 "체크 해제 → 왼쪽이 좁아짐"이 공짜로 됐었다)을
깨뜨렸다. 명시된 min/max는 범례 선택 상태와 무관하게 항상 고정이라, 토글해도 축이 그대로였다.

**진단 과정에서 첫 실측이 틀렸었다**: `dispatchAction({type:'legendUnSelect', name:...})`로
재현 시도했더니 거의 변화가 없어(`[4.053,27219.9]`→`[4.1,27219.9]`) "원래 데이터가 이 정도밖에
안 차이 나나?"로 오판할 뻔했다. 실제 사용자 클릭과 동일한 액션 타입(`legendToggleSelect`)으로
재시도하니 `[4.053,27219.9]`→`[443.5,27219.9]`로 확연히 바뀜 — `legendUnSelect`/`legendSelect`
액션쌍이 `legendselectchanged` 이벤트를 기대한 형태로 안 쏘는 것으로 보임(정확한 원인 미확정,
재현법만 `legendToggleSelect`로 교정). 전체 X 정렬로 확인해보니 최솟값 6개
(비엔피파리바카디프생명·교보라이프플래닛생명·카카오페이손보·처브라이프생명·악사손보·AIG손보,
전부 `estimated:true`=연1회 공시사)가 실제로 왼쪽 끝을 끌어내리고 있었고, 7번째로 작은
실공시(non-estimated) 회사(푸본현대생명, 702.9)와는 100배 가까이 차이 났다 — owner가 기억하는
"추정 끄면 확 좁아진다"는 현상이 데이터로도 설명됨.

- `renderBubble()`에 `bubbleChart.off('legendselectchanged')` + `on('legendselectchanged', ...)`
  추가(클릭 핸들러와 동일한 재등록 패턴, 재렌더마다 리스너 중복 방지). 콜백은 `params.selected`로
  현재 보이는 시리즈를 판별해 `rows`를 다시 필터링하고, 그 부분집합의 min/max에 같은 로그 패딩을
  적용해 `setOption({xAxis:{min,max}})`(이번엔 notMerge 없이 — 병합만, 나머지 옵션 보존)로 축만
  갱신.
- **검증**(모두 `legendToggleSelect`로 실제 클릭 재현): "추정" 끄기 `[4.053,27219.9]` →
  `[443.5,27219.9]` → 다시 켜면 `[4.053,27219.9]`로 정확히 복원. "생명보험" 끄기(최댓값을 쥔
  삼성생명 제외) `[4.053,27219.9]` → `[4.053,19689.9]` → 복원도 확인. 콘솔 에러 0, `pytest
  tests/test_deploy_assets.py` 10/10.

---

## 2026-08-18c — 버블맵 로그축 자동범위가 오른쪽을 통째로 비운다 (owner 채팅 피드백)

owner가 세션 중 직접 채팅으로: *"라이브 배포된거 보니까 왜캐 버블끼리 서로 다닥다닥 붙어있지?
x축 범위를 log(10조)로 하니까 오른쪽에 쓸데없는 공백이 많이 생기는데 걍 x축 범위를 10조로
fix하지말고 그때그때 바뀌게 하면 안되냐?"* — 비동기 핸드오프가 필요 없는 라이브 대화라 inbox
티켓 없이 바로 처리.

**원인 확인**: 코드에 `xAxis.max` 리터럴은 없었다(grep으로 직접 확인) — owner가 본 "10조 고정"은
ECharts 로그축의 **자동 range 계산이 실데이터 최댓값을 다음 10의 거듭제곱으로 반올림**하는
동작이었다. 브라우저에서 실측: 현재 데이터 실제 범위는 6.4억~17,174.6억(약 1.72조)인데, 렌더된
축의 실제 extent는 `[1, 100000]`(=1억~10조) — 데이터가 로그 스케일 5.0 decade 중 4.24만 차지하고
나머지 0.77 decade(선형으로 보면 82,825억, 실데이터 전체 범위보다 큰 공간)가 통째로 빈 채였다.

- `renderBubble()`에 `xAxis.min`/`max`를 명시 — 매 렌더마다 **현재 필터된 `rows`**(업권 필터가
  걸려 있으면 그 부분집합)의 `nbCsm` 실제 min/max에 로그 패딩(`Math.pow(10, 0.2)`≈×1.58, 양옆
  대칭)만 줘서 계산. 하드코딩 없음 — 다음 분기 데이터가 늘거나 줄어도, 업권 필터를 바꿔도 그때
  그때 재계산된다(owner의 "그때그때 바뀌게" 요청 그대로).
- 브라우저 실측: 전체(ALL) `[4.053, 27219.9]`(로그 스팬 3.83, 기존 5.0 대비 ~23% 압축) — 오른쪽
  dead zone이 27,220억까지로 줄어 기존 100,000억보다 훨씬 타이트. 업권 필터를 손해보험만으로
  바꾸면 `[6.369, 19689.9]`로 그 부분집합 범위에 맞춰 추가로 좁혀짐(정상 동작, 필터 전환 후
  전체로 복귀해도 원래 값 정상 복원 확인). 콘솔 에러 0. `pytest tests/test_deploy_assets.py`
  10/10.
- Y축(NB CSM 배수, 선형축)은 그대로 — 선형 auto-range는 로그축 같은 "다음 10의 거듭제곱" 병리가
  없고 owner도 X축만 지목했다.

---

## 2026-08-18b — D-2 follow-up: 버블맵 X축을 실제로 2026.2Q로 이동 (inbox `20260818T0210Z`)

owner가 아래 2026-08-18 항목(D-2)의 캡션 수정을 라이브에서 재확인하고 지적: *"index html 아랫부분
버블맵에서 신계약CSM 규모가 왜 26.1Q 기준이라고 뜨는거? 얘도 고쳐야지."* D-2는 **표기만** 정직하게
고쳤을 뿐(owner가 그 시점엔 그렇게 명시적으로 스코프를 못 박았었다 — "버블맵의 TARGET = '2026.1Q'
고정 자체는 유지. 문구만 바꾸는 게 이번 스코프"), X축이 실제로 2026.1Q에 묶여 있는 **근본 원인**은
그대로였다. owner가 원인(X 값이 배수와 같은 `NB_CSM_multiple.json` 행에서 나옴)과 회사 탈락 여부
(23사 전부 CSM_waterfall에 2026.2Q 有, 0사 탈락)까지 직접 실측해 티켓에 적어줬다.

- **`buildBubbleData()` 재작성 — X·Y 소스 완전 분리**:
  - **X(신계약CSM)**: `NB_CSM_multiple.json`에서 떼어내 `wfNbItem`(CSM_waterfall 항목2, 이미
    로드돼 있던 인덱스)의 **회사별 최신값**으로. 고정 리터럴 없음 — `크기`(closing/closingK)와
    완전히 같은 패턴이라 다음 분기도 자동 갱신된다. 전사 최신(`xGlobalMaxK`, 데이터에서 계산)과
    다른 회사만 "추정"(연1회 공시사가 자기 최근 마감연도 값을 대신 쓰는 것).
  - **Y(배수)**: `NB_CSM_multiple.json` + `NB_TARGET_Q`(2026.1Q 고정) 그대로 — owner 보류 결정
    변경 없음(KIDI 월납초회 6월치 미수집).
  - "옮겨도 회사가 안 빠진다"는 owner의 사전 실측 그대로 확인 — 브라우저 실측 `shownCount`가
    수정 전후 33으로 동일.
- **⚠ 스케일 함정(owner가 미리 계산해 지적)**: 연1회 공시사 추정값이 기존엔 `raw/4`(1분기 스케일
  가정 — X가 2026.1Q=1분기 누계였을 때 기준)였는데, X 기준이 반기누계(2026.2Q)로 바뀌었으니
  `raw/2`가 맞다(연 12개월분을 6개월 스케일로). 안 고치면 추정사 버블만 X가 절반으로 찌그러져
  실공시 버블과 나란히 못 비교됨. 실측(AIG손해보험): raw 986.82564 → 493.41282(정확히 ÷2).
  범례 텍스트("÷4"→"÷2")·커버리지표 remark 텍스트도 같이 수정.
- **분리 표기**: `.section-desc`, `metaEl`, 툴팁 전부 X와 Y를 별도 문장/줄로. 이전엔
  `X·Y 기준 ${NB_TARGET_Q}`로 한 덩어리였던 게 이젠 "X축(신계약CSM)=회사별 최신(~2026.2Q) ·
  Y축(배수)=2026.1Q 고정 · 크기(기말)=회사별 최신(~2026.2Q)". 툴팁은 X/Y 각 줄이 `d.srcQ`(X)와
  `d.multPeriod`(Y, 신설 필드)를 각자 표기하도록 재작성 — 이전엔 하나의 if(`d.estimated`)/else로
  묶여서 X만 추정이거나 Y만 추정인 경우를 구분 못 했다.
- **부수 발견: 커버리지표 "직전값 이월" 라벨 버그**: 검증 중 발견 — `r.closingK !== TARGET_QKEY`
  분기가 신계약CSM/기말 둘 다 "회사별 최신"이 된 지금은 의미가 없어졌는데(더 최신인 회사까지
  "이월"로 오표시), 여전히 살아있어서 삼성생명 등 2026.2Q 실공시 회사 6곳(확인분)이 전부 "직전값
  이월"로 잘못 표시되고 있었다(브라우저 실측으로 발견, pre-existing — 이번 세션이 만든 버그는
  아니지만 같은 함수·같은 "정직한 라벨" 취지라 즉시 수정). 그 분기와 이제 안 쓰는 `TARGET_QKEY`
  상수 삭제, 해당 케이스는 단순히 "실공시"로. 재확인: 6개사 전부 "실공시"로 정상 표시.
- 잔여 하드코딩 `period: estimated ? srcQ : '2026.1Q'`도 제거(owner가 "잔여 하드코딩"으로 직접
  지목) — X가 이제 항상 실제 조회된 분기 문자열을 쓰므로 삼항연산자 자체가 불필요해짐.
- **검증**: 브라우저 실측 — 삼성화재(KR0008) X=12,423.5억이 `CSM_waterfall.json`의 2026.2Q
  원값(fetch로 직접 대조)과 정확히 일치. `표시 33社`(census 37사 중) 수정 전후 불변. ECharts
  시리즈 13(생명)+9(손해)+11(추정)=33 정합. 콘솔 에러 0. 375px — 모바일은 `renderBubbleList()`가
  X/신계약CSM 자체를 안 보여주는 완전히 별도 경로라 무변경(확인만). `pytest
  tests/test_deploy_assets.py` 10/10.

---

## 2026-08-18 — owner 라이브 QA 4건(D-1~D-5): 분기 하드코딩 제거·BS 토글 버그·법정준비금 재배치·배당 기간선택 (inbox `20260818T0026Z`)

owner가 라이브에서 직접 발견한 4개 결함/불만을 한 티켓으로 발주(`inbox/designer/20260818T0026Z`).
선행 티켓 `inbox/designer/20260815T0130Z`(validation, 분기 라벨 stale)는 이 티켓의 D-2가 그대로
흡수해 착수 없이 `_resolved/`로 이동. 전부 화면 전용 — 데이터/마스터/빌더 무수정.

**D-1. 민감도 패널 캡션 — "왜 25.4Q냐"에 대한 설명 부재**

owner 질문의 전제("경영공시 PDF라 그런가")는 틀렸지만 증상은 진짜였다: IFRS17 가정민감도
(할인율·손해율·사업비율 충격)는 소스가 DART가 맞고(`sensitivity_heatmap.json` 각 행에
`rcept_no`), 다만 **사업보고서 연 1회 주석**이라 반기·분기보고서엔 그 표 자체가 없다 — 한화생명/
삼성화재/DB손보 3사의 원문 XML에서 라벨 출현횟수로 재검증(예: 한화생명 "사업비율" 사업보고서
35회 vs 반기보고서 0회). `period: FY2025`가 최신인 게 정상이고 다음 갱신은 FY2026 사업보고서
(2027-03경) — 파서 조치 불필요, 화면에 그 사실을 안 적어놔서 매번 같은 질문이 반복될 뿐이었다.

- `IFRS17.html` `#senCap` 렌더 라인에 " · 보험계약 가정민감도는 사업보고서 연 1회 공시 —
  반기·분기보고서 미공시 항목" 상시 부기. `senAo`(as-of) 계산 로직 자체는 무변경.

**D-2. 분기 하드코딩 전수 제거 + PL 워터폴 실버그**

owner: "html에 조회대상기간들 그때그때 업데이트되게... PL breakdown 그래프가 25회기 기준으로
돼있는데 26.2Q 기준으로 업데이트."

- **IFRS17.html**: `<h2>3) CSM 시계열 (2023.1Q ~ 2026.1Q)</h2>` / 같은 문자열의 aria-label /
  `#wfPeriodLine` 커버리지 줄 — 3곳에 복붙돼 있던 "2023.1Q~2026.1Q"를 `ix.wfx`(CSM_waterfall)
  전사 데이터의 최소~최대 분기에서 `boot()` 1회 계산하는 `CSM_RANGE_LABEL`로 통합. h2는
  `<span id="histRange">`로 쪼개고 `mountDash()`에서 채움(템플릿이 회사 선택 시점에 클론되므로
  boot() 완료 후 실행 보장). aria-label은 `canvasHist.setAttribute`로 동적 부여.
- **index.html 버블 캡션**: "2026.1Q" 리터럴이 TARGET 계산·섹션 설명 문단·`metaEl`·툴팁 경고문·
  커버리지표 `TARGET_QKEY` 5곳에 흩어져 있던 걸 `const NB_TARGET_Q = '2026.1Q'` 하나로 통합.
  owner가 TARGET **값 자체는 그대로 유지**(NB 배수 분모 재수집 보류 중이라 아직 맞음)하라고
  명시했으므로 로직은 안 건드리고 참조만 단일화. 문단·메타 텍스트는 "X·Y는 2026.1Q 연누계 고정
  (배수 분모인 월납초회보험료가 2026.1Q까지만 수집됨) / 크기는 회사별 최신(2026.1Q~2026.2Q 혼재)"로
  재작성 — `metaEl`의 크기 상한은 `rows`의 `closingK` 최댓값을 `qlabel()`로 역변환해 실제 데이터
  범위를 반영(하드코딩 아님).
- **푸터**: `IFRS17.html`/`index.html`/`공시보고서.html` 3파일의 "(2026.1Q)"를
  `<span id="footerQ">`로 교체하고 각 페이지가 이미 로드하는 마스터에서 최댓값을 계산해 채움
  (IFRS17: `ix.wfx` gMax 재사용 / index: K-ICS `getLatestQuarter` + CSM `globalMaxQuarter` 중
  더 최신 것 / 공시보고서: `GLOBAL_QUARTERS[0]`). **`K-ICS.html:1464` 푸터는 owner가 명시적으로
  "아직 맞다"고 지정 — 미변경** (`kics_disclosure.json` 최신이 실제로 2026.1Q, 2026.2Q 행 0건).
- **PL 워터폴 실버그**: `plPeriod(name,"year")`가 `qs.find(x=>/\.4Q$/.test(x))`로 **가장 최근
  4Q**를 찾아 쓰고 있었는데, 이 함수는 desc 정렬 배열에서 `.find()`를 쓰므로 최신 반기(2026.2Q)가
  있어도 그보다 오래된 마감연도(FY2025.4Q)를 우선 채택 — "연도" 모드가 회기 마감 여부와 무관하게
  구조적으로 1년 지연되는 버그였다(owner 신고 증상의 근본원인). 최신 분기를 무조건 우선하도록
  뒤집고, 신설 `fyPartialLabel(qn)`(2Q→"반기누계", 그 외→"N분기누계")로 안 닫힌 회기를 라벨링
  ("2026.2Q(반기누계)"). `plTablePeriods()`의 연도 모드 컬럼 헤더도 동일 헬퍼로 통일(차트 캡션과
  표 헤더가 같은 분기를 다르게 표기하는 불일치 방지). `WF_WINDOW` 주석의 하드코딩 예시([2023,2024,
  2025,2026.1Q])도 패턴 설명으로 교체(이 상수 자체는 CSM Panel 2용이라 실제 버그와는 무관 — 확인
  결과 CSM Panel 2의 `wfYearBuckets()`는 이미 최신-우선이라 버그 없음, PL만 문제였음).
- 브라우저 실측(KR0008, 2026.2Q PL 데이터 보유): `plCap` = "당기순이익 워터폴 · 2026.2Q(반기누계) ·
  …" (수정 전이었다면 "FY2025 (연 누계)"). `histRange`="2023.1Q ~ 2026.2Q", `wfPeriodLine`="…37개사
  · 2023.1Q ~ 2026.2Q · 연 누계(YTD) 기준", `footerQ`(양쪽 페이지)="2026.2Q", K-ICS 푸터만
  "2026.1Q" 그대로.

**D-3. 🔴 17BS T자 `+` 버튼 무반응 — CSS 우선순위 버그**

owner: "+버튼이 안먹히고 있어." 원인: `.bs-l2-rows{display:flex; …}`(작성자 스타일)가 UA 기본
`[hidden]{display:none}`을 cascade 특이도로 이겨서, `renderBsZone()`의 `box.hidden = !showing`
토글이 DOM 속성은 정확히 바꾸는데 화면은 항상 `display:flex`로 남아있었다 — 버튼을 눌러도 아무
일도 안 일어나는 것처럼 보임(`aria-expanded`는 정상 토글돼 접근성 트리와 실제 표시가 어긋나는
상태이기도 했음).

- 수정: `.bs-l2-rows[hidden]{display:none}` 한 줄 추가.
- **4개 HTML 파일 전수 `hidden` grep 감사**(owner: "같은 패턴이 다른 패널에도 있는지 함께
  점검"): `IFRS17.html`에 이 클래스 + 곧 삭제될 `.bs-reserve-note`(D-4에서 제거) 외 없음.
  `K-ICS.html`/`index.html`/`공시보고서.html`은 `hidden` 속성 자체를 전혀 안 씀(grep 매치는 전부
  `overflow:hidden` CSS로 무관한 false positive) — 이 세 페이지는 애초에 이 버그 클래스가 아님.
  `IFRS17.html`의 PL 재보험 세부 토글(`tr.style.display` 방식)도 `hidden` 속성을 안 써서 무관.
  → **결론: 사이트 전체에서 이 충돌은 `.bs-l2-rows` 한 곳뿐이었고 지금 고쳤다.**
- 브라우저 실측(KR0008): 자산/부채/자본 3구역 전부 클릭 → `box.hidden===false` +
  `getComputedStyle(box).display==="flex"` 확인. 콘솔 에러 0.

**D-4. 법정준비금 → 이익잉여금 하위 행으로 재배치**

owner: "그아래 법정준비금은 별도 섹션으로 넣지 말고, 이익잉여금 하위항목으로 넣어야지." 기존
`#bsReserveNote`(자본 T자 아래 별도 참고 블록)를 삭제하고 자본 세부 목록 안에 통합.

- `renderDetailRows()`가 각 행에 `data-name`(항목명)을 심도록 확장(항목번호가 아니라 이름으로
  앵커링 — 섹션/레벨 데이터계약이 "항목번호 하드코딩 금지"를 원칙으로 삼고 있어 동일 관례 적용).
- 신설 `renderReserveSubrows(box, reserveRows, anchorName)`: `box` 안에서 `data-name===anchorName`
  ("이익잉여금")인 행을 찾아 그 바로 뒤에 준비금 3항목(해약환급금·비상위험·대손, 섹션="준비금")을
  들여쓴(`.bs-l2-row-sub`) 하위 행으로 삽입. 앵커를 못 찾으면(이익잉여금 행 자체가 없는 비상장사
  등) 잔차("기타·미표시") 행 앞에 삽입해 잔차가 항상 마지막 줄로 남게 함(최초 구현은 `box`
  끝에 그냥 붙여서 잔차 뒤에 준비금이 오는 어색한 순서였음 — 라이나생명보험으로 실측하다 발견,
  같은 세션에 수정).
- **합산 금지 유지**: `renderBsZone()`이 자본 세부용 `renderDetailRows(box, rows, total)`은
  기존과 동일하게 `rows`(섹션="자본"만, 4/30/31)로만 호출해 잔차 계산 — 준비금 배열은 별도
  변수(`reserveRows`)로 분리해 `renderReserveSubrows`에 **따로** 넘기므로 잔차 합산에 안 들어감.
  실측(KR0008): 준비금 3항목 합이 6.95조인데 잔차는 △81억(소액) — 만약 잘못 합산됐다면 잔차가
  그만큼 더 음수로 튀었을 것.
- HTML(`#bsReserveNote`/`#bsReserveRows`)·CSS(`.bs-reserve-note`)·`renderBsSection()`의 옛 렌더
  블록 전부 삭제(내 변경으로 죽은 코드가 됐으므로 보존하지 않고 제거 — CLAUDE.md 원칙).
- 신규 CSS: `.bs-l2-row-sub{padding-left:14px;opacity:.75}` + 라벨/값 폰트 축소·`--muted` 색.
  모바일(`@media max-width:640px`) 쪽은 기존 `.bs-l2-row{grid-template-columns:1fr}` 규칙을
  그대로 상속받아 별도 대응 불필요(375px 실측으로 확인).

**Real-viewport 재확인 — 3세션째(08-14b/c/d) carried-over 항목 종결**

이전 세션들은 `window.innerWidth===0`으로 Browser pane이 아예 compositing을 안 해 375px 검증이
불가능했다. 이번 세션은 `resize_window`(mobile preset) 후 `window.innerWidth`가 실제로 375를
반환 — `computer{screenshot}`은 여전히 "pane not displayed" 에러로 실패했지만(픽셀 스크린샷은
여전히 못 얻음), DOM/computed-style 쿼리로 진짜 375px 레이아웃을 검증할 수 있었다: `.bs-t`
flex-column 전환, `.bs-l2-row`/`.bs-l2-row-sub` 1열 grid, `body.scrollWidth<=innerWidth`(가로
오버플로우 없음), `공시보고서.html`의 신설 기간선택 셀렉터도 `.controls{flex-wrap:wrap}`로
정상 줄바꿈. 콘솔 에러 0.

**D-5. 공시보고서.html(배당) 개편**

owner: "너무 대충 만든거아냐? 주당액면가는 필요없으니까 빼고, 얘도 KICS.html 처럼 조회기간
분기/연 선택창 만들어서 분기는 직전 5분기(26.2Q~25.2Q), 연간은 직전 4년(2023, 2024, 2025,
2026.2Q누계) 보여주게 바꿔라."

- 항목1(`주당액면가액`) `ITEM_DEFS`에서 제거.
- 기간 선택 `<select id="period">`(K-ICS.html과 동일 라벨/옵션 스타일: 기간: 분기/연도) 신설.
  컬럼은 **회사별이 아니라 전사 공통 윈도**로 고정 — `boot()`에서 `dividend.json` 전체 행을
  훑어 `GLOBAL_QUARTERS`(전사 공시분기 desc 배열)를 1회 계산하고, `globalRecentQuarters(5)`
  (분기 모드)·`globalYearPeriods()`(연도 모드, 최신+직전 3개년말 4Q, 안 닫힌 최신 회기는
  "…누계" 라벨)가 여기서 파생. K-ICS.html의 `getRecentQuarters`/`getYearEndQuarters`가
  **회사별로 필터링된 데이터**에서 윈도를 뽑는 것과 달리 이건 의도적으로 전사 공통 —
  회사마다 헤더(컬럼셋) 자체가 달라지면 "이 회사는 이 분기 미공시"가 "그 분기 컬럼이
  아예 없음"으로 뭉개져 owner가 우려한 0값-맹점류 착시가 재발할 수 있어서, 헤더는 고정하고
  값만 정보없음으로 비우는 쪽을 택함.
  `renderCompanyTable`/`renderClassTables`가 `quartersOf(code)` 대신 `selectedPeriods()`를
  쓰도록 교체. `latestKpiQuarter()`(KPI 카드용, 회사별 최신 4Q 앵커)는 그대로 둠 — 별개 로직.
- `#divTableTitle`("분기별 배당 지표" / "연도별 배당 지표")을 모드에 맞춰 동적으로 갱신(내 변경으로
  생긴 부정확 표시를 내가 직접 방지 — 연도 모드에서 "분기별"이라고 써 있으면 오독 유발).
- **KPI 카드 vs 기간선택 충돌(owner D-5 4번)**: KPI는 배당성향·수익률처럼 대부분 4Q(사업보고서)
  에만 실리는 지표라 "선택한 여러 분기 중 하나"로 대응시킬 방법이 없음(다중 분기 윈도를 단일
  스냅샷 카드에 매핑하는 문제) — **"최신 4Q 우선 앵커" 그대로 유지, 조회기간 선택과 무관**으로
  결정하고 `#divKpiCap` 캡션에 명시("아래 조회기간 선택과 무관하게 항상 최신 고정"). owner가
  "정하고 캡션에 명시"라고 명시적으로 요청한 항목이라 결정 자체를 여기 남긴다.
- 푸터 "(2026.1Q)"도 D-2와 같은 방식으로 `GLOBAL_QUARTERS[0]` 기반 동적 span.
- 0-vs-정보없음 유지 확인(owner 상습 함정 경고): KR0008 분기 모드에서 "현금배당금총액"이
  2025.2Q/3Q·2026.1Q에 실제 `0백만원`, 2026.2Q(그 회사 그 분기 미공시)엔 `정보없음` — 로직
  자체(`val()`/`fmtItem()`)는 무변경이라 애초에 깨질 이유가 없었지만 실측으로 재확인.
- 브라우저 실측(KR0008): 분기 헤더 정확히 `2025.2Q,2025.3Q,2025.4Q,2026.1Q,2026.2Q`(owner
  예시와 일치), 연도 헤더 정확히 `2023,2024,2025,2026.2Q누계`(owner 예시와 일치). 종류주 표
  2개(보통주/우선주) 정상.

**부수 처리**

- publishing 티켓 `inbox/designer/20260803T0900Z`(UH-7, `baseline_2025_4Q`→`baseline` 키 rename)
  — 급하지 않은 건이었으나 이번에 HTML을 대거 만지는 김에 처리: `K-ICS.html:1090`을
  `row.baseline || row.baseline_2025_4Q`로. 브라우저 실측(K-ICS.html, KR0008, 분기 모드):
  Forward Outlook 패널이 정상 렌더(baseline note에 실제 수치 출력), 콘솔 에러 0.
  `_resolved/`로 이동, publishing에 확인 통지.
- validation 티켓 `inbox/designer/20260815T0130Z`(분기 라벨, 이 owner 티켓 D-2가 흡수) —
  착수 없이 상태만 `resolved`로 바꾸고 `_resolved/`로 이동.

**검증**

- `python -m pytest tests/test_deploy_assets.py` 10/10, 수정 전후 결과 동일(구조적 변경 없음).
- 4개 페이지(`IFRS17.html`/`index.html`/`K-ICS.html`/`공시보고서.html`) 전부 데스크톱(1280px)+
  모바일(375px) 콘솔 에러 0. `K-ICS.html`의 owner 지정 "건드리지 말 것" 대상(푸터, 지급여력비율
  라벨 매칭 등) 무변경 확인.
- `TODO_designer.md` BS-TACCOUNT 섹션의 3세션째 carried-over 항목 2개(real-viewport recheck,
  자산/부채 `+` 실동작 확인) 전부 종결. DIVIDEND-PAGE 섹션에 D-5 반영.

---

## 2026-08-15b — T자 세부는 "주요 계정 하이라이트"라 총계와 안 맞는다는 걸 화면에 표시 (inbox `20260814T1710Z`)

validation이 실측(290셀)으로 잡아냄: owner가 8/15 파서 발주에서 T자 레벨2 세부를 "최대 15줄
하이라이트, 폐쇄검산 대상 아님"으로 확정해뒀는데, 그 사실이 화면 어디에도 없어서 세부를 펼쳐보면
총계보다 훨씬 작은 합계가 "깨진 것"처럼 읽혔다(최악 사례: 신한라이프 자산 — 세부합이 총계의
29%). 데이터/파서 문제 아님, 화면 라벨만.

- validation이 제안한 두 옵션(캡션 문구 vs 잔차 행) 중 **잔차 행을 채택**(정보량이 크고 T자
  비율감 유지). `renderDetailRows()`에 `total` 인자를 추가해 `총계 − Σ표시된 세부`를 "기타·미표시"
  회색 행으로 렌더 단에서 계산(마스터에 잔차 항목 신설 안 함). 캡션에도 짧게 "(주요 계정만)" 추가.
  준비금 참고 목록은 애초에 비교할 총계가 없는 섹션이라 `total`을 안 넘기면 자동으로 잔차 행이
  안 붙음(추가 분기 불필요).
- 검증쪽이 지목한 최악 사례 회사(신한라이프/KR0094)로 재현 확인 — 세부합+잔차가 총계와 반올림
  오차 내로 일치. 콘솔 에러 0, `pytest tests/test_deploy_assets.py` 10/10(이 사이 publishing이
  `dividend.json` git 추적+keep-list를 자체적으로 마쳐 그 절반 FAIL도 같이 사라짐 — 이 세션 조치
  아님, 참고로만 기록).
- `inbox/designer/20260814T1710Z` 답변 작성 후 `_resolved/`로 이동.

---

## 2026-08-15 — 공시보고서.html "준비 중" 껍데기 → 배당현황 화면 (inbox `20260814T2230Z`)

사용자가 "배당화면 개발 지금 진행해라"로 지시 — 직전 세션에서 확인만 하고 착수를 미뤄뒀던 항목.
owner의 C-4 체인(원 발주 `inbox/_resolved/20260814T0746Z`) 마지막 다리: downloader가 39사×FY2023-
2026×4개 reprt_code로 DART alotMatter를 수집하고, parser가 `dividend.json`(24개사=상장사 전원,
1,924행, 항목 1-10)을 빌드해 designer에 통지한 상태였다.

- **페이지 자체는 그대로 재사용**: owner가 "새 페이지·새 탭 만들지 말 것"을 명시했으므로 기존
  4-tab nav 무변경, `공시보고서.html`의 "준비 중입니다" 자리를 실제 배당 화면으로 교체.
  이 페이지는 IFRS17.html/K-ICS.html과 달리 common.css 마이그레이션 이전 로컬 토큰/클래스
  체계를 그대로 쓰고 있어(`:root`에 --card 등 재정의, `.tab`/`.panel` 로컬 재정의 — 2026-07-22
  주석에 이미 "손대지 말 것"으로 명문화돼 있음) 그 위에 얹어서 작업, 페이지 CSS 아키텍처는
  건드리지 않음.
- **구조**: 회사 셀렉터(다른 3페이지와 동일 UX) → KPI 스트립 4장(현금배당금총액·주당현금배당금·
  현금배당성향·현금배당수익률, 최신 4Q 앵커) → 회사단위 표(항목1-7 × 전체 공시분기, 가로스크롤) →
  종류주별 미니테이블(항목8-10, 실제 존재하는 종류주마다 하나씩).
- **드롭다운 스코프 결정**: kics_disclosure 39사 전체가 아니라 `dividend.json` 자체가 가진
  24개사로만 구성. 이 DART 엔드포인트(alotMatter)가 비상장 15개사를 구조적으로 다루지 않아
  (파서 원 통지에 이미 명시) 그 15사를 셀렉터에 넣어봐야 "영구 데이터없음" 스텁만 남는 항목이 될
  뿐 — 다른 페이지들의 "풀 유니버스 + 빈 상태" 관행과 다르게 간 것이 의도적 판단(실익 없는 UI
  확장 회피).
- **종류주 자동 유도**: `classesOf()`가 회사별 실제 존재하는 종류주(보통주/우선주)를 데이터에서
  뽑아낸다 — 하드코딩된 "이 4개사는 우선주 있음" 리스트 없음. `renderClassTables()`가 종류주가
  하나도 없는 회사(실제로 있음 — 롯데손해보험·흥국화재 등, item 8-10 자체가 원천 데이터에 없음)를
  "종류주별 세부 공시 없음"으로 자연스럽게 처리.
- **결측 vs 진짜 0 — owner가 원 발주에서 두 번(다운로더 함정 메모 + 파서 통지) 강조한 그 구분**:
  항목5/6(배당총액)의 명시적 `0`은 그대로 `0`으로 표시(무배당의 정당한 신호). 그 외 행이 아예
  없는 결측은 전부 `"정보없음"`으로 — 실측(삼성화재)에서 현금배당성향이 4Q에만 있고 1-3Q는
  "정보없음"으로 정확히 갈리는 것을 확인, `0%`으로 잘못 채워지는 사고 없음.
- **검증**: 로컬 `http.server`. 삼성화재(KR0008, 보통주+우선주 둘 다 있는 4개사 중 하나) —
  2023.4Q 보통주 16,000원/6.50% vs 우선주 16,005원/8.60%, 서로 다른 실수치로 분리 렌더 확인.
  롯데손해보험(KR0003) raw JSON 직접 조회로 "종류주 데이터 자체가 없는 회사" 확인(코드 버그 아님,
  진짜 데이터 특성). 콘솔 에러 0.
- **`pytest tests/test_deploy_assets.py`**: `dividend.json`을 fetch하기 시작하는 순간 걸리는
  `test_docs_agree_with_what_pages_fetch`가 예상대로 FAIL — `claude-agent-designer.md` §1 표에
  `공시보고서.html` 행을 갱신해 designer 쪽 절반은 통과시킴. `claude-agent-publishing.md` 쪽,
  `dividend.json` 자체의 git 미추적, `validate_data_contract.py` 게이트 미배선은 이미
  `inbox/publishing/20260814T2230Z`(P-1~P-4)에 등재된 퍼블리싱 소관 — 중복 발주 안 하고 손도 안 댐.
- 배포는 시도하지 않음. `inbox/designer/20260814T2230Z` 답변 작성 후 `_resolved/`로 이동.

---

## 2026-08-14d — BS 패널 T자 재설계 + 최상단 이동 (owner 직접 발주, inbox `20260814T1250Z`)

사용자가 세션을 "inbox 확인해라"로 재개. `inbox/designer/`에 새 메시지 3건 — 그중 owner 발주
`20260814T1250Z__owner__IFRS17__bs_taccount_top_panel.md`가 핵심. owner 원문(2026-08-14):

> 이거 디자인도 17.html의 맨 위로 올리고, 가능하면 재무상태표처럼 왼쪽 자산 & 우상단 부채 &
> 우하단 자본 그림 그려주고, 그 아래에 세부 테이블 — 자산 옆에 + 아이콘 클릭하면 좀 더 세부적으로
> 나오는 식

이번엔 "오케스트레이터 과도주문"이 아니라 **owner가 직접 드릴다운을 요구**했다 — 08-14b에 박아둔
"L2/L3는 과도주문" 주석이 다음 날 무효가 된 셈. 동시에 확인한 두 건: validation의
`20260814T0135Z` 문서갭 티켓은 이미 SUPERSEDED 상태였고(다른 세션이 `20260814T0232Z` 리포인트로
먼저 처리, `_resolved/`로 정리), parser의 배당(dividend) 데이터 준비 통지는 이번 건과 무관해
사용자에게 별도로 안내만 하고 착수하지 않음.

- **레이아웃**: `data-pane="bs"` 패널을 `kpi7` 스트립 바로 다음(맨 위)으로 이동, 나머지 6개 패널
  `h2` 번호를 1~6→2~7로 일괄 재부여. T자는 `.bs-t{display:flex}` 좌측 자산 / `.bs-t-right{flex-
  direction:column}` 우측 부채(위)·자본(아래). 우측 두 구역 높이를 `flexGrow`로 부채:자본 실제
  값 비율에 맞춰(자산=부채+자본이 면적으로 읽히게, owner 권장사항) — 자본잠식 등 음수 부호에서는
  비율이 무의미하므로 50/50 fallback.
- **데이터 계약**: `EQ_SECTION_LEVEL_BRIDGE` 상수로 현재 `IFRS17_BS.json`(항목 1-7, 섹션/레벨
  컬럼 아직 없음 — parser 발주 `20260814T1250Z`가 별도 진행 중)을 임시 매핑
  (`{1:[자산,1],2:[부채,1],3:[자본,1],4:[자본,2],5·6·7:[준비금,2]}`). 렌더 함수(`renderBsZone`/
  `eqTotal`/`eqDetailRows`)는 이 브릿지를 거친 `섹션`/`레벨` 필드만 보고 동작 — 항목번호 분기가
  코드 어디에도 없다. 실제 `섹션`/`레벨` 컬럼이 오면 그 값이 브릿지보다 우선하므로, 파서 랜딩
  시점에 HTML을 고칠 필요가 없다(owner가 정확히 이 계약을 요구했다).
- **세부 미공시 처리(owner D-3)**: 레벨2 행이 0개인 섹션은 `+` 버튼을 `disabled`+
  `aria-disabled="true"`+title="세부 미공시". "파서가 아직 그 섹션 계정을 안 만들었다"(지금 자산·
  부채 전 회사)와 "회사가 원래 세부를 공시 안 한다"(비상장 15사, 예: 라이나생명보험)가 결과적으로
  같은 "레벨2 없음" 조건이라 같은 코드로 자동 처리됨 — 회사 화이트리스트 없음.
- **준비금 격리**: 해약환급금·비상위험·대손(항목 5·6·7)은 자본 레벨2 목록에 안 섞고 `#bsReserveNote`
  로 완전 분리 — 이익잉여금 내부 적립이라 자본 폐쇄검산에 넣으면 이중계상(owner 명시 경고).
- **죽은 코드 이번엔 진짜 삭제**: `renderBsL2`/`toggleBsL2`/`toggleBsDrill`/`renderBsReserves`/
  `renderBsAociWaterfall`/`eqAociSteps`/`AOCI_LIAB_DECAL`와 `charts.bsAoci` 배선(dispose/resize)을
  전부 제거. 08-14b·08-14c 두 번 "코드 보존(호출만 안 함)"으로 남겨뒀지만 스키마가 그 사이 두 번
  더 바뀌면서 둘 다 "그대로 되살리면 오표시" 경고문이 붙는 신세였다 — 세 번째도 재사용 불가능할
  게 뻔해서 이번엔 보존의 의미가 없다고 판단, 삭제하고 이력은 changelog에만 남긴다.
- **검증**: 로컬 `http.server`, 4개사(삼성화재=세부 있음/자본 AOCI 1행, 메리츠화재=총계 교차검증
  기존 리포인트 수치와 정확히 일치, 라이나생명보험=owner가 예시로 든 비상장사 — 총계만 있고 자산
  `+` 비활성 정확히 재현, 카카오페이손해보험=BS행 0건 스텁). flexGrow 실측(삼성화재 부채76:자본24)이
  원시값 비율(74.48/97.88=76.1%)과 일치. 콘솔 에러 0(5개사 연속 전환). `pytest
  tests/test_deploy_assets.py` 10 passed(신규 JSON 없어 keep-list 무변경 — owner가 명시 요구한
  회귀 없음 확인).
- **스크린샷 미첨부**: 이 세션 Browser pane이 여전히 compositing 안 됨(`window.innerWidth===0`
  실측 — 08-14b/c부터 이어지는 동일 세션 제약, 이번엔 원인까지 수치로 확인). owner D-5가 스크린샷을
  요청했으나 08-14c와 같은 방식(DOM/속성 직접 조회, network log)으로 대체 검증하고 inbox 답변에
  근거 수치를 그대로 남김.
- inbox 3건 처리: `20260814T1250Z`(owner, T자 발주) → `status: answered`(파서 섹션/레벨 랜딩 후
  재확인 예정이라 미종결), `20260814T0135Z`(validation, 이미 SUPERSEDED) → 조치 불요 확인 후
  resolved+archive, `20260814T2230Z`(parser, dividend.json) → 무관한 별건이라 사용자에게 안내만
  하고 미착수.

---

## 2026-08-14c — Panel 7 repoint: equity_composition.json → IFRS17_BS.json (parser 아카이브 대응)

owner 지시로 `equity_composition.json`(항목 1-49)을 아카이브하고 17BS 정본을 `IFRS17_BS.json`
(항목 1-7, 1,632행/38사/2022.4Q-2026.2Q, 8열 — `값_당분기` 없음, 전부 스톡)으로 교체 —
`inbox/designer/20260814T0232Z__owner__MULTI__panel7_repoint_to_ifrs17_bs.md` (병렬 발주: parser
`20260814T0232Z` 아카이브, publishing `20260814T0232Z` keep-list, validation `20260814T0135Z`
문서갭 지적). validator가 "IFRS17.html이 아직 equity_composition.json을 fetch해 배포 시 라이브
404"라고 재확인해 온 게 트리거.

- **`PATHS.eqx` 교체**: `equity_composition.json` → `IFRS17_BS.json` (`IFRS17.html:267`).
- **항목번호 재매핑** (스키마 자체가 다르다 — 그대로 두면 자산 자리에 부채가 들어감): 구
  40/41/1/6/10(자산/부채/자본/AOCI/해약환급금) → 신 1/2/3/4/5. `renderBsSection` 한 곳만 고치면
  됨(08-14b 축소 이후 L1 5타일 경로가 유일한 라이브 소비자).
- **L2/L3 죽은 코드(재노출 금지, 08-14b 컨벤션)에 항목번호 함정 경고 주석 추가**: 그 함수들의
  2-7/20-31/5·10·12·14는 아카이브된 구 스키마 번호다. 신 스키마도 항목 1-7을 쓰므로 그대로
  되살리면 null이 아니라 **다른 항목의 실값이 엉뚱한 라벨로** 뜬다(예: 신규 항목5=해약환급금준비금인데
  `renderBsL2`는 "이익잉여금"이라 라벨링) — null-안전 실패가 아니라 조용한 오표시라 명시적으로
  적어둠.
- **`claude-agent-designer.md` §1 표 갱신**: `IFRS17_BS.json` 추가(+ "6-panel"이 이미 stale이던 것도
  "7-panel"로 같이 정정 — validation이 지적한 갭은 애초에 `equity_composition.json`이 이 표에
  한 번도 없었던 것; `test_docs_agree_with_what_pages_fetch`가 잡음).
- **검증**: 로컬 `http.server`로 39개 드롭다운 전체 순회 — 콘솔 에러/경고 0, `IFRS17_BS.json` 200
  (equity_composition.json은 아예 fetch되지 않음 — 404 원천 제거). 항등식 확인(KR0001 메리츠화재
  43조1,159억=37조2,642억+5조8,517억). "미공시" 폴백 재확인(KR0004 예별손해보험 — 전분기 항목5
  없음). no-BS-data 스텁 재확인(KR1098 카카오페이손해보험, 크래시 없음). Browser pane 컴포지팅
  미작동(08-14b부터 세션 지속 이슈)이라 스크린샷 대신 network log + DOM 텍스트로 검증.
  `pytest tests/test_deploy_assets.py::test_docs_agree_with_what_pages_fetch` — designer 쪽(문서)은
  통과, **publishing 쪽(`claude-agent-publishing.md`)은 아직 실패** — 그건 별도 발주
  (`inbox/publishing/20260814T0232Z`)로 퍼블리싱 세션 소관, designer 범위 밖.
- **push는 여전히 blocked**: 이 리포인트는 designer 절반만 처리한 것. publishing keep-list 교체가
  끝나야 `test_docs_agree_with_what_pages_fetch` 전체가 통과하고, RED=0 게이트까지 맞아야 push
  가능. 두 조건 다 designer 소관 밖.

## 2026-08-14b — BS-DRILLDOWN 과도주문 정정 — Panel 7을 owner 원 요구 5타일로 축소

같은 날 저녁 사용자가 "inbox 다시 확인해라, owner가 과도주문 정정발주 보냈대"로 세션 재개. `inbox/designer/`엔 새 메시지가 없었지만, `inbox/parser/20260814T0035Z__owner__MULTI__equity_tier2_stop.md` +
`inbox/validation/20260814T0035Z__owner__MULTI__equity_scope_rollback_core_shrink.md` (둘 다 owner→parser/validation, 같은 시각)이 발견됐다. 둘 다 owner의 실제 발주 원문을 인용:

> high level의 17BS (자산/부채/자본/AOCI) 를 빠르게 OpenDart API로 가져오고, 가능하면 해약환급금준비금 정도까지 긁어보라고 (**안되면 pass**)

즉 8/13에 parser·validation·designer 세 곳에 뿌려진 정교한 스펙(AOCI 8-컴포넌트 워터폴, 법정준비금 3종
분해, BS 40번대 9항목, Tier-2 15개사 본문 XML)은 owner의 이 한 줄을 "오케스트레이터"(스펙을 작성한
세션)가 부풀린 것이었다 — owner가 두 정정발주에서 명시: "오케스트레이터 발주 오류다, 파서/검증
잘못 아니다" / "이 정정을 계기로 새 작업 만들지 말 것, 줄이는 작업이다" / "게이트/화면에서 빼는
것으로 충분(파일에서 지우지는 말 것)".

designer 앞으로 온 정정발주는 없었지만 — 어제 만든 Panel 7이 바로 그 과도 스펙을 그대로 시각화한
것이라 같은 정정이 적용돼야 한다고 판단, 사용자에게 처리 방향 확인 후(`AskUserQuestion`) 축소 진행.

- **L1 5타일로 축소**: 자산총계(40)·부채총계(41)·자본총계(1)·AOCI(6)·해약환급금준비금(10) — owner
  원문 그대로. 항목42(보험계약부채 비중) sub-label은 애초에 owner가 요구한 적 없어 같이 제거.
  해약환급금준비금이 `null`인 회사(BS 주기행 없는 절반가량)는 범용 "—" 대신 **"미공시"**로 표기
  (owner 원 요구가 "안되면 pass"였으므로, 이건 오류가 아니라 그 회사가 이 항목을 그렇게 공시한다는
  사실이라는 뜻을 명확히 담기 위함 — 사용자가 지정한 표기).
- **L2 자본구성 스택 / L3-a AOCI 워터폴 / L3-b 법정준비금 — 숨김(코드는 보존)**, 2026-07-30c의 CSM
  보조표 숨김과 동일 컨벤션. `renderBsL2`/`toggleBsL2`/`toggleBsDrill`/`renderBsAociWaterfall`/
  `renderBsReserves`/`eqAociSteps` 함수 정의 그대로 유지, 호출만 제거. 자본총계 타일을 `<button
  aria-expanded>`에서 평범한 `<div>`로 되돌림(더 이상 클릭 가능하지 않으므로 버튼 시맨틱을 유지하는
  게 오히려 오해 소지). `.bs-l1` 그리드를 고정 3열에서 `repeat(auto-fit,minmax(150px,1fr))`로 변경
  (5타일 자동 줄바꿈). 재노출 경로는 `#bsL1` 마크업 주석 + `renderBsSection` 위 주석에 인라인으로
  남겨둠(다음에 되돌릴 사람이 두 지점만 보면 됨).
- **재검증**: 5타일 실데이터 재확인(삼성화재 KR0008 — 자산 97.88조/부채 74.48조/자본 23.40조/AOCI
  9.66조/해약환급금 4.13조, 전부 변경 전과 동일 수치). "미공시" 케이스 직접 확인(서울보증보험
  KR0150 — 보증보험사라 해약환급금 구조적 결측). equity 0행 회사(카카오페이손해보험) 스텁 경로
  무영향 확인. 콘솔 에러 0(4개사 연속 전환 포함).
- **바뀌지 않은 것**: 데이터 배선(`PATHS.eqx`/`ix.eqx`/`eqVal`/`eqQuartersOf`/`eqLatestQuarter`)은
  그대로 — owner도 "파일에서 지우지 말 것"이라 했고, 5타일도 결국 이 인덱스를 그대로 쓴다. 게이트
  RED=207(축소 진행 중, 완료 시 ~20 예상)이라 여전히 라이브 배포 안 함 — 이번 정정과 무관하게
  유지되던 조건.
- 8/13 두 inbox 스레드(`_resolved/20260813T0422Z…aoci_panel_spec.md`,
  `…0436Z…placement_decision.md`)는 이미 resolved+archived라 재오픈하지 않음 — 이 changelog 항목이
  정정의 기록.

---

## 2026-08-14 — IFRS17.html Panel 7 "재무상태표 · 자본의 질" — BS 3단 드릴다운 (inbox `20260813T0422Z`/`20260813T0436Z`, mockup)

Owner backlog: 신규 마스터 `equity_composition.json`(AOCI·법정준비금·BS 상위 9행, 항목 1-49) 화면 발주. 배치·범위·UX는 owner가 후속 메시지에서 직접 확정(신규 페이지도 K-ICS.html도 아닌 IFRS17.html 신규 섹션, 3단 드릴다운) — designer는 그 스펙대로 구현.

- **데이터 배선**: `PATHS.eqx`/`ix.eqx` — 기존 `wfx`/`plx` 롱포맷 인덱스(code→quarter→항목번호)와 동일 패턴으로 boot()에 추가. equity 커버리지가 CSM/PL과 달라(kics 39사 기준) 드롭다운에 회사가 몇 개 더 늘어남(정상 — "기존 셀렉터 재사용" 요구의 자연스러운 결과).
- **L1** (항목 40/41/42/1): 자산총계·자본총계·부채총계(보험계약부채 비중 인라인) 3타일. 자본총계만 버튼(L2 토글).
- **L2** (항목 2-7): 자본구성 6행. **스택 막대가 아니라 행별 크기비례 막대**로 구현(디자이너 판단) — AOCI·자본조정이 상시 음수라 100%-스택은 음수 구성요소에서 깨짐, 행 방식이 부호 무관 안정적. 이익잉여금/AOCI 행이 L3-b/L3-a 토글 버튼.
- **L3-a AOCI 워터폴** (항목 20-31): **기존 PL 워터폴(`renderPlWaterfall`, Panel 4)의 0선교차 custom renderItem `[y0,y1]` 패턴을 그대로 재사용**(발주문이 지목한 그 렌더러 — 재구현 안 함). 기초(20)→①FVOCI평가손익(21,자산측)→②보험계약순금융손익(22,부채측)→재보험(23)→위험회피(24)→기타(25+26+27+28+**31**, 파서가 같은 날 저녁 추가한 소유주거래 항목까지 포함해 `20+29+31=30` 롤포워드와 정확히 닫히도록 구성)→기말(30). 색맹 안전: 부채측 막대에 ECharts `decal`(대각선 해치) + x축 라벨에 "(자산측)"/"(부채측)" 텍스트 병기, 색상 단독 구분 없음.
- **L3-b 법정준비금** (항목 5,10,12,14): 해약환급금준비금(10) accent 배경+굵게 강조, 비상위험(12)·대손(14) 보통, 잔여(이익잉여금−3종) 톤다운.
- **드릴다운**: 회사 변경마다 L1만 노출로 리셋(L2/L3 템플릿 기본 `hidden`), 자본총계→L2, 이익잉여금/AOCI→L3-b/L3-a(아코디언, 동시 1개만). 전부 네이티브 `<button>`+`aria-expanded`/`aria-controls`, focus-visible은 common.css 전역 상속. 음수 △는 기존 `samo`/`fmtEokKR` 재사용(신규 포맷터 없음).
- **검증**: 로컬 `python -m http.server 8896`(`.claude/launch.json` "Dashboards (static)"). 삼성화재(KR0008) 자산=부채+자본 화면수치 검산, L2 6행 합=자본총계·L3-b 4행 합=이익잉여금 정확 일치. 0선교차는 KB라이프생명 2026.1Q 실데이터로 `echarts.getOption()` 직접 조회해 두 막대([31,467→△975,383], [△975,383→86,012])의 부호반전을 확인. 카카오페이손해보험(equity 0행, validation 기록된 그 회사)에서 스텁 메시지 정상. 콘솔 에러 0(회사 8회 전환 포함).
- **미검증 (세션 도구 한계, 코드 결함 아님)**: 이 세션은 Browser pane이 compositing되지 않아(스크린샷 실패) 실제 키보드 Enter 활성화·375px 실뷰포트 픽셀검증을 못 함 — 대조군으로 기존 프로덕션 `.subtoggle` 버튼(PL 재보험 세부펼치기)에 동일 키 이벤트를 보내도 똑같이 무반응이라 세션 전역 한계로 확인(신규 코드만의 문제 아님). 구조적으로는 네이티브 `<button>` + 기존 640px 미디어쿼리 패턴 그대로라 위험 낮음으로 판단하되, 다음 세션에서 실뷰포트 재확인 권장 — `TODO_designer.md` BS-DRILLDOWN 항목에 등재.
- **배포**: 라이브 배포 안 함. validation 게이트가 이 스레드 작성 시점 RED=207(파서 재검증 3라운드째, `inbox/parser/20260813T1330Z…red_round2.md`)이라 RED=0 전까지 목업 상태 유지(발주문 gating 그대로). publishing keep-list 등재·owner 승인 이후 배포 판단 — 그쪽 스레드가 정본.
- inbox `20260813T0422Z__owner__MULTI__aoci_panel_spec.md` / `20260813T0436Z__owner__MULTI__ifrs17_html_placement_decision.md` 둘 다 `status: resolved` → `_resolved/`.

---

## 2026-07-30 — CSM bubble map annual-filer fallback + 전 35사 보조표 (inbox `20260730T0035Z`)

Owner flagged on live: IM라이프·IBK연금보험 missing from the index.html CSM Bubble Map. Root cause was not a data bug — it's disclosure-cadence mismatch: `buildBubbleData()` hard-required a `2026.1Q` row, but 13 of the 35 CSM_waterfall companies only disclose IFRS17 annually (4Q-only), so their `NB_CSM_multiple.json` row for the current quarter doesn't exist. Owner decided: add estimated bubbles (visually distinct) **and** ship a new full-census supplementary table — both, not either/or.

- **`buildBubbleData()` reworked**: census is now the 35 `CSM_waterfall` item-6 (기말 CSM) companies, not the 34 `NB_CSM_multiple` companies (picks up IBK연금보험, which has no NB row at all). For any code missing/incomplete at 2026.1Q, falls back to the most recent quarter with both `신계약CSM_연누계` and `신계약CSM배수_연누계` non-null (Group A, 10사) — `nbCsm` for chart X is that value **÷4** (annualized→quarterly estimate), `multiple` is carried over unscaled (ratio, so ÷4/÷4 cancels). Companies with no valid multiplier anywhere (Group B: 코리안리·IBK연금·BNP파리바카디프 — reinsurer/pension zero premium, or unit-error pending correction) get `multiple:null` and are excluded from the chart but kept in the row set for the table; IBK연금보험's 신계약 CSM has no NB row at all, so it falls back further to `CSM_waterfall` item-2 (신계약 CSM) directly.
- **Bubble chart**: estimated points render as a separate always-legended gray/dashed series (`#adb5bd`, dashed border, label `name+'*'`), tooltip branches to explain the ÷4/이월 provenance + a "2026.1Q 미공시 — 연1회 공시사" warning line. `#bubble-meta` now reports `표시 32社 (실공시 22 · 추정 10)`. `.section-desc` gets one clause explaining the `*` convention. Mobile `#bubble-list` (unaffected data-wise — already used per-company latest quarter) merges its existing off-quarter chip into `추정 <Q>` + muted row text, no duplicate chip.
- **New card — "IFRS17 — CSM 수록 현황 (전 35사)"**: table below the bubble map, reusing the already-fetched `NB_CSM_multiple.json`/`CSM_waterfall.json` (no new fetch, no master-JSON writes). Columns: 회사/구분/기말CSM/기말기준분기/신계약CSM(연누계)/신계약기준분기/NB배수/비고. Sorted 생보→손보, 기말CSM desc within group. Estimated/no-multiplier rows get `var(--muted)` text + left gray border; remark column carries a text chip (실공시/추정 (FY÷4)/직전값 이월/배수 미산출) so the estimate flag isn't color-only (a11y). `<table>`+`<caption class="sr-only">`+`scope="col"` throughout — first table on the site with an explicit caption. KR0075 (비엔피파리바카디프) shows `검증중` in place of every numeric cell — its 기말 CSM (299,584억) is a suspected 100x unit error, corrected separately via `inbox/parser/20260730T0035Z__owner__KR0075_FY2024-FY2025__bnp_cardif_csm_100x_unit.md`; this design-side change is display-only and doesn't touch the master.
- Verified live in Claude Preview: 1280px (32-bubble chart incl. 10 gray-starred, per-sector filter legend behavior, tooltip formatter output for both estimated and real points, all cross-checked against the inbox's own census numbers — every Group A/B figure matched within ±1억 rounding), 375px (merged mobile chip + muted text on all 10 estimated rows, `document.body.scrollWidth === innerWidth` — no horizontal body scroll despite the 640px-min-width table). 0 console errors both breakpoints. `pytest tests/test_deploy_assets.py` — 8/8 pass (no inline data added, no new master write).
- Not touched (owner hard constraints, unchanged): bubble map's 3-axis definition (X=신계약CSM/Y=NB배수/size=기말CSM), headline KPI calc (`업계 총 기말 CSM` still shows the pre-existing 163.8조 pending the KR0075 fix upstream).

---

## 2026-07-30b — CSM 보조표 KR0075 하드코딩 해제 + 전사 카운트 동적화 (배포 전 blocker)

Owner 배포 전 점검: 07-30 작업에서 `renderCsmCoverageTable()`에 `verifying = r.code === 'KR0075'`를 하드코딩해 BNP파리바카디프를 '검증중'으로 계속 가리고 있었는데, parser가 그새 100배 단위오류를 override로 정정 완료(`inbox/parser/20260730T0035Z...` 답변 — CSM_waterfall/NB_CSM_multiple 3개 root 파일 직접 수정)했다. 코드는 정정을 몰라서 정상 데이터를 계속 숨기고 있었다.

- `verifying` 하드코딩 제거. KR0075는 이제 데이터가 자연스럽게 흘러 Group A(연1회 공시 fallback, `estimated:true`)로 census — 기말CSM 2,996억(2025.4Q) · 신계약CSM 128억 · 배수 0.86× · 비고 "추정 (FY÷4)"로 정상 표시. 버블에도 정상 편입(생명 추정 시리즈).
- **부수 발견**: 검증 중 census가 35→36사로 늘어난 것을 확인(`예별손해보험` KR0004가 CSM_waterfall에 신규 온보딩됨, 카카오페이 KR1098 기말 CSM도 3,412억→3.4억으로 크게 변경 — 둘 다 designer 손 밖의 상류 데이터 변경, 조사하지 않음). 카드 제목에 하드코딩돼 있던 "(전 35사)"가 이 때문에 즉시 stale해질 게 보여, `<span id="csm-coverage-count">`로 바꿔 `renderCsmCoverageTable()`이 실제 row 수를 매번 채우도록 동적화(재발 방지, 요청 범위 밖이었지만 동일 카테고리의 명백한 버그라 즉시 수정).
- 재검증: file:// 프리뷰(로컬 http 서버 포트 충돌로 file://로 확인 — fetch는 정상 동작) — KR0075 정상 표시, 카운트 36 표시, 콘솔 에러 0. `pytest tests/test_deploy_assets.py` 8/8.
- 카카오페이/예별손해보험 수치 변경은 designer가 임의로 판단하지 않음 — 표는 마스터를 그대로 반영할 뿐이고, 데이터 정합성은 parser/validation 소관.

## 2026-07-30c — CSM 보조표 숨김 (owner: 디자인이 추함)

Owner 채팅 피드백: "수록현황 테이블은 안보여주는게 낫겠는데? 너무 추하게 생겼어" — 제거 vs 숨김 vs 재디자인 중 확인, owner는 **숨기기(코드 보존)** 선택.

- `#csm-coverage-panel`에 `display:none` 인라인 추가(주석: 재노출은 이 style 제거만 하면 됨). `renderCsmCoverageTable()`/`buildBubbleData()` Group B 로직/CSS는 전부 보존 — 렌더는 계속 돌지만 화면엔 안 보임(36행 빌드, 콘솔 에러 0 확인).
- 버블맵(추정 버블 + 범례 + 툴팁)은 이 피드백과 무관 — 그대로 노출.
- 재노출 시 할 일: `display:none` 제거 + (owner가 "추하다"고 한 원인 — 8컬럼 plain table, 36행, 칩 스타일 — 를 실제로 다시 디자인할지 owner와 논의 필요. 이번엔 숨김만 요청받았고 재디자인은 안 함).
- `pytest tests/test_deploy_assets.py` 8/8.

---

## 2026-07-22 — Treemap red→blue reverted (owner: finviz identity outweighs this gap)

The 07-21d commit (`2e4f114`) changed `index.html`'s `colorForRatio()` below-threshold color from red (`hue=0`) to blue (`hue=215`) for colorblind distinguishability, plus the `.swatch` legend gradient and `legendThresh` caption text. Owner objected on sight: the site's treemap ("K-ICS Market Map") deliberately mirrors finviz.com's red/green market-map convention — that's an intentional brand/identity reference, not an unexamined default, and changing it wasn't worth the tradeoff.

- Reverted `colorForRatio()` (both `kics` and `basicCapital` branches) to `hue=0` red, `.swatch` gradient back to the red stops, `legendThresh` caption back to "빨강 <...". Left a one-line code comment pointing at this decision + `docs/a11y_baseline.md` #12 for a future reader.
- **Not reverted** (owner had no objection to these): `--muted`, bubble-legend green, missing-data placeholder gray, IFRS17 `NB_LINE_COLORS` palette, active-tab underline — all 4 other 07-21d items stay as committed.
- `docs/a11y_baseline.md` §2b row #12 updated to record the revert + the reasoning: this was never a hard WCAG 1.4.1 fail to begin with, since every treemap/list cell already renders its ratio as on-cell numeric text in addition to color (not color-only) — so colorblind users aren't blocked, just without the color as a redundant fast-scan cue. Documented as an accepted exception rather than a fix.
- Verified: `git diff` scoped to exactly the 4 reverted lines/comment (no other lines touched), Playwright regression harness re-run clean.

## 2026-07-21d — A11y owner-review queue resolved (owner: "do it now")

Owner gave explicit sign-off to finish the 5 color/contrast items the 07-21 audit had left in the owner-review queue (deferred per `common.css`'s own "don't change token values without sign-off" rule), plus the related active-tab color-only gap. All verified with `scripts/a11y_contrast_check.py` before touching any file — no eyeballed colors.

- **`--muted` (#7)**: already sitting uncommitted in the tree from before this session (`#6c757d`→`#6a737a`, 4.83:1 white / 4.58:1 `--card`) — kept, now committed together with the rest instead of left in limbo.
- **Bubble-legend green (#8)**: `index.html` `● 손해` label `#16a34a`(3.30:1)→`#15803d`(5.02:1). Left `SECTOR_COLOR['Non-Life']` (the actual dot fill) untouched — that's a graphic at the 3:1 threshold, already fine; only the text label needed it.
- **Missing-data placeholder (#9)**: `#adb5bd`(2.07:1)→`#6a737a` in `K-ICS.html` (donut center) and `IFRS17.html` (senTable ×2) — same value as the fixed `--muted`, for consistency rather than inventing a third gray.
- **`NB_LINE_COLORS` (#11)**: tried a minimal 2-color swap first (only replacing the two flagged clashing colors) — kept re-introducing new clashes elsewhere in the 6-set every time, because colorblind confusability isn't pairwise-decomposable for 6 hues. Ran a proper search instead: varied hue AND lightness, used one near-neutral gray slot (grays are trivially distinguishable from any hue regardless of colorblindness type). Final: `["#0d6efd","#009E73","#D55E00","#343a40","#CC79A7","#7c5c00"]` — global worst pair ΔRGB 82 across both protanopia/deuteranopia (was 39), all 6 colors ≥3:1 on white.
- **Treemap/bubble diverging scale (#12)**: the real fix target was the *live* `colorForRatio()` function (both `kics` and `basicCapital` branches), not just the CSS `.swatch` legend gradient — the function drives the actual per-cell HSL color. Below-threshold `hue=0`(red)→`hue=215`(blue); above-threshold green unchanged. Mobile list bars reuse the same function so they inherit the fix with no separate edit. Updated the `.swatch` gradient stops and the `#legendThresh` caption text ("빨강"→"파랑") to match. Verified blue↔green endpoints at ΔRGB 63–122 under simulation (was ~1.1–1.3:1 *contrast ratio*, i.e. nearly invisible, for the old red↔green). **Found in passing**: `.bubble-swatch` CSS class is dead code — defined, never referenced in any page's markup (the bubble chart colors dots by sector via `SECTOR_COLOR`, unrelated to this gradient) — left alone, noted so it isn't mistaken for a live issue later.
- **Active-tab color-only (#13)**: `common.css` `.tab.active{text-decoration:underline;...}` — additive, no color touched.
- **Verified**: local static-server preview, all 3 pages (index/K-ICS/IFRS17), 0 console errors. Confirmed computed values directly: legend text color, swatch gradient stops, `legendThresh` caption wording, `colorForRatio(90,'kics')` returns `hsl(215,...)` (blue) at the browser console, active-tab `text-decoration-line:underline` on K-ICS.html.
- **Not touched**: item #10 (teal/pink pair) needed no change — already compliant. Item #14 (static aria-labels) stays open — it's a content-authoring task (live-region/per-render text summary), different category from a color fix.

## 2026-07-21c — J-ESR MVP reverted, hold confirmed until 2026-09/10

Built the 07-21b page below (verified working, 0 console errors), then owner corrected: J-ESR had been put on hold entirely (not just the full-coverage/treemap upgrade — the display MVP too) pending the EDINET 有報 window (~2026-09/10), when individual-company data actually becomes meaningful. That hold decision predates this session but wasn't findable anywhere in the repo (inbox, TODO, memory) when checked — it was made in a session that never persisted it. Removed `J-ESR/index.html`. Kept the `.gitignore` addition (`J-ESR/edinet_key.txt`, `J-ESR/raw/`) since it's an unrelated secret/bloat safeguard, not part of the reverted display work. Saved `project_jesr_scope_timing` memory so this doesn't repeat. **Next J-ESR designer work should not start without an explicit fresh go-ahead, regardless of what any existing inbox thread says** — the 06-24 order reads as still-active but is superseded by this hold.

## 2026-07-21b — J-ESR MVP: 일본 보험그룹 ESR 현황 페이지 (inbox `20260624T0337Z`, resolved after ~1 month stale) — reverted same day, see 07-21c above

3건의 designer inbox 스레드(0113Z 트리맵 발주→0337Z로 superseded, 0337Z 재발주, 0600Z parser 데이터 핸드오프)가 2026-06-24부터 미착수 상태로 남아있던 것을 발견 및 처리.

- **신규 `J-ESR/index.html`**: Korea 4-페이지와 분리된 독립 화면(owner 하드 제약 — index.html/K-ICS.html/IFRS17.html 무변경). `../common.css` 링크로 토큰/브랜드만 재사용, 자체 헤더(한국 대시보드로 돌아가는 링크 1개)만 추가.
- **트리맵 아닌 카드+랭킹바**: 최초 발주(0113Z)는 Korea index.html 마켓맵과 동형 트리맵이었으나, owner가 데이터의 as-of 혼재(2025.3~2026.3 짬뽕)를 이유로 재발주(0337Z)하며 트리맵을 취소하고 카드/랭킹바로 변경 지시. `jesr_master.json`(parser 0600Z 산출, 11개사 records)을 fetch해 `as_of_consistent` 플래그로 3구간 분리 렌더:
  - **2026.3末 확정 (4사)**: 東京海上238%·MS&AD214%·Sompo270%·ソニーFG177% — 서로 비교 가능 명시.
  - **직전분기 참고 (5사, dim 처리)**: 第一生命213%(25.12)·日本生命224%(25.3)·住友生命184%(25.9)·明治安田216%(25.3)·富国260.9%(25.9) — "위 구간과 직접 비교하지 마십시오" 문구로 오해 차단.
  - **공표대기 (2사)**: T&D·かんぽ生命 — 감사 미완료, placeholder 카드.
- **카드 구성**: ESR% 큰 숫자 + 미니 바(구간 내부만 시각 랭킹, 구간 간 비교 유도 안 함) + basis 라벨(新J-ICS, mutual 5사는 `_preliminary` 시 "(예비치)" 표기) + entity_type 배지(그룹(연결)/상호회사) + `yoy_change_pp`(음수 △, 양수 +, samo 관례 재사용) + provenance 출처 링크(각사 決算短信/決算説明資料 원문 URL).
- **데이터 정직**: 상단 캡션 "그룹 연결 기준 · 개별사 전수는 2026.10 EDINET 有報 후" + fetch 후 실시간 커버리지 카운트("수록 11社 (확정4·참고5·대기2)"). `所要資本`/`적격자본` 전항목 null(parser 핸드오프 명시 한계)이라 도넛/컴포넌트 분해는 시도하지 않음 — ESR% 단일값만.
- **10월 확장 훅(미구현, 주석만)**: JS에 "as_of_consistent 커버리지가 넓어지면 이 카드그리드를 index.html 패턴의 RecSplit 트리맵으로 교체" 코멘트. 렌더 로직이 이미 fetch→filter→section 구조라 개별사 전수 추가는 HTML 무변경으로 흡수 가능(append-friendly).
- **검증**: 로컬 static 서버(`python -m http.server`)로 `J-ESR/index.html` 프리뷰. 콘솔 에러 0. `get_page_text`로 11개사 전부 올바른 구간(4/5/2)·라벨·출처링크 확인. 375px 모바일: `grid-template-columns` 1열로 축소, `scrollWidth===clientWidth`(가로 스크롤 없음) 확인.
- **inbox 정리**: 3건 전부 `## 답변` 작성 후 `_resolved/`로 이동(0113Z superseded-note, 0337Z 구현상세+페이지경로+11社 카운트, 0600Z 데이터 소비 확인). 세 파일 모두 애초 git 미추적 상태(커밋된 적 없음)였음 — 단순 파일 이동으로 처리, TODO_designer.md 업데이트만 커밋 대상.

## 2026-07-21 — A11y baseline + audit (inbox `20260721T0233Z`)

Owner backlog item: adopt an A11y audit baseline (owner had reviewed 42 external skills, flagged `ui-ux-pro-max` as a fit for the designer stage's long-open A11y TBD). Went local instead — see rationale in `docs/a11y_baseline.md` §4.

- **Baseline formalized**: WCAG 2.1 AA, table + method in `docs/a11y_baseline.md`. New tool `scripts/a11y_contrast_check.py` (WCAG contrast ratio + protanopia/deuteranopia colorblind simulation via linear-RGB matrix transform — replaces hand-computed contrast checks). New local skill `.claude/skills/a11y-audit/SKILL.md` for repeat audits.
- **Audited all 4 deployed pages** (index/K-ICS/IFRS17/공시보고서): color contrast (every `common.css` token + page-local literal used as text), chart-palette colorblind confusability (K-ICS teal/pink pair, IFRS17 `NB_LINE_COLORS` 6-color palette, index treemap/bubble diverging gradients), keyboard access (every `addEventListener('click')` on a non-native element), focus visibility, form-label association.
- **Fixed (purely additive, no rendered-value change)**:
  - `index.html` treemap cells (`.cell`) and mobile list rows (`.li-row`) were click-only — the page's primary company→detail navigation had **no keyboard path** (WCAG 2.1.1 fail). Added `tabindex="0"`, `role="link"`, `aria-label` (reused the existing `title` text), and a `keydown` handler (Enter/Space → same nav as click).
  - `index.html` custom toggle switch hides its real `<input>` at 0×0/opacity:0, so the sitewide `:focus-visible` ring landed on an invisible element (WCAG 2.4.7 fail). Added `.toggle-input:focus-visible + .toggle-label{outline:...}` targeting the visible label.
  - `공시보고서.html` was the one page **not** linking `common.css` — meaning it had no focus-visible ring and no `prefers-reduced-motion` handling at all. Added the `<link>` in the same position as the other 3 pages.
  - 10 chart `<canvas>`/ECharts `<div>` containers (K-ICS ×5, IFRS17 ×5, index ×2) had no `role`/`aria-label` for screen readers. Added `role="img"` (`role="group"` for the treemap, since its cells are now focusable link children) + concise Korean labels.
  - Active-tab links (`K-ICS.html`, `IFRS17.html`, `공시보고서.html`) had no `aria-current` — screen readers couldn't tell which page is current beyond the color-only highlight. Added `aria-current="page"` alongside the existing `active` class (`index.html` has no self-referencing tab, no change needed there).
- **Owner-review queue** (touches an existing rendered value — owner-gated per `common.css`'s own token-value rule, not auto-fixed): `--muted` on `--card` = 4.45:1 (just under AA); index.html bubble-legend bold-green text = 3.30:1; `#adb5bd` "no data" placeholders = 2.07:1; IFRS17 `NB_LINE_COLORS` has 2 pairs (orange/red, purple/teal) that get close under deuteranopia/protanopia simulation; index.html treemap/bubble red↔green diverging scales lose contrast under the same simulation (partially mitigated — every cell already has a tooltip + on-cell numeric label). Full detail + severities in `docs/a11y_baseline.md` §2b.
- **Corrected a stale doc note**: the previous `#ff9f40` medium-confidence-badge gap in `claude-agent-designer.md` §5.3 no longer applies (literal isn't in any of the 4 pages anymore) — removed.
- **Verification**: Claude Browser preview, all 4 pages, 1280px + 375px — 0 console errors. Dispatched `Enter` keydown on a treemap cell → navigated to `K-ICS.html?company=...` exactly like a click. `공시보고서.html` confirmed loading `common.css` with `:focus-visible` rule present.
- **Docs**: `docs/agents/claude-agent-designer.md` §5.3 rewritten to point at the new baseline doc + skill instead of the old ad-hoc gap list.

## 2026-06-20

- **도넛 섹션 잠정 숨김** (inbox 20260620T0238Z): K-ICS.html `#donut-section-panel`(`<div class="panel">` L156)에 `style="display:none"` 추가 — 자본성증권 소진율 도넛 2개(tier1·tier2) 분자 오류로 잠정 비표시. 마크업/JS/데이터 보존, 복구는 `style` 제거만으로 가능. inbox resolved.
- **차트 공통 테마** (DESIGN-V2 P2): Chart.js `Chart.defaults` 전역 설정 — Pretendard 폰트·그리드 `#e9ecef`·틱 `#6c757d`·tooltip dark(rgba 33,37,41,0.9). IFRS17.html ECharts에 'iq' 테마 등록(`echarts.registerTheme`) + `echarts.init(el,'iq',...)` 적용. K-ICS.html Chart.js 동일 defaults 적용.
- **MOB-KICS 카드뷰** (MOB-KICS): K-ICS.html 피벗 테이블 → 모바일(≤640px)에서 분기별 카드 렌더. `renderMobileCards(data,quarters,displayOrder)` 신설 — 분기=카드헤더, 항목=행(`kics-mob-row`), 서브아이템 들여쓰기(`sub`). `renderPivotTable`에서 `window.innerWidth<640` 분기 후 early-return. 기존 데스크톱 테이블·subtoggle 로직 무변경.
- **KPI 카운트업 애니** (DESIGN-V2 P2 완결): `countUp(el,target,fmt,dur)` — ease-out cubic, 600ms, `prefers-reduced-motion` 즉시세팅. index.html 히어로 KPI 3개(총CSM·지급여력비율·수록사), IFRS17.html Panel 7 KPI 4개(기말CSM·상각·신계약CSM·NB배수) 적용. text-only KPI(기준분기)는 미적용.
- **검증**: Playwright 29/29 GREEN.

## 2026-06-16 (W1) — 워터폴/PL 기간 윈도잉 통일 + 롯데 PL 부호버그 + inbox 정리 (owner 직접)

- **inbox 정리**: answered designer 9건 전부 `inbox/_resolved/`로 이관(designer inbox open 0).
- **기간 윈도잉 통일**(owner): CSM waterfall(P1) + PL table(P4)을 시계열과 동일 규약으로 — **분기=최신 포함 직전 5분기**(WF_Q_WINDOW 6→5), **연도=연말+최신부분 [2023,2024,2025,2026.1Q]**. PL table은 `selectPeriods` 재사용(plTablePeriods 재작성, 연도 라벨 FY2025→2025로 시계열과 정합).
- **롯데손보 PL waterfall 투자손익 부호버그**: 데이터는 정상(투자손익 -55,656백만=△557억, 보험손익 +27,200). 원인=**zero-crossing 렌더버그** — 스택+투명 placeholder 방식이 0선 넘는 step에서 placeholder(음수)/colored(양수)를 양·음 따로 쌓아 빨강 막대가 0 위로 뜸. **fix=ECharts custom renderItem**으로 막대를 [min,max] 직접 그림(`encode:{y:[0,1]}` 자동 스케일). 보험손익(+0.03) → 투자손익이 0선 아래로 뻗어 끝점 △0.03. (CSM waterfall은 CSM이 stock이라 0선 안 넘어 무영향.)
- **검증**: Playwright 하니스 +5 assert → **29 GREEN**(롯데 geo y0>0>y1, 윈도잉 분기5/연도4, 콘솔0).

## 2026-06-16 (G1/G2) — IFRS17 +버튼 SOT 통일 + 점선 시리즈 legend 명시 (inbox 20260616T0506Z)

owner 라이브 QA. master 무수정, 확정결정 4개 유지.

- **G1 재보험 +버튼 → K-ICS `.subtoggle` 미러**: `.subtoggle`(+:hover)를 **common.css로 승격**(§5 공유 컴포넌트), K-ICS inline 제거. IFRS17 장기재보험 토글을 inline-styled `<span>＋`→**`<button class="subtoggle">`**(동일 마크업·`+`/`−`·`aria-expanded`·이름 뒤). 양 페이지 단일 SOT.
- **G2 점선 시리즈 식별**: Panel 2 dataset 라벨을 코드주석→legend로. 실선="기말 CSM 잔액 (실선)", 점선="**신계약 CSM 시계열 (점선)**". legend·tooltip 동일, 스와치 색만으론 못 가리던 점 해소.
- **검증**: Playwright 하니스에 G1/G2 assert 추가 → **24 GREEN**(fresh 브라우저). ※preview는 구 common.css 캐시로 +버튼이 처음 UA버튼처럼 보였으나 fresh 로드 정상.
- **배포 주의**: common.css 갱신 시 재방문자 캐시로 신규 공유 규칙이 안 먹을 수 있음 → 배포 `common.css?v=<hash>`/캐시헤더 무효화 필요(publishing/owner).

## 2026-06-16 (DS1b + DS2) — index 히어로 KPI/typeahead + Playwright 회귀 하니스

owner "둘 다 진행" 지시로 DESIGN-V2 P2 슬라이스 + DS2를 함께 착수.

- **DS1b — index 히어로 KPI 스트립 + 회사 typeahead** (DESIGN-V2 P2): `index.html` 상단에 KPI 4카드(**업계 총 기말 CSM 163조 · K-ICS 지급여력비율 중위값 193% · 수록 39社 · 기준 2026.1Q**) — 로드된 데이터에서 산출(`updateKpis`/`totalClosingCsmEok`=wfRowsRaw 항목6 최신 합, `_median`=GROUPED ratio). degrade-safe 진입 애니(`@keyframes riseIn` + reduced-motion 무력화). 회사 typeahead(input+datalist 39사 → IFRS17.html?company= 점프). 토큰 사용, 팔레트/△ 유지, 모바일 2열. 검증: KPI 채움·datalist 39·콘솔0.
- **DS2 — Playwright 회귀 하니스**(`webapp-testing` skill): venv에 playwright+chromium 설치(번들 Chromium 이 머신 구동 OK = Edge/Chrome `--dump-dom` 0바이트 우회). `tests/regression_dashboards.py`(+README) — `with_server.py` 서버 자동관리(좀비0), 데스크탑 뷰포트로 윈도잉까지 assert. **22 assert GREEN**: index(KPI·typeahead·트리맵) / K-ICS(드롭다운≥40·**KB 2026.1Q=185.87**) / IFRS17(as-of·shock↑↓·△·연도[2023,2024,2025,2026.1Q]·분기 last5·미제공사·각 페이지 콘솔0). 캔버스 텍스트(도넛 100%+)는 visual-only 갭으로 문서화.

## 2026-06-16 (DS1) — frontend-design skill 도입: 디자인 시스템 + common.css 추출 (inbox 20260616T0036Z) + KB 렌더 버그

**frontend-design 스킬**로 designer 프롬프트 TBD(디자인 시스템/common.css/A11y/차트 규약)를 정식화. 무파괴(extraction, not redesign) — common.css 값 = 기존 렌더값 1:1. 정찰 Workflow(4 병렬 read-only)로 3파일 토큰 인벤토리 + KB 버그 위치 선행 파악.

- **신규 [`common.css`](../common.css)**: `:root` 토큰(surface/ink·brand·금융 status·type·spacing 4px·radius·misc) + 공통 chrome(body/header/.brand/.tabs/.tab/.container/.select/.panel h2·p/table base/.num·.small-muted·.muted) + A11y(`:focus-visible`·`prefers-reduced-motion`). 22 규칙.
- **3 HTML 배선**: `<link href="common.css">`를 inline `<style>` **앞**에 추가 + 각 inline에서 중복 chrome/토큰 제거. 페이지 고유(.panel/.controls 여백·`*{box-sizing}`·차트/컴포넌트 클래스·@media)는 유지. index `.tab` cursor:pointer는 common으로 흡수.
- **`*{box-sizing}` 미추출**: 3파일 모두 보유하나 hoist 시 레이아웃 시프트 위험 → 페이지 inline 유지(보수적).
- **프롬프트 §5 정식화**: `docs/agents/claude-agent-designer.md` skeleton→정식(토큰표·extraction contract·A11y 갭·차트 규약 legend밀도/donut stack/모바일 scope·확정결정 4개 잠금). status 라인·assets/ 경로 참조 정정.
- **KB 2026.1Q 렌더 버그 수정** (parser inbox 20260616T0050Z): 원인=분기축 아님, **항목명 라벨 변형 정확매칭**. `K-ICS.html` line 491이 긴 형 `'다. 지급여력비율 : 가 ÷ 나 × 100'`만 매칭 → 2026.1Q 짧은 형 `'지급여력비율'` 쓰는 ~15사 누락(undefined→null). 두 변형 OR 매칭으로 수정. 데이터 정상(파서 책임 아님).
- **검증(무회귀, preview 라이브)**: 3페이지 reload — common.css 200·콘솔 0·computed style 일치(데스크탑 베이스=common, 모바일 @media override 동작)·index treemap 39행. 확정결정 4개 유지(삼성생명 senTable △/↑/정수). KB 분기차트 2026.1Q=185.87% 표시 확인.
- **DS2(webapp-testing/Playwright)**: 미구현, 가부 회신 — flaky한 건 Edge headless(0바이트)지 preview 아님(이번 세션 정상). Playwright는 28건 회귀 자동화·서버 라이프사이클로 가치 有 → 스모크 spike 후 전용 세션 권장.
- **배포 주의**: common.css는 신규 루트 에셋 → HTML과 함께 배포 필요(publishing handoff).

## 2026-06-16 — owner 라이브 QA 3차 10건 (inbox 20260616T0007Z) — IFRS17.html

전부 표시 레이어만(master·viz JSON 무수정). 검증: node --check + 헬퍼 단위테스트(fmtShock·asOfFromRcept·samo·selectPeriods) + preview 라이브(삼성생명·현대해상·아이엠라이프, 콘솔에러 0).

- **D1 민감도 as-of**: `asOfFromRcept(rcept_no)` 추가(제출월→기준일: 1~4월=직전FY 12-31 등). senCap "기준: FY2024 사업보고서 (2024-12-31)". 긴 공시각주는 표 아래 muted 보존. 데이터 `period`/`as_of` null → parser inbox `20260616T0030Z`로 정식 채움 요청(`senAsOf()`가 as_of 채워지면 우선 사용).
- **D2 fmtShock 보강**: 부호·방향어 없는 선두 "3.27%"(상향충격)→**3.27%↑**, ^▲→↑ ▼→↓. 삼성생명 케이스(bare %) 해결. 검증 "3.27%↑/↓"·"3.40%↑(실손보상 2.62%)".
- **D3 억 정수 반올림**: senTable `fmtNum(,0)`(0||2=2자리 버그) → **`samo()`**(정수+△). △3,765 / 3,298.
- **D4 현대해상 키컬러**: `#FFB81C`(노랑) → **`#F47920`**(주황). 스와치 rgb(244,121,32).
- **D5/D6 분기 미제공사**: `wfHasQuarterly()`(4Q-only=데이터기반 11사: AIG·아이엠라이프·하나생명 등). 분기 모드 → Panel1 waterfall 생략+"분기 공시 미제공" 메시지(D5), Panel2 시계열은 연 fallback(D6).
- **D7 축 윈도잉**: `selectPeriods()` — 시계열(hist)·NB배수(nb): 분기→직전 5분기, 연도→연말(4Q→연표기)+최신부분(2026.1Q). 검증 연도=[2023,2024,2025,2026.1Q], 분기=[2025.1Q..2026.1Q].
- **D8 상각 5년캡**: bucket 'y5_plus(5년 초과)' 제거 + yearly maxY 10→5. amort=[1년이내,1–3년,3–5년].
- **D9 모바일 1시점**: ≤640px → waterfall 최신 1버킷, 시계열 최신 1시점. 0폭 preview에서 hist 1시점 확인.
- **D10 로딩 경감**: boot 9 JSON 직렬 await → **Promise.all 병렬** + "데이터 불러오는 중…" 힌트. (지연 원인=직렬 9 round-trip.)

## 2026-06-14 — owner 라이브 QA 글리치 4건 (inbox 20260614T0712Z)

디자이너-단독 4건 처리, 상류대기 3건 ack. (전체 내역 = inbox 답변 섹션.)

- **(1) K-ICS 드롭다운 누락사** [G3 코리안리/G9 예별]: `K-ICS.html` 데이터로드 `.then`에 `fillMissingCompanies()` — 하드코딩 옵션에 없는 `kics_disclosure.json` 원수사명 전부 append(가나다순). JS 선택로직이 `select.options` 검색이라 옵션 추가만으로 선택됨. 옵션 30→48. 코리안리·예별·AIG손해보험·카카오페이손해·서울보증·교보라이프플래닛 선택 가능 확인. (완전 데이터화는 curated 순서 보존 위해 P2 보류.)
- **(2) 현대해상 키컬러** [G5]: `IFRS17.html` KEY_COLORS `#008A3E`(초록)→**`#FFB81C`**(노란주황, owner값). 밝은색이라 line 32% 보정 자동. 스와치 rgb(255,184,28) 검증.
- **(3a) ΔCSM 헤더** [G4a]: IFRS17 sens 테이블 "ΔCSM(백만)"→**"ΔCSM(억원)"** (라벨만).
- **(3d) 모바일 테이블**: IFRS17 `@media≤640` th/td `4px 6px`→`6px 8px` + `.num{white-space:nowrap;word-break:keep-all}`.
- **상류대기 ack**: (3b) shock 표기 통일·(3c) 흥국 행분리 → parser/ifrs17 `sensitivity_heatmap.json` 단계. (4) 기본자본 소진율 100%+ → publishing tier1 overflow→tier2 데이터 대기(donut >100% 표현 위해).
- 검증: Edge headless dump, JS에러 0.

### 2026-06-14 (후속) — sensitivity 3b/3c 매듭 + 푸본현대 parser handoff
- **(3c) 흥국 행분리 = parser 완료 확인**: `sensitivity_heatmap.json` 흥국생명 6행 정리됨(product line 혼입 제거). designer 표 정상.
- **(3b) shock 표기 = designer display 파싱으로 완료**: parser가 자유텍스트 유지 → IFRS17 `fmtShock()` 추가(top-level helper, sens 렌더 line ~955 적용). `{n}% 증가/상승`→`{n}%↑`, `{n}% 감소/하락`·`(-){n}%`→`{n}%↓`. 복합/비정형 원문보존. 검증: 흥국 3.27%↑/↓·9.16%↑/↓·2.62%↑ /0.26%↑.
- **푸본현대생명보험(KR0083) 오파싱 발견 → parser reparse 요청**: sensitivity_heatmap 엔트리가 "기말 보험계약부채" 잔액표 오추출(shock=금액). inbox `20260614T1140Z__designer__KR0083_FY2024__sensitivity_misparse.md`. (display 레이어로 불가 = 추출단계 정정 필요.)

### 2026-06-15 — inbox 2건: 도넛 실값 병기 + CSM null="—"
- **owner 0435Z(도넛 100%+)**: 정식 발주 = 직전 구현분과 동일. 추가로 owner "실값 병기 권장" 반영 → 툴팁 "100%+ (실제 242.5% · 발행액이 인정한도 초과)". status answered.
- **parser 1300Z(CSM null 렌더)**: 동양생명·NH농협손해 `csm_delta=null`(미공시)이 senTable에 "0"으로 렌더(Number(null)=0 버그) → **null이면 "—"(회색)+title** 처리, pl_impact도 동일. 마스터 무수정(표시레이어). 검증: 동양생명 CSM 전부 "—". status answered.

### 2026-06-14 (후속3) — 기본자본 소진율 "100%+" 표기 (owner 결정 복원)

owner가 **(4) 도넛 100%+ 표기를 복원** 지시. 직전 "구현 취소"(publishing artifact 논거 수용)는 **번복**.
- owner 논거: 분자(신종자본증권 발행액)=KOFIA, 분모(인정한도)=공시 → 독립 소스라 사전 100% cap은 애초 불가능. 계산값 >100%면 그냥 "100%+"로 표기하는 게 정당(현재 raw "242%"가 더 오도, `Math.min`은 원호만 캡할 뿐 중앙 숫자는 raw 노출 중이었음).
- 수정(`K-ICS.html` renderDonut): 중앙 텍스트 `pct>100 ? '100%+' : pct.toFixed(0)+'%'`, 툴팁 사용 `'100%+ (인정한도 초과)'`. 원호 `Math.min(...,100)` 캡은 도넛 360° 한계라 유지. 색(>=100 빨강) 유지.
- publishing artifact 주장과 무충돌: "100%+"는 정확값(242.5%)을 안 박아 artifact 오도도 회피. 파서가 excess 추출해 ≤100% 되면 자동으로 정상 %로 표시됨(분기 없음).
- 검증: canvas 중앙 텍스트는 DOM 미노출 → 코리안리 도넛 렌더+인정>한도(>100% 경로) 확인, 실제 "100%+" 글자는 배포 후 육안.

### 2026-06-14 (후속2) — 민감도 stub 마감 + (4) 도넛 보류 확정
- **parser 푸본현대 수정 확인(resolved)**: `_has_shock_rows` 가드로 푸본현대·KB손해·미래에셋·신한을 `status="partial"`(scenarios=[]) 재분류 — ±shock 행 없는 롤포워드 오태깅. KR0083 thread resolved→`_resolved/` 이동.
- **민감도 stub 사용자친화화**: IFRS17 Panel 6 stub "status=partial / 시나리오 없음"(개발자스러움) → "% 충격 기준 보험위험 민감도표가 없는 보험사입니다."(없는 회사) / "민감도 데이터 미수록 보험사입니다."(엔트리 자체 없음). 검증(Edge dump): 푸본현대·KB손해 깔끔한 stub, "7,095,833" 금액 노출 사라짐.
- **(4) 기본자본 소진율 100%+ 도넛 = 구현 취소(보류 아님)**: publishing 조사 결론(`20260614T1141Z__publishing__...`) — tier1 100%+는 Ⅴ.1 excess 파싱누락 artifact, 파서가 excess 추출하면 정의상 ≤100% → `K-ICS.html` `Math.min(...,100)` 캡이 정답. "XXX%+ 도넛+overflow 툴팁" 미구현. designer 액션아이템에서 제거(publishing 재통지 시 재개). 메시지 answered.

## 2026-06-13 — 트리맵 색 임계 앵커 130/200% + 민감도 권고선 정합 (TREEMAP-SCALE, inbox 🟠4)

권고선 = **130%** (owner 확정 2026-06-13; 기본자본비율은 50%). 사이트 메인 K-ICS 차트들은 모두 "지급여력비율 130% 기준선"을 씀.

- 트리맵 `colorForRatio` K-ICS 피벗 = **130%**(원래 값 유지) + **200% 충분 영역**(200%+ 더 진한 초록, 시각용 앵커). <130%는 빨강(미달 깊이 ∝ 채도). 매끈한 발산 그라데이션 → 권고선 기준 빨강/초록 + 200 캡으로 의미 강화.
- 범례 `#legendThresh` 임계 표기 신설: kics "(빨강 <130% 권고미달 · 초록 ≥130%, 200%+ 진함)", basicCapital "(빨강 <50% · 초록 ≥50%)". 모바일 리스트 막대도 동일(같은 함수).
- **정합 수정**: 내가 2026-06-11 만든 K-ICS 금리민감도 패널의 권고선이 `감독당국 권고 150%`로 박혀 있어 사이트 표준(130%)과 불일치 → **130% 기준선으로 정정**(메인 차트 문구와 통일). (작업 중 treemap을 150%로 잠시 바꿨다가 owner 정정으로 130% 환원.)
- basicCapital 50% 피벗은 미변경.
- 검증(Edge headless dump): 130~135% 회사(IBK연금·롯데·하나)가 초록, <130%는 빨강 경계 확인, legendThresh "130%" 렌더.

## 2026-06-13 — IFRS17 회사 키컬러 적용 (KEYCOLOR-V1, owner hex 확정)

화이트 베이스 유지 + 회사 선택 시 키컬러를 **차트 데이터잉크 + select 액티브에만** (배경·패널보더 무채색 유지 — 절제). owner 2026-06-12 hex 확정분 반영.

- `KEY_COLORS` 맵(표시명 키) + `keyColorOf(coName)` → `{primary, line, soft, fill, isKey}`. 미등록사 = 기본 블루(#0d6efd) fallback. 모듈 스코프 `currentKey`를 `renderCompany`에서 회사별 설정, 각 렌더가 읽음.
- **WCAG 보정**: relative luminance > 0.45(KB 옐로우 등)는 선/축용 `line`을 32% 어둡게 (#FFBC00 → rgb(173,128,0)). 막대 면적엔 원색.
- 적용: Panel1/4 워터폴 total 막대 → primary (증감 초록/빨강 의미색 유지), Panel2 기말선+좌축+그리드 → line/soft, Panel5 연누계선 → line/soft, Panel3 상각막대 → fill(0.6). select 보더+box-shadow 링 + 회사명 옆 키컬러 스와치.
- 확정 팔레트: 삼성#1428A0·한화#F37321·신한#0046FF·KB#FFBC00·NH#00A05E·미래에셋#F58220·DB#0E8C3A·현대해상#008A3E·교보#0B5D52·메리츠#E60012·롯데#DA291C. 그린 4곳은 회사 1곳씩 보는 화면이라 충돌 없음.
- 검증(Edge headless dump): 삼성생명 스와치 rgb(20,40,160)=#1428A0, select 링 네이비, JS에러 0, 패널 렌더 정상. 캔버스 색 자체는 DOM 미노출 — 스와치/링이 같은 currentKey라 차트도 동일 색. (K-ICS 재사용은 공통 js 추출 P2 과제, 이번 패스는 IFRS17만.)
- Report: `artifacts/designer/ifrs17_keycolor_v1_20260613.md`.

## 2026-06-12 — 음수 △ 전면화 + K-ICS 위험액 하위 토글 + P1 퀵윈 + 버블 로그축 (4페이지)

- **음수 △ 표기 (사용자 최우선 지시)**: IFRS17 `fmtNum`·워터폴/PL 축라벨·total 라벨·시작/끝 툴팁, K-ICS `sensFmtDelta`('−'→'△')·forward note Δ·민감도표 `fmtAmt`/`fmtPct`. (K-ICS `formatNumber`/`fmtEokInt`, IFRS17 `samo()`는 기존 적용분.) index는 음수 노출 지표 없음.
- **K-ICS 하위위험 토글**: 17(생명장기)→29~35(1-1~1-7), 19(시장)→36~40(3-1~3-5) 기본 접힘 + 항목명 옆 +/− 버튼(`.subtoggle`). `SUB_ITEM_NUMS` → `RISK_SUB_GROUPS`+`subOpen`(재렌더에도 상태 유지). 36~40은 기존 미표시 데이터 — 신규 노출.
- **P1 퀵윈 (4페이지)**: Pretendard Variable(dynamic subset CDN)+`tabular-nums`, IQ 파비콘(data-URI SVG, 잉크색 중립), meta description/OG, 공통 푸터(출처·기준분기·면책·△ 관례), 이모지 제거(📊/🚧). **팔레트 교체는 보류** — 사용자 피드백("남색이야말로 AI스럽다") 반영, 방향 재논의 중.
- **index 버블 겹침 보정**: X축 linear→**log10**(좌측 군집 해소), 라벨 흰 외곽선(textBorder 2px), labelLayout moveOverlap:shiftY + labelLine, nbCsm>0 필터(log 요건).
- **index 회사 약칭 + 트리맵 꼬리 '기타' 묶음 (사용자 지적)**: `shortName()`(접미사 룰: 화재해상보험→화재, 손해보험→손보, 생명보험→생명, 재보험→삭제 + 영문 약칭 NAME_ABBR ~16건) — 트리맵 셀/모바일 리스트/버블 라벨/버블 리스트 표시 전용 (툴팁·클릭 내비는 원래 이름 = 조회 키 유지). 트리맵 코너 슬리버: 예상 면적 < 2,400px²(≈60×40) 소형사는 그룹당 '기타 N사' 한 칸으로 병합 — 중립색, 호버에 명단+비율, 모바일 리스트는 전체 로스터 유지. Edge headless 검증: 기타 3사(처브·BNP카디프·교보라이프플래닛), 셀명 메리츠화재/코리안리/DB손보 등 약칭 확인.
- **신한EZ손해(KR0051) PAA 표기 (inbox 🔴1)**: IFRS17 P1/4/6 stub → "CSM 분리공시 미제공 (PAA·단기계약 중심)" 전용 문구. 목록 제외 대신 표기 채택(PL 패널 공시 살아있음). `PAA_ONLY` Set (코드+표시명 — renderCompany는 회사코드 키임에 주의). Edge headless 검증. KEY_COLORS 시안은 inbox 답변에 작성, owner hex 확정 대기(→ 06-13 확정·적용).
- **index 제목 위계 정리 (사용자 지적)**: 헤더 힌트 "K-ICS Market Map (Balanced Treemap)" → "보험사 공시 대시보드", 트리맵 패널에 `.section-title` "K-ICS — 지급여력 마켓맵" 신설(버블 패널과 동일 패턴). `<title>`도 "보험사 마켓맵"으로. 기술용어 노출 제거.
- 검증: preview_eval 브리지 반복 행(06-07 screenshot 행과 동일 증상) → **Edge headless `--dump-dom`** 대체. K-ICS(한화손해 quarter): 토글 2·하위행 12 기본숨김·△21. IFRS17(한화손해): △47·차트 DOM 정상. index: 22社 로그축 렌더. 15s budget 내 완주. launch.json 포트 8765→8889(좀비 점유).

## 2026-06-11 — K-ICS 금리 민감도 패널 (kics_rate_sensitivity.json)

New panel between 소진율 donuts and Forward Outlook: **5-point 민감도 곡선** (±50/±100bp × 적용전 점선/적용후 실선, Chart.js) + ±100bp Δ%p KPI 칩 + 적용전후 상세표(가용/요구/비율) + 회사별 기준분기 select. 150% 권고선은 메인 차트 annotation 스타일 재사용. `refreshBottomPanels()`에 합류 (회사 change + URL 파라미터 둘 다).

- Coverage 29/30 (미커버 미래에셋생명 1곳 → placeholder). 적용후 미공시(post_dash)는 적용전 단독 렌더 (삼성생명 케이스 확인).
- Preview 검증: 메리츠 2024.4Q 수치 일치([238.23 … 244.42], −10.01/−3.82%p), 분기 전환, stub, mobile 375px (차트 260px, 표 nowrap+가로스크롤). **Console 0.**
- 거대 인라인 데이터 라인(209-211) 미접촉. 서브에이전트 구현 + 메인 세션 nit 2건 (범례 usePointStyle, 표 nowrap).
- Report: `artifacts/designer/kics_rate_sensitivity_panel_20260611.md`.

## 2026-06-11 — index.html 모바일 CSM 버블 → 리스트 (M2 패턴 미러)

≤640px에서 스캐터 숨기고 `#bubble-list` 표시: 생보/손보 그룹 · **최신 기말 CSM desc** 정렬 · 값=**NB CSM 배수** · 미공시사는 최신 분기 fallback + `25.4Q`/`24.4Q` 칩 · 클릭=IFRS17 cross-nav. 데스크톱 스캐터는 자체 패스(2026.1Q 고정) 그대로 — 리스트는 raw 마스터 별도 패스 (32社, 칩 10곳).

- 칩이 `.li-name` ellipsis에 잘리던 버그 → flex(.li-nm 말줄임 + 칩 고정) 구조로 수정, 이름 칸 36→44% (버블 리스트 스코프만).
- 모바일 `#bubble-meta`를 리스트 기준으로 갱신; matchMedia 전환 리스너(모바일 진입=리스트 갱신, 복귀=스캐터 재렌더+resize, 0×0 init 가드).
- Preview 검증 375px+desktop 전환, **Console 0.** 플래그: `.bubble-swatch`는 dead CSS.
- Report: `artifacts/designer/mobile_bubble_list_20260611.md`.

## 2026-06-07 — Panel 3 당기순이익 bridge waterfall (PL_breakdown.json)

Replaced Panel 3 (old Chart.js 손보-only 당기순이익 분해 from `net_income_breakdown.json`) with a CSM-style **bridge waterfall** from new root `PL_breakdown.json`, reusing Panel 1's 보험사 + 기준(연도/분기) controls. First cut — owner to refine details.

- 5-bar bridge (exact reconciliation verified, 손보+생보): **보험손익(1) → 투자손익(17) → 영업외손익(21) → (−)법인세(23) → 당기순이익(24)**. 단위 백만원, 축 조원, ECharts (Panel 1 technique).
- Source has **both YTD(`값`) and 당분기(`값_당분기`)** → 분기 mode works now (unlike CSM): 연도=latest `*.4Q` via `값`; 분기=latest Q via `값_당분기`. Shared #wfPeriod drives both panels.
- `PATHS.plx` (root + data/ fallback), `ix.plx` = company→quarter→항목번호→{y,q}. Template canvas→div `#chartPl`; `destroyCharts` wf+pl now ECharts(dispose), amort/nb/hist Chart.js.
- **Company-name mismatch flagged:** PL uses 삼성생명보험/미래에셋생명보험/코리안리재보험/KB라이프생명 (vs dropdown 삼성생명/…/코리안리/케이비라이프생명보험). `plResolve()` fuzzy-matches (substring); 케이비라이프↔KB라이프 still gaps → stub. Publishing to canonicalize names.
- Removed orphaned `ni`(net_income_breakdown) wiring. Pre-existing dead `payload.pl`/`ix.pl` left untouched.
- Verified via ECharts getOption + canvas dims (screenshot tool hung renderer-side): 현대해상 FY2025 (396,111→…→561,106 reconciles), 현대해상 2026.1Q 당분기, 삼성생명→삼성생명보험 resolve, 삼성화재 mobile, 처브라이프 stub. **0 console errors** both breakpoints.
- Report: `artifacts/designer/panel3_pl_bridge_waterfall_20260607.md`.

## 2026-06-07 — Panel 1 windowed CSM waterfall (company × 연도/분기)

Replaced Panel 1's single fixed 2024 year-end waterfall with a **company × period windowed** waterfall, mirroring `K-ICS.html` selector methodology.

- New source: root `CSM_waterfall.json` (long-format: 원수사명/항목번호/항목명/공시분기/값, 23사 × 2023.1Q~2026.1Q, **단위 억원**, YTD/연 누계). Wired as `PATHS.wfx` (root primary + `data/dart/viz/` fallback), indexed `ix.wfx` = company→quarter→항목번호→값.
- **연도 mode (shipped):** trailing 4 buckets (latest period + prior year-ends), expanded to 18 bars — 기초 CSM → 연도별 신계약·이자·가정/경험·상각 → 기말 CSM. Axis in 조원, tooltips in 조+억. Component reconciliation + year-end chain verified.
- **분기 mode (stubbed):** UI present; `WF_QUARTER_READY=false` shows "당분기 데이터 준비 중" because the file is YTD. Flip flag when 당분기 table lands.
- Old stages-based `renderWfEcharts` removed. Company dropdown = union of legacy 28사 + new 23사, so legacy-only insurers still list and Panel 1 stubs gracefully.
- Mobile: ECharts `dataZoom` (inside+slider) on the 18-bar chart, defaults to latest ~2 groups; `@media (max-width:640px)` unchanged, desktop math untouched.
- Verified via Claude Preview at 1280px + 375px (삼성생명 10.7조→13.6조, 현대해상, DB생명, 분기 stub, 라이나 missing-data stub): **0 console errors**.
- Report: `artifacts/designer/panel1_windowed_csm_waterfall_20260607.md`.
- Hand-off to publishing: relocate `CSM_waterfall.json` → `data/dart/viz/`; flip `WF_QUARTER_READY` when per-quarter table ships.

---

## Archive (pre-2026-06)

- 2026-05-31 — Designer stage created (split from publishing): new prompt `claude-agent-designer.md`; TODO/changelog seeded from root TODO items (MOB-KICS / MOB-IFRS17 / VIS-DONUT / VIS-CHARTLEGEND / M3 / Panel 7 / INDEX-BUBBLE-V2 / F17 Tier2); done archive imported (M1·M2·treemap cleanup·panel pruning·F6·F17 P3·single-source·dead CDN·console·INDEX-C12·KICS-HTML-SUB·F1).
- 2026-05-30 — Panel 3 clean 4-bar swap (data from publishing F17-T1): raw 12-row bar dump → 4-bar 당기순이익 분해 (보험손익·투자손익·영업외 → 당기순이익) + 보종별 caption. Source `net_income_breakdown.json` (Tier1 10/10 손보). 생보 graceful stub, 콘솔 0.
- 2026-05-28 — IFRS17 패널 정리 + F6 yearly amort Panel 2: F6 desktop 10y / mobile 5y (`matchMedia`); coarse → 4-bucket. Panel 5 KPI 카드 + Panel 6 BS 스냅샷 제거(`docs/archived_metrics.md` archive), 패널 1–6 재번호. Generators kept. → follow-up Panel 7.
- 2026-05-28 — index treemap text cleanup + mobile list sort: "기준 XXX" meta line 제거(`.meta` CSS/JS), size-aware labels(name ≥46×60px, ratio ≥26×44px). 모바일 리스트 정렬 ratio desc → 지급여력기준금액 desc (삼성생명 top, 라이나 9th). Preview 검증, 콘솔 0.
- 2026-05-28 — M2 mobile responsive (treemap → vertical list): index.html ≤640px swaps treemap for `#map-list` + `renderList(sector)` mirroring `render()` (same data/color/toggle/click-through). 생명/손해 그룹, ratio desc. Preview 375px 검증, 콘솔 0.
- 2026-05-28 — M1 mobile responsive foundation (4 pages): added `@media (max-width:640px)` to all 4 (zero queries existed before). Header/tabs scroll, panel/chart heights ↓, table overflow-x, map 76vh→58vh. ≤640px scoped → desktop unaffected. Preview 375/1280 검증.
- 2026-05-28 — HTML single-source refactor (P1 + P4): root `K-ICS.html` vs `templates/K-ICS.html` drift(line 171 forward blob) → root sole copy, `git rm templates/{index,K-ICS,IFRS17,공시보고서}.html`. P4: index.html dropped unused `xlsx.full.min.js` CDN (~900KB, 817→816 lines). Preview now `python -m http.server 8000` from root. Deferred: data-JSON dup (P2), shared CSS/nav (P3), K-ICS inline data → external (P5).

Pre-2026-05-28 HTML/viz design entries are in the compressed historical archive of root `docs/claude-changelog.md` ("## Historical archive (compressed)"): IFRS17.html 6-panel dashboard (initial) / K-ICS.html Phase 4 (자본성증권 도넛 + Forward Outlook dual-axis) / index.html treemap (initial Phase 3) / K-ICS sub-items + transition toggle.
