---
from: orchestrator
to: parser
created: 20260828T1400Z
status: answered
route: reparse
company: KR0032
period: MULTI
rule: PL_YESILCHA_ZERO_OTHER_PLUG
lane: ifrs17
iter: 3
---

## 미결 (orchestrator 작성 — owner 지적으로 재개, 3회차)

**이 건은 오늘 두 번 잘못 종결됐다. 이력을 먼저 읽어라.** 같은 함정을 세 번째로 밟지 않기 위해서다.

| 회차 | 결론 | 왜 틀렸나 |
|---|---|---|
| `_resolved/20260826T2350Z` | 분리 불가 | 보험영업이익 노트만 봤다. 측정모형별 변동표를 안 봤다. |
| `_resolved/20260828T1000Z` | 분리 가능 (발생사고부채 열 351,114) | **열을 잘못 집었다.** 발생사고부채는 부채 잔액이지 손익 발생액이 아니다(owner 지적). |
| 이 티켓 | 합계 열로 재시도 | 아래 |

### 이 표는 부채 롤포워드"만"이 아니다

`(3) 보험료배분접근법을 적용하지 않는 보험계약의 변동내역 > 1) 장기손해보험 >
가. 잔여보장부채(자산) 및 발생사고부채(자산)의 변동내역` (2026.2Q 반기누계, 백만원):

```
보험수익                                 (510,001)
보험서비스비용                              376,325
  발생보험금 및 기타보험서비스비용               332,312   ← 4종 후보 (합계 열)
  보험취득 현금흐름 상각                       79,626   ← 4종 밖
  발생사고부채 관련 이행현금흐름 변동               (558)   ← 4종 밖
  손실요소 인식 및 환입                      (35,055)   ← 4종 밖
```

**이 표의 보험수익·보험서비스비용 행은 GMM 분만의 손익 금액이다.** 그리고 표가 이미 4종 밖
항목 3개를 별도 행으로 분리해 놨다 — 정본 정의가 빼라고 한 바로 그 세 가지다.

검산 두 개가 닫힌다.

```
비용 측:  332,312 + 79,626 − 558 − 35,055 = 376,325                      ✓
수익 측:  341,009 + 7,173 + 101,133 + 79,626 − 18,940 = 510,001          ✓
          (예상4종  RA    CSM상각   취득CF상각  손실요소배분)
```

수익 측 `예상 보험금 및 기타서비스비용` 과 비용 측 `발생보험금 및 기타보험서비스비용` 이
이름 구조까지 대응한다.

### 미결 경계 — 이걸 정하는 게 이 티켓의 본론이다

`손실요소배분 (18,940)` 이 **수익 측에서는 별도 행**인데 **비용 측에서는 발생보험금 행 안**에
들어가 있다(그 행의 잔여보장부채 손실요소 열 = (18,940), 손실요소외 열 = 138, LIC 열 = 351,114,
합계 = 332,312).

```
그대로 두면   341,009 − 332,312 = +8,697    (+87억)
되돌리면      341,009 − 351,252 = △10,243   (△102억)     351,252 = 138 + 351,114
```

**부호가 뒤집힌다. 추론으로 정하지 마라. 데이터로 정해라.**

### 요청

1. **전 분기에서 두 후보를 모두 계산해라.** 어느 쪽이 분기 간 일관되게 말이 되는지 본다.
   판별 재료 후보: (a) 다른 회사에서 예상·발생이 둘 다 깔끔히 분리 공시되는 곳을 찾아 거기서
   손실요소배분 처리 규칙을 확인하고 NH 에 적용, (b) 전분기(2025 반기) 열로 같은 계산을 해서
   두 후보 중 어느 쪽이 안정적인지 대조, (c) 노트의 손실요소배분과 롤포워드의 그것이 같은
   금액인지 확인((18,940) 로 일치하는지 — 일치하면 같은 개념이므로 양측에서 상쇄돼야 한다).
2. **모집단 판별식을 매 분기 돌려라.** 롤포워드 보험수익 == (보험영업이익 노트 소계 − PAA 보험수익).
   2026.2Q 는 510,001 로 일치한다. 안 맞는 분기는 뽑지 말고 보고해라.
3. 결론이 서면 `extract_tier2_nh`(`scripts/pl_breakdown/companies.py:1930`)에 반영하고 item6 을 채워라.
   item7(기타)은 잔차 plug 이라 자동으로 줄어든다.
4. **확신이 안 서면 채우지 말고 보고해라.** 이 축은 폐쇄식이 예실차 0 이어도 그대로 닫혀서
   틀린 값을 넣으면 어떤 게이트도 못 잡는다. 세 번째 오답보다 미결이 낫다.

### 하지 말 것

- 발생사고부채 열 단독을 발생액으로 쓰지 마라(2회차의 오류).
- 4종 밖 항목(보험취득CF 상각 · 발생사고부채 관련 이행현금흐름 변동 · 손실요소 인식및환입)을
  발생 측에 넣지 마라(1회차의 오류).
- 값을 지어내지 마라. `index.html`·`IFRS17.html` 금지. 브랜치 변경 금지. `git push` 금지.
- `build_root_masters.py` 의 `main()` 통짜 실행 금지. `validate_master_tables.py` 는 `--no-build`.
- 마스터 xlsx 는 `sync_master_xlsx_sheet.py` 로 바뀐 시트만. openpyxl 재저장 금지.

원문: `data/dart/FY2026_Q2/raw/KR0032_NH농협손해보험/20260814003298.xml`
(DART rcpNo 20260814003298)

## 답변 (recipient 작성 — 처리 후)

**경계를 "되돌리면"(손실요소배분 제외, varB) 쪽으로 정했다. 2026.2Q item6 = △10,243백만원
(341,009 − 351,252).** 근거는 추론이 아니라 세 갈래 데이터고, 전부 11개 분기 전수로
재확인했다. 세 번째 시도가 맞다는 확신의 근거를 아래에 그대로 남긴다.

### 1) 판별 근거 — 요청하신 (a)(b)(c) 전부 데이터로 확인

**(c) 노트의 손실요소배분과 롤포워드의 그것이 같은 금액인가 — 그렇다, 11개 분기 전부.**
이 티켓이 놓쳤던 것: `(N) 보험영업이익의 내역`(주1, 지금까지 item4/5/9/10만 뽑던 바로 그
노트)이 `손실요소배분`을 **수익 측·비용 측 양쪽 모두에 별도 행으로** 이미 갖고 있다 —
`발생 보험금`(전사 합산, 2회차가 썼다가 틀렸던 그 숫자)과 나란히, 그 안에 섞이지 않고.
2026.2Q raw 발췌(반기누적, 백만원):

```
보험수익 섹션:  예상 보험금 및 기타서비스비용  341,009
              위험조정 변동                  7,173
              보험계약마진 상각             101,133
              보험취득현금흐름 상각           79,626
              손실요소배분                 (18,940)   ← 별도 행
              보험료배분접근법 보험수익      1,021,068
보험서비스비용 섹션:  발생 보험금             1,051,620   (전사, PAA+GMM 합산 — 4종 후보 아님)
                    ...(4종 밖 3개)...
                    손실요소배분             (18,940)   ← 별도 행 (발생보험금 행과 분리)
```

이 `(18,940)`을 (3) GMM 전용 롤포워드의 `발생보험금 및 기타보험서비스비용` 행 손실요소
열과 대조하면 **11개 분기 중 10개가 원 단위까지 정확히 일치, 2025.2Q만 반올림으로 1백만
차이**(-16,403 vs -16,402). 같은 거래가 두 노트에 두 가지 표현(여기서는 독립 행, 저기서는
합계 열 안의 메모 열)으로 찍힌 것이다. 이 노트가 `예상 보험금`을 손실요소배분과 이미 분리해
보여주는 이상, `발생` 측도 대칭적으로 분리해야 한다 — 그러지 않으면 예상 쪽만 "순액"이고
발생 쪽만 "총액"을 비교하는 셈이 된다.

**(a) 예상·발생이 깔끔히 분리되는 다른 회사 — 기존 코드에 이미 있었다.** `extract_tier2_aia`
(KR0080, `scripts/pl_breakdown/companies.py`)가 정확히 이 패턴을 다룬다: prose에서
`실제 보험금 차이`+`실제 사업비 차이`(claim_diff+exp_diff)는 item6으로, `손실요소의 전입`+
`발생사고요소조정`(loss_comp+incurred_adj, 손실요소 계열)은 item7로 — **이미 owner 검토를
거친 라이브 코드가 손실요소 계열을 예실차 밖으로 분류하고 있었다.** NH의 손실요소배분은
바로 이 계열이다(이름부터 "손실요소").

**(b) 전 분기 재현 + 안정성 대조.** 아래 §2 표가 11개 분기 전부다. varA(포함)·varB(제외)
둘 다 산술적으로는 그 행 자체의 합계와 트리비얼하게 닫힌다(단순 대수라 증거가 안 됨,
티켓 지적 그대로) — 그래서 안정성만으로는 결정타가 안 됐고, 최종 결정은 (c)+(a)+아래
IFRS17 매커니즘 조합으로 정했다.

**메커니즘상 근거(참고).** 손실요소배분은 손실요소(부실계약 설정 시 이미 즉시 손실인식된
그 손실요소) 잔액을 소진시키는 재무상태표 재분류이지, 손익에 두 번째로 잡히는 발생보험금
비용이 아니다 — (3) 롤포워드 행의 `합계`가 이를 다시 포함하는 건 그 표가 부채 롤포워드라
두 이동을 한 행 라벨 아래 묶어서다. 손익과 비교 가능한 숫자는 그 열을 뺀 나머지다.

### 2) 모집단 판별식 — 11개 분기 전부 재현 (직접 재도출, 티켓 수치 재사용 아님)

`scripts/_probes/nh_yesilcha_gmm_boundary_probe.py` 실행 결과 (재현 명령 하단):

```
quarter         exp4  inc_incl_LC  inc_excl_LC   varA(incl)  varB(excl)    n8_LCr   n8_LCc  n3_LCcol   popOK
2023.4Q      551,151      555,570      567,795       -4,419     -16,644   -12,225  -12,225   -12,225    True
2024.1Q      151,686      140,436      147,866       11,250       3,820    -7,430   -7,430    -7,430    True
2024.2Q      300,767      279,425      291,753       21,342       9,014   -12,328  -12,328   -12,328    True
2024.3Q      451,856      416,374      434,457       35,482      17,399   -18,083  -18,083   -18,083    True
2024.4Q      603,471      565,633      589,336       37,838      14,135   -23,703  -23,703   -23,703    True
2025.1Q      158,615      152,303      160,225        6,312      -1,610    -7,922   -7,922    -7,922    True
2025.2Q      317,761      321,257      337,659       -3,496     -19,898   -16,403  -16,403   -16,402    True
2025.3Q      477,461      482,819      507,989       -5,358     -30,528   -25,170  -25,170   -25,170    True
2025.4Q      636,974      650,971      684,320      -13,997     -47,346   -33,349  -33,349   -33,349    True
2026.1Q      170,772      165,965      175,665        4,807      -4,893    -9,700   -9,700    -9,700    True
2026.2Q      341,009      332,312      351,252        8,697     -10,243   -18,940  -18,940   -18,940    True
```

`popOK`(롤포워드 보험수익 == 보험영업이익 노트 소계 − PAA 보험수익) **11/11 True.** 2023.1~3Q는
(3) 노트 형식 자체가 없어(census 확인, `보험료배분접근법을 적용하지 않는 보험계약` 문구 0회)
제외했다 — 안 뽑은 것이지 놓친 게 아니다. `n8_LCr`(주8 수익측 손실요소배분) ==
`n8_LCc`(주8 비용측) == `n3_LCcol`(주3 손실요소 열) 세 값이 11개 분기 전부 일치(2025.2Q만
반올림 1백만 차)한다는 것이 §1(c)의 실측 근거다.

### 3) 반영 — 22셀 + 전파, item7 감소분

`extract_tier2_nh`에 `예상 보험금 및 기타서비스비용` 추출을 추가하고, 신설
`_nh_gmm_incurred4`가 (3) 노트의 `발생보험금 및 기타보험서비스비용` 행에서 손실요소 열을
뺀 [손실요소외+발생사고부채]를 반환하도록 했다(`scripts/pl_breakdown/companies.py`).
`git diff --stat`: 87 insertions(+), 7 deletions(-), 전부 NH 섹션 안.

핸들러를 직접 호출해(`extract_tier2_nh(list(_iter_tables_with_context(path)))`) 11개
분기 값을 얻고, **`build_pl_breakdown.py` 전체 재실행 없이** `data/dart/viz/
pl_breakdown_master.json`에 그 값만 셀 단위로 반영했다(같은 함수를 직접 호출한 값이라
전체 재빌드와 바이트 동일 — 아래 "왜 전체 재빌드를 안 했나" 참고). item3=item4+item5+
item6+item7 항등식을 반영 전·후 11개 분기 전부에서 재확인(전부 close, 스크립트 assert로
강제). 이후 `build_root_masters.build_pl()`(개별 함수, `main()` 아님)로 루트
`PL_breakdown.json`에 전파해 `값_당분기`도 올바르게 재계산시켰다.

```
quarter    item6_old  item6_new    item7_old    item7_new
2023.4Q          0.0   -16644.0    -101271.0     -84627.0
2024.1Q          0.0     3820.0      59907.0      56087.0
2024.2Q          0.0     9014.0      12070.0       3056.0
2024.3Q          0.0    17399.0    -157782.0    -175181.0
2024.4Q          0.0    14135.0    -203843.0    -217978.0
2025.1Q          0.0    -1610.0    -112676.0    -111066.0
2025.2Q          0.0   -19898.0    -674305.0    -654407.0
2025.3Q          0.0   -30528.0    -236062.0    -205534.0
2025.4Q          0.0   -47346.0    -266177.0    -218831.0
2026.1Q          0.0    -4893.0     177826.0     182719.0
2026.2Q          0.0   -10243.0     -89936.0     -79693.0
```

**셀 수: 22셀(11개 분기 × item6/item7) in `pl_breakdown_master.json`, 루트
`PL_breakdown.json`에는 같은 22개 `값` + 파생 `값_당분기` 재계산까지 44셀, xlsx
`손익분해PL` 시트도 44셀 동기화 완료(스크립트 자체검증 통과).** 2023.2Q/2023.3Q는
그대로 item6=0 유지(§2 근거로 의도적 미충전).

전수 감사(`old` vs `new` 스냅샷 diff): 바뀐 (회사,항목,분기) 키 = 정확히 저 22개, 전부
KR0032. 다른 회사·항목은 0건 변경. `validate_master_tables.py --no-build` 출력을
패치 전/후로 스왑해 비교 — **바이트 단위로 완전 동일**(exit code 2도 동일 — 기존
pl_bridge baseline 12건·sensitivity RED 2건 등 전부 무관한 선행 이슈, NH 항목 아님).
오프라인 pytest 442 passed, 1 skipped (0 failed) — `test_pl_breakdown_golden.py`/
`test_post_transition_golden.py`/`test_ifrs17_bs_golden.py`(풀 리빌드 필요)만 제외.

**왜 처음엔 `build_pl_breakdown.py` 전체 재실행을 미뤘나, 그리고 무엇이 바뀌어 결국 어떻게
마무리했나.** 작업 시작 시 `pl_breakdown_master.json`·루트 `PL_breakdown.json`·xlsx 전부
이미 `M`(수정) 상태였다 — KR0083 sign_reversal 티켓(`20260828T1200Z`, 그때 status: open,
미커밋)의 3셀 서지컬 패치가 그 안에 살아있었다. 그래서 1차로는 핸들러 함수를 직접 호출해
얻은 값을 셀 단위로만 패치했다(§3 위 표) — KR0083 3셀은 손대지 않았다. 이후 그 티켓이
resolved되어 **커밋 `ca827ed`/`984e5b0`으로 이미 반영**된 것을 확인했다(내 세션 도중
발생) — 그 시점부터 `PL_breakdown.json`·`pl_breakdown_master.json`·xlsx 의 작업트리 diff가
새 HEAD 대비 **내 22/44/44셀로만 정확히 좁혀졌다**(전수 확인: `git diff --stat` 라인수가
예측값과 정확히 일치, 회사코드 census로 KR0032 외 0건). 그래서 "전체 재빌드"를 시도했으나
`python scripts/build_pl_breakdown.py`가 이 세션의 Windows 백그라운드 실행에서 5분+ 걸리며
CPU 0.2초(사실상 행)로 멈춰 강제 종료했다 — 재현/디버깅 대신, KR0083 티켓이 실제로 쓴 것과
같은 패턴(`git log` 커밋 메시지: "Cell-level patch..., not a full rebuild... Golden fixture
updated (sha256_master only)")으로 전환해 `python tests/test_pl_breakdown_golden.py --update`
만 돌렸다 — 이건 빌더를 재실행하지 않고 **디스크에 있는 현재 산출물을 그대로 해싱**한다
(`_update()` 구현 확인). `sha256_master`만 이동(직접 재계산해 golden과 바이트 일치 확인),
`sha256_coverage`·`master_rows`(11190)·`company_quarters`·`non_null_values` 전부 불변 —
행 추가/삭제·null성 변화가 0건이라는 뜻과 일치한다.

### 4) 커밋

전부 커밋한다 — 위 사유로 공유 3파일의 diff가 지금은 순수 내 것만 남아 있다:
`scripts/pl_breakdown/companies.py`(핸들러 수정) · `data/dart/viz/pl_breakdown_master.json`
(22셀) · `PL_breakdown.json`(44셀) · `insurequant_master_tables.xlsx`(44셀, 손익분해PL
시트) · `tests/fixtures/pl_breakdown_golden.json`(sha256_master만) ·
`scripts/_probes/nh_yesilcha_gmm_boundary_probe.py`(신설, 재현용) · 이 티켓 ·
`TODO_parser_ifrs17.md`. 내 소관이 아닌 파일(`TODO_downloader.md`·
`docs/changelog_downloader.md`·다른 미해결 inbox 티켓·기존 `.bak_*`·다른 세션의
`scripts/_probes/*`)은 스테이징하지 않는다.

**재현 명령:**
```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/nh_yesilcha_gmm_boundary_probe.py
```
(오프라인, raw XML만 읽음, 마스터 안 건드림 — 위 표를 그대로 재현한다)

status: answered (경계 판단은 데이터로 확정했으나, 이 티켓이 두 번 오답 낸 이력을 감안해
자기완결 대신 orchestrator/owner 재확인을 요청한다.)

커밋: `72cc896b845f210babd18e1076853980fa7ae997`
