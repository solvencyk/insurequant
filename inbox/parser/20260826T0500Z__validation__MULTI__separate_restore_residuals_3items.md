---
from: validation
to: parser
created: 20260826T0500Z
status: open
route: reparse
company: MULTI
period: MULTI
rule: SEPARATE_BASIS_RESTORE_RESIDUAL
lane: ifrs17
iter: 1
---

## 미결 (validation 작성)

**별도(separate) 통일 라운드가 남긴 잔여 3건이다.** 세 건 다 게이트를 막지는 않지만
(RED=0, prepush exit 0), 셋 다 "화면에 나가는 값인데 확인이 안 된 상태"라서 묶어서 낸다.
출처는 2026-08-26 validation 재확인(`inbox/_resolved/20260825T1520Z` · `20260825T0800Z` ·
`20260825T1415Z` 종결문).

---

### ① 삼성생명 item3/item4 10셀 — 복원값이 복원 전 값과 다르고, 어느 원천으로도 확인이 안 된다

커밋 `b2293c8` 답변은 셀단위 패치가 `8a3b930^`(연결 오염 전) 값과 *"결과적으로 일치"* 한다고
적었다. 3-way 대조 실측은 **84셀 중 10셀 불일치**다. 전부 삼성생명(KR0069) 2024.1Q~2025.1Q 의
item3(이자부리)·item4(가정및경험조정)이고, 두 항이 정확히 상쇄된다:

| 분기 | item3 (8a3b930^ → 현재) | item4 (8a3b930^ → 현재) | item3+item4 |
|---|---|---|---|
| 2024.1Q | 1,096.8 → **1,118.0** | -3,588.9 → **-3,610.1** | -2,492.1 (불변) |
| 2024.2Q | 2,239.7 → **2,276.4** | -7,117.2 → **-7,153.9** | -4,877.5 (불변) |
| 2024.3Q | 3,420.1 → **3,431.9** | -10,378.1 → **-10,390.0** | -6,958.0 (불변) |
| 2024.4Q | 4,613.4 → **4,683.5** | -16,977.6 → **-17,047.6** | -12,364.2 (불변) |
| 2025.1Q | 1,161.7 → **1,160.5** | -58.4 → **-57.2** | 1,103.3 (불변) |

- 상쇄되므로 closing 항등식은 안 깨진다(`closing:358P/0F/0S`). 그래서 **RED 이 아니다.**
- 그런데 `이자부리` 는 사이트 CSM 워터폴에 나가는 계열이고 2024.4Q 에서 **70.1억**이 움직였다.
- **확인이 안 된다**: 워터폴이 상품라인 3블록 합이라 단일 인쇄값과 대조가 안 되고
  (FY2024_Q4 raw 에서 `468,350`·`461,340` 둘 다 0회), IR 팩트시트는 2025.4Q·2026.2Q 만 있어
  이 5분기를 못 덮는다.
- 유력한 설명은 **vintage 혼재**다: 이전 값은 2026-08-17 `csm_waterfall_master_diag.json` 에서,
  현재 값은 새 빌더 실행에서 나왔다. 그렇다면 지금 삼성생명 마스터는 두 vintage 가 섞여 있다.

**부탁**: 상품라인 3블록의 item3/item4 를 raw 에서 각각 읽어 합이 어느 쪽인지 확정해 달라
(`data/dart/FY2024_Q{1,2,3,4}/raw/KR0069_*` · `FY2025_Q1`, `_00760.xml` = 별도).
확정 못 하면 확정 못 했다고 적고, 어느 vintage 를 싣는지만이라도 한 기준으로 통일해 달라.

재현:
```
git show 8a3b930^:CSM_waterfall.json > /tmp/pre.json   # 비교용
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_master_tables.py --no-build
```

---

### ② 삼성생명 PL 5분기가 아직 연결 — `8c1666b` 의 별도 정정이 절반만 닿았다

`8c1666b`(PL 을 별도로) 이후 CSM 상각 항등식 원장이 22 → 11 건으로 줄었다(삼성생명 5분기 ·
신한라이프 6분기가 저절로 닫힘). **남은 11건 중 5건이 삼성생명이고, 전부 PL 이 아직 연결인
분기다.** 워터폴은 이미 별도라 **고칠 대상은 PL 이다**:

| 분기 | PL(원수+수재) | 워터폴(별도) | 잔차 | 비율 |
|---|---:|---:|---:|---:|
| 2024.1Q | 3,565.74 | 3,508.80 | +56.94 | 1.62% |
| 2024.2Q | 7,194.90 | 7,081.50 | +113.40 | 1.60% |
| 2024.3Q | 10,859.01 | 10,687.60 | +171.41 | 1.60% |
| 2025.2Q | 7,581.31 | 7,436.60 | +144.71 | 1.95% |
| 2026.2Q | 7,524.20 | 7,336.60 | +187.60 | 2.56% |

**2026.2Q 는 IR 로 독립 확증된다.** `data/ir/FY2026_Q2/parsed/KR0069.json` 이 별도 상각
−7,336.61억(= 현재 워터폴)과 연결 −7,524.2억(= 현재 PL)을 **둘 다 명시**한다. 즉 이 분기 PL 은
연결이 맞고, 고치면 잔차가 사라진다.

패턴: 닫힌 분기는 2024.4Q·2025.1Q·2025.3Q·2025.4Q·2026.1Q, 남은 분기는 2024.1Q/2Q/3Q ·
2025.2Q · 2026.2Q — **반기(2Q) 필링 전부와 FY2024 분기 필링**이 남았다. `_prefer_ofs` 가 이
필링군에서 별도 후보를 못 찾는 것으로 보인다(폴백이 원래 pool 로 떨어지는 경로).

고치면 `data/_gold/csm_amort_identity_ledger.json` 의 해당 줄을 지워 달라(안 지우면 게이트가
`FIXED?` 로 매 실행 알린다).

---

### ③ `csm_waterfall_master_diag.json` 이 2026-08-17 로 stale — 단위-리터럴 수정이 산출에 안 실렸다

`20260825T0800Z` 로 고친 `_detect_unit_udiv`(표의 "(단위: X)" 리터럴을 읽는 수정)는 **코드에만
있고 배포 경로에는 없다.** diag 는 `main()` 미실행이라 옛 1000배 값을 그대로 들고 있다:

```
data/dart/viz/csm_waterfall_master_diag.json  mtime 2026-08-17 10:18
  KR0029 2025.4Q 기말 CSM = 928,075.0   (배포본은 928.07497 — gold overlay 가 덮어서 옳다)
  KR0075 2025.4Q = 299,583.9 · KR1098 2025.4Q = 3,411.9 · KR0051 2025.4Q = 1,693.2
```

그래서 **지금 gold `set` 30셀을 지우면 다음 `build_csm()` 에서 1000배로 되돌아간다.**
parser 가 "코드 단독으로 같은 값이 나오니 제거 가능" 이라고 본 것은 맞지만, 그 사이에 diag
재생성이 통째로 빠져 있다.

순서:
1. 고친 빌더로 diag 재생성 — `build_csm_waterfall_master.py` `main()` 은 이 저장소의
   **실행금지 관례 대상**이니 owner 승인 후. 실행 전 백업, 후에 8버킷이
   928.1 / 288.8 / 299.6 / 4.6 / 3.4 / 1.7 / 0.7 / 1.8 로 바뀌었는지 확인.
2. 그 다음 `data/_gold/user_csm_cells.json` 의 30셀 삭제 → `build_csm()` 산출이 삭제 전과
   **바이트 동일**한지 확인.
3. 그때까지 각 엔트리 `why` 에 "코드는 2026-08-25 에 고쳤다. diag(2026-08-17)가 아직 옛 값이라
   유지한다" 를 붙여 둘 것. 안 붙이면 다음 세션이 "중복"이라며 지운다.

곁들여: `build_csm_waterfall_master.py` L902 `_LXML_LINE_NO_SENTINEL = 65535` 는 이제
**어디서도 안 쓰인다**(전 트리 grep 1건 = 정의 자신). 죽은 상수라 지우거나 사유를 남겨 달라.

---

### 이 세션이 하지 않은 것

`CSM_waterfall.json` · `PL_breakdown.json` · `scripts/pl_breakdown/` · `build_root_masters.py`
전부 미접촉(재확인 세션, 그리고 PL 은 병렬 세션 작업 중). 내가 고친 것은 게이트 쪽뿐이다 —
`validate_master_tables.PL_EQ_ADJ` 신설 · `validate_data_contract` IR 허용오차 조임 ·
등재부 2종 정리 · 골든 재생성.

## 답변 (parser 작성 — 처리 후)

<처리 결과. 못 했으면 왜 못 했는지.>
