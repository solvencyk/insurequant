---
from: orchestrator
to: parser
created: 20260821T2010Z
status: open
route: reparse
company: MULTI
period: MULTI
rule: LEAF_VS_RAW_MISMATCH
lane: kics
iter: 1
---

## 미결 (sender 작성)

`inbox/_resolved/20260821T1030Z` 를 종결하면서 **그 티켓 스코프 밖이라 아무도 안 맡은 잔여
4건**을 분리했다. 1030Z 는 자기 수용기준(F1 7개사 111셀)을 충족했으므로 닫았다 — 이 4건은
그 작업 중에 **새로 발견된 것**이고, 방치하면 감사기가 매번 같은 4줄을 찍는데 아무도 안 보는
상태가 된다.

재현: `C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/leaf_scale_residue_audit.py`
→ 현재 `대조 셀 4,516 | 불일치 4 | 앵커불가 21`

```
회사            분기        item 열          저장      원문환산     차이   원문(백만원)
예별손해보험      2023.4Q    36  값_적용후     0.00    652.39   -652.39    65,239
예별손해보험      2024.1Q    36  값_적용후     0.00    716.06   -716.06    71,606
예별손해보험      2024.2Q    36  값_적용후     0.00    567.90   -567.90    56,790
처브라이프생명    2024.4Q    35  값           46.81     44.99     +1.82     4,499
```

> **감사기 오탐 3건은 내가 고쳤다.** 교보생명 item35 3분기가 "마스터가 틀렸다"로 잡히던 것은
> `pre_raw = max(set(cand), key=cand.count)` 가 카운트 동률(1 vs 1)일 때 `set` 순회 순서로
> 승자를 정해서 원문 첫 occurrence 대신 0.0 을 고른 탓이었다(1030Z 답변이 정확히 진단했다).
> 동률이면 원문에 먼저 나온 값을 쓰도록 고쳤고 불일치가 **7 → 4** 로 줄었다. 남은 4건은 진짜다.

### 1. 예별손해보험(KR0004) 2023.4Q · 2024.1Q · 2024.2Q — item36 값_적용후 (최우선)

item36(금리위험액) 적용후가 **0.00 으로 저장**돼 있는데 raw 에는 단일 occurrence 로
65,239 / 71,606 / 56,790 백만원(= 652.39 / 716.06 / 567.90 억원)이 명확히 있다.

1030Z 답변이 **강제 기입을 거부한 이유가 타당하다** — 안전가드 dry-run 로그:
```
HOLD 예별손해보험 20XX.XQ parent19: 부모후 재현실패 계산=1,446~1,601 vs 저장=1,106~1,341
```
즉 item36후를 넣으면 저장된 item19후(시장위험액)와 안 맞는다. **item19 자체나 다른 leaf 에
별도 오류가 있다는 뜻**이고, 추측으로 채우면 한 칸 고치고 두 칸 틀리는 전례가 된다.

→ **item36 만 보지 말고 item19 와 item36~40 을 한 세트로** raw 에서 재확인해라. 어느 칸이
틀렸는지 확정한 뒤에 고쳐라. 예별손해는 구 MG손해(KR0004)로 부실사 자본잠식 이력이 있어
값 자체가 특이할 수 있다 — 추세만으로 판단하지 말고 원문을 봐라.

### 2. 처브라이프생명(KR0092?) 2024.4Q — item35 값(적용전) 46.81 vs 44.99

diff 1.82억. 1030Z 는 "적용전 컬럼이라 스코프 밖"이라 안 건드렸다. 스코프 문제였을 뿐
**틀렸는지 여부는 판정 안 된 상태**다. raw 에서 item35(그밖의위험액 계열) 값을 확인하고,
마스터가 틀렸으면 고치고 맞으면 감사기가 왜 다른 값을 집는지 적어라.

### 3. 아이엠라이프 2023.1Q — parent17 hold (미조사)

1030Z 답변 표에 있으나 감사기 목록에는 안 잡히는 항목이다(`_leaves_mode` 내부 hold):
`계산=1449.35 vs 저장=1459.26` (diff 9.91, 허용 밖). **미조사**로 남았다. item17(생명장기
손해보험위험액)과 item29~35 를 raw 에서 대조해라.

## 부탁 (수신자가 할 일)

1. 위 3건을 raw 대조로 판정하고, **어느 칸이 틀렸는지 확정한 뒤에** 셀 단위로 고쳐라.
2. 고친 뒤 감사기를 다시 돌려 불일치 수를 붙여라(목표: 스코프 내 0).
3. `앵커불가 21` 이 무엇인지도 한 줄로 설명해라 — 지금은 아무도 그게 뭔지 모른다.
4. 게이트 재확인: `validate_kics_disclosure.py` exit 0 유지.

## 하지 말 것

- **원문 근거 없이 값을 채우지 말 것.** 1030Z 가 안전가드에 걸렸을 때 강제 기입하지 않은 판단은
  옳았다. 같은 상황이면 같은 선택을 하고 이유를 적어라.
- 마스터 통째 read-modify-write 금지 — 셀 단위로.
- `insurequant_master_tables.xlsx` 전체 재생성 금지 — `scripts/sync_master_xlsx_sheet.py "K-ICS공시"`.
- `git push`·커밋 금지 — orchestrator 가 한다.

## 답변 (parser-kics, 2026-08-21)

**결론: 4건 전부 마스터가 이미 맞다. 수정 0건.** 감사기(`leaf_scale_residue_audit.py`)의 단순
휴리스틱이 회사별 표 관례 차이를 못 잡아 낸 오탐이었다. 원문을 더 깊이 대조한 근거는 아래.

### 1. 예별손해보험(KR0004) item19·36-40 — item36후=0.00 이 맞다 (최우선 항목 판정 완료)

raw(`data/disclosure/FY2023_Q4·FY2024_Q1·FY2024_Q2/raw/KR0004_예별손해보험.pdf`)의
**"③ 주식위험 경과조치 또는 금리위험 경과조치"** 표를 fitz 원문 그대로(줄 단위, 아무 파싱 안 거침)
읽으면 세 분기 전부 `금리위험 65,239 / -` (2023.4Q 기준, 다른 두 분기도 동일 패턴) — **후 컬럼이
숫자가 아니라 대시(-)로 인쇄돼 있다.** 감사기가 참조하는 `scan_occurrences()`는 이 대시를
"전후 동일(캐리포워드)"로 해석해 652.39(=65,239백만÷100)를 원문값이라 주장했는데, 이 해석은
**이 회사 이 표에서는 틀렸다** — 다음 근거로 확정했다.

같은 표가 직접 인쇄하는 **시장위험액 후 합계(213,547→110,677)** 를 `MARKET_M` 상관행렬로
독립 재현했다 (`scripts/_probes/probe_20260821_kr0004_verify.py`):

| 분기 | 원문 인쇄 시장위험액후 | 가설 A: 금리후=0 | 가설 B: 금리후=652.39(캐리포워드) |
|---|---|---|---|
| 2023.4Q | 110,677백만 | **110,676.67** (diff −0.33) | 144,690.51 (diff +34,013.51) |
| 2024.1Q | 116,622백만 | **116,622.58** (diff +0.58) | 154,518.38 (diff +37,896.38) |
| 2024.2Q | 134,130백만 | **134,130.03** (diff +0.03) | 160,137.79 (diff +26,007.79) |

가설 A(금리위험 후=0)가 회사 자신이 인쇄한 시장위험액후 합계를 소수점 이하까지 정확히
재현하고, 가설 B(대시=캐리포워드)는 26,000~38,000백만 어긋난다. 즉 **예별손해는 이 분기들에
금리위험 경과조치의 인정 위험액을 0으로 산출**했다(신규 도입/기존측정 위험 경과조치가 흔히
쓰는 단계적 phase-in 구조상, 초기 분기엔 인정액이 0일 수 있다 — 주식위험(37)은 같은 표에서
188,325→112,644로 부분감소했으니 리스크마다 잔여율이 다른 것 자체는 이례적이지 않다).
`item19후`(1106.77/1166.22/1341.30)는 그 회사 인쇄값을 그대로 옮긴 것이라 이미 맞고,
`item36후`=0.00도 이미 맞다 — **1030Z 의 안전가드(부모후 재현실패)가 정확히 이 모순을 잡아낸
것**이었다(감사기가 잘못된 652.39를 밀어넣으려 했다면 부모와 어긋났을 것이다).

### 2. 처브라이프생명(KR0100) 2024.4Q item35(적용전) — 46.81 이 맞다

raw p54-55(`[생명·장기손해보험위험액-대재해위험]` 전용표, 업무보고서 AH725/AI725 양식)의
"Ⅲ.총계" 당기(2024.4Q) 대재해위험액 = **4,681백만원 = 46.81억원** — 마스터 값과 정확 일치.
감사기가 집은 44.99는 **다른 표**(p47, "② 장수위험·사업비위험·해지위험 및 대재해위험 경과조치"
결합표)의 대재해위험 행 `4,499/-` 에서 온 것이다. `fill_subitems_to_disclosure.py`의
`_is_life_catastrophe_table` 가드가 애초에 item35를 **전용 대재해위험표**에서만 소싱하도록
설계돼 있어(SKILL 문서에 명시) 마스터는 처음부터 올바른 표를 봤다 — 경과조치 결합표의 44.99는
같은 개념의 회사 내부 별도 산출치(diff 1.82억=3.9%, 발행사 표간 불일치)일 뿐, 정본이 아니다.
[[feedback-issuer-inconsistent-keep-as-disclosed]] 관례대로 정본 표 값을 유지한다.

### 3. 아이엠라이프(KR0076) 2023.1Q parent17 — 1459.26 이 맞다 (미조사 항목, 조사 완료)

raw의 "② 표"(장수·해지·사업비·대재해 경과조치)가 직접 인쇄하는 생명·장기손해보험위험액후
합계 = 145,926백만 = **1459.26억원** — 마스터와 정확 일치, 회사 자신이 공시한 결합 subtotal을
그대로 옮긴 값이다. `_leaves_mode`(rebuild_combined_transition_after.py --leaves)가 낸
HOLD("계산=1449.35 vs 저장=1459.26", diff 9.91)는 **leaf 7개(29-35후)에서 R7 상관행렬로 역산한
값**과의 차이인데, 개별 leaf도 전부 raw와 정확히 일치함을 직접 확인했다(사망 194.49·장수 0·
장해질병 1388.44·장기재물 0·해지 0·사업비 0·대재해 0, 전부 raw occurrence와 소수점까지 일치).
diff 9.91(=0.68%)은 회사가 leaf를 백만원 단위로 반올림한 뒤 합산한 것과, 그 합산 subtotal
자체를 더 정밀한 내부 수치로 별도 반올림한 것 사이의 정상적 반올림 캐스케이드다 — 게이트의
8_life 허용오차(`max(2.0, 0.05×|기대값|)`=72.96)에 한참 못 미친다. `_leaves_mode`의 0.2%
문턱(2.92)은 그 스크립트 자신의 쓰기 안전장치(보수적으로 좁게 잡은 것)이지 정확성 판정
기준이 아니다 — HOLD가 뜬 것 자체가 "쓰지 마라"는 올바른 신호였고, 정말로 손대지 않는 게 맞다.

### 4. "앵커불가 21" 이 무엇인가

`leaf_scale_residue_audit.py`가 APPLIERS 18사×전분기(234버킷) 순회 중, item15전(마스터)과
raw의 "기본요구자본" occurrence 최댓값의 비율이 0.01(백만원→억원) 이나 1.0(이미 억원) 어느
쪽에도 안 걸리면 그 (회사,분기) 전체를 건너뛰고 "앵커불가"로 센다(`scripts/_probes/
probe_20260821_anchor_fail_census.py`로 21건을 두 부류로 분해):

- **비율이상 11건**(전부 4Q/연간공시: 흥국화재·에이비엘×3·하나생명×2·농협생명×2·
  교보라이프플래닛×2·IBK연금) — 원인 확인함: 연간(4Q) 필링은 K-ICS 본문 뒤에 **감사보고서
  주석/부속명세서가 같은 경과조치 표를 천원(千원) 단위로 재인쇄**한다(에이비엘 2023.4Q 실측:
  p525의 기본요구자본 `1,325,470,599` = p37 본문표(백만원)의 `1,325,471`×1000 — 즉 p525는
  같은 금액을 천원 단위 숫자로 다시 적은 것). 감사기가 "여러 페이지 중 최댓값"을 앵커로 쓰다
  보니 이 후미 표의 부풀린 숫자를 집어 스케일 판정이 깨진다 — **진단 스크립트 자신의
  다중-occurrence 처리 한계**이지 마스터 데이터 문제가 아니다.
- **occurrence없음 10건** — 흥국화재/흥국생명 2024.4Q(기존에 알려진 wrong-document 이력,
  TODO 6회차 기록)·하나생명 2024.2Q(스캔본, 별도 기록)·악사손해 2024.3Q(텍스트밀도 낮음),
  나머지 6건은 **전부 교보생명(KR0073)의 홀수분기**(2023.1Q·2023.3Q·2024.1Q·2024.3Q·2025.1Q·
  2025.3Q). 교보생명 홀수분기 PDF는 "기본요구자본" 라벨이 텍스트스트림상 값과 떨어진 라벨나열
  블록에 있어(예: p6 "…I. 기본요구자본\n5. 운영위험액\n3. 이익잉여금…" 처럼 라벨만 연속
  나열되고 값은 다른 블록에 있음) 감사기의 "라벨 다음 2개 숫자" 단순 휴리스틱이 못 찾는다.
  **이건 이 read-only 진단 스크립트만의 한계다** — production 파이프라인(`fill_period_to_
  disclosure.py`)은 raw text-stream 이 아니라 docling MD 표를 파싱하므로 이 함정에 안 걸리고,
  실제로 교보생명 2023.1Q 등은 마스터에 item1-28이 정상 적재돼 있다(직접 확인).

**요약**: 앵커불가 21건 중 마스터 데이터가 실제로 의심스러운 사례는 0건이다. 전부 이 진단
스크립트의 "여러 occurrence 중 하나 고르기" 로직의 한계(4Q 부속명세서 중복 인쇄 11건,
교보 홀수분기 텍스트순서 10건)로 설명된다.

### 재현

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/leaf_scale_residue_audit.py
→ 대조 셀 4,516 | 불일치 4 | 앵커불가 21  (수정 전후 불변 — 이번 라운드는 판정만, 쓰기 없음)
```

새 진단 스크립트(전부 read-only): `scripts/_probes/probe_20260821_leaf_residual4.py` ·
`probe_20260821_kr0004_pages.py` · `probe_20260821_kr0004_verify.py` ·
`probe_20260821_anchor_fail_census.py` · `probe_20260821_anchor_ratio_cause.py` ·
`probe_20260821_chubb_item35.py`.

**게이트**: `kics_disclosure.json` 무변경(이 티켓 단독으로는 diff 0) — 다른 두 티켓(tier2
3줄·KB손해 vision)의 변경과 합쳐 세션 끝에 한 번에 검증(본 세션 최종 보고 참조).
