---
from: orchestrator
to: parser
created: 20260821T1105Z
status: answered
route: reparse
company: KR0071
period: 2023.3Q
rule: LEAF_VS_RAW_MISMATCH
lane: kics
iter: 1
---

## 미결 (sender 작성)

**흥국생명 KR0071 2023.3Q item24 = 8,313 은 원문에 없는 값이다.**

raw `data/disclosure/FY2023_Q3/raw/KR0071_흥국생명보험.pdf` **p11**:

```
Ⅲ. 기타 요구자본 (1+2+3)                      8,313   8,313   8,534
1. 업권별 자본규제를 활용한 종속회사의 요구자본 환산치      -       -
2. 비례성원칙을 적용한 종속회사의 요구자본 대응치          -       -
3. 업권별 자본규제를 활용한 관계회사의 요구자본 환산치   8,313   8,313   8,534
```

- item23(부모) 8,313 ✅ · item26(관계회사) 8,313 ✅ · item25 0 ✅
- **item24(종속회사) 는 원문이 "-" 인데 마스터에 8,313 이 들어 있다.** → 23 = 24+25+26 이
  16,626 이 되어 부모의 2배가 된다.

같은 "-" 를 item25 는 0 으로, item24 는 8,313 으로 읽었다 — **같은 기호의 두 가지 해석**이라
진행 중인 티켓 `20260821T1030Z` 의 F3(dash semantics)와 같은 계열이다. 그 티켓을 처리하면서
같이 보는 것이 자연스럽다.

## 할 일

1. item24 를 원문대로 고친다. **"-" 를 0 으로 쓸지 결측으로 둘지**는 같은 표의 item25 처리와
   일관되게 하고, 고른 쪽을 답변에 근거와 함께 적어라(이 표의 "-" 는 "해당없음"이다).
2. **다른 분기·회사도 같은 패턴이 있는지 훑어라**: item23 ≠ item24+item25+item26 인 (회사,분기).
   현재 마스터 실측으로는 이 1건뿐이지만(적용전 401검사, 적용후 203검사), 고친 뒤 재확인할 것.
3. 셀 단위 수정. 마스터 통째 rewrite 금지.

## 하지 말 것

- 원문에 표가 있다. 면제 등재 금지.
- `insurequant_master_tables.xlsx` 이번 턴에 손대지 마라.

## 답변 (recipient 작성)

**1셀 수정, 검증 완료.** `scripts/fix_20260821_kr0071_item24_fabricated_dash.py` (신규, 상주).

- **선택**: item24 값(적용전) `"8313"` → `"0"`. 같은 표의 item25 처리(이미 0)와 일관되게 "-"="해당없음"=0
  으로 읽었다. item24_적용후는 **건드리지 않음**(원래도 None, item25/26_적용후와 동일하게 결측 유지 —
  근거 없이 새 값을 만들지 않는다).
- **전사 스윕**(item23 = item24+item25+item26, 양쪽 컬럼, tol=1.5억 — 3개 독립반올림 정수합의 정상
  노이즈 ±1억을 흡수하도록 조정, 원래 0.5억 문턱은 DB손해·한화생명·삼성생명·교보생명에서 diff=1
  인 반올림 노이즈 26건을 오탐으로 잡아냈었다): **수정 전 위반 1건(KR0071 2023.3Q만) → 수정 후 0건**.
  다른 (회사,분기)에 같은 패턴 없음 확인.

### 검증

```
=== sweep BEFORE fix ===
  KR0071 흥국생명보험 2023.3Q [값] item23=8313.0 vs 24+25+26=8313.0+0.0+8313.0=16626.0
위반 1건

KR0071 2023.3Q item24 값(적용전): '8313' -> '0'
wrote kics_disclosure.json

=== sweep AFTER fix ===
위반 0건
```

- 게이트: `validate_kics_disclosure.py`에 이 항등식(item23=24+25+26) 자체를 검사하는 룰이 없어서
  RED 카운트에는 영향 없음(세션 시작 RED=12 → 이 티켓 처리 후에도 12, 티켓3에서 KR0087 몫만 빠짐).
  데이터 정합성 문제였지 게이트 통과/불통과 문제는 아니었다.
- 셀 census: row_count 변동 0(UPDATE만, INSERT 없음), combo 변동 0.
- 골든: 세션 마지막 최종 확인에 포함(아래 종합 결과).

### 안 한 일

- `insurequant_master_tables.xlsx` 손대지 않음(지시대로).
- 면제 등재 없음 — 원문에 표가 있고 값도 명확했다.
