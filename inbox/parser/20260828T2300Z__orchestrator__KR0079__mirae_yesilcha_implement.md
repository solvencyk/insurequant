---
from: orchestrator
to: parser
created: 20260828T2300Z
status: open
route: reparse
company: KR0079
period: MULTI
rule: PL_YESILCHA_ZERO_OTHER_PLUG
lane: ifrs17
iter: 1
---

## 미결 (orchestrator 작성 — 선행 조사 `20260828T2110Z` 결과 (a) 확정)

**미래에셋생명(KR0079) item6 을 구현한다. item11 은 아래 조건이 닫힐 때만.**

선행 조사가 형식을 규명했다: 한국어 라벨 표가 아니라 DART XBRL 구조화 공시이고, PAA 구분은
별도 표가 아니라 컬럼 헤더(`보험료배분접근법을 적용한 보험계약 외의 보험계약`, 24회 전부 부정형)다.
재현 스크립트 `scripts/_probes/mirae_yesilcha_survey.py`, 상세는 그 티켓 `## 답변`.

### 검증된 것 (orchestrator 독립 확인)

population 3중 대사가 `594,378,172,139원` 으로 닫힌다. orchestrator 가 별도 포괄손익계산서를
직접 떠서 `일반보험서비스수익` 누적이 같은 값임을 확인했다. **원수 보험수익 전체를 덮으며
NH 식 GMM-only vs GMM+PAA 비대칭이 없다.**

### 반드시 먼저 확인할 것 — 발생 측 열 선택 (이 티켓의 최대 리스크)

조사가 발생 4종을 **`LIC열만`** 으로 잡아 item6 = 322,653.643456 − 340,773.783421 =
△18,120.139965백만원 을 냈다. **이것이 NH 에서 owner 가 지적한 바로 그 함정이다.**

NH 실측: 발생 행이 LRC 열(손실요소 · 손실요소외)과 LIC 열에 모두 값을 갖는다. 정답은
**합계 열에서 손실요소배분을 되돌린 값**(351,252)이었고, LIC 열 단독(351,114)은 138 만큼
어긋났다. 작아서 티가 안 났을 뿐 원리가 틀렸다. 근거는 `_resolved/20260828T1400Z` 의 종결 확인 절.

**요구:**
1. 미래에셋 롤포워드에서 그 발생 행이 **LRC 열(손실요소·손실요소외)에 값을 갖는지** 확인해라.
2. 갖는다면 NH 와 같은 원리로 계산해라 — 합계 열에서 손실요소배분 상당액을 되돌린다.
   LIC 열 단독값과 얼마나 다른지 **두 값을 모두 보고해라.**
3. 안 갖는다면(LRC 열이 전부 0) LIC 열 단독이 곧 합계이므로 조사값이 유효하다. 그 사실을 명시해라.
4. 경계 규칙(손실요소배분 = 4종 밖, 양측 상쇄)이 이 회사에서도 성립하는지 **대조로 확인**해라.
   NH 에서 쓴 판별식: 노트의 손실요소배분 값이 롤포워드 손실요소 열과 분기별로 일치하는가.

### item11 (재보험) — 조건부

조사가 item11 = +1,775.344202백만원 을 냈으나 **Tier-1 `출재보험서비스수익` 과 대사가 안 됐다**
(조사 자체가 중간신뢰로 보고). **그 대사가 닫히기 전에는 item11 을 채우지 마라.**
원인 규명이 먼저다. 못 닫으면 item11 은 `0` 으로 두고 왜 못 닫는지 답변에 적어라.
이 축은 폐쇄식이 예실차 0 이어도 닫혀서 틀린 값을 어떤 게이트도 못 잡는다. 오답보다 미결이 낫다.

### 분기 스코프

조사가 **2023.2Q~2025.1Q 구간의 표 레이아웃 확인**을 요청했다. 전 분기에서 같은 구조인지
확인하고, 형식이 다른 구간은 뽑지 말고 보고해라.

### 선행 조건

`20260828T1600Z`(항목32) → `20260828T2100Z`(ABL) → `20260828T1900Z`(NH 재보험) 순으로
같은 마스터에 쓰는 티켓이 대기 중이다. **orchestrator 가 순서를 지정할 때까지 시작하지 마라.**
시작 시 `git status` 확인, 끝난 뒤 전수 항등식 감사로 아래가 살아있는지 확인해라.

```
KR0083 2024.3Q  item27 △2,652억 · item28 △53억 · item30 △5억
KR0032 2026.2Q  item6  △102억  · item7  △797억
```

### 하지 말 것

- `index.html`·`IFRS17.html` 금지. 브랜치 변경 금지. `git push` 금지. `git add -A` 금지.
- `build_root_masters.py` 의 `main()` 통짜 실행 금지. `validate_master_tables.py` 는 `--no-build`.
- 마스터 xlsx 는 `sync_master_xlsx_sheet.py` 로 바뀐 시트만. openpyxl 재저장 금지.
- 골든은 `--update` 재생성 + 커밋에 이유 기록.

원문: `data/dart/FY2026_Q2/raw/KR0079_미래에셋생명/20260814004054.xml`

## 답변 (recipient 작성 — 처리 후)
