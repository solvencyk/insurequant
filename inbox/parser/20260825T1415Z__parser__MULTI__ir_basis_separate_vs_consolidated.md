---
from: parser
to: parser
created: 20260825T1415Z
status: open
route: escalate
company: MULTI
period: MULTI
rule: IR_BASIS_DETERMINATION
lane: ifrs17
iter: 1
---

## 미결 (parser 작성 -- 오케스트레이터 발주 조사 결과)

**질문**: 보험사 IR 자료(팩트시트/실적발표)의 CSM·PL 은 연결 기준인가 별도 기준인가.
owner 가 "CSM·PL 마스터를 별도로 통일" 을 결정했고 다른 세션이 복원 중인 상황에서,
owner 가 "IR 공시자료 PL 은 연결 아니냐"고 재질문해 급히 검증했다.

### 판정: **IR = 별도(separate).** owner 의 "별도 통일" 결정은 CSM·PL 둘 다 옳다.

단, **삼성생명의 PL_breakdown.json 은 현재 연결 기준으로 오염돼 있어 별도 복원 대상에
반드시 포함돼야 한다** — 이 사실이 이번 조사의 핵심 실무 결론이다.

### 파싱 4개사 (data/ir/*/parsed/{KR0069,KR0068,KR0008,KR0011}.json)

**CSM 축 — 4/4사 전원 별도 확인:**

| 회사 | 방법 | 결과 |
|---|---|---|
| 삼성생명(KR0069) | IR xlsx sheet 명시 라벨 `CSM 상세 (별도)` / `보험부채 movement (별도)` + `CSM_waterfall.json` 8a3b930^(전) vs 8a3b930(후) 대조 | 6항목×2개분기(2025.4Q,2026.2Q) 전부 **before(별도)와 일치**(오차 ≤0.1억), after(연결)와는 70~1044억 괴리 |
| 한화생명(KR0068) | IR xlsx (라벨 없음, 구조적 관례) vs 마스터(8a3b930 영향권 밖, 전후 불변) | 6항목 전부 **정확히 일치**(오차 <0.1억) |
| 삼성화재(KR0008) | IR xlsx CSM sheet vs 마스터(전후 불변) | 6항목 전부 **정확히 일치** |
| DB손해보험(KR0011) | IR xlsx BEL,CSM변동 sheet vs 마스터(전후 불변) | 6항목 전부 **정확히 일치**(단 별도/연결 대조 표는 이 회사 IR 파일에 없어 라벨로는 미확증) |

**PL 축 — 4개사 중 3개사는 이미 별도, 삼성생명만 연결로 새는 이상치:**

| 회사 | IR 별도 라벨/값 | IR 연결 라벨/값 | 마스터(PL_breakdown.json) 일치 |
|---|---|---|---|
| 삼성생명(KR0069) | Ⅴ-4 별도 손익계산서, 당기순이익 1,200,474백만(FY25 1H) | Ⅴ-2 **"연결 손익계산서"**, 당기순이익 1,471,076백만 / item17,24 | **연결과 일치** (item1 은 시트 자체 각주 "별도기준" 이라 판별불가, item17/24 로 판별) |
| 한화생명(KR0068) | "(별도) 요약손익" 각주 **"※ SAP 기준"**, 당기순이익 510,212백만(FY26 1H) | "(연결) 요약손익" 각주 "※ GAAP 기준", 당기순이익 904,525백만 | **별도(SAP)와 정확히 일치**(2개 분기 교차검증) |
| 삼성화재(KR0008) | `PL-별도감독` 공식표, 당기순이익 1,253,442백만(FY26 1H) | `PL-연결감독` 공식표 + "손익현황" 탭 각주 **"주) 연결재무제표 기준"**, 당기순이익 1,373,953백만 | **별도와 정확히 일치**(item1 은 손보 다품목 스코프 문제로 미판별, item24 로 판별) |
| DB손해보험(KR0011) | (연결/별도 대조표 IR 파일에 없음, 단일표만) | - | 단일 IR 표와 정확 일치(별도/연결 판별 불가, 대형 자회사 없는 회사라 정합적) |

### 결론 및 근거

1. **CSM 은 4사 전원 별도로 수렴한다.** owner 결정("별도 통일")이 CSM 에 대해 옳다는
   결정적 근거. 특히 삼성생명은 `before`/`after` 8a3b930 대조가 가능해 **owner 가 제시한
   예시 수치(신계약CSM 별도 32,606.0 vs 연결 32,984.9, 2024.4Q)를 그대로 재현**했다.

2. **PL 은 "연결 기준으로 확인됨" 이라는 전제 자체가 삼성생명 단일사례에 근거한 것으로
   보인다.** 실제로 한화생명·삼성화재·(추정)DB손해보험 3사는 **이미 별도 기준으로 정확히
   맞아떨어진다** — 별도 통일 작업이 이 3사의 PL 을 건드리면 오히려 깨진다(지금 맞는 걸
   틀리게 만듦). 삼성생명만 연결로 새어 있다.

3. **삼성생명의 연결 누출은 CSM 에서 일어난 것과 같은 계열의 결함으로 보인다.** 8a3b930
   이 CSM 의 line-65535 오염 블록 선택 버그를 고치면서 "진짜처럼 보이는" 블록을 골랐지만
   그게 하필 연결 섹션 소속이었다(별도 섹션이 아니라). PL 도 같은 회사가 같은 성격의
   추출기 결함을 가진 것으로 추정된다 — **다만 PL 추출 로직 자체는 확인하지 않았다**
   (범위 밖: 이 티켓은 판정 전용, 마스터 파일은 미접촉).

4. **owner 재확인 필요 항목**: (a) 위 판정을 승인하면 진행 중인 별도 복원이 CSM 뿐 아니라
   **PL_breakdown.json 의 삼성생명(KR0069) 항목도 연결→별도로 정정**해야 한다(다른 3사
   PL 은 이미 정답이므로 건드리면 안 됨 -- 전사 일괄 basis-flip 은 금물, 회사별 감사 필요).
   (b) PL 축은 4개사만 확인했다 -- 전사(47개사) 스케일에서 삼성생명 같은 "연결 누출" 이
   더 있을 수 있어, 별도 복원 작업의 검증 단계에서 회사별 IR 대조 전수 감사를 권고한다.

### 근거 파일

- `data/ir/FY2025_Q4/parsed/KR0069.json` (PDF 소스, page 7/11 그대로 인용)
- `data/ir/FY2026_Q2/parsed/{KR0069,KR0068,KR0008,KR0011}.json` (xlsx 소스, 시트/셀 좌표 그대로 인용)
- 재현 명령(전부 읽기 전용, 마스터 미접촉):
  ```
  git show 8a3b930^:CSM_waterfall.json   # 별도(before)
  git show 8a3b930:CSM_waterfall.json    # 연결(after) == 현재 워킹트리
  ```
  각 parsed JSON 의 `notes`/`segment_insurance_income.note` 필드에 셀 좌표·수치까지 적어뒀다.

### 추신 -- 조사 종료 시점 실시간 관측 (병렬 복원 세션 진행상황)

이 티켓을 쓰는 시점에 `CSM_waterfall.json`(워킹트리, 미커밋)을 HEAD 와 대조해보니
**삼성생명(KR0069) 60셀이 이미 `after`(연결)에서 `before`(별도) 값으로 되돌아가 있었다**
(예: 2026.2Q 이자부리 2654.2→2584.6, 2025.4Q 신계약CSM 31214.5→30594.7 -- 위 판정의
"before" 수치와 정확히 일치). 아이엠라이프(KR0094)도 24셀 변경 중. 즉 **병렬 복원 세션이
이미 이 티켓과 같은 결론(별도)으로 삼성생명 CSM 을 고치고 있다** -- CSM 축은 서로 다른
방법(나는 IR 대조, 그쪽은 아마 raw 재추출)으로 같은 답에 도달한 것으로 보인다.

반면 같은 시점 `PL_breakdown.json`(워킹트리)은 **KR0069 셀 변경 0건**(변경은 전부 KR0082,
무관한 별건)이었다. 즉 **삼성생명의 PL 연결-누출은 아직 복원 대상에 안 들어가 있다** --
이게 이 티켓의 실질적 행동 항목이다: CSM 복원이 끝나는 대로 PL_breakdown.json 의
KR0069 item17(투자손익)·item24(당기순이익) 도 같은 별도 기준으로 재추출 필요.

## 답변 (원 sender 재확인 필요)

<owner/오케스트레이터가 위 판정을 검토 후 기입>
