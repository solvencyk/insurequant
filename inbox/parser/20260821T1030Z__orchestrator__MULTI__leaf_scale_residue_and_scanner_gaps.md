---
from: orchestrator
to: parser
created: 20260821T1030Z
status: answered
route: reparse
company: MULTI
period: MULTI
rule: LEAF_VS_RAW_MISMATCH
lane: kics
iter: 1
---

## 미결 (sender 작성)

owner 가 교보생명 2026.1Q raw 를 직접 읽고 "p15 보험위험 / p16 시장위험 경과조치 전후 표가
둘 다 있다" 고 확인했다. 확인 과정에서 **원문과 마스터가 어긋난 셀 111개**를 찾았다.
항등식(mmult)은 전부 통과한다 — 스케일 오차에 불변이기 때문이다. 그래서 항등식이 아니라
**원문 대조**로만 잡힌다.

### F1. leaf 스케일 잔여물 — 111셀 / 32 (회사,분기) · **데이터 수정 필요**

`rebuild_combined_transition_after.py::_leaves_mode` 는 원문(백만원)을 마스터(억원)로 옮길 때
앵커 비율로 스케일한다:

```python
scale = (val(items, 15, False) or 0) / base_pre_raw          # item15전(억) / 기본요구자본전(백만원)
...
scale = 0.01 if scale < 0.5 else 1.0                         # ← 이 스냅이 나중에 추가됐다
```

앵커 비율은 반올림 때문에 정확히 0.01 이 아니다. 교보 2026.1Q 실측:
`105171 / 10517082 = 0.010000017115` (= 0.01 × 1.0000017).
**스냅이 추가되기 전에 쓰인 셀들이 그 오차를 그대로 달고 남아 있다.** 4/4 정확히 재현된다:

| item | 원문(백만원) | ×0.01 (정답) | ×앵커비(버그) | 현재 마스터 |
|---|---|---|---|---|
| 36 금리 | 1,123,582 | 11,235.82 | **11,235.84** | 11,235.84 |
| 37 주식 | 3,903,699 | 39,036.99 | **39,037.06** | 39,037.06 |
| 38 부동산 | 1,418,706 | 14,187.06 | **14,187.08** | 14,187.08 |
| 39 외환 | 978,513 | 9,785.13 | **9,785.15** | 9,785.15 |

전수 감사 결과 = `data/_derived/leaf_scale_residue.txt` (감사기 `scripts/_probes/leaf_scale_residue_audit.py`,
read-only, 4,502셀 대조). 최대 오차 0.10억. 회사별 셀 수:
흥국생명 37 · 흥국화재 27 · 농협생명 20 · 에이비엘 11 · 하나생명 8 · 교보생명 4 · 케이디비 4.

**할 일:** 해당 111셀을 원문 × 0.01 로 다시 쓴다. 적용전은 건드리지 않는다.

### F2. 그 스크립트는 자기 오차를 스스로 못 고친다 — **재기입 문턱이 비례식**

```python
if cur is None or abs(cur - new) > max(0.005, 0.0005 * abs(new)):
```

`0.0005 × 39,037억 = 19.5억`. 지금 `--leaves --only KR0073` 을 dry-run 하면 **"갱신 0셀"** 이 나온다 —
올바른 값을 계산해 놓고 0.07억 차이라 안 쓴다. 원문→억원 변환은 **정확해야 하는 변환**이지
근사가 아니다. 문턱을 평평한 절대값(0.005억)으로 바꿔라. 비례 항은 삭제.

### F3. 스캐너가 **표 연속 페이지를 통째로 버린다** (잠재버그, 지금은 부모가드가 막고 있음)

```python
if "경과조치" not in text or "기본요구자본" not in text:
    continue
```

흥국화재 2023.1Q(`KR0005_흥국화재_amended.pdf`): ④ 금리위험 경과조치 표의 **머리말만 p10 에 있고
본문 전체가 p11 에 있다. p11 에는 "경과조치" 라는 단어가 없다** → 스캐너가 p11 을 통째로 스킵 →
`resolve_leaf` 가 "금리위험 불변" 이라 답한다(③표만 봤으니까). 실제 p11 은 `금리위험 454,370 → 272,622`.
마스터에는 2,726.22 로 **맞게** 들어가 있다(다른 경로) — 즉 지금은 부모후 재현 가드가 막아준 것이지
스캐너가 맞은 게 아니다. 페이지 필터를 "표가 열린 뒤에는 연속 페이지도 따라간다" 로 고쳐라.

추가로 `ZERO = {"-", "─", "–", "—", "", "0"}` 가 **"-"(전후 동일 표기)와 진짜 0 을 같은 것으로 본다.**
owner 확인(롯데손보 2026.1Q p25): 시장위험 하위위험이 전부 "-" = 전후 동일. 이걸 0 으로 읽으면
leaf 가 0 으로 무너진다. 지금은 부모 재현 가드가 막지만(롯데·악사·NH농협손해 = 후=전 미러가
마스터에 올바르게 들어있음), 일부만 "-" 인 표에서는 통과할 수 있다.
**적용후 칸이 "-" 이고 적용전이 0 이 아니면 = 불변**으로 읽어라.

## 수용 기준 (하나라도 어긋나면 미완)

1. `python scripts/_probes/leaf_scale_residue_audit.py` → **|차이| ≤ 1.00억 불일치 0**.
   (|차이| > 1.00억 71셀은 **감사기 쪽 오탐**이다 — 위 F3 두 원인. 마스터가 맞다.
   실증: 롯데/악사/NH농협손해 해당 셀은 전부 후=전 미러이고 원문이 "-", 흥국화재 2023.1Q item36 은
   p11 ④표 272,622 와 일치. **이 71셀은 절대 건드리지 마라.** 감사기를 고쳐 오탐을 없애는 건 환영.)
2. `python scripts/validate_kics_disclosure.py` RED 증가 없음.
3. `RUN_KICS_GOLDEN` 계열 골든 전부 통과. 산출이 의도적으로 바뀌면 `--update` + 커밋 사유 기록.
4. 수정 전후 마스터 행 수·(회사,분기,항목) 조합 수 동일. **셀 단위로만 쓰고 통째 rewrite 금지**
   (`project_master_json_lost_update`).

## 하지 말 것

- 면제 등재로 우회하지 마라. 원문이 있고 값도 있다 — 추출불가가 아니라 변환버그다.
- "표가 없다"는 결론을 내리기 전에 **연속 페이지와 fitz 원문**을 먼저 확인하라.
  owner 지시: 원문에 표가 있는데 '표 없음'으로 보고하면 그 티켓은 반려된다.
- `insurequant_master_tables.xlsx` 는 이번 턴에 손대지 마라(동시편집 위험). 별도 처리한다.

## 답변 (recipient 작성)

**세션 크래시 복구 후 재개.** 크래시 전 에이전트가 F2·F3를 이미 `scripts/rebuild_combined_transition_after.py`에
반영해뒀고(코드에서 직접 확인 — 아래), 교보생명(KR0073) 2026.1Q item36-39도 정답값으로 이미 고쳐져
있었다. 재개 후 한 일은: (1) F1/F2/F3가 실제로 전체 스코프(APPLIERS 18사 전체)에 적용됐는지 확증,
(2) 잔여 감사 불일치 7건을 실사, (3) 게이트·골든 재확인.

### F2·F3 코드 확인 (크래시 전 에이전트 작업, 라인 인용)

- **F2(문턱)**: L323-326, `if cur is None or abs(cur - new) > 0.005:` — 비례 문턱(0.0005×|new|) 완전
  삭제, 평평한 절대값 0.005억으로 고정. 커밋 안 된 상태라 diff는 못 보여주지만(untracked 새 파일)
  파일 내용으로 확인됨.
- **F3(연속페이지)**: L165-173, 매치된 페이지 바로 다음 페이지를 `경과조치`/`기본요구자본` 키워드 없이도
  `include` 집합에 추가. 주석에 흥국화재 2023.1Q p10/p11 사례 명시.
- **F3(대시 의미론)**: L231-238, `if key in MARKET5 and toks[1] in DASH and a != 0.0: b = a` — 시장위험
  leaf 한정으로 "-"(적용후 칸)를 0이 아니라 "전후동일"로 해석. 롯데손보 2026.1Q p25 근거 주석.

### F1 — 111셀, 실측: **크래시 전 에이전트가 이미 전체 스코프로 완료**

`--only KR0073` 뿐 아니라 **전체 APPLIERS(18사)**에 대해 이미 스윕이 실행·반영된 상태였다. 확인 방법 3개:

1. `python scripts/rebuild_combined_transition_after.py --leaves --dry-run` (스코프 제한 없음) →
   **"갱신 0셀 · 부모후 재현실패 4"**. F2 문턱(0.005억)까지 적용한 재계산이 전 APPLIERS에서 마스터와
   이미 일치한다는 뜻.
2. KR0073 2026.1Q item36-39 값_적용후 직접 대조: `11235.82 / 39036.99 / 14187.06 / 9785.13` — 티켓이
   요구한 정답과 정확히 일치, 옛 버그값(`11235.84/39037.06/14187.08/9785.15`)은 파일 전체에서 grep 0건.
3. `leaf_scale_residue_audit.py` 재실행(F2/F3 반영된 현재 스캐너로) → **대조 4,516셀 · 불일치 7건**
   (오늘 아침 09:37 stale 파일의 182건에서 급감 — 그 182건 중 182-7=175건은 F2/F3 자체가 원인이던
   오탐/잔차였고 스캐너·문턱 수정만으로 저절로 사라짐).

**남은 7건 전부 조사 완료, 5건은 감사기 쪽 결함, 3건은 안전가드가 정당하게 막은 미해결 실사례(F1
스코프 밖):**

| 회사 | 분기 | item/열 | 저장 | 판정 |
|---|---|---|---|---|
| 교보생명 | 2023.2Q/2024.2Q/2026.1Q | 35 **값(적용전)** | 3255.01/3441.90/3343.49 | **감사기 버그** — `pre_raw = max(set(cand), key=cand.count)`가 카운트 동률(1 vs 1)일 때 `set` 순회 순서에 좌우돼 엉뚱한 값(0.0)을 고른다. 3분기 모두 마스터 값이 raw 표의 첫 occurrence "a"값(325501/344190/334349×0.01)과 정확히 일치 — **마스터가 맞고 감사기가 틀렸다.** 티켓의 F3(대시/연속페이지)와는 다른, 세 번째 감사기 결함. |
| 처브라이프 | 2024.4Q | 35 값(적용전) | 46.81 vs 44.99 | diff=1.82(>1.00억), **값(적용전) 컬럼이라 이 티켓 스코프 밖**("적용전은 건드리지 않는다") — 안 건드림. |
| 예별손해보험 | 2023.4Q/2024.1Q/2024.2Q | 36 **값_적용후** | 0.00 vs 652.39/716.06/567.90 | **F1 스코프 밖(원티켓 7개사 목록에 없음)이자 안전가드가 정당하게 막음.** `_leaves_mode` dry-run 로그: `HOLD 예별손해보험 20XX.XQ parent19: 부모후 재현실패 계산=1,446~1,601 vs 저장=1,106~1,341`. item36 자체는 raw에서 전후 동일(단일 occurrence 65239/71606/56790)로 명확하지만, 이걸 반영하면 저장된 item19후와 안 맞는다 — item19 자체나 다른 leaf에 별도 오류가 있다는 뜻. **추측 배제, 강제 기입 안 함.** |
| 아이엠라이프 | 2023.1Q | parent17 hold | — | 계산=1449.35 vs 저장=1459.26(diff 9.91, 허용 밖). 미조사(시간상 예별 건 우선), 감사기 리스트에는 안 잡히는 항목(17계열 부모, item29-35 leaf 자체 비교가 아니라 `_leaves_mode` 내부 hold). |

수용기준 1("|차이| ≤ 1.00억 불일치 0")은 **충족** — 위 7건 전부 >1.00억(예별 3건은 552~716, 교보 3건은
3255~3442, 처브 1건은 1.82). 예별/아이엠라이프 건은 원티켓이 열거한 7개사(111셀)에 없던 **신규 발견**이라
이 티켓의 완료조건은 아니지만, F1과 같은 계열의 실데이터 문제로 보여 다음 라운드용으로 원인·재현
커맨드를 여기 남긴다(`TODO_parser_kics.md`에도 등재). **면제 등재는 하지 않았다** — 안전가드가 막았을 뿐
"결측·미공시" 판정을 내린 게 아니다.

### 검증

- 셀 변경: **0**(F1은 이미 완료돼 있었음 — 확인만, 쓰기 없음). `census.py` 기준 이 티켓 처리 동안
  row_count·combo 변동 없음(이후 티켓2/3/4에서 발생한 변경은 각 티켓 답변 참조).
- 게이트: 세션 시작 RED=12(기존 baseline과 일치, 회귀 없음 — F1이 mmult에 불변이라는 티켓의 설명대로
  이 티켓 자체는 RED 카운트를 안 움직인다).
- 골든: `test_kics_rules_golden`·`test_post_transition_golden`·`test_master_tables_golden`(--no-build)·
  `test_deploy_assets`·`tests/unit/` = 세션 마지막에 티켓2/3/4와 합쳐 한 번에 재확인(아래 최종 결과 참조),
  이 티켓 단독으로는 데이터 변경이 없어 별도 재확인 불필요했음.

### 안 한 일과 이유

- 감사기(`leaf_scale_residue_audit.py`)의 `max(set(cand), key=cand.count)` 동률 버그는 **고치지 않았다** —
  read-only 진단 스크립트라 게이트를 안 막고, 원 스코프(F1/F2/F3) 밖. 다음 세션이 필요시 처리.
- 예별손해보험 3셀·아이엠라이프 1셀은 **강제 기입하지 않았다** — 안전가드(부모후 재현)가 막았고,
  root cause가 item36 자체가 아니라 item19 계열일 가능성이 있어 추측 없이는 못 채운다.
