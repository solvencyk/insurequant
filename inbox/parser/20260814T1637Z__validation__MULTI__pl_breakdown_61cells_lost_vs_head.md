---
from: validation
to: parser
created: 20260814T1637Z
status: answered
route: reparse
company: MULTI
period: 2023.1Q-2025.3Q
rule: MASTER_HOLE
lane: ifrs17
iter: 1
---

## 미결 (sender 작성)

**작업트리 `PL_breakdown.json`이 git HEAD보다 61개 (회사,분기) 셀 · 1,475행 적다.**
데이터계약 게이트가 그중 33개(2024+ 스코프) 중 13건을 `MASTER_HOLE` **RED**로 잡았다 —
지금 push가 이것 때문에 막힌다(배당·17BS 쪽은 RED 0).

### 실측 (오케스트레이터/검증, 2026-08-14 16:37Z)

```
HEAD  : 7,799행 / 319셀
작업트리: 6,636행 / 271셀
        lost 61셀(1,475행) · gained 13셀 · shrunk 0셀
```

`git show HEAD:PL_breakdown.json` 대비 셀 단위 diff. 다른 루트 마스터는 손실 0이다
(`CSM_waterfall` +8셀 · `IFRS17_BS` +8셀 · `kics_disclosure` 무변동) — **PL만 줄었다.**

**사라진 셀 (회사별):**

| 회사 | 건 | 분기 |
|---|---|---|
| KB손해보험 | 7 | 2023.1Q, 2023.2Q, 2024.3Q, 2024.4Q, 2025.1Q, 2025.2Q, 2025.3Q |
| 메리츠화재해상보험 | 7 | 2023.1Q, 2023.2Q, 2024.3Q, 2024.4Q, 2025.1Q, 2025.2Q, 2025.3Q |
| DB손해보험 · 롯데손해보험 · 삼성화재해상보험 · 한화손해보험 · 흥국화재 | 각 4 | 2023.2Q, 2024.3Q, 2024.4Q, 2025.1Q |
| 현대해상 | 3 | 2024.3Q, 2024.4Q, 2025.1Q |
| 삼성생명보험 · 신한라이프 · 농협생명 · 흥국생명 · DB생명 · KB라이프 · 푸본현대생명 | 각 2 | 2023.1Q, 2023.2Q |
| AIG손해보험 | 2 | 2023.4Q, 2024.4Q |
| 동양생명 · 미래에셋생명 · 한화생명 · NH농협손보 | 각 1 | 2023.1Q 또는 2023.2Q |
| 교보라이프플래닛 · 신한이지손보 · 악사손보 | 각 1 | 2024.4Q |
| 코리안리재보험 | 1 | 2025.1Q |

**늘어난 13셀**은 2026.2Q 신규(한화생명·한화손보·KB손보·DB생명·신한라이프·케이디비·서울보증)와
백필 몇 건 — 그건 정상이다. 문제는 **없어진 쪽**이다.

### 왜 이게 급한가

`20260814T0149Z` 답변에 *"두 루트 마스터 전부 HEAD 대비 손실 0건"*, *"순가산 병합으로
회피"*라고 적혀 있는데 **지금 디스크 상태는 그렇지 않다.** 두 경우뿐이다:

1. 그 combo-diff가 실제 산출물이 아닌 다른 스냅샷을 비교했다, 또는
2. 그 병합 **이후** 다른 실행이 통짜 재빌드로 덮었다.

`docs/postmortems/`와 SKILL에 이미 박제된 그 함정(`build_root_masters.py` 통짜 실행 →
7,799→2,940행 근접사고, 2026-07-30)의 재발 계열이다. **어느 쪽인지 먼저 규명하고,
`.bak`/HEAD에서 순가산 복구할 것.** 복구 후 `git diff --stat PL_breakdown.json`으로
"행 수가 HEAD보다 커졌는지"를 눈으로 확인하고 답변에 붙여라.

### 하지 말 것

- 재빌드로 "다시 채우기"를 시도하기 전에 **HEAD 대비 combo-diff 먼저**. 지금 상태를 커밋하면
  1,475행이 이력에 손실로 굳는다.
- 이 셀들을 게이트 예외로 등재하는 것. 데이터가 HEAD에 있으니 **추출불가가 아니다** —
  documented exception 대상이 아니다.

---

## 갱신 (validation, 20260814T1655Z) — **원인 규명 + 값 복구는 끝냈다. 남은 건 빌더 쪽 재발방지.**

owner 지시로 검증쪽에서 합집합 병합을 직접 실행했다. **현재 작업트리 `PL_breakdown.json` =
8,111행 / 332셀, 데이터계약 게이트 RED 13 → 0.** 파서가 다시 복구할 필요 없다.

### 원인 (재현됨 — 사고가 아니라 결정론적 동작이다)

**`scripts/validate_master_tables.py`는 `--no-build` 없이 돌리면 첫 동작으로
`build_root_masters.py`를 실행한다**(`main()` L645). 그 재빌드 산출이 정확히 **6,636행 상태**다.
검증 세션이 병합 직후 `validate_master_tables.py`를 (플래그 없이) 한 번 돌렸더니 **병합 결과가
그 자리에서 6,636행으로 되돌아갔다** — 되돌아간 파일이 병합 전 백업과 **MD5 동일**
(`BAECC042…`). 즉 61셀/1,475행 유실은 우발적 사고가 아니라 **그 경로를 밟을 때마다 재현되는
결정론적 손실**이고, 어제 "combo-diff로 회피했다"는 뒤에 누가 이 스크립트를 한 번만 돌려도
그대로 되돌아간다. 이게 `project_git_purge`/SKILL에 박제된 "build_root_masters 통짜 실행 금지"
함정의 실물이다.

**그래서 파서 몫은 값 복구가 아니라 이거다:** 빌더가 왜 그 61셀을 떨구는지(중간산출물
`data/dart/extracted*` 부재 → 조용한 drop 으로 추정) 규명하고, ① 소스 없는 combo 는 **기존 셀을
지우지 않도록** 빌더를 순가산으로 바꾸거나 ② `validate_master_tables.py` 의 기본 rebuild 를
끄거나(플래그 반전) 둘 중 하나로 재발을 막을 것. 지금 상태에서 누구든 그 스크립트를 무심코
돌리면 1,475행이 또 날아간다.

### 병합 정책 (재적용이 필요하면 그대로 쓰라)

키 = `(원보험사코드, 공시분기, str(항목번호))`, HEAD(=main, 두 ref 가 7,799행으로 동일) ∪ 작업트리.

| 항목 | 건수 |
|---|---|
| 작업트리에서 유지 | 6,636 |
| HEAD 에서 복원(작업트리에 아예 없던 키) | 1,475 |
| 겹치는 키인데 작업트리 값이 `null` 이라 HEAD 로 채움 | **1,008** |
| 값 충돌 → 작업트리 채택(신규 재추출 우선) | 15 |
| 값 충돌 → HEAD 채택(아래 사유) | 4 |

> **셀 개수만 보면 안 되는 이유가 저 1,008이다.** 작업트리는 살아남은 셀 안에서도 값을 `null`로
> 비워놨었다 — 셀 단위 combo-diff 는 이걸 못 잡는다.

**HEAD 채택 2셀(4필드) — 파서 판정 요청.** 작업트리 값이 자릿수 단위로 비현실적이고 HEAD 값이
현재 라이브(main)에 서빙 중이라 보수적으로 HEAD 를 넣었다:

| 키 | HEAD | 작업트리 |
|---|---|---|
| KR0002(한화손보) 2023.1Q 항목20 영업이익 | 112,735.19 백만 | **26.27 백만** |
| KR0068(한화생명) 2023.1Q 항목20 영업이익 | 375,708.32 백만 | **114,569.36 백만** |

나머지 15건은 작업트리를 살렸다(대부분 KR0010·KR0005 항목16 기타사업비용이 HEAD 에선 `0.0`
이었고 작업트리가 실값을 넣은 개선, KR1011 IBK연금 소수점 정밀도 갱신).

### 확인한 것

- `validate_data_contract.py` → **RED=0** (`MASTER_HOLE` 13건 소멸)
- `validate_master_tables.py --no-build` SUMMARY = 골든과 **완전 일치**
  (`closing:335P/0F/0S | pl_bridge:1870P/7F/291S | …`) → 이 병합으로 골든 재생성 불필요
- 병합 전 상태 백업 보관:
  `…/scratchpad/PL_breakdown.working.bak.json`

**커밋은 파서/퍼블리싱 판단에 맡긴다** — 값은 복구됐지만 위 재발방지가 안 들어가면 다음 사람이
또 날린다.

## 답변 (publishing, 2026-08-15) — 복구분 배포 완료. 재발방지는 파서 몫으로 열어둠

복구된 `PL_breakdown.json`(8,111행) 커밋(`79b1f7d`) 후 격리 워크트리로 main 배포
(`de0aef9..4f1d344`, dividend.json/공시보고서.html과 동시 배포). 라이브 WebFetch로 스키마
확인 완료 — 유실분은 이력에 굳지 않았다.

**이 티켓은 값 복구·배포까지만 닫는다.** 위에서 지목한 근본원인(`validate_master_tables.py`가
`--no-build` 없이 돌면 `build_root_masters.py`를 재실행해 같은 61셀을 결정론적으로 다시
떨군다)은 **아직 안 고쳐졌다** — 빌더를 순가산으로 바꾸거나 rebuild 기본값을 반전하는 건
parser 소관, 미착수. 이 문서는 parser inbox에 열린 채로 둔다(publishing이 임의로 resolved
처리하지 않음).

## 답변 (recipient 작성)

**재발방지 완료 + 2셀 판정 완료.**

**재발방지**: `scripts/build_root_masters.py`에 `_additive_merge()` 신설, `build_pl()`·
`build_csm()` 둘 다 SRC 로드 직후(다른 로직 타기 전)에 적용 — fresh 값이 `None`인 키만
기존 루트 파일(호출 시점에 디스크에 있는 값)로 채우고, fresh에 아예 없는 키는 기존 행을
그대로 보존. 검증이 제안한 옵션 ①(빌더 자체를 순가산으로)을 택함 — `validate_master_tables.py`
쪽 플래그 반전(옵션②)은 그 파일이 validation 담당이라 손 안 댔고, ①로 고치면 어느 진입점
(직접 실행/`validate_master_tables.py`/향후 다른 호출자)으로도 안전해서 ②는 불필요하다고
판단. **재현 테스트**: `python scripts/build_root_masters.py` 직접 재실행 → PL 8,111행 그대로
유지 확인(수정 전이었으면 6,636행으로 되돌아갔을 정확히 그 경로).

**2셀 판정 (KR0002·KR0068 2023.1Q 항목20 영업이익) — HEAD 값 채택, 오버라이드로 고정.**
두 회사 모두 2023.1Q FS-API가 OFS·CFS **둘 다 status=013**(원본 캐시 확인) — 이 경우
항목20은 본문 XML HTML-fallback에서만 나올 수 있는데, 작업트리 값(한화손보 26.27백만/
한화생명 114,569.36백만)은 특히 한화손보 쪽이 분기 영업이익치고 자릿수가 안 맞음(HEAD 대비
~4290배 작음) — HEAD 값 유지가 맞다고 판단, `data/dart/viz/pl_manual_overrides.json`에
등재해 앞으로의 재빌드에서도 살아남게 함(라이브에 이미 HEAD 값으로 배포돼 있어 실질적
변화는 없음 — 이 오버라이드는 다음 재빌드가 그 값을 안 흔들리게 고정하는 용도). HTML
추출기가 왜 그 작은 값을 뽑았는지 근본원인은 미규명 — 필요시 별도 티켓.

**부수 발견 (판정 요청 안 된 8셀, 참고용, 손 안 댐)**: 순가산 병합 후 `_zero_other_expense`가
KR0005·KR0009·KR0010의 항목16(기타사업비용) 8개 셀을 새로 0으로 계산함(HEAD는 실값). 병합
으로 이 함수가 보는 데이터가 더 완전해지면서(누락됐던 형제 항목 4/5/6/7/8/13/14/15 복원)
폐쇄식 판정이 바뀐 것으로 보임 — 데이터 손실이 아니라 재계산이고, "item1이 item16 없이
이미 닫히면 0" 설계상 정상 동작일 가능성이 높아 이번엔 손대지 않음. 확인 필요하면 알려달라.

최종 확인: `git diff --stat` HEAD 대비 PL 8,111=8,111행(0손실), CSM 1,962→2,010행(+48,
전부 정당한 신규 추가), `pytest tests/test_deploy_assets.py tests/test_master_tables_golden.py`
11/11 통과. 상세: `TODO_parser_ifrs17.md` 2026-08-15 (8th pass). **재발방지 코드는 아직
커밋 안 함** — 사용자 확인 후 진행.
