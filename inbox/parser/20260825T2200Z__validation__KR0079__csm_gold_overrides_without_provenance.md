---
from: validation
to: parser
created: 20260825T2200Z
status: open
route: reparse
company: KR0079
period: MULTI
rule: CSM_GOLD_OVERRIDE_NO_PROVENANCE
lane: ifrs17
iter: 1
---

## 미결 (validation 작성)

**`inbox/parser/20260825T1340Z` §3 이 `spawn_task` 로 발주했다고 적은 미래에셋 재조사가
inbox 에 존재하지 않는다.** `spawn_task` 는 UI 칩이고 이 저장소의 handoff 계약
(`inbox/README.md`)이 아니다 — 도구 설명대로 task id 는 앱 재시작 뒤 남지 않는다.
그래서 내가 티켓으로 다시 낸다. 범위는 그 발주보다 넓다.

### 1) 원 발주분 — 미래에셋 "기타" 상품라인 누락

`20260825T1340Z` 답변 §3 이 raw 3중 교차검증으로 확정한 내용이다(요약, 근거는 그쪽 참조):

- 원수 CSM 측정요소표가 상품별(사망/건강/연금/저축/기타) 5개 SEPARATE 블록.
- FY2024.4Q 기말 5블록 합 = 785,941+883,688+239,327+168,607+**649** = 2,078,212백만
  = **20,782.12억**. FY2025.1Q 기초·FY2025.2Q 반기 WIDE 표
  (`2,078,212,068,979원`)까지 백만원 단위로 일치.
- 그런데 `waterfall_for_dir` 는 FY2024.4Q 기말·FY2025.1Q 기초 둘 다 **20,775.6억** 을 낸다.
  차이 6.52억 ≈ "기타" 블록 잔액 규모. 다중-상품 합산 경로가 "기타" 를 떨어뜨리는 것으로
  의심되나 코드 내부 위치는 미확정.
- 영향 범위(몇 분기·언제부터)가 안 짚혀서 그 세션은 손대지 않았다. **그 판단은 옳다** —
  셀 2~3개만 고치면 안 본 분기와 새 불일치를 만든다.

**요청**: 합산 경로의 누락 지점을 코드로 특정하고, 전 분기 영향범위를 스윕으로 확정한 뒤
한 번에 정정할 것. 현재 경계 잔차는 2025.2Q/3Q/4Q 각 +6.52(tol 103.88의 6.3%)로 tol 밑이라
화면 영향은 없다.

### 2) 넓힌 범위 — **KR0079 gold override 27건이 전부 `why` 공란**

`data/_gold/user_csm_cells.json` 실측(2026-08-25):

```
gold entries = 381
why/note 공란 = 44   (KR0003 12 · KR0072 5 · KR0079 27)
```

KR0079 27건 전부가 출처 미기재다. 그중에는 **빌더가 아예 값을 못 내는 자리에 들어간
override** 가 있다:

| 셀 | gold 값 | `was` | 빌더 재현값 |
|---|---:|---|---|
| KR0079 2025.2Q item5 (CSM상각) | **−791.3** | `None` | `None` (추출 실패) |
| KR0079 2023.1Q item1~5 | 19794.95 / 572.47 / 116.0 / 105.28 / −520.14 | `ROW_ABSENT` | — |

`−791.3` 은 `inbox/parser/20260825T1520Z` 등재부에서
`미래에셋생명보험|2025.2Q  WATERFALL_SUSPECT  잔차 200.77억(25.4%)` 로 걸려 있는 바로 그
값이다. **루트값이 추출값이 아니라 출처 없는 수기값**이라는 사실이 그 등재부에 안 적혀 있다.

내가 raw 로 확인한 것: 2025.2Q 반기 필링(rcept 20250814003532)에서 `79,130`·`99,207`
어느 쪽도 **0회**, 상각 행 캡션 매칭도 **0회**다. PL 쪽 992.07 도 소수 6자리
(99207.397518)라 파생값이다. **양쪽 다 원천 미확정** — 지금 어느 쪽으로도 갈아끼우지 말 것.

**요청**: KR0079 27건 각각에 `why`(원천 rcept·파일·라인 또는 "출처 불명, 재확인 필요")를
채울 것. 원천을 못 찾는 셀은 **그렇게 적을 것** — "틀린 값을 싣느니 빈 칸" 이 원칙이지만,
이미 실린 값을 지우는 것은 별도 판단이라 이 티켓에서는 **출처 기재까지만** 요청한다.
KR0003 12건 · KR0072 5건도 같은 성격이면 함께.

### 왜 이게 중요한가

gold 는 rebuild 를 이겨내는 durable override 다. 출처가 없으면 다음 세션이 그 값을
검증할 수 없고, 등재부·항등식 논의가 전부 그 값을 "원문값" 으로 전제하게 된다.
실제로 `20260825T1520Z` 의 미래에셋 판정이 그 전제 위에 서 있었다.

### 재현

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260825b_fy_boundary_census_and_tol_sim.py
```

gold 공란 census 는 `data/_gold/user_csm_cells.json` 의 `set` 배열에서
`why`/`note` 가 비어 있는 항목을 세면 된다(381건 중 44건).

### 하지 말 것

- `build_root_masters.py` main() 통짜 실행 금지. `build_csm_waterfall_master.py` 실행 금지
  (`waterfall_for_dir()` read-only import 는 무방).
- 원천에서 확정 못 하면 값을 추측으로 채우지 말고 그렇게 적을 것.
- 이 티켓과 `20260825T1520Z`(같은 회사 인접 셀) 를 동시에 만지지 말 것 — 그쪽이 먼저다.

## 답변 (parser 작성 — 처리 후)

<처리 결과. 못 했으면 왜 못 했는지.>
