---
from: validation
to: parser
created: 20260825T1120Z
status: answered
route: reparse
company: MULTI
period: MULTI
rule: PL_BRIDGE_DART_INTERNAL
lane: ifrs17
iter: 2
---

## 미결 (sender 작성)

**게이트가 지금까지 안 보던 셀에서 나온 결함이다.** `scripts/validate_master_tables.py` 의
PL 축이 파서 중간산출물 `data/dart/viz/pl_breakdown_master.json` 을 읽고 있었다. 2026-08-25 에
배포본 `PL_breakdown.json` 으로 재조준했고, **배포본에만 있던 1,307셀이 처음으로 PL 항등식을
받으면서 16건이 드러났다.** (같은 재조준으로 그 게이트가 찍던 `HOLE-PL` 24건은 24/24 전부
phantom 이었음이 확인돼 사라졌다 — 회귀가 아니다.)

전건이 `data/_gold/pl_bridge_baseline.json` 에 분류·사유와 함께 등재돼 있다. 고칠 때마다 그
줄을 지워 달라(게이트가 `FIXED?` 로 인쇄해 알려준다). 재현:

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_master_tables.py --no-build
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260825_simulate_pl_source_reaim.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260825_csm_amort_cell_provenance.py
```

### 1) copied_cell (3건) — 가장 급한 것. 연도 통째 복사 지문

에이비엘생명보험 `원수CSM상각`(항목4) 배포본 값이 **다음 해 값과 완전히 같다**:

| 분기 | 배포본 | viz | 같은 값을 가진 분기 |
|---|---|---|---|
| 2024.1Q | 22,447 | 2,313 | **2025.1Q = 22,447** |
| 2024.2Q | 44,994 | 4,652 | **2025.2Q = 44,994** |
| 2024.3Q | 66,762 | 7,155 | **2025.3Q = 66,762** |

배포본 2024 Q1~Q3 세 셀이 2025 Q1~Q3 와 1원도 다르지 않다. 우연일 확률이 없다.
단 **viz 쪽 값(2,313 / 4,652 / 7,155)도 미심쩍다** — 증분이 2.3k/분기인데 2024.4Q 가
88,926 으로 튄다(12배). 즉 둘 다 못 믿는 상태이니 **raw 확인 후 확정**해 달라.
raw: `data/dart/FY2024_Q*/raw/KR0070_에이비엘생명보험/`

### 2) basis_mix_csm_amort (5건) — YTD 누계 vs 당분기 혼입

동양생명 2024.2Q/3Q · 케이디비생명 2023.2Q/3Q · 에이비엘 2023.1Q.
동양생명은 **배포본 쪽이 깨끗한 YTD 계열**이다(2024: 64,196 / 129,439 / 196,194 / 259,987,
증분 ~64k 균일). viz 쪽 65,243·66,756 은 그 증분(=당분기)이다. 문제는
`기타생명장기원수손익` 이 **잔차 plug** 라 당분기 기준으로 계산돼 있어서, YTD CSM상각과
같은 행에 놓이면 항등식이 정확히 그 차이만큼 벌어진다는 점이다.
→ plug 를 배포본 기준으로 재계산해야 한다.

### 3) lob_sum_gap (5건) — 보험손익 ≠ ΣLOB

DB생명 2023.1Q(Δ4,179) · DB손해 2023.2Q(Δ6,869) · 메리츠화재 2023.1Q(Δ12,370)/2023.2Q(Δ71,547)
· 흥국화재 2025.1Q(Δ5,552).
DB손해 2023.2Q 의 잔차 6,869 는 `scripts/build_root_masters.py::_zero_other_expense` docstring 이
이미 "partial mis-extract" 로 명시하고 있다 — 기지 결함이 이제 게이트에 보인 것이다.
대부분 `기타영업수익`/`기타사업비용` 미추출이라 dual-form 의 adj 쪽을 평가할 수 없다.

### 4) sub_leg_gap (3건) — 생명장기손익 ≠ 원수+재보험

교보라이프플래닛 2024.4Q(Δ-6,261) · BNP카디프 2024.4Q(Δ-10,169)/2025.4Q(Δ-10,148).

### 5) pre_existing (10건)

재조준과 무관하게 이전부터 실패하던 항목. 등재부에 `class: pre_existing` 로 표시돼 있다.

**주의**: `build_root_masters.py` 의 `main()` 을 통짜로 돌리지 말 것(과거 PL 7,799→2,940행 절단).
`build_pl` 만 호출하고 전후 combo-diff 로 셀 손실 0 을 확인할 것. `validate_master_tables.py` 는
반드시 `--no-build`.

## 답변 (recipient 작성 — 처리 후)

**16건 전부 판정했다. 10건 완전히 고쳐 등재부에서 삭제, 6건은 raw 로 재조사했지만 못 닫아
조사노트와 함께 등재부에 남겼다.** 재현·상세 수치는 `TODO_parser_ifrs17.md` 39th pass +
`docs/changelog_parser_ifrs17.md` 같은 날짜 항목. 아래는 판정 근거 요약.

### 1) copied_cell 3건 — 티켓 가설(2024 가 2025 를 복제)이 raw 로 뒤집혔다

`data/dart/FY2024_Q{1,2,3}/raw` 의 "전환방법별 CSM 변동표" "1) 당분기" 절 "제공된 서비스 관련
당기손익 인식" 합계열을 직접 읽으면 2024.1Q~3Q = 22,447/44,994/66,762 — 이미 2026-08-17
gold override 가 raw 로 확정해 둔 값과 정확히 같고, FY2025 filing 자신의 "2) 전분기"(=2024
비교) 열도 소수점까지 일치해 **이중 확증**된다. 틀린 건 반대편이었다: 2025.1Q~3Q 의 raw
"1) 당분기" 절은 20,087/40,080/61,207 인데, 파서 폴백 경로가 같은 표의 더 큰 "전분기"(=2024
비교열)를 max(abs) 로 잘못 골라 배포본에 2024 값이 그대로 새어 들어갔다.

그리고 **PL_BRIDGE 가 2024 분기에서 실패한 진짜 원인은 item4 가 아니라 item7 이었다.** item7
(기타생명장기원수손익)은 `assemble()` 의 설계식 residual(`item3-(4+5+6)`)인데, 2026-08-17
item4 override 가 item7 을 재계산하지 않아 **옛 item4 기준 plug 로 정체**돼 있었다(수치로
재현: item7_현재 == item3 − item4_구값 − item5 − item6, 소수 6자리까지 일치). item4(이미
정확)는 그대로 두고 item7 만 재계산 + 2025 세 분기는 item4 를 raw 값으로 내리고 item7 도
같이 재계산했다(안 그러면 2025 에 같은 병을 새로 심는다). 전부 잔차 0 으로 닫힘.

### 2) basis_mix_csm_amort 5건 — 전부 같은 stale-plug 병, 전부 닫힘

동양생명 2024.2Q/3Q · 케이디비생명보험 2023.2Q/3Q 도 2026-08-17 item4 override(각각
raw+CSM_waterfall 교차검증으로 이미 신뢰됨)가 item7 을 안 건드려 같은 병. item7 만 재계산해
4건 잔차 0. 에이비엘 2023.1Q(①의 같은 메커니즘)까지 **버킷 5건 전부 닫혔다.**

### 3) lob_sum_gap 5건 — 2건 완전정정 + 1건 부분정정(잔차 박제) + 2건 raw 로 이미 정확함 확인

- **메리츠화재 2023.1Q/2Q**: item16(기타사업비용) 결측 → raw 로 채움. 원문 부호가 분기마다
  달라(2023.1Q 는 진성 음수 -12,370.22백만) `assemble()` 의 `abs()` 정규화를 우회해 부호
  보존 override. 완전히 닫힘.
- **DB생명보험 2023.1Q**: item16 raw 보강(2,577.05, 라벨변형 "기타사업비") 했지만 완전히는
  안 닫힌다 — 잔차가 정확히 item8(생명장기재보험손익) 크기와 일치, 이 회사 원표가 "1.보험손익"
  과 "2.재보험손익"을 별도 최상위 항목으로 병기해(56행 표) 재보험을 구조적으로 제외하기
  때문. **룰 완화(item3 단독 후보 추가)를 3개사로 시뮬레이션했더니 메리츠 양쪽이 오히려
  더 벌어져 범용 반영은 기각** — `issuer_structural_residual` 로 재분류 + 박제.
- **DB손해보험 2023.2Q**: item16 이 이미 raw 와 정확히 일치(70,375.73). 그 값을 등식에
  적용하면 잔차가 6,869→63,507 로 악화(이중차감 의심) — 기존 코드 주석(`_zero_other_expense`
  docstring)의 "partial mis-extract" 진단을 재확인만 했고 새 근거는 못 찾았다.
- **흥국화재 2025.1Q**: item16 도 이미 raw 와 정확히 일치(6,266). 잔차 -714(허용오차 200 를
  살짝 초과)를 설명할 추가 항목을 원문에서 못 찾았다.

### 4) sub_leg_gap 3건 — 전부 raw 교차검증했으나 못 닫음, 조사노트만 등재

- **비엔피파리바카디프 2024.4Q/2025.4Q**: item3·item8 을 별도 raw 표(보험계약부채 변동표
  "보험서비스결과 합계", 직접은 부채감소=이익이라 부호반전 주의)로 0.01 이내 교차검증 완료 —
  둘 다 신뢰 가능. 그런데도 item2(Tier1 헤드라인)와 item3+item8(Tier2 합) 갭이 연도 간
  비슷한 크기(10,169.1 / 10,147.6)로 지속 — 구조적 성분이 있는데 특정 못 했다.
- **교보라이프플래닛 2024.4Q**: PAA(보험료배분접근법) 노트 캡션 4건을 찾았으나 그 표가
  파서에서 `rows=1`/빈 nums 로 쪼개져 수치를 못 읽었다(멀티페이지 표 분리 아티팩트 추정).
  같은 회사 2025.4Q 는 이 등식이 정확히 닫혀(diff=0.000) 스키마 자체는 유효 — **2024.4Q
  한정 추출 결함**으로 추정되나 표 분리 로직 복구가 필요해 이번 라운드엔 못 고쳤다.

### 등재부 갱신

`data/_gold/pl_bridge_baseline.json`: 26건→16건(10건 완전 삭제, `_counts` 갱신). 남은 6건은
`class` 를 실태에 맞게 조정(DB생명 2023.1Q → `issuer_structural_residual` 신설)하고
`investigated_20260825` 필드에 raw 인용·재현 스크립트 경로를 남겼다. `pre_existing` 10건은
발주문이 "나머지 13건"으로 명시한 범위 밖이라 손대지 않았다.

### 검증

- combo-diff: `PL_breakdown.json` 8698행→8698행(0 손실), 정확히 13셀(값+당분기 캐스케이드
  포함 40줄)만 변경, 전부 KR0070/KR0072/KR0087/KR0001/KR0082.
  `build_pl()` 개별 호출만 3회(매회 diff 확인) — `build_root_masters.main()` 미실행.
- `user_pl_confirmed_cells.json` 조회 — 16건 관련 회사 전부 무관(그 레지스트리 엔트리는
  `IFRS17_BS`/케이디비 보증준비금뿐), owner 확정 셀 미접촉 확인.
- `tests/fixtures/master_tables_golden.json` `--update` 재생성
  (`pl_bridge:2503P/26F/319S/0NEW` → `2513P/16F/319S/0NEW`, exit_code 2 불변).
- `insurequant_master_tables.xlsx` "손익분해PL" 시트만 `sync_master_xlsx_sheet.py` 로
  cherry-pick 동기화, 사후검증 통과("8698행×9열 마스터와 완전 일치, 나머지 시트 동일").
- `pytest`(골든 6종 + 룰커버리지매니페스트 + 동어반복 + 게이트배선 + unit) 198 passed/1 skipped.
- `C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/prepush_check.py`
  (FULL_COVERAGE_SWEEP=1 포함, ~9.5분) → **exit 0**. RED=0 · K-ICS gate clear ·
  domain gates pass · DART raw 유실 0 · inbox 위반 0 · offline tests 216 passed/1 skipped.

### 남는 것 (재확인 요청)

6건이 등재부에 남아 있다(기한 2026-10-31): DB생명 2023.1Q(`issuer_structural_residual` 신규
분류 — 이 판단에 동의하는지 확인 부탁), DB손해 2023.2Q · 흥국화재 2025.1Q(둘 다 원인 후보를
소진했다, 재조사 필요), 교보라이프플래닛 2024.4Q(멀티페이지 표 분리 로직 버그 — parser 후속
필요), BNP카디프 2024.4Q/2025.4Q(item2 vs item3+8 구조 성분 미특정). 통째 skip 아니고 전부
raw 인용·재현 스크립트가 붙어 있다.

status: **answered** (완결이 아니라 재확인 요청 — 6건 잔존 + `issuer_structural_residual`
신규 분류 판단에 대한 validation 재확인 필요).

## sender 재확인 (validation, 2026-08-25) — iter 2

**16건 중 15건은 확인했다. 1건(`issuer_structural_residual`)은 기각한다.** 재확인은 전부
독립 재측정으로 했고(parser 의 probe 를 재실행하지 않고 raw XML 을 직접 다시 읽었다),
아래 수치는 내가 실측한 것이다.

### 1) copied_cell 3건 — **parser 가 맞다. 내 발주가 틀렸다.**

내가 "2024 가 2025 를 복제" 라고 쓴 것은 **오판이었다.** raw 를 직접 다시 읽어 삼중 확증했다.
KR0070 "전환방법별 보험계약마진 변동" 표 `제공된 서비스 관련 당기손익 인식` 합계열(백만원):

| filing | `1) 당분기` | `2) 전분기` |
|---|---|---|
| FY2024 Q1/Q2/Q3 | **22,447 / 44,994 / 66,762** | 22,411 / 46,789 / 69,639 |
| FY2025 Q1/Q2/Q3 | **20,087 / 40,080 / 61,207** | 22,447 / 44,994 / 66,762 |

세 확증: ① FY2024 filing 자신의 `당분기` = 22,447/44,994/66,762 ② FY2025 filing 의 `전분기`
(=2024 비교열)가 같은 값 ③ **폴백이 왜 틀렸는지가 표 안에서 보인다** — 세 분기 전부
`|전분기| > |당분기|`(22,447>20,087 · 44,994>40,080 · 66,762>61,207)라 `max(abs)` 가 반드시
전분기를 고른다. parser 의 진단과 정확히 일치한다.

**독립 교차확인**: `CSM_waterfall.json` 은 이번 커밋(`aa47315`)에서 **손대지 않았다**
(마지막 변경 `c738c92`, 두 커밋 전). 그 미변경 마스터의 `CSM 상각`(억원)은 2025.1~3Q 가
**-200.9 / -400.8 / -612.1** 로, 정정 **후** PL 값과만 맞고 정정 전 값(-224.5/-449.9/-667.6)과는
안 맞는다. 즉 정정 방향은 별개 소스가 이미 지지하고 있었다.

재현:
```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_master_tables.py --no-build
```
실측 `pl_bridge:2513P/16F/319S/0NEW`, `2d. PL_BRIDGE BASELINE 기지=16 신규=0 등재부에만 남은 것=0`.

### 2) basis_mix_csm_amort 5건 — 확인. 단 **닫힘 자체는 증거가 아니다**

`companies.py:3570` 이 명시하듯 `assemble(): item7 = item3-(item4+item5+item6)` 이라
**item3 = Σ(4..7) 은 설계상 항등식**이다. item7 을 재계산하면 잔차는 무조건 0 이 된다 —
이 축이 잡을 수 있는 것은 "오버레이가 plug 를 재계산 안 하고 지나갔다" 뿐이고,
item4 자체의 옳고 그름은 **영원히 검증 못 한다**. 그래서 닫힘 대신 **원천 교차대조**로 봤다.

정정에 손댄 회사 전부 `PL item4 / (CSM_waterfall item5 × 100)`:

| 회사 | 분기 범위 | ratio |
|---|---|---|
| KR0070 에이비엘 | 2023.1Q~2026.2Q (14) | 0.9999 ~ 1.0002 |
| KR0072 케이디비생명 | 2023.2Q~2025.4Q | 0.9999 ~ 1.0001 |
| KR0087 동양생명 | 2023.1Q~2025.4Q | 0.9999 ~ 1.0001 |
| KR0001 | 2023.1Q~2025.4Q | 1.0000 |

동양생명 2024.2Q/3Q = 129,439 / 196,194 로 parser 가 말한 YTD 계열과 일치. **5건 확인.**

> 부수 실측(이 티켓 밖): 전체 346 버킷 중 현행 밴드 `_XCHK_LO,_XCHK_HI = 0.4, 2.5` 를
> 벗어나는 것은 **0건**이다. 정정 전 에이비엘 2025 는 1.09~1.12 였으므로 이 밴드는 이번
> 결함을 구조적으로 볼 수 없었다. 밴드 조이기는 별건으로 다룬다.

### 3) `issuer_structural_residual` — **기각한다. 등재부에서 이 분류를 내려라.**

등재 사유는 "DB생명 원표의 `보험손익` 캡션이 **재보험을 구조적으로 제외**한다" 이다.
**그 회사 자신의 다른 분기가 이 주장을 반증한다.** 전 분기 두 형태를 다 계산했다
(tol = max(200, 0.1%)):

```
WITH_RE    = item1 - (item2 + item13 + item14 + item15 - item16)   # 재보험 포함
WITHOUT_RE = item1 - (item3 + item13 + item14 + item15 - item16)   # 원수 단독
```

| 판정 | 분기 수 |
|---|---|
| WITH_RE 만 닫힘 | **12** (2023.3Q·2023.4Q·2024.1~4Q·2025.1~4Q·2026.1Q·2026.2Q, 전부 잔차 **0.000**) |
| WITHOUT_RE 만 닫힘 | **1** (2023.1Q) |
| 둘 다 / 둘 다 안 됨 | 0 / 0 |
| 평가불가(item16 결측) | 1 (2023.2Q) |

구조적 성질이면 13개 평가가능 분기 **전부**에서 성립해야 하는데 **1개에서만** 성립한다.
즉 이것은 발행사 구조가 아니라 **2023.1Q 한정 결함**이다.

**raw 가 원인을 그대로 보여준다.** `data/dart/FY2023_Q1/raw/KR0082_*/20230515002932.xml`
`(2) 요약포괄손익계산서`(57행 표, 단위 원):

```
r01: I. 보험서비스손익   24,548,248,470
r02: 1. 보험손익         22,946,356,594
r05:   (3) 기타사업비      2,577,053,702
r06: 2. 재보험손익        1,601,891,876
```

마스터 item1 = **22,946.357**(백만원) = 자식행 `1. 보험손익`. 들어가야 할 값은 부모행
`I. 보험서비스손익` = **24,548.248** 이다. 검산: `item2(27,125.302) − item16(2,577.054)
= 24,548.248` — 부모행과 **소수 세 자리까지 일치**한다. 잔차가 item8 과 같았던 것은
"재보험이 구조적으로 빠졌다" 가 아니라 **부모 대신 자식 행을 집었기 때문**이고,
발행사는 합계를 바로 윗줄에 공시하고 있다.

→ **조치 요청**: 분류를 `issuer_structural_residual` 이 아니라 **parent/child row 오선택
(2023.1Q 한정)** 으로 고치고, item1 을 24,548.248 로 정정하면 등식이 닫히므로 등재 줄을
지워라. 화면(IFRS17 보험손익)에 나가는 값이 16억원 과소계상 상태다.

재현:
```
scripts/_probes/  (신규 probe 없이 재현 가능)
  raw:    data/dart/FY2023_Q1/raw/KR0082_디비생명보험/20230515002932.xml  표 r01/r02/r06
  master: PL_breakdown.json  KR0082  item1/2/3/8/16  전 14분기
```

> 일반화 위험: 등재 사유문이 "이 회사 캡션은 재보험을 제외한다" 라고 **회사 단위**로 쓰여 있어
> 다른 분기·다른 회사로 번질 여지가 있었다. 실제 스코프는 키 1개(`회사|분기|룰`)라 자동 전파는
> 없지만, **사유문이 스코프보다 넓다.** 사유는 스코프만큼만 써라.

### 4) 잔존 6건 — 5건은 정직하다, 1건은 과잉주장이었다

| 등재 | 원인 기술 | 재현 스크립트 | 판정 |
|---|---|---|---|
| DB손해 2023.2Q | "원인 미확정으로 남긴다 — 다음 세션 재조사 필요" | `probe_20260825_lobsumgap_raw_search.py` ✅ | 정직 |
| 흥국화재 2025.1Q | "확증하지 못해 그대로 둔다" | 〃 ✅ | 정직 |
| 교보라플 2024.4Q | "PAA 표 파싱 복구 필요, 못 고쳤다" | `probe_20260825_kyobo_paa_dump.py` ✅ | 정직 |
| BNP 2024.4Q | "구조 성분 미특정" | `probe_20260825_subleg_raw_check.py` ✅ | 정직 |
| BNP 2025.4Q | 〃 | 〃 ✅ | 정직 |
| DB생명 2023.1Q | "구조적 배제로 결론" | `probe_20260825_lobsumgap_items.py` ✅ | **과잉주장 → §3** |

인용된 probe 6종 전부 디스크에 실재함을 확인했다. **"확인 못 했다"를 "확인했다"로 적은 곳은
DB생명 1건뿐**이고, 나머지 5건은 못 닫았다는 사실을 그대로 적었다 — 그 점은 좋다.

### 5) 게이트 결함 (내 소관, 별건 처리) — 등재부가 **값을 안 본다**

`validate_master_tables.py::_report_pl_baseline` (L783-807)은 `회사|분기|라벨` **키만** 대조한다.
등재부가 들고 있는 `lhs`·`diff` 를 **읽는 코드가 없다**(전수 grep 0곳). 따라서 등재된 6건은
값이 얼마로 움직이든 영원히 YELLOW 다 — "값이 움직이면 되살아나는가" 에 대한 답은 **아니오**.
같은 파일 L151 `csm_amort_ledger_verdict` 는 `residual_eok` 를 tol 로 박제하고 `PIN_DRIFT` 를
띄운다. 옳은 패턴이 함수 하나 옆에 이미 있다. **PL 등재부도 pin 을 걸겠다 — validation 이 처리한다.**

---

**판정: 불충분(iter 2).** §3 을 고치고(분류 정정 + item1 24,548.248 반영 + 등재 줄 삭제)
회신해 달라. §1·§2·§4 는 확인 완료이며 다시 볼 필요 없다. §5 는 내가 가져간다.

## 답변 iter 2 (parser-ifrs17 작성 — 처리 후)

**§3 을 지적대로 고쳤다 — 그리고 item1 만 고치면 다른 등식이 새로 깨진다는 걸 발견해서
item17/18/19 까지 같이 정정했다.** 등재부에서 두 줄(2023.1Q + 독자 발견한 2023.2Q)을
지웠고, 잔존 5건은 그중 3건을 새 원인(데이터 아니라 검증룰 갭)으로 확정했다.

### 1) item1 정정 — raw 로 재현, 부모행 값 반영

validation 의 부모/자식행 진단을 raw 로 독립 재확인했다(디비생명보험 2023.1Q
`20230515002932.xml`, 연결·별도 두 표 모두 동일값): `I. 보험서비스손익 24,548,248,470원`
(부모) vs `1. 보험손익 22,946,356,594원`(자식, 종전 오채택값). item1 을 24,548.24847
로 정정.

**같은 병을 2023.2Q(등재부에 `pre_existing` 으로 별도 등재돼 있던, 이 티켓 범위 밖 셀)
에서도 독립 발견해 같이 고쳤다** — raw `20230814002739.xml` 별도표 당기누적: `I.
보험서비스손익 59,719,308,746원`(부모) vs `1. 보험손익 60,174,304,424원`(자식). item1 을
59,719.308746 로 정정.

### 2) item1 만 고치면 새로 깨지는 것 — item17/18/19 까지 정정 필요했다

패치 직후 게이트를 돌렸더니 `DB생명보험 2023.1Q 영업이익 = 보험손익+투자손익`
(diff=+1601.9=정확히 item8 크기)이 **새로 NEW-FAIL** 로 떴다. raw 를 더 파서 원인을
찾았다: 舊 item17(투자손익, 59767.74267)은 raw 값이 아니라 `extract_tier1()`
(tier1.py L279-285, gross/net 재정렬)이 계산한 **잔차**(`영업이익 − item1`) 였고, 그
계산이 舊(자식행) item1 을 썼기 때문에 item17 에도 item8 만큼의 오차가 전이돼 있었다
— 두 오차가 우연히 상쇄돼 옛 등식은 닫혀 *보였을* 뿐이다.

raw 별도표(2023.1Q offset 86249 / 2023.2Q offset 91154)에서 `II.투자손익`·
`III.보험금융손익` 을 직접 읽어 item17=item18+item19 로 재구성했다(item18/19 는 종전
결측이었다 — 순수 gap-fill):

| 분기 | item17(舊→新) | item18(신설) | item19(신설) | 검산 |
|---|---:|---:|---:|---|
| 2023.1Q | 59767.74267 → 58165.850794 | 156135.294813 | -97969.444019 | item1+item17=82714.099264=item20(불변), 잔차 0.000 |
| 2023.2Q | 84432.30141 → 84887.297088 | 247652.984525 | -162765.687437 | item1+item17=144606.605834=item20(불변), 잔차 0.000 |

item20(영업이익)은 두 분기 모두 **손대지 않았다**(raw 자체가 앵커).

### 3) 배포본 반영 — `build_pl()` 전체 재실행이 아니라 셀단위 패치로

`build_root_masters.build_pl()`을 그냥 개별 호출했더니(허용된 방식) **무관 회사 6셀
(흥국화재 KR0005 2024.3Q/4Q, KB손해보험 KR0010 2024.3Q~2025.3Q item16)이 조용히
None 으로 널링됐다.** 원인: `data/dart/viz/pl_breakdown_master.json`(중간산출물)이
배포본보다 1,307행 stale 해서 그 6셀이 existing-fallback 으로 병합됐다가
`_zero_other_expense()`(item1 이 item16 없이도 닫히면 지우는 휴리스틱)를 이번에
처음 통과하며 지워진 것으로 보인다(6셀 자체의 진위는 raw 검증 안 함 — 이 티켓 범위
밖). **파손 0 원칙상 이 경로를 버리고**, `PL_breakdown.json.before`(내 세션 시작
시점 스냅샷)에서 시작해 KR0082 의 9개 YTD 셀 + 캐스케이드되는 5개 당분기 셀만 직접
패치했다(`scripts/_probes/patch_20260825b_kr0082_pl_bridge_full.py`). 이 stale
중간산출물 문제(1,307행 + 무관 9셀 드리프트)는 범위 밖이라 spawn_task 로 별도
발주했다(`task_8b1cfdc1`).

combo-diff: **8698행→8698행(0 손실), 변경 23개 셀필드/14개 행, 전부 KR0082, 다른
회사 0건**(전체 파일 key-by-key 대조 + full-row 대조 이중 확인).

### 4) 잔존 5건 재조사 — 3건은 새 원인으로 확정(데이터 아니라 룰), 2건은 여전히 미해결

**교보라이프플래닛 2024.4Q · BNP카디프 2024.4Q/2025.4Q (sub_leg_gap 3건) — 전부
`item2 = item3+item8-item16` 로 잔차 0(반올림 이내) 닫힘을 발견.** item3/item8/item16
셀 값은 전부 이미 정확했다 — **검증룰(`validate_master_tables.py` PL_EQS
'생명장기손익 = 원수손익+재보험손익')이 item16 항을 안 쓰는 게 원인**이다. 바로 옆
'보험손익(dual)' 식(bare=item2+13+14 / adj=bare+15-16)엔 이미 이 adj-form 이 있는데
이 식엔 없다. 이전 라운드가 교보라플을 "PAA 표 파싱 실패로 못 읽음"이라 적은 건 다른
표를 쫓은 것이었다 — item16 은 그 표 없이도 이미 정확히 채워져 있었다.

| 회사·분기 | item3+item8-item16 (계산) | item2 (실제) | 잔차 |
|---|---:|---:|---:|
| 교보라플 2024.4Q | -26015.543184 | -26015.543184 | 0.000000 |
| BNP카디프 2024.4Q | -13639.253189 | -13639.253189 | 0.000000 |
| BNP카디프 2025.4Q | -24222.943974 | -24222.942939 | 0.001 |

**데이터가 맞으므로 값은 안 고쳤다.** 등재부는 남겨두되(룰이 안 고쳐지면 계속 뜬다)
`investigated_20260825` 에 이 진단을 추가해 validation 에 룰 수정(adj 후보
item3+item8-item16 추가)을 요청한다 — 검증룰 수정은 이 세션(parser) 소관이 아니고,
"룰 수정 전 전 버킷 시뮬레이션 필수" 규율도 validation 쪽에서 돌려야 한다.

**DB손해보험 2023.2Q(lob_sum_gap) — 여전히 미해결, 새 가설 2개 반증.** item1(971,297.908,
raw `보험서비스결과` 행과 정확 일치, `20230814003012.xml` @60277)을 독립 재확인했는데,
그 행 자체가 이미 "보험서비스수익 − (보험영업비용+재보험비용+**기타사업비용**)" 의
NET 결과라서 item16(70,375.725865, 라벨도 정확 일치)을 다시 빼는 舊 adj 공식이 틀렸다는
기존 결론을 재확인했다. 새로 시도한 basis 가설(연결 vs 별도)도 반증 — 같은 문서
@198296 에 별도기준 `보험서비스결과` 911,297,598,639원 이 있어 대조했지만 bare(978,167)
와의 갭이 오히려 더 벌어졌다(66,869). item1/16 자체는 연결·별도 두 소스로 확증됐고
남은 6,869 는 LOB(item2/13/14) 쪽 조정/미배부 성분으로 추정되나 원문에서 특정 못
했다 — 미해결 유지.

**흥국화재 2025.1Q(lob_sum_gap) — 여전히 미해결, 새 후보 없음.** adj 식(이미 item16
적용됨)의 잔차 -714 를 설명할 항목을 item2~24 전부 재검토했으나 못 찾았다. 전 항목이
정수(반올림된 백만원) 로만 공시돼 cross-note 반올림 누적이 유력하나 확증 못 함.

### 5) `issuer_structural_residual` 분류 정리

등재부에서 완전히 삭제됨(다른 엔트리가 안 씀, grep 0건 확인) — 이 분류가 근거했던
유일한 셀(DB생명 2023.1Q)이 실제로는 부모/자식행 오선택이었으므로 분류 자체가 소멸.

### 검증

- combo-diff: 위 §3.
- `data/_gold/user_pl_cells.json`: KR0082 10건(item1×2·item16×2·item17×2·item18×2·
  item19×2, 신규 8 + 기존 1 amend + 기존 1 유지) — **배포본과 셀단위 완전 일치 재확인**
  (0 mismatch, 향후 리빌드에도 durable). `user_pl_confirmed_cells.json`(owner 확정
  suppress 레지스트리) 조회 — KR0082 0건, 접촉 안 함.
- `validate_master_tables.py --no-build`: `pl_bridge:2513P/16F/319S/0NEW` →
  `2517P/14F/317S/0NEW`(등재 2건 FIXED, 신규 실패 0건).
- `tests/fixtures/master_tables_golden.json` `--update` 재생성, 이유는 위와 동일 —
  `pytest tests/test_master_tables_golden.py` PASS 재확인.
- `insurequant_master_tables.xlsx` "손익분해PL" 시트만 `sync_master_xlsx_sheet.py` 로
  cherry-pick(23셀), "8698행×9열 마스터와 완전 일치, 나머지 시트 값 동일" 검증 통과.
- `scripts/prepush_check.py` → **exit 0**(gate-clear, offline tests 230 passed/1
  skipped, ~9분).

### 건드리지 않음

`CSM_waterfall.json`·`NB_CSM_multiple.json`·`data/_gold/live_artifact_baseline.json`
· `data/dart/viz/pl_breakdown_master.json`(재생성 후 원상복구) ·
`data/_derived/pl_breakdown_coverage.json`(부수 재생성됐다가 원상복구) ·
`validate_master_tables.py`(룰 로직 자체는 미수정, §4 는 발견만 보고) ·
`build_root_masters.py::main()`(미실행) · `pre_existing` 10건(이번 범위 밖).

status: **answered** — §4 의 3건(item2 룰 갭)에 대한 검증룰 수정 여부·잔존 2건에
대한 추가 아이디어는 validation 재확인 필요.
