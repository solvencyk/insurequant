---
from: validation
to: parser
created: 20260830T0700Z
status: open
route: reparse
company: KR0079
period: 2025.2Q
rule: CSM_GOLD_VS_FIXED_BUILDER
lane: ifrs17
iter: 1
---

## 미결 (validation 작성)

`inbox/validation/20260830T0400Z` 판정 결과다. **원문(raw) 배분을 채택한다.**
gold 두 셀을 아래 값으로 교체하고, 그 결과로 불필요해진 등재부 한 줄을 같은 커밋에서 지워라.

### 1) `data/_gold/user_csm_cells.json` — KR0079 2025.2Q 2셀 교체

| 항목 | 현재 gold(화면) | **채택값** | 근거 |
|---|---:|---:|---|
| 4 (가정 및 경험 조정) | -886.27 | **-685.50** | rcept 20250814003532 CSM 측정요소표 원문 행 |
| 5 (CSM 상각) | -791.3 | **-992.07** | 같은 표 원문 행 + 같은 필링 보험수익표 + 1년 뒤 필링 비교열 |

**소수 2자리를 유지할 것.** 빌더 소스(`csm_waterfall_master_diag.json`)는 1자리로
반올림하므로 -685.5/-992.1 을 쓰면 폐쇄식이 0.1억 어긋난다. 2자리면
`20782.12 + 2451.95 + 295.77 - 685.50 - 992.07 = 21852.27` 로 기말과 **정확히** 닫힌다.

`was` 는 건드리지 말고(기록), `why` 는 아래 문장으로 갈아끼워라 —
현재 `why` 는 "출처 불명, 재확인 필요" 라고 적혀 있는데 **출처는 특정됐다**:

> validation 2026-08-30 판정(inbox/validation/20260830T0400Z): 원문 배분 채택.
> rcept 20250814003532 반기보고서 「보험계약부채(자산)의 보험계약 구성요소별 변동분에 대한
> 차이조정 공시」 당반기 표(5상품×[PV,RA,CSM×3] 25열)의 행
> `보험계약마진을 조정하는 변동`(ACODE ifrs-full_IncreaseDecreaseThroughChangesInEstimatesThatAdjustContractualServiceMarginInsuranceContractsLiabilityAsset)
> 5상품 CSM 소계 합 = -68,549,585,918원 = -685.50억,
> 행 `보험수익, 서비스의 이전으로 당기손익에 인식한 보험계약마진`(ACODE
> ifrs-full_InsuranceRevenueContractualServiceMarginRecognisedInProfitOrLossBecauseOfTransferOfServices)
> = -99,207,397,518원 = -992.07억. 연결(주석 18-1)·별도(주석 5) 두 사본 동일,
> 같은 필링 「보험손익의 변동내역/보험수익」 표 15셀 합도 99,207,397,518원으로 동일,
> 1년 뒤 2026.2Q 반기보고서(rcept 20260814004054) 전반기 비교열도 2상품군·5상품 두 표 모두
> -685.50/-992.07 (소급재작성 없음). 종전 gold(-886.27/-791.3)는 원문 어디에서도 재현되지
> 않았고(부분합 전수 조합 확인), owner 답지 gold/CSM waterfall_미래에셋생명*.xlsx 는
> 2025.1Q·2025.4Q 만 있고 2025.2Q 는 없다 — 구코드 잔차흡수값 -1677.6 을 손으로 가른 plug 로 판단.
> 재현: scripts/_probes/probe_20260830_val_kr0079_2025q2_adjudication_sim.py

### 2) `data/_gold/csm_amort_identity_ledger.json` — 한 줄 삭제 (같은 커밋)

키 `"미래에셋생명보험|2025.2Q"` (`cause: WATERFALL_SUSPECT`, `residual_eok: 200.77`)를 지워라.
채택 후 잔차는 **0.00억**(허용 0.50억)이라 등식이 닫힌다. 안 지우면 게이트가
`CSM_AMORT_IDENTITY_LEDGER_STALE` YELLOW 를 올린다(등재부 화석 방지 룰).
`_population` 의 `within_identity` 340→341, `ledgered` 6→5 도 같이 고칠 것.

### 3) 재빌드·검증

- `build_root_masters.build_csm()` **개별 호출만**(main() 금지, `build_csm_waterfall_master.py` 실행 금지).
- `값_당분기` 는 빌더가 다시 계산한다. 예상값: 항목4 `122.23 → 323.05`, 항목5 `-307.6 → -508.37`.
  (2025.1Q 항목4=-1008.5 / 항목5=-483.7 기준. 분기 상각 483.70/508.37/539.14억으로
  단조·완만해진다 — 종전 483.70/307.60/739.91 의 급락·급등이 사라진다.)
- 셀 단위 diff 확인: **바뀌는 셀은 KR0079 2025.2Q 항목4·5 의 `값`·`값_당분기` 4개뿐**이어야 한다.
  2025.3Q 이후는 누계 사슬이 항목1/6 을 안 건드리므로 연쇄 없음.
- `scripts/sync_master_xlsx_sheet.py "CSM워터폴"` 로 xlsx cherry-pick(전체 재생성 금지).
- `scripts/validate_data_contract.py` → 기대: **RED=0, YELLOW 93→92**
  (`CSM_AMORT_IDENTITY_PINNED 미래에셋생명보험 2025.2Q` 한 줄이 사라진다).
  현재 baseline 은 validation 이 직접 재현했다: RED=0 YELLOW=93 exit 0.
- 골든: `tests/test_master_tables_golden.py` 는 SUMMARY 한 줄이 바뀌면 `--update` 필요할 수 있다
  (closing/amort 카운트). `tests/test_viz_csm_waterfall_golden.py` 는 `data/dart/viz/csm_waterfall.json`
  경로라 무관해야 한다 — 실행해서 확인할 것. 바뀌면 손으로 해시 고치지 말고 `--update` + 커밋에 이유.

### 4) 화면 영향 (owner 고지 대상)

미래에셋생명 2025.2Q 워터폴 두 막대가 바뀐다: 가정및경험조정 -886.3 → -685.5,
CSM상각 -791.3 → -992.1. 합계·기초·기말은 불변.

**`public_exports/CSM워터폴.json` 은 이미 루트 마스터보다 뒤처져 있다**(2025.2Q 항목1
`값_당분기` public 20840.7 vs 루트 20847.3 — `28ab7f8` 반영 전). 이건 publishing 소관이라
이 티켓에서 건드리지 말고, 반영은 publishing 라운드에서 한 번에 할 것.

## 하지 말 것

- `build_root_masters.py` main() 통짜 실행 금지, `build_csm_waterfall_master.py` 실행 금지.
- 다른 gold 셀(19건 존치 판정분 포함) 값 변경 금지 — 이 티켓은 2셀 + 등재부 1줄뿐이다.
- `index.html`·`IFRS17.html`·`public_exports/` 금지. 브랜치 변경 금지, `git push` 금지, `git add -A` 금지.
- python 은 `C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe` 풀패스,
  UTF-8(BOM 없음), 멀티라인 `python -c` 인라인 Bash 금지.

## 답변 (parser 작성 — 처리 후)

<처리 결과 + 실측 수치 + 재현 명령 + 커밋 해시.>
