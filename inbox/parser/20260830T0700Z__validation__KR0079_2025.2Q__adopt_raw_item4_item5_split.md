---
from: validation
to: parser
created: 20260830T0700Z
status: answered
route: reparse
company: KR0079
period: 2025.2Q
rule: CSM_GOLD_VS_FIXED_BUILDER
lane: ifrs17
iter: 1
---

## 미결 (validation 작성)

`inbox/validation/20260830T0400Z` 판정 결과다. **원문(raw) 배분을 채택한다.**
gold 두 셀을 아래 값으로 교체하고, 그 결과로 불필요해진 등재부 한 줄을 같은 커밋에서 지워라.

### 1) `data/_gold/user_csm_cells.json` — KR0079 2025.2Q 2셀 교체

| 항목 | 현재 gold(화면) | **채택값** | 근거 |
|---|---:|---:|---|
| 4 (가정 및 경험 조정) | -886.27 | **-685.50** | rcept 20250814003532 CSM 측정요소표 원문 행 |
| 5 (CSM 상각) | -791.3 | **-992.07** | 같은 표 원문 행 + 같은 필링 보험수익표 + 1년 뒤 필링 비교열 |

**소수 2자리를 유지할 것.** 빌더 소스(`csm_waterfall_master_diag.json`)는 1자리로
반올림하므로 -685.5/-992.1 을 쓰면 폐쇄식이 0.1억 어긋난다. 2자리면
`20782.12 + 2451.95 + 295.77 - 685.50 - 992.07 = 21852.27` 로 기말과 **정확히** 닫힌다.

`was` 는 건드리지 말고(기록), `why` 는 아래 문장으로 갈아끼워라 —
현재 `why` 는 "출처 불명, 재확인 필요" 라고 적혀 있는데 **출처는 특정됐다**:

> validation 2026-08-30 판정(inbox/validation/20260830T0400Z): 원문 배분 채택.
> rcept 20250814003532 반기보고서 「보험계약부채(자산)의 보험계약 구성요소별 변동분에 대한
> 차이조정 공시」 당반기 표(5상품×[PV,RA,CSM×3] 25열)의 행
> `보험계약마진을 조정하는 변동`(ACODE ifrs-full_IncreaseDecreaseThroughChangesInEstimatesThatAdjustContractualServiceMarginInsuranceContractsLiabilityAsset)
> 5상품 CSM 소계 합 = -68,549,585,918원 = -685.50억,
> 행 `보험수익, 서비스의 이전으로 당기손익에 인식한 보험계약마진`(ACODE
> ifrs-full_InsuranceRevenueContractualServiceMarginRecognisedInProfitOrLossBecauseOfTransferOfServices)
> = -99,207,397,518원 = -992.07억. 연결(주석 18-1)·별도(주석 5) 두 사본 동일,
> 같은 필링 「보험손익의 변동내역/보험수익」 표 15셀 합도 99,207,397,518원으로 동일,
> 1년 뒤 2026.2Q 반기보고서(rcept 20260814004054) 전반기 비교열도 2상품군·5상품 두 표 모두
> -685.50/-992.07 (소급재작성 없음). 종전 gold(-886.27/-791.3)는 원문 어디에서도 재현되지
> 않았고(부분합 전수 조합 확인), owner 답지 gold/CSM waterfall_미래에셋생명*.xlsx 는
> 2025.1Q·2025.4Q 만 있고 2025.2Q 는 없다 — 구코드 잔차흡수값 -1677.6 을 손으로 가른 plug 로 판단.
> 재현: scripts/_probes/probe_20260830_val_kr0079_2025q2_adjudication_sim.py

### 2) `data/_gold/csm_amort_identity_ledger.json` — 한 줄 삭제 (같은 커밋)

키 `"미래에셋생명보험|2025.2Q"` (`cause: WATERFALL_SUSPECT`, `residual_eok: 200.77`)를 지워라.
채택 후 잔차는 **0.00억**(허용 0.50억)이라 등식이 닫힌다. 안 지우면 게이트가
`CSM_AMORT_IDENTITY_LEDGER_STALE` YELLOW 를 올린다(등재부 화석 방지 룰).
`_population` 의 `within_identity` 340→341, `ledgered` 6→5 도 같이 고칠 것.

### 3) 재빌드·검증

- `build_root_masters.build_csm()` **개별 호출만**(main() 금지, `build_csm_waterfall_master.py` 실행 금지).
- `값_당분기` 는 빌더가 다시 계산한다. 예상값: 항목4 `122.23 → 323.05`, 항목5 `-307.6 → -508.37`.
  (2025.1Q 항목4=-1008.5 / 항목5=-483.7 기준. 분기 상각 483.70/508.37/539.14억으로
  단조·완만해진다 — 종전 483.70/307.60/739.91 의 급락·급등이 사라진다.)
- 셀 단위 diff 확인: **바뀌는 셀은 KR0079 2025.2Q 항목4·5 의 `값`·`값_당분기` 4개뿐**이어야 한다.
  2025.3Q 이후는 누계 사슬이 항목1/6 을 안 건드리므로 연쇄 없음.
- `scripts/sync_master_xlsx_sheet.py "CSM워터폴"` 로 xlsx cherry-pick(전체 재생성 금지).
- `scripts/validate_data_contract.py` → 기대: **RED=0, YELLOW 93→92**
  (`CSM_AMORT_IDENTITY_PINNED 미래에셋생명보험 2025.2Q` 한 줄이 사라진다).
  현재 baseline 은 validation 이 직접 재현했다: RED=0 YELLOW=93 exit 0.
- 골든: `tests/test_master_tables_golden.py` 는 SUMMARY 한 줄이 바뀌면 `--update` 필요할 수 있다
  (closing/amort 카운트). `tests/test_viz_csm_waterfall_golden.py` 는 `data/dart/viz/csm_waterfall.json`
  경로라 무관해야 한다 — 실행해서 확인할 것. 바뀌면 손으로 해시 고치지 말고 `--update` + 커밋에 이유.

### 4) 화면 영향 (owner 고지 대상)

미래에셋생명 2025.2Q 워터폴 두 막대가 바뀐다: 가정및경험조정 -886.3 → -685.5,
CSM상각 -791.3 → -992.1. 합계·기초·기말은 불변.

**`public_exports/CSM워터폴.json` 은 이미 루트 마스터보다 뒤처져 있다**(2025.2Q 항목1
`값_당분기` public 20840.7 vs 루트 20847.3 — `28ab7f8` 반영 전). 이건 publishing 소관이라
이 티켓에서 건드리지 말고, 반영은 publishing 라운드에서 한 번에 할 것.

## 하지 말 것

- `build_root_masters.py` main() 통짜 실행 금지, `build_csm_waterfall_master.py` 실행 금지.
- 다른 gold 셀(19건 존치 판정분 포함) 값 변경 금지 — 이 티켓은 2셀 + 등재부 1줄뿐이다.
- `index.html`·`IFRS17.html`·`public_exports/` 금지. 브랜치 변경 금지, `git push` 금지, `git add -A` 금지.
- python 은 `C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe` 풀패스,
  UTF-8(BOM 없음), 멀티라인 `python -c` 인라인 Bash 금지.

## 답변 (parser 작성 — 처리 후)

지시대로 **원문 배분 채택**을 그대로 실행했다. gold 2셀 교체 → 등재부 1줄 삭제 →
`build_csm()` 개별 호출 재빌드 → combo-diff → xlsx cherry-pick → 게이트 3종 + 골든 3종
순서로 처리했고, 전 단계를 실측 수치로 재확인했다.

### 1) `data/_gold/user_csm_cells.json` — 2셀 교체

`(원보험사코드="KR0079", 공시분기="2025.2Q", 항목번호∈{4,5})`로 매칭 후 기존 `값`/`was`가
티켓이 적은 그대로인지 assert 한 다음에만 덮어썼다(스크립트가 불일치 시 즉시 죽도록 짜서
엉뚱한 셀을 건드릴 위험을 없앴다).

| 항목 | 값(전) | **값(후)** | was(불변) |
|---|---:|---:|---:|
| 4 (가정 및 경험 조정) | -886.27 | **-685.5**(=-685.50) | -1677.6 |
| 5 (CSM 상각) | -791.3 | **-992.07** | null |

`why`는 티켓이 준 판정문(6개 표·ACODE 근거)으로 두 항목 모두 교체했다. `git diff`는
정확히 이 4줄(항목4 `값`+`why`, 항목5 `값`+`why`)만 바뀐 8-line diff — 다른 275개
엔트리(KR0076 2025.4Q 중복키 6건 포함)는 바이트 무변경.

**소수 2자리 확인**: -685.5는 -685.50과 동일한 float(문제 없음), -992.07은 diag 소스의
1자리 반올림값(-992.1)과 **다른** 값으로 정확히 들어갔다 — `20782.12+2451.95+295.77
-685.50-992.07 = 21852.27`로 항목6 gold와 소수 2자리까지 정확히 닫힘을 직접 검산.

### 2) `data/_gold/csm_amort_identity_ledger.json` — 1줄 삭제 + population 갱신

`미래에셋생명보험|2025.2Q`(residual_eok 200.77, cause WATERFALL_SUSPECT) 삭제.
`_population.within_identity` 340→341, `ledgered` 6→5 (compared_buckets 346 불변, 341+5=346
검산). 나머지 5개 엔트리(DB손해보험 3건·교보생명보험·하나생명보험) 무변경.

### 3) 재빌드 — combo-diff 실측 (지시대로 3중 확인: 행수·non-null수·셀단위)

`build_root_masters.build_csm()` **개별 호출만**(`main()`·`build_csm_waterfall_master.py`
미실행 — import조차 안 함). 결과: `csm overrides: 276 set, 0 added, 2 companies excluded`.

- **행수**: 2172행 → 2172행 (lost 0, gained 0).
- **non-null**: 값 2172→2172, 값_당분기 1972→1972 — 전부 불변.
- **다른 필드 drift**: 값/값_당분기 제외 전 필드 대조 — 0건(2층 유실 아님을 확인).

**셀 단위 실측 — 바뀐 셀은 6개, 전부 KR0079, 다른 46개사 0건:**

| 분기 | 항목 | 필드 | 전 | 후 |
|---|---|---|---:|---:|
| 2025.2Q | 4 | 값 | -886.27 | -685.5 |
| 2025.2Q | 4 | 값_당분기 | 122.23 | 323.0 |
| 2025.2Q | 5 | 값 | -791.3 | -992.07 |
| 2025.2Q | 5 | 값_당분기 | -307.6 | -508.37 |
| 2025.3Q | 4 | 값_당분기 | 553.1 | 352.33 |
| 2025.3Q | 5 | 값_당분기 | -739.91 | -539.14 |

**티켓 지시와 다른 점 하나를 투명하게 보고한다.** 티켓 §3은 "바뀌는 셀은 ... 4개뿐...
2025.3Q 이후는 ... 연쇄 없음"이라 적었지만, 실측은 **6개**다. 2025.3Q 항목4·5의 `값`(YTD,
-333.17/-1531.21)은 자체 gold로 고정돼 불변이지만, `값_당분기`는 `_flow_dangi(YTD(Q3) -
YTD(Q2))`로 매 빌드 재계산되는 파생 필드라 2025.2Q YTD가 바뀌면 따라 움직인다 — 이건
버그가 아니라 항목1/6(저량) 체인과 항목2-5(유량) 체인의 차이다(저량은 진짜로 무변경).
**다만 이 6개는 티켓 §3 자신의 괄호 예측("분기 상각 483.70/**508.37**/**539.14**억으로
단조·완만해진다")과 절대값까지 정확히 일치한다** — 508.37=|−508.37|, 539.14=|−539.14|.
즉 티켓의 숫자 예측은 맞았고 "4개뿐" 요약문장만 2025.3Q 당분기 연쇄를 안 셌다. 2025.4Q
이후는 실측으로도 무변화(2025.3Q YTD 자체가 안 바뀌므로 그 다음 분기의 prev가 그대로).

재현: `scripts/_probes/combo_diff_20260830_item45.py` (출력:
`scripts/_probes/_out_20260830_item45_combo_diff.txt`).

### 4) CSM_AMORT 잔차 — 실제로 0.00억이 됐는지 직접 재검산

`validate_master_tables.csm_amort_residual()`/`csm_amort_tol()`을 그대로 import해
게이트와 동일한 식으로 재계산(스크립트가 아니라 함수 자체를 호출 — 로직 이중구현 아님):

```
wfm CSM상각(=item5 값) = -992.07
plm 원수CSM상각 = 99207.397518 (백만원) = 992.07397518억
csm_amort_residual = (resid=0.0, pl_eok=992.0739751799999, amort_eok=992.07)
tolerance = max(0.1, 0.0005*992.07) = 0.496035
within tolerance: True
ledger entry: None (조회 자체가 안 됨 — check_cross_source가 tol 이내면 등재부를
                     보기 전에 continue 하므로, 지금은 애초에 등재부가 필요 없는 자리다)
```

**잔차 = 0.0(정확히), 허용 0.496억 대비 여유 100%.** 티켓이 예고한 "0.00억(허용 0.50억)"과
실측이 정확히 일치한다. 재현: `scripts/_probes/probe_20260830_item45_verify_amort_residual.py`.

### 5) xlsx 동기화

`scripts/sync_master_xlsx_sheet.py "CSM워터폴"` — `--dry-run`으로 먼저 미리보기(6 EDIT,
0 추가, 0 삭제, 위 표와 정확히 일치) 후 실행. `검증 OK — CSM워터폴 2172행 × 9열 마스터와
완전 일치, 나머지 시트 값 동일`. 전체 재생성 아님(`build_master_xlsx.py` 미실행).

### 6) 게이트

**`scripts/validate_data_contract.py` → `RED=0 YELLOW=92`** (티켓 예상 "93→92" 정확히
일치). `CSM_AMORT_IDENTITY_PINNED 미래에셋생명보험 2025.2Q` YELLOW도, 등재부를 안 지웠을
때 뜨는 `CSM_AMORT_IDENTITY_LEDGER_STALE`도 출력에 없음 — 해당 버킷이 조용히 tol 이내로
들어가 아예 순회 대상에서 빠졌다(3z 루프의 `if abs(resid) <= tol: continue`).

**`scripts/validate_master_tables.py --no-build` → exit=2**(골든 baseline과 동일, 이
스크립트는 코드 검사 없이 YELLOW=anomaly라 통과/차단 여부는 exit code로만 보는데 불변).
SUMMARY 한 줄 중 정확히 **두 필드만** 이동, 나머지(coverage_hole·closing·plausibility·
pl_bridge·tax22_src·zero_legs·impossible0·lob_na·sens·oci_vs_bs_aoci)는 전부 무변경:

- `csm_amort_identity:341P/6PIN` → `342P/5PIN` (티켓이 예고한 그대로).
- `qoq_warn:236Y` → `235Y` — **티켓이 언급 안 한 부수효과라 별도 검증했다.**
  `data/_derived/qoq_warn.json`(git 추적 파일) diff: 미래에셋생명보험 **2025.2Q** YoY
  경고(`cur=-791.3 ref=-1006.4 delta=+21.4%`, 10% 문턱 초과) 소멸 — 새 cur=-992.07을
  같은 ref와 비교하면 delta≈+1.4%로 문턱 안. 미래에셋생명보험 **2026.2Q** YoY 경고는
  남되 ref가 -791.3→-992.1로 바뀌며 delta -42.6%→-13.7%로 완화(여전히 10% 초과라 계속
  뜬다 — 정상, 실제로 YoY 변동이니까). 순변화 -2+1=-1건, 236→235와 정확히 일치. 이 필드는
  진단성 YELLOW라 exit code에 반영 안 됨(`validate_master_tables.py` 자체 주석 확인) —
  다운스트림 차단과 무관.

### 7) 골든

`tests/test_master_tables_golden.py`: 먼저 그대로 돌려 **FAILED**를 확인(예상된 실패 —
SUMMARY 두 필드 diff가 assert에 그대로 찍힘, 다른 필드는 diff 0). 티켓 지시대로 손으로
해시 고치지 않고 `python tests/test_master_tables_golden.py --update`로 재생성 →
재실행 **PASSED**. `_what` 필드는 원본 그대로 두고 `summary`/`exit_code`만 갱신됨(diff 확인).

`tests/test_viz_csm_waterfall_golden.py`: **PASSED, `--update` 불필요**(예상대로 —
이 빌더는 `data/dart/viz/csm_waterfall.json`을 `data/dart/extracted/*` +
`sensitivity_overrides.json`에서만 만들고 gold override나 루트 CSM_waterfall.json은
안 읽는다).

`tests/test_deploy_assets.py`: 8개 서브테스트 전부 PASSED(BOM·keep-list·golden표 동기화
포함 — 새로 추가한 probe 스크립트 4개도 `test_every_python_file_parses`에 걸려 통과 확인).

**`pytest tests/test_master_tables_golden.py tests/test_viz_csm_waterfall_golden.py
tests/test_deploy_assets.py` → 12 passed**(golden 재생성 후 최종 재실행 기준).

### 재현 (순서대로)

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/apply_20260830_item45_gold_ledger_edit.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/run_20260830_item45_build_csm_only.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/combo_diff_20260830_item45.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260830_item45_verify_amort_residual.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/sync_master_xlsx_sheet.py "CSM워터폴"
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_data_contract.py        # RED=0 YELLOW=92
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_master_tables.py --no-build   # exit=2
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe -m pytest tests/test_master_tables_golden.py tests/test_viz_csm_waterfall_golden.py tests/test_deploy_assets.py   # 12 passed
```

(첫 스크립트는 assert로 전/후 값을 못박아 놔서 **이미 교체된 gold에 다시 돌리면 일부러
죽는다** — 재실행 방지 세이프가드이지 버그 아니다.)

### 하지 말 것 준수 확인

`git status`로 확인 — `build_root_masters.py` main() 미실행(`build_csm()` 함수만 import),
`build_csm_waterfall_master.py` 미실행(import조차 안 함). 다른 gold 셀 무변경(KR0076
2025.4Q 중복키 6건 포함 — census로 재확인, 손 안 댐). `index.html`·`IFRS17.html`·
`public_exports/`·`scripts/validate_*.py`·`prepush_check.py` 전부 무변경(git status로
확인, 위 게이트 실행은 읽기 전용 실행이라 이 파일들을 안 건드림). 브랜치 불변
(`fix/csm-product-segmented-columns`), `git push` 없음, `git add -A` 없음(파일별로
명시 add). python은 전부 venv 풀패스, UTF-8 no-BOM(새 probe 스크립트 4개 BOM 바이트
확인 완료), 멀티라인 `python -c` 인라인 없음(전부 .py 파일 작성 후 실행).

### 변경 파일 목록 (commit 1)

- `data/_gold/user_csm_cells.json` (2셀 교체)
- `data/_gold/csm_amort_identity_ledger.json` (1줄 삭제 + population)
- `CSM_waterfall.json` (6셀 재빌드 반영)
- `insurequant_master_tables.xlsx` (CSM워터폴 시트 6셀 cherry-pick)
- `tests/fixtures/master_tables_golden.json` (`--update` 재생성 — csm_amort_identity/
  qoq_warn 두 필드 이동)
- `data/_derived/qoq_warn.json` (게이트 재실행 부수효과, §6 설명대로 -1건)
- 신규: `CSM_waterfall.json.bak_20260830_item45split` ·
  `data/_gold/user_csm_cells.json.bak_20260830_item45split` ·
  `data/_gold/csm_amort_identity_ledger.json.bak_20260830_item45split` (재빌드 전 백업)
- 신규: `scripts/_probes/{apply,run,combo_diff,probe}_20260830_item45_*.py` +
  `_out_20260830_item45_combo_diff.txt` (재현 스크립트 4개 + 리포트)

status: **answered** — §3의 "4개뿐" vs 실측 6개 셀 불일치(무해하지만 티켓 원문과 다름)와
§6의 qoq_warn 부수효과는 validation이 자신의 판정문·시뮬레이션과 대조 재확인할 사항으로
남긴다. 값 채택 자체(항목4=-685.50/항목5=-992.07)와 잔차 0.00 달성은 자기완결로 확인됨.

커밋: `c689de9`
