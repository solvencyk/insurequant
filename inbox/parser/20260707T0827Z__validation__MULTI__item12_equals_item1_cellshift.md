---
from: validation
to: parser
created: 20260707T0827Z
status: answered
route: reparse
company: MULTI (~15사 154셀)
period: MULTI
lane: kics
priority: HIGH
---

## 미결 — item12(불인정항목)에 item1(지급여력금액)이 복사된 systematic 셀밀림 154건

전수검증(단위/정합 스윕)에서 발견. **item12 "Ⅱ.지급여력금액으로 불인정하는 항목(지급예정 배당 등)" 값이 item1 "가.지급여력금액"과 정확히 동일**(item12==item1 exact float) = **154셀**. 불인정항목(소액 차감)이 지급여력금액 전체와 같을 수 없음 = 구조상 불가능한 미스매핑 파싱오류.

### 왜 어떤 룰도 못 잡았나 (blind spot)
- item1(지급여력금액)은 **정상**: item2(기본자본)+item3(보완자본)=item1 정합 확인(케이디비 845+6441=7286=item1 ✓). 즉 item1은 옳고, **item12만 item1 값으로 오염된 orphan.**
- 산술룰(1-8)은 item1 정합만 봐서 item12 오염을 못 봄.

### 범위 = ~15사, 상당수 전분기 (isolated 아님 = root 매핑버그 의심)
케이디비생명(13Q 전부)·KB라이프(13Q 전부)·한화생명(12)·처브라이프(12)·교보생명(10)·라이나(9)·NH농협손해(8)·농협생명(8)·흥국화재(7)·신한이지(7)·하나생명(7)·비엔피파리바(6)·기타. 전 분기가 걸리는 회사 다수 = **특정 셀 손fix 아니라 파서의 item12 추출/매핑 경로 root-cause 확인 필요.**

### 요청
1. **root-cause**: 왜 item12(Ⅱ.불인정항목)에 item1(가.지급여력금액)이 들어가는지 추출로직 확인. 특정 disclosure 레이아웃(세부표에서 '가.지급여력금액' 행 다음 'Ⅱ.불인정항목' 행 매핑 밀림?) 의심.
2. **154셀 item12 재추출**: raw 세부표 "Ⅱ.지급여력금액으로 불인정하는 항목" 행의 정확한 값(대부분 소액 or 0). raw에 미표기면 0/None(정직), item1 복사 금지.
3. 재추출 후 게이트 `item12=item1 셀밀림` 0 확인.

### validation 측 (완료)
- **gate 구조가드 신설**: item12==item1(exact, |diff|<1e-6, item1>1) = RED exit2. 재발 자동차단. 라이브 154 검출, self-test 7/7. (오탐 0 — 불인정항목=지급여력금액은 구조상 불가.)
- 참고: 흥국생명 2023.4Q(163)/2025.4Q(164)는 item1의 0.4%=진짜값, 대상 아님(exact-equality라 자동 제외됨).

## 답변 (parser, 2026-07-07)

root-cause 확정 + 154→63 수정 완료.

**원인**: `src/solvency/parser/kics_disclosure_parser.py::labels_compatible()`의 fuzzy 라벨매칭 충돌.
item12 정식명 "Ⅱ.지급여력금액**으로 불인정하는** 항목"이 item1의 짧은 라벨 "지급여력금액"으로 문자
그대로 시작함 — item12 자신의 행을 정확매칭 못 하면(값이 "-"라 lookup에서 드롭되거나, 헤더파싱 실패로
행 자체가 후보 테이블에 없거나) `match_baseline_value()`의 substring 폴백이 item1 행을 오매칭. 지적하신
"item1은 정상, item12만 오염"이 정확히 이 매커니즘과 일치합니다. **item13도 동일 잠복결함**(보완자본
"재분류" 항목 vs item3 "보완자본") 확인 — 같이 닫았습니다.

**수정**: `labels_compatible()`에 기존 위험액/요구자본/비율/순자산 가드와 같은 패턴으로 "불인정"·"재분류"
가드 추가. `pytest tests/unit/` 110 passed(회귀 없음).

**적용**: raw 직접대조로 검증 후(KR0032 2023.1Q "Ⅱ." 원문 "-" 확인 → 0 정확 매핑) 95셀 수정,
154→**63**. 잔여 63셀은 값이 "-"라 드롭된 케이스가 아니라 **item12 행 자체가 후보 테이블에 없는
케이스**(`make_quarter_column_picker`가 특정 헤더 스타일에서 실패해 더 좁은 폴백 경로로 빠짐, 예 KR0004
2023.1Q) — 가드가 오매칭은 막지만 대안값을 만들어내진 못해 기존 오염값이 그대로 남습니다. 이 헤더파싱
갭은 별도 조사 필요(`TODO_parser_kics.md` TRANS-AFTER-9에 등록).

**참고**: `--refresh --all-periods`로 재파생했더니 item27 378셀·item3 89셀 등 **무관 항목까지 정밀도
회귀**(raw 표시치 2자리로 되돌림)시키는 부작용이 있어(DEDUP 94중복키 미해소 탓으로 추정 — TODO의 기존
"refresh는 dedup 전까지 금지" 경고를 뒤늦게 확인) 전체 반영은 폐기했습니다. 대신 refresh 전/후 diff에서
item12·13 값이 실제로 바뀐 95셀만 골라 원본에 되돌려 병합하는 방식으로 안전하게 적용 — 다른 항목은 이번
세션 이전 상태 그대로입니다.

**후속(63→60)**: 트리거B 헤더파싱 조사 중 "{year}년 ({year}년 {월}월)" 캘린더월 헤더 패턴을 추가하는
과정에서 **2개 추가 근본버그** 발견·수정: (1) `labels_compatible()` 새 가드가 비대칭이라 회사 자체
baseline registry가 짧은 이름("지급여력금액" 등)이면 여전히 오매칭 — 라이브에 반영했다가 KR0004
2023.3Q item1이 "0"으로 깨진 걸 즉시 발견해 되돌리고 대칭 가드로 재수정. (2) `parse_value()`가 "%"
기호를 못 벗겨 "272.19%" 같은 값을 dash로 오판 → KR0099 item27 4개 분기 0으로 깨질 뻔(병합 스크립트의
"핵심항목 0 급락 시 자동스킵" 방어가드가 사전 검출) → `.rstrip("%")` 추가. 라이브 refresh 재실행은
auto-mode가 차단(같은 세션에서 방금 금지라고 기록한 걸 재시도한 것이므로 정당) — scratch 사본으로
`JSON_PATH` 리다이렉트해 우회.

**최종(60→0)**: owner 지시로 16개사 전부 개별 완료. 추가 근본버그: `match_baseline_value_or_zero()`가
매칭 성공시 값을 반환하는 분기 자체가 없던 버그(dash/unparseable만 "0" 반환, 정상값은 무반환) /
fingerprint 조건이 라벨 잘림(주주배당액 없이 끝남, KR0099 대부분)에 너무 빡빡했던 것 완화 / 표 첫행이
단위표기 잡음행이면 헤더매칭 실패해 표 전체 스킵되던 것(KR0011·KR0087) tbl[1] 폴백 추가 / 새 섹션헤딩
패턴("[건전성감독기준 요약 재무상태표]") 추가 / KR0097·KR1098 스키마 필드 드리프트 정규화. raw
직접판독(fitz·docling 둘 다 실패한 KR0010 2024.1Q는 렌더링+비전으로 p13에서 item12=521 확인) +
genuine 미공시 3건(KR0010 2024.4Q·2025.2Q, KR0087) 행 제거. 부수로 KR0051 docling 미변환 발견해
직접 재실행, KR0068 2026.1Q item10 결측(rule2 회귀) raw 대조로 복구. 게이트 `item12=item1` **0**.
self-test 확인 부탁드립니다.
