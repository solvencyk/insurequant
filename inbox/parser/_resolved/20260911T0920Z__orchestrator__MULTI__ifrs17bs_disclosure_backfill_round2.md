---
from: orchestrator
to: parser
created: 20260911T0920Z
status: resolved
route: reparse
company: MULTI
period: 2023.1Q~2026.2Q
lane: ifrs17
iter: 2
---

## 미결 (sender 작성)

### owner 지시 (2026-09-11 18:20) — "AIG·악사 등등 다음 라운드도 지금 실시해"

1 라운드(티켓 `_resolved/20260911T0109Z`)로 +925 칸 들어갔으나 **코어 1·2·3 이 모두 갖춰지지 않은
(회사,분기) 가 아직 117 개**다(sender 실측, 20 개사 대상 그리드). 117 개 전부 경영공시 PDF 가 디스크에 있다.
같은 엔진(`scripts/bs_disclosure/`)을 고쳐서 이 117 개를 닫는다.

### 실패 원인 2 가지 — 사이드카 `data/_derived/bs_from_disclosure.json` skip 240 건 분류 실측

| 원인 | 건수 | 대상 회사 | 무엇이 잘못됐나 |
|---|---:|---|---|
| **(A) 표 고정 조건 과엄격** | **106** | AIG(KR0029)·악사(KR0049)·하나손해(KR0050)·카디프(KR0075)·IBK연금(KR1011)·하나생명(KR0097)·처브(KR0100)·라이나(KR0074) | "적격 별도재무상태표 표를 찾지 못함 (별도표제/비지배지분부재/운용자산부재 3조건 불충족)". 1 라운드 수용기준 1 번의 **`별도` 표제 조건**이 원인이다 — 자회사가 없어 연결재무제표를 안 만드는 회사는 표 제목이 그냥 "재무상태표"라 `별도` 가 없다. sender 가 조건을 잘못 잡았다 |
| **(B) QoQ ±30% hold 의 앵커 오류** | 9 (항목3) + 5 (기타) | 교보라이프플래닛(KR1010) 2023.1Q~2024.2Q 5 분기, 예별(KR0004) 2025.3Q, 신한이지(KR0051) 2025.1Q, 아이엠(KR0076) 2024.2Q, 카카오페이(KR1098) 2025.2Q | 자본총계를 **직전 4Q 하나**하고만 비교한다. KR1010 은 2023 년 유상증자로 61,432 → 103,768 백만원이 된 것이 맞고(마스터 2023.4Q 가 그 수준이라 4Q 대조 42/42 EXACT 였다), KR0004 2025.3Q 는 MG→예별 자본확충으로 △333,646 → +29,899 가 맞다. 진짜 값을 게이트가 막고 있다 |
| (C) Q4 앵커 없음 | 17 + 12 | 항목 31 등 마스터에 4Q 가 없는 항목 | "Q4 보정 실패" / "calibration_untested" — 앵커가 없으면 통째로 못 넣는 구조 |

### 요청

**(A) 표 고정 조건 수정**
- `별도` 표제 조건을 **"같은 PDF 안에 연결재무상태표가 따로 있을 때만"** 요구하도록 바꿔라.
  연결표가 없는 회사(단일법인)는 표제가 "재무상태표"만으로 적격이다.
- 비지배지분 부재·`I. 운용자산` 부재·6-4 시장위험 배제·첨부파일 참조 배제 조건은 유지.
- 고친 뒤 위 8 개사 × 코어 결측 분기(총 82 분기)를 재실행. 회사별로 첫 분기 1 개는 4Q 정답지 또는
  직전/직후 분기 마스터와 자릿수·기준 대조를 붙여서 보고.

**(B) QoQ 가드 수정**
- 앵커를 **직전·직후 양방향**으로. 둘 중 하나라도 ±30% 안이면 통과.
- 둘 다 밖이면 skip 이 아니라 **DART 공시이력에서 사유(유상증자·감자·합병·경과조치)를 찾아** 사유가 확인되면
  적재하고 사유를 `근거` 필드에 남긴다. 사유를 못 찾을 때만 hold.
- KR1010 2023.1Q~2024.2Q 자본총계 5 칸, KR0004 2025.3Q 는 위 근거로 이번에 반드시 닫아라.

**(C) 앵커 없는 항목**
- 4Q 앵커가 없어도 `항목1 == 항목2 + 항목3` 이 닫히고 축척이 유일하게 결정되면 적재. `근거` 에
  "앵커 없음, 항등식+축척으로 채택" 명기. 항목 31(이익잉여금)은 자본 세부 합이 자본총계와 맞는지로 검산.

### 수용 기준
- 코어 1·2·3 결측 (회사,분기) **117 → 0** 이 목표. 못 닫는 칸은 **칸 단위 skip_reason** 필수(원문 부재·스캔·서식 판별불가 등 구체적으로).
- 마스터 반영은 1 라운드와 같은 경로(오버라이드 셀 단위 UPSERT → build_ifrs17_bs → validate_data_contract RED=0 → 골든 --update → xlsx 17BS sync).
- 1 라운드에서 들어간 925 칸은 값이 바뀌면 안 된다(변경 0 확인).
- **push 금지.** 동시 서브에이전트 4 개 이하(세션 한도).

### 참고
- 1 라운드 종결 기록: `inbox/parser/_resolved/20260911T0109Z__orchestrator__MULTI__ifrs17bs_disclosure_core_backfill.md`
- sender 실측 스크립트: `<scratchpad>/residual.py`, `skipwhy.py` (필요 시 재작성)

## 답변 (recipient 작성 — 처리 후)

**요약**: 코어(항목1·2·3) 결측 (회사,분기) **117 → 29**(88건 닫음, 75%). sender 가 짚은
가설 (A)(B)(C)는 방향은 맞았으나 **실측 재현해보니 실제 메커니즘은 다른 6가지 엔진 버그**
였다(아래). 이미지 기반 5개사는 서브에이전트 4개(세션 한도 준수) 병렬로 렌더링+비전
백필. 1라운드 925칸(및 그 이전 데이터 포함 총 7,967행)은 스냅샷 대조로 **전부 값 불변**
확인. `validate_data_contract.py` **RED=0**. push 안 함. 상세는 `TODO_parser_ifrs17.md`
88th pass(재현 명령 포함), 요약은 `docs/changelog_parser_ifrs17.md` 참조.

**(A)에 대해 — 실측 결과 "별도 표제 과엄격"이 아니었다.** `page_is_consolidated()`는
이미 "신호 없음(None)"을 낮은 우선순위 후보로 받아들이고 있었다(별도 표제가 없어도
채택 가능). 106건을 하나하나 재현한 결과 진짜 원인은:
1. 헤더 마커 사각 — `당분기`/`당기말`/`당분기말`/`해당분기`/`전기기말`가 `_CUR_WORD_RE`/
   `_PREV_WORD_RE`에 없어 헤더 인식 자체가 실패(악사손해 다수 분기).
2. 헤더-데이터 열 오프셋 — 헤더 텍스트가 데이터보다 그리드 1칸 뒤에 찍히는 표가 있어
   라벨에 다음 칸 값이 섞이고 당기/전기/증감이 하나씩 밀려 읽힌다(AIG손해·악사손해).
   `_pad_shift()` 신설로 해결.
3. "(별도)"/"(연결)" 괄호수식어가 별개 텍스트런이라 fitz 가 "재무상태표별도"(순서반전)로
   추출하는 문서가 있어 `page_is_consolidated()`가 놓쳤다(AIG손해 2026.1Q) — 양쪽 순서
   다 인식하도록 수정.
4. "6-4" 마커가 "...206"+"-4,558,932"(숫자조판 우연)에서 오탐돼 진짜 표 페이지를
   배제시켰다 — "6-4."(마침표 포함)로 좁혔다(원 의도는 `시장위험`/`익스포져` 마커가 중복
   커버해 안전).
5. **△(세모) 음수 미인식 — 가장 파급력 큰 버그.** `parse_number()`가 괄호만 음수로 알고
   있었다. 한국 회계 관행의 세모(△) 표기를 만나면 `float()` 가 죽고 그 행이 **스킵 사유도
   없이 조용히** 유실됐다(KR0004 2025.1Q 자본총계 실측 — "△244,064,855,740"). 음수 항목이
   있는 분기마다 재발 가능한 구조적 함정이었다.
6. 표 하나가 페이지/표객체 여러 개로 쪼개짐 — 기존 1페이지 이어짐을 최대 8페이지로
   확장(처브라이프 KR0100, 자산총계~부채와자본총계가 6페이지에 걸침) + 같은 페이지 안에서
   표 객체 2개로 쪼개지는 경우도 병합(KR0100 2023.3Q) + 한 논리열 안에서 총계류 행만
   별도 칸에 분산되는 경우 재구성(IBK연금 KR1011, `_reconcile_two_blob_span` 신설).
운용자산(감독규정 별표) 서식은 요청대로 전면배제→낮은 우선순위 후보로 다운그레이드했다
(AIG·악사 등은 이 서식이 유일한 공시형식).

**(B) QoQ 양방향 앵커**: `nearest_before`/`nearest_after` 신설, 직전·직후 둘 중 하나라도
±30% 안이면 통과하도록 `_qoq_gate()`로 교체. **DART 공시이력 확인은 요청대로 실행** —
웹검색으로 KR1010(교보라이프플래닛)의 2023년 7차 유상증자(누적 3,690억)+IFRS17 도입
연간손실 214억원(인사이트코리아·ZDNet 기사)을 확인해 2023.1Q/2Q/3Q 자본총계 3칸을 사람이
승격했고, KR0004(예별손해) 2025.3Q는 티켓이 지목한 "+29,899"와 추출값이 정확히 일치해
승격했다(`*_agent_note.json`). KR0004 2024.1Q~3Q 항목3(같은 유형, 항등식은 닫히나 외부
근거가 상대적으로 약함)과 KR1098 2025.2Q는 owner 재확인용으로 skip_reason 에 추출값을
보존한 채 남겼다.

**(C) 4Q 앵커 없는 항목**: `safe_set`에서 항목2/3의 "q4_table_found=False일 때만" 조건을
제거해 "그 항목 자체가 Q4 앵커가 없는" 경우로 넓혔다. 안전장치(항등식 1=2+3 게이트)는
그대로 유지. 추가로 **`target_quarters_for`가 항목1만 확인하던 사각을 발견** — "항목1은
이미 있는데 항목2/3만 빠진" 분기(KR1010 5개 분기가 정확히 이 사각이었다)가 재처리 대상에서
조용히 제외되고 있었다. 코어 3항목 전부 기준으로 확장하고, 기존값 재기록 방지 가드
(`already_present`)를 새로 넣어 1라운드 데이터가 절대 덮어써지지 않게 했다.

**잔여 29건**(전부 skip_reason 기록): 이미지 미착수 12(KR0049 3·KR0080 3·KR1011 4·KR0100
1·KR0097 1) · 경영공시 첨부파일 미수집 9(KR0050 전부, "재무제표 사항은 첨부 파일 참조") ·
원문 데이터결함 1(KR0097 2024.1Q, 헤드라인 자체가 1,000만원 오차) · QoQ hold 7(KR0004 5·
KR0051 1·KR1098 1, owner 재확인 권장).

**owner 재확인 필요 2건**: (1) KR0004 2026.1Q/2Q 항목1이 직전 대비 48배 — 스케일 오판
의심. (2) KR0004 2024.1Q~3Q 항목3 — 트렌드는 자본잠식 진행형으로 그럴듯하나 KR1010 만큼
강한 외부 근거는 없음.

**재현 명령**: TODO_parser_ifrs17.md 88th pass 맨 끝 참조 (extract_bs_from_disclosure →
merge → build_ifrs17_bs → validate_data_contract → sync_master_xlsx_sheet "17BS").

---
**sender 재확인 (orchestrator, 2026-09-11 20:40)**: 마스터 7,967→8,840행(+873), 코어 결측 117→29, 1라운드 925칸 변경 0, `validate_data_contract.py` **RED=0** YELLOW=88 직접 재실행 확인. 예별 2026.1Q/2Q 항목1(48배 점프)은 owner 판단 대기로 보류 유지. 종결.
