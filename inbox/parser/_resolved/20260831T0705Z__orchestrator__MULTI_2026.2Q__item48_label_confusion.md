---
from: orchestrator
to: parser
created: 20260831T0705Z
status: answered
route: reparse
company: MULTI
period: 2026.2Q
rule: 47_tier2_census / 48_tier2_limit
lane: kics
iter: 1
---

## 미결 (sender 작성)

**`item48`(보완자본 한도)에 `item3`(보완자본) 값이 그대로 복사된다. 2026.2Q 온보딩에서 확인한 9사 중 7사.**

라벨 매처가 **"보완자본 한도"를 "보완자본"으로 매칭**하는 것으로 보인다(접두사 일치가 더 긴 라벨을 먹는 형태). 매번 값이 `item3` 와 소수점까지 동일하다.

| 회사 | 오염된 item48 | 원문 정답 | 검산 (`item14전 × 50%`) |
|---|---|---|---|
| KR0050 하나손보 | 6065 (=item51) | 2338.28 | — (2026-08-29 별도 확인) |
| KR0095 메트라이프 | 21854 | 10446.96 | 20894×0.5 = 10447 |
| KR0002 한화손해 | 41849 | 17798.33 | 시계열 16,972 → 17,798 로 자연스러움 |
| KR0074 라이나 | (item3 복사) | 원문값 | 확인됨 |
| KR0049 악사 | 2653 | 1269.56 | 2539×0.5 = 1269.50 (diff 0.06) |
| KR0075 BNP카디프 | 234 | 508.40 | 1016.80×0.5 = 508.40 |
| KR0008 삼성화재 | 97415 | 77777.70 | ×0.5 = 77777.5 (diff 0.20) |

**독립 검산식이 있다**: `item48 == item14(적용전) × 50%`. 일곱 건 전부 이 식으로 정답이 확인됐다. 추출 직후 이 항등식으로 자체 검사만 걸어도 조용한 오염을 막을 수 있다.

**부수로 확인된 두 가지 (같은 표 관련, 함께 반영 요망)**:

1. **TFI 표(items 47-54)는 반드시 표 단위로 채워야 한다.** 47/48/49 만 고치면 `53_tfi_memo_rows` 가 "같은 표를 반쯤만 읽었다"며 **새 RED 로 발화**한다 — 악사·BNP카디프 두 곳에서 재현됐다. 50/51/52/53/54 까지 같이 채워야 순변화가 음수가 된다.

2. **TFI 항등식은 보편식이 아니다.** CAPPED 회사는 `item51 = min(item47, item48) + item49` (적용후 컬럼은 `+ item54`), UNCAPPED 회사는 `item3 ≈ item47` 로 닫힌다. `kics_json_rules.py` 906행 부근에 이 구분이 이미 문서화돼 있고 KR0075 가 UNCAPPED 반례로 이름이 박혀 있다. **KR0050 온보딩 스크립트의 단일 공식을 보편식처럼 재사용하면 UNCAPPED 회사에서 틀린 값을 만든다** (오케스트레이터가 실제로 그 힌트를 잘못 넘겼고, 담당 에이전트가 코드에서 반례를 찾아 바로잡았다).

**요청**: `kics_disclosure_parser.py` 의 라벨 매칭에서 "보완자본 한도"/"보완자본 한도 적용 전"/"보완자본"을 구분하고, 추출 직후 `item48 == item14전 × 50%` 자체검산을 걸 것.

원문 근거: 각 회사 `[지급여력비율의 경과조치 적용에 관한 사항] 1) 공통적용 경과조치 관련` 표.
적용된 데이터 정정: `data/_derived/_patch*_2026q2_KR*.json` 의 item47-54 셀 `근거` 참조.

## 답변 (recipient 작성 — 처리 후)

**부분 처리 — 담당 발주(KR0005·KR0075 TFI item47-54 백필) 범위 내에서만.** 이 티켓이 요청한
`kics_disclosure_parser.py` 라벨 매처 코드 수정 + 자체검산 배선은 **안 함**(내 발주 범위 밖,
데이터 패치만 담당). 아래는 데이터 쪽 기여분.

- **KR0005 흥국화재 = 8번째 확인 사례.** 표에 없던 7사 목록에 KR0005는 없었지만 내 발주(item48
  검산 필수 지시)로 직접 확인 — item48 라이브값 29380 = item3(헤드라인 보완자본, md L379
  '보완자본 | 29,380')를 그대로 복사한 오염값. 원문 '보완자본 한도' 행(PDF p17, fitz 확인)은
  1,132,703백만원(전=후) → 11327.03억. 독립검산 item14_전×50%=22654×0.5=11327 과 diff 0.03
  으로 일치. `data/_derived/_patch_2026q2_KR0005.json`에 정정 셀 추가(값=값_적용후=11327.03).
- **KR0075**: 이 티켓이 이미 정답(508.40)을 표로 갖고 있었고, 기존 `_patch2_2026q2_KR0075.json`
  이 이미 그 값으로 47/48/49를 채워둔 상태였다(내 세션 시작 전 완료돼 있었음, 재검산만 재확인:
  item14_전×50%=1016.80×0.5=508.40 일치). 부수로 같은 표의 item50/51도 patch2가 "원문에 없다"고
  잘못 판단해뒀던 것을 발견해 정정(md L411-412에 실제로 존재, 176,419/23,386백만원) — 별도
  answered 처리는 안 하고 이 티켓과 무관하므로 내 stage TODO 항목에만 기록.
- **게이트 재검증**: KR0005 `47_tier2_census` RED→GREEN(격리 스크래치 비교, 범위 밖 변경 0건).
  상세는 `TODO_parser_kics.md` 2026-08-31(KR0005·KR0075 TFI 표 item47-54 백필) 항목.

**남은 것**: 이 티켓의 본 요청(라벨 매처 코드 수정 + `item48==item14전×50%` 추출 직후 자체검산
배선)과, 목록의 나머지 6사(KR0050/95/02/74/49/08) 데이터 정정 상태 확인은 **미확인·미처리** —
병행 세션들이 이미 처리했을 가능성 있음(라이브 마스터가 KR0005/KR0075 값을 이미 내 패치와
동일하게 흡수한 정황으로 볼 때 다른 세션이 이 티켓 전체를 동시에 작업 중일 수 있음). sender가
전체 진행 상황을 재확인해달라.

## 답변 2 (후속 세션, 2026-08-31 — 본 요청 완결)

**이 티켓의 본 요청(라벨 매처 일반화 + 자체검산 배선)을 완료.** `scripts/fill_tfi_
table_to_disclosure.py` 신설 — item47-54 전체(이 표의 8개 행)를 다루는 최초의 범용 표
추출기. `보완자본 한도 적용 전`/`보완자본 한도`/`보완자본`을 긴 것부터 순서대로
`_match_row_label`에 넘겨 이 티켓이 지적한 접두사 충돌을 구조적으로 해결했고,
`item48 == item14(적용전) × TIER2_LIMIT_RATIO` 자체검산도 배선했다(어긋나면 신규
item48은 저장하지 않고 SELFCHECK_BLOCKED로 표시 — 이번 라운드 발동 0건).

**단 기존 오염값은 정책상(절대 덮어쓰기 금지) 건드리지 않는다** — 대신 원문 재추출값과
대조해 `conflict`로 전건 보고한다. 목록의 원 7사(KR0050/95/02/74/49/75/08)는 지금
전부 GREEN(전 항목 일치, conflict 0) — 이미 다른 세션들이 해소해 뒀다. **원 7사 밖에서
동일 패턴 11사를 2026.2Q에서 새로 확인**(KR0051·68·71·72·73·80·97·99·100·104·1098)
**+ 과거분기 KR0029 2개 분기(2025.2Q·2025.3Q)** — 즉 이 버그는 원 티켓이 파악한 것보다
훨씬 넓게, 그리고 이번 라운드에도 계속 재발하고 있었다. 전건 원문 word-단위 대조로
정답 확정(예: KR0072 raw "보완자본 한도 776,466백만" = 7764.66억 = 내 후보, existing
16273 = item3(보완자본 1,627,315백만) 그대로 복사). `TODO_parser_kics.md` 2026-08-31
(item47-54 TFI 표 범용 추출기 신설) 항목에 13개 셀 전건 existing/candidate 쌍 열거.

**owner/validation 판단 필요**: 위 13개 오염 셀을 conflict 로그의 후보값(원문검증됨)으로
교체할지 승인만 하면 즉시 반영 가능 — 셀단위 UPSERT라 범위밖 영향 없음 확인됨. 그 외
게이트 신규 RED 19건(`53_tfi_memo_rows` 등, 전부 이 오염값을 안 덮어쓴 직접 결과)도
같은 TODO 항목에 원인·근거와 함께 기록.

## 답변 3 (coordinator 승인 반영, 2026-08-31)

coordinator가 A-E 갈래 판정을 내려 위 13건(+ 추가 조사로 확정된 4건, 합 17셀)을
raw 재대조 후 확정 반영했다. `item48==item14전×50%` 유도값 주입은 전 셀 안 함(coordinator
(B) 지시대로 — 17셀 전부 raw에 인쇄된 값을 그대로 읽은 것). 상세 표(회사·항목·기존/정정값·
원문근거)와 게이트 delta(RED 99→90, `47_tier2_census_post` 17→10)는
`TODO_parser_kics.md` 2026-08-31(coordinator 지시 — item47-54 conflict 22건 중 17셀
확정반영) 항목 참조. 나머지 5건(KR0079 4셀·KR0008 2025.3Q item52)은 여전히 미반영
(각각 D 지시·기존 owner 예외로 존치).

이 티켓의 본 요청(라벨 매처 일반화 + 자체검산 + 오염 데이터 정정)이 전부 처리됨.

status: resolved
