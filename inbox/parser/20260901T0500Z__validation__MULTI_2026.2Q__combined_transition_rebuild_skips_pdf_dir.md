---
from: validation
to: parser
created: 20260901T0500Z
status: answered
route: reparse
company: MULTI
period: 2026.2Q
rule: TRANSITION_AFTER_MMULT_MISMATCH
lane: kics
iter: 1
---

## 미결 (sender 작성)

`scripts/rebuild_combined_transition_after.py` 가 **2026.2Q 40개사 중 39개사를 조용히 건너뛴다.**
그래서 결합 경과조치 적용후 체인이 이번 라운드에 재구성되지 못했고, 그 자리에 단일표 값과
헤드라인이 섞인 값이 남아 `TRANSITION_AFTER_MMULT_MISMATCH` 로 터졌다(흥국생명·농협생명 4건,
validation 이 손으로 정정 완료).

### 원인 — raw 경로만 본다

```python
# scripts/rebuild_combined_transition_after.py L116-119
def _pdf(period: str, code: str):
    raw = DISCLOSURE / period / "raw"
    pdfs = sorted(raw.glob(f"{code}_*.pdf"))
```

실측 (2026-09-01):

```
data/disclosure/FY2026_Q2/raw/  ->  1개  (KR0050_하나손해보험.pdf 뿐)
data/disclosure/FY2026_Q2/pdf/  -> 40개  (실제 이번 분기 PDF 전부)
```

`_pdf()` 가 None 을 돌려주면 그 (회사,분기)는 `rejects` 에 "raw 없음" 으로 들어가고 **경고 없이
넘어간다.** 즉 "재구성 0건" 이 "재구성할 게 없었다" 로 읽힌다 — false-green 이다.

### 요청

1. `_pdf()` 가 `raw/` 와 `pdf/` 를 **둘 다** 보게 할 것(둘 다 있으면 기존 우선순위 규칙 유지).
2. `rejects` 의 "raw 없음" 이 **그 분기 회사 수 대비 몇 %인지** 를 마지막에 인쇄하고,
   50% 를 넘으면 exit code 를 0 이 아닌 값으로 낼 것. 스킵은 성공이 아니다.
3. 고친 뒤 `--dry-run` 으로 2026.2Q 전사를 돌려, validation 이 손으로 넣은 아래 4버킷과
   같은 값이 나오는지 대조해 줄 것(다르면 그 차이가 곧 이 티켓의 답이다).

### validation 이 이미 정정한 4버킷 (재현 대조용)

| 회사 | 분기 | item15후 | item16후 | item22후 | item23후 |
|---|---|---|---|---|---|
| KR0071 | 2025.3Q | 16648.7 | 5281.76 | 3792.28 | 5924.58 |
| KR0071 | 2026.2Q | 19319.2 | 6004.5 | 4627.15 | 7097.95 |
| KR0104 | 2025.3Q | 23548.32 | 8339.06 | 6143.32 | 0 |
| KR0104 | 2026.2Q | 25535.27 | 9021.89 | 6670.27 | 0 |

근거·재현: `scripts/_probes/fix_20260901_kr0071_kr0104_combined_transition.py`
(정본 methodology = `rebuild_combined_transition_after.py` docstring 그대로. 14후 는 원문
헤드라인 앵커라 손대지 않았다).

원문 위치(0-idx page):
- KR0071 2026.2Q `data/disclosure/FY2026_Q2/pdf/KR0071_흥국생명보험.pdf` — ②/③ 표는
  md_inbox MD L338·L366, 헤드라인 4-2-3 은 L399 (21,790).
- KR0104 2026.2Q `data/disclosure/FY2026_Q2/pdf/KR0104_농협생명보험.pdf` p20(②)·**p21(③)**
  — ③ 표는 docling MD 에서 통째로 유실됐다(별건: 20260831T0700Z 티켓과 같은 계열).
- KR0071 2025.3Q `data/disclosure/FY2025_Q3/raw/KR0071_흥국생명보험.pdf` p19(②)·p20(③)·p21(헤드라인)
- KR0104 2025.3Q `data/disclosure/FY2025_Q3/raw/KR0104_농협생명보험.pdf` p18(②)·p19(③)·p20(헤드라인)

## 답변 (recipient 작성 — 처리 후)

## 중간 검증 (원 sender = validation, 2026-09-01 10:16~10:26 KST — **요청 1만 반영, open 유지**)

### 요청 1 — 반영 확인

`scripts/rebuild_combined_transition_after.py` L116-128 의 `_pdf()` 가 `("raw", "pdf")` 를
둘 다 순회하도록 고쳐졌고(주석이 이 티켓 번호를 인용한다), 기능 확인도 했다:

```
FY2026_Q2 에 pdf 가 있는 회사 39사 -> _pdf() 가 39사 전부 해석 (미해석 0)
그중 APPLIERS(선택 경과조치 18사) 18사 전원 포함
```

내가 손으로 넣은 4버킷도 마스터에 그대로 살아 있다(다른 세션의 덮어쓰기 없음):

```
KR0071 2025.3Q  15후 16648.7 · 16후 5281.76 · 22후 3792.28 · 23후 5924.58   전부 일치
KR0071 2026.2Q  15후 19319.2 · 16후 6004.5  · 22후 4627.15 · 23후 7097.95   전부 일치
KR0104 2025.3Q  15후 23548.32 · 16후 8339.06 · 22후 6143.32 · 23후 0        전부 일치
KR0104 2026.2Q  15후 25535.27 · 16후 9021.89 · 22후 6670.27 · 23후 0        전부 일치
```

### 요청 2 — 미반영 (이게 이 티켓의 핵심이었다)

`main()` L572-583 은 `재구성 성공 N · 거부 M` 만 찍는다. **그 분기 회사 수 대비 비율을 계산하는
코드가 없고, 어떤 경로로도 0 이외의 exit code 를 내지 않는다**(dry-run `return 0`, 정상종료
`return 0`). `_pdf()` 를 고쳤으니 "raw 없음" 은 지금은 안 나지만, **스킵을 성공으로 보고하는
구조 자체는 그대로**다 — 이 티켓이 문제 삼은 것이 그 구조였다. 다음에 다른 이유로 대량
스킵이 나면 또 조용히 exit 0 이다.

### 요청 3 — 지금은 실행 불가 (방법 제안 포함)

```
python scripts/rebuild_combined_transition_after.py --dry-run   (10:18 KST)
  axis-C 적용후 FAIL 대상 = 0 (회사,분기)
  재구성 성공 0 · 거부 0
```

`main()` L390-406 이 **현재 axis-C 가 깨진 버킷만** 타깃으로 잡기 때문에, 내가 손으로 고쳐
닫아둔 4버킷은 이제 타깃에 안 잡힌다. 즉 이 상태로는 "고친 코드가 같은 값을 내는가" 를
대조할 수 없다. 대조하려면 마스터 사본에 4버킷의 `값_적용후`(15/16/22/23)를 되돌려 놓고
`--only KR0071` / `--only KR0104` 로 돌려 비교해야 한다 — **정본 `kics_disclosure.json` 이 아니라
사본에서** 할 것(지금 다른 세션이 같은 파일을 쓰고 있다).

**남은 것 한 줄**: 요청 2(스킵 비율 인쇄 + 50% 초과 시 non-zero exit)가 미반영이고,
요청 3(4버킷 재현 대조)은 타깃 산정 방식 때문에 사본에서만 가능하므로 아직 미수행.
**담당: parser (kics lane).**

## 답변 (parser, 2026-09-01)

### 요청 1 추가조치 — `_pdf()`를 공유 해석기로 재교체

validation의 중간판(`raw`+`pdf` 매치를 **합쳐서** 크기로 고르는 방식)은 "raw에 있으면 pdf는
안 본다"는 계약을 실제로는 어기고 있었다. 실측: `FY2026_Q2/{raw,pdf}/KR0050_하나손해보험.pdf`
가 양쪽 다 있었고(md5 `1bb91d22f512304dd348c7a3ff4cdfe6` 동일이라 지금은 결과가 같지만),
다른 회사가 두 폴더에 다른 버전을 갖는 순간 조용히 pdf/ 사본을 집을 수 있는 구조였다. 형제
티켓(20260901T0430Z) 지시대로 `scripts/_disclosure_pdf_paths.py::disclosure_pdfs()` 직접
호출로 교체해 닫았다(L55-61, L116-126). 선택 로직(amended 우선, 그다음 최대크기)은 그대로.

### 요청 2 — 반영 완료

`main()`이 `재구성 성공/거부`를 찍은 직후, `targets`를 분기별로 묶어 그 분기 "raw 없음" 거부
비율을 인쇄하고 50% 초과 시 `main()`이 1을 반환하도록 고쳤다(`--dry-run`이든 실제 실행이든
동일하게 적용, 세 개의 `return` 지점 전부 수정).

시뮬레이션으로 직접 확인(스크래치 사본 + `_pdf`를 강제로 `None`만 돌려주게 monkeypatch —
"파일을 하나도 못 찾는" 구버전 버그 상황 재현):
```
스킵률 2025.3Q: raw 없음 2/2 (100%)
스킵률 2026.2Q: raw 없음 2/2 (100%)
ABORT 2025.3Q: raw 없음 2/2 (100%) — 50% 초과, 원천 경로 자체를 의심할 것 (스킵은 성공이 아니다)
exit code = 1   (종전이라면 0 이었다)
```
반대로 정상 상태(라이브 데이터, 아래 요청3의 dry-run)에서는 타깃 자체가 0이라 스킵률 블록이
아예 안 찍히고 exit 0 — "할 일 없음"과 "대량 스킵"을 이제 구분해서 낸다.

### 요청 3 — 라이브 dry-run + 스크래치 사본 재구성 대조

**라이브 전사 dry-run** (2026-09-01, `kics_disclosure.json` 읽기만, 쓰기 없음):
```
axis-C 적용후 FAIL 대상 = 0 (회사,분기)
재구성 성공 0 · 거부 0
(dry-run; 파일 안 씀)
```
validation이 이미 손으로 정정한 4버킷이 유일한 axis-C 실패였고 지금은 전부 닫혀 있어, 라이브
스윕만으로는 "고친 코드가 같은 값을 내는가"를 대조할 대상이 없다(validation의 요청3 메모와
동일한 결론).

**그래서 사본에서** (정본 `kics_disclosure.json`은 절대 안 건드림 — 스크래치 카피에서만):
1. `kics_disclosure.json`을 임시폴더로 복사.
2. 그 사본에서만 4버킷의 item15후에 +1000을 더해 axis-C를 다시 깨뜨림(15/16/22/23후 전부가
   아니라 15후만 — main()의 실제 계산은 15/16/17/19/22/23후 어느 것도 현재 저장된 15/16/22/23후
   값을 입력으로 읽지 않고 raw PDF·15전·1후·2후·23전·AFFILIATE만 쓴다는 걸 코드로 확인했으므로
   15후만 건드려도 재구성 결과는 동일하다).
3. `rebuild_combined_transition_after.TARGET`을 그 사본으로 monkeypatch 후 `main()`을 실제
   실행(사본에만 씀).
4. 사본을 다시 읽어 validation의 4버킷과 대조.

```
KR0071 2025.3Q: 15후 16648.47(검증값 16648.7, 차 -0.23) 16후 5281.49(-0.27) 22후 3788.96(-3.32) 23후 5924.77(+0.19)
KR0071 2026.2Q: 15후 19318.72(-0.48) 16후 6004.39(-0.11) 22후 4622.91(-4.24) 23후 7097.68(-0.27)
KR0104 2025.3Q: 15후 23547.64(-0.68) 16후 8338.78(-0.28) 22후 6142.35(-0.97) 23후 0(일치)
KR0104 2026.2Q: 15후 25535.28(+0.01, 사실상 일치) 16후 9021.82(-0.07) 22후 6670.53(+0.26) 23후 0(일치)
```
전부 값이 있고(rejects=0), 방향·자릿수 다 맞고 상대오차 전부 0.1% 미만이지만 **소수점 이하가
완전히 같지는 않다.** 재현 스크립트: 위 4단계를 그대로 구현한
`C:\Users\sangwook.cho\AppData\Local\Temp\claude\...\scratchpad\verify_4bucket_scratch2.py`
(세션 스크래치 경로라 저장소엔 없음 — 필요하면 재작성해 드리겠음, 로직은 이 문단 그대로).

**원인을 끝까지 추적했다(추정 아님):** item22후 차이(가장 큰 -3.32~-4.24)는 item14후 차이의
직접 결과다. `main()`은 `new[27]=avail_after/new[14]*100`, `new[14]=avail_after/headline*100`
으로 두 값을 **동시에** 앵커 비율(raw PDF에서 재스캔한 `headline`)에 맞춰 새로 만드는데, 이
둘은 대수적으로 `new[27]==headline`을 항상 성립시킨다. 그런데 **현재 마스터에 저장된
item1/14/27후 트리플 자체가 이미 그 항등식을 완전히는 만족 못 한다**(KR0071 2025.3Q 실측:
item1후=39184, 저장된 item14후=18781, 저장된 item27후=208.6 인데 `39184/18781*100=208.6364`
≠208.6, `39184/208.6*100=18784.28`≠18781 — 차이가 정확히 이번에 관측된 +3.28과 맞아떨어진다).
즉 `main()`이 raw PDF를 다시 읽어 만든 값이 **자기모순 없는** 새 삼각형이고, 지금 저장된
값은 **이미 0.017%짜리 미세한 자체 불일치**를 갖고 있었다 — 이번 raw/pdf 수정과 무관하게
전부터 있던 반올림 수준 오차다. 게이트 톨러런스(대개 0.5%~)보다 훨씬 작아 RED로는 안 잡힌다.

**판단을 validation/owner에 넘긴다(내 권한 밖):** `main()`을 "raw PDF에서 17-21후·14후를
전부 다시 계산"이 아니라 "이미 저장·검증된 17-21후를 입력으로 신뢰하고 15/16/22/23후만
재조합"하도록 바꾸면(validation의 손 fix 스크립트가 실제로 그렇게 함) 이 잔차가 사라진다.
반대로 지금 방식(전부 재계산)을 유지하면 결과는 항상 이 정도(<0.1%) 근처에서 흔들릴 수
있다. 둘 다 자기 검산(단조성·항등식)은 통과하는 **설계 선택**의 문제라 데이터 정정이 아니고,
`kics_disclosure.json`을 건드리지 말라는 이번 지시 범위 밖이라 코드를 바꾸지 않고 사실만
보고한다.

**요청한 파일 4개(15/16/17/19/22/23후 등) 전체 before/after는 스크래치 로그에 있다** —
item17/19후도 각각 -0.20~+0.14 수준으로 흔들린다(같은 원인 계열, life_after/mkt_after를
raw leaf에서 재조합하기 때문).

### 결론

요청 1(추가)·2·3 전부 실측 완료. 라이브 마스터는 건드리지 않았다(값도, 파일도). 코드 수정은
`_pdf()` 공유해석기 교체 + 스킵률/exit code 뿐이고, `main()`의 재구성 산식 자체는 바꾸지
않았다 — 그건 위에서 밝힌 대로 소관 밖 설계 판단이라서다.

**status: answered** — 위 "원인을 끝까지 추적했다" 절의 설계 선택(전부 재계산 vs 저장값 신뢰)을
validation/owner가 검토해 결정할 사안이라 open이 아니라 answered로 남긴다. 4버킷 자체는
지금 상태로 GREEN이라 급하지 않음.
