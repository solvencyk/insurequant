---
from: orchestrator
to: parser
created: 20260831T0710Z
status: open
route: reparse
company: KR0008
period: 2026.2Q
rule: 19_market
lane: kics
iter: 1
---

## 미결 (sender 작성)

**`fill_market_subitems_to_disclosure.py` 의 `_bare_subrisk_item()` 이 총계행을 "합계"/"계"로 적는 서식을 못 읽는다.**

삼성화재 2026.2Q 의 ③④⑤⑥ 표(주식/부동산/외환/자산집중 위험액 현황)는 총계행 라벨이 `Ⅲ. 합 계2)` · `Ⅲ. 합 계` · `계` 뿐이고, 위험명 문자열("주식위험액" 등)은 **표 헤더에만** 나오고 값이 있는 행에는 없다. 매처는 col0 이 문자 그대로 `주식위험`/`부동산위험`/… 이어야 잡으므로 네 항목이 통째로 빠졌다. ②금리위험액 표만 우연히 총계행이 `Ⅳ. 금리 위험액` 이라 item36 하나만 잡혔다.

**로더의 거부 동작 자체는 옳았다.** `v5=[13833,0,0,0,0]` 로 sqrt 추정이 13,833 이 나와 공시 item19=157,702 와 91.2% 어긋나자 저장을 거부했다:

```
MKT-SKIP KR0008 2026.2Q: est=13833 item19=157702 rel=91.2% (not stored)
```

세부를 원문에서 채운 뒤 실제 룰 엔진으로 재계산하니 `sqrt(V'·MARKET_M·V) = 157,702.03` vs 공시 157,702.0 — **오차 0.00002%** 로 닫힌다. 즉 item19 가 옳고 세부 추출이 틀렸던 것이다.

**요청**: 총계행 매칭을 `합계` / `합 계` / `계` / 로마숫자 접두(`Ⅲ.`·`Ⅳ.`) 를 포함하도록 보강하고, 표 헤더의 위험명으로 어느 위험액 표인지 식별하도록 할 것. 지금 구조는 회사가 표기만 바꿔도 조용히 빠진다.

데이터는 이번 라운드에서 이미 정정됨(`data/_derived/_patch_2026q2_KR0008.json`). 이 티켓은 코드 소관.

raw: `data/disclosure/FY2026_Q2/pdf/KR0008_삼성화재해상보험.pdf` p.33-37

## 답변 (recipient 작성 — 처리 후)
