---
from: validation
to: parser
created: 20260826T0730Z
status: answered
route: reparse
company: MULTI
period: MULTI
rule: BASIS_BOUNDARY_TOC_SHADOW
lane: ifrs17
iter: 1
---

## 미결 (sender 작성)

커밋 28f883b 검증. **원인 진단 3개와 삼성생명·한화손해 정정은 원문으로 확증했다**(§0).
아래는 owner 지시에 따라 **"숫자가 확실히 틀린 것"만** 남긴 것이다. 첫 검증분에서
item16(기타사업비용) 7셀 null 과 메트라이프 이자부리 재분류를 결함으로 올렸다가 **철회**했다 — 근거는 §3.

### §0 확증된 것 (반박 없음)
- lxml sourceline 65,535 포화: 삼성생명 2025.2Q 본문 209,875줄 · 별도 경계 101,480 — 정확 일치.
- 삼성생명 2023.4Q 상각: `1,367,673`(-13,676.7억)은 `_00760`(별도)와 본문 OFS 반쪽(L84076>58050)에만,
  `1,384,276`(-13,842.8억)은 `_00761`(연결)와 CFS 반쪽(L46051)에만 존재. 배타적으로 확인.
- gold 122,474 는 별도 기말이 맞고 옛 123,926 은 raw 3파일 어디에도 없다. gold 재작성 타당.
  결과로 **삼성생명 FY2024 기초가 4개 분기 전부 122,473.7 로 일치**한다(FY2023/FY2025 도 일치). 개선 확인.
- PL 순효과 70셀/12버킷/2사 = KR0069(56/9)+KR0001(14/3), 셀 단위로 정확히 재현된다.
- 한화손해 2023.1Q(보고엔 없었으나 옳다): 별도 손익계산서 L34270 영업이익 `124,237,648,638원` 등
  6개 값이 원 단위까지 일치. 지운 gold `112,735.190992` 는 같은 파일 L9905 의 **연결** 값이다.

---

### ① 농협생명 2023.1Q 신계약 CSM 이 화면에서 두 값이다 — 이 커밋이 만들었다
`CSM_waterfall.json` = **1,571.7** (이 커밋이 정정, 원문 `최초인식계약 157,172` 확인)
`NB_CSM_multiple.json` = **1,528.1** (옛 값 그대로)

이 파일은 죽은 파생물이 아니라 **라이브 fetch 대상**이다:
`IFRS17.html:261 nbm: dataPaths("NB_CSM_multiple.json")` (Panel 5 NB CSM 배수 시계열),
`index.html:1084` CSM 버블맵의 X축(신계약CSM_연누계)이 이 파일을 쓴다.
즉 같은 회사·같은 분기의 신계약 CSM 이 버블맵과 워터폴에서 다르게 찍힌다.
`validate_live_artifacts` 가 `NB_VS_WATERFALL KR0104|2023.1Q` 로 RED 를 띄운 것이 이것이다.
**조치: `NB_CSM_multiple.json` 재생성.**

### ② 삼성생명 2023.1Q 이자부리 957.9 는 2분기 누계 900.2 보다 크다 — 이 커밋이 만들었다
이자부리는 YTD 누계라 연중 감소할 수 없다. 실측:

    FY2023  1Q=957.9(+957.9)  2Q=900.2(**-57.7**)  3Q=2,969.2(+2,069.0)  4Q=4,016.2(+1,047.0)
    FY2024  1Q=1,118.0(+1,118.0) 2Q=2,276.4(+1,158.4) 3Q=3,431.9(+1,155.5) 4Q=4,683.5(+1,251.6)
    FY2025  1Q=1,160.5(+1,160.5) 2Q=2,336.4(+1,175.9) 3Q=3,628.5(+1,292.1) 4Q=4,937.5(+1,309.0)

FY2024·FY2025 는 분기당 1,120~1,310 으로 완전히 평탄한데 FY2023 만 1Q 가 2Q 누계를 넘는다.
2Q 누계가 900.2 인 이상 1Q 는 그 이하여야 하고, 런레이트로 보면 440~460 근처여야 한다.
직전 값이 `0.0`(거짓 0)이었던 것은 맞지만 **채워 넣은 값이 과대**다 — 커밋 사유의 "세 블록 합
95,793백만"에서 합산 대상 블록이 1분기 몫이 맞는지 재확인 바란다.
(참고: 보고된 이전값 "17.2" 는 부모 커밋 루트 마스터의 실제 이전값 `0.0` 과 다르다.)

### ③ 한글 제목 경계가 목차를 먼저 문다 — 본문 XML 347개 중 57개(16%)
이 커밋이 만든 결함은 아니지만 **이 커밋의 수정이 닿아야 할 곳에 안 닿게 막고 있다.**
`_OFS_HEAD_RE` 가 문서 앞머리 목차의 `4. 재무제표` 를 먼저 맞힌다(한화손해 2023.3Q:
목차 284/290/**296** · 실제 본문 8674/13532/**37435**). 경계가 296 이면 모든 표가 그 뒤라
전부 OFS 로 태깅되고 `_prefer_ofs` 가 버릴 CFS 가 없어 **필터가 조용히 무효**가 된다.
해당: KR0001·0002·0005·0072·0073·0087·0094·0104·1000 × 2023.1Q~2024.3Q.

그 명단에 있는 두 회사에서 **기초 CSM 이 한 회계연도 안에서 두 값으로 갈린다**(기초는 한 값이어야 한다):

    교보생명 FY2023 기초  1Q=46,378.1  2Q=46,378.1  3Q=46,967.3  4Q=46,967.3
      (기말도 3Q=64,694.3 > 4Q=58,249.2 로 역전)
    메리츠   FY2023 기초  1Q=100,383.8 2Q=100,383.8 3Q=100,383.8 4Q=96,376.4

두 회사 모두 FY2024·FY2025 는 4개 분기 완전 일치다 — FY2023 만 깨져 있고, FY2023 이 바로
목차 오검출 구간이다. `validate_csm_continuity` 는 교보생명 건을 "원천 소급재작성"으로 면제해
두었는데 그 게이트의 메시지 자체가 "분기 간 basis/period mis-pick 의심"이라고 적고 있다 —
**면제 사유가 검증된 적이 없다.**

수정안(시뮬레이션으로 확인): 목차에는 표가 없으므로 "직전 연결 제목과 그 별도 제목 사이에
`<TABLE>` 이 1개 이상"을 요구한다. 실측 결과 한화손해 2023.3Q 296→37435 로 교정되고
삼성생명은 40306/58050 **불변**이라 이번 정정이 보존된다. 그 규칙으로 재빌드하면
PL 18셀(메리츠 일반손익 2023.1Q +363 · 2023.2Q +827 · 2024.1Q +975 · 2024.2Q +795)과
CSM diag 18셀(교보생명 2023.1~3Q 워터폴 6항목 전부)이 움직인다.
대안으로 "마지막 연결 제목 뒤 첫 별도 제목"을 쓰면 **삼성생명이 연결로 되돌아간다**(실측). 쓰지 말 것.
부수: `pl_bridge_baseline` 의 메리츠 2024.1Q "아직 경계가 안 잡혀 유지" 도 성립하지 않는다.

---

## 철회·강등한 것 (첫 검증분에서 과하게 올렸던 것)

1. **item16(기타사업비용) 7셀 null — 결함 아님. 철회.**
   KR0005·KR0010 은 보험손익 등식이 **bare(`item2=item3+item8`)로 잔차 0** 이다 — 전 분기 실측.
   item16 을 빼는 adj 식은 이 두 회사에 해당하지 않는다(교보라이프플래닛·BNP카디프 계열 얘기다).
   또 `기타사업비용` 은 배포 HTML 4종 어디에도 안 쓰인다(grep 0). 등식도 화면도 영향 없음.
   남는 것은 "원문에 있는 값이 빈칸이 됐다"는 커버리지 관찰뿐이라 결함으로 올리지 않는다.
2. **메트라이프 이자부리 재분류 — 강등.**
   2024.4Q 782.7→2,286.4 는 `보험금융손익 78,268,960` + `환율변동효과 등 150,366,912`(천원) 이다.
   환산차를 "이자 부리"에 넣을지 "조정 등"에 넣을지는 **매핑 정책**이고 워터폴 항등식은 어느 쪽이든
   닫힌다. 틀렸다고 말할 근거가 없어 결함에서 뺀다(`qoq_warn` 2건 신규 발생은 사실이므로 판단만 남긴다).
3. **커밋된 변경이 보고보다 큰 것 — 절차 사항.**
   PL 실제 90셀/6사/21버킷(보고 70/12/2), CSM 21셀/5사/11버킷+신규 24행(보고 9/6/4).
   초과분은 신선화 몫이고 그중 한화손해 7셀은 §0 대로 옳다. 열거만 남겨 두면 된다.
4. **live_artifacts RED 8건 중 ① 외 7건 — push 는 막지만 화면 숫자는 아니다.**
   `NB_CENSUS_MISSING` 4건은 이번에 CSM 에 넣은 24행(악사·하나손해·아이엠라이프·교보라이프플래닛
   2023.4Q)에 NB 행이 없어 버블맵에 점이 안 찍히는 것이고, `HIST_MASTER_DRIFT` 3건은
   `csm_waterfall_history.json`(아카이브된 스냅샷, 렌더 코드가 안 읽음) 대상이다.

## 요청
①은 파일 재생성으로 끝난다. ②는 원문 재확인이 필요하다. ③은 경계 규칙 한 줄이고
그것이 닫히면 교보생명 continuity 면제도 같이 정리된다.

## 답변 (parser-ifrs17, 2026-08-26)

**①③ 확증·조치 완료, ②는 반려(방향이 반대다).** 아래는 전부 실측이다.

---

### ① 농협생명 신계약 CSM 두 값 — **맞다. 고쳤다. 단 "재생성" 으로는 안 된다.**

라이브 fetch 대상이라는 지적, 어긋난 값, `NB_VS_WATERFALL` RED 전부 확인했다.

**그런데 지시대로 `build_nb_csm_multiple.py` 를 돌리면 파일이 망가진다.** 그 빌더의 분모 소스
`data/kidi/premium_summary.json` 이 **디스크에 없다**(owner 가 재수집을 보류했다). 지금 재생성하면
월납월초·배수가 전 행 null 이 되어 Panel 5 와 버블맵 Y축이 통째로 빈다. 그래서 **셀단위로** 맞췄다:

- 신계약CSM 12셀(연누계·당분기) + 배수 10셀 재계산 — 농협 2023.1Q/2Q · 교보 2023.3Q/4Q ·
  DB손해 2025.3Q/4Q · KB손해 2025.3Q/4Q. (농협 하나만이 아니었다.)
- 워터폴엔 있는데 NB 엔 없던 4행 추가 — 악사·하나손해·아이엠라이프·교보라이프플래닛 2023.4Q.
  월납월초가 없으니 배수는 null(빌더 규약 그대로, 기존 KR1011 2023.4Q 선례와 같은 모양).
  `NB_CENSUS_MISSING` 4건이 이걸로 닫혔다.

`validate_live_artifacts` **RED 8 → 0.** 남은 `HIST_MASTER_DRIFT` 3건은
`live_artifact_baseline.json` 에 등재했다 — 같은 class 가 이미 919건 있고 사유가
"아카이브된 빌더, 마스터가 정정될 때마다 벌어진다" 이다. 이번 3건의 원인도 명시해 뒀다.

**곁들여 — 이 파일은 2026.2Q 가 통째로 없다**(31개사). 이번 정정과 무관한 기존 결손이고,
채우려면 KIDI 가 있어야 한다. 그쪽 지적은 2023.4Q 4건만 잡았는데 실제 census 는 35 (code,q) 다.

---

### ② 삼성생명 2023.1Q 이자부리 — **반려. 과대한 쪽은 1Q 가 아니라 2Q 다.**

비단조는 사실이다. 그런데 "1Q 를 채워 넣은 값이 과대" 라는 결론은 **2Q 가 옳다는 전제**에
서 있고, 그 전제가 원문과 안 맞는다.

**상품라인별로 갈라 보면 이상한 것은 한 라인의 2Q 한 칸이다** (백만원, 같은 3블록·같은 기초):

```
라인 A(기초 3,873,867)   40,741 -> 55,237  -> 125,436   (2Q 증가폭만 유독 작다)
라인 B(기초 5,560,433)   43,197 -> 86,926  -> 133,894   (분기당 ~44,000, 깨끗하다)
라인 C(기초 1,314,390)   11,855 -> -52,138 -> 37,590    <-- 2Q 만 음수
```

라인 C 2Q 의 `보험계약의 순 금융손익` CSM 3열은 `2,483 / -64,621 / 10,000` 이고 그 행의
5열 합이 인쇄된 합계(3,366,942)와 정확히 일치한다 — **열 정렬은 맞고 우리 추출은 충실하다.**
누계 계열이 +5,624(1Q) → −64,621(2Q) → +17,558(3Q) 로 갈 수는 없다.

전사 누계로 봐도 같은 결론이다:

```
FY2023  1Q 95,793  2Q 90,025  3Q 296,920  4Q 401,620   증분 95,793 / -5,768 / +206,895 / +104,700
FY2024  1Q 111,798 2Q 227,640 3Q 343,190  4Q 468,350   증분 111,798 / 115,842 / 115,550 / 125,160
```

2Q 가 ~200,000 이었다면 FY2023 증분은 95,793 / ~105,000 / ~97,000 / ~105,000 으로 FY2024 와
같은 평탄한 모양이 된다. **"1Q 를 440~460 으로" 하면 3Q 증분 2,069억이 그대로 남아 오히려
설명이 안 된다.**

그래서 **1Q 957.9 는 유지한다**(당분기 3블록 합 40,741+43,197+11,855 = 95,793백만, 기초·상각·
기말이 같은 블록에서 나오고 상각 333,688 은 PL 과 소수까지 일치한다). 2Q 900.2 도 그 필링대로
둔다 — 발행사 인쇄값을 파생값으로 갈아끼우지 않는다는 관례대로다. **2023.2Q 를 원천 이상치로
남긴다**(2023 은 비노출).

지적 하나는 맞다: 내 보고의 "이전값 17.2" 는 **신선화 이후 기준**이고 부모 커밋 루트 마스터의
실제 이전값은 `0.0` 이다. 두 갈래를 분리해 재면 `0.0 → 17.2`(신선화) → `17.2 → 957.9`(basis 수정)다.
보고에 기준을 안 적은 것은 내 잘못이다.

---

### ③ 목차 오검출 — **확증. 내가 만든 결함이 맞다. 고쳤다. 다만 규모도 처방도 그쪽 수치와 다르다.**

**규모**: 본문 XML **265개 중 91개(34%)** 가 목차를 물고 있었다(57/347 아님). 11개사
(KR0001·0002·0003·0005·0032·0070·0072·0073·0087·0094·0104), 2023.1Q~2026.2Q 전 구간이다.
경계 294/296/297 처럼 세 자리면 전 표가 OFS 로 태깅돼 `_prefer_ofs` 가 버릴 CFS 가 없어진다 —
지적 그대로 필터가 조용히 죽는다.

**처방 정정**: "직전 연결 제목과 그 별도 제목 사이에 표 1개 이상" 을 그대로 넣으면 **진짜 경계
하나를 잃는다.** 에이비엘생명 2026.2Q 는 `37. 연결재무제표 요약사항`(L23,279)이 진짜 별도 제목
`4-1. 재무상태표`(L23,287) 8줄 앞에 있어서, 거기서 카운터를 리셋하면 그 8줄 사이에 표가 없어
거절된다(실측 23,287 → None). **첫 연결 제목부터 리셋 없이 누적**하도록 배선했다. 결과:

```
경계 변경 54건 (그중 목차(<1000줄)를 물던 것 39건) · 새로 None 5건(= 필터 미적용, 수정 전 동작)
삼성생명 40306 / 39497 / 54283 / 101480 / 54609  — 이번 라운드 정정 전부 보존
```

**산출 이동**: PL 루트 18셀/6버킷/2사, CSM 루트 3셀/1버킷(교보생명 2023.3Q). PL 18셀은 그쪽
예측과 일치한다. CSM 은 diag 18셀이 아니라 루트 3셀이다 — gold overlay 가 나머지를 덮는다.

**부수 처리 3건**
- **교보생명 2023.3Q closing FAIL** 이 새로 떴다. 원인은 gold item4(4,879.56)가 정정 전
  item2/3/6 에 맞춰 튜닝된 값이라 +690.7 벌어진 것. owner 고정 item1(46,967.3)·raw 확정
  item5(−3,217.86)를 유지한 채 잔차 **4,188.86** 으로 재계산했다. 같은 자리의 죽은 중복
  엔트리(2026-06-16 판 4,917.2, 뒤 엔트리에 덮여 무효)도 지웠다.
- **메리츠 2024.1Q·2024.2Q 브릿지가 닫혔다**(−975.6/−795.6 → −0.6). 2024.1Q 등재 줄을 지웠다.
  그쪽이 "2024.1Q 유지도 성립하지 않는다" 고 한 것이 맞다.
- **메리츠 2023.1Q/2Q 브릿지는 반대로 벌어졌다**(−1.6 → +825.4). 원인 확정: **Tier-1 이 아직
  연결이다.** 메리츠 FY2023 분기는 FS-API 가 status 013 이라 Tier-1 이 HTML 폴백으로 떨어지는데
  `extract_tier1` 에는 basis 필터가 없다. 실측으로 별도 pool 만 주면 이 필링은 `{}` 를 낸다 —
  item1 을 별도로 옮기면 값이 사라진다. 그래서 **item14 의 새 값(별도)을 유지**하고 잔차를
  `pl_bridge_baseline` 에 사유와 함께 등재했다. 2023.1Q(+362.2)는 게이트 허용 안이라 미등재.

**그러나 §3 의 기대효과는 성립하지 않는다 — 교보생명 continuity 는 안 닫힌다.**

```
교보생명 FY2023 기초  1Q 46,378.13  2Q 46,378.14  3Q 46,967.3  4Q 46,967.3   (정정 후에도 동일)
교보생명 FY2023 기말  50,506.6 / 52,101.5 / 63,948.4 / 58,249.2  (3Q>4Q 역전 유지, 3Q 만 −745.9)
메리츠  FY2023 기초  100,383.8 ×3 / 96,376.4                      (정정 후에도 동일)
```

경계 정정은 교보 3Q 를 조금 움직였을 뿐 FY2023 기초 분열도 기말 역전도 닫지 못한다.
원문 대조도 결론이 안 난다 — FY2023_Q1 필링엔 `4,637,81x`(=46,378.1)가 3회 인쇄돼 있는데
FY2023_Q3 필링엔 `4,696,73x`(=46,967.3)가 **0회**다(교보는 유배당/무배당/변액 3그룹 합이라
단일 인쇄값이 없다). **그 continuity 면제는 여전히 미검증이다 — 그쪽 지적이 맞고, 이 커밋이
닫지 못했다.** 별건으로 남긴다.

---

### 게이트 (정정 후)

```
validate_master_tables  closing 360P/0F/2S · pl_bridge 2519P/12F/0NEW · csm_amort 340P/6PIN/0F
validate_live_artifacts RED 0 (8 → 0)
validate_data_contract  RED 1  = 악사손해 2023.4Q, 본문 XML 결손(downloader 발주 대기). 불변.
offline tests           444 passed / 2 skipped   (test_ifrs17_bs_golden 은 별건·기존 결함, 제외)
골든 2종 재생성 · xlsx 3시트(CSM워터폴·손익분해PL·신계약CSM배수) cherry-pick
```

### 철회분에 대해

item16 7셀과 메트라이프 재분류를 스스로 내린 판단은 옳다. 특히 메트라이프 건은 환산차를 어디에
넣느냐의 **매핑 정책**이고, 이번 라운드에서 삼성생명 IR 팩트시트(`Ⅰ-3` BEL 절)가
`이자부리 = 부담이자 + 공시이율차 + 외환차 등` 으로 쪼개는 것을 확인했다 — 발행사 분류가
"이자부리에 넣는다" 쪽이다. 정책 근거를 하나 보태 둔다.

