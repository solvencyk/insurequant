---
from: orchestrator
to: parser
created: 20260922T1200Z
status: open
route: defect
company: KR0080
period: 2024.4Q
lane: kics
iter: 1
---

## 미결 (sender 작성)

**kics_disclosure.json 항목46(3-1-5. 금리위험 순자산가치(금리경사)) 이 억원이 아니라 백만원으로
들어가 있다 — AIA생명 2024.4Q 1칸.** 100배 과대.

### 1. 무엇이 틀렸나

| | 값 |
|---|---:|
| 마스터 item46 (현재) | `3607646.0` |
| 원문(백만원) | `17,729,547 − 14,121,901 = 3,607,646` 백만원 |
| 억원 환산 정답 | **`36076.46`** |

원문 = `data/disclosure/FY2025_Q2/raw/KR0080_에이아이에이생명보험.pdf` **p25** 의
`직전반기(2024.4Q)` [금리위험액 현황] 표. 같은 표의 Ⅰ.자산총계 / Ⅱ.부채총계 금리경사 열.
(2024.4Q 자기 보고서는 475쪽 번들이라 표가 이미지로 박혀 있어 다음 반기 보고서의
`직전반기` 열로 대조했다.)

같은 셀의 나머지 5개 시나리오는 정상이다 — 자산−부채로 재계산해 마스터와 대조한 결과
항목41 `34994.80` / 42 `35007.07` / 43 `31579.25` / 44 `38349.68` / 45 `33881.90` 전부 일치,
46만 어긋난다. 270개(회사×짝수분기) 전수 대조에서 100배 이탈은 이 1건뿐이다.

### 2. 왜 게이트가 못 잡았나 (중요)

`36_irr` 룰은 항목41~46에서 금리위험액을 역산해 item36과 대조한다. 그 식의 경사 항은
`R_steep = max(base − steep, 0)` 인데,

- 틀린 값: `max(34,994.8 − 3,607,646, 0) = 0`
- 맞는 값: `max(34,994.8 − 36,076.46, 0) = 0`

**둘 다 0이라 룰이 구조적으로 이 오류를 볼 수 없다.** 단위 오류가 `max(·,0)` 뒤로 숨는
자리다. RED=0 이었던 이유가 이것이고, 값을 고쳐도 36_irr 결과는 안 바뀐다.

### 3. 발주

1. `kics_disclosure.json` KR0080 / 2024.4Q / 항목46 의 `값` 을 `36076.46` 으로 셀 단위 수정
   (통째 read-modify-write 금지). `값_적용후` 가 있으면 같이.
2. 게이트 보강 제안 — **판단은 parser/validation 쪽에 맡긴다.** 지금 있는 자료로는
   `자산총계 − 부채총계 == 항목41~46` 이 유일하게 이 오류를 잡는 등식이고, 그 입력이
   2026-09-22 신설된 `kics_duration_gap.json` / `data/_derived/kics_irr_balance.json`
   에 270칸 전부 들어 있다. plausibility 룰(형제 시나리오 대비 |배율| ≥ 10)로도 잡힌다.
   어느 쪽을 어디에 배선할지는 룰 소유자가 정해 달라.

### 4. 참고 — 같은 대조에서 나온 '발행사 표 불일치' 2건 (수정 대상 아님)

`data/_derived/kics_duration_gap_audit.json` 에 9건이 잡히는데 위 1건을 뺀 8건은
발행사 표가 스스로 안 맞는 경우다. 공시대로 두는 게 맞다고 보고 마스터는 안 건드렸다.

- **비엔피파리바카디프생명 2023.2Q** — 6개 시나리오 전부 자산−부채가 공시 순자산가치보다
  약 0.74% 작다(원문 p22 확인, 추출 오류 아님).
- **DB생명 2025.2Q** — 금리하락·금리경사 2개 열만 안 맞는다(원문 p26 확인).

`kics_duration_gap.json` 의 해당 행 `비고` 에 박제해 뒀다.
