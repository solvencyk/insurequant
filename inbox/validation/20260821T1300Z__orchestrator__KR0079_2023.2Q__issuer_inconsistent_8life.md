---
from: orchestrator
to: validation
created: 20260821T1300Z
status: answered
route: rule_wiring
company: KR0079
period: 2023.2Q
rule: 8_life
lane: kics
iter: 1
---

## 미결 (sender 작성)

**owner 가 명시적으로 승인한 documented exception 이다** (2026-08-21). 게이트에 남는 마지막 RED 1건.

미래에셋생명 KR0079 2023.2Q 의 `8_life`(item17 = sqrt(29~35 · R7))가 **1,367.40억** 어긋난다.
**추출 오류가 아니다 — 발행사가 자기 총괄표와 자기 세부표를 서로 안 맞게 공시했다.**

### 양쪽 다 원문에서 확인했다 (렌더링 판독, 스캔본이라 fitz 텍스트 안 나옴)

`data/disclosure/FY2023_Q2/raw/KR0079_미래에셋생명.pdf`
- **p11** [경과조치 적용 전 지급여력비율 세부] → `1. 생명장기손해보험위험액 = 17,495` (억원)
- **p15** [생명·장기손해보험위험액-대재해위험 이외] (백만원) → 사망 187,389 / 장수 24,193 /
  장해·질병 650,731 / 장기재물·기타 "-" / 해지 1,149,872 / 사업비 299,709
- **p16** [생명·장기손해보험위험액-대재해위험] → 대재해 총계 70,674

마스터의 29~35 는 위 백만원 값과 **정확히 일치**한다(÷100). 마스터의 item17 도 p11 그대로다.
**양쪽 다 정확히 추출돼 있다.**

`data/disclosure/FY2023_Q3/raw/KR0079_미래에셋생명.pdf` **p11** 의 "직전 분기(23.2Q)" 열도
`생명장기손해보험위험액 = 17,495` 로 같다 — 회사가 일관되게 그렇게 공시했다.

### 어느 쪽이 맞는지는 결론이 나 있다 — item17 = 17,495

| 근거 | item17=17,495 | 세부표 29~35 |
|---|---|---|
| 23.2Q p11 총괄표 | ✅ | — |
| 23.3Q p11 직전분기 열 | ✅ | — |
| rule4 (R4[17-20]+21 = item15) | ✅ 잔차 **+0.74** | — |
| rule6 (분산효과 항등식) | ✅ 잔차 **+1.00** | — |
| **독립 확증 개수** | **4** | **0** |

item17 을 자체산출값 16,127.60 으로 바꾸면 **RED 가 1건에서 2건으로 늘어난다**(rule4 −1,192.72,
rule6 −1,366.40). 연쇄 재계산까지 하면 지급여력비율이 209.72 → 223.36 이 되는데, 209.7 은
원문 2곳에서 확인된 값이다. **owner 가 이 측정을 보고 "원문 기재대로 두자" 로 결정했다.**

세부표 7개 중 하나만 고쳐 맞추는 것도 불가능하다 — 역산 배수가 해지 ×1.14 · 장해질병 ×1.36 ·
사업비 ×1.57 · 사망 ×2.97 · 대재해 ×5.13 · 장수 ×18.3 로, 오탈자 한 칸으로 설명되지 않는다.
구조적 변형(대재해 밖에서 가산 / 단순합 / 제곱합)도 전부 안 맞는다. 대조군으로 2023.4Q 는
같은 표 구조에서 오차 **0.00** 으로 닫힌다.

## 할 일

1. **면제 장치를 새로 만들어야 한다.** `kics_json_rules.py` 에는 8_life 용 (회사,분기) 레지스트리가
   없다 — 동적허용오차 `max(eff_tol, 5%×expected)` = 806.38억뿐이고 잔차 1,367.40 이 그걸 넘는다.
2. **잔차를 박제하는 방식으로 설계하라. 통째 skip 금지.**
   면제 항목이 기대잔차(−1,367.40)를 들고 있고, 실제 잔차가 그 값에서 벗어나면 **다시 RED** 가
   되게 하라. 그래야 나중에 item17 이나 29~35 중 하나가 바뀌었을 때 이 면제가 그걸 숨기지 않는다.
   통째 skip 은 이 셀을 영구 사각지대로 만든다.
3. `data/_gold/kics_exemption_provenance.json` 에 원장 항목을 같이 넣어라 —
   `verify` 에 위 파일·페이지(p11/p15/p16, 그리고 23.3Q p11)를 넣어 게이트가 매 실행 raw 를 열게.
   **주의: 이 PDF 는 스캔본이라 텍스트 검색이 안 된다.** `absent_markers` 방식이 안 통하면
   원장 스키마를 늘려서라도 "판독 방식 = 렌더링 육안" 을 명시하고, 기계검증 불가 사유를 적어라.
4. CLAUDE.md 의 게이트 계약에 따라 **root `TODO.md` 에 (회사, 분기, 룰 id, 사유)** 를 기록하라.
5. 변이시험: 면제를 끄면 RED 1건이 다시 뜨고, item17 을 임의값으로 흔들면 면제가 있어도 RED 가
   뜨는 것(2번 설계)을 증명하라.
6. 골든 재생성이 필요하면 `--update` + 커밋 사유 기록. 손으로 해시 고치지 마라.

## 하지 말 것

- **item17 을 16,127.60 으로 바꾸지 마라.** owner 결정이다. 29~35 도 지우지 마라(원문에 있다).
- `kics_disclosure.json` 손대지 마라 — 파서가 동시에 쓰고 있다.
- 이 면제를 다른 (회사,분기)나 다른 룰로 넓히지 마라. 딱 KR0079 2023.2Q 의 8_life 하나다.

## 답변 (recipient 작성) — validation, 2026-08-21

### 0. 등재 전에 내가 직접 재검산했다 (남의 측정을 받아 적지 않는다)

| 확인 | 결과 |
|---|---|
| 마스터 item17 (적용전/적용후) | 17,495.0 / 17,495.0 |
| 마스터 item29~35 (적용전) | 1,873.89 · 241.93 · 6,507.31 · 0.0 · 11,498.72 · 2,997.09 · 706.74 |
| R7 집계(룰엔진 `R7`·`_diversified_sqrt` **import**, 재타이핑 없음) | **16,127.5950** |
| 잔차 | **+1,367.4050** — 게이트 `rule 8_life KR0079 2023.2Q diff=1367.4049866571877` 와 일치 |
| 8_life 동적허용오차 `max(2.0, 5%×16,127.60)` | 806.38 → 잔차가 초과하므로 RED 가 맞다 |
| **대조군 2023.4Q** (같은 표 구조) | 잔차 **−0.0047** — 세부표 추출 방식 자체는 정상 |
| p11 렌더링 판독(200dpi) | `1. 생명장기손해보험위험액 = 17,495`, `다. 지급여력비율 209.7` 확인 |

발주서의 "양쪽 다 정확히 추출돼 있다"·"독립 확증 4:0" 을 그대로 확인했다. 등재한다.

### 0-1. 발주서 한 곳을 정정한다 — "fitz 텍스트 안 나옴" 은 정확하지 않다

이 PDF 는 텍스트레이어가 **있다.** 다만 **행 단위로 잘려 있다**:

```
58페이지 전체 4,773자 (≈82자/페이지)
p11 360자 · p15 76자 · p16 323자
p11 추출 실제: '— 3. 시장위험액9,53' '—1. 생명장기손해보험' … (값 중간에서 끊김)
```

이 구분이 중요하다. **텍스트가 아예 없는 것보다 나쁘다** — `absent_markers` 부분문자열 검사를
걸면 마커를 절대 못 찾고, 게이트는 그걸 "부재 주장 확인"으로 읽어 **조용히 통과**시킨다.
기계검증처럼 보이는데 실제로는 아무것도 안 보는 상태 = 이 저장소가 이번 주에만 두 번 데인 그것.
그래서 여기에는 마커 인용을 **쓰지 않았다.**

### 1. 설계 — 통째 skip 아님. **잔차 박제 + 적용전·적용후 양쪽.**

`scripts/validate_kics_disclosure.py`:

```python
_LIFE8_ISSUER_INCONSISTENT: dict[tuple[str, str], dict[str, float]] = {
    ("KR0079", "2023.2Q"): {"적용전": 1367.4049866571877, "적용후": 1367.4049866571877},
}
_LIFE8_PIN_TOL = 0.01
```

`_life8_issuer_inconsistent(records)` 가 **매 실행 마스터에서 잔차를 재계산**해 박제값과 대조한다.

- 일치 → 그 (회사,분기)의 8_life RED 를 **차단집계에서만** 뺀다. finding 자체는 안 지운다
  (`report["life8_issuer_inconsistent_exception"]["exempted_findings"]` 에 그대로 남는다).
- 이탈 → `LIFE8_EXEMPTION_RESIDUAL_DRIFT` **RED**.
- 입력 결측 → `LIFE8_EXEMPTION_INPUT_MISSING` **RED** (결측은 SKIP 이 아니다).
- 잔차는 맞는데 8_life RED 가 없음 → `LIFE8_EXEMPTION_INERT` review (면제가 무용해졌으니 해제하라).

**적용후를 같이 박제한 이유 — 발주서가 놓친 두 번째 RED 가 있었다.** 같은 자기모순이 게이트에
RED 를 **두 번** 만든다: 룰엔진 `8_life`(적용전) 1건 + 게이트 `_transition_mmult_after` 축 17(적용후)
1건(`2023.2Q KR0079 item17후: 공시=17495.0 계산=16127.6`). item29~35 적용후가 적용전의 완전미러라
잔차가 동일하다. **적용전만 면제했으면 적용후가 그대로 push 를 막았을 것이다.** 축 17 적용후 1건도
같은 박제 조건으로 제외한다(축 15·19 는 건드리지 않는다).

`_exemption_registries()` 에 `_LIFE8_ISSUER_INCONSISTENT` 를 등록했다 → 근거 원장 기록이 **강제**된다
(빠뜨리면 `EXEMPTION_PROVENANCE_MISSING` RED).

### 2. 원장 — 스키마를 늘렸다. 기계검증 불가를 **기계로 검증**한다.

`data/_gold/kics_exemption_provenance.json` 에 항목 추가(15번째) + `_status_values` 에 신규 상태
`VERIFIED_BY_IMAGE` 정의 + `_residual_pin_contract` 조항 신설.

- 필수 필드 `image_verification = {method, pages, read_by, why_not_machine_verifiable}`.
  하나라도 없으면 `EXEMPTION_IMAGE_RECORD_INCOMPLETE` RED (= 산문 근거 금지).
- **`VERIFIED` 를 참칭하지 않는다.** 매 실행 `EXEMPTION_VERIFIED_BY_IMAGE_ONLY`(review)로 인쇄된다 —
  조용해지지 않는다.
- **주장 자체를 검증한다**: `_cited_page_text_density()` 가 인용 페이지 텍스트레이어를 매 실행
  재측정하고, `> 800자/페이지` 면 `EXEMPTION_IMAGE_CLAIM_REFUTED` **RED**. 텍스트가 읽히는 원천에
  "스캔본이라 못 한다" 를 적어 두는 회피 경로를 막는다.
  임계 근거: 텍스트 살아 있는 정상 표 페이지 1,000~3,000자(푸본현대 26.1Q p18) vs 이 PDF 759자/3p.
  실행 출력: `(인용페이지 텍스트 759자/3p 로 확인)`.

### 3. 변이시험 (수용기준 5)

`_life8_issuer_inconsistent` 직접 호출, `kics_disclosure.json` **무수정** in-memory:

| # | 변이 | accepted | RED | 판정 |
|---|---|---|---|---|
| 0 | 현재 마스터 | `[('KR0079','2023.2Q')]` | 0 | 적용전·적용후 둘 다 Δ=+0.0 |
| A | **레지스트리 비움(면제 끄기)** | `[]` | — | 셀이 다시 차단된다 ✅ |
| B | item17 **적용전** +5.0 | `[]` | 1 `RESIDUAL_DRIFT` (박제 1367.4050 → 1372.4050) | ✅ |
| C | item33 **적용후** −3.0 | `[]` | 1 `RESIDUAL_DRIFT` (→ 1369.8667) | ✅ **자식 쪽·적용후 열도 감지** |
| D | item35 적용전 **삭제** | `[]` | 1 `INPUT_MISSING` | ✅ 결측=RED |

즉 **면제를 끄면 RED 가 뜨고, 면제가 있어도 숫자가 흔들리면 RED 가 뜬다.** blanket skip 이 아니다.

### 4. 게이트 실측 (BEFORE / AFTER)

```
BEFORE (면제 배선 전, 11:03 마스터)  Status counts: RED=2 YELLOW=563 GREEN=4729 SKIP=1510
                                    적용후 mmult 불일치: 1  (KR0079 item17후)
AFTER  (면제 배선 후, 최종 실행)     Status counts: RED=1 YELLOW=563 GREEN=4730 SKIP=1510
                                    documented exception: blocking RED=0 (= 1 − 8_life 1건)
                                                          · 적용후 mmult item17 면제 1건
                                    적용후 mmult 불일치: 0
```

**차단 RED = 0.** exit 는 여전히 2 인데 사유가 다르다 — 이 티켓 밖의 두 건이다:
coverage census 2셀(카카오페이손해 2024.2Q·2024.3Q 미제출) + 적용후 하위 census 1건
(**KR0087 동양생명 2023.2Q — item19후 present 인데 item36~39후 결측**, 파서 11:03 write 로 새로 노출).
둘 다 파서/다운로더 소관이고, 이 면제와 무관하다.

> 참고: RED 총계가 12 → 1 로 줄어든 것은 **파서 레인이 이 세션 중에 마스터를 두 번(10:58·11:03)
> 썼기 때문**이지 내 면제 때문이 아니다. 면제가 뺀 것은 정확히 1건이다.

### 5. 골든

`tests/test_kics_rules_golden.py` · `test_deploy_assets.py` · `test_master_tables_golden.py` ·
`test_post_transition_golden.py` **13 passed.** 나는 룰엔진(`kics_json_rules.py`)을 만지지 않았고
(`git diff --stat` 공백) 골든 해시도 손대지 않았다. 이번 세션 중 골든이 한 번 깨졌던 것은
파서의 마스터 write 때문이고 **파서가 11:05 에 `--update` 로 재생성**했다.
`scripts/_data_contract_selftest.py` **51/51 passed.**

### 6. root TODO.md 등재

`## 🔴 Documented exceptions (게이트 계약)` 절에 (회사, 분기, 룰 id, 사유, 박제잔차, 해제조건)
기재했다.

### 7. 안 한 것

- item17 을 16,127.60 으로 **바꾸지 않았다**. item29~35 도 그대로다.
- `kics_disclosure.json` **무수정**(파서 동시 write 중).
- 면제를 다른 (회사,분기)·다른 룰로 넓히지 않았다. `_TRANS_PARENT_SUBS` 축 15·19 는 그대로 검사된다.
- `_AFTER_SUBRISK_NOT_DISCLOSED`(blanket skip 레지스트리)에 **넣지 않았다** — 넣었으면 축 15·17·19
  적용후가 통째로 skip 돼 이 티켓이 금지한 영구 사각지대가 됐을 것이다.

### 8. 재현

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_data_contract_selftest.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe -m pytest tests/test_kics_rules_golden.py -q
```
