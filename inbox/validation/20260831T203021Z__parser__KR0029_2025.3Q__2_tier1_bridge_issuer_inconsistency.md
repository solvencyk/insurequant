---
from: parser
to: validation
created: 20260831T203021Z
status: open
route: escalate
company: KR0029
period: 2025.3Q
rule: 2_tier1_bridge
lane: kics
iter: 1
---

## 미결 (sender 작성)

AIG손해보험(KR0029) 2025.2Q/2025.3Q 11건 RED 해소 발주 처리 중 10건은 raw 재확인으로
닫았으나, `2_tier1_bridge`(2025.3Q, 적용전)는 **원문 자체의 자기모순**으로 보여 값을 고치지
않고 여기로 넘긴다. TODO.md L319 "Tier2/기본자본다리 issuer self-inconsistency" documented
exception 표(계열 ①/②)와 형태가 같은 패턴이라, owner 위임 등재 여부를 판단해 달라.

### 룰이 보는 값 — 전부 원문에 애매함 없이 인쇄됨

raw PDF `data/disclosure/FY2025_Q3/raw/KR0029_AIG손해보험.pdf` p14 (fitz 직접 확인,
docling MD `md_inbox/FY2025_Q3/KR0029_AIG손해보험.md` L313-326과 완전 일치, 컬럼 오분절
없음):

```
가.지급여력금액(기본자본+보완자본)  6,362   <- item1
  기본자본주2)                     6,304   <- item2
  보완자본                            59   <- item3  (item2+item3=6,363, item1=6,362과 1 오차는 반올림 노이즈)
Ⅰ.건전성감독기준 재무상태표 순자산   6,362   <- item4  (자기 7구성행 합과 정확 일치, rule "4"는 GREEN)
Ⅱ.지급여력금액으로 불인정하는 항목       0   <- item12
Ⅲ.보완자본으로 재분류하는 항목           0   <- item13
```

주2) 각주: "기본자본은 순자산에서 지급여력금액 불인정 항목(단, 보완자본 한도를 초과한 금액을
제외) 및 보완자본으로 재분류하는 항목을 차감한 금액" = item2 = item4 − item12 − (한도초과 보정).

`item4 − item12 − item13 = 6,362 − 0 − 0 = 6,362 ≠ item2(6,304)`, 잔차 **−58.0**.

### 왜 "한도초과" 로 설명이 안 되는지 확인함

같은 페이지 `[지급여력비율의 경과조치 적용에 관한 사항]` TFI 표(raw p15, word좌표로 재확인,
docling 이 이 표만 "경과조치" 유령 컬럼을 만들어 3컬럼처럼 보이게 했던 것도 좌표로 우회
확인함):

```
보완자본 한도 적용 전   -        -       <- item47 = 0 (전/후 둘 다 대시)
보완자본 한도          127,776  127,776  <- item48 = 1,277.76
해약환급금...초과분     5,870    5,870    <- item49 = 58.70
보완자본               5,870    5,870    <- item51 = 58.70 (= item3, 표 자체는 정합)
```

룰의 `_tier2_branch`로 분류하면 branch=CAPPED (min(0,1277.76)+58.7=58.7≈item3, 정확) 이지만
`한도초과=max(0, debt−item48)=max(0, 0−1277.76)=0` 이라 축 A(`2_tier1_bridge`)의 보정항이
0이 된다 — item47이 한도(1,277.76)에 전혀 안 걸려 있어(0≪1,277.76) "한도초과" 메커니즘
자체가 이 분기엔 적용 대상이 아니다. 즉 −58 갭은 한도초과 계산 오류가 아니라 **item49(58.7,
해약환급금 부족분 초과분)가 item12/item13 어느 쪽에도 반영 안 된 상태에서 item2가 이미
그만큼 깎여 인쇄된 것**으로 보인다(58.7 ≈ 58.0 갭).

참고로 같은 회사 2026.2Q는 item13(재분류항목)=754가 item49(754.39)와 사실상 같은 값으로
공시돼 다리가 닫힌다(item4 7070−item12 300−item13 754=6016≈item2 6015, 잔차 −1). 즉 이
회사는 **분기마다 item49의 tier1→tier2 재분류를 item13에 반영하기도, 안 하기도 한다** —
2025.3Q는 반영 안 한 케이스로 보인다.

### 시도했으나 채택 안 한 것

- item13을 58.7로 "보정"해 채우는 것 — raw는 명확히 0을 인쇄한다(대시 아님, 숫자 0). 파생값으로
  원문 숫자를 갈아끼우는 것은 이 프로젝트 규칙 위반("issuer-inconsistent keep as disclosed").
- item2/item4/item12를 수정하는 것 — 넷 다 원문에 애매함 없이 직접 인쇄된 값.

### 부탁

TODO.md의 owner-위임 문서화 예외(계열 ①/②) 패턴으로 등재할지, 아니면 다른 판단을 원하는지
확인 바람. 등재 시 `_TIER2_ISSUER_INCONSISTENT` 원장(`data/_gold/kics_exemption_provenance.json`)
스키마는 이미 있는 코드를 그대로 쓰면 될 것으로 보인다(재구현 안 함, validation 소관이라 파서가
직접 건드리지 않았음). 박제 잔차 후보: **−58.0** (branch=CAPPED, scope=EXCL, tol 0.01).

## 답변 (recipient 작성 — 처리 후)
