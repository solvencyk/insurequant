---
from: owner
to: validation
created: 20260715T0801Z
status: resolved
route: blind_spot
company: MULTI
period: 2026.1Q
rule: POST_TRANSITION_PARENT_CENSUS
iter: 1
---

## 미결 (sender 작성)

**게이트 사각:** 2026.1Q push 게이트가 통과했으나, 5개 경과조치 적용사에서 요구자본 세부 **부모 항목의 `값_적용후`가 결측**인 채로 통과함. K-ICS.html 적용후 모드에서 공란으로 노출됨 (상세 = 병행 발주 `inbox/parser/20260715T0801Z__...__post_transition_scr_subtable_gap.md`).

결측: KR0068 한화·KR0073 교보·KR0097 하나·KR0003 롯데손해·KR0104 농협, 항목 17/18/19(및 15/16/20~23 일부) `값_적용후`.

**왜 사각인가:** 산술 등식(적용전 기준)은 다 닫혀 있어 RED=0(false-green). 적용후는 검증 사각이라는 기존 인식(memory: after-capture all rules, 게이트 배선 70df46f)에도 불구, **부모 요구자본 항목 적용후 census가 강제되지 않아** 결측을 못 잡음.

**요청 (게이트 보강):**
1. **적용후 parent census**: 회사가 경과조치 적용사(항목 1~28 중 `값_적용후`가 하나라도 있으면 적용사로 판정)이면, 요구자본 부모 항목 **15,16,17,18,19,20,21,22,23**의 `값_적용후`는 **present 필수**. 결측 시 RED (단, 구조적 미공시로 확정돼 registry 등록된 셀만 documented exception).
2. **cross-quarter continuity 시그널**: 어떤 (회사,항목)이 인접 분기에 `값_적용후`가 있었는데 당 분기 갑자기 결측이면 = 추출갭 시그니처 → RED로 승격(continuity-break-is-RED 원칙 준용). 구조적 미공시로 오면제 금지.
3. NO_POST_TRANSITION_DISCLOSURE(항목 4/12/13, 구조적 적용후 미공시)와 **추출갭 결측을 구분**해서, 전자만 면제·후자는 RED.

**참고:** 세부 하위위험 29~35/36~40의 홀수분기 미공시는 별개(정당). 이 룰은 그 상위 부모 요구자본 항목의 적용후 결측만 대상.

## 답변 (validation 처리 — 2026-07-15)

**게이트 신설 완료. 3요청(부모 census + continuity + 구조적/추출갭 구분) 모두 반영.**

### 신설 룰 `_post_transition_parent_census` (scripts/validate_kics_disclosure.py)
- **적용사 판정**: 별도 seed 불필요 — continuity 시그널 자체가 필터. (요청대로 `_TRANSITION_APPLIERS`
  18사가 아니라 **적용후를 실제 공시하는 전체 회사**를 대상; 그래서 elective 아닌 공통경과조치사 **한화생명
  (KR0068)·삼성생명(KR0069)·코리안리(KR1000)** 등도 포착 — 이들이 기존 검사(18사 하드코딩)에서 빠져 있던
  이유이자 한화생명 결측이 안 잡히던 근본원인.)
- **대상 부모**: 15기본요구자본·16분산효과·17생명장기·18일반손해·19시장·20신용·21운영 = **코어(RED)**,
  22법인세조정·23기타요구자본 = **조정(코어 break 동반 시만 RED, 단독은 review·비차단)**. 22/23은
  종속회사·법인세 유무로 legit-absent가 흔해(단독 토글) hard-RED에서 분리.
- **판정**: (회사,항목) 값_적용후 시계열에서 **직전 공시분기에 적용후 present인데 당 분기 결측**이고
  (이후 재출현=SANDWICHED / 당 분기=최신=TRAILING)이면 RED. 도입초 onset·항구적 중단은 flag 안 함(오탐억제).
- **요청3(구조적 vs 추출갭)**: 항목 4/12/13(구조적 적용후 미공시)은 **census 대상에서 원천 제외**(요구자본
  부모만) → 구조적은 flag 자체가 안 됨. 추출갭만 RED. 면제 registry `_POST_PARENT_NOT_DISCLOSED`=**비어있음**
  (요청대로 "오면제 금지"; exemption 추가는 owner 권한이라 자체 seed 안 함).

### 게이트 배선 = 양쪽
1. `validate_kics_disclosure.py` (K-ICS 게이트, 전분기 리포트+exit2) — parser 워크리스트.
2. `validate_data_contract.py` `check_census` (push 게이트, **display 분기만 차단** — 다른 census와 동일
   scope, git-purge 과거분기 제외). ← **"push 게이트가 통과"의 정정 지점.**

### 현재 상태 (⚠️ provisional — parser가 병행 티켓으로 데이터 채우는 중)
- **2026.1Q 라이브 = 이미 해소됨**: 병행 parser 세션이 지목 5개사(KR0068·73·97·03·104) 2026.1Q 15~23
  값_적용후를 방금 전량 UPSERT(mtime 17:32). 산술 mmult/항등식/분산효과 후 검사 전부 0 통과(fill 정합).
  → **게이트가 갭 있을 땐 잡고, 채워지니 통과**함을 확인(설계대로 작동).
- **push 게이트 census RED=47 (8 회사·분기, 전부 historical display)** = 이번 게이트가 새로 표면화:
  | 회사·분기 | 성격 |
  |---|---|
  | **삼성생명 2025.1Q** [16~21] | 🆕 진짜 추출갭 (매분기 공시하다 1분기만 유실, SANDWICHED) |
  | **흥국생명 2024.4Q** [15~21] | 🆕 진짜 추출갭 (동일 패턴) |
  | **한화생명 2025.2Q·2025.3Q** | 병행 parser 티켓 2차 목록에 이미 있음 |
  | **동양생명 2024.4Q·2025.1Q·2025.4Q** | scan/image PDF 이력 — raw 확인 필요(구조적이면 exemption) |
  | **하나생명 2024.4Q** [16,18~21] | 기존 `_AFTER_SUBRISK_NOT_DISCLOSED`(감사보고서 스타일) — 부모후도 미공시면 exemption |
- **비차단**: 22/23 단독 review 14건(IBK연금 2026.1Q 22/23 등 — 종속회사 legit-absent 가능), non-display
  historical(코리안리 2023~2024·농협 2023.2Q 등).

### 후속
- **삼성생명·흥국생명(신규 발견)** + 나머지 display 갭 = parser 발주 `20260715T0835Z__validation__…__
  post_parent_continuity_worklist.md`. 완료기준: 게이트 `post_transition_parent_census.red` = 0.
- **동양·하나 2024.4Q가 raw상 진짜 부모후 미공시로 확인되면** → owner가 `_POST_PARENT_NOT_DISCLOSED`에
  (회사,분기) 등재(자체 판단 waiver 안 함).
- 상세 이력: TODO_validation.md V18 · changelog 2026-07-15.

### 추가 (2026-07-16) — parser fill 적대검증 완료, push RED 47→4

parser가 워크리스트 처리(삼성생명·동양생명·한화생명·흥국생명 등 fill). 재검증:
- **미러fill(후=전) 정당성 PASS** (V17 가짜복사 재발 아님): 삼성/동양/한화 = 공통경과조치사라 요구자본
  후=전이 정답(가용자본 item1만 2025.2Q 실효과). 무회귀(mmult·항등식·ratio·분산효과 0).
- **push 게이트 census RED 47 → 4.** 잔여 2건 = **owner 결정 필요** (raw 도출불가):
  - **흥국생명 2024.4Q [15,16,22]**: image PDF + TIR/TER 다중경과 R4 재현불가 → **exemption 권장**.
  - **하나생명 2024.4Q [16]**: 비표준(감사보고서) 공시, item16 파생가능하나 입력 item17후 불확실 →
    **exemption 권장**(또는 item16 파생 채움 지시).
- 둘 다 `_POST_PARENT_NOT_DISCLOSED` 등재하면 push 게이트 clear. validation 자체 waiver 안 함(owner 권한).
