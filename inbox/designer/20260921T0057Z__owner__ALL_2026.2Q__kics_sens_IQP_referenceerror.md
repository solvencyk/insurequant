---
from: owner
to: designer
created: 20260921T0057Z
status: answered
route: escalate
company: ALL
period: 2024.4Q-2026.2Q
rule: LIVE_JS_RUNTIME_ERROR
iter: 1
---

## 미결 (sender 작성)

**라이브 www.insurequant.com 의 K-ICS 금리민감도 패널이 39사 중 36사에서 죽어 있다.**
owner 신고: "라이나생명 2026.2Q 금리민감도 데이터가 있는데 사이트에는 미공시라고 나온다."

### 실측 (오케스트레이터, 2026-09-21)

라이브 페이지에서 직접 재현했다.

```
https://www.insurequant.com/K-ICS.html?iq_internal=1&company=라이나생명보험
renderSensDetail() → ReferenceError: IQP is not defined   (K-ICS.html:1368)
```

- `K-ICS.html:1368-1369` 이 `IQP()` 를 부르는데 **그 함수가 파일 어디에도 없다.**
  `git log -S "function IQP"` → `392e8ec`(팔레트 B, origin/main 은 `17e3733`)가
  `function IQP(){ return (window.IQTheme && IQTheme.chart().primary) || '#0f6e68'; }`
  를 넣었고, **`2dbc4ca`("IFRS17 섹션 네비 통일 + 헤더 실측 sticky + 메타발언 삭제",
  2026-09-20)가 정의만 지우고 호출 2곳을 남겼다.**
- 데이터는 멀쩡하다. `kics_rate_sensitivity.json` 은 루트·`origin/main` 둘 다
  KR0074 2026.2Q 6행(적용전/적용후 × 3 measure) 전부 non-null 이다.
- 터지는 지점이 `datasets.push({borderColor: IQP()})` 안이라 **`적용후` 행이 있는 회사만**
  터진다(36사). `적용전` 만 있는 3사는 `'#6c757d'` 리터럴이라 멀쩡히 그려진다.
- 증상이 "미공시" 로 보이는 이유: 예외가 `renderDurationCards()` **뒤**,
  `sens-placeholder` 를 숨기는 줄 **앞**에서 터진다. 그래서
  듀레이션 카드(2.08년/△45)는 그려지고, placeholder 는 직전 회사에서 세팅된
  "…금리 민감도 공시 항목이 아직 없습니다" 문구를 그대로 달고 남는다.
  차트·KPI·표는 영원히 `display:none`.
- 브랜치 `fix/csm-product-segmented-columns` 의 `K-ICS.html` 은 `origin/main` 과
  **바이트 동일**이다. 한 번 고치면 둘 다 덮는다.

### 시킬 일

1. `IQP` 복구. `theme.js` 는 `origin/main` 에도 있으니 `392e8ec` 가 넣었던 정의를
   그대로 되살리거나, 호출 2곳을 `(window.IQTheme && IQTheme.chart().primary) || '#0f6e68'`
   로 인라인한다. **팔레트 B 색을 바꾸지 말 것** — 복구지 재디자인이 아니다.
2. **placeholder 가 성공 경로에서 반드시 초기화되도록** 고친다. 지금은 예외 한 번에
   "미공시" 문구가 다음 회사까지 따라다닌다. 렌더 진입 시 문구를 리셋하든,
   `renderSensDetail` 를 try/finally 로 감싸든 designer 판단.
3. 배포 4종(`index.html`·`K-ICS.html`·`IFRS17.html`·`공시보고서.html`) 전체에
   **같은 유형(호출은 있는데 정의가 없는 식별자)** 이 더 없는지 훑는다.
   오케스트레이터의 1차 스캔(`<script>` 밖 CSS `var()`·`rgba()` 까지 긁어서 노이즈 많음)
   에서 진짜로 보이는 건 `IQP` 하나였지만, 그 스캔은 게이트가 아니다.
4. 검증은 **실제 런타임으로**. 로컬 서빙 + 브라우저에서 39사를 순회하며
   `renderRateSensitivity()` 가 예외 없이 끝나고 `sens-chart-wrap`·`sens-table` 이
   보이는지 확인한다. 스크린샷 한 장(라이나생명 2026.2Q)을 산출로 남긴다.
   라이브/로컬 디버깅은 항상 `?iq_internal=1` 로 진입(GA4 내부트래픽).
5. `TODO_designer.md` 맨 위 갱신 + `docs/changelog_designer.md` 기록.

배포(main cherry-push)는 하지 말 것. owner 승인 후 publishing 이 한다.

## 답변 (recipient 작성 — 처리 후)

**원인은 진단 그대로다 — `2dbc4ca`가 섹션 네비를 `theme.js`로 옮기면서 그 주변에 있던
`IQP()` 정의를 같이 지웠고, `K-ICS.html:1368-1369`의 호출 2곳은 남았다.**

### 1) `IQP` 복구

`K-ICS.html:273`에 원래 정의를 그대로 되살렸다(`392e8ec`가 넣었던 문자열과 100% 동일 —
`git show 392e8ec -- K-ICS.html`로 대조):

```js
function IQP(){ return (window.IQTheme && IQTheme.chart().primary) || '#0f6e68'; }
```

위치도 원래 자리(`jsonPath` 선언 바로 앞, 옛 섹션-네비 주석 뒤)로 되돌렸다 — 재디자인이
아니라 복구라서 팔레트 B 색·호출부(`K-ICS.html:1385-1386`, 옛 1368-1369에서
`renderSensDetail`/`renderSensDetailInner` 분리로 16줄 밀림)는 손대지 않았다.

### 2) placeholder 스트랜딩 방지

`renderSensDetail`(`K-ICS.html:1351`)을 얇은 래퍼로 바꾸고 실제 본문은
`renderSensDetailInner`(`K-ICS.html:1362`)로 옮겼다. 래퍼가 try/catch로 감싸
어떤 예외든 `console.error` 로깅 + `sensHideAll('금리 민감도 패널을 불러오는 중
오류가 발생했습니다.')` 로 상태를 강제 리셋한다 — 이번 버그류(호출은 있는데 정의가
없는 식별자) 전체에 대한 안전망이라, 다음에 또 같은 실수가 나도 "직전 회사의
placeholder 문구가 눌러붙는" 증상 자체가 재발하지 않는다. `sensHideAll`은 차트
destroy·KPI·표·듀레이션 카드까지 전부 숨기므로 half-render가 안 남는다.

### 3) 배포 4종 스윕

CSS `var()`/`rgba()`/한글 주석 노이즈를 걷어낸 정적 스캐너
(`scripts/_probes/_20260921_scan_undefined_calls.py` — 문자열/주석/템플릿리터럴을
제거한 뒤 `식별자(` 호출을 모으고, `function`/`const`/`let`/`var`/destructuring/
함수 파라미터/arrow 파라미터/`catch`/`for...of`/`class`로 정의된 이름과 알려진
전역(Chart/echarts/IQTheme/DOM API 등) 목록을 빼는 방식)으로 4페이지 전부 훑었다.

- `index.html` — 0건
- `K-ICS.html` — 수정 전 `IQP` 1건(기대대로), 수정 후 0건
- `IFRS17.html` — `formatter` 2건 → 확인해 보니 `formatter(items){...}`(툴팁 콜백)
  객체 리터럴 shorthand 메서드 정의라 오탐. 실제 정의 누락 아님
- `공시보고서.html` — 0건
- `K-ICS.html`의 `afterDraw` 1건도 같은 이유(플러그인 훅 shorthand 메서드)의 오탐

결론: **진짜 결함은 `IQP` 하나였다.** 나머지는 스캐너의 구조적 한계(object shorthand
method는 호출문과 문법적으로 구분이 안 됨)에서 나온 노이즈.

### 4) 런타임 검증

로컬 `python -m http.server 8896`(`.claude/launch.json`의 "Dashboards (static)")로
서빙, `http://localhost:8896/K-ICS.html?iq_internal=1`을 Claude Browser로 열어 확인.

- **39사 전건**: `#company` select의 모든 옵션을 순회하며 `renderRateSensitivity()`
  호출 → **예외 0건**, `#sens-chart-wrap`·`#sens-table` 전부 `display:block`,
  `#sens-placeholder`는 전부 `display:none`(39/39 데이터 보유 확인 — 2026.2Q 기준
  모든 회사가 이미 반기 공시를 냈다).
- **엣지케이스 3종**도 확인: ① 회사 미선택 → "상단의 보험사를 선택하면..." placeholder
  정상 표출, 차트/표 숨김. ② `IQP`를 임시로 `throw`하도록 몽키패치해 "정의는 지워졌는데
  호출부가 남는" 이번 버그류를 인위 재현 → `renderSensDetail()`이 예외를 밖으로 던지지
  않고 자체 흡수, placeholder에 에러 문구가 뜨고 차트/표/듀레이션 카드 전부 숨김(half-render
  없음). ③ 정상 회사로 재렌더 → 차트/표 정상 복귀(안전망이 다음 렌더를 막지 않음).
- 스크린샷: `C:\Users\sangwook.cho\AppData\Local\Temp\claude\C--Users-sangwook-cho-Desktop-insurequant\8c0afc6e-aced-4947-80b1-d3bcff7ee649\scratchpad\kics_sens_lina_2026Q2.png`
  (라이나생명보험 2026.2Q, `#sens-chart` canvas를 `toDataURL()`로 직접 캡처 — 팔레트 B
  틸 색 라인이 정상 렌더됨을 확인).
- `pytest tests/test_deploy_assets.py` — 11 passed(인라인 데이터 없음·BOM 없음·
  keep-list 등 회귀 없음).

### 손대지 않은 것

- `kics_rate_sensitivity.json` 등 마스터 JSON 전부 무수정(진단대로 데이터는 정상이었다).
- 팔레트 B 색 값(`IQP()`가 반환하는 틸)·호출부의 `borderColor`/`backgroundColor` 용법
  변경 없음 — 복구지 재디자인이 아니라는 지시를 그대로 지켰다.
- `main` cherry-push·배포 스크립트 실행 없음(publishing/owner 승인 사항).

Claude Sonnet 5, 단일 세션, 약 40분(원인 재확인 + 수정 2건 + 정적 스윕 스크립트 작성 +
Playwright 없이 Claude Browser로 39사 실렌더 + 엣지케이스 3종 + 스크린샷 저장 포함).
