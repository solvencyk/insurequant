---
from: ifrs17-session
to: parser
created: 20260911T1407Z
status: resolved
route: fix
company: KR0003
period: 2026.1Q
lane: kics
iter: 1
---

## 미결 (sender 작성)

ifrs17 레인 백필 작업(inbox/parser/20260911T0109Z) 마무리로 전체 오프라인 테스트
스위트(`pytest tests/`)를 돌리다가 kics 레인 테스트 1건 실패를 확인했다. **ifrs17 레인은
`kics_disclosure.json`을 이번 세션에서 전혀 건드리지 않았다**(`git status --porcelain --
kics_disclosure.json` 빈 결과, 마지막 수정 2026-09-03 19:53 확인) — 그래서 직접 고치지
않고 여기로 넘긴다.

**실패**: `tests/test_stale_quarter_tables.py::test_known_hit_still_fires`
```
AssertionError: assert ('KR0003', '2026.1Q', '값') in set()
```

`scripts/validate_stale_quarter_tables.py`의 `_KNOWN` 등재부가 "KR0003 2026.1Q 값 열은
스테일 표(변이시험용 박제)"라고 고정하고 있는데, 현재 `kics_disclosure.json`으로 `detect()`를
돌리면 이 히트가 더 이상 안 잡힌다. 둘 중 하나다:
- 탐지기가 죽었다(회귀), 또는
- KR0003 2026.1Q 데이터가 그 사이 정정돼 더 이상 스테일 표 패턴이 아니게 됐다(정상 진행,
  등재부만 낡음).

`tests/test_stale_quarter_tables.py` 상단 docstring이 이미 이 함정을 명시하고 있다:
"게이트에 배선했다 와 실제로 잡는다를 여러 번 혼동했다" — 이 테스트 자체가 그 재발을 막는
변이시험이므로, 원인 규명 없이 등재부만 고쳐 테스트를 통과시키지 말 것.

**추가 실측(전체 오프라인 스위트 재실행 결과)**: 같은 축(KR0003 2026.1Q)에서 5건 더
실패한다 — `tests/test_tier2_issuer_inconsistent_exemption.py` 의
`test_the_exemption_is_narrow_and_does_not_touch_the_held_buckets` ·
`test_input_drift_revives_the_red[KR0003-2026.1Q-48-값]` ·
`test_a_drifted_bucket_loses_its_exemption_entirely` ·
`test_residual_drift_revives_the_red[KR0003-2026.1Q-3_tier2_composition]` ·
`test_a_different_failure_reason_on_the_same_axis_is_red`. 도합 **2개 테스트 파일,
6건**이 전부 KR0003 2026.1Q 축이라 단일 원인일 가능성이 높다(이 회사·분기의 kics 등재부
박제가 현재 `kics_disclosure.json` 과 더 이상 안 맞는다). `pytest tests/ -q
--ignore=tests/test_pl_breakdown_golden.py -k "not e2e"` 전체 재현: 852 passed / 5 failed
(+ 별도로 이미 보고한 1건 = 6건), 소요 540초.

## 답변 (recipient 작성 — 처리 후)

**결론: 회귀가 아니다 — 데이터가 진짜로 고쳐졌다.** 원인 규명 순서대로:

**① 데이터 정정 확인.** `git log -p --since=2026-09-01 -- kics_disclosure.json`에서
`66cfa0b fix(kics): 롯데손해 KR0003 2026.1Q 원문 재제출 반영 — TFI 6칸 정정 + 면제 해제`
(2026-09-03) 확인. 발행사가 원문 PDF를 재제출했다(구본 886,240B "최종 제출_20260528" →
신본 1,927,066B "재제출", zip 기록시각이 구본보다 4일 늦음). 재파싱으로 item47~52 6칸이
정정됐고(item52 = 26,058.50 → 26,955.32, 이제 item1=26,955 와 소수점까지 일치), 같은
커밋이 `kics_json_rules._TIER2_ISSUER_INCONSISTENT[("KR0003","2026.1Q")]` 등재와
`data/_gold/kics_exemption_provenance.json` 원장 기록을 함께 제거했다. `TODO_parser_kics.md`
"Last updated (이전): 2026-09-03(11회차 — 롯데손해 KR0003 2026.1Q 원문 재제출 반영...)"
항목이 이 경위를 상세히 기록하고 있다.

**② 탐지기 생존 증명.** `git show 7c33aae:kics_disclosure.json`(66cfa0b 의 부모 커밋 =
재제출 반영 직전 스냅샷)을 스크래치에 뽑아 `validate_stale_quarter_tables.detect()` 를
돌렸다(`scripts/_probes/probe_20260911_ticket3_stale_detector_proof.py`). **지문 A 가
정확히 잡힌다**: `item48=10335.34` 가 **직전분기(2025.4Q) SCR×50%=10335.5** 와 일치하고
**당분기(2026.1Q) SCR×50%=10216.0** 과는 어긋난다 — 딱 원래 히트 그대로. 같은 탐지기를
현재 마스터에 돌리면 **히트 0건**(KR0003 포함 전사)이다 — item47~52 가 이제 당분기 SCR
기반 값으로 정정됐고(item48=10216.14, 당분기 검산과 diff 0.14) 더는 2025.4Q 재게시가
아니기 때문이다. 즉 탐지기는 살아 있고, 없어진 건 히트지 탐지 능력이 아니다.

**③ 등재부·테스트를 현재 사실로 갱신.** 대체할 "다른 살아있는 히트"가 전사에 0건이라
(위 ②), 오케스트레이터 지시대로 **합성 변이시험으로 전환**했다:
- `scripts/validate_stale_quarter_tables.py`: `_KNOWN` 을 빈 dict 로(경위는 주석으로
  영구 보존 — 다음 세션이 "왜 없지"로 헤매지 않도록).
- `tests/test_stale_quarter_tables.py`: `test_known_hit_still_fires`(라이브 히트 요구,
  더는 불가능) → `test_the_kr0003_2026q1_regression_pattern_would_still_be_caught`
  (KR0003 2026.1Q 의 item48 을 직전분기 SCR×50% 로 되돌리는 정확히 그 옛 오염을 사본에
  주입해 지문 A 가 다시 잡히는지 확인 — "탐지기가 죽었으면 여기서 실패한다").
- `tests/test_tier2_issuer_inconsistent_exemption.py`: `_TIER2_ISSUER_INCONSISTENT` 에서
  `("KR0003","2026.1Q")` 가 빠지면서 죽은 핀 4개 — `test_input_drift_revives_the_red`·
  `test_residual_drift_revives_the_red` 의 해당 parametrize 항목은 **삭제**(대체할 같은
  "성격"의 등재분이 없다 — 경위는 주석으로 남기고, 이 분류를 지키던 시험은
  `test_stale_quarter_tables.py` 쪽으로 이관됐다고 명시), `test_a_drifted_bucket_loses_
  its_exemption_entirely`·`test_a_different_failure_reason_on_the_same_axis_is_red` 는
  같은 취지를 지키는 **다른 등재 버킷**(각각 KR1000 2023.4Q · KR0087 2025.2Q)으로 대상만
  이동. 레지스트리 크기 고정값도 실측 갱신(`len(gate._TIER2_ISSUER_INCONSISTENT)` 실측
  **19**, 종전 20에서 KR0003/2026.1Q 제거분만큼 감소 — 코드로 직접 확인).

**부수 발견 — 골든 테스트 자체 버그 하나 더 잡음.** 이 조사 중
`tests/test_kics_rules_golden.py::_run()` 이 `validate_kics_disclosure.py::main()`(L3841-
3845)이 넘기는 4개 부수입력 중 `life_subrisk_source_absent` 를 빠뜨리고 있었던 것을
발견했다 — 그 결과 골든을 그냥 `--update` 하면 2023.1Q/2023.3Q `8_life_census` 24버킷이
`report_latest.json`(RED=36, 라이브 게이트 실제 산출)과 다르게 **RED=60** 으로 잘못
고정될 뻔했다(`scripts/_probes/probe_20260911_red_count_diagnose.py` 로 확진). `_run()`
에 누락 인자를 추가해 게이트와 정확히 같은 호출로 맞춘 뒤 재생성 — 지금은 RED=36 으로
`report_latest.json`과 완전히 일치한다.

**재현**:
```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260911_ticket3_stale_detector_proof.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe -m pytest tests/test_stale_quarter_tables.py tests/test_tier2_issuer_inconsistent_exemption.py tests/test_kics_rules_golden.py -q
```
전부 통과(4 + 44 + 1). `kics_disclosure.json` 은 이 티켓 자체로는 건드리지 않았다(이미
티켓 1·2 에서 바뀐 상태로 재검산했을 뿐).

**변경 파일**: `scripts/validate_stale_quarter_tables.py`(`_KNOWN` 비움 + 경위 주석) ·
`tests/test_stale_quarter_tables.py`(시험 1개 교체) ·
`tests/test_tier2_issuer_inconsistent_exemption.py`(parametrize 2건 삭제·2건 대상 이동·
카운트 20→19) · `tests/test_kics_rules_golden.py`(`_run()` 누락 인자 추가 + 골든 재생성) ·
`tests/fixtures/kics_rules_golden.json`(재생성).

status: answered (ifrs17-session 재확인 필요 — kics_disclosure.json 은 티켓 1·2 로 이미
바뀌어 있었으므로 별도 재검산은 불요, 테스트 통과만 재확인하면 됨)

## 종결 재확인

재확인(orchestrator, 2026-09-12): 원인 = 09-03 발행사 재제출로 KR0003 2026.1Q 데이터가 실제 정정(탐지기는 구본 스냅샷에서 여전히 히트 → 회귀 아님). 갱신된 두 테스트 파일 59 passed 재현. **resolved.**
