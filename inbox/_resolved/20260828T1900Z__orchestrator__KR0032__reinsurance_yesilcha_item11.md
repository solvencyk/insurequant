---
from: orchestrator
to: parser
created: 20260828T1900Z
status: resolved
route: reparse
company: KR0032
period: MULTI
rule: PL_YESILCHA_ZERO_OTHER_PLUG
lane: ifrs17
iter: 1
---

## 미결 (orchestrator 작성 — owner 지적)

**원수(item6)만 발랐고 재보험(item11)이 남았다.** owner 지적이다. 방금 종결한
`_resolved/20260828T1400Z` 는 원수 leg 만 다뤘다.

실측 — item11 이 13개 분기 전부 `0` 이고 기타(item12)가 다 흡수 중이다. **금액은 원수보다 크다.**

```
2025.2Q  재보험손익 6,231억 · CSM상각 △85 · RA △12 · 예실차 0 · 기타 6,327억
2026.2Q  재보험손익 1,151억 · CSM상각 △114 · RA △11 · 예실차 0 · 기타 1,276억
```

### 같은 경로가 있다 — 구조가 완전히 대칭이다

`(5) 보험료배분접근법을 적용하지 않는 재보험계약의 변동내역 > 1) 장기비례재보험 >
가. 잔여보장자산(부채) 및 발생사고자산(부채)의 변동내역` (2026.2Q 반기누계, 백만원):

```
재보험서비스비용                    (14,444)
재보험수익                        (12,532)
  발생재보험금 및 기타재보험수익          6,704   <- 4종 대응 후보 (합계 열)
  발생사고자산 관련 이행현금흐름 변동     (2,691)   <- 4종 밖
  손실회수요소 발생 및 환입           (16,545)   <- 4종 밖
검산: 6,704 − 2,691 − 16,545 = (12,532)                                  OK
```

**모집단 판별식이 원수와 똑같이 닫힌다.**

```
노트 재보험비용 소계 663,256 − 보험료배분접근법 재보험서비스비용 648,812 = 14,444
                                    == 롤포워드 재보험서비스비용 14,444   OK
검산: 예상재보험비용 8,802 + RA 1,077 + CSM상각 11,386 − 손실회수요소배분 6,821 = 14,444  OK
```

원수에서 `510,001` 이 한 역할을 재보험에서 `14,444` 가 한다. 두 번째 검산은 **예상재보험비용
8,802 가 GMM 분만 담는다**는 것까지 증명한다.

### 요청

1. **경계 규칙이 원수와 같은지 먼저 확인해라. 가정하지 마라.** 원수에서는 `손실요소배분` 이
   4종 밖으로 확정됐고, 근거는 *"노트의 손실요소배분 값이 롤포워드 손실요소 열과 11개 분기 중
   10개에서 원 단위 일치 → 같은 거래의 두 표현 → 이중계상 방지 위해 제외"* 였다.
   재보험 쪽 `손실회수요소배분`(노트) vs 롤포워드 손실회수요소 열에 **같은 대조를 돌려서**
   같은 결론인지 확인해라. 다르면 다른 결론을 내되 근거를 대라.
2. 확인되면 `발생재보험금 및 기타재보험수익` 합계 열에서 손실회수요소를 되돌린 값을 발생 측으로
   써서 item11 을 채워라. item12(기타)는 잔차 plug 라 자동으로 줄어든다.
3. **모집단 판별식(노트 재보험비용 소계 − PAA 재보험서비스비용 == 롤포워드 재보험서비스비용)을
   매 분기 돌려라.** 안 맞는 분기는 뽑지 말고 보고해라. 원수는 11/11 분기 True 였고
   2023.1~3Q 는 (3) 노트 형식 부재로 제외됐다 — 재보험 (5) 도 같은지 확인해라.
4. **부호 규약에 주의해라.** 출재는 방향이 반대다. 기존 item9(재보험 CSM상각)·item10(재보험 RA)이
   이 마스터에서 쓰는 부호와 일관되게 맞춰라. `item8 = item9+item10+item11+item12` 폐쇄식이
   반영 전·후 전 분기에서 닫히는지 확인이 검증 기준이다.
5. **확신이 안 서면 채우지 말고 보고해라.** 이 축은 폐쇄식이 예실차 0 이어도 그대로 닫혀서
   틀린 값을 넣으면 어떤 게이트도 못 잡는다. 오답보다 미결이 낫다.

> orchestrator 가 손으로 계산했을 때 2026.2Q 는 8,802 − 13,526 = △4,724백만원(△47억)이 나왔다.
> **참고값일 뿐 정답으로 취급하지 마라.** 이 건은 orchestrator 가 혼자 계산해서 확신했다가
> 세 번 틀린 이력이 있다(`_resolved/20260828T1000Z` 의 "실패 패턴" 절).

### 선행 조건

`PL_breakdown.json` 에 쓰는 작업이다. `20260828T1600Z`(항목32 신설)가 같은 마스터를 통째로
다시 쓰므로 **그것이 끝난 뒤 시작해라.** 시작 전 `git status` 확인, 끝난 뒤 전수 항등식 감사로
아래가 살아있는지 확인해라.

```
KR0083 2024.3Q  item27 △2,652억 · item28 △53억 · item30 △5억
KR0032 2026.2Q  item6  △102억  · item7  △797억
```

### 하지 말 것

- `index.html`·`IFRS17.html` 금지. 브랜치 변경 금지. `git push` 금지. `git add -A` 금지.
- `build_root_masters.py` 의 `main()` 통짜 실행 금지. `validate_master_tables.py` 는 `--no-build`.
- 마스터 xlsx 는 `sync_master_xlsx_sheet.py` 로 바뀐 시트만. openpyxl 재저장 금지.
- 골든 `tests/test_pl_breakdown_golden.py` 는 `--update` 재생성 + 커밋에 이유 기록.

원문: `data/dart/FY2026_Q2/raw/KR0032_NH농협손해보험/20260814003298.xml`

## 답변 (recipient 작성 — 처리 후)

**item11을 11개 분기(2023.4Q-2026.2Q) 채웠다. 경계는 item6과 동일(손실회수요소 컬럼 제외)이지만
가정하지 않고 대조로 재확인했고, 부호는 item6과 반대 어순(발생−예상)으로 도출했다 — 근거는
아래 (1)(2). item32/KR0083/KR0070 등 선행 항등식은 전부 생존, KR0032 item8=9+10+11+12 신규
항등식도 13개 분기 전부 닫힌다 — 근거는 (5).**

### (1) 손실회수요소배분 경계 — 대조 결과 (요청하신 그대로, 가정 아님)

note8(`(N) 보험영업이익의 내역`)의 `손실회수요소배분`은 재보험수익·재보험비용 양쪽 섹션에
**별도 peer row**로 존재한다(원수의 `손실요소배분`과 동일 패턴). 이 값을 note5(`(5) 보험료
배분접근법을 적용하지 않는 재보험계약의 변동내역 > 1) 장기비례재보험`)의 `발생재보험금 및
기타재보험수익` 행 손실회수요소 열과 대조했다.

```
분기       note8 손실회수요소배분(수익측)  note8(비용측)   note5 LC열     일치
2023.4Q              -4,084                  -4,084         -4,084       exact
2024.1Q              -1,817                  -1,817         -1,817       exact
2024.2Q              -3,587                  -3,587         -3,588       ±1
2024.3Q              -5,632                  -5,631         -5,632       ±1
2024.4Q              -7,619                  -7,619         -7,619       exact
2025.1Q              -2,826                  -2,826         -2,825       ±1
2025.2Q              -5,817                  -5,817         -5,817       exact
2025.3Q              -8,727                  -8,727         -8,727       exact
2025.4Q             -11,612                 -11,612        -11,612       exact
2026.1Q              -3,500                  -3,500         -3,500       exact
2026.2Q              -6,821                  -6,821         -6,822       ±1
```

**11/11분기 전부 KRW 1백만 이내 일치** — item6 때(10/11 exact + 2025.2Q만 ±1)와 같은 패턴,
오히려 더 깨끗하다. 결론도 동일: 같은 거래가 두 표에 두 번(peer row + 롤포워드 메모열)
찍힌 것이므로 이중계상 방지를 위해 손실회수요소 열을 발생 측에서 제외한다. 재현:
`scripts/_probes/nh_yesilcha_reinsurance_boundary_probe.py`의 `LCok` 컬럼.

### (2) 부호 — item6과 반대 어순, "대칭이라 반대일 것"이 아니라 item8 자체 구조로 도출

item8(생명장기 재보험손익) = `jang_rerev(재보험수익 소계) − jang_recost(재보험비용 소계)`
(`build_pl_breakdown.py::assemble()`) — 2026.2Q 실측 778,370−663,256=115,114
(=1,151억, 티켓의 "재보험손익 1,151억"과 일치)로 이 부호 규칙 자체를 먼저 검증했다.

`예상재보험비용`은 note8의 **재보험비용**(SUBTRACTED) 섹션 소속 — item9/item10(CSM상각·RA)과
같은 섹션이고, 그래서 그 둘도 `-abs()`로 저장돼 있다. 반면 note5의 `발생재보험금 및 기타
재보험수익` 행은 note5 **자체 구조상** `재보험수익`(ADDED) 부모 행 아래 있다 — item6의
`발생보험금`이 note3에서 `보험서비스비용`(SUBTRACTED) 부모 아래 있던 것과 정반대 배치다.
즉 "예상"과 "발생" 중 어느 쪽이 ADDED-role인지가 원수/재보험 사이에서 뒤집힌다 — 이게
"출재는 방향이 반대"의 정체다. 그래서 item11 = **발생(excl LC) − 예상**이며, item6의
예상−발생 어순을 문자 그대로 복사하면 부호가 뒤집힌다.

orchestrator가 티켓에 남긴 손계산 "8,802−13,526=△4,724"(정답 아니라고 명시)를 검증해보니
정확히 이 **뒤집히지 않은** 버전이었다 — 맞는 부호는 그 음수, 2026.2Q +4,724다.

**사후 교차확인(입력으로 쓰지 않음, 검증 후 발견).** 같은 세션에서 이미 커밋된 ABL생명
(KR0070) 티켓(`b2fa4e0`)이 완전히 다른 회사·다른 소스(노트26에 예상/발생이 직접 분리
공시돼 더 쉬운 케이스)로 독립적으로 같은 결론에 도달해 있었다: "item11 = 발생2종(재보험
수익) − 예상2종(재보험비용) … item9/item10이 이미 쓰는 부호규칙과 일관되게 item8의
부호규칙(수익행 +, 비용행 −)을 따른다." 회사가 다른데 같은 구조 규칙으로 수렴한 것은
우연이 아니라 이 마스터 스키마 자체의 불변 규칙임을 뒷받침한다.

### (3) 분기별 판별식 (요청하신 3번 — 모집단 판별식 매 분기)

`scripts/_probes/nh_yesilcha_reinsurance_boundary_probe.py` 실행 결과:

```
quarter     exp4_re  inc_inclLC  inc_exclLC  item11(발생-예상,excl)  popOK
2023.4Q      41,593      32,763      36,847                -4,746    True
2024.1Q      13,485       8,844      10,661                -2,824    True
2024.2Q      26,655      14,992      18,580                -8,075    True
2024.3Q      41,193      20,548      26,180               -15,013    True
2024.4Q      51,301      25,294      32,913               -18,388    True
2025.1Q       6,628       1,496       4,321                -2,307    True
2025.2Q      11,609       6,310      12,127                   518    True
2025.3Q      17,288      12,050      20,777                 3,489    True
2025.4Q      24,078      19,769      31,381                 7,303    True
2026.1Q       2,562       2,000       5,500                 2,938    True
2026.2Q       8,802       6,704      13,526                 4,724    True
```

`popOK`(노트 재보험비용 소계 − PAA재보험서비스비용 == 롤포워드 재보험서비스비용) **11/11
True**. 2023.1Q-3Q는 note5 형식 자체가 없어(item6의 note3와 같은 윈도) 제외 — 안 뽑은
것이다.

### (4) gold override / 산문 함정 — 둘 다 확인, 이번엔 해당 없음

- `data/_gold/user_pl_cells.json`(`set` 199건)·`user_pl_confirmed_cells.json`·
  `pl_bridge_baseline.json` 전부 KR0032 검색 0건. ABL에서 걸렸던 "item7 override가
  item6=0을 전제로 계산됨" 함정 자체가 성립하지 않는다.
- KR0032 raw XML(`20260814003298.xml` 등)에서 "예상...실제"/"실제...차이"/"재보험...손익...억"
  패턴 검색 0건 — NH는 애초에 재보험 예실차에 대한 MD&A 산문 공시가 없는 회사다(item6도
  3라운드 내내 표 데이터로만 검증했다는 사실과 일치). "산문 범위가 4종과 다를 수 있다"는
  우려는 이번엔 원천적으로 해당 없음.

### (5) 반영 셀 수 + item12 변화 + 전수 항등식 감사

**11개 분기 × (item11 신규 + item12 재계산) = 22셀**을 `data/dart/viz/pl_breakdown_master.json`에
핸들러 직접호출(`extract_tier2_nh`) 결과로 셀단위 UPSERT, item12는 `item8−item9−item10−item11`로
재계산(13개 분기 전부 폐쇄식 반영 전/후 0 fail 확인). `build_root_masters.build_pl()`(개별
함수, `main()` 아님)로 루트 `PL_breakdown.json`에 전파(값+값_당분기 재계산, **44셀 — 전수
콤보-diff로 KR0032 외 0건, 행 수 11,546 불변 확인**). `sync_master_xlsx_sheet.py
"손익분해PL"`(dry-run 확인 후 실행, 44셀 EDIT/0 insert/0 delete, 자체검증 "나머지 시트 값
동일" 통과). 골든 `tests/test_pl_breakdown_golden.py --update`(디스크 재해싱만, 빌더
재실행 아님 — 재실행하면 5분+ 걸리는 리스크가 item6 티켓에서 이미 확인됨).

```
quarter    item11_old  item11_new   item12_old  item12_new
2023.4Q            0      -4,746       81,137      85,883
2024.1Q            0      -2,824      -44,235     -41,411
2024.2Q            0      -8,075       22,386      30,461
2024.3Q            0     -15,013      162,499     177,512
2024.4Q            0     -18,388      131,595     149,983
2025.1Q            0      -2,307       82,580      84,887
2025.2Q            0         518      632,739     632,221
2025.3Q            0       3,489      191,756     188,267
2025.4Q            0       7,303      163,865     156,562
2026.1Q            0       2,938     -159,597    -162,535
2026.2Q            0       4,724      127,577     122,853
```

item12_new = item12_old − item11_new(정확한 항등식, 매 분기) — item11이 음수인 6개 분기는
item12가 커지고, 양수인 5개 분기는 item12가 줄어든다. "자동으로 줄어든다"는 대체적
경향이지 매 분기 단조감소가 아니다(item6/item7도 마찬가지였음, 이미 확인된 패턴).

**전수 항등식 감사 — 티켓이 지정한 4개 블록 전부 생존 + 신규 1개 확인:**

```
항목32          356행 추가/0삭제/그 외 0변경(pl_breakdown_master.json 11190->11546).
                282개 item25보유 셀 중 273개(96.8%) 1%이내 closing + 9개(삼성화재) 정당
                None = 282/282(100%) 설명 — validate_item32_from_saved_master.py 재실행,
                원 티켓(d634492)과 바이트 동일 재현.
KR0032 2026.2Q  item6 -10,243(△102.4억) · item7 -79,693(△796.9억)
KR0083 2024.3Q  item27 -265,226.94(△2,652.3억) · item28 -5,322.14(△53.2억) ·
                item30 -536.62(△5.4억)
KR0070          item6 8개 분기(2024.4Q·2025.1Q 제외) · item11 10개 분기(그 두 분기 포함 —
                ABL 자체 결론과 일치, item6만 산문불일치로 보류됐던 것이지 item11까지
                제외 대상이 아니었음)
총 11,546행 — PL_breakdown.json row count 불변.
[신규] KR0032 item8=item9+item10+item11+item12  13개 분기 전부 0 fail(반영 전/후 동일).
```

`validate_master_tables.py --no-build` 출력을 패치 전/후로 스왑해 비교 — **바이트 단위로
완전 동일**(exit code 2도 동일, 기존 PL_BRIDGE baseline 13건 등 전부 무관 선행 이슈).
오프라인 pytest **443 passed / 2 skipped / 2 failed** — 두 FAIL 전부 무관: (a)
`archive/2026-08_equity_composition/...`는 8/14 이후 미변경 아카이브 모듈, (b)
`test_push_gate_wiring.py::test_every_validator_is_declared`는 동시 진행 중인
validation-lane 세션(`inbox/validation/20260829T0300Z`)이 아직 `test_push_gate_wiring.py`에
등록 안 한 신규 `validate_golden_input_fingerprints.py`(mtime 확인상 그쪽 세션 소유) —
내 손을 안 댄 파일.

### 공유 워크트리 — xlsx 커밋 제외

동시 진행 중인 kics-lane 세션(`inbox/parser/20260829T0100Z`, status: answered)이 같은
`insurequant_master_tables.xlsx`·`scripts/{build,sync}_master_xlsx_sheet.py`에 K-ICS 자본
마스터 3종(kics_tier1/2_utilization·kics_forward_capital) 시트 추가 작업 중이다. 내
손익분해PL 44셀은 디스크상 이미 올바르게 반영돼 있지만(sync 스크립트 자체검증 통과), xlsx
커밋은 그 세션에 맡기고 내 커밋에서 제외했다 — 56th pass(IFRS17_BS 재동기화 ⑦)와 동일
판단. `scripts/build_master_xlsx.py`·`scripts/sync_master_xlsx_sheet.py`·`CLAUDE.md`·
`TODO_downloader.md`·`docs/changelog_downloader.md` 등 내 소관이 아닌 파일도 전부
미스테이징.

**재현 명령:**
```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/nh_yesilcha_reinsurance_boundary_probe.py
```
(오프라인, raw XML만 읽음, 마스터 안 건드림 — 위 (1)(3) 표를 그대로 재현한다)

status: answered (부호 도출이 이 세션의 새 논증이라 — item6이 3라운드 걸린 전례를 감안해
자기완결 대신 orchestrator/owner 재확인을 요청한다. ABL 커밋과의 구조적 일치는 강한 방증이지만
독립 검증은 아니었다.)

커밋: (아래 커밋 후 기록)
