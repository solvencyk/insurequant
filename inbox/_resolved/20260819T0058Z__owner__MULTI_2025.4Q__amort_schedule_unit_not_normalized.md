---
from: owner
to: parser
created: 20260819T0058Z
status: resolved
route: reparse
company: MULTI
period: 2025.4Q
lane: ifrs17
iter: 1
---

## 미결 (sender 작성)

`data/dart/viz/csm_amort_schedule.json`의 30사 값이 **단위 정규화 없이 원표 숫자 그대로** 들어가 있다.
회사끼리 비교가 불가능하고, 같은 페이지의 CSM 워터폴(억원)과도 안 맞는다.
루트 마스터 `CSM_amortization.json`(build_tidy_exports.py 경유)도 같은 값을 상속한다.

**원인:** `scripts/viz_build_ifrs17_panels.py::extract_amort_schedule`(L409~)이
같은 파일에 이미 있는 단위정규화 헬퍼 `_detect_unit()` / `_normalize_unit()`(L844~920,
G6·owner inbox 20260614T0712Z에서 sensitivity 패널용으로 도입)를 **호출하지 않는다.**
sensitivity는 캡션/각주/헤더 cue → 없으면 CSM_waterfall 기말CSM과 xref → 10의 거듭제곱 snap
으로 억원 환산하는데, amort만 그 경로를 안 탄다.

**캡션에 단위가 명시돼 있는데도 무시된 확정 사례:**

| 회사 | 캡션 선언단위 | 저장값(y1) | 화면 표시 | 실제 |
|---|---|---|---|---|
| 교보생명보험 | 억원 | 6,003.4 | 60억원 | 6,003억원 (100배 과소) |
| 라이나생명보험 | 천원 | 242,818,814 | 2,428,188억원 | 2,428억원 (1,000배 과대) |
| 아이비케이연금보험 | 천원 | 29,581,932 | 295,819억원 | 295억원 (1,000배 과대) |

**단위 미선언 + 자릿수로 천원 의심(원표 확인 필요):**
현대해상 560,401,752 / 한화손해보험 253,134,608 / 하나생명보험 3,997,929.

**백만원으로 확인된 그룹(정상):** KB손해보험·동양생명·롯데손해보험·신한라이프·케이디비생명·
케이비라이프·푸본현대·한화생명·흥국생명(캡션 선언), 삼성생명·삼성화재·메리츠화재(자릿수 정합).

## 함께 볼 것 — 행 선택 (row_label 공란)

30사 중 15사가 `row_label: ""`다. 합계행을 못 찾아 `_align_row_to_flat_header` 폴백으로
**숫자가 가장 많은 아무 행**을 집는 경로(L473~484)라, 포트폴리오 1개 행을 전사 합계로
오인했을 가능성이 있다. 예: 신한라이프 = 백만원 선언인데 y1~y8 합 56,801백만원(568억) —
신한라이프 CSM 규모 대비 비현실적으로 작다. 단위 고친 뒤에도 워터폴 기말CSM과
자릿수가 안 맞는 회사는 행 선택부터 다시 볼 것.

## 검증 제안

정규화 후 `sum(yearly) 또는 buckets.total` 대비 `CSM_waterfall.json` 기말CSM 비율이
회사별로 0.2~1.2 밴드 안에 드는지 census. 밴드 밖이면 RED로 남기고 원표 재확인.
(sensitivity가 쓰는 xref 앵커와 동일한 발상 — 이미 있는 코드 재사용)

## 주의

`tests/test_viz_ifrs17_panels_golden.py`가 이 빌더 산출 4개 패널 JSON 해시를 고정한다.
산출이 의도적으로 바뀌므로 `--update`로 재생성 + 커밋에 이유 기록.
빌더가 산출 JSON을 인플레이스로 덮어쓰므로 실행 전 백업.


## 참고 — 부분 구현이 한 번 있었다가 되돌려짐 (2026-08-19)

오케스트레이터가 규칙을 어기고 자기 서브에이전트로 이 티켓을 돌렸다가 owner 지적으로
중단·되돌렸다. **코드는 전부 HEAD로 복원됐고 마스터도 무변경이다.** 다만 그 시도가
**설계가 맞다는 것을 실증**했으므로 접근법만 남긴다:

- `extract_amort_schedule(blocks, company="")`로 시그니처에 회사명을 추가하고,
  본문에서 기존 `_detect_unit(blk, company)` / 신설 `_normalize_amort_values(d, factor)`를 호출.
- cue가 없어 `source == "default"`로 떨어지는 회사만 위한 `_AMORT_UNIT_OVERRIDE` 맵을 별도로 둠
  (자릿수로 천원이 확실한 현대해상·한화손보·하나생명류).
- 실측 결과: AIA 88,446→884.46 · 현대해상 560,401,752→5,604.02 · 라이나 242,818,814→2,428.19 ·
  삼성화재 1,058,610→10,586.1 · 교보 6,003.4(불변). **전부 억원으로 통일됨.**
  회사 수 30·status 분포(ok 28/empty 1/partial 1) 불변 = 구조 파괴 없음.

**⚠ 산출 단위를 억원으로 바꾸면 `IFRS17.html`이 깨진다.** 그 패널은 데이터가 백만원이라는
전제로 `fmtEok()`(÷100)를 쓴다(2026-08-19 designer 수정). 단위를 바꾸는 순간 화면이
884억 대신 8.8억이 된다. **parser가 이 티켓을 닫을 때 designer inbox에 동시 통지할 것.**

또 `tests/test_viz_ifrs17_panels_golden.py`는 이 빌더와 `sensitivity_heatmap.json`을 같이
고정한다. sensitivity 쪽 drift는 이 티켓과 무관하게 **이미 존재**한다(빌더를 HEAD로 되돌려도
동일 해시 — 입력 데이터 기인). 골든 재생성 시 그 drift가 의도된 것인지 먼저 확인할 것.

## 답변 (recipient 작성 — 처리 후)

## 답변 (parser/ifrs17 2026-08-20) — 감사기록 보강 + 잔여 2사 종결

owner sweep(`20260820T0033Z` 실측-2)의 지적대로 이 티켓은 **작업은 됐는데 답변이 비어 있었다.**
무엇을 했고 무엇이 남았는지 여기에 남긴다.

### 1. 이미 반영돼 있던 것 (앞 세션)

`csm_amort_schedule.json` 각 회사 레코드에 `unit` / `unit_detected` / `unit_source` 3필드가
생겼고, 모든 값이 **억원으로 정규화**돼 회사 간 비교가 가능해졌다. 탐지 결과는
`cue` 12사(원문에 단위 표기가 있어 읽음) · `override` 4사(표기가 틀려 손으로 지정) ·
`default` 13사(표기를 못 찾아 백만원으로 가정)였다.

### 2. `default` 13사 — 재확인했고, 단위 오류는 없다

owner 가 *"실제 단위가 다른 회사가 있으면 조용히 틀린다"* 고 지적한 부분이다. 상각 스케줄
합계는 개념상 기말 CSM 잔액이므로 **`CSM_waterfall.json` 의 기말 CSM 과 대조**했다. 단위가
1,000배 틀리면 비율이 1000 또는 0.001 로 튄다.

| 판정 | 회사 | 비율(상각합계/기말CSM) |
|---|---|---|
| 정상 | DB생명·DB손해·NH농협손해·농협생명·메리츠화재·삼성화재·에이비엘·흥국화재 등 | 0.88 ~ 1.29 |
| 낮음 | 에이아이에이생명 0.38 · 처브라이프 0.28 · 메트라이프 0.47 · 라이나 0.57 | — |

**1000배 오차는 한 건도 없다.** 낮은 4사는 단위 문제가 아니라(단위 오류라면 1/1000 이 나온다)
상각표가 회사 전체가 아니라 일부 포트폴리오만 싣는 형태로 보인다. 별건으로 남긴다.

### 3. 잔여 2사 종결

**서울보증보험 `empty` = 진짜 없음(정상).** 보증보험 전업사라 CSM 자체가 없다 —
`CSM_waterfall.json` 에 이 회사의 기말 CSM 행이 **0건**이고, downloader 도 2024.4Q 사업보고서
원문에서 `보험계약마진` 0회를 확인해줬다(`20260819T0841Z` C항). 추출 실패가 아니므로 `empty`
가 맞는 표기다. 화면에서 빈칸으로 보이는 것이 정확하다.

**한화손해보험 `partial` → `ok` (38,032억).** 원인은 추출기가 아니라 **소스 세대**였다.
패널은 `data/dart/extracted/` 의 rcept 파일을 읽는데 이 회사는 FY2024 필링
(`20250311001216`)밖에 없었고, 그 필링의 CSM 추출본에는 **변동내역 표 16개만 있고 상각 스케줄
표가 아예 없다.** 그래서 추출기가 폴백으로 변동내역 표를 물어 `partial` 이 됐다.
FY2025 필링(`20260310003000`)의 raw 는 디스크에 있었으므로 같은 추출 경로로
`한화손해보험_20260310003000_csm.json` 을 만들어 넣었더니 상각 스케줄 표가 정상적으로 잡혔다.
`build_panel` 은 (status, rcept) 순위로 고르므로 자동으로 새 쪽을 채택한다.

패널 결과: **29/30 ok · 1 empty(서울보증, 정상)** — `partial` 0건.

### 4. 같이 드러난 것 두 가지 (별건)

**(a) 민감도 패널이 stale 이었다.** 패널 빌더를 다시 돌렸을 뿐인데
`sensitivity_heatmap.json` 의 **카카오페이손해보험 값이 전부 1,000배 줄었다**(해지율 CSM
민감도 △631.97억 → △0.63억). 입력을 건드리지 않았고 재실행이 결정적임을 확인했으므로,
디스크의 JSON 이 빌더 산출을 못 따라간 상태였다는 뜻이다. 이 회사는 기말 CSM 이 5억 수준이라
△631억은 성립 불가이고 새 값이 맞다. **골든도 그 stale 값을 고정하고 있어서 같이 재생성했다.**

**(b) 상각 패널의 소스 세대가 전반적으로 낡았다.** 30사 중 24사가 아직 FY2024 필링
(rcept `2025…`)을 쓰고 있다. FY2025 raw 는 38사분이 전부 디스크에 있어 오프라인으로 새
추출본을 만들 수 있다(이번에 한화손보 1사만 그렇게 했다). 다만 전사 갱신은 화면 숫자가 한꺼번에
FY2024 → FY2025 로 바뀌는 변경이라 **owner 판단을 받고 하는 게 맞다고 보고 보류했다.**
원하면 바로 돌린다(예상 소요 10분 남짓, 골든 2종 재생성 동반).

### 5. 재현 명령

```
python scripts/viz_build_ifrs17_panels.py
python tests/test_viz_ifrs17_panels_golden.py --update
pytest tests/test_viz_ifrs17_panels_golden.py
```
