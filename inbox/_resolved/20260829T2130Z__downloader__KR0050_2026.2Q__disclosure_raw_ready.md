---
from: downloader
to: parser
created: 20260829T2130Z
status: resolved
route: raw_ready
company: KR0050
period: 2026.2Q
lane: kics
iter: 1
---

## 미결 (sender 작성)

**2026.2Q 정기경영공시 첫 게시사 = 하나손해보험(KR0050) 1개사. raw 확보 완료.**

```
data/disclosure/FY2026_Q2/pdf/KR0050_하나손해보험.pdf
  1,620,955 bytes · 60 pages · %PDF- · %%EOF 존재
  sha256 67de71db057a7217791d46467df8dec4...
```

`run_harness.py --stage parse` 가 걷는 경로(`data/disclosure/<period>/pdf/`)에 그대로
넣었다. **PDF→MD(docling) 변환은 하지 않았다 — parser 소관이다.**

### 내용 검증 (파일명·응답코드 아님, 바이트로 확인)

`scripts/_probes/verify_q2_disclosure_content.py` 3중 검사 전부 통과:

1. **freshness** — FY2026_Q1 파일과 SHA256 다름
2. **period** — 1페이지 원문 `2026년 상반기 경영공시 / [기간 : 2026. 1. 1 ~ 2026. 6. 30]`,
   2026-2Q 마커 검출 · **2026-1Q/3.31 마커 0건**
3. **doctype** — `보험업감독규정 제7-44조` 명시, 지급여력비율 34회·지급여력금액 19회·
   경과조치 40회

본문 실측 요약표(전분기 대비): 자산 29,085 / 부채 22,749 / 자본 6,336 / 당기순이익 △712,
**지급여력비율(경과조치 전) 152.13** (2025.2Q 141.27, +10.86). 텍스트 레이어 정상이라
KR0005 류 OCR 경로는 불필요하다.

### ⚠ 파싱 주의 1건

요약표의 **`지급여력비율 (경과조치 후)` 행에 값이 비어 있다**(라벨만 있고 숫자 없음).
경과조치 미적용인지, 별도 세부표에만 실렸는지는 downloader 가 판정하지 않았다 —
`_TRANSITION_KIND` 레지스트리와 대조해 판단할 것. 값 없음을 0 으로 채우지 말 것.

### 나머지 38개사

**미게시 확정 37사 + 미확인 1사.** 생보 22사는 생명보험협회 일괄페이지 기준년 2026 그리드에서
전원 `2분기 = "-"`(1분기만 다운로드 가능), 손보 15사는 자사 listing 최신 행이 2026 1분기.
**코리안리(KR1000)만 미확인** — 사이트가 모든 transport 에서 응답 없음(서버측 다운).
게시되는 대로 같은 채널로 통지한다.

## 답변 (recipient 작성 — 처리 후)

**요약: 추출은 끝냈고 검증했지만, 코디네이터 지시로 마스터(`kics_disclosure.json`) 삽입은
되돌렸다 — 39사 중 1사만 게시된 부분 분기라 coverage census 게이트를 깬다. docling MD·
추출 로직·검증 결과는 전부 보존했다. 다음 확인 8/31(월), 나머지 사가 게시되면 그대로 재적용.**

### 1. docling 변환 (PDF→MD)

`run_harness.py --stage parse --period FY2026_Q2 --companies KR0050` 실행 완료.

```
inputs=1  ok=1  skip=0  fail=0  elapsed_s=163.9
[ok] KR0050  162.5s conf=0.85
```

산출: `data/disclosure/FY2026_Q2/parsed/KR0050_하나손해보험.md` +
`md_inbox/FY2026_Q2/KR0050_하나손해보험.md` (66,501 bytes, 둘 다 gitignore 대상이라
git 추적은 안 되지만 디스크에 영속 — 재실행 불필요, 다음 세션이 그대로 재사용 가능).

### 2. 추출 결과 (fill_period → fill_subitems → fill_market_subitems → fill_market_irr_from_pdf)

items 1-46 전량 추출 성공, item28(기본자본비율)만 결측(라벨이 원문에 직접 안 찍히는
파생값이라 정상 — [[reference-kics-item28-computed]]). 8_life·19_market·36_irr 세 축
모두 자체 golden 재현 통과(`fill_market_subitems_to_disclosure.py` 출력: `golden 19_market:
ok=1 bad=0` · `golden 36_irr: ok=1 bad=0`).

값 샘플(억원, 전년동기 대비): 자산 29,085 / 부채 22,749 / 자본 6,336 / 당기순이익 △712.
item1(지급여력금액)=7,114 · item14(지급여력기준금액)=4,677 · item27(지급여력비율)=152.13
(원문 요약표와 소수점까지 일치) · item17(생명장기위험액)=2,444(29-35 합 3,488.4, 비율
1.427 — 분산효과 정상범위) · item19(시장위험액)=905(36-40 합과 MARKET_M 재현 정확).

**부수 발견 — item28·47-54 온보딩 결함 (신규 스크립트로 정정, `scripts/fix_20260829_kr0050_
2026q2_onboarding.py`, `--dry-run` 지원, KR0050+2026.2Q 외 다른 셀은 손대지 않음):**
- item28(기본자본비율) 결측 → item2/item14×100 산출 UPSERT = 22.42890742.
- item47-54(TFI표, "[지급여력비율의 경과조치 적용에 관한 사항] (1) 공통적용") 부분결측 +
  item48 오염. `fill_period_to_disclosure.py`의 라벨 매처가 "보완자본 한도"(48) 템플릿을
  "보완자본"(3/13) 행에 잘못 매칭해 6065(=item51 값)를 저장했고, "기본자본(TFI표)"(50)·
  "보완자본(TFI표)"(51) 템플릿은 아예 매치 실패(행 미생성)했다 — KR0050 자신의 과거 12개
  분기(2023.2Q~2026.1Q)는 47-54가 전부 연속 공시돼 있어 이번 분기만의 국소 결함으로 보인다
  (근본원인은 `kics_disclosure_parser.py` 라벨매칭 — 공용 코드라 이 스크립트는 데이터만
  고치고 코드는 안 건드렸다). raw(L553-564, 백만원)로 직접 정정:
  `47=2007.83·48=2338.28(오염값 6065→정정)·49=4057.56·50=1048.87·51=6065.39·
  52=7114.26(반올림값 7114→정밀화)·53=0·54=0`. 자체검산 `item51==min(47,48)+49+54`:
  `6065.39 == min(2007.83,2338.28)+4057.56+0 == 6065.39` (정확히 닫힘, 쓰기 전 스크립트가
  자동 확인 후 진행).

### 3. `지급여력비율(경과조치 후)` 처리

**결론: 진짜 미공시가 맞다 — 0으로 채우지 않았고, 다른 표에도 값이 없다.** 원문 전수 확인:

- `[지급여력비율의 총괄]`(L410-423), `4-2-3 최근 3개년 변동요인`(L645-656) 표 모두 "경과
  후" 행이 당분기·-1분기·-2분기 **3개 분기 전부 공란**.
- `(1) 공통적용 경과조치 관련`(L549): **"해당사항 없음"** 명시, 표 자체가 "적용 전 경과조치"
  단일 컬럼만 존재(적용후 컬럼 없음).
- `(2) 선택적용 경과조치 관련`(L566): **"해당사항 없음"** 명시.
- `②③` 세부표(장수·해지·사업비·대재해, 주식·금리): "적용 전"에만 값, "적용 후"는 전부 공란.
- 금리민감도 절 각주: *"(경과조치 미신청 회사) 당사는 선택경과조치를 적용하지 않아 경과조치
  전·후 금액 및 비율이 동일함"*.
- `data/_derived/kics_transition_applicability.json` 레지스트리: KR0050은 2023.1Q부터
  2026.1Q까지 **13개 분기 연속** TFI/TAC/TIR/TER/TIRR/PCA_DEFER **전부 X**(RPT만 O, 보고
  기한 연장이라 재무수치 무관) — 이번 분기도 raw 표(L497-505)가 동일하게 확인.
- KR0050 자신의 직전 2개 분기(2025.4Q·2026.1Q) 마스터 실측: 공시된 모든 항목(1-46, 47-54
  포함)이 `값_적용후 = 값`으로 일관되게 미러링돼 있다 — 이 회사의 기존 관례.

판정: `_TRANSITION_APPLIERS`(18사 FSS 정본) 밖 비적용사 + 이번 분기 raw의 명시적 "해당사항
없음"×2 + 13개 분기 무중단 registry X + 회사 자신의 과거 관례 네 가지가 전부 일치하므로,
`값_적용후 = 값`으로 미러링(0 채움이 아니라 원문이 명시한 "전후 동일"을 반영)하는 것이 맞다고
판단해 처리했다. item27_후 = 152.13 (item1_후=7,114·item14_후=4,677도 동일 미러링). **단,
아래 4번 사유로 이 미러링은 마스터에 반영되지 않고 되돌려졌다** — 스크립트(`scripts/
fix_20260829_kr0050_2026q2_onboarding.py`)는 보존했으니 다음 재적재 때 그대로 재실행하면
된다.

### 4. K-ICS 게이트 + coverage census 충돌 → 마스터 삽입 보류

1차로 `kics_disclosure.json`에 KR0050 2026.2Q(54행, items 1-54)를 적재하고
`validate_kics_disclosure.py`를 돌린 결과, **KR0050 2026.2Q 버킷 자체는 findings 29건
전부 GREEN(26)/legit-SKIP(3), RED 0건**(report 전체를 코드/분기 키로 재귀 스캔해 findings
뿐 아니라 4개 구조게이트—census·parent-zero·breakdown·continuity—전부 확인, 히트 0).
item28·item47-54 픽스 전에는 rule 8(item28 결측)·rule 47_tier2_census(TIER2_PARTIAL_ROWS)
2건 RED였으나 위 3번 스크립트로 해소 확인.

**그런데 같은 실행에서 `Coverage census: MISSING_CELLS(RED)=36 collapsed_quarters=
[('2026.2Q', 1)]`이 새로 뜨는 것을 발견**했고, 거의 동시에 코디네이터가 `prepush_check.py`
전체 실행 결과(`PRE-PUSH VERDICT: gate RED=56 ... BLOCKED`, `validate_data_contract.py`
MISSING_FILER_CELL 38건)로 같은 문제를 지적했다. 직접 재현:

```
python scripts/validate_data_contract.py
RED [kics_disclosure] MISSING_FILER_CELL 메리츠화재해상보험 2026.2Q  (... 정규공시사 38개사 전부)
```

`validate_data_contract.py:317` 주석 그대로: *"No exception mechanism — every RED counts
(fix or owner-escalate, owner 2026-06-16)"* — 부분 분기(39사 중 1사)를 마스터에 넣지 말라는
설계라, KR0050 1사만 있는 2026.2Q를 적재하면 나머지 38사가 전부 "결측"으로 오검출된다(공시
안 낸 것과 못 뽑은 것을 게이트가 구분 못 하게 되는 상태 — 면제로 뚫으면 안 되는 이유).

**조치: `git checkout -- kics_disclosure.json`으로 되돌림.** 결과 확인:
- `kics_disclosure.json`: 22,742행 → **22,688행**(세션 시작 시점과 완전 동일, KR0050
  2026.2Q 행 0건).
- `validate_kics_disclosure.py`: **exit 0**, `MISSING_CELLS(RED)=0 collapsed_quarters=[]`.
- `validate_data_contract.py`: **exit 0**, `SUMMARY RED=0 YELLOW=92`(YELLOW는 전부 ifrs17
  레인의 기존 IFRS17_BS 항목, 무관).
- `pytest tests/test_kics_rules_golden.py`: PASS(재생성 불필요 — 마스터가 원상이라 골든
  대조 자체가 변화 없음).
- `pytest tests/test_post_transition_golden.py` · `test_rule_coverage_manifest.py` ·
  `test_identity_tautology.py` · `tests/unit/`(140개) · `test_deploy_assets.py` ·
  `test_master_tables_golden.py`: **전부 PASS**(151개 계).
- `scripts/validate_golden_input_fingerprints.py`: 되돌린 직후 `post_transition` 축만
  FAIL(`INPUTS_MOVED`, md_inbox 파일 496→497개 — 이번 세션이 새로 만든 KR0050 2026.2Q MD
  1개가 원인, kics_disclosure.json 쪽은 원상이라 무변화). golden(`test_post_transition_
  golden.py`)이 실제로는 byte-identical PASS임을 먼저 확인한 뒤(입력이 늘어도 산출은 안
  움직임 — 마스터에 그 회사·분기 행이 없어 반영할 곳이 없어서), `--update`로 지문 갱신 →
  재실행 **RED=0 clear**. 디프는 `post_transition` 항목 한 줄(496→497개·해시)만 바뀌고
  다른 5개 빌더(ifrs17_bs·pl_breakdown·viz_csm_waterfall·viz_ifrs17_panels·dividend, 전부
  동시작업 중인 ifrs17 레인 소관) 지문은 바이트 단위로 무변화 — 병행 세션 충돌 없음 확인.
- `insurequant_master_tables.xlsx`: 이번 세션에서 애초에 건드리지 않았음(git diff 없음) —
  되돌릴 것 자체가 없다.

### 5. 남긴 것 / 다음에 쓸 것

- **디스크에 영속(git 추적 안 됨, 그래서 안전)**: `data/disclosure/FY2026_Q2/{pdf,raw}/
  KR0050_하나손해보험.pdf`(downloader가 이미 배치), `data/disclosure/FY2026_Q2/parsed/` +
  `md_inbox/FY2026_Q2/KR0050_하나손해보험.md`(이번 세션이 docling으로 생성).
- **커밋 대상(코드/지문만, 마스터 데이터는 0)**: `scripts/fix_20260829_kr0050_2026q2_
  onboarding.py`(신규 — item28·47-54 정정 + 값_적용후 미러링, KR0050+2026.2Q 하드필터,
  `--dry-run` 지원, 재실행 시 자체검산 후 진행) · `tests/fixtures/builder_input_
  fingerprints.json`(post_transition 지문 갱신 1줄).
- **8/31(월) 이후 재적재 절차** (나머지 38사 중 상당수가 게시된 뒤): ①
  `fill_period_to_disclosure.py` → `fill_subitems_to_disclosure.py` →
  `fill_market_subitems_to_disclosure.py` → `fill_market_irr_from_pdf.py`(신규 게시사
  전체, KR0050 포함 재실행— idempotent라 KR0050도 안전) ② `scripts/fix_20260829_
  kr0050_2026q2_onboarding.py`(KR0050 item28·47-54·미러링 재적용 — 위 ①이 47-54는 다시
  틀리게 채울 수 있으니 순서상 ① 다음에 실행) ③ `validate_kics_disclosure.py` RED=0 확인
  ④ **이번에는 coverage census가 안 막을 것**(39사 대부분 동시 적재라 `collapsed_quarters`
  발생 안 함) ⑤ 골든 `--update` + `validate_golden_input_fingerprints.py --update`.

**2026.2Q는 39사 중 1사(하나손해보험)만 게시돼 마스터 삽입을 보류한다. 다음 확인 8/31(월).**
downloader가 나머지를 받아오면 그때 한 번에 넣는다.

커밋: `ff09b80` (`fix/csm-product-segmented-columns`, push 안 함) — `TODO_parser_kics.md` ·
이 티켓 · `scripts/fix_20260829_kr0050_2026q2_onboarding.py`(신규) ·
`tests/fixtures/builder_input_fingerprints.json` 4파일.
