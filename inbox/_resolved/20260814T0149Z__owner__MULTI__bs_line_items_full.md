---
from: owner
to: parser
created: 20260814T0149Z
status: resolved
route: backlog
company: MULTI
period: 2023.3Q-2026.1Q
lane: ifrs17
priority: LOW
iter: 1
---

## 미결 (sender 작성)

**BS 세부항목 추출. 우선순위 낮음 — `20260814T0149Z__owner__MULTI__q1_amendment_and_q2_priority.md`
(2026.2Q + 정정공시) 끝난 다음에 착수할 것.**

owner 지시 원문: *"high level BS 추출하는건 OpenDart API가지고 하면 수집도 쉽고, 솔직히 검증할
건덕지도 없이 쉬운 작업이야."* → **가볍게 간다. 지난 라운드처럼 부풀리지 말 것**(그건 발주문
잘못이었다, `20260814T0035Z` 참조).

### 할 일

`equity_composition.json`에 현재 BS 상위 9행(40-49)만 들어가 있다. **캐시 BS 행을 마저 담아라.**

- 소스: `data/dart/_fs_api_cache/*.json`의 `sj_div == "BS"`. **새로 받을 것 없다** —
  지금 쓰는 36개 항목이 이미 같은 응답에 딸려온 것이고, 안 쓰는 표준계정이 더 있다.
- 실측(오케스트레이터, OFS 255파일): distinct BS `account_id` **200개**,
  그중 아직 안 쓰는 것 중 빈도 20+ 가 **42개**. 상위 예시:
  유형자산(255) · 기타자산(250) · 기타부채(250) · 무형자산(236) · 투자부동산(234) ·
  충당부채(204) · 이연법인세부채(199) · 차입부채(168) · 파생상품자산(162)/부채(169) ·
  현금및현금성자산(153) · 사용권자산(134) · 리스부채(123) · 보험계약자산(111) ·
  계약자지분조정(96) · 사채(80) …
- **항목번호 배정 방식은 네가 정해라.** 기존 1-49와 안 겹치기만 하면 된다(50번대 이후 권장).
  내가 항목표를 다시 써서 넘기지 않는다 — 그게 지난번 실패 원인이다.
- 전부 **스톡** → `값 == 값_당분기`. 단위·기준(OFS/CFS)·9키 스키마는 기존 그대로.

### 딱 하나 주의 — 합산하지 말 것

상각후원가 계열이 회사마다 다르게 쪼개져 있다:
`dart_LoansAtAmortisedCost`(142) / `ifrs-full_FinancialAssetsAtAmortisedCost`(135) /
`dart_SecuritiesAtAmortisedCost`(117) / `dart_OtherFinancialAssetsAtAmortisedCost`(103)
— 회사마다 **다르게 조합**해서 쓴다. 이래서 항목 48이 예약만 되고 비어 있다.

**원행을 그대로 각각 담아라.** 소계를 만들지 말 것. 합치면 이중계상 난다
(한화손보가 합계태그·분리태그를 동시공시하던 P2-4 함정과 같은 부류).
소계가 필요하면 화면에서 하면 된다.

### 검증 — 새 룰 만들지 마라

owner가 "검증할 건덕지 없다"고 명시했다. 실제로 그렇다:

- 이미 있는 `EQ_BS_IDENTITY`(40 == 41 + 1, 40 == 49) 하나로 BS가 제대로 잡혔는지 판정된다.
- 세부행은 **census 코어에 넣지 말 것.** 회사마다 쓰는 계정이 다르니 결측이 정상이다.
- 잔차 역산 금지. 결측을 0으로 채우기 금지. 부모-자식 룰 신설 금지.
- validation에 별도 발주 안 한다. 필요해지면 그때 간다.

### 골든

`tests/test_equity_composition_golden.py`가 산출을 고정하고 있으니 **`--update`로 재생성 +
커밋 메시지에 이유** 적으면 끝. 새 골든 신설 불필요.

## 답변 (recipient 작성)

owner 취소(20260814T0216Z). 착수 안 함.
