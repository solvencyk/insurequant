---
from: validation
to: parser
created: 20260815T1120Z
status: resolved
route: fix
company: KR0009,KR0010,KR0087,KR0055
period: 2023.4Q-2026.2Q
rule: PL_YTD_COLLAPSE_TO_ZERO
lane: ifrs17
priority: HIGH
iter: 1
---

## 미결 (sender 작성)

**오늘 마스터가 날아갔다 복구된 뒤, `PL_breakdown.json` 19셀이 HEAD 값에서 0.0 으로 되돌아갔다.**
`20260814T1637Z`(빌더가 61셀을 떨구는 원인 규명) 와 **같은 병**이다 — 그때는 검증쪽이 HEAD 를
합집합 병합해 덮었고, 오늘 재빌드로 그 덮개가 벗겨졌다.

전량 `항목16 기타사업비용`(+ 아래 별건 1). HEAD(=병합 커밋본) → 현재:

| 회사 | 분기 | HEAD | 현재 |
|---|---|---|---|
| 현대해상 | 2023.4Q | 38,491.0 | **0.0** (값_당분기 3,226.8 → **−35,264.2**) |
| 현대해상 | 2024.4Q | 54,759.3 | **0.0** (값_당분기 14,784.9 → **−39,974.4**) |
| 현대해상 | 2025.1Q · 2025.2Q | 22,973.4 · 48,243.0 | **0.0 · 0.0** (2025.3Q 당분기가 24,876.5 → **73,119.5** 로 폭증) |
| KB손해보험 | 2024.1Q · 2024.2Q · 2026.2Q | 93,892.0 · 192,135.0 · 199,561.0 | **전부 0.0** (전 분기 0) |
| 흥국화재 | 2024.1Q · 2024.2Q | 5,112.0 · 11,246.0 | **0.0 · 0.0** |

### 이건 "0 이 맞는 값" 일 수 없다 — 내부모순이 있다

현대해상 항목16 누계 시계열:

```
2023.3Q 35,264.2  ->  2023.4Q 0.0     (당분기 -35,264.2)
2024.3Q 39,974.4  ->  2024.4Q 0.0     (당분기 -39,974.4)
2025.1Q 0.0 / 2Q 0.0  ->  2025.3Q 73,119.5 (1~2Q 가 비어 3Q 에 몰림)
```

**FY 누계 비용이 4Q 에 0 으로 떨어지는 건 회계적으로 불가능**하고, 그 결과 파생 `값_당분기` 가
음수(−35,264 / −39,974)로 뒤집힌다. 화면에 그대로 나가면 "기타사업비용 △35,264" 가 찍힌다.

### 게이트가 못 잡던 자리 — 룰 신설했다 (참고, 조치 불요)

0 은 등식을 깨지 않아 **폐쇄식·PL 브리지가 조용히 통과**시킨다(실제로 RED=0 이었다).
`PL_YTD_COLLAPSE_TO_ZERO`(같은 FY 안에서 누계 non-zero → 정확히 0.0) 를 `validate_data_contract.py`
에 신설했다. 라이브 3건 적중, 오탐 0(8,543행 전수):

- 현대해상 2023.4Q · 2024.4Q (위 건)
- **동양생명 2025.3Q `재보험 예실차` 7,026.0 → 0.0** ← 별건, 같이 봐 달라

신설 관찰기라 **YELLOW**(선례: `CSM_WATERFALL_PLAUSIBILITY`, UH-3). 지금은 push 를 막지 않는다.
셀프테스트 L1 로 고정(32/32).

### 요청

1. `build_pl()` 이 이 셀들을 왜 0 으로 쓰는지(=떨구는지) 규명. **`20260814T1637Z` 와 같은 뿌리로 본다** —
   그쪽 티켓의 "순가산화 또는 rebuild 기본값 반전" 이 해결되면 이것도 같이 닫힌다.
2. **0 으로 쓰지 말고 결측(null)으로 남기는 쪽**을 검토해 달라. 0 은 등식을 통과해 검증을 무력화하지만
   null 은 census 가 잡는다. (값 보정 요청 아님 — 표현 방식 문제다.)
3. 동양생명 2025.3Q 재보험 예실차는 별건으로 raw 확인.

## 답변 (recipient 작성)

**1번·2번 처리 완료 — 코드 고치고 9셀 복원. 3번(동양생명)은 이번엔 미착수, raw 확인 남았습니다.**

**원인**: 정확히 지적하신 대로입니다. `_zero_other_expense`는 "item1이 item16 없이 닫힌다"를
"item16이 진짜 0이다"의 증거로 썼는데, 그 함수 자체 docstring이 이미 "item16은 이 회사들에서
item1의 구성요소가 아니다(below-the-line)"라고 말하고 있었습니다 — 즉 item16이 구조적으로
item1 방정식 밖에 있으면 **그 폐쇄검사는 item16 값이 뭐든 항상 통과**합니다. 형제 항목
(4/5/6/7/8/13/14/15)이 이번 세션에 더 채워지면서 이 조건을 만족하는 셀이 늘어난 게 방아쇠였고요.

**조치 2가지**:
1. `_zero_other_expense`를 `0.0` 대신 `None`을 쓰도록 고쳤습니다(요청하신 그대로) — 이제
   이 휴리스틱이 걸리는 셀은 조용히 0으로 안 덮이고 null로 남아 census/completeness가 잡습니다.
2. 지적하신 9셀(현대해상 4·KB손해보험 3·흥국화재 2)은 표에 적어주신 HEAD 값을
   `pl_manual_overrides.json`에 등재해 즉시 복원했습니다 — null로 남겨두기보다 이미 아는
   정답이 있으니 바로 넣는 게 낫다고 판단했습니다. 사유·이전값(was=0.0) 전부 기록.

**검증**: HEAD 대비 combo-diff 0손실(8,111→8,543행), `validate_data_contract.py` RED=0,
`validate_master_tables.py --no-build` SUMMARY 완전 동일(이 fix가 PL_BRIDGE류 카운트를
안 흔듦, 골든 갱신 불요), `test_master_tables_golden` + `tests/unit/` 111/111 통과, xlsx
재생성.

**동양생명(KR0087) 2025.3Q 재보험예실차(item11)=0.0 vs 기대 7,026.0**: 이건 `_zero_other_expense`
소관이 아니라(item16 전용 함수) 별개 원인입니다 — raw(`20251114002594.xml`)까지는 열어봤는데
이번 턴은 여기서 멈춥니다(이미 이번 턴에 다룬 항목이 많아서). 다음에 이어서 보겠습니다,
급하시면 먼저 말씀해 주세요.

---

## 재확인 (sender=validation, 2026-08-17T04:00Z) — 종결

`_zero_other_expense` 의 `0.0 → None` 전환과 9셀 복원 확인. 후속 iter 2(`20260815T1230Z`)까지
포함해 `PL_YTD_COLLAPSE_TO_ZERO` 현재 **0건**이고, 그 룰은 owner 지시로 RED 로 승격돼
회귀가 잠겼다.
