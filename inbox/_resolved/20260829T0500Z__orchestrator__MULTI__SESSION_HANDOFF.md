---
from: orchestrator
to: parser
created: 20260829T0500Z
status: superseded
route: backlog
company: MULTI
period: MULTI
lane: ifrs17
iter: 1
---

## 세션 중단 핸드오프 (2026-08-28 저녁)

owner 가 자리를 비워 백그라운드 에이전트 3개를 **의도적으로 중단**했다. 마스터 무결성은
중단 직후 확인했다 — 8종 전부 정상 파싱, 반쯤 쓰인 파일 없음, 폐쇄식 깨짐 0건.

### 살아 있는 상태 (재개 전 이 값들이 그대로인지 먼저 확인할 것)

```
PL_breakdown.json  11,546행 · 항목32  356셀
KR0083 2024.3Q  item27 △2,652억 · item28 △53억 · item30 △5억   (DART API 부호반전 정정)
KR0032 2026.2Q  item6  △102억 · item7 △797억 · item11 +47억     (원수·재보험 예실차)
KR0070 2026.2Q  item6  +52.4억 · item11 10개 분기               (ABL)
IFRS17_BS.json  6,852행 (삼성생명 2024 4개 분기 별도 기준 정정 완료)
xlsx  12시트 (기본자본소진율·보완자본소진율·자본비율전망 신규 3종 포함)
```

### 중단 시점의 미완 작업 3건 — 디스크에 부분 작업이 남아 있다

| 티켓 | 어디까지 갔나 | 남은 것 |
|---|---|---|
| `20260828T1900Z` NH 재보험 item11 | **완료. `d95d801` 로 커밋됨** | 부호 규약(출재 방향)을 티켓 `## 답변`과 대조 |
| `20260829T0100Z` 자본 마스터 3종 | 시트 3개 반영 완료(미커밋) | `기본자본소진율` 100% 초과 13행의 `비고` 채우기 |
| `inbox/validation/20260829T0300Z` 지문 게이트 | `prepush_check.py`·`test_push_gate_wiring.py` 수정됨(미커밋) | 배선 완료 + **일부러 깨뜨려 push 가 막히는지 재현** |

미커밋 변경이 워킹트리에 그대로 있다(`git status` 로 확인). `.bak_20260828_*` 백업 파일들도
남아 있으니 정상 종료 후 정리할 것.

### 대기 중인 티켓 (착수 전)

- `20260829T0200Z` CSM상각 `공시분기` placeholder — as-of 채우기. xlsx 를 쓰므로 위 자본
  마스터 건이 끝난 뒤 시작.
- `20260828T2300Z` 미래에셋(KR0079) item6 — 조사 완료(경로 확인됨), 구현 대기.
  **발생 측을 `LIC열만` 으로 잡은 것이 NH 에서 지적된 함정과 같은 형태**라 열 선택 확인이 선행 조건.
- 서울보증보험 forward 커버리지 결측 — 신규 발견, 티켓 미작성.
- designer: 항목32 워터폴 반영 + 신규 3시트 다운로드 팝업 반영.

### 이 세션에서 확립된 것 (재개 시 반복하지 말 것)

1. **예실차는 3사 모두 뽑을 수 있었다.** "분리 불가" 로 닫혀 있던 근거는 NH 한 회사의 잘못된
   결론을 나머지에 확대 적용한 것이었다. NH·ABL 완료, 미래에셋 경로 확인.
2. **LOB·측정모형 판단은 주석 목차부터 뽑아라.** 목차를 안 보고 숫자부터 계산해서 같은 건에서
   세 번 연속 틀렸다. 상세는 `_resolved/20260828T1000Z` "실패 패턴" 절.
3. **손실요소배분은 4종 밖**(데이터로 확정, `_resolved/20260828T1400Z`).
4. **회사 산문 검증자료는 개념 범위가 다를 수 있다.** ABL 2024.4Q 산문 △270억 =
   4종 예실차 △11억 + 발생사고요소조정 △258억.
5. **gold override 가 옛 전제로 계산돼 있을 수 있다.** ABL item7 override 6건이 item6=0 전제였다.

## 답변 (recipient 작성 — 처리 후)


---

## 2회차 핸드오프 (2026-08-30, owner 장기 부재)

브랜치·라이브 모두 최신이고 게이트 clear. 마스터 11종 무결성 확인 완료(반쯤 쓰인 파일 없음,
폐쇄식 깨짐 0).

### 이 세션에서 확립된 것 — 재개 시 반복하지 말 것

1. **폐쇄식의 절반은 아무것도 검증하지 않는다.** `pl_bridge:3057P` 중 **구성상 참 1,608 · 진짜 1,135**.
   `item7 = item3−(4+5+6)`, `item12 = item8−(9+10+11)`, `item23 = item22−item24` 가 잔차 plug 라
   그 등식들은 구성상 참이다. **item5·6·9·10·11·19·23 은 어떤 차단 룰의 입력도 아니다**(변이시험
   탐지율 0%). SUMMARY 가 이제 이것을 인쇄하고 `test_rule_coverage_manifest.py` 가 박제한다.
   상세: `inbox/_resolved/20260829T2130Z__validation__MULTI__pl_eqs_constructive_tautology.md`.
2. **따라서 item6(예실차) 값은 폐쇄식으로 검증할 수 없다.** 이 세션이 3개사 50분기를 채웠는데
   실제로 작동한 검증은 전부 **독립 앵커**였다 — NH 보험수익 510,001 두 노트 일치 · 미래에셋
   3중 대사 594,378,172,139 · ABL 회사 산문 50억/3억 · 서울보증 소계 검산. **"폐쇄식 닫혔으니
   맞다" 로 판단하지 마라.**
3. **`test_identity_tautology.py` 를 PL 에 배선하지 마라.** 널모델이 등식 단위 반올림을 가정하는데
   PL 은 원÷1e6 이라 건전한 항등식도 잔차가 정확히 0 이다. 적용하면 9축 전부 RED 이고 excess
   1위가 하필 진짜 검산 축(EQ9)이다. **이 탐지기는 이 마스터에서 작동하지 않는다.**
4. **파싱 결함 다섯 부류가 나왔고 산술 검증으로는 하나도 안 잡힌다.** 라벨 변형 / 파일 경로
   (분기보고서는 `xml/` 하위) / 같은 문서 내 중복 표 중 손상 사본 선택 / **빈칸 압축으로 고정
   인덱스 밀림** / CFS·OFS 선택기가 정상 사본을 버림. 뒤 셋은 값이 20~55배 튀어서 우연히 잡혔다.
5. **회사 카테고리로 항목 유무를 단정하지 마라. 이 세션에서 세 번 틀렸다** — 미래에셋 "형식이
   달라 못 뽑는다"(라벨 튜플 하나로 해결) · ABL "산문과 안 맞으니 값 없다"(산문이 더 넓은 개념)
   · 서울보증 "보증 전문사라 LOB 분해 없다"(고유 5분류로 실재). 셋 다 원문을 열고서야 뒤집혔다.
6. **`0` 과 결측을 구별 못 하는 자리가 네 번 문제를 만들었다.** `assemble()` 의 owner 규칙
   (2026-06-08 "미공시 시 0표시")이 추출 실패 `None` 을 `0.0` 으로 바꿔 오염을 만든 사례가
   확인됐다. 그 규칙 재검토 제안 3안이 `inbox/_resolved/20260830T0000Z` 에 있다 — **owner 결정 대기.**

### 미완 / 대기

| 건 | 상태 |
|---|---|
| **2026.2Q 정기경영공시** | **8/31(월) 재확인.** 하나손보 1사만 게시돼 파싱·검증까지 끝났으나 마스터 삽입 보류(39사 중 1사면 census 가 38 RED). 재실행 스크립트 `scripts/fix_20260829_kr0050_2026q2_onboarding.py`. 주말 게시 0회 규칙이라 8/31 이 그 창의 유일한 영업일 |
| 코리안리 LOB 예외처리 | `lob_na` 축 신설·커밋 완료(`b2f9660`). 도메인 문서 §4.1b 표 추가 완료. **item2 라벨 제안은 미착수** |
| leg-coverage 잔여 22건 | 예별손해 2건은 후보 표까지 특정했으나 폐쇄식 불일치로 미확정(비워 둠). 나머지는 2023 분기라 저우선 |
| 출재 CSM 워터폴 | `CSM_waterfall.json` 에 출재 축이 없다. 별도 마스터로 뽑아야 하는 신규 과제 — 명문화만 됨 |
| `_csm_goldmap.py`·`_csm_status_matrix.py` | `QS` 에서 2026.2Q 명시 배제. 리포트 헬퍼라 push 는 안 막지만 그 리포트에 최신 분기가 없다 |
| plug 구조 제거 | owner 결정(2026-06-08) 사안. 제안까지만 |

### 살아 있어야 할 값 (재개 시 먼저 확인)

```
PL_breakdown.json 11,546행 · 항목32 356셀 · item3 폐쇄식 깨짐 0
KR0083 2024.3Q item27 △2,652억      KR0032 2026.2Q item6 △102억 · item11 +47억
KR0070 2024.4Q item6 586.0          KR0079 2025.4Q item6 = None (되살리지 마라)
KR0005 2026.2Q item2 79,459.0       KR0150 2025.4Q item13 △78.6억
게이트: RED=0 · 골든 입력지문 6/6 · lob_na 14NA/0BAD · tax22_src 282P/0F/74S
```

## 종결 (orchestrator, 2026-08-30) — status: superseded

이 노트는 2026-08-28 저녁 중단 시점의 인수인계였다. 열거된 항목이 전부 끝나 **더 들고 있을
이유가 없다.** 실측 확인:

- 미완 3건: NH 재보험 item11 `d95d801` 커밋됨 / 자본 마스터 3종 커밋됨 / 지문 게이트
  `0ebb0ca` 배선 + `668bb10` 로 실제 차단 재현까지 끝.
- "살아 있어야 할 값" 8줄 전수 재확인: PL 11,546행 · 항목32 356셀 유지, 나열된 셀 값 불변
  (`KR0079 2025.4Q item6 = None` 포함 — 되살아나지 않았다. 오늘 option 1 로 구조적으로
  고정돼 `_GOLD_CELL_OVERRIDE` 박제 없이도 재빌드가 None 을 재현한다).
- 6번 항목("`0` 과 결측을 구별 못 하는 자리")이 가리키던 owner 결정 대기는 **2026-08-30 에
  owner 가 option 1 을 승인**해 종결됐다(commit cd79127).
- 대기 티켓 목록은 전부 이 라운드에서 답변·종결됐다.

인수인계 노트를 활성 inbox 에 남겨 두면 매 세션 미결로 다시 읽힌다 — 이 스레드가 그 예다.
