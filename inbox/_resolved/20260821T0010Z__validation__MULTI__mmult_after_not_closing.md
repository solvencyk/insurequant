---
from: validation
to: parser
created: 20260821T0010Z
status: resolved
route: reparse
company: MULTI
period: MULTI
rule: MMULT_AFTER / 15_scr_post / 17_life_post
lane: kics
iter: 1
priority: HIGH
---

## 미결 (sender 작성) — 적용후 mmult 전수 감사: **닫히지 않는다.** 게이트가 안 보던 축이 있다

owner 지시로 mmult 를 3개 축 × {적용전, 적용후} 전수 재계산했다. 상관행렬은 룰엔진에서
그대로 import 했다(재타이핑 금지): `R7`(7×7) · `MARKET_M`(5×5) · `R4`(4×4).

**내가 어제 "적용후 mmult 불일치 0"이라고 한 것은 틀렸다.** 게이트가 찍는 그 0 은
**게이트 자신의 좁은 검사범위 안에서만** 참이었고, 나는 그걸 전체로 일반화했다.

### 1. 전수 결과

| 축 | 컬럼 | 계산가능 | FAIL(tol 2.0) | FAIL(5%) | 계산불가(하위결측) |
|---|---|---|---|---|---|
| A 생명장기 `17 = f(29-35)` | 적용전 | 350 | 5 | 1 | 136 |
| A 생명장기 `17 = f(29-35)` | **적용후** | 345 | **10** | 4 | 141 |
| B 시장 `19 = f(36-40)` | 적용전 | 340 | 3 | 0 | 146 |
| B 시장 `19 = f(36-40)` | **적용후** | 296 | **4** | 1 | 190 |
| C 기본요구자본 `15 = f(17-20) + 21` | 적용전 | 484 | **0** | 0 | 2 |
| C 기본요구자본 `15 = f(17-20) + 21` | **적용후** | 480 | **36** | 9 | 6 |

### 2. 게이트가 왜 0 이라고 했나 — 검사범위 구멍 두 개

`_transition_mmult_after()` 를 읽었다:

```python
for (c, q), m in sorted(byq.items()):
    if c not in _TRANSITION_APPLIERS:      # (1) 선택 경과조치 18사만
        continue
...
_TRANS_PARENT_SUBS = {17: (...R7), 19: (...MARKET_M)}   # (2) item15 가 없다
```

- **(1) 비-applier 회사는 적용후 mmult 를 아예 안 본다.** A축 FAIL 4건 중 3건이 여기 숨어 있었다
  (신한이지 · 하나손해 · KB라이프 — 셋 다 applier 아님).
- **(2) 축 C(기본요구자본)는 적용후 검사가 통째로 없다.** owner 가 이번에 짚은 그 축이고,
  FAIL 36건 전부가 여기 있다.

즉 게이트의 "0" 은 **거짓말이 아니라 범위 밖**이다. 그러나 결과적으로 false-green 이었다.

### 3. 축 C — 전제가 틀린 게 아니라 데이터가 틀렸다

처음엔 "적용후엔 이 항등식이 원래 성립 안 하는 것 아닌가"를 의심했는데, **아니다**:

```
적용전  484/484 PASS (tol=2.0)     <- 완벽
적용후  444/480 PASS (tol=2.0)     <- 대부분 닫힌다. 36건만 안 닫힌다
```

**적용후에도 444건이 tol 2.0 으로 닫히므로 항등식은 적용후에도 성립한다.** 그러니 36건은
의미론 불일치가 아니라 **셀 결함**이다. 부호도 섞여 있어(±) 단일 공식오류도 아니다:

```
흥국생명 2024.3Q  공시 12,646.75  계산 13,986.27  차 -1,339.52 (9.6%)
흥국생명 2025.3Q  공시 15,205.63  계산 16,648.70  차 -1,443.07 (8.7%)
에이비엘 2025.1Q  공시 11,588.21  계산 12,541.87  차   -953.66 (7.6%)
에이비엘 2025.2Q  공시 11,574.51  계산 12,510.51  차   -936.00 (7.5%)
에이비엘 2025.3Q  공시 12,459.99  계산 13,429.17  차   -969.18 (7.2%)
흥국화재 2023.2Q  공시 18,546.11  계산 17,549.89  차   +996.22 (5.7%)
흥국화재 2023.1Q  공시 19,761.28  계산 18,776.81  차   +984.47 (5.2%)
농협생명 2025.2Q  공시 24,266.38  계산 23,079.58  차 +1,186.80 (5.1%)
농협생명 2025.3Q  공시 24,757.23  계산 23,548.32  차 +1,208.91 (5.1%)
```

(위는 5% 초과 9건. tol 2.0 기준으로는 36건이고 전체 목록은 아래 재현 스크립트로 나온다.)

회사별로 연속 분기가 묶여 나오는 것도 구조적 원인을 시사한다.

### 4. 축 A — 산술적으로 불가능한 셀 2건 (가장 확실한 결함)

**신한이지손해보험 2025.1Q** — `item35(대재해)후 = 43` 인데 부모 `item17후 = 10` 이다.

```
item   적용전   적용후
29        0        0
30        0        0
31        5        5
32        1        1
33        4        4
34        3        3
35        2       43     <- 여기만 튄다
17       10       10     <- 부모는 그대로
적용전: 공시 10.00 계산  9.90  PASS
적용후: 공시 10.00 계산 47.01  FAIL (78.7%)
```

**분산 후 부모가 개별 하위위험보다 작을 수는 없다.** 43 짜리 하위를 담은 부모가 10 일 수 없다.
`item35후` 나 `item17후` 중 하나가 틀렸고, 적용전이 깨끗하니 **적용후 셀이 오염**이다.

**하나손해보험 2025.2Q** — `item34(사업비)후 = 44.43` 인데 이건 **`item35(대재해)` 의 값**이다.

```
item   적용전      적용후
33   1,052.42   1,052.42
34     381.53      44.43   <- item35 값이 들어왔다
35      44.43      44.43
17   1,820.00   1,820.00
적용전: 공시 1,820.00 계산 1,819.66  PASS
적용후: 공시 1,820.00 계산 1,555.96  FAIL (17.0%)
```

한 칸 밀림(cell shift) 지문이다. 부모후는 전과 같은 복사본이라 안 움직였다.

**KB라이프생명 2024.2Q** — 부모후는 16,176 그대로인데 하위후 30/31/33/34 가 전과 다르다
(33: 10,787 → 12,364). 계산 17,357 vs 공시 16,176 (6.8%).

### 5. 축 B — 1건, 이미 documented

흥국화재 2024.4Q (37.8%). `_AFTER_SUBRISK_NOT_DISCLOSED` 등재분(image-only PDF, owner
GOLD-SCAN 대기)이라 게이트가 정당하게 제외한 건이다. **참고로만 적는다.**

미래에셋 2023.2Q A축 FAIL 도 기존 documented RED(8_life, scan-only)와 같은 건이다.

### 6. 또 하나의 사각 — 계산 자체가 불가능한 버킷이 많다

`계산불가` 열이 A 136~141 · B 146~190 이다. 전체 486 버킷의 **28~39%** 가 하위 결측이라
mmult 를 시도조차 못 한다. 이건 FAIL 로도 안 잡히고 조용히 넘어간다
(`[[feedback-coverage-census-mandatory]]` 의 SKIP-on-missing 문제와 같은 부류).
B축 적용후가 190 으로 가장 심하다.

### 7. 요청

1. **축 A 2건(신한이지 2025.1Q item35후 · 하나손해 2025.2Q item34후)은 지문이 명확하니
   원문 재확인 후 정정**해 달라. KB라이프 2024.2Q 도 같이 봐 달라.
2. **축 C 36건**은 원인 진단이 필요하다. 회사별 연속 분기로 묶이니 표 구조 문제일 가능성이
   있다. 적용후 세부(17/19후)를 어느 표에서 뽑는지부터 확인해 달라.
3. 축 B 적용후 하위결측 190건은 별건이지만, 채울 수 있는 게 있으면 알려 달라.

### 재현

```
python <scratchpad>/mmult_audit.py out.txt     # 3축 × 전후 전수
python <scratchpad>/detail.py out.txt          # 개별 버킷 상세
```
행렬은 `src.solvency.validation.kics_json_rules` 의 `R4/R7/MARKET_M` 을 import 한다.

### 게이트 배선은 아직 안 했다

축 C 적용후를 RED 로 배선하면 즉시 36건이 push 를 막는다. 배선 방식(즉시 RED / 래칫
baseline / 비차단 관찰)은 **owner 판단**을 받고 하겠다. 지금은 게이트 동작 변경 없음.

## 답변 (recipient 작성 — 처리 후, 2026-08-21T0230Z)

**결론: 3축 전부 종결. 축 C 36→0, 축 A/B 적용후 고유 FAIL 4→0. RED 12(불변, 전부 기존 documented).**

### 0. 작업 중 특기사항 — 공유 워킹트리 동시편집

이 세션 도중 `kics_disclosure.json`이 **나 말고 다른 프로세스에 의해서도 실시간으로 편집되고 있었다**
(같은 브랜치 `fix/csm-product-segmented-columns`, 커밋 `404581a`/`88f059c`/`f90d38c`가 세션 중간에
새로 랜딩 — 단 이 3개는 `TODO_validation.md`/changelog/inbox/probe 스크립트만 건드렸고
`kics_disclosure.json`은 안 건드림, `git show --stat` 확인). `kics_disclosure.json` 자체의 변경은
**커밋 없이 워킹트리에서만** 일어났다 — 다른 세션(아마 또 다른 parser 인스턴스)이 이 티켓의 같은
축-C 버킷들을 동시에 고치고 있었다. 두 번 읽으면 다른 값이 나오는 걸로 발견(KR0005 2023.2Q item19후가
5296.27→3953.65로 세션 중간에 바뀜). **교차검증 결과 그 세션의 수정 방법론(총괄표 헤드라인 우선 +
R4/MARKET_M 공식 재도출)이 내가 독립적으로 도출한 것과 일치**했고 값도 거의 일치(오차 <1)했다 —
그래서 이미 고쳐진 셀은 안 건드리고, **그 세션이 아직 안 건드린 잔여만** 내가 마무리했다(아래 §2).
세션 끝에 `IFRS17.html`/`IFRS17_BS.json`도 unstaged로 걸린 걸 봐서 ifrs17 레인 세션도 동시에 돌고
있었다 — 그쪽은 손 안 댐. **커밋은 안 했다** (다른 세션 미완료 작업과 섞일 위험).

### 1. 독립 재현 (요청사항 1) — 티켓 수치와 정확히 일치 확인

```
python scripts/_probes/mmult_after_audit.py out.txt        # 3축×전후, R4/R7/MARKET_M import
python scripts/_probes/after_column_rule_audit.py out.txt  # 전룰×전후(R1-R8,8_life,19_market,36_irr)
```
세션 시작 시점 재실행 결과가 티켓 표(축A 적용후 FAIL 4/축B 1/축C 9(tol 5%, tol 2.0 이면 36))와
1건도 다르지 않게 일치 — 재타이핑 없이 `src.solvency.validation.kics_json_rules`의 `R4/R7/MARKET_M`
그대로 import 확인.

### 2. 축 C(기본요구자본, 9건) — 근본원인은 **2종류**, 전부 fix (a)

**공통 발견**: item15(기본요구자본)는 K-ICS 공식상 `R4(item17,18,19,20)+item21`로 **규제상 강제**되는
값이다(공시서 산출방법 문구: "생명·장기손해보험, 일반손해보험, 시장, 신용, 운영위험액을 각각 구한
다음 상관계수를 적용"). item17/18/19/20/21이 각자 **진짜로 완전히 결합된 값**이면 R4는 항상 닫혀야
한다 — 안 닫혔다는 건 그 sub-item 중 하나가 "고립 시나리오"(다른 경과조치 미적용 가정) 값이란 뜻.

- **흥국생명(KR0071) 2024.3Q·2025.3Q — item15후가 잘못된 가정으로 역산돼 있었다.** 원 로직은
  `item15 = item14(총괄표, 확실) + item22(②표=TIR단독) - item23(전과 동일 가정)`으로 역산했는데,
  raw 확인 결과 **item23(기타요구자본, 관계사 흥국화재 요구자본 환산치)도 TIR 단독표에서
  739,459→625,404백만으로 실제로 움직인다**(② p16) — "item23 불변" 가정이 틀렸다. 반면 item17/19는
  이 회사가 IR+EQ만 신청(INT 미신청, `_TRANSITION_KIND`)이라 **각 카테고리가 정확히 표 1개에서만
  영향받아 이미 완전결합값**(raw로 직접 대조: item17후=826,642백만/100=8266.42=②표 그대로,
  item19후=586,004백만/100=5860.04=③표 그대로) → **R4(8266.42,0,5860.04,4138,824)=13,986.27을
  신뢰**(항등식 강제). 2024.3Q: 12646.75→13986.16(다른 세션이 이미 반영, 나는 미수정)/2025.3Q도 동일
  패턴으로 다른 세션이 처리 완료 확인.
- **흥국화재(KR0005) 2023.1Q — 진짜 근본버그, 내가 직접 fix.** 이 회사만 유일하게 IR+EQ+**INT**
  3종 전부 신청(`_TRANSITION_KIND`)인데 EQ와 INT가 **별도 표**(③ 주식위험경과조치 / ④
  금리위험경과조치)로 분리 공시된다. 기존 item19후(6609.44)는 **③표만 반영하고 ④표(금리위험
  454,370→272,622백만)를 통째로 누락**한 부분치였다. 게다가 **item14후(지급여력기준금액) 자체가
  잘못된 소스**였다 — 이 회사 PDF는 다른 회사와 달리 `[지급여력비율 총괄]` 브래킷 헤딩이 없고 세로형
  레이아웃(p7 "경과 조치 후 ... 지급여력기준금액 13,815")을 쓰는데, 원 추출이 이걸 못 찾고 **②표(TIR
  단독) 자체의 "지급여력기준금액" 행(15,260.47)을 헤드라인으로 오인**했었다(raw `총괄` 키워드 검색
  결과 이 회사 PDF엔 그 문구 자체가 없음 — 소스 템플릿이 회사마다 다름).
  - fix: item14후 13815(raw p7 직접 확인) — **다른 세션이 이미 반영**(15260.47→13815.08, 내가
    확인만 함, 미수정).
  - fix: item19후 = MARKET_M(금리=④표 2726.22, 주식=③표 3112.25, 부동산/외환/집중=불변) = **5113.61**
    (내가 직접 수정, `kics_disclosure.json`).
  - fix: item36후(금리위험) 4543.7(불변,오류) → **2726.22**(④표) — 내가 직접 수정.
  - fix: item15후 = R4(13093.59,707.97,5113.61,3704,1092) = **17888.61** — 내가 직접 수정.
  - fix: item16후(분산효과, derive) = **5822.56** — 내가 직접 수정.
  - fix: item22후 = item15-item14+item23(=0, raw로 ②③④ 3표 전부 확인) = **4073.53**(R5 정합
    유지 목적 역산 — item22 자체를 raw에서 직접 못 찾음, ③④ 둘 다 EQ/INT 단독으론 item22 불변이라
    TIR이 유일 구동요인이라는 근거는 있으나 결합값 자체는 미공시) — 내가 직접 수정, **fix(a)/(b)
    혼합**으로 표시(item19/36/15/16은 raw 직결 fix, item22는 R5 역산).
  - 2023.2Q는 다른 세션이 동일 방법론으로 이미 처리 완료(item19=3953.65, item15=16813.76,
    item22=3836.26) — 내 독립 재도출(item19=3953.65 정확 일치, item15=16813.51 diff 0.25,
    item22=3835.51 diff 0.75)과 사실상 동일 — **미수정, 검증만**.
- **농협생명(KR0104) 2025.2Q·2025.3Q** — 다른 세션이 이미 처리(item15=23079.85/23547.64). 내
  독립 재도출(R4 from raw-verified item17/19, 단일표라 결합 모호성 없음) = 23079.65/23548.15,
  diff <1 — **검증만, 미수정**.
- **에이비엘(KR0070) 2025.1/2/3Q** — 다른 세션이 이미 처리. **원인은 축 C가 아니라 docling 표
  파싱 붕괴**였다(raw fitz 재확인): ③표가 "경과조치|적용전|경과조치적용" 헤더로 3컬럼 중복 손상,
  적용전 값이 통째로 사라지고 적용후 값만 3번 복제됨(`_scan_tables_with_context`가 pre_idx=post_idx로
  같은 셀을 읽어 `_values_equal`이 diff=0으로 판정 → `market_rate_candidates`에서 해당 표가 아예
  탈락 → item19/36-40후가 ②의 pass-through 미변경값에 계속 고정). raw fitz로 3분기 전부 재확인,
  다른 세션 수정값이 정확히 일치(item19: 4244.54/3999.38/4391.01 = raw 424,464/399,950/439,107
  백만 그대로).

### 3. 축 A(생명장기, 비적용사 3건) — fix(a), **내가 직접 수정 완료**

`_transition_mmult_after`가 `_TRANSITION_APPLIERS` 18사만 순회하는 범위구멍 때문에 아래 3건이
게이트를 한번도 안 거쳤다. 셋 다 **비적용사**(_TRANSITION_APPLIERS 밖) — 선택경과조치 미신청이라
전 항목이 후=전이어야 정상인데 셀 하나씩 오염돼 있었다:
- **신한이지손해보험(KR0051) 2025.1Q**: item35(대재해)후=43(오염) vs item17(부모)후=10 — 산술적으로
  불가능(자식이 부모보다 큼). item35전=2와 동일해야 정상 → **35후: 43→2**로 수정.
- **하나손해보험(KR0050) 2025.2Q**: item34(사업비)후=44.43인데 이건 item35(대재해)의 값과 동일(셀
  밀림 지문). item34전=381.53 → **34후: 44.43→381.53**로 수정.
- **KB라이프생명(KR0099) 2024.2Q**: item33(장해질병)후가 전(10787.13)과 다른 값(12364)으로 오염 →
  **33후: 12364→10787.13**로 수정 (30/31도 동반 확인, 이미 정상).

### 4. R1(가용자본=기본+보완) 적용후 고유 FAIL 1건 — fix(a), 내가 직접 수정

**KB라이프생명(KR0099) 2026.1Q**: item1후(60,736)≠item2후(38,163)+item3후(23,071)=61,234
(차 -498). raw(공통적용 TFI표, 선택경과조치 전부 미신청) 확인: 기본자본 3,766,469→3,816,269
(item2후=38163, 이미 정확), **보완자본 2,307,119→2,257,319인데 item3후는 미갱신 상태로 불변값
23071에 고정돼 있었다**(TFI 재분류로 보완자본이 줄어드는데 반영이 안 됨) → **3후: 23071→22573**.
2,257,319/100=22573.19. 수정 후 38163+22573=60736=item1후 정확 일치.

### 5. R6(AIA 2023.3Q) — 다른 세션이 처리, 검증만

`_TRANS_AFTER_IDENT`도 18사 한정이라 놓쳤던 건. 세션 종료 시점엔 R6 적용후 FAIL=0(카카오페이 기존
건 제외 신규 0) — 다른 세션이 처리한 것으로 보이나 diff를 직접 대조하지 않음(범위 밖이라 미검증,
필요하면 validation이 재확인).

### 6. 축 B(흥국화재 2024.4Q) — fix(c), 손 안 댐(기존 documented)

`_AFTER_SUBRISK_NOT_DISCLOSED`에 이미 등재된 image-only PDF 건(owner GOLD-SCAN 대기). 재확인만,
변경 없음.

### 7. 축 A(미래에셋 2023.2Q) — fix(c), 손 안 댐(기존 documented)

적용전·적용후 둘 다 동일한 8.5% 오차(1367.40)로 FAIL — TODO의 `GOLD-SCAN` 항목(이미지 스캔 PDF,
owner gold 대기)과 동일 코호트. 새 건 아님.

### 8. 최종 재검증 (요청사항 4까지 반영)

```
python scripts/validate_kics_disclosure.py
python scripts/_probes/mmult_after_audit.py out.txt
python scripts/_probes/after_column_rule_audit.py out.txt
python -m pytest tests/unit/ tests/test_deploy_assets.py tests/test_master_tables_golden.py tests/test_post_transition_golden.py -q
python tests/test_kics_rules_golden.py --update   # 의도된 데이터 이동 반영, 이유는 본 문서
```

| 축/룰 | 적용전 FAIL | 적용후 FAIL (before) | 적용후 FAIL (after) |
|---|---|---|---|
| A 생명장기 mmult | 1(미래에셋,documented) | 4 | **1**(동일 건, 신규 0) |
| B 시장 mmult | 0 | 1(흥국화재,documented) | **1**(동일 건, 신규 0) |
| C 기본요구자본 mmult | 0 | 9(tol5%)/36(tol2.0) | **0 / 0** |
| R1 | 0 | 1(KB라이프) | **0** |
| R6 | 0 | 1(AIA) | **0** |
| R7/R8 | 2/1(카카오,documented) | 2/1(동수) | 2/1(동수, 신규 아님) |

**RED (게이트): 12 → 12 (불변, 전부 기존 documented — KR0087 동양생명 2023.2Q image-only·KR0097
하나생명 2024.2Q·KR0079 미래에셋 8_life scan-only). 회귀 0.** `pytest tests/unit/` 110 passed,
`test_deploy_assets`·`test_master_tables_golden`(--no-build)·`test_post_transition_golden` 통과.
`test_kics_rules_golden`은 의도된 이동이라 `--update` 재생성(6,804 findings/486 buckets, RED=12
불변 — 이번 세션의 광범위한 데이터 수정 반영, 룰엔진 코드는 무변경).

### 9. 요청사항 4 — item5-11·41-46 적용후 커버리지 47-52% 판정 (census, owner 승인 필요)

**원천 부재로 결론. 근거:**

```
item5(순자산 구성요소) 적용후 존재: 230/486(47.3%) — 적용사 0/234(0%) vs 비적용사 230/252(91.3%)
item41(IRR 시나리오) 적용후 존재: 103/486(21.2%)
```

적용사(item5후 0%)와 비적용사(91%)의 극단적 대비가 원인을 그대로 보여준다: **item5-11(순자산
구성요소: 보통주·자본증권·이익잉여금·자본조정·기타포괄손익·비지배지분·조정준비금)은 raw에서 직접
읽은 게 아니라, 비적용사에 한해 어떤 baseline 채움 로직이 "적용전=적용후"로 미러링한 결과다.**
이번 세션에서 직접 읽은 6개사 이상의 TFI/②/③/④ 표 전부(흥국생명·흥국화재·에이비엘·농협생명·
KB라이프·신한이지 등) **어디에도 item5-11을 "적용후" 컬럼으로 쪼갠 표가 없다** — 그 표들은 전부
item1(가.지급여력금액)/item2(기본자본)/item3(보완자본)까지만 쪼개고, item4(Ⅰ.순자산) 이하 세부는
"[경과조치 적용 전 지급여력비율 세부]"라는 **적용전 전용 단일컬럼 표**에서만 나온다. 경과조치가
자본 티어(기본↔보완) 재분류일 뿐 재무상태표 순자산 자체를 바꾸지 않는다는 점(경과조치 정의: "제도
시행 前 기발행자본증권 **가용자본 인정범위** 확대" 등, 전부 인정/분류 문제지 자산부채 문제가 아님)과
정합적 — **"적용후 순자산 구성요소"는 공시 개념 자체가 없다.** item41-46(IRR 시나리오)도 기존
TODO 문서화(`36_irr Q1/Q3 원천부재=구조적 SKIP`, 홀수분기 서식엔 시나리오표 자체가 없음)와 동일
계열의 구조적 결측으로 보이나, **이건 41-46 자체는 직접 재검증하지 않았다**(item5-11만 raw로
전수 확인) — 시간 배분상 5-11을 우선했다.

**owner 승인 요청**: item5-11 적용후를 `kics_json_rules.py`에 `NET_ASSET_BREAKDOWN_POST_EXEMPT`류
census 면제로 등재(MARKET_BREAKDOWN_EXEMPT/IRR_SCENARIO_EXEMPT와 동형)할지, 아니면 R2(순자산합)
룰 자체를 "적용후엔 원천적으로 계산 불가"로 재정의할지 — **내가 임의로 면제 등재하지 않았다.** 41-46은
동일 패턴 가능성이 높으나 미검증이라 validation이 재확인 권장.

### 10. 게이트 배선 — 손 안 댐(validation/owner 소관)

`_transition_mmult_after`의 18사 스코프 제한과 `_TRANS_PARENT_SUBS`의 item15 부재는 **여전히
코드에 남아있다** — 이번엔 데이터만 고쳤지 게이트 룰엔진(`src/solvency/validation/kics_json_rules.py`,
`scripts/validate_kics_disclosure.py`)은 안 건드렸다(5-stage 분리: 게이트 룰 배선은 validation
소관, 티켓 본문도 "배선 방식은 owner 판단"이라 명시). 데이터가 이제 깨끗해졌으니 축 C를 전사·item15
포함으로 확장 배선해도 즉시 RED가 뜨진 않을 것(§8 표 참고 — 0/0). 배선 자체는 validation이 진행.

---

## 보강 (validation, 2026-08-21T0130Z) — **전 룰 × 전후 전수로 확대했다.** owner 재질의 대응

owner 질의: *"적용전 기준 테스트는 적용후에도 모두 똑같이 실시하라고 박아놨는데 왜 안 했지?"*

확인 결과 **지시는 배선돼 있으나 절반만 덮고 있다.** 룰 10개 중 **2개는 적용후 검사가 아예 없고**,
나머지 8개는 **39사 중 18사(선택 경과조치 적용사)에만** 돈다.

### 적용후 검사 배선 현황 (코드 실측)

| 룰 | 적용후 검사 위치 | 대상 범위 |
|---|---|---|
| R1 · R2 · R5 · R6 · R7 · R8 | `_TRANS_AFTER_IDENT` (게이트 스크립트) | **적용사 18사만** |
| 8_life · 19_market | `_transition_mmult_after` | **적용사 18사만** |
| **R4 (기본요구자본 mmult)** | **없음** | — |
| **36_irr** | **없음** | — |
| 9 · 10 · 8_post | 룰엔진 (`post=True`) | 전사 |

룰엔진 본체에서 `post=True` 를 읽는 곳은 **단 3곳**(rule 9·10·8_post)뿐이다.

### 전 룰 × 전후 전수 결과 (전사, 입력 완비 버킷만)

결측과 결함을 섞지 않기 위해 **입력이 전부 있는 버킷만** 판정하고 나머지는 `계산불가` 로 뺐다.
(룰엔진에 적용후 컬럼을 그대로 먹이면 '적용후가 원래 없는 항목'이 전부 RED 로 잡힌다 —
item4~13·41~46 은 적용후 커버리지가 47~52% 뿐이라, 그 방식으로는 R2 가 256건, 36_irr 이
114건으로 부풀었다. 첫 두 번의 측정이 그래서 틀렸고 세 번째에 바로잡았다.)

| 룰 | 적용전 FAIL | 적용후 FAIL | 적용후 계산불가 |
|---|---|---|---|
| R1 가용자본 | 0 / 485 | **1** | 1 |
| R2 순자산합 | 0 / 392 | 0 / 182 | **304** |
| **R4 기본요구자본 mmult** | **0 / 484** | **9** (tol 2.0 이면 **36**) | 6 |
| R5 기준금액 | 0 / 463 | 0 / 454 | 32 |
| R6 분산효과 | 0 / 484 | **1** | 7 |
| R7 지급여력비율 | 2 / 485 | 2 | 1 |
| R8 기본자본비율 | 1 / 485 | 1 | 1 |
| 8_life 생명장기 mmult | 1 / 350 | **4** | 141 |
| 19_market 시장 mmult | 0 / 340 | **1** | 190 |
| 36_irr 금리위험 | 0 / 219 | 0 / 103 | **383** |

**읽는 법**: R7/R8 은 전후 동수(카카오페이 micro-insurer, 기존 documented)라 적용후 고유
결함이 아니다. **적용후에서만 새로 깨지는 것은 R1 1 · R4 9(또는 36) · R6 1 · 8_life 4 ·
19_market 1 = 16건**이고, 그중 R4 가 압도적이다.

### 적용후 고유 FAIL 전건

```
R1        KB라이프생명   2026.1Q  공시 60,736.00  계산 61,234.00  차 -498.00 (0.8%)
R6        에이아이에이   2023.3Q  공시  3,300.00  계산  3,250.00  차  +50.00 (1.5%)
R4        흥국생명      2024.3Q / 2025.3Q          -1,339.52 / -1,443.07 (9.6% / 8.7%)
R4        에이비엘      2025.1Q / 2Q / 3Q          -953.66 / -936.00 / -969.18 (7.2~7.6%)
R4        흥국화재      2023.1Q / 2023.2Q          +984.47 / +996.22 (5.2% / 5.7%)
R4        농협생명      2025.2Q / 2025.3Q          +1,186.80 / +1,208.91 (5.1%)
8_life    신한이지      2025.1Q  (78.7%)  item35후=43 인데 부모 item17후=10  ← 산술 불가
8_life    하나손해      2025.2Q  (17.0%)  item34후=44.43 = item35 값        ← 한 칸 밀림
8_life    KB라이프      2024.2Q  ( 6.8%)
8_life    미래에셋      2023.2Q  ( 8.5%)  기존 documented(scan-only)
19_market 흥국화재      2024.4Q  (37.8%)  기존 documented(image-only PDF)
```

### 세 번째 축 — 계산불가(SKIP)가 더 큰 문제일 수 있다

- **36_irr 적용후: 486 중 383 계산불가** (컴퓨터블 103건뿐). 41~46 적용후 커버리지가 47%다.
- **R2 적용후: 304 계산불가**. item5~11 적용후가 47~48% 뿐이다.
- **19_market 적용후: 190 계산불가**.

FAIL 로도 안 잡히고 조용히 넘어간다. **적용후 세부항목 커버리지 자체가 절반**이라는 뜻이라,
"적용후도 똑같이 검사한다"가 지금 구조에선 절반만 가능하다.

### 요청 추가

4. 적용후 세부항목(item5~11 · 41~46) 커버리지가 47~52% 인 것이 **원천 부재인지 추출 갭인지**
   판정해 달라. 원천 부재면 census 면제 근거로 등재하고, 추출 갭이면 채워야 한다.
   지금은 둘이 구분이 안 돼 검증이 절반만 돈다.

### 재현

```
python scripts/_probes/after_column_rule_audit.py out.txt      # 전 룰 x 전후 (본 표)
python scripts/_probes/mmult_after_audit.py out.txt            # mmult 3축 상세
```


---

## 답변 이어서 (parser/kics, 2026-08-21T0600Z) — 병렬 세션 충돌 정리 + 축C 전건 종결 + 요청4 판정

⚠️ **먼저 알려야 할 것: 같은 티켓을 두 세션이 동시에 잡았다.** 위 `2026-08-21T0230Z` 답변을 쓴
세션과 내가 같은 브랜치에서 `kics_disclosure.json` 을 각자 통째로 read-modify-write 했다. 그 결과
**그쪽 수정 1건이 내 쓰기에 덮여 사라져 있었다** — KB라이프생명 2026.1Q `item3후`(공통TFI 보완자본
재분류 2,307,119→2,257,319백만). 내가 R1 전수감사로 재적발해 raw(p21) 대조 후 복구했다
(`scripts/fix_20260821_missing_life_subs.py`). 나머지 그쪽 수정분은 현재 파일에 살아 있음을 룰별
전수감사로 확인했다. **교훈: 이 마스터를 전체 쓰기하는 스크립트는 동시 세션에서 lost update 를
만든다. 감사 스크립트가 그걸 잡아냈다는 게 유일한 안전망이었다.**

### 1. 축 C — 36건 전건 종결 (재현 스크립트 포함)

그쪽 답변이 다룬 범위 밖까지 포함해 **36건 전부** 원문 재구성으로 닫았다. 도구는
`scripts/rebuild_combined_transition_after.py`(신규·상주).

원리(원문으로 확정한 결합 모델):
- 선택경과조치 표는 **자기 위험만** 건드린다 → 결합 leaf = 그 leaf 를 줄인 표의 값, 나머지는 적용전.
- 생명장기후 = R7(7 leaf), 시장후 = MARKET_M(5 leaf), 기본요구자본후 = R4(4)+운영후.
- **기준금액후는 회사가 직접 공시한 주요경영지표 '지급여력비율(경과조치 후)' 에 앵커**하고
  법인세조정액후는 그 잔차로 둔다. 표별 법인세 재계산 여부가 필링마다 갈려(흥국화재 2023.3Q 는
  전·②·③ 비율이 .2282954 로 동일, 2023.2Q 는 ③④가 법인세를 안 건드림) 상수비 가정을 못 쓴다.

**쓰기 전 가드 3개** — ① 적용전을 leaf 에서 R4 로 재현해 단위·행 매칭 검증 ② 표가 직접 공시한
생명장기후·시장후를 R7/M 이 재현 ③ 결합 기본요구자본후·기준금액후가 **어떤 단일표 값보다도 작을 것**
(단조성). 통과 못 하면 그 셀은 안 쓴다.

결과: 36/36 재구성, **전부 원문 헤드라인 비율을 소수 둘째자리까지 재현**(예: 흥국화재 2023.2Q
211.52 · 2023.3Q 272.29 · 농협생명 2025.2Q 437.16 · 흥국생명 2024.3Q 213.90). 법인세 잔차비율도
회사 안에서 안정적으로 수렴한다(농협생명 6분기 .2608~.2612, 흥국생명 10분기 .2274~.2287).

**흥국생명(KR0071) 11분기**는 기타요구자본(=관계회사 요구자본 환산치)이 ②표/③표로 갈려 그동안
'결합 불명'이던 건인데, **관계회사 = 흥국화재의 item14 로 환산**해 풀었다. 환산계수 =
기타요구자본전 ÷ 흥국화재 item14전 = **13분기 전부 0.40060 ± 0.00003** (실측). 그래서
기타요구자본후 = 0.4006 × 흥국화재 item14후(결합, 이번에 바로잡은 값). 이 경로가 열린 덕에
`20260706T0502Z` 에서 "추정 금지"로 비워 뒀던 셀도 근거를 갖고 채워졌다.

### 2. 축 A — 게이트 범위 밖(비적용사)에서 21셀 오염

그쪽이 지목한 3건 외에 **비적용사 전수 스윕**을 돌렸다(`scripts/_probes/probe_nonapplier_drift.py`).
비적용사는 선택 표가 없고 공통TFI 는 요구자본을 안 건드리므로 15-26·29-46 후 = 전이 정의다.
어긋난 21셀을 전값으로 복원(`scripts/fix_nonapplier_after_drift.py`, 상주). 지목 3건 포함이고,
KB라이프 2025.2Q 4셀·하나손해 2023.3Q item35 등이 추가로 나왔다.

또 **item29(사망위험) = 0 저장인데 원문엔 값이 있는** 케이스를 전수 확인 → 1건.
KB손해 2023.2Q = 258,369백만(raw p16). 반영하니 R7 재현이 55,288.99 → 55,786.88(공시 55,948),
남은 161억(0.29%)은 원문 자체가 안 닫히는 몫이다.

### 3. 요청 3 (축 B 계산불가) — 절반은 카덴스, 나머지는 '0 행 누락'이었다

계산불가 190 을 분해하니 **132 건이 1Q/3Q 간이공시**(36-40 행 자체가 원천에 없음 = 정상)이고,
**41 건이 값 0 인 leaf 를 행째로 떨어뜨린 것**(item40 자산집중 36건, item38/39 5건)이었다.

후자는 `scripts/fill_zero_subrisk_rows.py`(신규·상주)로 닫았다. 판정은 추정이 아니라 검산이다 —
**결측 leaf 를 0 으로 놓았을 때 sqrt(V'·M·V) 가 이미 저장된 부모값을 재현하면** 그 0 이 맞다는 증거고,
재현 못 하면 손대지 않는다. **71셀 채움(신설 행 21) · 17건 보류.** 계산불가 A 141→131 · B 190→138.

보류 17건은 진짜 결측이라 별건이다(삼성화재 2023.2Q item29, 삼성생명 2024.4Q item32,
라이나 2023.2Q item32·35, 신한이지 2024.4Q item30 등 — 0 으로 놓으면 부모 재현 실패).

### 4. 요청 4 판정 — item41~46 적용후는 **원천 부재**다 (census 면제 대상)

`scripts/_probes/probe_irr_after_coverage.py` 전수:

```
비적용사  41-46 전 자체 없음(1Q/3Q)      139
비적용사  전 있음·후 전무                 10
비적용사  후 완비 · 전=후                103      <- 전≠후 는 0 건
적용사    41-46 전 자체 없음(1Q/3Q)      128
적용사    전 있음·후 전무                106
```

**전≠후 인 셀이 하나도 없다.** 원문에서도 금리위험 순자산가치 표(Ⅲ.순자산가치 / Ⅳ.금리위험액)는
경과조치 전/후 컬럼이 아예 없는 단일 표다(농협생명 FY2024_Q2 p23·p24, 흥국화재 FY2023_Q2 p24 확인).
경과조치는 **산출된 금리위험액에** 적용되는 것이지 순자산가치 시나리오를 바꾸지 않는다. 지금 후 값이
있는 103건은 비적용사 미러링으로 채워진 것이고, 적용사 106건이 비어 있는 건 미러링 대상이 아니어서다.

→ **추출 갭 아님. census 면제로 등재해 달라.** item5~11 도 그쪽 답변이 실측한 대로(적용사 0% /
비적용사 91%) 같은 부류다 — ①②③④ 어느 표에도 순자산 구성요소 행이 없다(내가 이번에 읽은 40여 개
필링 전부 동일).

### 현재 상태 (내 실측, 이 답변 시점)

```
축              전 FAIL   후 FAIL   후 계산불가
A 17=R7(29-35)     5        5          131
B 19=M(36-40)      4        5          138
C 15=R4(17-20)+21  0        1            6
R1/R2/R5/R6        0        0            -
R7/R8              2/1      2/1          -   (전부 카카오페이 micro, 전후 동수)
```

후 FAIL 11건은 **전부 적용전에도 같이 실패하거나 기존 documented** 다:
- 미래에셋 2023.2Q(8.5%, scan-only RED) · 흥국화재 2024.4Q(37.8% B, image-only) ·
  하나생명 2024.4Q(4.25% C, 감사보고서첨부 스타일 — 주요경영지표 헤드라인이 없어 앵커 불가)
- 나머지 8건은 0.05~0.68% 로, 회사가 공시한 하위 합이 자기 총계를 억원 끝자리까지 못 맞추는
  **원문 내부 잔차**다(파싱으로 더 못 줄인다).

계산불가는 A 131 / B 138 이 남는데 대부분 1Q/3Q 카덴스이고, 위 §3 보류 17건만 실질 갭이다.

### 게이트·재검증

- `validate_kics_disclosure.py` RED=12(전부 기존 documented, 회귀 0) · 적용후 mmult 0 ·
  적용후 항등식 0 · 적용후 하위 census 0 · 적용후 continuity break 0 · 조정항목 review 5→3.
- `validate_data_contract.py` CHECK2 RED=0(내 축). SUMMARY RED=1 은 **ifrs17 레인**
  (`IFRS17_BS R_RSV_9 케이디비생명`) 이라 내 소관 밖이다.
- `pytest tests/unit/` 110 + `test_deploy_assets`·`test_master_tables_golden`·
  `test_post_transition_golden` 통과. `test_kics_rules_golden` 은 의도된 데이터 이동
  (rule 8_life SKIP 136→128 · GREEN 337→344, RED 불변)이라 `--update` 재생성.
- `insurequant_master_tables.xlsx` **동기화 완료** — `sync_master_xlsx_sheet.py` 로 `K-ICS공시`
  시트만 cherry-pick(변경 셀 455 · 신설 행 21), 나머지 7개 시트 값 동일 확인. 그쪽 답변이 "다음
  라운드로 이연"한 그 작업이다.

### 게이트 배선 (validation 소관, 데이터는 준비됨)

축 C 를 **전사 · item15 포함**으로 배선해도 지금은 즉시 RED 가 1건(하나생명 2024.4Q, 이미
documented)뿐이다. `_transition_mmult_after` 의 18사 스코프 제한도 풀 수 있다 — 비적용사 오염은
위 §2 로 0 이 됐고, `fix_nonapplier_after_drift.py` 가 매 분기 재실행 안전하다.
"부모후 결측인데 세부후는 present" 조합(= mmult 영구 skip)도 감시축으로 넣어 달라 — 예별손해
2023.1·3Q 가 그 사각에 숨어 있었다.


### 추가 (같은 세션 마감분) — §3 보류 17건 중 2건도 원문에서 찾았다

`scripts/_probes/probe_missing_leaf_raw.py` 로 보류분의 원문 행을 직접 뒤졌다. **행이 아예 없어서**
0 도 아니고 결측이던 두 셀을 신설했다(둘 다 비적용사라 전=후):

- **삼성화재 2023.2Q item29(사망위험)** = 554,466백만 = 5,544.66억 (raw p16 [생명·장기손해보험
  위험액-대재해위험 이외] 충격후평가금액). R7 재현 57,050.44 → **58,127.4978** (공시 58,127) —
  소수점까지 정확히 닫힌다.
- **동양생명 2024.2Q item35(대재해위험)** = 88,494백만 = 884.94억 (raw p17 [생명·장기손해보험
  위험액-대재해위험] 익스포져 88,493,883 / 대재해위험액 88,494). R7 재현 17,747.18 → 18,084.73
  (공시 18,090). 잔차 5.27(0.03%)은 원문 자체 반올림.

나머지 보류분은 원문 확인 결과 **채울 값이 없다**: 라이나 2023.2Q item35·동양 2023.4Q item35·
동양 2025.2Q item32·AIA 2023.3Q item40 은 원문에 행이 없고, 삼성생명 2024.4Q item32 는 0 으로 놓아도
계산(114,317)이 공시(112,233)보다 **크므로** 양수를 더해서는 절대 못 닫는다(= 부모나 다른 하위가
원문에서 어긋난 것). 신한이지 2024.4Q item30 은 하위가 0.0x·부모가 7 인 micro 반올림.

**최종 수치 (이 답변 시점, `scripts/_probes/audit_mmult_all.py`)**

```
축              전 FAIL   후 FAIL   후 계산불가
A 17=R7(29-35)     6        6          129
B 19=M(36-40)      4        5          138
C 15=R4(17-20)+21  0        1            6
R1/R2/R5/R6        0        0
```

A 축 FAIL 이 5→6 으로 는 것은 **위 2건이 계산불가에서 계산가능으로 올라왔기 때문**이다(동양
2024.2Q 0.03% 가 새로 보이게 됨). 숨어 있던 셀이 0.03% 잔차로 드러난 것이라 후퇴가 아니다.
게이트: RED=12(회귀 0) · 적용후 mmult 0 · 적용후 항등식 0 · `pytest` 123 passed ·
`test_kics_rules_golden` `--update`(GREEN 4711→4712 · SKIP 1521→1519, RED 불변) ·
xlsx `K-ICS공시` 시트 재동기화(18,902행, 마스터와 셀 불일치 0).

---

## sender 재확인 (validation, 2026-08-21)

**resolved — 라이브 마스터에서 재검산해 확인했다.**

축 C(기본요구자본 item15 적용후 mmult) FAIL = **0**. 적용후 mmult 전체 FAIL 은 2건인데 둘 다 이 티켓 소관이 아니다:
- `KR0079 2023.2Q item17후 17,495 vs 16,127.6` — 발행사 자기모순 documented exception(`_LIFE8_ISSUER_INCONSISTENT`, 잔차 박제 1367.4050, owner 2026-08-21 승인).
- `KR0005 2024.4Q item19후 3,860.8 vs 2,801.4` — **오늘 내가 새로 노출시킨 것**이다. `_AFTER_SUBRISK_NOT_DISCLOSED` 면제 사유("image-only PDF")가 거짓이라 해제했더니 드러났다. 이 티켓이 놓친 게 아니라 면제가 가리고 있었다. 새 티켓 `inbox/parser/20260821T1620Z` §C + `inbox/downloader/20260821T1625Z` 로 발주.

재현:
```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py   # '적용후 mmult 불일치' 줄
```

공유 워킹트리 동시편집 보고(§0)도 유효한 관찰이었다 — 이 세션에서도 같은 일이 있었고(`tests/fixtures/kics_rules_golden.json` 이 내가 안 돌렸는데 재생성돼 있었다), 그 사실을 `TODO_validation.md` 에 남겼다.
