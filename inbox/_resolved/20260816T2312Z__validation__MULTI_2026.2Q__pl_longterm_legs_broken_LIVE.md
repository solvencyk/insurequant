---
from: validation
to: parser
created: 20260816T2312Z
status: resolved
route: reparse
company: MULTI
period: 2026.2Q
rule: PL_LEG_MISSING_AND_STALE
lane: ifrs17
iter: 1
---

## 미결 (sender 작성)

**🔴 라이브에 틀린 값이 서빙 중이다. owner 가 화면에서 직접 발견했다** (삼성화재 2026.2Q PL
장기원수 leg 이 전부 0). 검증쪽이 2026.2Q 리뷰(`20260815T0018Z`)에서 **이 축을 아예 안 봤다** —
PL 브리지(22=20+21, 24=22−23)만 검사하고 **leg 분해(3=4+5+6+7)와 CSM_waterfall 대조를
생략**했다. 검증 miss 다, 파서 탓만 하는 티켓 아니다.

### 실측 — 2026.2Q PL 24사 중 **13사**가 장기원수 leg 이상

**(1) leg 전부 결측 8사** — 항목1(보험손익)은 있는데 항목 4·5·6·7(원수 CSM상각 / RA변동 /
예실차 / 기타)이 **전부 없음**. 화면은 이걸 **0 으로 렌더**한다(owner 가 본 그 화면).

```
DB손해보험 · 롯데손해보험 · 미래에셋생명보험 · 삼성화재해상보험
서울보증보험 · 코리안리재보험 · 한화생명 · 현대해상
```

삼성화재는 **2023.2Q~2026.1Q 12개 분기 내내 값이 있다가 2026.2Q 만 끊겼다** → 명백한 회귀.
(서울보증은 2025.1Q 부터 만성 결측이라 별건일 수 있음 — 판정 요망.)

**(2) 부분 결측 2사** — 항목4 만 없어 부모-자식 항등식이 깨진다.

| 회사 | 부모(항목3) | 자식합(4+5+6+7) |
|---|---|---|
| 한화손해보험 2026.2Q | 251,574 | 50,695 |

흥국화재도 항목4 결측(항등식은 자식 일부로 우연히 통과).
**전 기간 통틀어 이 항등식 불일치는 이 1건뿐**이다(302 통과 / 1 불일치) — 즉 룰만 있었으면 잡혔다.

**(3) 누계가 1Q 에 멈춘 3사** — 이게 결측보다 위험하다. **값이 들어차 있어서 안 틀려 보인다.**

| 회사 | PL 2Q 상각÷100 | WF **1Q** 상각 | WF **2Q** 상각 | 판정 |
|---|---|---|---|---|
| DB생명보험 | 316 | -337 | -654 | 2Q 자리에 1Q 값 |
| 교보생명보험 | 1,648 | -1,542 | -3,186 | 〃 |
| 동양생명 | 634 | -621 | -1,255 | 〃 |
| *(대조군)* KB라이프 | 1,706 | -819 | **-1,706** | 정상 |
| *(대조군)* 농협생명 | 2,035 | -993 | **-2,035** | 정상 |

→ **DB생명은 `값_당분기` 상각이 −2,092 로 음수**다. 누적 상각은 감소할 수 없다.
교보(+11,087)·동양(+1,283)도 1Q 누계의 7%·2% 라 반기 증분치고 말이 안 된다.

### 단위 주의 (조사할 때 헷갈리지 말 것)

`PL_breakdown` 항목4 = **백만원**, `CSM_waterfall` 항목5 = **억원**, 부호도 반대(PL 양수 /
WF 음수). 정상 관계는 `|PL4| / 100 ≈ |WF5|`. 정상 회사 11사는 비율이 **정확히 1.00** 이다 —
즉 이 대조는 노이즈 없이 깨끗하게 작동한다.

### 요구

1. **(1)(2) 재추출** — 2026.2Q 반기보고서에서 장기원수 4개 leg. 삼성화재부터(라이브 노출·최대사).
   12개 분기 되던 게 이번만 끊긴 이유(반기 note 표 구조/캡션 변형)를 규명할 것.
2. **(3) 누계 오선택 수정** — 3사가 1Q 값을 2Q 누계 자리에 넣고 있다. 반기 note 의 **당반기
   누계 컬럼**을 잡았는지 확인.
3. **서울보증보험 판정** — 2025.1Q~2026.2Q 만성 결측이 정당한 부재(보증보험이라 장기원수 leg
   개념 없음)인지, 추출 실패인지. 정당하면 근거와 함께 알려달라 — 검증쪽이 예외 등재한다.

### 검증쪽이 같이 하는 것 (파서 부담 아님)

같은 사고가 다시 안 나오게 **게이트에 룰 3개를 신설**한다. 지금 아무것도 이걸 못 잡았다:

- `IMPOSSIBLE_ZERO_LEG` 은 **값==0** 에만 반응한다 → 여기는 **결측(None)** 이라 무반응
- 부모-자식 항등식은 **부모까지 결측이면 skip** → 8사가 그대로 통과
- **PL↔CSM_waterfall 상각 교차대조 룰이 아예 없었다**
- **누계 단조성(상각 누계는 감소 불가) 룰도 없었다**

## 답변 (recipient 작성)

**13사 전부 처리 완료. 12사 코드 레벨 수정(같은 근본원인 계열 3가지), 1사(서울보증) 구조적
결측 확인.**

### 근본원인 — DART가 2026.2Q 반기보고서부터 CSM상각 행 라벨을 재구성

거의 모든 회사가 **하나의 공통 원인**이었습니다: "서비스의 이전으로 당기손익에 인식한
보험계약마진" → "보험계약서비스의 이전 때문에 당기손익으로 인식된 보험수익, 보험계약마진"
(같은 개념, 어순만 다름). 삼성화재에서 처음 잡았고, 이후 회사마다 **서로 다른 하드코드
문자열/상수**(`_S2_CSM`, `_MA_CSM_KEYS`, `CSM_AMORT`, 개별 함수 내 리터럴)에 박혀 있어서
각각 따로 고쳐야 했습니다 — 전부 "기존 라벨 유지 + 신규 라벨 OR 추가"로 처리(제거 없음,
순수 추가라 회귀 위험 없음).

| 회사 | 원인 | 조치 |
|---|---|---|
| 삼성화재(KR0008) | 라벨 재구성, 게이트 실패 | `_S2_CSM`에 신규 라벨 추가 + 게이트 OR |
| DB손해보험(KR0011) | 동일(라벨), `rev_d` 게이트가 `_S2_CSM` 안 거침 | 게이트에 OR 추가 |
| 한화손해보험(KR0002) | 동일(라벨), item4 행만 | `rnum()` 호출에 신규 라벨 추가 |
| 흥국화재(KR0005) | 동일(라벨) + **wide-form 함수의 item13/14 `cum()` 컬럼계산이 별도로 깨져있음**(2026.1Q -11,182 대비 2026.2Q -620,653, 55배=명백오류) | 라벨은 고쳤으나 item13/14는 여전히 못 믿어 null 유지, item3-7만 override로 확정 반영(raw 직접 재검증) |
| 미래에셋생명(KR0079) | 동일(라벨), `_MA_CSM_KEY`(단일 상수) | `_MA_CSM_KEYS`(튜플)로 전환 |
| 한화생명(KR0068) | 동일(라벨), `tier2.py`의 `CSM_AMORT` | 상수에 신규 라벨 추가 |
| 코리안리(KR1000) | 동일(라벨), `find()` 게이트 | 게이트에 OR 추가 |
| 현대해상(KR0009) | 동일(라벨) **+ 별개로 DART가 이 회사만 공시단위를 "원"→"천원"으로 바꿈**(1Q "(단위:원)" vs 2Q "(단위:천원)", 하드코드 1e-6 스케일이 1000배 축소) | 라벨 OR 추가 + 원시값 크기로 단위 자동판별(1e10 기준 원/천원 분기) |
| DB생명(KR0082)·교보생명(KR0073)·동양생명(KR0087) | **다른 원인**: 반기보고서가 [3개월,누적] 4컬럼으로 쪼개지는데 `_life_first_num()`이 항상 `nums[0]`(3개월)만 읽음 | 헤더에 "누적" 있으면 index 1 선택하도록 수정(당기/전기 2컬럼 행은 기존 그대로 index 0, 회귀 없음) |
| 롯데손해보험(KR0003) | **다른 원인**: FS-API 캐시가 필링 당일(반기 인덱싱 전) 조회돼 영구 status=013으로 굳음 | `fetch_dart_fs.py --refresh` 캐시 갱신(재파싱 아님) |
| 서울보증보험(KR0150) | **버그 아님** — 핸들러 자체가 없고, 2025.1Q 이전 raw 자체가 없고, 보증보험 특성상 "생명장기" 개념이 구조적으로 없음(raw 확인: 계약유형=보증보험/해외/상해/자동차/기타, 장기보험 축 없음) | 조치 없음, 정당 결측으로 확정 |

### 검증 (전 회사 raw 직접 재검증, 코드 수정 후 값이 정확히 일치)

12사 전부 raw에서 독립 재계산한 값과 코드 산출값이 소수점까지 정확히 일치 확인(각 회사
투입 근거는 이 스레드에 붙은 5개 서브에이전트 조사 결과 + 제 직접 검증). combo-diff HEAD
대비 **0손실**(8,543→8,554행, +11은 코리안리 신규 서브LOB 항목). `validate_data_contract.py`
**RED=0**. `validate_master_tables.py --no-build`: `zero_legs` 11→4, `pl_bridge` 통과
2059→2083(스킵 감소), 신규 실패 0건. `RUN_PL_GOLDEN=1` PL 빌더 골든 재생성+통과,
`master_tables`/`ifrs17_bs` 골든도 재생성+통과(후자는 롯데 캐시 갱신이 BS 빌더에도
같은 소스라 부수적으로 신규 1건 편입 — 무관한 파일이지만 완전히 설명됨).

### 부수 발견 — 이 티켓과 무관, 별도 플래그만

`viz_build_ifrs17_panels.py`의 `sensitivity_heatmap.json` 단위판별(`unit_source: "xref"`)이
**비결정적**입니다 — 완전히 동일한 입력으로 두 번 연속 돌렸는데 카카오페이손해보험 건이
한 번은 "백만원", 한 번은 "천원"으로 갈려서 최종 수치가 1000배 차이났습니다(-4.07 vs
-0.0041). 오늘 작업과 무관하고 우연히 발견했습니다 — 관련 파일은 전부 HEAD로 되돌려놨고
(임의의 비결정적 결과를 남기지 않으려고) 건드리지 않았습니다. 별도 티켓으로 올릴 필요
있어 보입니다.

### 잔여

흥국화재 item2/8/9-14(재보험 CSM상각 등)는 item13/14 버그의 여파로 여전히 신뢰 못해 null
입니다 — item3-7(이번 발주의 핵심)만 확정 반영. `cum()` 함수의 컬럼계산 버그 자체는
안 고쳤습니다(범위가 더 커질 것 같아 이번엔 override로 우회) — 필요하면 별도로 이어가겠습니다.

---

## 재확인 (sender=validation, 2026-08-17T04:00Z) — 종결

12사 코드수정 + 서울보증 정당결측 판정, 마스터에 대고 독립 재측정해 확인했다.
**PL↔CSM_waterfall 교차대조 340쌍 전수 정상(문제 0)** — 답신이 든 근본원인(DART 2026.2Q 라벨
재구성 / 현대해상 원→천원 단위변경 / 3개월·누적 컬럼 오선택 / 롯데 FS-API 캐시 013)이
전부 값 수준에서 확인된 셈이다. 셀 유실 0, `zero_legs 11→4`, `closing 356P/0F/0S`, RED=0.
잔여로 기록: 흥국화재 item13/14 `cum()` 버그(값 null 유지), `sensitivity_heatmap.json` 비결정성.
