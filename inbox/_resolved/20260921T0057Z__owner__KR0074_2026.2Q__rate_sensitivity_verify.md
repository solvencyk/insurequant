---
from: owner
to: parser
created: 20260921T0057Z
status: resolved
route: reparse
company: KR0074
period: 2026.2Q
rule: RATE_SENSITIVITY_CENSUS
lane: kics
iter: 1
---

## 미결 (sender 작성)

owner 가 "라이나생명 2026.2Q 금리민감도 데이터가 있는데 사이트엔 미공시" 라고 신고했다.
화면 쪽 원인은 별건(JS `ReferenceError`, designer 발주 완료)으로 확정됐다. **여기서는
데이터가 실제로 원문과 맞는지만 본다.** 화면 버그와 별개로 값이 틀렸을 가능성을 배제해야 한다.

### 현재 마스터 상태 (실측)

`kics_rate_sensitivity.json` (루트·`origin/main` 동일):

- KR0074 2026.2Q = 6행 (적용전 3 + 적용후 3), 5개 충격값 전부 non-null.
- 지급여력비율 적용전/적용후: -100bp 337.12 / -50bp 344.02 / base 349.43 / +50bp 353.20 / +100bp 356.41
- 지급여력금액 듀레이션 2.0828, 컨벡서티 -45.47 / 기준금액 듀레이션 4.8995, 컨벡서티 135.36
- **적용전 = 적용후 완전 동일** (2024.4Q·2025.2Q·2025.4Q 도 마찬가지)

### 시킬 일

1. KR0074 2026.2Q 6행을 **원문(정기경영공시 PDF / Docling MD)과 셀 단위로 대조**한다.
   raw 가 없으면 downloader 로 라우팅하고 여기서 끝내지 말 것.
2. **적용전=적용후 동일이 진짜인지** 판정한다. 라이나생명이 경과조치 미적용사라
   정당하게 같은 것인지, 아니면 적용후 파싱 실패로 적용전이 미러링된 것인지.
   FSS 붙임-1 기반 `_TRANSITION_KIND` 등재부(경과조치 종류 정본)로 먼저 확인하고,
   등재부에 없으면 원문 개요표(page3 류)로 확인한다. 헤드라인 대조가 우선,
   공통적용 세부표만 보고 "같다" 고 결론 내지 말 것(기존 오판 전례 있음).
   미러링이면 삭제·재추출, 정당 동일이면 그 근거를 등재부/TODO 에 박는다.
3. **2026.2Q 커버리지 census**: 현재 39사 225행. 6의 배수가 아니다 = 적용후가 없는 회사가 섞여 있다.
   회사별로 (적용전/적용후 × 3 measure) 기대 그리드를 self-census 로 세우고,
   빠진 칸이 "원문에 없음" 인지 "우리가 못 뽑음" 인지 **칸 단위로** 가른다.
   `SKIP-on-missing` 은 검증 무력화다 — 결측은 근거를 적어 등재한다.
   금리민감도는 반기 공시(2024.4Q 도입)이니 짝수 분기만 기대 그리드에 넣는다.
4. 결과를 이 티켓 `## 답변` 에 적고, `TODO_parser_kics.md` 맨 위 갱신.
   값이 바뀌면 `scripts/validate_kics_rate_sensitivity.py` 를 돌려 RED=0 확인.

## 답변 (recipient 작성 — 처리 후)

**결론: KR0074 데이터는 100% 맞다(수정 0건). 적용전=적용후는 진짜다. census 로 KR0074 와는
무관한 다른 3사의 결측 9칸을 찾아 고쳤다.**

### 1) KR0074 2026.2Q cell-by-cell — raw path: `md_inbox/FY2026_Q2/KR0074_라이나생명보험.md`

L816-829 "2) 금리 민감도 분석" 표(raw PDF 도 fitz 로 대조, docling 손실 없음). 마스터 6행(적용전
3 + 적용후 3) 전부 원문과 자릿수까지 일치:

| measure | base | -100bp | -50bp | +50bp | +100bp |
|---|---|---|---|---|---|
| 지급여력비율 | 349.43 | 337.12 | 344.02 | 353.20 | 356.41 |
| 지급여력금액 | 71,251 | 72,573 | 71,988 | 70,442 | 69,605 |
| 지급여력기준금액 | 20,390 | 21,527 | 20,926 | 19,944 | 19,529 |

RS1(비율=금액/기준금액×100)·RS2(적용전 base = kics_disclosure item1=71251/item14=20390/
item27=349.4409024) 둘 다 수식대로 정확히 닫힌다. 듀레이션(금액 2.0828·기준금액 4.8995)·
컨벡서티(금액 -45.47·기준금액 135.36)도 `extract_kics_rate_sensitivity.duration_convexity()`
재호출 결과와 소수 4자리까지 일치. **손댈 셀이 없다.**

### 2) 적용전=적용후 판정 — 진짜(경과조치 미신청). 미러링/파싱실패 아님

- **원문 자신의 각주**(공통적용 세부표가 아니라 **이 금리민감도 표 자체**의 각주):
  "주3) (경과조치 미신청 회사) 당사는 선택경과조치를 적용하지 않아 경과조치 전·후 금액 및
  비율이 동일함". 헤드라인/세부표 구분 함정을 피하려 표 자신의 각주로 직접 확인했다(세부표
  단독 대조가 아님).
- `_TRANSITION_KIND`(`scripts/validate_kics_disclosure.py` L536-556, FSS 2023-03-20 붙임-1
  정본) 18사 목록에 **KR0074 부재** — TFI(공통조치)만 받는 나머지 21사 쪽.
- 별건 산출물 `scripts/_probes/_build_kr0074_patch.py`(2026-08-31, `kics_disclosure.json`
  전용, 이번 세션 이전 라운드)가 raw p.18 [지급여력비율의 경과조치 적용에 관한 사항] O/X표로
  독립 확인: TAC=TIR=TER=TIRR=PCA_DEFER 전부 X.
- 같은 항등식이 마스터의 KR0074 나머지 3개 분기(2024.4Q·2025.2Q·2025.4Q)에도 일관.

### 3) 2026.2Q 커버리지 census

수정 전: 39사 225행(6의 배수 아님). 39사 중 36사 6/6 · **3사 3/6**(전부 적용후 3행 결측):
KR0050 하나손해보험 · KR0069 삼성생명보험 · KR1098 카카오페이손해보험. 칸 단위 원인:

| 회사 | 원문 상태 | 판정 |
|---|---|---|
| KR0050 하나손해 | 적용후 셀 **공백**(대시도 아님) + 표 각주 "주3) (경과조치 미신청 회사)...전·후 금액 및 비율이 동일함" | 추출 갭(우리가 못 뽑음) |
| KR0069 삼성생명 | 적용후 셀 "-"(대시). MD 는 각주 누락했지만 raw PDF fitz 전체텍스트(0-idx p43)에 동일 각주 확인 — docling 리딩오더가 다단 레이아웃에서 각주를 표보다 앞쪽으로 떨어뜨렸다 | 추출 갭(우리가 못 뽑음) |
| KR1098 카카오페이 | raw PDF 원문 자체가 두 번째 블록 라벨을 "경과조치전"으로 **오식**(fitz 텍스트+480dpi 렌더 둘 다 확인, `artifacts/_kr1098_2026q2_labelzoom_480dpi.png`). 숫자는 첫 블록과 완전 동일 재인쇄 + 동일 미신청 각주. 추출기 dedup 로직이 "OCR 중복"으로 오판해 버림 | 추출 갭(우리가 못 뽑음, 원문엔 있음) |

3사 전부 `_TRANSITION_KIND` 부재(TFI-only) — "원문에 없음"은 0건, 전부 "우리가 못 뽑음"으로
판정. `SKIP-on-missing`으로 넘기지 않고 칸 단위로 고쳤다(아래 4).

### 4) 수정 + 게이트 재확인

`scripts/fix_20260921_ratesens_2026q2_nonapplier_mirror.py` — 3사 × 3행(적용전 값을 적용후로
미러, 듀레이션·컨벡서티는 `extract_kics_rate_sensitivity.duration_convexity()` 재호출로
산출, 재타이핑 아님). 가드: 실행 전 (사,2026.2Q)당 적용전 3행·적용후 0행 assert / 실행 후
기존 789행 byte-identical 재확인(append만 발생) / 백업
`kics_rate_sensitivity.json.bak_pre_20260921_nonapplier_mirror`. 결과: 789→798행(+9).

`validate_kics_rate_sensitivity.py` 수정 전후 **gate RED=0 불변**:
```
SUMMARY  RS1:0RED(+1exc) | RS2:0RED(+4exc) | RS3:64Y | RS4:1Y | RS5:0RED(+17exc) | gate RED=0
```
2026.2Q census 재실행: 39사 전부 6/6 = 234행, 중복 (코드,경과조치여부,measure구분) 키 0건.

재현:
```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_rate_sensitivity.py
```

**게이트 사각 발견 + 통지**: RS4(회사 자체가 그 분기를 안 가진 hole)·RS5((사,분기)가
kics_disclosure 엔 있는데 rate_sensitivity 에 통째로 없는 결손) 어느 쪽도 "적용전은 있는데
적용후만 결측"인 이 패턴을 못 잡는다 — 데이터가 39×225로 비-6의-배수인데도 RS1-5 는 전부
GREEN/documented였다. `inbox/validation/20260921T0100Z__parser__ALL_2026.2Q__ratesens_phase_level_census_gap.md`
로 RS6 후보 통지.

**미착수(범위 밖, 근거만 기록, 추측 채움 안 함)**: KR0050·KR0069 는 2024.4Q/2025.2Q/2025.4Q,
KR1098 은 2025.4Q 도 같은 적용전-only 패턴이 이력에 있다. "미신청" 각주의 느슨한 정규식은
전 분기 alt-hit ≥1 이지만, 정확 문구 매치는 docling 공백처리가 분기마다 달라 신뢰할 수 없다
(KR1098 2026.2Q 조차 정확매치 0인데 이번 세션 fitz/렌더 직접확인으론 각주가 실재했다).
이번 세션은 **2026.2Q 만** raw 개별 확인을 마쳤으므로 나머지 분기는 다음 라운드가 같은
방법으로 마무리해야 한다(회사·분기 목록은 위 TODO_parser_kics.md 17회차 항목에 그대로 있음).

변경 파일: `kics_rate_sensitivity.json`(+9행, 789→798) · 신규
`scripts/fix_20260921_ratesens_2026q2_nonapplier_mirror.py` · 프로브 6종
`scripts/_probes/_20260921_*.py` · 렌더 `artifacts/_kr1098_2026q2_*.png`(2개) · 신규 inbox
`inbox/validation/20260921T0100Z__parser__ALL_2026.2Q__ratesens_phase_level_census_gap.md`.
`TODO_parser_kics.md`(17회차) · `docs/changelog_parser_kics.md`(2026-09-21) 갱신 완료.

## 종결 (orchestrator, 2026-09-21)

재확인: `kics_rate_sensitivity.json` HEAD 789 → 798 (append 9 · remove 0). 2026.2Q census 39사 × 6 = 234행, 6 아닌 회사 0. 미러 3사 × 3행 × 7값 전부 적용전 == 적용후 (불일치 0). KR0050·KR0069·KR1098·KR0074 전부 `_TRANSITION_KIND` 부재 확인. `validate_kics_rate_sensitivity.py` gate RED=0 재현. RS6 후보는 `inbox/validation/20260921T0100Z` 에서 이어간다.
