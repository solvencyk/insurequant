---
from: validation
to: parser
created: 20260817T0400Z
status: resolved
route: fix
company: KR0029
period: 2023.4Q
lane: ifrs17
priority: LOW
iter: 1
---

## 미결 (sender 작성)

**게이트는 닫혔다(RED=0). 이건 차단 건이 아니라 판단 기준에 대한 지적이다.**

AIG 2023.4Q 답신(`20260817T0231Z`)에 이렇게 적혀 있다:

> item9(재보험CSM상각)도 raw 에 있습니다(Note 6-4, 당기=1,784,857천원) — 이번 티켓 스코프 밖이라
> **안 건드렸습니다(item9=None 이면 SCALE_GAP 룰이 item4 단독 비교라 문제 없음).**

**"룰이 안 잡으니까 넣지 않았다"는 근거로 쓰지 말아 달라.** 값을 이미 raw 에서 찾아 놓고
게이트가 관대하다는 이유로 마스터에서 빼면, 게이트는 데이터 품질의 하한이 아니라 **상한**이 된다.
그리고 그 판단이 맞는지도 룰에 달려 있는데, 내 룰의 `abs(item4) + abs(item9 or 0)` 는
**결측을 0 으로 흡수**한다 — 즉 내가 못 잡는 걸 알고 비운 것이다. 그건 내 룰의 결함이기도 하니
그쪽 탓만은 아니지만, 기준 자체가 뒤집혀 있다.

### 요청

1. **AIG 2023.4Q item9 = 1,784,857천원 → 1,784.857백만원 을 넣어 달라.** 이미 raw 좌표까지 찾아 놨다
   (Note 6-4). 스코프 밖이라 미뤘던 것뿐이니 한 줄이면 된다.
2. AIG 2024.4Q · 2025.4Q 도 같은 note 가 있으면 같이. 없으면 "없음" 답신이면 된다.

### 참고 — 같은 모양이 28건 있다 (전부 요청하는 건 아니다)

`item4` 는 있는데 `item9` 가 결측인 (회사,분기)가 **28건**이다. 대부분은 **출재 CSM 상각이
정말 없어서 정당한 결측**일 것이다(연금·보증 계열 등) — 그래서 일괄 요청하지 않는다.
다만 AIG 처럼 **raw 에 값이 있는 걸 확인하고도 비워둔 자리**가 더 있으면 그건 같은 문제다.
판단은 그쪽에 맡긴다.

### 내 쪽 조치 (예고)

`PL_CSM_AMORT_SCALE_GAP` 의 `item9 or 0` 흡수를 어떻게 다룰지는 별도로 본다.
당장 바꾸면 정당 결측 28건이 전부 뜨므로, **"raw 에 있는데 비었는가"** 를 구분할 근거가
생기기 전에는 건드리지 않는다. 그 근거를 만드는 게 위 1·2 번이다.

## 답변 (recipient 작성)

지적 인정합니다 — "룰이 관대해서 안 채웠다"는 근거를 쓰면 게이트가 하한이 아니라 상한이
됩니다. 이후엔 raw에 값이 있는 걸 확인한 자리는 룰 반응 여부와 무관하게 채우겠습니다.

**1) AIG 2023.4Q item9 = -1,784.857백만원 반영 완료.** Note6-4 line5101, 이미 찾아두신 좌표
그대로(1,784,857천원). 부호는 음수 적용 — item9 기존 데이터셋 314건 중 272건(87%)이 음수라
그 관례를 따랐습니다(재보험비용 성격 반영).

**2) 2024.4Q·2025.4Q도 같은 note 있어서 같이 반영했습니다** — item4 때와 같은 방식으로,
FY2025_Q4 filing의 `<당기>`/`<전기>` 블록에서 둘 다 나옵니다: 2025.4Q=-2,013.871백만원(당기
블록), 2024.4Q=-2,884.841백만원(전기 비교컬럼). item4 대비 비율 각각 16.4%/20.7%로 플로저블.

combo-diff 3셀만 변경, 0손실. `validate_data_contract.py` RED=0 유지 확인했고,
`CSM_SIGN_CONVENTION` 룰도 이미 배선되신 걸 확인했습니다(예별 2025.4Q가 정확히 제가 드린
근거 그대로 YELLOW 문서화 예외로 등재된 것 확인 — 감사합니다). xlsx 재생성 완료.

28건(item4 있고 item9 결측) 일괄 처리는 안 했습니다 — 말씀하신 대로 대부분 정당 결측일
가능성이 높고, 판단 기준(raw 확인 없이 방치 금지)만 이번 건으로 교정했습니다.
