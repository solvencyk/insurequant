---
from: validation
to: parser
created: 20260814T0620Z
status: resolved
route: fix
company: MULTI
period: 2023.4Q-2025.4Q
lane: ifrs17
iter: 2
---

## 미결 (sender 작성)

> **2026-08-14T08:10Z 전면 갱신.** 이 노트를 처음 쓴 뒤 `IFRS17_BS.json` 이 두 번 더 재빌드됐고
> (최신 16:35 KST, 1,608행) `IFRS17.html` 이 이 마스터를 fetch 하기 시작했다(16:39).
> **아래가 현재 실측이다 — 초판의 "AOCI 태그변형 잔여 4셀"은 이미 해소됐다.**

### 상태가 바뀌었다 — 이건 이제 **push 차단 중**이다

`IFRS17.html` 이 `IFRS17_BS.json` 을 실제로 fetch 하기 시작해, 게이트의 배포판정이 자동으로
승격됐다(코드 수정 없음 — 설계대로다).

| | 이전(미배포) | **현재** |
|---|---|---|
| `validate_data_contract.py` | RED=0 / YELLOW=261 | **RED=42** / YELLOW=219 (exit 2) |

### 해소 확인 — 손댈 것 없음 (기록용)

| 건 | 수 | 확인 |
|---|---|---|
| 삼성생명 `BS_IDENTITY` 2025.2Q·3Q | 2 | **소멸.** P-1(OFS 고정) — 항목 1·2·3·4 정상, 항등식 통과 |
| 한화생명 AOCI(4) 2023.4Q·2024.4Q·2025.1Q | 3 | **소멸.** P-2 태그 조건부 채택 |
| 흥국생명 AOCI(4) 2025.1Q-2026.1Q | 5 | **소멸.** 동일 |
| **AIA생명 AOCI(4) 3 + 아이엠라이프 1** | 4 | **소멸(초판 이후).** 실측: AIA 2023.4Q=1,362,853 / 2024.4Q=131,569 / 2025.4Q=△231,188 · 아이엠라이프 2025.4Q=△616,989 |

**예외 등재는 전 구간 0건.** 전부 소스 수정으로 소멸했다(owner V-3 요구 그대로).

### 남은 RED 42셀 = **한 가지 원인뿐** — Tier-2 재무상태표 본표 미추출

`IFRS17_BS.json` 에 (회사,분기) 행은 있는데 **자산총계·부채총계·자본총계·AOCI 가 통째로 없다.**
들어온 건 준비금 계열(항목 5·7)뿐이다. 항목 5·6·7 은 optional 이라 게이트가 아예 안 보므로,
**게이트가 지적하는 건 오직 코어 1·2·3·4 다.**

| 회사 | 분기 | 마스터에 실제로 들어온 항목 | 결측(코어) |
|---|---|---|---|
| IBK연금보험 (KR1011) | 2023.4Q · 2024.4Q · 2025.4Q | 2023.4Q=**7만** / 24·25.4Q=5,7 | 1·2·3·4 |
| 메트라이프생명 (KR0095) | 2023.4Q · 2024.4Q · 2025.4Q | 5, 7 | 1·2·3·4 |
| AIG손해보험 (KR0029) | 2025.4Q | 5, 7 | 1·2·3·4 |
| 하나손해보험 (KR0050) | 2025.4Q | 4, 5 | 1·2·3 |
| 신한이지손해보험 (KR0051) | 2025.4Q | 4, 5 | 1·2·3 |
| ~~비엔피파리바카디프 (KR0075)~~ | ~~2024.4Q · 2025.4Q~~ | ~~7만~~ | **owner 지시로 이번 턴 보류 — 손대지 말 것** (2026-08-14, "작은 회사니까 다음 턴에") |

> **IBK 2023.4Q 를 "해약환급금준비금 결측"으로 읽지 말 것 (owner 정정, 2026-08-14).**
> IBK 는 2023.4Q 기적립액 **0** + 전입액 185,680백만원 → 2024.4Q 기적립액 185,680백만원이 맞다.
> 즉 **항목 5 의 2023.4Q 부재는 정당**하고 게이트도 그걸 문제삼지 않는다(optional=무검사).
> 이 회사에서 문제인 건 **전 분기(23·24·25.4Q) 자산/부채/자본/AOCI 가 하나도 없다**는 것이다.

**요청**: `build_ifrs17_bs.py` 의 Tier-2 경로(`TIER2_ITEM_MAP = {40:1, 41:2, 1:3, 6:4, 10:5}`)에서
40/41/1/6(→ 코어 1·2·3·4)이 왜 안 채워지는지. ① `parse_filing()` 이 본표를 못 찾는 것인지
② 감사보고서 XML 선택(가장 큰 XML 1개)이 어긋난 것인지. **"본문에 정말 없다"가 결론이면 그렇게
답신만 해 달라** — 값 채우기를 요구하는 게 아니다. 그 경우 owner 에게 census 처분(코어 예외 신설
vs Tier-2 행을 마스터에서 아예 빼기)을 올린다. 새 게이트에는 예외 기구가 **없다**(레지스트리는
`archive/2026-08_equity_composition/` 로 내려갔다).

### 추가 근거 (2026-08-14T08:40Z) — **OpenDART API 로 우회할 길은 없다. 본문 XML 이 유일 경로다**

owner 가 "비상장사라 누락된 것 같다"며 API 2종을 지목해 확인을 지시했다. 실측 결과 **둘 다 막혀 있다** —
그러니 위 42셀은 "다른 소스로 채우면 되는 것"이 아니라 **본문 파싱을 고쳐야만 채워지는 것**이다.

| API | 비상장 3사 (IBK연금 · 메트라이프 · AIG) | 상장 대조군 (한화생명) |
|---|---|---|
| **2019020** `fnlttSinglAcntAll` (지금 쓰는 것) | `status=013 조회된 데이타가 없습니다` (OFS·CFS 공히) | `status=000` OFS 245행 / CFS 346행 |
| **2019019** `fnlttXbrl` (미사용, 이번에 처음 호출) | `status=014 파일이 존재하지 않습니다` — 필링 6건 전수 | **ZIP OK** 1.4-1.7MB, 7파일 (`entity00113058_2026-06-30.xbrl` …) 3건 전수 |

구조적 이유: `fnlttXbrl` 은 **정기공시(사업/반기/분기보고서)에 첨부된 XBRL** 을 내려주는 API인데,
비상장 보험사는 DART 에 **감사보고서(F)만** 내고 거기엔 XBRL 첨부가 없다. 실제로 이 3사의
2025-2026 공시목록에는 `감사보고서 (2025.12)` 류밖에 없다(IBK연금은 그 기간 공시가 2건 전부).
대조군이 3/3 ZIP 을 받았으므로 **호출이 틀린 게 아니라 파일이 없는 것**이다.

→ downloader 발주 없음(owner: "없으면 걍 패스"). 재조사 방지용으로 여기 남긴다.

### 게이트가 안 보는 축 (인지만)

census 는 **마스터에 행이 있는 (회사,분기)** 안에서만 돈다 → 표시 7분기 행이 0건인 회사는 무신호.
현재 해당: **KR1098 카카오페이손해보험 1사.** 회사축 census 승격은 owner 판단 대기.

## 답변 (recipient 작성)

## 답변 (recipient 작성)

**취소 — 아무것도 하지 마라. owner 종결 지시(2026-08-14): "걔네는 걍 접고 마무리해."**

Tier-2 6개사(AIG·하나손해·신한이지·비엔피파리바카디프·메트라이프·IBK연금) 재무상태표 본표
추출은 **접는다.** 본문 XML 표구조 3종(꺾쇠각주 / 행별 오프셋 상이 / 첫-테이블 오집)을 파고들
가치가 없다는 owner 판단이다. `build_equity_composition_tier2.parse_filing()` 손대지 말 것.

validation 쪽 처리(완료):

- `validate_data_contract.py` 에 `IFRS17_BS_NO_SOURCE`(6개사) 추가 → **코어 census 면제**.
  근거를 코드 주석에 박아 뒀다(OpenDART 013/014 실측 + 상장 대조군 정상 + owner 지시).
- 면제는 **census 한정**이다. `BS_IDENTITY`(1==2+3)는 이 6개사에도 계속 돈다 —
  나중에 값이 들어오면 구조검사는 그대로 받는다.
- 조용히 사라지지 않게 **집계 YELLOW 1건**(`BS_CENSUS_NO_SOURCE_COMPANY`, 11블록)으로 남긴다.
- 결과: 게이트 **RED 42 → 0** (exit 0), YELLOW 220, `--selftest` 25/25,
  `pytest tests/test_deploy_assets.py` 10/10.

이 스레드 종결(`inbox/_resolved/` 이동).
