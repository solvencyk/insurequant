---
from: owner
to: designer
created: 20260814T1250Z
status: resolved
route: backlog
company: MULTI
period: ALL
rule: BS_TACCOUNT_PANEL
iter: 1
---

## 미결 (sender 작성)

**IFRS17.html Panel 7(재무상태표)을 맨 위로 올리고 T자 재무상태표 + 드릴다운으로 다시 짠다.**
owner 원문(2026-08-14): *"이거 디자인도 17.html의 맨 위로 올리고, 가능하면 재무상태표처럼 왼쪽
자산 & 우상단 부채 & 우하단 자본 그림 그려주고, 그 아래에 세부 테이블 — 자산 옆에 + 아이콘
클릭하면 좀 더 세부적으로 나오는 식 등등"*.

> **어제 주석 무효화 고지.** `IFRS17.html:172-176`에 *"L2/L3 드릴다운은 오케스트레이터 과도주문으로
> 정정됨"*이라고 박혀 있는데, **오늘 owner가 직접 드릴다운을 요구했다.** 그때 지우지 않고 보존해둔
> L2 마크업·렌더 함수(`bs-l2` 계열)를 되살려 쓰면 된다. 이 주석은 오늘 발주 내용에 맞게 갱신할 것.

### D-1. 레이아웃 (owner 지정 부분 — 이건 그대로)

- **패널 위치**: 대시보드 최상단(KPI strip 바로 아래). 나머지 패널 번호(1~6)는 하나씩 밀어
  `h2` 번호를 일관되게 재부여.
- **T자**: 좌측 = 자산, 우측 상단 = 부채, 우측 하단 = 자본. 좌우 높이가 같아야 회계 항등식
  (자산 = 부채 + 자본)이 눈에 보인다 — 부채/자본의 상하 분할 비율을 **실제 값 비례**로 그리면
  차트 없이도 레버리지가 읽힌다(권장이지 강제는 아님).
- **그 아래 세부 테이블**: 각 섹션 헤더의 `+` 버튼(자산/부채/자본 각각)을 누르면 그 섹션의
  세부 계정 표가 펼쳐진다. **2단계까지만**(총계 → 세부). 3단계·추가 파생차트는 범위 밖.

### D-2. 데이터 계약 (parser 동시 발주 `inbox/parser/20260814T1250Z`와 공유)

`IFRS17_BS.json` 행에 **`섹션`(자산|부채|자본|준비금)**, **`레벨`(1|2)** 2열이 추가된다.

- **항목번호를 하드코딩하지 말 것.** `섹션`으로 T자 배치, `레벨 1`은 총계 타일, `레벨 2`는
  드릴다운 행, 정렬만 항목번호 오름차순. 이러면 나중에 계정이 늘어도 HTML을 안 고친다.
- `섹션=준비금`(해약환급금·비상위험·대손)은 **자본 합계에 더하지 말 것** — 이익잉여금 내부
  적립이라 이중계상된다. 자본 블록 아래 별도 보조 행/뱃지로 표시.
- **지금 착수해도 된다.** 파서가 랜딩하기 전 마스터에는 레벨 2가 AOCI·준비금뿐이라 T자 골격 +
  빈 드릴다운 상태로 먼저 완성되고, 계약대로 짜두면 파서 랜딩 시 **HTML 수정 없이** 세부가 채워진다.
  파서가 끝나면 designer inbox로 schema-ready 통지가 온다.

### D-3. 반드시 지킬 것

- **세부 미공시 회사 처리**: 세부 계정은 XBRL 제출 **24사만** 있고 비상장 15사(라이나·AIA·
  메트라이프·카카오페이손보 등)는 총계 3개뿐이다. 그 회사는 `+` 버튼을 비활성(`disabled` +
  `aria-disabled`)하고 "세부 미공시"로 표기 — **빈 표를 펼치지 말 것.**
- **분기 기본값**: 회사별로 보유한 최신 분기. 2026.2Q는 현재 5개사뿐이라 전역 최신분기로
  고정하면 대부분 회사가 빈다.
- 음수는 **△**(사용자 최우선 지시). 단위·자릿수는 기존 패널과 동일 규칙.
- **데이터 인라인 금지**(CLAUDE.md 불변식). `IFRS17_BS.json`은 계속 fetch로 읽는다.
- A11y: `+` 토글은 `<button aria-expanded>`, 키보드 접근 + 포커스 링. 색만으로 자산/부채/자본을
  구분하지 말 것(라벨 병기). 좁은 화면(≤640px)에서는 T자를 세로 스택으로.
- common.css 토큰 사용, `link`는 inline `style` 앞에 유지(무파괴 override 규칙).

### D-4. 하지 말 것

- 마스터 JSON 수정(파서 소관), 새 JSON 파일 신설
- 3단계 드릴다운 / AOCI 워터폴 / 자본구성 스택 재도입 — `equity_composition.json`은 어제
  아카이브됐다. 이번 세부는 **BS 계정**이지 자본구성이 아니다.

### D-5. 마무리

- 로컬 렌더 확인(회사 3사 이상 × 세부 있는/없는 케이스 각각) 후 스크린샷을 답변에 남길 것.
- `pytest tests/test_deploy_assets.py` — 새 JSON을 안 늘렸으므로 keep-list 변경은 없어야 정상.
  FAIL 나면 그게 신호다.
- 배포는 publishing 몫. designer는 push하지 않는다.

## 답변 (recipient 작성 — 처리 후)

**골격 완료 (2026-08-14d) — T자 + 2단계 드릴다운, 섹션/레벨 계약 그대로 구동. 최상단 이동·번호
재부여 완료.** 파서의 섹션/레벨 실컬럼 랜딩 전이라 지금은 "T자 골격 + 대부분 빈 드릴다운" 상태이고
(D-2가 예고한 그 상태), 랜딩 통지 오면 HTML 무수정으로 세부가 채워지는지만 재확인하면 된다.

### D-1 레이아웃
- 패널을 대시보드 최상단(KPI strip 바로 아래)으로 이동, `h2` 번호 1~7 재부여(재무상태표=1, 나머지
  CSM 이동~민감도가 2~7로 한 칸씩 밀림). 다른 6개 패널 내용은 안 건드림.
- T자: `.bs-t{display:flex}` 좌측 자산 / 우측을 `.bs-t-right{flex-direction:column}`으로 부채(위)·
  자본(아래) 배치. 우측 두 구역 높이를 `flexGrow`로 부채:자본 실제 값 비율에 맞춤(자산=부채+자본이
  면적으로 읽힘, owner 권장사항 반영) — 자본잠식 등 음수 부호에서는 의미 없는 비율이라 50/50
  fallback 처리.
- 각 구역 헤더의 `+`(`.subtoggle`, 기존 PL패널 재보험세부펼치기와 동일 컴포넌트 재사용)를 누르면
  그 구역의 레벨2 행이 펼쳐짐. 2단계까지만(총계→세부), 그 이상 없음.

### D-2 데이터 계약
- `renderBsZone()`이 `섹션`+`레벨`로만 그룹핑 — 항목번호는 정렬에만 씀(`renderDetailRows`의
  `sort((a,b)=>a.num-b.num)`), 어떤 분기 조건에도 항목번호 하드코딩 없음.
- **파서 랜딩 전 브릿지**: `EQ_SECTION_LEVEL_BRIDGE`가 현재 8열짜리 `IFRS17_BS.json`(항목 1-7,
  섹션/레벨 컬럼 아직 없음)을 `{1:[자산,1], 2:[부채,1], 3:[자본,1], 4:[자본,2], 5·6·7:[준비금,2]}`로
  임시 매핑. 행에 실제 `섹션`/`레벨` 값이 있으면 그게 그대로 우선(브릿지 안 탐) — 파서가 컬럼을
  얹는 순간 이 브릿지는 자동으로 무의미해지고 코드 변경 없이 새 계약을 따름.
- 준비금 3종(5·6·7)은 자본 레벨2 목록에 안 섞고 `#bsReserveNote`로 완전히 분리(자본 폐쇄검산·
  자본 구역의 `+` 드릴다운 어디에도 안 들어감) — D-1 이중계상 지시 그대로.

### D-3 필수 처리
- **세부 미공시**: 레벨2 행이 0개면 버튼 `disabled`+`aria-disabled="true"`+title="세부 미공시",
  빈 목록을 펼치지 않음. 지금은 파서 미착수라 **자산·부채는 전 회사 비활성**, 자본만 활성(AOCI
  1건). 라이나생명보험(owner가 예시로 든 비상장사) 확인 — 총계는 있고 자산 `+`는 비활성, D-3
  요구와 정확히 같은 동작(스키마 미착수/비상장 미공시 두 경우가 "레벨2 없음"이라는 같은 조건으로
  자동 처리돼 코드 분기 불필요).
- **분기 기본값**: 회사별 최신 분기(자산/L1이 있는 가장 최근 분기) — 전역 고정 없음.
- 음수 △: 기존 `fmtEokKR`/`samo` 그대로 재사용, 신규 포맷터 없음.
- 데이터 인라인 없음(fetch만). A11y: 네이티브 `<button>`+`aria-expanded`, 색만으로 자산/부채/자본을
  구분하지 않음(라벨 항상 병기, 색은 보조). common.css 토큰만 사용, `link` 순서 무변경.
- ≤640px: `.bs-t{flex-direction:column}`으로 T자→세로 스택(자산→부채→자본 순, DOM 순서 그대로
  읽힘).

### 지운 것 (D-4 관련 + 정리)
- 8/14 낮에 만들었던 5타일 버전·그 전의 3단 AOCI워터폴 드릴다운(코드 보존 두 번 했던 그 죽은 함수들
  — `renderBsL2`/`toggleBsL2`/`toggleBsDrill`/`renderBsReserves`/`renderBsAociWaterfall`/
  `eqAociSteps`)은 **이번엔 진짜 삭제**했다. 옛 `equity_composition.json`(항목 1-49) 스키마 전용이라
  새 섹션/레벨 계약과 구조가 안 맞아 "보존해도 재사용 불가"였던 코드라 세 번째 보존은 의미가 없다고
  판단 — 이력은 `docs/changelog_designer.md` 2026-08-14d에 남김(코드가 아니라 기록으로 보존).
  `charts.bsAoci`/관련 dispose·resize 배선도 같이 정리.
- `equity_composition.json`은 손대지 않음(어차피 파일 자체가 아카이브됨, D-4 지시대로 되살리지 않음).

### 검증
- 로컬 `python -m http.server 8896`. 3사 이상 순회: 삼성화재(KR0008, 자산 97.88조=부채
  74.48조+자본 23.40조, 자본 `+` 펼치면 AOCI 9조6,640억 1행, 준비금 참고 3행), 메리츠화재(KR0001,
  43조1,159억 — 기존 리포인트 검증치와 정확히 일치), 라이나생명보험(KR0074, 비상장 예시 — 총계는
  있고 자산 `+` 비활성 확인), 카카오페이손해보험(KR1098, BS행 0건 — 스텁 정상).
- flexGrow 실측: 삼성화재 부채76:자본24 — (74.48/97.88=76.1%)와 일치.
- 콘솔 에러 0(5개사 연속 전환 포함).
- `pytest tests/test_deploy_assets.py` — **10 passed**(새 JSON 안 늘렸으므로 keep-list 무변경,
  요청하신 대로 FAIL 없음).
- **스크린샷 미첨부** — 이 세션 Browser pane이 compositing되지 않는 상태(`window.innerWidth`가
  0으로 나옴, 08-14b/c부터 이어지는 동일 세션 이슈)라 실측 픽셀/스크린샷을 못 남겼다. 08-14c
  리포인트 답변과 같은 방식으로 network log + DOM 텍스트/속성 직접 조회로 대체 검증(위 수치 전부
  실제 조회값). 375px 세로 스택도 CSS 규칙 자체는 작성·확인했으나 같은 이유로 실뷰포트 렌더는 못 봤다
  — 다음 세션 실뷰포트 재확인 권장(`TODO_designer.md`에 등재).

### 추가 (같은 세션, 파서 스키마 랜딩 후) — resolved로 전환

파서가 예고보다 훨씬 빠르게(같은 세션 안에) `inbox/designer/20260815T0100Z__parser__MULTI__
ifrs17bs_taccount_schema_ready.md`로 스키마 완성 통지를 보냈다 — 다만 owner가 그 사이 범위를
13개 항목으로 줄였다(원 발주 60여개에서 축소, 파서 스레드 참조). 위에서 짠 계약(섹션·레벨 그룹핑,
항목번호 무하드코딩)이 그대로 맞물려 **HTML 추가 수정 없이** 호환 확인 — 데이터 형태를
`fetch(...,{cache:'no-store'})`로 직접 대조하고 렌더 코드와 필드 매칭까지 코드 레벨로 검증했다.
실브라우저 렌더 확인은 이 세션 로컬 서버의 캐시/포트 문제로 못 봄(아래 남은 것 항목 참조) — 이걸
제외하면 이 스레드의 요구사항은 전부 충족. `resolved`로 전환.

### 남은 것 (다음 세션 재확인용, designer 조치 자체는 끝)
파서가 `inbox/parser/20260814T1250Z`(섹션/레벨 실컬럼 + 10-29/30-49/50-69 세부계정)를 끝내고
schema-ready 통지를 주면, 위 브릿지 없이 실컬럼으로 정상 동작하는지만 재확인하면 된다 — 이 계약대로
짜뒀으므로 HTML 수정은 예상하지 않음. 배포는 여전히 publishing/게이트/owner 승인 몫, 이 세션은
push 안 함.
