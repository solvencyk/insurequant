---
from: owner
to: parser
created: 20260820T0033Z
status: resolved
route: backlog
company: MULTI
period: MULTI
lane: ifrs17
iter: 1
---

## 미결 (sender 작성) — 전 프로젝트 상태 점검 리마인더

owner 요청으로 inbox 전수 점검을 돌렸다. **parser가 가장 많은 미결을 안고 있다 (open 8건).**
아래는 새 작업 발주가 아니라 **이미 열려 있는 티켓의 리마인더 + 실측으로 새로 드러난 잔여 3건**이다.

## 전체 현황 (오케스트레이터 실측 2026-08-20T0033Z)

inbox 활성 스레드 **78건** (`_resolved/` 173건 제외):

| status | 건수 | 의미 |
|---|---|---|
| `open` | 13 | 아직 착수 안 됐거나 진행 중 |
| `answered` | **51** | 수신자는 답했는데 **원 sender가 재확인을 안 함** — 프로토콜 4단계가 통째로 안 돌고 있다 |
| `resolved` | 13 | 종결됐는데 `_resolved/`로 안 옮김 |
| `superseded` | 1 | |

`answered` 51건의 원 sender: validation 21 · owner 21 · downloader 6 · parser 2 · publishing 1.
작성월 분포는 2026-06월 19건 · 07월 19건 · 08월 13건 — **38건이 두 달 넘게 매달려 있다.**

**푸시 게이트는 열려 있다**: `validate_data_contract.py` → `RED=0 YELLOW=227 provisional=False`.
지금 막고 있는 건 게이트가 아니라 **닫히지 않은 스레드**다.

---

## parser inbox의 `open` 8건 (드레인 대상)

| 티켓 | lane | 내용 |
|---|---|---|
| `20260612T0900Z__owner__ALL__backlog_digest` | kics | 시장위험 하위(36-40) backfill + 생보 적용후 요구자본 적재 — **2개월 이상 open** |
| `20260616T0230Z__validation__...nb_csm_partial_extract_corrupts_history` | ifrs17 | partial 추출이 NB CSM 시계열 오염 (근본원인 확정본) |
| `20260616T0420Z__downloader__...nb_csm_interim_raw_ready` | ifrs17 | 위 건 raw 30/30 확보 완료 — **추출만 남음** |
| `20260803T0520Z__validation__...rate_sensitivity_provenance_sidecar` | kics | `kics_rate_sensitivity` 사이드카 부재 = 소스 신선도 무검증 |
| `20260819T0116Z__owner__...surrender_reserve_item5_semantics_and_backfill` | ifrs17 | 항목5 의미론 + backfill |
| `20260819T0140Z__downloader__...fs_api_negative_cache_fixed_raw_ready` | ifrs17 | 아래 ✅ 실측 참조 |
| `20260819T0500Z__owner__...statutory_reserves_non_api_companies` | ifrs17 | 법정준비금 non-API 회사 |
| `20260819T0841Z__downloader__...reserve_raw_gaps_fetched` | ifrs17 | raw A~D 확보 완료 — **추출만 남음** |
| `20260819T0858Z__publishing__...ifrs17_bs_golden_stale` | ifrs17 | 아래 🔴 실측 참조 |

`20260616T0420Z`와 `20260819T0841Z`는 **downloader가 raw를 다 넣어놓고 기다리는 상태**다.
파서가 안 돌면 그 raw가 그냥 놀고 있다 — 우선순위 상단에 두는 게 맞다.

---

## 🔴 실측-1 — `test_ifrs17_bs_golden.py` 골든이 **이중으로** stale

publishing 티켓(`20260819T0858Z`)은 5,389 -> 5,587 drift를 보고했는데, 그 뒤로 파서가 더 작업해서
**현재 루트 `IFRS17_BS.json`은 5,686행**이다. 즉 골든은 두 번 밀렸다.

```
tests/fixtures/ifrs17_bs_golden.json : rows 5389  (2026-08-14 생성)
publishing 배포 시점               : rows 5587
현재 루트 IFRS17_BS.json           : rows 5686  <- 지금
```

`CLAUDE.md` 불변식 3에 따라 **산출이 의도적으로 바뀐 것이면 손으로 해시 고치지 말고 `--update`로
재생성 + 커밋에 이유 기록**이다. 판단은 빌더 소유자(parser) 몫이라 publishing이 안 건드리고 넘긴 건이
맞다. 다만 지금은 골든이 FAIL로 상주 중이라 **이 빌더에 대한 회귀 안전망이 사실상 꺼져 있다.**

---

## 🔴 실측-2 — `amort_schedule_unit_not_normalized` 티켓이 **답변 없이 resolved**

`inbox/parser/20260819T0058Z__owner__MULTI_2025.4Q__amort_schedule_unit_not_normalized.md`
- `status: resolved`
- **`## 답변` 섹션이 완전히 비어 있다.**

실제 데이터를 열어보니 **작업은 됐다** — `csm_amort_schedule.json`에 `unit`/`unit_detected`/
`unit_source` 필드가 생겼고 교보생명=억원·라이나/현대해상/하나생명/메트라이프/IBK연금/한화손보=천원으로
탐지돼 있다. 총계도 회사 간 비교가능한 크기로 정렬됐다(삼성화재 140,739억 = 14.1조).

**문제는 두 가지다:**
1. **감사기록이 없다.** 무엇을 어떻게 고쳤는지 티켓에 한 줄도 없어서, 다음 세션이 이 결정을 재구성할 수 없다.
2. **잔여 2사가 남아 있다.**

| 회사 | status | unit_source | total |
|---|---|---|---|
| 서울보증보험 | `empty` | None | **None** |
| 한화손해보험 | `partial` | override | **None** |

또 `unit_source`가 `default`인 회사가 **13/30**이다 — 원문에서 단위를 못 읽고 백만원으로 가정한 것이라,
그중 실제 단위가 다른 회사가 있으면 조용히 틀린다. `cue` 12 · `override` 4만 근거가 있는 셈이다.

**요청:** 답변 섹션을 채워서 무엇을 했는지 남기고, 잔여 2사 + `default` 13사 재확인 여부를 명시해 달라.
designer TODO는 아직 *"그 티켓이 닫히기 전엔 이 패널 숫자를 신뢰하지 말 것"* 이라고 적혀 있어서,
이 티켓이 제대로 안 닫히면 **화면 쪽이 계속 경고 상태로 남는다.**

---

## ✅ 실측-3 — `20260819T0140Z`는 데이터가 이미 착지했다 (닫아도 될 가능성)

downloader의 negative-cache 수정 + 재취득이 반영돼서 `IFRS17_BS.json` **2026.2Q가 24개사**다
(발주 시점 14사). 다만 downloader가 예고한 대로 **흥국화재만 7행**이고 동종사는 16~18행이다.

| 2026.2Q | 행수 |
|---|---|
| 흥국화재 | **7** |
| 서울보증보험 | 16 |
| 삼성화재해상보험 · KB손해보험 | 18 |

흥국화재 본문 XML 폴백(`data/dart/FY2026_Q2/raw/KR0005_흥국화재/`)이 부분적으로만 먹은 상태로 보인다.
그 한 건만 정리하면 이 티켓은 닫힌다.

---

## 🧹 housekeeping — `resolved`인데 안 옮긴 13건

전부 `inbox/parser/` 안에 있다. 프로토콜 3단계(자기완결 -> `_resolved/`로 이동)가 안 돌았다.
오케스트레이터가 임의로 옮기지 않았다 — **위 실측-2처럼 답변이 빈 채로 resolved된 건이 섞여 있어서**,
옮기기 전에 소유자가 한 번 훑는 게 맞다고 판단했다.

```
20260616T0210Z · 20260803T0400Z · 20260803T0540Z · 20260803T0546Z · 20260813T0530Z
20260814T0149Z · 20260814T0538Z · 20260815T0940Z · 20260815T1230Z · 20260817T0231Z
20260817T0400Z · 20260819T0058Z(x2)
```

## 답변 (recipient 작성 — 처리 후)

## 답변 (parser/ifrs17 2026-08-20)

### 처리한 것

| 티켓 | 결과 |
|---|---|
| `20260819T0858Z` publishing 골든 stale | **resolved** — 빌더 결정성 확인 후 골든 재생성(5,389 → 6,215행), pytest 통과 |
| `20260819T0140Z` FS-API negative cache | **resolved** — 흥국화재 2026.2Q **7행 → 19행** |
| `20260820T0052Z` 연1회 공시 5사 raw | **resolved** — 악사 1→3분기, 아이엠라이프 4분기, 교보라플 1→4분기 |
| `20260819T0841Z` 준비금 raw A~D | **resolved** — 2023.1Q·2023.2Q 각 18사 신규, 서울보증 2024.4Q 코어 복구 |
| `20260819T0058Z` amort 단위 | **답변 채움 + 잔여 2사 종결** (실측-2 지적분) |
| `20260820T0430Z` R-RSV 룰 | **answered** — 룰 버그 1건 + 오분류 3건 반증, 근거 첨부 |

### 실측-2 (amort 티켓) 회신

**감사기록을 채웠다.** 그리고 잔여 2사를 끝냈다.
- **서울보증보험 `empty` 는 정상**이다. 보증보험 전업사라 CSM 자체가 없다(`CSM_waterfall.json`
  0행 + 원문 `보험계약마진` 0회). 결측이 아니라 legit zero다.
- **한화손해보험 `partial` → `ok`(38,032억).** 원인은 추출기가 아니라 소스 세대였다. 패널이
  읽는 FY2024 필링에는 상각 스케줄 표가 아예 없고 변동내역 표만 있다. FY2025 필링의 추출본을
  넣으니 정상 인식됐다.
- **`default` 13사는 단위 오류 없음.** 상각합계를 기말 CSM 과 대조했더니 비율 0.88~1.29 로,
  1,000배 오차는 한 건도 없다. **designer TODO 의 "이 패널 숫자를 신뢰하지 말 것" 경고는
  해제해도 된다** (아래 새로 찾은 별건 하나는 owner 판단 대기).

### 실측-3 (흥국화재) 회신

닫았다. 원인은 캐시가 아니라 폴백 범위였다 — 본문 XML 폴백이 총계 4개만 채우고 T자 드릴다운
세부(10~15·20~24·30·31)를 안 채우고 있었다. 그걸 고치면서 **연1회 공시사 전체가 같이 좋아졌고,
악사손해보험의 항목31(이익잉여금) 결측도 해소**됐다.

### 이번에 새로 찾은 것 — 보고와 판단 요청

**1. 🔴 본문 XML BS 폴백이 별도(OFS) 대신 연결(CFS) 표를 고르고 있었다.**
정기보고서 본문에는 "자산"으로 시작하는 표가 한 필링에 4~6개 있고(요약연결재무정보 /
연결재무상태표 / 요약재무정보 / 별도재무상태표 / 전환일 소급표 / 심지어 증감표), 문서 순서상
**연결이 먼저** 나온다. 기존 규칙은 "첫 표를 잡고 break" 라 사실상 연결을 고르는 규칙이었다.
이 마스터의 계약은 OFS 고정이므로 "산수는 맞고 소스가 틀린" 셀이었다.

실측 오염: DB손해 2023.1Q 자산 57.5조(연결)가 별도 44.6조 자리에, 한화생명 2023.1Q 146.6조가
별도 109.4조 자리에. **현대해상 2023.1Q·2023.2Q 는 '자산부채 현황' 증감표를 물어 자산총계가
음수(△1.2조)** 였다.

고친 방식: 후보를 전부 모아 ① 전환일 소급표 최후순위 ② 별도>불명>연결 ③ 총계를 담은 표 우선
④ 요약보다 전체 ⑤ 마감항등식 순으로 순위를 매겨 하나만 고른다. 그래도 새는 2023.1Q 류를 위해
**그 회사 FS-API 실적 대비 자산총계가 ±15% 밖이면 그 표에서 나온 값을 통째로 버린다**
(20건 기각). 틀린 값보다 빈 칸이 낫다는 기존 원칙을 따랐다.

결과: 손실 16셀(전부 음수 쓰레기 또는 연결 오염) · 신규 419셀 · 정정 62셀.

**2. 🔴 민감도 패널이 stale 이었고 골든이 그 stale 을 고정하고 있었다.**
입력을 안 건드리고 패널 빌더만 재실행했는데 **카카오페이손해보험 민감도가 전부 1,000배
줄었다**(해지율 CSM 민감도 △631.97억 → △0.63억). 이 회사는 기말 CSM 이 5억이라 △631억은
성립 불가이므로 새 값이 맞다. 골든 2종을 재생성했다.

**3. ⏸ owner 판단 요청 — 상각 패널의 소스 세대.**
30사 중 **24사가 아직 FY2024 필링(rcept `2025…`)** 을 쓰고 있다. FY2025 raw 는 38사분이 전부
디스크에 있어 오프라인으로 갱신 가능하다(이번엔 한화손보 1사만 그렇게 했다). 전사 갱신은 화면
숫자가 한꺼번에 FY2024 → FY2025 로 바뀌는 변경이라 **승인 후 진행하는 게 맞다고 보고 보류**했다.

**4. ⏸ owner 판단 요청 (재확인) — 연1회 공시사의 중간분기 이월.**
`20260819T0500Z` 에서 이미 올렸으나 미회신이다. 2024.6말 업권 앵커 -19.3% 의 원인을 이번에
특정했는데 **정확히 이 8사**다(라이나 2.25조 · 메트라이프 2.09조 · AIA 0.76조 · AIG 0.29조 ·
IBK연금 0.19조 · 악사 0.16조 · 하나생명 0.06조 · 아이엠라이프 0.01조 = 2023.4Q 기준 5.8조).
이월 여부만 정해주시면 그 앵커는 바로 닫힌다.

### 게이트 현황

```
validate_data_contract.py   RED=2  YELLOW=265   ← 둘 다 R-RSV-9 룰 오탐(아이엠라이프 2022.4Q)
validate_master_tables.py   SUMMARY 동일, sens 만 1R→2R (카카오페이, 위 2번의 정상화 결과)
pytest 골든 15개            전부 통과 (BS·viz패널·master_tables 3종 재생성)
```

**RED 2건은 데이터 결함이 아니라 검증 룰의 기대 그리드 버그**다(해약환급금준비금 제도 시행이
2023 인데 2022.4Q 를 기대 그리드에 넣는다). 근거를 붙여 validation 에 회신했고 별도 메시지도
넣었다. 파서가 baseline 에 임의로 줄을 추가하는 것은 결함 은폐라 하지 않았다.

### 미결로 남긴 것

- `20260616T0230Z` · `20260616T0420Z` (NB CSM 진단파일) — **owner/validation 선택 필요**:
  (1) `extracted_history/*_csm.json` 27건 raw 재추출(진짜 fix, 큰 작업) vs (2) root 마스터 →
  진단파일 sync 스크립트(작지만 재추출 아님). 선택 없이 진행하지 않는다.
- `20260819T0116Z` · `20260819T0500Z` — 미착수 8사 + AIG 준비금 핸들러, BS 전기 컬럼 확장.
- `20260612T0900Z` · `20260803T0520Z` — kics 레인이라 이 세션 범위 밖.

### housekeeping

`resolved` 인데 안 옮긴 13건은 답변 유무를 확인한 뒤 `_resolved/` 로 옮겼다(답변이 비어 있던
`20260819T0058Z` 는 위에서 채운 뒤 옮겼다).

---

### 종결 (owner 확인, 2026-08-20)

**종결 — owner 판단요청 2건 모두 해소됨(실측 확인).**

1. **상각 패널 소스 세대**: 답변 작성 시점엔 '30사 중 24사가 FY2024 필링'이었으나 **이후 라운드에서 갱신됐다.** 실측: `csm_amort_schedule.json` rcept 연도 분포 = **2026년(FY2025) 38사 / 2025년(FY2024) 1사**. 잔여는 현대해상 하나뿐이고 그 회사 FY2025 raw는 디스크에 있다(`FY2025_Q4/raw/KR0009_현대해상_20260312001448`) → 별도 티켓으로 발주.
2. **연1회 공시사 이월**: owner가 직접 결정·반영 완료(위 `20260819T0500Z` 종결 노트 참조).

답변이 '미결로 남긴 것'에 올린 NB CSM 진단파일 2건은 owner가 2026-08-20에 **drop 결정**했다(`_resolved/20260616T0230Z` — 대상 파일이 IFRS17.html에서 이미 폐기). 게이트도 그 뒤 **RED=2 → RED=0**으로 닫혔다(R-RSV-9 오탐 해소 확인).
