---
from: parser
to: designer
created: 20260616T0605Z
status: resolved
route: render
company: MULTI
period: 2026.1Q
lane: kics
iter: 1
depends_on: inbox/publishing/20260616T0600Z__parser__MULTI_2026.1Q__forwardcap_2026q1_rebaseline_confidence_formula.md
---

## 발주 (parser-kics → designer) — K-ICS.html forward 패널 재임베드 (publishing 산출물 종속, BLOCKED)

owner QA(`inbox/parser/20260616T0506Z`) 후속의 **HTML 단계**. publishing이 forward_capital_simulation을
2026.1Q 베이스라인으로 재생성 + 신뢰도 산식 수정(`inbox/publishing/20260616T0600Z`)하면, 그 산출물을
K-ICS.html에 재임베드해야 함.

### 요청 (publishing이 새 forward JSON 산출한 뒤 unblock)
- `K-ICS.html` **L205 `window.FORWARD_DATA`** 인라인 데이터를 publishing의 새 `forward_simulation_v3.json`
  (2026.1Q 베이스라인 + 수정된 신뢰도)로 **교체**.
- `fillForwardConfidence`(L907-936)가 읽는 키 — `row.confidence.level`("모델 신뢰도" 뱃지, L928) +
  `t1_gap_pct`/`t2_gap_pct`("Face vs BS 차이(T1/T2)", L934) — 가 새 산식 결과와 정합하는지 확인.
- 화면 라벨이 **2026.1Q**를 가리키는지(분기 표기 정합). 신뢰도 산식 수정 후 "낮음" 뱃지가 합리적 수로 줄었는지
  육안 검증(preview).

### 주의
- 데이터·산식은 publishing 소관(마스터·forward JSON read-only). designer는 **HTML 구조/임베드/표시**만.
- 음수는 △(세모) 표기 유지. publishing이 `## 답변`에 JSON 경로 남기면 그때 착수.

### unblock 신호
publishing 0600Z가 `status: resolved` + 새 `forward_simulation_v3.json` 경로 회신 → 이 건 `status: open`으로
전환해 착수. 그 전엔 **blocked**(헛작업 방지).

## unblock 신호 (publishing 2026-06-16) — 산출물 준비됨, status blocked→open

publishing 0600Z **resolved**(2026.1Q 재베이스라인 + (a) T2-디커플 신뢰도 완료). 착수 가능.

- **임베드 소스(canonical)**: `templates/forward_capital_latest.json` (= L205 `window.FORWARD_DATA` 교체용). 타임스탬프 사본: `output/kics_forward_capital/20260616T063704Z/forward_simulation_v3.json`.
- **스키마 불변**: `fillForwardConfidence`가 읽는 `row.confidence.level` / `t1_gap_pct` / `t2_gap_pct` **그대로**. 신설 플래그 `t2_util_over_100`만 추가(issue_flags). 표시 변경 불필요.
- **신뢰도 분포**: high=15 · medium=2 · **low=21** (구 22/37 low). "낮음" 뱃지는 **합리적으로 줄지 않은 것처럼 보일 수 있음** — 단 (a) 효과로 T2 개념차 false-low 8개사는 high/medium으로 올라갔고, 남은 21 low은 전부 진짜 데이터갭(fsc_missing_t2 10·T1gap 4·kics_missing_t1 4·t2_util>100 3). 즉 **개념차로 깎인 건 0**. preview 육안검증 시 이 점 참고(낮음 총수보다 "어느 회사가 낮음인지"가 바뀐 게 핵심).
- **분기 라벨 2026.1Q** 정합 확인 요망(baseline_quarter=2026.1Q).
- ⚠️ 워킹트리 `M K-ICS.html`은 세션 전부터 있던 designer 작업분(publishing 무관). publishing은 `--no-html`로 HTML 미터치.

## 답변 (designer 2026-06-16)

완료. `K-ICS.html` L205 `window.FORWARD_DATA` → `templates/forward_capital_latest.json` 교체 완료.

- 레코드 수: 37 → 38 (신규사 1 추가 반영)
- `row.confidence.level` / `t1_gap_pct` / `t2_gap_pct` 스키마 그대로 — `fillForwardConfidence` 변경 없음
- `t2_util_over_100` issue_flags 신설 → HTML 표시 무관, 무시
- 신뢰도 분포 확인: high=15·medium=2·low=21 (inbox 명시값 일치)
- **L1104 baseline 라벨**: `Baseline (2025.4Q 적용후)` → `Baseline (2026.1Q 적용후)` 수정 (분기 표기 정합)
- 스키마 키 `baseline_2025_4Q` 는 JSON 내 그대로 — L1097 `row.baseline_2025_4Q` 참조 유지
- 미리보기 육안 검증: 낮음 뱃지 총수(21)보다 "어느 회사가 낮음인지"가 바뀐 점 참고 (inbox 안내 확인)

push ready — publishing 트리거 가능.
