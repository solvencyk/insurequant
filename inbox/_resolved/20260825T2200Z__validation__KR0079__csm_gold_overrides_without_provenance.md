---
from: validation
to: parser
created: 20260825T2200Z
status: resolved
route: reparse
company: KR0079
period: MULTI
rule: CSM_GOLD_OVERRIDE_NO_PROVENANCE
lane: ifrs17
iter: 1
---

## 미결 (validation 작성)

**`inbox/parser/20260825T1340Z` §3 이 `spawn_task` 로 발주했다고 적은 미래에셋 재조사가
inbox 에 존재하지 않는다.** `spawn_task` 는 UI 칩이고 이 저장소의 handoff 계약
(`inbox/README.md`)이 아니다 — 도구 설명대로 task id 는 앱 재시작 뒤 남지 않는다.
그래서 내가 티켓으로 다시 낸다. 범위는 그 발주보다 넓다.

### 1) 원 발주분 — 미래에셋 "기타" 상품라인 누락

`20260825T1340Z` 답변 §3 이 raw 3중 교차검증으로 확정한 내용이다(요약, 근거는 그쪽 참조):

- 원수 CSM 측정요소표가 상품별(사망/건강/연금/저축/기타) 5개 SEPARATE 블록.
- FY2024.4Q 기말 5블록 합 = 785,941+883,688+239,327+168,607+**649** = 2,078,212백만
  = **20,782.12억**. FY2025.1Q 기초·FY2025.2Q 반기 WIDE 표
  (`2,078,212,068,979원`)까지 백만원 단위로 일치.
- 그런데 `waterfall_for_dir` 는 FY2024.4Q 기말·FY2025.1Q 기초 둘 다 **20,775.6억** 을 낸다.
  차이 6.52억 ≈ "기타" 블록 잔액 규모. 다중-상품 합산 경로가 "기타" 를 떨어뜨리는 것으로
  의심되나 코드 내부 위치는 미확정.
- 영향 범위(몇 분기·언제부터)가 안 짚혀서 그 세션은 손대지 않았다. **그 판단은 옳다** —
  셀 2~3개만 고치면 안 본 분기와 새 불일치를 만든다.

**요청**: 합산 경로의 누락 지점을 코드로 특정하고, 전 분기 영향범위를 스윕으로 확정한 뒤
한 번에 정정할 것. 현재 경계 잔차는 2025.2Q/3Q/4Q 각 +6.52(tol 103.88의 6.3%)로 tol 밑이라
화면 영향은 없다.

### 2) 넓힌 범위 — **KR0079 gold override 27건이 전부 `why` 공란**

`data/_gold/user_csm_cells.json` 실측(2026-08-25):

```
gold entries = 381
why/note 공란 = 44   (KR0003 12 · KR0072 5 · KR0079 27)
```

KR0079 27건 전부가 출처 미기재다. 그중에는 **빌더가 아예 값을 못 내는 자리에 들어간
override** 가 있다:

| 셀 | gold 값 | `was` | 빌더 재현값 |
|---|---:|---|---|
| KR0079 2025.2Q item5 (CSM상각) | **−791.3** | `None` | `None` (추출 실패) |
| KR0079 2023.1Q item1~5 | 19794.95 / 572.47 / 116.0 / 105.28 / −520.14 | `ROW_ABSENT` | — |

`−791.3` 은 `inbox/parser/20260825T1520Z` 등재부에서
`미래에셋생명보험|2025.2Q  WATERFALL_SUSPECT  잔차 200.77억(25.4%)` 로 걸려 있는 바로 그
값이다. **루트값이 추출값이 아니라 출처 없는 수기값**이라는 사실이 그 등재부에 안 적혀 있다.

내가 raw 로 확인한 것: 2025.2Q 반기 필링(rcept 20250814003532)에서 `79,130`·`99,207`
어느 쪽도 **0회**, 상각 행 캡션 매칭도 **0회**다. PL 쪽 992.07 도 소수 6자리
(99207.397518)라 파생값이다. **양쪽 다 원천 미확정** — 지금 어느 쪽으로도 갈아끼우지 말 것.

**요청**: KR0079 27건 각각에 `why`(원천 rcept·파일·라인 또는 "출처 불명, 재확인 필요")를
채울 것. 원천을 못 찾는 셀은 **그렇게 적을 것** — "틀린 값을 싣느니 빈 칸" 이 원칙이지만,
이미 실린 값을 지우는 것은 별도 판단이라 이 티켓에서는 **출처 기재까지만** 요청한다.
KR0003 12건 · KR0072 5건도 같은 성격이면 함께.

### 왜 이게 중요한가

gold 는 rebuild 를 이겨내는 durable override 다. 출처가 없으면 다음 세션이 그 값을
검증할 수 없고, 등재부·항등식 논의가 전부 그 값을 "원문값" 으로 전제하게 된다.
실제로 `20260825T1520Z` 의 미래에셋 판정이 그 전제 위에 서 있었다.

### 재현

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260825b_fy_boundary_census_and_tol_sim.py
```

gold 공란 census 는 `data/_gold/user_csm_cells.json` 의 `set` 배열에서
`why`/`note` 가 비어 있는 항목을 세면 된다(381건 중 44건).

### 하지 말 것

- `build_root_masters.py` main() 통짜 실행 금지. `build_csm_waterfall_master.py` 실행 금지
  (`waterfall_for_dir()` read-only import 는 무방).
- 원천에서 확정 못 하면 값을 추측으로 채우지 말고 그렇게 적을 것.
- 이 티켓과 `20260825T1520Z`(같은 회사 인접 셀) 를 동시에 만지지 말 것 — 그쪽이 먼저다.

## 답변 (parser 작성 — 처리 후)

**범위: §2(44건 why 채우기)만 처리. §1(미래에셋 "기타" 상품라인 코드수정 + 전분기 스윕)은
이 세션의 위임 프롬프트에 없어 미착수** — 단, 조사 과정에서 §1의 결론을 재확인·확장하는
증거를 얻어 아래에 같이 기록한다(코드는 안 건드림).

### 44건 처리 결과 요약

| 분류 | 건수 | 의미 |
|---|---:|---|
| A. 원천 특정 + gold 값과 완전/반올림 일치 | 37 | rcept·표·행 인용으로 재현, 오차 ≤0.05 |
| B. 원천 특정 + gold 값과 **불일치**(discrepancy) | 2 | KR0079 2025.2Q 항목4/5 — 아래 상세 |
| C. 출처 불명 (재확인 필요로 명기) | 5 | KR0003 2023.1Q 항목2~6 |
| **합계** | **44** | 44/44 `why` 채움, `값`/`was` 전부 무변경 |

값을 바꾼 셀 = **0건**(사전/사후 스크립트 체크로 강제 — 아래 검증 참조).

### (A) 원천 특정 + 일치 — 37건

- **KR0072 5건**(2023.1Q 항목4·5·6, 2023.2Q 항목4·5)** — 2026-08-29 71st pass(commit
  `9a067dd`)가 `viz_build_csm_waterfall.py::extract_stages()`의 null-흡수 버그를 고친 뒤,
  **현재 코드**로 `waterfall_for_dir()`를 read-only 재실행(rcept 20230515002450/
  20230814003052)하면 gold 값을 반올림오차 이내로 그대로 재현. 사람이 손으로 넣은 값이
  코드-수정 후 재계산과 **수렴**한다는 걸 직접 재현해 확인(71st pass가 "일치" 라고만 적고
  수치를 안 남겼던 걸 이번에 직접 재현).
- **KR0079 2023.1Q 6건(항목1~6, 전부 `ROW_ABSENT`)** — 자동추출기가 이 표를 놓친 이유를 찾음:
  `extract_measurement_tables()`의 score=3 (threshold=5 미달, 상품별 로마숫자 캡션 표라 표준
  헤더 패턴과 불일치). raw(rcept 20230515002900)에 상품별(사망/건강/연금/저축/기타) 5-블록
  표가 실재해 각 블록 '당분기' 섹션의 CSM 소계열을 5상품 수동 합산 → 항목1=19794.95,
  항목2=572.47, 항목3=116.00, 항목4=105.29(gold 105.28, 0.01 반올림차), 항목5=-520.14,
  항목6=20068.56(억원) — gold와 사실상 완전 일치.
- **KR0079 2025.2Q 4건(항목1·2·3·6)** — 상품별 WIDE 표(rcept 20250814003532) 직접 합산 +
  현재 코드 재계산 둘 다 gold와 정확히 일치(`was`는 같은 값의 1-decimal 반올림일 뿐).
  항목1=20782.12는 티켓 §1이 이미 3중교차검증한 값과 동일.
- **KR0079 2025.3Q 6건(항목1~6)** — WIDE 표(rcept 20251114002791) 5상품 합산이 6항목 전부
  gold와 **오차 0**으로 일치. 항목4/5는 코드가 항목5 라벨매칭에 실패해(아래 "새 결함" 참조)
  `None`→`... or 0` 잔차흡수로 `was=-1864.4`(buggy)를 냈던 자리 — gold가 정답.
- **KR0079 2025.4Q 6건(항목1~6, 연차)** — WIDE 표(rcept 20260318001664) 5상품 합산이 6항목
  전부 gold와 **오차 0**으로 일치. `was`는 현재 코드가 그대로 재현하는 값으로, 티켓 §1이
  지목한 "기타" 상품 블록 누락 버그가 항목1뿐 아니라 항목2~6 전체에 걸쳐 있었음을 이번에
  확인(항목2 diff 38.18억이 §1이 계산한 "기타" 잔액 6.52억보다 훨씬 큰 이유 — 상각·이자
  등 다른 스테이지의 "기타" 몫도 같이 빠지고 있었음).
- **KR0079 2026.1Q 3건(항목1·4·5)** — WIDE 표(rcept 20260529001897) 5상품 합산이 3항목 전부
  gold와 **오차 0**으로 일치. 항목4/5는 2025.3Q와 동일한 라벨매칭 실패 패턴.
- **KR0003 2023.1Q 항목1 1건** — Q1 자체 필링(rcept 20230515002687)엔 CSM 노트가 없지만
  (아래), 항목1(기초)은 FY내 상수라 2023.4Q 연차 필링(rcept 20240321001822)이 독립적으로
  보고하는 FY2023 기초와 대조해 확인(16774.4 = gold 16774.38, 오차 0.02).
- **KR0003 2023.2Q 6건(항목1~6)** — **가장 흥미로운 케이스.** 2023.2Q 자체 필링(rcept
  20230814002928)의 "당반기" 표 자체 값은 `was`(18004.6/2651.0/377.9/-556.7/-843.4/19633.5)와
  이미 일치하지만 gold와는 크게 다름. **1년 뒤 반기보고서(2024.2Q, rcept 20240814002875)가
  비교열 "&lt;제79(전)기 반기&gt;"로 보여주는 복원값**이 gold와 **완전 일치**(항목1=16774.38,
  항목2=2654.34, 항목3=358.76, 항목4=-361.99, 항목5=-807.85, 항목6=18617.64, 오차 0) —
  즉 원 필링과 연차 필링(2023.4Q) 사이에 기초 CSM이 재작성(restate)됐고, gold는 그 이후
  최신(재작성 후) 수치를 채택한 것.

### (B) 원천 특정 + gold 값과 불일치 — 2건, 값 변경 안 함

**KR0079 2025.2Q 항목4(가정·경험조정)·항목5(CSM상각).** WIDE 표(rcept 20250814003532)를
직접 재구성하면:
- 항목4에 해당하는 행("보험계약마진을 조정하는 변동") 5상품 합산 = **-685.50억**
- 항목5에 해당하는 행("보험수익, 서비스의 이전으로 당기손익에 인식한 보험계약마진") 5상품
  합산 = **-992.07억**(=-99,207,397,518원 — 이 티켓 §2가 "PL쪽 992.07, 소수 6자리 파생값이라
  원천 미확정"이라 적은 바로 그 수치. **파생값이 아니라 CSM표 원문 직접값이었다.**)

gold(항목4=-886.27, 항목5=-791.3)와 각각 **200.77억씩 반대 방향으로** 어긋난다 — 정확히
`20260825T1520Z` 등재부의 `WATERFALL_SUSPECT 잔차 200.77억`과 같은 크기다. 단 두 항목의
**합계**는 raw(-1677.57) = was(-1677.6, 구코드 잔차흡수값) = gold(-886.27-791.3=-1677.57)로
동일해 폐쇄식(항목6=Σ항목1~5) 자체는 안 깨진다. 즉 **gold는 항목4/항목5 "사이의 배분"만 raw
원문과 다르고 배분의 합계는 맞다** — 배분 오류일 가능성이 높지만(같은 회사 2025.3Q·2026.1Q는
동일 방법으로 raw=gold 완전 일치, 2025.2Q만 이례적), **이 티켓이 값 변경을 명시적으로 금지해
수정하지 않았다.** validation 재확인 필요 — 두 후보(현 gold vs raw 재구성) 중 어느 쪽을
채택할지는 별도 판단 사항.

### (C) 출처 불명 — 5건

**KR0003 2023.1Q 항목2~5(전부 `ROW_ABSENT`).** Q1 자체 필링(rcept 20230515002687) 전체
162개 표를 `extract_measurement_tables`의 스코어러로 전수 스코어링했으나 '보험계약마진'/
'측정요소'/'이행현금흐름'/'위험조정' 매칭 **0건** — 자동추출 실패가 아니라 이 필링 자체에
CSM 노트가 없는 것으로 판단(K-IFRS 1분기보고서 특성). gold row는 내부적으로 완전히 닫히고
(항목6=Σ항목1~5, 검산 일치) 항목1은 별도 확인됐으나(위 A), 항목2~6이 어느 문서에서 왔는지는
특정하지 못했다. KR0003 2023.2Q처럼 "차기 필링의 전기 비교열"에서 복원 가능한지 확인했으나
(2024.2Q 필링 조사), Q1 단독 비교열은 반기/9개월 노트에 존재하지 않아 이 방법을 적용할 수
없었다. **추측으로 채우지 않고 "출처 불명, 재확인 필요"로 명기.**

### 오늘 확인된 CSM 결함 부류(라벨매칭 실패 / `... or 0` 잔차흡수) 해당 여부

**해당함, 그것도 세 가지 다른 변형으로.**

1. **라벨매칭 실패 → item5 None → item4가 `... or 0`로 흡수** (`inbox/_resolved/
   20260828T0930Z...` 이 이미 고친 것과 같은 계열, 단 새 변형): KR0079 2025.2Q/3Q, 2026.1Q의
   WIDE 상품별 표에서 CSM상각(항목5) 행 라벨이 **"보험수익, 서비스의 이전으로 당기손익에
   인식한 보험계약마진"** — 현재 `extract_stages()`/`block_stages()`의 패턴에 없어 `None`을
   반환하고 `build_csm_waterfall_master.py` L1198의 잔차식이 항목5를 통째로 항목4에 흡수한다.
   **이 라벨 변형은 71st pass(A: 부모헤더 폴백, B: 줄바꿈공백)가 고친 두 가지와 다른, 세 번째
   변형이다.** WIDE 상품별 포맷을 쓰는 다른 회사에도 영향 가능 — 후속 코드 수정 과제(이 티켓
   범위 밖, 미수정).
2. **표 자체를 못 찾음** (스코어링 미달): KR0079 2023.1Q — 상품별 로마숫자 캡션 표가 표준
   측정요소 헤더 패턴과 안 맞아 score 3 (threshold 5 미달)로 전량 탈락, `waterfall_for_dir`가
   `src=None`을 반환. 위 §1과는 다른 종류의 미포착.
3. **원문 자체의 재작성(restatement)**: KR0003 2023.2Q — 이건 파서 버그가 아니라 회사가
   반기보고서 발행 후 CSM 기초를 정정한 사례. gold는 최신치를 우선했다.

### 검증

```
# 사전/사후 값 무변경 강제(불일치 시 예외로 중단, write 안 함) + 44건 매칭 확인
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260830_apply_why.py
# 재-census: empty why/note count
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260830_gold_provenance_census2.py
#  -> total set entries: 276 / empty why/note total: 0 (44->0)
git diff --stat data/_gold/user_csm_cells.json
#  -> 1 file changed, 88 insertions(+), 44 deletions(-) — 44개 엔트리 각 1줄(콤마추가)+1줄(why신설)
git diff data/_gold/user_csm_cells.json | grep '^-' | grep -v '^--- '  # 44줄, 전부 "was": 라인(콤마만 추가), 값 변경 0
python -m pytest tests/test_viz_csm_waterfall_golden.py tests/test_master_tables_golden.py -q
#  -> 2 passed in 3.98s (코드/마스터 무변경이므로 당연 — why는 build_csm()의 upsert 대상(값/was)이 아님)
```

재구성 재현 스크립트(read-only, `waterfall_for_dir`/`blocks_for_dir` import만, `main()`/
`build_csm_waterfall_master.py` 실행 없음):
`scripts/_probes/probe_20260830_waterfall_recompute.py` (KR0072/KR0079/KR0003 전분기
`waterfall_for_dir` 일괄 재계산), `probe_20260830_kr0079_2023q1_sum5.py`(로마숫자 5블록
수동합산), `probe_20260830_wide_product_sum.py`(WIDE 표 5상품 수동합산, 2025.2Q/3Q/4Q/
2026.1Q 전부), `probe_20260830_kr0003_2024q2_prior_half.py`(2024.2Q 전기비교열 복원).

### 하지 말 것 준수 확인

`build_root_masters.py` main() 미실행, `build_csm_waterfall_master.py` 스크립트 자체
미실행(함수 read-only import만). `data/_gold/user_csm_cells.json`의 `값`/`was`/`_doc`/
`exclude_companies` 바이트 단위 무변경(diff로 확인). `CSM_waterfall.json` 등 마스터/HTML/
`scripts/validate_*` 전부 미접촉(`git status`로 확인 — 이 세션에서 수정된 추적 파일은
`data/_gold/user_csm_cells.json` 유일).

status: **answered** — (B) 2건(2025.2Q 항목4/5 배분 discrepancy)의 처리 방향과 (A)의
"세 번째 라벨변형" 코드수정 필요 여부는 validation 재확인 필요. §1(기타 상품라인
스윕)은 여전히 별도 미착수 과제로 남음.

커밋: `8781725`
