---
from: owner
to: downloader
created: 20260819T0116Z
status: resolved
route: refetch
company: MULTI
period: 2026.2Q
iter: 1
---

## 미결 (sender 작성)

`IFRS17_BS.json`에 **2026.2Q가 14사뿐**이다(2026.1Q는 24사). 메리츠화재 등 10사가 통째로 빠져
owner가 마스터 xlsx "17BS" 시트에서 발견했다.

**원인 = FS API 캐시에 빈 응답이 굳어 있다.** `data/dart/_fs_api_cache/<corp>_2026_11012_*.json`
(11012 = 반기보고서 = 2Q)이 `status: 013 / "조회된 데이타가 없습니다" / list 0건`으로 저장돼 있다.
파일 mtime이 전부 **2026-08-15 00:32~02:00** — 반기보고서 접수일(2026-08-14) **바로 다음 날 새벽**이라
DART FS API가 아직 재무제표를 적재하기 전에 긁은 것이다. 빌더는 캐시만 보므로 조용히 결측이 된다.

**지금은 데이터가 있다(오케스트레이터 실측, 2026-08-19 라이브 호출):**

| 회사 | corp_code | 캐시(08-15) | 지금 API |
|---|---|---|---|
| 메리츠화재해상보험 | 00117744 | 013 / 0건 | **000 정상 / 238건 (BS 48)** |
| 삼성생명보험 | 00126256 | 013 / 0건 | **000 정상 / 148건 (BS 36)** |
| 현대해상 | 00164973 | 013 / 0건 | **000 정상 / 221건 (BS 40)** |

## 대상 (2026 / reprt_code 11012 / OFS·CFS 둘 다)

빈 캐시 9사: 메리츠화재해상보험(00117744) · 삼성생명보험(00126256) · 현대해상(00164973) ·
교보생명보험(00112882) · 농협생명보험(00909349) · 동양생명(00117267) · 미래에셋생명보험(00112332) ·
NH농협손해보험(00908155) · KB라이프생명(00160393).

**흥국화재(00103176)는 성격이 다르다** — OFS가 `status 000`인데 BS 행이 **사용권자산 1줄뿐인 빈 껍데기**
이고 CFS는 `013`이다. 빌더 docstring의 "한화손보 2026.2Q OFS 4-row blank shell"과 같은 부류.
재취득 후에도 껍데기면 parser 쪽 본문 XML 폴백 대상으로 넘길 것(본문 XML은 디스크에 있음).

## 요청

1. 위 10사 2026/11012 OFS·CFS 재취득(빈 응답 캐시 덮어쓰기).
2. **재발 방지**: `status: 013`(데이터 없음)은 **캐시에 굳히지 말 것.** 성공 응답만 캐시하거나,
   013은 짧은 TTL로 만료시켜야 한다. 지금 구조는 "공시 직후에 한 번 긁으면 그 분기는 영원히 결측"이다.
   같은 함정이 다음 분기에도 그대로 재현된다(분기보고서 접수 후 API 적재까지 며칠 걸림).
3. 완료되면 parser(ifrs17)에 raw-ready 통지 → `build_ifrs17_bs.py` 재빌드 필요.

## 참고

본문 XML(`data/dart/FY2026_Q2/raw/KR*/`)은 이미 다 받아져 있다(메리츠 4.5MB, 20260814002253.xml).
없는 건 FS API 캐시뿐이다.

## 답변 (recipient 작성 — 처리 후)

**9/10 확보(OFS 전부 BS 항등식 검산 통과), 1건(흥국화재)은 재취득해도 예상대로 빈 껍데기.**

10사 `--refresh` 실행 결과 (OFS 기준):

| 회사 | OFS 상태 | BS행 | 항등식 |
|---|---|---|---|
| 메리츠화재해상보험 | 000 | 48 | OK |
| 삼성생명보험 | 000 | 36 | OK |
| 현대해상 | 000 | 40 | OK |
| 교보생명보험 | 000 | 35 | OK |
| 농협생명보험 | 000 | 32 | OK |
| 동양생명 | 000 | 37 | OK |
| 미래에셋생명보험 | 000 | 44 | OK |
| NH농협손해보험 | 000 | 33 | OK |
| KB라이프생명 | 000 | 33 | OK |
| **흥국화재** | 000 (CFS는 013) | **1**(사용권자산뿐) | — |

흥국화재는 지시하신 그대로 "한화손보 4-row blank shell"류로 확인 — 캐시 문제가 아니라 DART
API 응답 자체가 빈 껍데기. parser에 본문 XML 폴백 대상으로 명시해서 넘김(본문은 이미 디스크에
있음, 지시하신 대로).

**재발방지(요청 #2) 완료** — `scripts/fetch_dart_fs.py`의 `_fetch_raw`를 **status=000만
캐시 파일에 쓰도록** 수정(013은 이번 호출에만 반환, 디스크엔 안 남김). 부수 수정:
`_refresh_cache`가 기존 파일을 먼저 지우고 재fetch하던 구조였는데, 이제 013을 안 쓰다 보니
"지우고 → 013 받으면 → 아무것도 안 남음"이 될 수 있어서 **사전삭제 자체를 제거**(force=True가
성공시에만 덮어쓰므로 실패해도 기존 좋은 캐시가 보존됨). 기존에 이미 확정된 영구결측
(2023 1Q/2Q 24개사, 비상장 15개사 등)은 캐시 파일이 이미 있으니 이번 변경으로 안 건드림 —
매번 재확인하지 않고 그대로 재사용됨.

parser raw-ready 통지: `inbox/parser/20260819T0140Z`.

## 종결 (2026-08-20, status-sweep `20260820T0033Z`에서 owner 확인)

`IFRS17_BS.json` 2026.2Q 14→24사 반영 실측 확인, "downloader 몫은 끝난 것으로 보인다"로
sign-off 받음. 흥국화재 잔여(본문 XML 폴백)는 parser 쪽 몫이라 그대로 넘어감. resolved로
닫고 `_resolved/`로 이동.
