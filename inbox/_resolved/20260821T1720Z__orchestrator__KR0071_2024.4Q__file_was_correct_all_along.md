---
from: orchestrator
to: parser
created: 20260821T1720Z
status: resolved
route: reparse
company: KR0071
period: 2024.4Q
rule: POST_TRANSITION_PARENT_MISSING
lane: kics
iter: 2
---

## 미결 (sender 작성)

**owner 정정. 디스크의 흥국생명 2024.4Q raw 는 처음부터 맞는 파일이었다.**
`inbox/downloader/20260821T1625Z` 의 "DART 사업보고서다" 결론과, 그걸 근거로 한 내 에스컬레이션은
**둘 다 틀렸다.** owner 가 자기 다운로드 폴더의 파일을 직접 제시했고, 디스크 파일과
**md5 가 같다**(`25ee539046c51cf5`, 35,932,312B, 538p).

### 왜 틀렸나 — 내가 이틀 전 문서에 적어놓고 그대로 반복한 함정

`fitz` 로 `"경과조치"` 를 찾으면 538페이지 전체에서 0회가 나온다. 그래서 셋이 차례로
"이 문서엔 K-ICS 내용이 없다" 고 결론냈다. **아니다 — p1~p112 가 통째로 스캔 이미지다.**
텍스트가 잡히는 p113~450 은 재무제표·감사보고서 부분이라 K-ICS 키워드가 없는 게 당연하다.

같은 함정을 흥국화재(KR0005)에서 downloader 가 이미 겪고 240dpi 렌더링으로 풀었으며,
나는 그 교훈을 파서에게 경고까지 해놓고 흥국생명에는 적용하지 않았다.
**키워드 부재는 원문 부재의 증거가 아니다. 텍스트레이어가 있어도 마찬가지다** —
이 문서는 페이지당 533자가 잡히는데도 본문이 이미지다.

### 원문 위치 (렌더링해서 내가 직접 확인)

`data/disclosure/FY2024_Q4/raw/KR0071_흥국생명보험.pdf`

| 페이지 | 내용 | 단위 |
|---|---|---|
| p44 | `[지급여력비율 총괄]` 경과조치 전/후 × 3분기 | 억원 |
| p47 | 경과조치 신청현황표 — **TFI O · 업무보고서 X · TAC X · TIR O · TER O · TIRR X** | — |
| p48 | `[경과조치 적용 전 지급여력비율 세부]` 항목 1~28 × 3분기 | 억원 |
| **p49** | `1) 공통적용 경과조치 관련` 적용전/적용후 2열 | **백만원** |
| **p50** | `② 장수위험·사업비위험·해지위험 및 대재해위험 경과조치` 적용전/적용후 | **백만원** |
| **p51** | `③ 주식위험 경과조치 또는 금리위험 경과조치` 적용전/적용후 | **백만원** |

RED 4건(`POST_TRANSITION_PARENT_MISSING`)에 필요한 적용후 부모값은 **p49~p51** 에 있다.
흥국생명은 TIR·TER 둘 다 신청한 다중 경과조치사이므로 **결합값**이 필요하다 —
각 표는 자기가 줄인 위험만 줄이고 나머지는 적용전 그대로다.

## 할 일

1. p49~p51 을 `get_pixmap(dpi=240~300)` 으로 렌더링해 **직접 판독**하라. fitz 텍스트는 안 나온다.
2. ②③ 결합으로 적용후 항목을 채운다. p44 헤드라인(경과조치 후 지급여력비율·금액·기준금액)이
   **결합 결과의 검산 앵커**다 — 결합값이 헤드라인을 재현하지 못하면 열을 잘못 집은 것이다.
3. **내가 준 페이지 번호도 검증하라.** 나는 이 건에서 이미 한 번 틀렸고, 교보생명에서는
   전기 열을 당기로 오독했다. 표 머리의 분기 표기를 직접 확인하고 넣어라.
4. 셀 단위 수정 + 전/후 census.

## 하지 말 것

- 면제 등재 금지. 원문에 다 있다.
- 키워드 0회를 부재의 근거로 쓰지 마라. 이 티켓이 그 실패의 기록이다.
- `insurequant_master_tables.xlsx` 손대지 마라.

## 답변 (recipient 작성)

**4셀 전부 채움, RED 4건 → 0건. 게이트 실측·hook verdict 아래.**

### 페이지 재검증 (내가 직접 렌더링·판독)

`get_pixmap(dpi=260)`으로 p43-52 렌더링, 전부 native text 0자(스캔) 재확인 — 키워드 미스가 아니라
진짜 래스터임을 재확인했다. 페이지 하단 인쇄 footer(`- 44 -`, `- 47 -`, `- 49 -`, `- 50 -`, `- 51 -`)가
물리적 PDF 페이지 인덱스와 오프셋 없이 정확히 일치함을 확인 후 사용 — 티켓이 준 페이지 번호는
**전부 맞았다**(교보생명 컬럼오독 같은 사고 없음).

| 페이지 | 확인한 내용 | 비고 |
|---|---|---|
| p44 | `[지급여력비율 총괄]` 24.4Q: 경과조치후 지급여력비율=207.0 / 지급여력금액=35,158 / **지급여력기준금액=16,987**(억원) | 결합 검산 앵커 |
| p47 | 신청현황: 공통(TFI)=O · 업무보고서=X · TAC=X · **TIR=O · TER=O** · TIRR=X | 다중경과조치 확인 |
| p49 | `1) 공통적용(TFI)` 표 — 요구자본측(지급여력기준금액 이하) 전=후 완전동일(2,240,443 양쪽) | TFI는 가용자본만 건드림, item2/3후 출처 재확인 |
| p50 | `② 장수·사업비·해지·대재해위험 경과조치`(TIR) 백만원 표 — 생명장기 1,400,224→922,222(사망/장해질병 불변, 장수 68,771→14,272, 해지 821,228→**"-"**, 사업비 214,577→57,537, 대재해 36,294→1,777), 시장위험 674,275 불변, **법인세조정액 428,816→348,792**, **기타요구자본 725,567→599,206** | TIR 단독 축 |
| p51 | `③ 주식위험 경과조치`(TER, 이 회사는 금리 아님) 백만원 표 — 생명장기 1,400,224 불변, 시장위험 674,275→591,521(금리 불변, **주식 339,125→208,389**, 부동산/외환 불변, 자산집중 없음), **법인세조정액 428,816→429,635**, **기타요구자본 725,567→703,625** | TER 단독 축 |

p50/p51 각 표는 **자기 축만 줄이고 나머지는 적용전 그대로**(티켓 지적대로) — 두 표의 법인세/기타요구자본이
서로 다른 값(348,792 vs 429,635, 599,206 vs 703,625)을 찍는 것 자체가 "결합값은 이 표들 중 하나를
그대로 못 쓴다"는 증거.

### 결합 방법 — 재발명하지 않고 기존 검증된 알고리즘을 재사용

`scripts/rebuild_combined_transition_after.py`가 이미 이 정확한 문제(leaf는 "누가 줄였는지"로 합치고,
부모는 R4/R7/MARKET_M 재계산, 기준금액은 헤드라인에 앵커, 법인세는 잔차)를 풀도록 짜여 있고 심지어
`AFFILIATE = {"KR0071": "KR0005"}`(흥국생명 item23=관계회사 흥국화재의 item14 환산치)까지 이미 등재돼
있었다. 다만 그 스크립트의 `main()` 타겟 선택이 "이미 있는데 틀린" 셀만 잡고(`if p15 is None: continue`)
우리 케이스("아예 없음")는 걸러냈다 — 그래서 매트릭스·결합공식·검증배터리를 **import로 재사용**하고
타겟 선택만 새로 짠 신규 스크립트 `scripts/fix_20260821_kr0071_2024q4_post_combine.py`를 상주시켰다.
이 파일이 원 실패("역산 item15 14,747 vs 헤드라인 16,987, Δ2,240" — `_POST_PARENT_NOT_DISCLOSED`
등재사유였던 그 계산)가 왜 틀렸는지도 답한다: item14=item15−item22+item23인데 그 시도는 item15(14,747)를
item14(16,987)와 **직접** 비교했다 — 22/23을 안 빼고 안 더한 것. 내 계산도 item15=14,747.27로 똑같이
나오지만, −item22(3,360.12)+item23(5,599.84)까지 하면 16,987.00으로 정확히 닫힌다.

`scan_occurrences()`(fitz 텍스트 스캔)는 이 PDF에서 0건 반환을 확인(스캔본이니 정상) — 그래서 p50/p51을
**내가 직접 읽은 백만원 표 그대로**를 `occ` 딕셔너리에 수기 입력(스크립트 안 주석에 출처 명시)하고,
그 뒤 R4/R7/MARKET_M 재계산·단조성·잔차법인세 sanity·affiliate 비율 검증은 전부 기존 코드 그대로 통과시켰다.

### 검증 결과 (쓰기 전 전부 통과)

```
affiliate KR0005: item14 전=18112.0 후=13978.0
affiliate ratio k(이 분기)=0.40062  13개 분기 중앙값=0.40061  |k-med|=0.00001   (통과)
life_after(R7)=922221.89 vs 표 922,222(diff 0.11)   mkt_after(M)=591521.16 vs 표 591,521(diff 0.16)
base_pre_calc(R4)=1,943,692.48 vs 표 1,943,692(diff 0.48)  → 적용전 재현 확인
단조성: 결합 기본요구자본후 14,747.27 ≤ min(15,308.54, 18,948.99)   결합 기준금액후 16,987 ≤ min(17,812.68, 21,688.90)
법인세 잔차: 336,011.79백만원 (전 428,816, 1.2배 상한 이내)
identity check (쓰기 직전): item15후-item22후+item23후 = 14747.27-3360.12+5599.84 = 16987.00 vs item14후=16987.00  diff=0.0000
```

교차검증: 위 알고리즘으로 item14/17/19/27/28/36-40후를 전부 독립 재계산했더니 **기존 마스터 저장값과
전부 정확히 일치**(예: item17후 계산=9222.22=저장값, item19후 계산=5915.21=저장값) — 내가 새로 쓰는
4셀 말고는 아무것도 바꿀 필요가 없다는 뜻이라 그 4셀만 썼다(타 항목은 계산·저장값 비교 로그만 남기고
파일에는 안 씀 — 사족 diff 방지).

### 셀 단위 census (before → after)

| 항목 | 값(적용전, 불변) | 값_적용후 before | 값_적용후 after |
|---|---|---|---|
| item15 (Ⅰ.기본요구자본) | 19437 | **None** | **14747.27** |
| item16 (분산효과) | 6167 | **None** | **5249.07** |
| item22 (Ⅱ.법인세조정액) | 4288 | **None** | **3360.12** |
| item23 (Ⅲ.기타요구자본) | 7256 | **None** | **5599.84** |

인접분기 연속성도 자연스럽게 닫힘: item15후 24.3Q 13986.16 → 24.4Q **14747.27** → 25.1Q 16189.02
(SANDWICHED 갭 해소). item23후 24.3Q 5902.86 → 24.4Q **5599.84** → 25.1Q 5794.25.

row_count/combo 변동: UPDATE 4건, INSERT/DELETE 0건(스크립트 write loop이 딱 이 4개 dict만 mutate).
그 외 파일 전체 git diff가 커 보이는 건 이 세션 이전(2026-07-21 마지막 커밋 이후) 오늘 여러 라운드의
미커밋 작업이 이미 트리에 쌓여 있어서다 — `git show HEAD:kics_disclosure.json`과 대조해 내 스크립트
자체의 write 대상이 정확히 4개 dict였음을 코드 레벨로 확인(WRITE_SCOPE 튜플 고정).

### 게이트

- `validate_data_contract.py`: RED **12 → 8**. 흥국생명보험 2024.4Q `POST_TRANSITION_PARENT_MISSING`
  4건 전부 소멸(재실행 grep 확인, 0 hit). 잔존 8건은 전부 이 티켓 밖(신한라이프/교보생명
  `KICS_36_irr`×4 + `TRANSITION_AFTER_IRR_MISMATCH`×4, TODO_parser_kics.md 기존 owner 결정 대기 항목,
  이번 세션에서 안 건드림 — diff 확인함).
- `validate_kics_disclosure.py`: RED=6 불변(36_irr 5 + 8_life 1, 기존 documented, 이 룰셋엔
  POST_TRANSITION_PARENT_MISSING 자체가 없어 원래도 무관).
- golden 배터리(`pytest tests/test_kics_rules_golden.py tests/test_post_transition_golden.py
  tests/test_master_tables_golden.py tests/test_deploy_assets.py tests/unit/`) = **123 passed**, drift 0.
- `sh .githooks/pre-push < /dev/null` (4분2초, coverage manifest 포함):
  ```
  PRE-PUSH VERDICT: gate RED=8 · inbox 기계적위반=0 · offline tests=pass → BLOCKED (fix or owner-escalate)  |  anomaly review queue=83
  ```
  BLOCKED인 이유는 위 잔존 8건(내 스코프 밖) 때문 — 이 티켓의 인수조건(흥국생명 4건→0)은 달성했다.

### 안 한 일 / 못 다룬 것

- 면제 등재 안 함(원문에 다 있었다). `insurequant_master_tables.xlsx` 안 건드림.
- item17/19/27/28/36-40후는 이미 정확했음을 재계산으로 확인만 하고 **쓰지 않음**(요청 스코프=15/16/22/23만).
- 부수 발견(고치지 않음, 이 티켓 스코프 밖): `KR0005`(흥국화재) 2024.4Q **item23 값_적용후=None** —
  이 회사 item23은 다른 모든 분기에서 값 0/값_적용후 0으로 미러돼 있는데 이 분기만 None. RED는 아님
  (핵심 15-21이 이미 채워져 있어 조정항목 단독 break는 review 등급, 비차단) — 별도 spawn_task로 플래그.
- 잔존 8개 RED(36_irr류)는 owner 결정 대기 상태 그대로 둠 — 이 티켓과 무관.

재현:
```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/fix_20260821_kr0071_2024q4_post_combine.py --dry-run
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_data_contract.py
sh .githooks/pre-push < /dev/null
```

---

## 종결 (orchestrator, 2026-08-21)

orchestrator 재확인 2026-08-21: 마스터 실측 — KR0071 2024.4Q 값_적용후 item1=35158 · item2=19510.19 · item3=15647.88 · item14=16987 · item27/28 산출값까지 채워져 있다. 파일이 처음부터 맞았다는 owner 정정도 티켓에 남았다.
