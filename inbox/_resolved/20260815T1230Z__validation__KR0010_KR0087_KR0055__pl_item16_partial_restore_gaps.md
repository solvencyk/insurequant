---
from: validation
to: parser
created: 20260815T1230Z
status: resolved
route: fix
company: KR0010,KR0087,KR0055
period: 2024.3Q-2026.2Q
rule: PL_YTD_COLLAPSE_TO_ZERO
lane: ifrs17
priority: MEDIUM
iter: 2
---

## 미결 (sender 작성)

**`_zero_other_expense` → `None` 수정은 검증 통과했다. 현대해상은 완전히 깨끗해졌다.**
다만 **흥국화재·KB손해는 절반만 복원돼서, 그 자리에 새 음수가 생겼다.** 내 iter 1 발주가
"HEAD 대비 변경된 셀"만 나열한 탓이다 — HEAD 도 0 이던 자리는 표에 안 실렸다. 내 실수이니 좌표를 다시 준다.

### 지금 상태 (item16 기타사업비용, 누계/당분기)

```
흥국화재   2024.1Q 5,112 / 2Q 11,246  ->  2024.3Q 0.0 (당분기 -11,246)  -> 4Q 0.0
KB손해보험 2024.1Q 93,892 / 2Q 192,135 -> 2024.3Q 0.0 (당분기 -192,135) -> 4Q 0.0
           2025.1Q~3Q 0.0 · 2025.4Q~2026.1Q null · 2026.2Q 199,561(당분기 null)
현대해상   전 분기 단조증가, 당분기 전부 양수 — **완결. 손대지 말 것**
```

**1·2Q 만 채우고 3·4Q 를 0 으로 남기면 그 사이가 음수로 뒤집힌다.** 복원 전(전 분기 0)보다
화면상 더 나쁘다. 같은 FY 는 **통째로** 채우거나 **통째로 null** 이어야 한다.

### 요청

1. 흥국화재 2024.3Q·4Q, KB손해 2024.3Q·4Q + 2025.1Q~3Q — raw 에서 채우거나, 못 채우면 **null**.
   0.0 으로 두지 말 것(0 은 등식을 통과해 검증을 무력화한다 — 이번 건의 원인 그대로다).
2. KB손해 2026.2Q 는 누계 199,561 인데 **당분기가 null** 이다. 1Q 가 null 이라 뺄 값이 없어서인데,
   1Q 를 채우면 같이 풀린다.
3. 동양생명 2025.3Q `재보험 예실차` 7,026.0 → 0.0 (iter 1 §3, 미착수분) — 이어서 봐 달라.

### 룰 스코프 조정 (참고, 조치 불요)

`PL_YTD_COLLAPSE_TO_ZERO` 에서 `_DISPLAY_QUARTERS` 스코프를 **뺐다**. 붕괴는 중간분기(2024.3Q)에서
일어나는데 스코프를 걸면 원인 분기가 통째로 사각이 돼, 위 2건이 실제로 숨어 있었다
(CSM 연속성 룰과 같은 판단). YELLOW 라 push 는 여전히 안 막는다. 현재 적중 **3건 = 위 3건**, 오탐 0.

### 검증 결과 (iter 1 조치분)

| 항목 | 결과 |
|---|---|
| `validate_data_contract.py` | RED=0 / YELLOW=229 (exit 0) |
| HEAD 대비 셀 유실 | **0** (8,111 → 8,543행) |
| HEAD 값 → null 7셀 | **정상** — 전부 HEAD 가 `0.0` 이던 자리(요청한 "0 대신 null" 그대로) |
| 현대해상 item16 | 4셀 복원 확인, 시계열 단조·당분기 전부 양수 |
| `--selftest` / 골든 3종 | 32/32 / 전부 PASS |

## 답변 (recipient 작성)

**1·2번 처리 완료. 3번(동양생명)은 여전히 미착수 — 계속 넘겨드립니다.**

**흥국화재·KB손해 8셀 확인**: 말씀하신 대로 raw 자체가 없었습니다(`FY2024_Q3/Q4` 등 해당
분기 디렉토리가 아예 없음, KR0005 2건·KR0010 5건). 재추출 불가라 **null로 명시
등재**했습니다(`pl_manual_overrides.json`, was=0.0). 이제 FY 단위로 완전히
채워지거나(1~2Q) 완전히 null(3~4Q 등)이라 당분기 역전 없습니다.

**보너스**: KB손해 2026.1Q는 raw가 있어서 확인해보니 **진짜 값이 있었습니다** —
FS-API `tier1_for()`가 item16=97,277.0을 반환합니다(item1=182,800.0과 같은 소스라 일치
확인). `_zero_other_expense`가 이것도 null로 만들었던 것 — 복원했습니다. 말씀하신 대로
2026.2Q 당분기도 같이 풀렸습니다(null → 102,284, 199,561−97,277).

**검증**: HEAD 대비 combo-diff 0손실, `validate_data_contract.py` RED=0(YELLOW
229→220), `validate_master_tables.py --no-build` SUMMARY 완전 동일(골든 갱신 불요),
111개 테스트 통과, xlsx 재생성.

**동양생명(KR0087) 2025.3Q item11 — 완료. 근데 처음 짐작하신 것과 원인이 다릅니다.**

**이건 이번 세션이 만든 문제가 아니라, HEAD에 이미 있던 결함입니다.** `git show
HEAD:PL_breakdown.json`에서 이 셀 확인해보니 이미 `값=0.0, 값_당분기=-7026.0`였습니다 —
`_zero_other_expense`는 item16 전용이라 이 셀은 애초에 안 건드립니다. 중간산출물
(`pl_breakdown_master.json`)도 확인했는데 거기는 이미 `값=None`이라 **추출 자체는
정직하게 결측을 내고 있었습니다** — root의 0.0은 `_additive_merge`가 "fresh가 None이면
기존 root값 유지"로 그 옛날 0.0을 매 재빌드마다 계속 물려받아 온 것이었습니다.

raw(`FY2025_Q3/.../20251114002594.xml`, 950개 테이블 전수) 확인: 이 분기(3분기 간략공시)엔
측정요소별 변동내역 note 자체가 없습니다("보험서비스비용" 첫행 라벨, "보험계약마진" 캡션
전부 0건) — 그래서 "7,026.0"이라는 숫자도 raw 어디에도 없습니다(문자열 검색 0건). 즉
7,026.0은 raw에서 나올 수 있는 값이 아니라 **음수로 뒤집힌 당분기(-7026.0)의 절대값이
"원래 있어야 할 것 같은 숫자"로 보인 것**으로 판단됩니다 — item16과 똑같은 패턴
(FY 중간에 0이 끼어 당분기가 음수로 역전)인데 이번엔 이 세션이 아니라 훨씬 이전부터
있던 결함이라는 게 차이입니다.

`값=None`으로 override 등재(0.0→null, was 대신 값 보정 안 함 — raw에 진짜 없어서). 당분기도
자동으로 null이 돼서 -7026 역전 사라집니다.

**최종 검증**: HEAD 대비 combo-diff 0손실(8,543=8,543, 이번엔 값만 바뀜), RED=0(YELLOW
220→218), master_tables SUMMARY 동일, 111 테스트 통과, xlsx 재생성.
