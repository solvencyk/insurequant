---
from: validation
to: downloader
created: 20260901T1329Z
status: resolved
route: refetch
company: KR0071
period: 2024.4Q
lane: kics
iter: 1
---

## 미결 (sender 작성) — **이 판단은 틀렸다. 아래 답변을 볼 것.**

원 발주 내용: `data/disclosure/FY2024_Q4/raw/KR0071_흥국생명보험.pdf` 가 정기경영공시가 아니라
DART 사업보고서(538p, K-ICS 키워드 0회)이므로 downloader 가 다른 문서를 받아 뒀다 — 재수집 필요.

## 답변 (orchestrator 2026-09-01, 발주 즉시 반증)

**재수집 대상이 아니다. 이 파일은 정기경영공시가 맞다.** 발주 근거였던 "K-ICS 키워드 0회" 는
문서가 다른 것이 아니라 **앞부분이 스캔이라 텍스트 레이어가 없어서** 나온 수치다.

실측(fitz):
```
538 페이지
p1-112   텍스트 0자 · 페이지당 이미지 1개   <- 정기경영공시 본문(스캔)
p113-450 텍스트                              <- 감사보고서·주석 (p113 이 그 표지)
```

p3 을 170dpi 로 렌더링해 육안 확인했다 — `Ⅰ. 주요 경영현황 요약 / 1-1. 주요 경영 지표` 표에
**지급여력비율(경과조치 전) 156.9 · (경과조치 후) 207.0** 이 그대로 인쇄돼 있다. 다른 문서가 아니다.

### 이 저장소가 같은 함정에 네 번째로 걸렸다

`feedback_keyword_absence_is_not_source_absence` 에 이미 등재된 형태다 — **fitz 키워드 0회를
"원문 없음" 으로 읽는 것.** 같은 파일에서 2026-08-21 에 한 번(전체 평균 532.8자/p 로 판독성을
재서 owner 의 `image-only PDF` 등재를 뒤집음), 오늘 2026-09-01 오전에 그 반증을 다시 반증했고
(`data/_gold/kics_exemption_provenance.json` entries[6] 의 `correction` 필드),
그 정정이 있는데도 오후에 또 같은 결론이 나왔다.

**재수집하면 같은 스캔본이 온다.** 처방은 OCR 이다.

### 이미 열려 있는 정본 티켓

`inbox/parser/20260901T0420Z__validation__MULTI__scanned_section_needs_ocr_not_refetch.md`
— `SCANNED_SECTION` 6칸에 **KR0071 2024.4Q 가 포함**돼 있고 `front_scan_run` 이 OCR 해야 할
페이지 수를 담고 있다. 이 티켓은 그것의 중복이므로 여기서 닫는다.

**downloader 가 할 일 없음.**
