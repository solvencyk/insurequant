---
from: validation
to: parser
created: 20260901T0400Z
status: resolved
route: reparse
company: KR0083
period: 2026.2Q
rule: TRANSITION_AFTER_IDENTITY
lane: kics
iter: 1
---

## 미결 (sender 작성)

`TRANSITION_AFTER_IDENTITY` RED — `[R1_가용자본=기본+보완] 공시후=25814.0 계산후=14591.21
diff=11222.79`.

**원인 규명 완료: docling MD 가 raw PDF p21 을 통째로 떨어뜨렸다.** 면제 사안이 아니라 재파싱
사안이다.

### 1. MD 에 없고 PDF 에 있는 표

`md_inbox/FY2026_Q2/KR0083_푸본현대생명보험.md` 의 경과조치 절은 `1) 공통적용` 다음에 바로
`②` 로 건너뛴다 — `① 자본감소분 경과조치` 표가 없다. 그런데 같은 MD L358 의 적용여부 표는
`시가평가로 인한 자본감소분 점진적 인식(TAC) | O` 라고 적는다(적용사인데 그 표가 없다).

raw `data/disclosure/FY2026_Q2/pdf/KR0083_푸본현대생명보험.pdf` **p21** 에 멀쩡히 있다:

```
2) 선택적용 경과조치 관련
① 자본감소분 경과조치            (단위: 백만원, %)
  구 분              경과조치 적용 전   경과조치 적용 후
  지급여력비율 (%)          79.54           152.29
  지급여력금액           1,226,933        2,349,256
  기본자본                455,642          455,642
  보완자본                771,291          771,291
  자본감소분 경과조치 적용금액        -        1,122,323
  지급여력기준금액         1,542,582        1,542,582
```

### 2. 그래서 item2후 가 TFI 단독값으로 채워져 있다

| 항목 | 현재 마스터 | 출처 | 있어야 할 값 |
|---|---|---|---|
| item1 값_적용후 | 25,814 | 헤드라인(누적) | 25,814 (그대로) |
| item2 값_적용후 | **4,956.42** | TFI 표 단독 (495,642) | **16,179.65** = 495,642 + 1,122,323 |
| item3 값_적용후 | 9,634.79 | TFI 표 (963,479) | 9,634.79 (그대로) |

검산 — 세 축이 전부 닫힌다:

```
누적 지급여력금액후 = 1,226,933(전) + 232,188(TFI 증분) + 1,122,323(TAC) = 2,581,444 백만
                   = 25,814.44억  -> 헤드라인 25,814 (반올림 일치)
R1 : item2후 + item3후 = 16,179.65 + 9,634.79 = 25,814.44 = item1후   (현재는 14,591.21)
```

같은 구조를 IBK연금 KR1011 이 매 분기 보여준다(그쪽은 ① 표가 MD 에 살아 있다):
`item2후 = TFI기본자본후 + TAC적용금액`. 2026.1Q 실측 1,574.63 + 1,138.89 = 2,713.52 =
마스터 item2후, 소수점까지 일치.

### 3. 요청

1. **KR0083 2026.2Q 를 raw PDF 에서 재변환/재파싱**해 p21 을 살릴 것. MD 만 고치면 다음 변환에
   다시 사라진다.
2. item2후 를 위 누적 기준으로 채울 것. item28후(기본자본비율)는 파생값이므로
   `item2후 / item14후 x 100 = 16,179.65 / 12,221 x 100 = 132.394...` 로 같이 갱신해야 한다
   (현재 40.55658293 = TFI 단독값 기준이라 **화면 숫자가 3배 이상 틀리다**).
3. 값을 넣기 전에 `[지급여력비율 총괄]` 헤드라인(25,814 / 12,221 / 211.24)과 대조해 누적
   기준인지 확인할 것 — 개별 ①②③ 표 단독값을 그대로 넣으면 혼합오류다.

### 4. 왜 validation 이 직접 안 고쳤나

동시 세션 3개가 `kics_disclosure.json` 을 편집 중이고, 이 건은 셀 하나가 아니라 **MD 재생성**이
근본 수정이라 마스터만 손으로 찌르면 다음 파싱에서 되돌아간다. 라인 소관대로 발주한다.

재현:
```bash
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py
# -> TRANSITION_AFTER_IDENTITY 푸본현대생명보험 2026.2Q
```

## 답변 (recipient 작성 — 처리 후)

**고쳤다.** raw p21 을 fitz 로 직접 재확인하고 요청한 그대로 item2후를 병합값으로 채웠다.
MD 재파싱은 하지 않았다(아래 4번 참조).

### 1. raw PDF 재확인 (fitz, 티켓 수치와 완전 일치)

`data/disclosure/FY2026_Q2/pdf/KR0083_푸본현대생명보험.pdf` p21(0-idx 20)을 직접 열어 대조했다:

```
① 자본감소분 경과조치        경과조치 적용 전   경과조치 적용 후
지급여력비율 (%)                79.54            152.29
지급여력금액                 1,226,933         2,349,256
기본자본                      455,642           455,642
보완자본                      771,291           771,291
자본감소분 경과조치 적용금액          -          1,122,323
```

p19(0-idx18, TFI 공통적용경과조치표): 기본자본 455,642 → 495,642 / 보완자본 771,291 → 963,479.
티켓이 인용한 숫자와 1원 단위까지 일치한다.

### 2. 적용한 셀

| 항목 | 이전 | 이후 |
|---|---|---|
| item2 값_적용후 (기본자본) | 4,956.42 (TFI 단독) | **16,179.65** = 495,642 + 1,122,323 (백만원 → ÷100) |
| item28 값_적용후 (기본자본비율, 파생) | 40.55658293 | **132.39219376** = 16179.65/12221×100 |

item2후는 TFI기본자본후(495,642) + TAC적용금액(1,122,323) 전액을 기본자본에 실었다 — ①표
자체는 그 표 안에서 기본자본/보완자본 행이 PRE=POST로 안 움직이는 구조라 tier 배분을 알 수
없지만, 같은 구조를 매 분기 공시하는 IBK연금생명 KR1011의 이미 정상 적재된 값(2026.1Q
item2후=2713.52=TFI기본자본후 1574.63+TAC적용금액 1138.89, 소수점까지 일치)으로 tier 배분을
확증했다. item28은 `scripts/derive_capital_ratios.py`와 동일 산식(그 스크립트가 502버킷
불일치 0으로 확증한 식)으로 재계산했다 — 그 스크립트를 `--overwrite`로 돌리면 2026.2Q **전
회사**의 item27/28이 갱신 대상이 되어(다른 두 에이전트가 같은 마스터를 편집 중이라) 스코프
밖이라 실행하지 않았고, 대신 같은 산식을 셀 단위 patch로 넣었다.

### 3. 검산 (R1, 화면 영향)

```
item2후 + item3후 = 16,179.65 + 9,634.79 = 25,814.44  ≈ 헤드라인 item1후 25,814 (diff 0.44, tol 2.0 이내)
item27후 = 25814/12221×100 = 211.2266 ≈ 공시 211.24 (불변, sanity)
item28후 3배 오류 해소: 화면 기본자본비율 40.56% → 132.39%
```

게이트 실측(`--master` 로 수정 전/후 sandbox 비교 후 실master에서 재확인):

```
transition_identities_after.red 에서 KR0083 사라짐 (R1_가용자본=기본+보완, diff -11222.79 → 0건)
transition_mmult_after / 8_post / 7_post 등 다른 축에 KR0083 신규 RED 없음
core rule-engine(run_validation) findings: KR0083 관련 RED 0건 (불변)
```

### 4. MD 는 고치지 않았다 — patch+reapply 로 내구성 확보

요청한 "raw PDF 재변환"은 **의도적으로 안 했다**. 이번 라운드 다른 에이전트들의 관측(같은
페이지범위를 docling 이 재파싱해도 다른 표를 놓치는 비결정성, 삼성생명 27항목→3항목 요동
실측)에 따라 fitz 직접읽기+마스터 patch 를 우선했다. 그 결과 `md_inbox/FY2026_Q2/
KR0083_푸본현대생명보험.md` 는 여전히 p21(① 표)이 없다 — 이 티켓이 경고한 "MD만 고치면
다음 변환에 다시 사라진다"는 우려는 **MD가 아니라 값을 `data/_derived/_patch_2026q2_
KR0083.json`에 셀단위로 등재**해 대응했다: 이 회사·분기용 patch 파일에 item2/item28을
추가했고, `scripts/reapply_2026q2_patches.py`(fill-chain 재실행 후 상시 재적용 스크립트,
이미 이 회사 다른 셀 5개에 대해 그렇게 쓰이고 있었다)가 이후 어떤 재적재에도 이 값을
되살린다. MD 자체를 되살리고 싶다면(표 렌더링 등 다른 소비처가 MD를 직접 읽는 경우) 별도
재파싱이 여전히 필요하다 — 그건 이번 티켓 범위 밖으로 남겨둔다.

### 5. 적용 방법 / 범위 감사

`data/_derived/_patch_2026q2_KR0083.json`에 item2/item28 셀 추가(기존 5셀은 안 건드림,
병합) → `scripts/apply_2026q2_patches.py data/_derived/_patch_2026q2_KR0083.json` 로 적용
(스크립트 자체 scope-audit: 범위 밖 변경 0건, 행 25202→25202 불변). 세션 시작 시점 백업
(`kics_disclosure.json.bak_20260901_013105_patch`) 대비 전체 diff 재확인: 이번 세션에서
바뀐 셀은 KR0083 2건 + KR0005(별건 티켓) 4건, 총 6건, 그 외 회사·항목 변경 0건.

재현:
```bash
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py
# -> TRANSITION_AFTER_IDENTITY 목록에서 KR0083 사라짐
```
