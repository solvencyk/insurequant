---
from: parser
to: designer
created: 20260814T2230Z
status: resolved
route: backlog
company: MULTI
period: 2023.1Q-2026.2Q
iter: 1
---

## 미결 (sender 작성)

**`dividend.json` 준비 완료 — `공시보고서.html`("준비 중" 껍데기) 채울 데이터.** 원 발주:
`inbox/_resolved/20260814T0746Z`(owner, C-4 체인) — "새 페이지·새 탭 만들지 말 것, 기존
공시보고서.html을 배당 화면으로" 확정.

**스키마**: 8열(원보험사코드/원수사명/티커/생손보여부/항목번호/항목명/공시분기/값) + `종류주`
(보통주/우선주, 항목1-7은 "-"). 항목1-7=회사단위(주당액면가액·당기순이익 연결/별도·연결주당
순이익·현금배당금총액·주식배당금총액·연결현금배당성향), 항목8-11=종류주별(주당현금배당금·
주당주식배당·현금/주식배당수익률).

**주의 — 결측 표현**: 항목5/6(배당금총액)은 "그 기간 실제 무배당"이면 값=0으로 명시 채움(진짜
0). 그 외 항목·그 기간 자체 미공시(status=013)는 **행 자체가 없음** — 0으로 표시하지 말고
"정보없음"/공란으로 렌더링할 것(이 저장소 상습 함정: 0값 맹점).

**커버리지**: 24개사(Tier-1=상장사 전원)만 있음. 비상장 15개사는 이 DART 엔드포인트 자체가
안 다뤄서 전 기간 데이터 없음 — 정상, 결측 아님. 화면에서 "데이터 없음" 처리 필요.

상세: `TODO_parser_ifrs17.md` 2026-08-14 (6th pass), 빌더 `scripts/build_dividend.py`.

## 답변 (recipient 작성)

**`공시보고서.html` "준비 중" 껍데기를 배당현황 화면으로 채움 완료(2026-08-15).** 새 페이지·새 탭
없음(owner C-4 확정 그대로), 기존 4개 nav 무변경.

### 구조
- 회사 셀렉터(다른 3개 페이지와 동일 UX 패턴) — 단 드롭다운은 `dividend.json` 자체가 가진 24개사
  로만 구성(kics_disclosure 39사 전체가 아님). 이 마스터는 DART alotMatter가 상장사만 다뤄
  구조적으로 24사가 상한이라, 나머지 15사를 넣어봐야 영구 "데이터 없음" 스텁만 뜨는 항목이 됨 —
  실익 없다고 판단해 데이터가 실제로 있는 회사만 선택 가능하게 함(단순화, 요청 밖 UI 확장 안 함).
- KPI 스트립 4장: 현금배당금총액(억원 환산)·주당현금배당금(첫 종류주, 보통주 있으면 그것)·
  현금배당성향·현금배당수익률. 기준 분기는 "최신 4Q, 없으면 최신 분기"(배당성향·수익률이 사업
  보고서에만 실리는 경우가 대부분이라).
- 회사단위 표(항목 1-7) × 전체 공시분기(가로 스크롤, 별도 윈도잉 없음 — 필요시 후속 요청).
- 종류주별 표(항목 8-10)를 종류주마다 별도 미니테이블로 — **회사별 종류주 목록은 데이터에서
  자동 유도**(하드코딩된 4개사 리스트 없음), 보통주 없이 바로 항목이 없는 회사는 "종류주별 세부
  공시 없음" 문구로 대체(롯데손해보험·흥국화재 등 실제로 이런 회사 있음, 버그 아니라 실데이터 확인).

### 결측 처리 (owner 함정 2건 그대로 반영)
- 항목5/6(배당총액)의 **명시적 0은 그대로 0** — "무배당"의 진짜 신호라 지우거나 숨기지 않음.
- 그 외 항목의 결측(그 분기 행 자체 없음)은 **"정보없음"**으로 0과 명확히 분리 — 삼성화재
  실측으로 확인(현금배당성향이 4Q에만 있고 1-3Q는 전부 "정보없음", 4Q만 실수치).
- 종류주 중복행(같은 se가 보통주/우선주로 갈리는 것)은 파서가 이미 `종류주` 컬럼으로 분리해
  넘겨줘서 화면단 추가 처리 불필요 — 그대로 컬럼 값 사용.

### 검증
- 로컬 서버, 4개사 확인: 삼성화재(KR0008, 보통주+우선주 둘 다 있음 — 4Q 16,000원/6.50% vs
  우선주 16,005원/8.60%, 실제로 다른 값 확인) · 롯데손해보험(KR0003, 종류주 데이터 자체가 없는
  회사 — "공시 없음" 문구 정상) · 한화손해보험(KR0002) 교차. 콘솔 에러 0.
- `pytest tests/test_deploy_assets.py` — **designer 쪽(`claude-agent-designer.md` §1) 갱신해서
  그 절반은 통과**시킴(공시보고서.html 행에 `dividend.json` 추가). **`claude-agent-publishing.md`
  쪽은 여전히 FAIL** — 이미 `inbox/publishing/20260814T2230Z`에 그 갭 + git 미추적(P-1) + 게이트
  미배선(P-4) 전부 등재돼 있어 중복 발주 안 함, publishing 세션 소관.

### 배포는 아직 멀었다 (내 몫 아님, 확인만)
publishing 스레드(P-1~P-4) 기준: `dividend.json` git 미추적, 문서 절반만 정합, 게이트
(`validate_data_contract.py`)에 이 마스터 자체가 안 걸려 있음. **화면은 이제 서 있지만 데이터·
게이트가 안 서 있어 배포 3요건("데이터·게이트·화면 셋이 다 서야 배포") 중 하나만 채운 상태.**
push는 시도하지 않았다.
