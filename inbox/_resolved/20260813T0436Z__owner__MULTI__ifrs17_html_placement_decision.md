---
from: owner
to: designer
created: 20260813T0436Z
status: resolved
route: backlog
company: MULTI
period: 2023.1Q-2026.1Q
iter: 2
---

## 미결 (sender 작성)

**같은 날 발주된 `20260813T0422Z__owner__MULTI__aoci_panel_spec.md`의 후속이다.**
그 발주문 마지막 줄이 "배치안은 답변에 적고 owner 결정을 받을 것"이었는데,
**owner가 먼저 답을 줬다.** 아래가 그 결정이다. 배치 질문은 이걸로 종결이니 다시 묻지 말 것.

### 결정 1 — 배치 = `IFRS17.html` 신규 섹션 (신규 페이지 아님, K-ICS.html 아님)

owner 원문: "17 기준 자산/부채/자본(>AOCI>해약환급금준비금까지 가능하면 narrow down) 정도를
IFRS17.html에 **별도 칸 할애**해서 보여주면 될 거 같은데?"

현재 `IFRS17.html`은 `1) CSM 이동` ~ `6) 민감도(ΔCSM)` 6개 섹션이다(L98~L129).
그 뒤에 **`7) 재무상태표 · 자본의 질`** 을 추가한다. 기존 6개 섹션은 건드리지 말 것.

### 결정 2 — 범위 = 3단 드릴다운 (owner의 "narrow down"이 이 뜻이다)

```
L1  자산총계 · 부채총계(그중 보험계약부채) · 자본총계      ← 항목 40/41/42, 1
L2      자본총계 → 자본금·자본잉여금·신종자본증권·        ← 항목 2/3/4/5/6/7
            이익잉여금·AOCI·자본조정
L3-a        AOCI → 자산측 FVOCI평가손익 vs 부채측          ← 항목 20~30 (워터폴)
            보험계약순금융손익 미스매치 분해
L3-b        이익잉여금 → 법정준비금 3종                    ← 항목 10/12/14
            (해약환급금준비금 강조)
```

- 원 발주문의 **패널 A(AOCI 워터폴) = L3-a**, **패널 B(자본구성 스택) = L2+L3-b** 다.
  새로 그리라는 게 아니라 **그 두 패널을 이 드릴다운 안에 배치**하라는 뜻.
- L1은 새로 추가되는 층이다. 파서에 항목 40~49를 추가 발주했다
  (`inbox/parser/20260813T0436Z__owner__MULTI__bs_drilldown_scope_extension.md`).
- **패널 C(업권/피어 추이)는 이번 섹션에서 뺀다.** owner가 요구한 건 "별도 칸" 하나지
  페이지 재편이 아니다. C는 보류 — 나중에 owner가 원하면 별건으로.

### 결정 3 — 드릴다운 UX는 페이지 이동 없이

L1 막대/타일을 누르면 그 자리에서 L2가 열리고, L2의 AOCI·이익잉여금을 누르면 L3가 열리는
형태. 탭이든 아코디언이든 클릭-투-익스팬드든 designer 판단. 다만:

- 초기 상태는 **L1만 펼침** — 섹션이 열리자마자 6단 차트가 쏟아지면 안 된다.
- 키보드 접근 가능해야 한다(`common.css` A11y 베이스라인, 원 발주문 공통규칙).
- **회사 선택은 페이지 기존 셀렉터를 재사용**하라. 섹션 전용 셀렉터를 새로 만들지 말 것.

### 유지되는 것 (원 발주문 그대로, 반복 안 함)

0선 교차 `custom renderItem` 필수 · △(세모) 음수 표기 · 데이터 HTML 인라인 금지 ·
색맹 안전(자산측/부채측을 색만으로 구분 금지) · 해약환급금준비금은 **AOCI 하위가 아니라**
이익잉여금 내 법정적립금 · **게이트 RED 0 전까지 목업까지만, 라이브 배포 금지**.

## 답변 (recipient 작성)

**구현 완료 (2026-08-14, mockup)** — `IFRS17.html`에 "7) 재무상태표 · 자본의 질" 신규 섹션 추가.
기존 6개 섹션(마크업·JS 모두)은 변경분에 포함되지 않음 — diff는 신규 섹션 추가 + 공유 유틸(`charts`
객체·`destroyCharts`·resize 핸들러)에 대한 최소 확장뿐.

### 데이터 배선
`equity_composition.json`(루트, 항목 1-49)을 `PATHS.eqx`/`ix.eqx`로 기존 `wfx`/`plx`와 동일한
(코드→분기→항목번호) 롱포맷 인덱스 패턴으로 `boot()`에 배선. 항목번호 자체는 owner가 확정한
마스터 스키마 상수를 그대로 참조(이 결합도는 CSM/PL 패널의 `wfVal(company,qtr,num)`과 동급).

### L1 (결정 2의 자산총계·부채총계·자본총계 = 항목 40/41/42/1)
3개 타일. 부채총계 타일에 보험계약부채 비중(42÷41, %) 인라인 표기. 자본총계 타일만 버튼
(`aria-expanded`, L2 토글) — 자산·부채 타일은 이 스펙에서 하위 드릴다운이 없어 정보성 타일로 유지.

### L2 (항목 2-7, = 원 발주문 패널 B의 절반)
자본총계 구성 6행(자본금/자본잉여금/신종자본증권/이익잉여금/AOCI/자본조정). **100%-스택 막대 대신
행별 크기비례 막대**로 구현했다(디자이너 판단 — "designer 판단" 위임 범위): AOCI·자본조정은 상시
음수 가능이라 스택 방식은 음수 구성요소에서 시각적으로 깨진다(가로폭 방향이 반대로 접힘).
행 방식이면 부호와 무관하게 안정적이고, 각 행에 △ 표기 실수치를 그대로 병기할 공간도 확보된다.
이익잉여금·AOCI 두 행만 버튼(L3-b/L3-a 토글) — 나머지 4행은 정보 표시만.

### L3-a AOCI 워터폴 (항목 20-31, = 원 발주문 패널 A = 결정 2의 L3-a)
**기존 PL 워터폴(`renderPlWaterfall`, Panel 4)의 0선교차 custom renderItem `[y0,y1]` 패턴을 그대로
재사용**했다 — 발주문이 지목한 "기존 CSM 워터폴/PL 워터폴 렌더러"가 이것이고, 재구현하지 않았다.
단계: 기초(20) → ①FVOCI금융자산평가손익(21, 자산측) → ②보험계약자산(부채)순금융손익(22, 부채측)
→ 재보험(23) → 위험회피(24) → 기타(25+26+27+28+**31**) → 기말(30). "기타"에 항목31(소유주거래 등
AOCI변동 — 파서가 같은 날 저녁 P2-3에서 추가)을 포함시켜 최신 롤포워드 공식 `20+29+31=30`과
정확히 닫히도록 구성(21..28의 합은 정의상 29와 같으므로 +31을 더하면 정확히 기말과 일치).
①과 ②를 인접 배치해 "시각적으로 마주보게" 요구를 충족. **색맹 안전**: 부채측(②) 막대에 ECharts
`decal`(대각선 해치 패턴) 추가 + x축 라벨에 "(자산측)"/"(부채측)" 텍스트 병기 — 색상 단독 구분 없음.

### L3-b 법정준비금 (항목 5,10,12,14, = 원 발주문 패널 B의 나머지 절반)
해약환급금준비금(10)은 accent 배경(연한 파랑)+굵은 글씨로 강조, 비상위험(12)·대손(14)은 보통,
"기타 이익잉여금(미처분 등)"(=5−10−12−14 잔여)은 톤다운(opacity). 원 발주문은 "해칭/톤다운"을
예시로 들었는데 정확한 표현 형태는 위임 범위로 판단해 accent+굵기 조합을 택함(목적은 대비 확보).

### 결정 3 — 드릴다운 UX
- 초기 상태 L1만: L2/L3 컨테이너가 템플릿에서 기본 `hidden` — 회사 변경 시 `mountDash`가 매번
  템플릿을 새로 clone하므로 자동으로 초기상태 리셋(별도 상태관리 불필요).
- L1 자본총계 → L2, L2 이익잉여금/AOCI → L3-b/L3-a. **아코디언 방식 선택**(designer 판단):
  L3-a/L3-b 중 하나만 동시에 열림 — 섹션을 컴팩트하게 유지.
- 키보드 접근: 전부 네이티브 `<button>`(div+onclick 아님) — Tab 도달, Enter/Space 활성화가
  브라우저 기본 동작으로 보장됨. `aria-expanded`/`aria-controls` 배선. focus-visible은 common.css
  전역 규칙(`:focus-visible{outline:2px solid var(--primary)}`) 상속, 별도 CSS 불요.
- 회사 선택은 기존 `#company` 셀렉터 그대로 재사용 — 신규 셀렉터 없음. 부수효과: equity 마스터
  커버리지가 CSM/PL과 달라(kics 39사 앵커) 드롭다운에 회사가 몇 개 더 나타날 수 있음(정상 —
  "재사용" 요구의 자연스러운 결과이자 사이트 전체 회사 커버리지 확대).

### 유지되는 것 확인
음수 △는 기존 `samo()`/`fmtEokKR()` 재사용(신규 포맷터 없음) · 데이터 HTML 인라인 없음(전부
`fetch`) · `common.css` 토큰·link 순서 무변경 · 게이트 RED 0 전까지 라이브 배포 안 함(아래).

### 검증 (로컬 `python -m http.server 8896`, `.claude/launch.json` "Dashboards (static)")
- 항등식 육안 검산: 삼성화재(KR0008) 자산 97.88조 ≈ 부채 74.48조 + 자본 23.40조. L2 6행 합 =
  자본총계(반올림 오차 1억원 이내). L3-b 4행 합 = 이익잉여금 정확히 일치(13조 7,198억원).
- 0선교차 실데이터 검증: KB라이프생명 2026.1Q에서 `echarts.getOption()`으로 실제 렌더 좌표를
  직접 조회 — ①막대 [31,467 → △975,383], ②막대 [△975,383 → 86,012] 둘 다 부호반전 확인,
  렌더 에러 없음(PL 워터폴과 동일 코드경로라 신뢰도 높음). 흥국화재(발주문 워크드 예제 회사)도
  별도 확인.
- 결측 경로: 카카오페이손해보험(equity 0행 — validation 노트에 기록된 그 회사) 선택 시 스텁
  메시지 정상 노출, `bsWrap` 숨김 확인.
- 콘솔 에러: 회사 8회 전환 + 드릴다운 반복 토글 전 구간 0건.
- **미검증 (세션 도구 한계 — 코드 결함 아님)**: 이 세션은 Browser pane이 compositing되지 않는
  상태(스크린샷 시도 시 "pane is not displayed"로 실패)라 실제 키보드 Enter 활성화·375px 실뷰포트
  픽셀검증을 끝까지 못 했다. 대조군으로 **기존 프로덕션 버튼**(`.subtoggle`, Panel 4 "재보험 세부
  펼치기")에 동일한 Enter 키 이벤트를 보내봐도 똑같이 무반응 — 세션 전역 한계이지 신규 코드만의
  결함이 아님을 확인했다. 구조적으로는 네이티브 `<button>` + 기존 640px 미디어쿼리 패턴 그대로라
  위험은 낮다고 판단하나, 다음 세션에서 실뷰포트가 되는 환경에서 재확인 권장
  (`TODO_designer.md` BS-DRILLDOWN 항목에 등재).

### Gating — 라이브 배포 안 함
`equity_composition.json`은 루트에 존재하고 화면은 이를 정상 소비하지만, validation 게이트가
이 답변 작성 시점 **RED=207**(파서 재검증 3라운드째 진행 중 —
`inbox/validation/20260813T0422Z…rules_and_gate.md` · `inbox/parser/20260813T1330Z…red_round2.md`
참조)이라 발주문 gating 조건(RED 0)을 아직 만족 못 함. **push/배포는 시도하지 않았고, 이 세션은
로컬 워킹트리 변경만 남긴다.** RED=0 확인 + publishing keep-list 등재 + owner 승인 이후 배포
판단 — 그 판단은 이 스레드가 아니라 validation/publishing 스레드가 정본.

status: resolved(designer 몫 완료 — 배치·구현 전부). 배포 여부는 별도 스레드 소관.
`_resolved/`로 이동.
