---
from: validation
to: validation
created: 20260824T0410Z
status: resolved
iter: 3
route: blind_spot
company: KR0068
period: 2025.2Q
rule: 2_tier1_bridge
lane: kics
---

## 미결 — 한화생명 2025.2Q 다리 잔차 −30,095 의 **인과가 규명되지 않았다**

`inbox/parser/20260821T1425Z` iter-6 §7 에서 "후속 티켓" 으로 남긴 항목을 분리한다.
그 티켓은 3,000줄이라 이 한 건이 묻힌다.

**면제하지 않는다. RED 로 남긴다.** 이유는 아래 §3.

### 1. 실측 (2026-08-24 기준, 게이트 재현)

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py
  -> RED [2_tier1_bridge] KR0068 2025.2Q diff = -30,095.0   (blocking RED 중 최대)
```

마스터 값(전부 raw p16-17 과 정확히 일치함을 parser iter-3 이 확인, validation 재확인):

```
item4(Ⅰ 순자산)         213,475
item12(Ⅱ 불인정)          30,921
item13(Ⅲ 재분류)         100,874
item47(한도 적용 전)      ...       item48(한도)  ...   한도초과 = 70,821.30
item2(공시 기본자본)       82,506
```

`item4 − (item12 − 한도초과) − item13 = 213,475 + 39,900.29 − 100,874 = 152,501.29`
→ 실제 82,506 대비 **+69,995.29**. 클램프(한도초과 ≤ item12) 적용 후 잔차 −30,095.

### 2. 남아 있는 단서 (인과 아님 — 패턴일 뿐)

TFI 표의 **보완자본 전후차 825.75** 가 다리 잔차 계열 중 하나(826)와 거의 같다.
**"거의 같다"는 인과가 아니다.** 이 단서만으로 면제하면 패턴을 원인으로 단정하는 것이고,
이 저장소가 반복해서 사고를 낸 방식이 정확히 그것이다.

### 3. 왜 면제하지 않나

같은 라운드에 등재한 14버킷은 전부 **원문에서 모순을 직접 보여줄 수 있다** —
"두 표가 서로 다른 값을 인쇄한다"(코리안리 7분기) 또는 "한 표가 자기 구성행과 안 닫힌다"
(롯데·BNP·동양). 이 버킷은 둘 다 아니다: 값은 전부 원문과 맞고, 어느 표가 어떻게 모순인지
말할 수 없다. **모르는 것을 사면하지 않는다.**

### 4. 다음에 해 볼 것 (조사 방향, 순서대로)

1. **`item12` 자체의 스코프 확인.** 한화생명은 클램프가 발동하는 10칸 중 유일하게 클램프 후에도
   안 닫히는 버킷이다. `한도초과(70,821.30) > item12(30,921)` 가 2배 넘는데, 다른 9칸은 클램프
   후 정확히 닫힌다. 이 회사의 `Ⅱ. 지급여력금액으로 불인정하는 항목` 이 다른 회사와 다른
   구성(예: 한도초과분을 애초에 제외하고 인쇄)인지 raw 각주로 확인.
2. **같은 발행사 이웃 분기 대조.** 2025.1Q · 2025.3Q 가 닫히는지. 이 회사만/이 분기만인지가
   갈린다(면제 등재의 필수 대조군).
3. **item47 의 스코프.** 이 회사는 iter-3 §2 에서 "보완자본 = 한도적용전 그대로(한도로 안 잘림)"
   패턴의 대표 사례였다(2023.1Q raw `보완자본 10,563,959 = 한도 적용 전 10,563,959`, 한도
   5,952,436 보다 큰데도 안 잘림). 그렇다면 `한도초과 = max(0, 47−48)` 이라는 우리 계산 자체가
   이 회사에서 의미가 다를 수 있다.

### 5. 하지 말 것

- **825.75 ≈ 826 으로 잔차를 박제하지 말 것.** 잔차 크기가 −30,095 인데 826 을 근거로 대면
  숫자가 안 맞는다. 사유가 틀린 면제는 다음 세션의 잘못된 일반화 씨앗이 된다(iter-6 에
  롯데 2024.4Q 에서 실제로 사유를 바꿔 적은 전례).
- 허용오차를 건드리지 말 것.


---

## sender 재확인 (validation, 2026-08-24) — **owner 가 면제를 결정했다. 등재했다.**
## **status 는 `open` 그대로 둔다 — 면제는 push 를 푼 것이지 원인을 닫은 게 아니다.**

### 1. 무엇이 바뀌었나

위 §3 에서 나는 "모르는 것을 사면하지 않는다" 며 RED 를 유지했다. **그 판단 자체는 지금도
맞다고 본다** — 이 버킷은 여전히 어느 표가 어떻게 모순인지 말할 수 없다. 바뀐 것은 근거의
종류다. **owner 가 raw PDF 를 직접 열어 보고 결정했다(2026-08-24):**

> "흠 그러네? 차이가 있는건 사실인데 원문이 그렇게 적혀있고, 별다른 언급은 없어.
> 왜그런지 모르겠네. 걍 원문대로 오차 용인하고 처리하자"

**owner 가 원문을 직접 본 것은 이 저장소에서 얻을 수 있는 가장 강한 provenance 다.** 다만
그것은 "인과가 규명됐다" 와 **다른 종류의 근거**이므로, 그 차이가 기록에서 지워지지 않게
했다(§3).

### 2. 박제값 — **826 이 아니라 −30,095.00 이다.** 위 §5 의 경고가 그대로 유효하다

발주에는 박제 잔차가 **826.00** 으로 적혀 왔다. 근거 산수도 맞다:

```
item4 213,475 − item12 30,921 − item13 100,874 = 81,680
item2 82,506 (공시)                              차 826.00
```

**그런데 룰이 실제로 emit 하는 diff 는 −30,095.00 이다.** 각주 주2)의 괄호("보완자본 한도를
초과한 금액을 제외")를 룰이 문자 그대로 읽기 때문이다:

```
한도초과 = min(item47 − item48, item12) = min(140,128.28 − 69,306.99, 30,921)
         = min(70,821.29, 30,921) = 30,921        <- branch=CAPPED
기대 item2 = 213,475 − (30,921 − 30,921) − 100,874 = 112,601
잔차       = 82,506 − 112,601 = -30,095.00
```

**826 을 박으면 등재 즉시 `TIER2_EXEMPTION_RESIDUAL_DRIFT` RED 가 되어 면제가 아예 성립하지
않는다**(실측 확인했다). 박제는 owner 에게 제시된 숫자가 아니라 **룰이 내는 값**이어야 매 실행
재검산이 성립한다. 두 값은 같은 축의 같은 불일치를 각주 해석 둘로 잰 것이다.

**두 값을 원장에 다 적었다** — `expected_residual` = −30,095.0 ·
`expected_residual_alt_reading` = {826.0, `_why`}. 안 적으면 다음 세션이 "박제값이 발주와
다르다" 로 읽고 826 으로 고치거나(→ 조용한 RED) 허용오차를 건드린다(위 §5 가 금지한 것).
`test_the_two_readings_of_the_hanwha_residual_are_both_recorded` 가 기계로 강제한다.

**위 §5 의 "825.75 ≈ 826 으로 박제하지 말 것" 은 지켰다** — 825.75 는 사유가 아니라
`owner_confirmation.open_lead` 에 **미규명 단서**로만 적었다.

### 3. 근거의 종류를 status 로 갈랐다 — `VERIFIED_BY_OWNER` 신설

같은 레지스트리의 다른 17건은 발행사 자기모순을 **산수로** 증명한다. 이 건은 아니다. 같은
`VERIFIED` 로 적으면 다음 세션이 **인과가 규명된 것으로 오독한다** — 이 저장소가 반복해서
데인 형태다. 그래서 원장 status 를 새로 만들고 게이트에 배선했다:

| 게이트 동작 | 내용 |
|---|---|
| 마커 검사 | **`VERIFIED` 와 동일하게 그대로 건다.** owner 판단은 "이 잔차를 용인한다" 이지 "숫자를 다시 안 봐도 된다" 가 아니다. 비우면 `EXEMPTION_VERIFIED_WITHOUT_MARKERS` RED |
| `owner_confirmation` | {read_by, date, what_was_read, verdict} **필수.** 누락시 `EXEMPTION_OWNER_RECORD_INCOMPLETE` RED — "누군가 확인했다" 는 산문과 같다 |
| 매 실행 인쇄 | `EXEMPTION_STANDS_ON_OWNER_JUDGEMENT` **review**. 조용해지지 않는다 |

변이시험 4건 신설: `test_an_owner_judgement_entry_must_say_who_read_what_and_when` ·
`test_an_owner_judgement_entry_still_has_to_pass_the_marker_check` ·
`test_an_owner_judgement_exemption_never_goes_quiet` ·
`test_the_two_readings_of_the_hanwha_residual_are_both_recorded`.

**이 status 는 선례가 아니다.** 원장 `scope` 에 "인과 미규명이므로 다른 회사·분기에 같은 사유를
적용하지 않는다" 를 박았다.

### 4. 해제조건 — 발주 요구대로 입력 4칸 전부 배선했다

`item2 · item4 · item12 · item13` 중 **하나라도** 움직이면 `TIER2_EXEMPTION_INPUT_DRIFT` RED,
결측이면 `..._INPUT_MISSING` RED. 변이시험이 네 칸을 **각각** 흔들어 증명한다
(`test_input_drift_revives_the_red[KR0068-2025.2Q-{2,4,12,13}-값]`).

갈래를 정하는 `item47 · item48` 과 위 §2 의 미규명 단서 `item51`(전 139,302.53 / 후 140,128.28)
도 같이 박았다 — 그 관계가 움직이면 자동 RED 다. 갈래가 `CAPPED` 를 벗어나면 flag 대조가
`TIER2_EXEMPTION_RESIDUAL_DRIFT` 로 잡는다(`test_the_hanwha_pin_catches_a_branch_change_...`).

### 5. raw 독립 재확인 (owner 가 본 숫자를 내가 다시 쟀다)

`data/disclosure/FY2025_Q2/raw/KR0068_한화생명.pdf` fitz 직접 판독:

```
p17 [경과조치 적용 전 지급여력비율 세부] (억원)
    지급여력금액 221,809 = 기본자본 82,506 + 보완자본 139,303      <- 헤드라인은 닫힌다
    Ⅰ 순자산 213,475 · Ⅱ 불인정 30,921 · Ⅲ 재분류 100,874 · SCR 138,614
    순자산 자기 구성행 합 48,289+30,685+85,295-8,459-26,801+36,220+48,247 = 213,476 (반올림 1)
    주2) "… 순자산에서 지급여력금액 불인정 항목(단, 보완자본 한도를 초과한 금액을 제외) 및
         보완자본으로 재분류하는 항목을 차감한 금액"
p18 [지급여력비율의 경과조치 적용에 관한 사항] (1) 공통적용 (백만원)
    22,180,868 / 22,263,443 · 기본 8,250,615 / 8,250,615 · 보완 13,930,253 / 14,012,828
    한도적용전 14,012,828 · 한도 6,930,699 · 초과분 6,999,555 · (신종) 0 · (후순위) 1,649,118
    TFI 구성식 6,930,699 + 6,999,555 = 13,930,254 vs 13,930,253 (반올림 1, 닫힌다)
```

**마스터 값은 두 표 모두와 정확히 일치한다.** 즉 위 §4 의 조사 방향 1·3(item12·item47 스코프)은
"우리가 잘못 읽었나" 가 아니라 **"발행사가 왜 그렇게 적었나"** 를 묻는 것이 맞다.

### 6. 이 티켓이 여전히 열려 있는 이유 — 남은 조사 방향

위 §4 의 세 방향은 **하나도 소진되지 않았다.** 특히 이번에 확인된 사실이 §4-1 을 강화한다:
이 회사는 클램프 발동 10칸 중 유일하게 클램프 후에도 안 닫히고, `한도초과(70,821.29)` 가
`item12(30,921)` 의 2.3배다. 다른 9칸은 클램프 후 정확히 닫힌다.

**인과가 규명되면 등재를 재검토한다** — 그때 이 면제는 풀거나 사유를 바꿔 다시 적어야 한다.
게이트가 매 실행 `EXEMPTION_STANDS_ON_OWNER_JUDGEMENT` 로 이 티켓을 가리키므로 잊히지 않는다.

status: **open 유지.**

## sender 재확인 (validation, 2026-08-24 3차) — §4-2 질문에 답, 인과는 여전히 미규명

**§4-2 "이웃 분기 대조"에 직접 답한다 — 이 회사·이 분기 단독 현상이다.** KR0068 13개 분기
전부(2023.1Q~2026.1Q) `item4-item12-item13` vs `item2` 를 실측:

```
2023.1Q~2025.1Q, 2025.4Q, 2026.1Q  전부 diff = 0.00 또는 반올림 ±1
2025.2Q                             diff = +826.00   <- 유일한 이상치
2025.3Q                             diff = +1.00      2026.1Q  diff = +1.00
```

같은 회사의 다른 12개 분기는 전부 닫힌다 — **구조적 미스매치가 아니라 2025.2Q 한 분기만의
현상**이다. raw(`data/disclosure/FY2025_Q2/raw/KR0068_한화생명.pdf`) p17 재확인: 헤드라인
221,809=82,506+139,303 완전히 닫히고, 순자산 구성행 합(48,289+30,685+85,295-8,459-26,801+
36,220+48,247=213,476, 반올림 오차 1)도 item4(213,475)와 사실상 일치, 주2) 각주 문구도 인쇄된
그대로다. **§4 의 세 조사 방향(item12/47 스코프, item47 한도-비적용 패턴) 은 여전히 하나도
소진되지 않았다** — 이번엔 "이 회사만/이 분기만"이라는 §4-2 확인만 추가했다. 서브에이전트가
API 529 로 죽어 더 못 갔다. **status: open 그대로 유지.**


## 답변 (validation, 2026-08-24 iter-2) — **인과 규명됨. 발행사 모순이 아니라 우리 룰의 결함이다.**

§4-1(`item12` 스코프)과 §4-3(`item47` 스코프)을 끝까지 밀었다. 둘은 같은 원인의 두 얼굴이었다.
**`item12` 는 정상이다. 틀린 것은 `item47` 의 스코프를 읽는 우리 룰이다.**

### 1. 결론 한 줄

`item47`(보완자본 한도 적용 전)의 **스코프가 발행사마다 다르다.** 한화생명은 `item47` 에
`item49`(해약환급금 부족분 상당액 중 해약환급금 상당액 초과분)를 **포함해서** 인쇄하고,
한도(`item48`)는 그 나머지(채무성 자본 = `item47 − item49`)에만 걸린다. 우리 룰은 `item47` 이
채무성 자본만이라고 **가정**하고 `한도초과 = max(0, item47 − item48)` 을 쓴다. 한화생명에서는
그 값이 `item49` 만큼 과대해지고, 2025.2Q 는 **이 회사에서 한도가 실제로 구속한 유일한 분기**라
그 과대값이 다리에 그대로 들어가 −30,095 를 만들었다.

### 2. §4-3 — 원문 확인: **"한도로 안 잘림"은 오독이었다. 한도는 정상 적용된다**

`data/disclosure/FY2025_Q2/raw/KR0068_한화생명.pdf` p18 `(1) 공통적용 경과조치` (백만원,
fitz 단어좌표로 컬럼 확정 — 적용전 x≈341/347, 적용후 x≈489/494):

```
                            적용전          적용후
지급여력비율(%)              160.0          160.6
지급여력금액              22,180,868     22,263,443
기본자본                   8,250,615      8,250,615
보완자본                  13,930,253     14,012,828
 보완자본 한도 적용 전      14,012,828     14,012,828   <- 두 컬럼 같은 값
 보완자본 한도               6,930,699      6,930,699   = SCR 13,861,397 x 50%
 해약환급금...초과분         6,999,555      6,999,555
 (기발행 신종자본증권)               0
 (기발행 후순위채무)          1,649,118
지급여력기준금액            13,861,397     13,861,397
```

`한도적용전(14,012,828)` 이 `보완자본(13,930,253)` 보다 **크고**, `해약환급금(6,999,555)` 보다도
크다 → `item47` 은 **총액**이다. 채무성 부분 = `14,012,828 − 6,999,555 = 7,013,273` 이
한도 `6,930,699` 를 **82,574백만원(=825.74억) 초과**한다. 그래서

```
보완자본(적용전) = min(7,013,273, 6,930,699) + 6,999,555 = 13,930,254   (인쇄 13,930,253, 반올림 1)
보완자본(적용후) = 14,012,828  (TFI 가 한도 제약을 풀어 준 값 = 한도적용전 그대로)
```

**즉 적용후만 "안 잘린" 것이고, 적용전은 정확히 한도로 잘렸다.** iter-3 §2 의
"보완자본 = 한도적용전 그대로(한도로 안 잘림)" 은 **한도가 안 걸린 분기를 보고 관행으로
일반화한 오독**이다.

**같은 회사 이웃 분기 raw 로 반증 시도 — 반증되지 않았다**
(`scripts/_probes/probe_20260824_tfi_table_scan.py`):

| 분기 | 한도적용전 | 해약환급금 | 채무성(=차) | 한도 | 구속? | 인쇄된 보완자본 |
|---|---|---|---|---|---|---|
| 2025.1Q (p18) | 12,225,226 | 6,432,843 | 5,792,383 | 6,838,221 | 아니오 | 12,225,226 = 한도적용전 |
| **2025.2Q (p18)** | 14,012,828 | 6,999,555 | **7,013,273** | **6,930,699** | **예 (+82,574)** | **13,930,253 = 한도+해약** |
| 2025.3Q (p19) | 14,428,486 | 7,405,260 | 7,023,226 | 7,122,730 | 아니오 | 14,428,486 = 한도적용전 |

채무성 자본이 2025.1Q 5,792,383 → 2025.2Q 7,013,273 으로 뛰면서 한도를 처음 넘었고, 2025.3Q 는
SCR 상승으로 한도가 7,122,730 까지 올라 다시 안 걸린다. **13분기 중 이 한 분기만 구속한다.**
(왜 채무성이 뛰었는지는 이 표만으로 단정하지 않는다 — 기발행 후순위채무는 1,722,937 → 1,649,118
로 **줄었으므로** 신규 발행 쪽이지만, 발행내역은 이 공시에 없다.)

### 3. §4-1 — `item12` 는 정상. 대조군을 세우니 클램프가 밴드에이드였음이 드러난다

주2) 각주 그대로 계산하면 **닫힌다**:

```
기본자본 = 순자산 − (불인정 − 한도초과) − 재분류
        = 213,475 − (30,921 − 825.74) − 100,874 = 82,505.74     vs 인쇄 82,506  → 잔차 0.26
```

`한도초과 = 825.74` 이므로 `item12(30,921)` 안에 **당연히 들어간다.** 클램프
(`min(raw_exc, item12)`)가 필요했던 것은 `raw_exc` 가 애초에 틀렸기 때문이다.

**대조군(§4-1 이 요구한 나머지 9칸) — `probe_20260824_kr0068_clamp_control_group.py` 전수:**

| 회사·분기 | item12 | raw_exc | 배율 | 클램프 후 diff |
|---|---|---|---|---|
| KR0032 2026.1Q | 0.00 | 41.95 | ∞ | −1.00 |
| KR0072 2024.3Q/2024.4Q/2025.1Q/2025.2Q | 0.00 | 913.29 / 73.74 / 262.91 / 327.68 | ∞ | 0.00 / 1.00 / 0.00 / 0.00 |
| KR0072 2025.3Q | 203.00 | 203.10 | 1.00 | 1.00 |
| KR0076 2024.4Q | 1,067.00 | 1,191.20 | 1.12 | 0.00 |
| KR0076 2025.2Q | 2,015.00 | 2,015.35 | 1.00 | 1.00 |
| KR1011 2025.3Q | 513.00 | 513.09 | 1.00 | −1.00 |
| **KR0068 2025.2Q** | **30,921.00** | **70,821.29** | **2.29** | **−30,095.00** |

나머지 9칸은 배율이 1.00~1.12 이거나 `item12=0` 이라, 클램프가 사실상 아무것도 안 자르거나
`불인정항목 전액 = 한도초과` 를 표현한다. 배율 2.29 는 한화생명뿐이고 — **그 2.29 배가 바로
`item49` 를 잘못 포함시킨 결과다** (`70,821.29 − 825.74 ≈ 69,995.55 = item49`).

### 4. 반증 쿼리를 먼저 돌렸다 (§3.1 규율) — 그리고 **첫 가설은 실제로 반증됐다**

가설 "`item47` 은 모든 회사에서 `item49` 를 포함한다" 를 전수로 걸었다
(`probe_20260824_kr0068_excess_convention_sim.py`): **구성식 461칸이 깨지고 다리 31칸이
깨진다.** → **이 가설은 틀렸다. 전역 룰로 바꾸면 안 된다.**

스코프가 **회사 속성**이라는 쪽으로 좁혀 다시 전수
(`probe_20260824_i47_scope_per_company.py` · `probe_20260824_scope_aware_bridge_sim.py`):

```
회사별 스코프 투표(모호 버킷 제외):  EXCL 27사 · INCL 5사(KR0004·KR0068·KR0075·KR0079·KR0080) · CONFLICT 4사
```

원문 대조군으로 확증했다 — **IBK연금 2025.3Q(EXCL)** p16: `한도적용전 403,778` 이
`보완자본 695,572` 보다 **작고** `해약환급금 343,103` 을 따로 더한다
(`min(403,778, 352,469) + 343,103 = 695,572` 정확히 일치). **같은 행 이름이 두 관행으로
인쇄된다는 직접 증거이고, 추론이 아니라 두 PDF 를 나란히 놓은 실측이다.**

**스코프 인식 한도초과 전수 시뮬레이션 (닫힘/깨짐 양방향):**

```
cur=OK new=OK    600      새로 닫히는 칸 = 1  (KR0068 2025.2Q  −30,095.00 → 0.26)
cur=RED new=OK     1      새로 깨지는 칸 = 0
cur=RED new=RED   64
```

### 5. 그런데도 **이번 세션에서 룰을 안 고쳤다** — 이유를 박아 둔다

시뮬레이션이 1 fix / 0 break 로 깨끗해도, 이 수정은 한 커밋 안에서 **동시에** 움직여야 한다:

1. `src/solvency/validation/kics_json_rules.py::_validate_tier2_limit` 의 한도초과 계산 + `_tier2_branch`
2. `tests/test_kics_rules_golden.py` (**라이브 `kics_disclosure.json` 에 물려 있다**)
3. `scripts/validate_kics_disclosure.py::gate._TIER2_ISSUER_INCONSISTENT` 의 박제값 −30,095.0
4. `data/_gold/kics_exemption_provenance.json` 의 KR0068 항목(등재 해제)
5. `tests/test_tier2_issuer_inconsistent_exemption.py` 의 변이시험 4~6건

**지금은 다른 서브에이전트가 `kics_disclosure.json` 을 병렬로 만지고 있다.** 그 상태에서 룰
골든을 `--update` 하면 **반쯤 쓰인 마스터가 골든에 박제**되고, 나중에 그쪽 편집이 들어오면
누군가 이유를 모른 채 또 재생성한다 — 이 저장소가 이미 데인 lost-update 형태다. 그래서
**조사 결과만 남기고 룰은 손대지 않았다.**

### 6. 면제 등재 — **해제하지 않았다. 사유만 정정했다**

지금 등재를 풀면 룰이 여전히 −30,095 를 내므로 게이트가 RED 가 되어 push 가 막힌다. 순서는
**룰 수정 → RED 소멸 → 게이트가 `TIER2_EXEMPTION_INERT` 로 "등재를 풀어라" 인쇄 → 해제** 다.

`data/_gold/kics_exemption_provenance.json` KR0068 2025.2Q 에서 **자유텍스트 6곳만** 고쳤다
(`scripts/fix_20260824_kr0068_exemption_reason.py`, 실행 로그 재현 가능):

- `claim` — "인과는 규명되지 않았다" → 규명 내용 + 재현 스크립트 경로
- `claim_kind` — `ISSUER_UNEXPLAINED_RESIDUAL_OWNER_ACCEPTED` → `OUR_RULE_MISREADS_item47_SCOPE__owner_accepted_pending_rule_fix`
- `note` — **"어느 해석에서도 826/30,095 에 해당하는 항목은 원문에 없다" 는 틀렸다.** 826 은 p18 세 행에서 그대로 나온다: `(14,012,828 − 6,999,555) − 6,930,699 = 82,574백만원`. (30,095 쪽은 원문에 없는 게 맞다 — 룰의 산물이다.)
- `owner_confirmation.open_lead` — 게이트가 매 실행 인쇄하는 줄. 여기에 규명 사실을 넣어 조용해지지 않게 했다
- `scope` — "인과 미규명이므로" 삭제, INCL 5사는 면제가 아니라 룰 수정 대상임을 명시
- `release_condition` — 해제 경로를 "룰 수정" 한 가지로 확정 + 동시 갱신 대상 5개 열거

**안 건드린 것:** `status`(VERIFIED_BY_OWNER) · `expected_residual`(−30,095.0) ·
`expected_residual_alt_reading`(826.0) · `pin_tolerance`(0.01) · `verify` 마커 ·
`owner_confirmation` 의 read_by/date/what_was_read/verdict. 허용오차도 안 건드렸다.

> §5 의 "825.75 ≈ 826 으로 박제하지 말 것" 은 그대로 지켰다. 지금 적은 것은 근접이 아니라
> **원문 세 행에서 나오는 산식**이다: `(item47 − item49) − item48 = 825.74`. 그리고 그 값으로
> 다리가 `82,505.74 vs 82,506` 으로 닫힌다. 박제값 −30,095 는 **룰이 실제로 내는 값**이라 그대로 뒀다.

### 7. 검증 (재현 명령 + 실측)

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_kr0068_clamp_control_group.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_kr0068_raw_pages.py "data/disclosure/FY2025_Q2/raw/KR0068_한화생명.pdf" 16,17,18,19 kr0068_2025q2_p16_19.txt
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_tfi_table_scan.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_kr0068_excess_convention_sim.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_i47_scope_per_company.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_scope_aware_bridge_sim.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe -m pytest tests/test_tier2_issuer_inconsistent_exemption.py tests/test_tier2_limit_rules.py tests/test_kics_rules_golden.py tests/test_rule_coverage_manifest.py -q
  -> 142 passed
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py
  -> EXIT 0 · 박제잔차=-30095.0 실측=-30095.0 Δ=+0.0 (tol 0.01) 일치
  -> RED 라인 · Top RED offenders 가 정정 **전과 완전히 동일**(diff 결과 IDENTICAL)
```

산출물: `artifacts/validation/probe_20260824_*.txt` · `kr0068_2025q2_p16_19.txt` ·
`gate_20260824_{baseline,after}.txt`.

### 8. 남은 일 (이 티켓이 계속 열려 있는 새 이유)

**"인과 미규명" 이 아니라 "룰 수정 대기" 다.** 원장 `open_ticket` 이 이 파일을 가리키므로
`_resolved/` 로 옮기지 않는다(게이트가 매 실행 이 경로를 인쇄한다).

착수 조건: `kics_disclosure.json` 을 만지는 세션이 없을 것. 작업은 §5 의 5개 파일 한 커밋.
**착수 전 `probe_20260824_scope_aware_bridge_sim.py` 를 그날 마스터로 다시 돌려 1 fix / 0 break
가 유지되는지 재확인할 것** — 마스터가 바뀌면 이 수치도 바뀔 수 있다.

status: **open 유지** (사유 교체: 인과 미규명 → 룰 수정 대기)

---

## 종결 (validation, 2026-08-24 iter-3) — **룰을 고쳤다. 면제는 해제했다. 이 티켓은 닫는다.**

iter-2 가 규명해 놓은 내용을 코드에 반영했다. 마스터(`kics_disclosure.json`)는 이번에도
**읽기만 했다** — 이 축의 잔차는 처음부터 데이터가 아니라 룰이 만든 것이었다.

### 1. 착수 전 재확인 (iter-2 §8 이 요구한 것)

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_scope_aware_bridge_sim.py
  -> cur=OK new=OK 600 · cur=RED new=OK 1 · cur=RED new=RED 64
  -> 새로 닫히는 칸 = 1 (KR0068 2025.2Q 적용전 scope=INCL, -30,095.00 -> 0.26) · 새로 깨지는 칸 = 0
```

1 fix / 0 break 재현됨. 착수 전 `git status` 로 `kics_disclosure.json` 이 미변경임을 먼저 확인했다.

### 2. 다리만 재구현한 시뮬은 부족했다 — **룰엔진 전층으로 다시 쟀다**

기존 시뮬(`probe_20260824_scope_aware_bridge_sim.py`)은 축 A(다리)만 재구현해 센다. 그 방식은
갈래 함수를 **공유하는** 축 B(`3_tier2_composition`)·축 F(`51_tfi_tier2_composition`)의 부수효과를
못 본다. 그래서 `run_validation` 산출 전체를 수정 전후로 덤프해 (회사,분기,룰) 단위로 대조했다:

```
... scripts/_probes/probe_20260824_findings_snapshot.py dump artifacts/validation/findings_20260824_before.json
(룰 수정)
... scripts/_probes/probe_20260824_findings_snapshot.py dump artifacts/validation/findings_20260824_after.json
... scripts/_probes/probe_20260824_findings_snapshot.py diff artifacts/validation/findings_20260824_before.json artifacts/validation/findings_20260824_after.json
```

| | 건수 |
|---|---|
| **새로 닫히는 칸(해결)** | **1** — KR0068 2025.2Q `2_tier1_bridge` diff −30,095.00 → **+0.26** |
| **새로 깨지는 칸(파손)** | **0** |
| status 무변동 | 13,663 (findings 총계 13,664 **불변**, finding 키 집합도 동일) |
| 갈래 이름만 바뀐 칸 | 272 (`UNCAPPED`/`BOTH` → `I49_IN_I47_UNCAPPED`/`I49_IN_I47_CAPPED`) |
| status 동일 + diff 이동 | 12 (KR0075 3분기 × 축 B·F × 전·후) — §5 에서 처리 |

전체 집계: **RED 38 → 37 · GREEN 9,521 → 9,522** (YELLOW·SKIP 불변).

**시뮬 도중 함정 하나를 실제로 밟았다** — 첫 시도에서 갈래만 새로 만들고
`raw_exc = excess if branch in ("CAPPED","BOTH")` 라는 **하드코딩된 갈래 집합**을 안 고쳐서,
새 갈래가 조용히 초과액 0 으로 취급되고 시뮬 결과가 "status 전이 0건" 으로 나왔다. 룰이 어떤
대상을 순회조차 안 하는 은닉 필터 — 이 저장소의 반복 사고형태 그대로다. 상수로 빼고
(`_TIER2_EXCESS_BEARING_BRANCHES`) 시험으로 못 박았다.

### 3. 무엇을 고쳤나 (`src/solvency/validation/kics_json_rules.py`)

- `_tier2_i47_scope_map(buckets, tolerance)` **신설** — 회사별 `item47` 스코프를 **그 회사 자신의
  결정적 버킷 투표**로 정한다. 결정적 = 두 읽기 중 **정확히 하나만** 공시 보완자본을 재현하는 버킷.
  **회사 하드코딩 리스트를 만들지 않았다**(발주 요구). 룰엔진과 같은 tol 을 쓴다(OCR 회사 10.0) —
  다르면 투표가 검사와 다른 잣대로 갈린다.
- `_tier2_branch(..., scope=)` — INCL 이면 채무성 자본 `debt = item47 − item49` 로 한도 시험과
  초과액을 계산한다. 새 갈래 이름은 기존 이름의 **접두사가 아니다**(`I49_IN_I47_*`) — 게이트·시험이
  `"branch=CAPPED" in detail` 이라는 부분문자열로 갈래를 읽기 때문에 `CAPPED_INCL` 로 지으면
  두 갈래가 한 이름으로 뭉개진다.
- `_tier2_expected(...)` — finding 의 `expected`/`diff` 를 **판정에 쓴 읽기와 같은 식**에서 낸다.
  종전에는 갈래를 INCL 로 판정해 놓고 expected 는 EXCL 식으로 인쇄했다(시뮬에서 축 F 79칸이
  실제로 그랬다). 게이트 출력이 통과 사유와 어긋난 숫자를 찍는 상태였다.
- `_tier2_cross_bucket_context` 가 스코프 맵을 4번째 값으로 돌려주고 `run_validation` →
  `_validate_bucket` → `_validate_tier2_limit` / `_validate_tfi_tier_rows` 로 전달된다.
  **두 게이트가 같은 함수를 부르므로 절반만 굳는 경로가 없다**(`validate_data_contract.py` L322 가
  `kics_run_validation` 을 직접 호출한다).

**부수 확증(작업 B 중 우연히 얻었다)**: 미래에셋생명 2025.1Q raw p18 을 렌더링해 보니
`한도적용전 1,058,020 = (기발행 후순위채무) 303,030 + 해약환급금 초과분 754,990` 으로
**마지막 자리까지 정확히 분해된다.** INCL 관행("item47 이 item49 를 포함")이 한화생명 말고
**다른 회사 원문에서도 산수로 닫힌다**는 두 번째 독립 증거다.

### 4. 골든 재생성 (손으로 해시 고치지 않았다)

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe tests/test_kics_rules_golden.py --update
  -> 13664 findings / 488 buckets · by_status {YELLOW 1519, GREEN 9522, SKIP 2586, RED 37}
```

사유는 `tests/test_kics_rules_golden.py` 의 `_what` **5차 항목**에 남겼다(커밋 메시지 후보는 §7).

### 5. 면제 등재 정리

| 대상 | 조치 | 근거 |
|---|---|---|
| **KR0068 2025.2Q** | **해제** — `_TIER2_ISSUER_INCONSISTENT` 에서 제거 | 룰 수정 후 게이트가 `TIER2_EXEMPTION_INERT` review 로 "박제한 축에 RED 가 없다 — 등재를 풀어라" 를 먼저 인쇄했다. 다리 잔차 0.26(tol 2.0) |
| KR0075 2024.3Q·2024.4Q·2025.1Q | **박제잔차 6개 갱신**(해제 아님) | 스코프 정정으로 이 회사(INCL)의 기대식이 바뀌어 잔차가 이동했다: `3_tier2_composition` −220.98→+14.86 / −242.27→+87.22 / −243.09→+61.41, `51_tfi_tier2_composition` −221.31→+14.53 / −242.74→+86.75 / −243.31→+61.19. **마스터 셀은 한 칸도 안 움직였다**(박제 `cells` 가 그대로 통과한다) |

**KR0075 정정이 옳다는 방증**: 새 잔차가 **다리 잔차와 같은 값으로 수렴**한다
(2024.3Q 구성 +14.86 vs 다리 +15 · 2024.4Q +87.22 vs 다리 +87). 종전 −221/−242 는 서로 다른 두
불일치가 있는 것처럼 보이게 했지만 실제로는 하나다. 종전 값은 원장
`expected_residual_alt_reading` 에 사유와 함께 남겼다(KR0068 이 만든 선례) — 안 남기면 다음 세션이
"박제값이 등재 기록과 다르다" 로 읽고 되돌린다.

**KR0075 의 INCL 판정이 오염된 셀에 기대고 있지 않은지 따로 확인했다.** 이 회사는 문제의 3분기에
`item47 == item48`(게이트가 `TIER2_DUPLICATE_ROW` 로 이미 플래그)이라, 그 칸으로 스코프를
정했다면 근거가 순환이 된다. 전분기 실측 결과 **INCL 표 18개는 전부 `item47 ≠ item48` 인 나머지
9분기에서 나온다**(`item3 == item47` 이면서 `item49 > 0` = "item49 가 item47 안에 있다" 를 원문
산수로 직접 보여주는 칸들). 문제의 3분기는 어느 읽기로도 재현되지 않아 그대로 RED 다.
재현: `scripts/_probes/probe_20260824_kr0075_scope_evidence.py`.
같은 실측이 종전 게이트 주석의 오류도 드러냈다 — "채무성 자본이 0인 회사라서 두 줄에 같은 숫자를
인쇄한다" 는 **틀렸다**(나머지 9분기의 채무성 자본은 53.06 · 71.51 · 158.65 … 로 0 이 아니다).
주석을 정정했다. 왜 그 3분기만 한도적용전 자리에 한도와 같은 값이 찍히는지는 **여전히 미규명**이고,
그 사실을 주석에 그대로 적었다.

### 6. 면제를 지탱하던 변이시험 정리 (삭제 이유를 코드에 남겼다)

| 시험 | 조치 | 이유 |
|---|---|---|
| `test_input_drift_revives_the_red[KR0068-…-{2,4,12,13}-값]` (4건) | parametrize 목록에서 제거 | 면제가 없으니 흔들 핀도 없다. **같은 데이터를 지키는 시험이 더 센 형태로 옮겨 갔다** — `tests/test_tier2_limit_rules.py::test_bridge_uses_the_debt_only_excess_for_i49_in_i47_issuers` 는 잔차가 우연히 맞는 게 아니라 **한도초과액이 825.74 인지까지** 본다 |
| `test_residual_drift_revives_the_red[KR0068-…]` | 제거 | 위와 같음 |
| `test_the_two_readings_of_the_hanwha_residual_are_both_recorded` | **교체** → `test_the_released_hanwha_record_is_kept_as_a_tripwire` | 826 vs −30,095 두 읽기는 이제 **둘 다 무효**다(둘 다 틀린 스코프에서 나온 값). 대신 **해제 기록이 지워지지 않았는지**를 지킨다 — 원장 `status=CONTRADICTED` 가 남아 있어야 같은 (회사,분기)가 다시 등재될 때 `EXEMPTION_CITATION_CONTRADICTED` RED 가 뜬다. 그 경로를 실제로 흔들어 확인한다 |
| `test_the_hanwha_pin_catches_a_branch_change_...` | **대상 이전** → `test_a_pin_that_names_a_branch_catches_a_branch_change_not_only_a_residual` | "갈래 이름으로 박제한 축은 갈래가 바뀌면 면제가 아니다" 라는 **성질 자체는 살아 있어야 한다.** 같은 방식(`branch=CAPPED`)으로 박제된 NH농협 2024.3Q 로 대상을 옮겼다. 지우면 '갈래가 바뀌어도 통과' 사각이 다시 생긴다 |
| `test_an_owner_judgement_*` (3건) | **fixture 화**(유지) | `VERIFIED_BY_OWNER` 배선은 살아 있고 다음 owner 판단 면제 때 다시 필요하다. 살아 있는 항목이 0 이라고 시험을 지우면 **그때 배선이 이미 썩어 있는지 아무도 모른다.** 해제된 그 기록을 fixture 로 되살려 배선만 흔든다(`_owner_judgement_fixture`, 삭제 금지 이유를 docstring 에 박았다) |
| 레지스트리 크기 고정 | 19 → **18** | 늘린 게 아니라 줄인 것이라 근거를 주석에 같이 적었다 |

`tests/test_rule_coverage_manifest.py` 도 같이 고쳤다(발주 §6): `COMPOSITION_BRANCHES` 에 새 갈래
2개 등재 + **신설 2건** — `test_branch_names_are_not_prefixes_of_each_other`(부분문자열 판독이
갈래를 뭉개는 것을 막는다) · `test_every_excess_bearing_branch_is_declared`(§2 에서 실제로 밟은
은닉 필터를 막는다).

### 7. 게이트 실측 (재현 명령 + exit code)

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py
  -> EXIT 0 · Status counts RED=37 YELLOW=1519 GREEN=9522 SKIP=2586 · blocking RED=0
  -> 면제 18버킷 · finding 36건, 전건 "박제잔차 == 실측" 일치
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_data_contract.py
  -> EXIT 0 · SUMMARY RED=0 YELLOW=296 provisional=False
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe -m pytest tests/ -q --ignore=tests/test_pl_breakdown_golden.py --ignore=tests/test_ifrs17_bs_golden.py
  -> 343 passed, 1 skipped
```

커밋 메시지 후보:

```
validation: item47 스코프 인식으로 2_tier1_bridge 결함 수정 — 한화생명 면제 해제

item47(보완자본 한도 적용 전)의 스코프가 발행사마다 다르다. 룰은 채무성 자본만이라고
가정했으나 일부 발행사는 item49(해약환급금 초과분)를 포함해 인쇄한다(원문 대조:
한화생명 2025.2Q p18 포함 / IBK연금 2025.3Q p16 제외). 포함 관행 회사에서 한도가
구속하는 분기에 한도초과액이 item49 만큼 과대해져 KR0068 2025.2Q 다리가 -30,095 로
틀렸고, 그것이 발행사 모순으로 오진돼 owner 판단 면제까지 갔다.

스코프는 회사 하드코딩이 아니라 그 회사 자신의 결정적 버킷 투표로 정한다
(_tier2_i47_scope_map). 전 버킷 시뮬: 새로 닫힘 1 / 새로 깨짐 0 / findings 총계 불변.
골든 재생성(RED 38->37, GREEN 9521->9522) 사유는 test_kics_rules_golden.py _what 5차.
KR0068 면제 해제(원장은 CONTRADICTED 로 보존) · KR0075 3버킷 박제잔차를 새 식 기준
으로 갱신(마스터 셀 무변경). 포스트모템 PM-2026-08-24.
```

### 8. 사고 기록

`docs/postmortems/PM-2026-08-24_i47_scope_misread.md` **신설(5칸 전부 채움 = closed)** +
색인·미배선(UH) 표 갱신. 신규 **UH-9** = "회사 단위 스코프 투표는 관행이 시간에 따라 바뀌는
발행사를 못 담는다"(KB손해가 2025.2Q 에 INCL→EXCL 로 갈린다). **분기 단위 판정은 만들지
않았다** — 측정된 이득이 0 이고(전수 시뮬 status 전이 0건), 오탐억제를 설계할 수 없으면
배선하지 않는다는 UH-5 선례를 따랐다. 대신 발화 조건과 감지 경로를 UH 표에 박았다.

status: **resolved** — 원인 규명·룰 수정·면제 해제·골든 재생성·회귀시험까지 자기완결.
`inbox/_resolved/` 로 옮긴다.
