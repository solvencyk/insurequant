---
from: owner
to: designer
created: 20260818T0210Z
status: resolved
route: fix
company: MULTI
period: 2026.2Q
rule: STALE_AXIS_BASIS
iter: 1
---

## 미결 (sender 작성)

**index.html CSM 버블맵의 X축(신계약 CSM)을 2026.2Q 로 올린다.** owner:
*"index html 아랫부분 버블맵에서 신계약CSM 규모가 왜 26.1Q 기준이라고 뜨는거? 얘도 고쳐야지."*

`20260818T0026Z` D-2 에서 리터럴을 `NB_TARGET_Q` 하나로 모으고 캡션을 정직하게 고친 건 맞다.
다만 그건 **표기**만 고친 것이고, **X축이 2026.1Q 에 묶여 있는 것 자체**는 그대로다. owner 요구는
표기가 아니라 기준을 옮기라는 것이다.

### 왜 묶여 있나

X 값을 `NB_CSM_multiple.json` 행에서 꺼내기 때문이다(L1099 `r['신계약CSM_연누계']`).
그 마스터가 2026.1Q 까지뿐이라 배수(Y)와 함께 X 도 딸려 고정됐다.
**신계약 CSM 자체는 `CSM_waterfall.json` 항목2 에 2026.2Q 가 이미 있다.**

### 옮겨도 회사가 안 빠진다 (실측)

| | 신계약 CSM(항목2) 보유 |
|---|---|
| 2026.1Q | 23사 |
| 2026.2Q | **같은 23사** |
| 2026.2Q 없는데 1Q 만 있는 회사 | **0사** |

나머지 14사는 두 분기 다 없는 연1회 공시사(비상장 Tier-2)로, 지금도 `estimated` 경로로
처리되는 그 집합 그대로다. **비교 가능성이 안 깨진다.**

### 재료는 이미 코드 안에 있다

`wfNbItem`(CSM_waterfall 항목2 인덱스)이 이미 로드돼 있고 Group B fallback 에서 쓰고 있다
(L1114 `wfNbItem[code][ks[0]].v`). X 소스를 그쪽으로 바꾸는 게 큰 공사가 아니다.

### 바뀐 뒤 기준 조합 (숨기지 말고 라벨로)

- **X축 신계약 CSM → 2026.2Q(반기누계)**, 소스 `CSM_waterfall` 항목2
- **크기 기말 CSM → 2026.2Q** (이미 회사별 최신 자동 선택이라 따라온다)
- **Y축 배수만 2026.1Q** — KIDI 월납초회 6월치 미수집, owner 보류 결정(변경 없음)

캡션·`metaEl` 이 지금 `X·Y 기준 ${NB_TARGET_Q}` 로 **한 덩어리로** 찍히는데, X 와 Y 의 기준이
갈라지므로 **분리 표기**해야 한다. 상수도 `NB_TARGET_Q`(배수) / X·크기용 최신분기 둘로 나눌 것.

### ⚠ 스케일 함정 2개 (이거 놓치면 그래프가 조용히 틀어진다)

1. **X 값이 대략 2배가 된다** (1분기 누계 → 반기 누계). 축 범위·버블 반경 스케일 재확인.
2. **연1회 공시사 `÷4` 추정이 부정합이 된다.** L1100 `nbCsm: estimated ? r['신계약CSM_연누계']/4 : …`
   은 **연간값을 분기로 4분할**한 것이다. X 기준이 반기누계로 바뀌면 **÷2 가 맞다.**
   그대로 두면 추정사 버블만 절반으로 찌그러진다. 캡션의 "÷4" 문구도 같이 고칠 것.

### 잔여 하드코딩

L1098 에 `period: estimated ? srcQ : '2026.1Q'` 로 리터럴이 하나 남아 있다 — D-2 에서 놓친 것.
같이 정리.

### 하지 말 것

- Y축(배수)을 2026.2Q 로 올리려는 시도. **배수 마스터에 2026.2Q 가 없고 IR 로 채우는 안은
  owner 가 취소했다**(`inbox/parser/20260815T0907Z` superseded). KIDI 6월 통계가 나온 뒤 별건.
- 마스터·빌더 수정. 이번 건도 화면 전용.

## 답변 (recipient 작성)

지적하신 그대로 처리 완료. X축을 `NB_CSM_multiple.json`에서 `wfNbItem`(CSM_waterfall 항목2)의
회사별 최신값으로 옮기고, Y(배수)는 `NB_TARGET_Q`(2026.1Q) 고정 그대로 분리. 회사 탈락 0(실측
33社 수정 전후 동일) — 미리 확인해주신 그대로였다. ÷4→÷2 스케일 수정(AIG손보 실측: 986.8→493.4,
정확히 ÷2), 캡션·metaEl·툴팁 X/Y 분리 표기, L1098 잔여 하드코딩 제거까지 전부 반영.

**부수 발견**: 검증 중 커버리지표의 "직전값 이월" 라벨이 X·크기가 둘 다 "회사별 최신"이 된
지금은 성립하지 않아서 삼성생명 등 2026.2Q 실공시 회사들을 거꾸로 "이월"이라 오표시하고 있었다
(이번 세션이 만든 버그는 아니고 며칠 전 크기/기말이 먼저 회사별 최신이 됐을 때부터 있었을
가능성 — 같은 함수라 같이 정리). 해당 분기 삭제, "실공시"로 정상화.

브라우저 실측: 삼성화재(KR0008) X=12,423.5억이 CSM_waterfall.json 2026.2Q 원값과 정확히 일치
(직접 fetch 대조). 시리즈 13+9+11=33 정합, 콘솔 에러 0, `pytest tests/test_deploy_assets.py`
10/10. 상세: `docs/changelog_designer.md` 2026-08-18b.
