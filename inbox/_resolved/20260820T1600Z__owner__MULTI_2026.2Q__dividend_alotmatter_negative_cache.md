---
from: owner
to: downloader
created: 20260820T1600Z
status: resolved
route: refetch
company: MULTI
period: 2026.2Q
iter: 1
---

## 미결 (sender 작성) — 2026.2Q 배당현황 재수집. **8/19에 고친 negative-cache 버그가 여기엔 안 고쳐졌다**

owner 지시(2026-08-20): *"26.2Q 배당현황 다시 시작하자"*.

`dividend.json` 2026.2Q가 **24사 중 5사뿐**이다(과거 2분기는 전부 23~24사). 원인을 캐시까지
파고들어 확정했다.

### 근본원인 = `fetch_dart_alotmatter.py`가 013 응답을 영구 캐시했다

```
data/dart/_alotmatter_cache/*_2026_11012.json   (11012 = 반기보고서)
  status "013" (조회된 데이타가 없습니다)  →  34개
  status "000" (정상)                      →   5개
```

배당 도메인 온보딩이 **2026-08-14**인데 그날이 반기보고서 법정기한(반기말+45일) **당일**이었다.
DART가 아직 색인하기 전이라 013이 돌아왔고, 그게 그대로 파일에 박혔다.

**`fetch_dart_fs.py`는 이 함정을 2026-08-19에 이미 고쳤다**(`_resolved/20260819T0116Z`).
그런데 같은 수정이 alotMatter 쪽에는 안 들어갔다:

```python
# scripts/fetch_dart_fs.py  L134  — 고쳐짐 (성공만 캐시)
if d.get("status") == "000":
    f.write_text(json.dumps(d, ensure_ascii=False), encoding="utf-8")

# scripts/fetch_dart_alotmatter.py  L53-55  — 안 고쳐짐 (무조건 캐시)
data = r.json()
CACHE.mkdir(parents=True, exist_ok=True)
f.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
```

게다가 `fetch_one()`은 **파일이 있으면 무조건 그걸 반환**한다(L47-48) — `fetch_dart_fs.py`에
있는 `--refresh` 같은 우회 플래그가 **아예 없다.** 그래서 지금 스크립트를 다시 돌려도
34사는 영원히 013이다.

## 요청

**1. `fetch_dart_alotmatter.py`에 `fetch_dart_fs.py`와 동일한 가드를 넣어라.**
   - `status == "000"`일 때만 캐시에 쓴다 (013은 매번 재확인).
   - `--refresh` 플래그 추가(기존 캐시 무시하고 재취득). 없으면 이번 34사를 못 고친다.

**2. 아래 34사 × `bsns_year=2026` × `reprt_code=11012` 재취득.**

| corp_code | 코드 | 회사 |
|---|---|---|
| `00103176` | KR0005 | 흥국화재 |
| `00112332` | KR0079 | 미래에셋생명보험 |
| `00112882` | KR0073 | 교보생명보험 |
| `00112998` | KR0150 | 서울보증보험 |
| `00113191` | KR1000 | 코리안리재보험 |
| `00113562` | KR0003 | 롯데손해보험 |
| `00117267` | KR0087 | 동양생명 |
| `00117744` | KR0001 | 메리츠화재해상보험 |
| `00120216` | KR0010 | KB손해보험 |
| `00124063` | KR0076 | 아이엠라이프생명보험 |
| `00126256` | KR0069 | 삼성생명보험 |
| `00139214` | KR0008 | 삼성화재해상보험 |
| `00148391` | KR0070 | 에이비엘생명보험 |
| `00159102` | KR0011 | DB손해보험 |
| `00160393` | KR0099 | KB라이프생명 |
| `00164973` | KR0009 | 현대해상 |
| `00167068` | KR0071 | 흥국생명보험 |
| `00171104` | KR0095 | 메트라이프생명보험 |
| `00187123` | KR0097 | 하나생명보험 |
| `00203102` | KR0100 | 처브라이프생명보험 |
| `00383198` | KR0049 | 악사손해보험 |
| `00459844` | KR0083 | 푸본현대생명보험 |
| `00460798` | KR0075 | 비엔피파리바카디프생명보험 |
| `00471891` | KR0050 | 하나손해보험 |
| `00499426` | KR0051 | 신한이지손해보험 |
| `00504232` | KR0074 | 라이나생명보험 |
| `00844635` | KR1011 | IBK연금보험 |
| `00908155` | KR0032 | NH농협손해보험 |
| `00909349` | KR0104 | 농협생명보험 |
| `00983606` | KR0029 | AIG손해보험 |
| `00992622` | KR1010 | 교보라이프플래닛생명보험 |
| `01295517` | KR0080 | 에이아이에이생명보험 |
| `01603877` | KR1098 | 카카오페이손해보험 |
| `01974696` | KR0004 | 예별손해보험 |

**3. 재취득 후 status 분포를 보고해라.** 아직 013이 남으면 그건 진짜 미제출이니 구분해서 알려라
(반기 배당을 실제로 공시 안 하는 회사가 있을 수 있다 — 5사만 000이었던 게 전부 타이밍
때문인지, 일부는 구조적인지는 재취득해야 갈린다).

**4. 기존 확정 영구결측 캐시는 건드리지 마라.** 2023~2025 분기 중 진짜로 데이터가 없는
013 파일들이 있다 — 이번 변경은 2026 반기만 대상이다(`fetch_dart_fs.py` 수정 때와 동일한 원칙).

## 하류

재취득 완료되면 parser에 통지 → `build_dividend.py` 재빌드 + `tests/test_dividend_golden.py`
`--update`. parser 티켓은 이미 열려 있다(`inbox/parser/20260820T1540Z`, 이 건 대기로 갱신했다).

## 답변 (recipient 작성 — 처리 후)

**1. 코드 수정 완료** (`scripts/fetch_dart_alotmatter.py`) — `fetch_dart_fs.py`와 동일 가드:
- `fetch_one()`: `status=="000"`일 때만 캐시 파일에 쓴다. 013은 그 호출에만 반환, 디스크엔 안 남는다.
- `fetch_one(..., force=True)` + 신규 `refresh_year_reprt(client, year, reprt)`: 지정한
  (year, reprt) 슬라이스를 전체 유니버스에 대해 무조건 라이브 재조회(기존 캐시 무시) —
  `fetch_dart_fs.py --refresh <corp> <year>`와 대응되는 개념이나, 이쪽은 "이번 사고가 특정
  회차(반기법정기한 당일) 하나에만 꽂힌 사고"라 (corp_code, year) 단위가 아니라 (year, reprt)
  단위로 스코프했다.
- CLI: `python scripts/fetch_dart_alotmatter.py --refresh 2026 11012`.

**2. 재취득 결과 — 39사 유니버스 전체를 2026/11012에서 강제 재조회.**

```
이전: 000=5   013=34
이후: 000=24  013=15   (19개사 013→000 전환)
```

| 상태 | 회사 |
|---|---|
| 013→000 (19사) | 메리츠화재해상보험·롯데손해보험·흥국화재·삼성화재해상보험·현대해상·KB손해보험·DB손해보험·NH농협손해보험·삼성생명보험·에이비엘생명보험·흥국생명보험·교보생명보험·미래에셋생명보험·푸본현대생명보험·동양생명·KB라이프생명·농협생명보험·서울보증보험·코리안리재보험 |
| 013 유지 (15사) | 예별손해보험·악사손해보험·하나손해보험·신한이지손해보험·라이나생명보험·비엔피파리바카디프생명보험·아이엠라이프생명보험·에이아이에이생명보험·메트라이프생명보험·하나생명보험·처브라이프생명보험·교보라이프플래닛생명보험·IBK연금보험·카카오페이손해보험·AIG손해보험 |

**주의 — AIG(KR0029)는 유니버스 스윕에서 빠졌었다.** `resolve_corp()`가 이름검색으로 AIG를
못 찾는 기존 문서화된 quirk(등록명이 "AIG손해보험"과 안 맞음) 때문에 `refresh_year_reprt()`의
39개사 루프가 실제로는 38개사만 돌았다. corp_code `00983606`을 직접 지정해 별도로
force-refetch — 역시 013 확정, 위 표에 포함시켰다(최종 013 유지 15사에 반영됨, 이 발견을
반영해 39/39 전수 확인 완료).

**3. 남은 15개 013은 캐싱 문제가 아니라 진짜 구조적 미제출로 판단한다.** 근거: 이 15개사는
2026-08-13 alotMatter 온보딩 때 별개로 확인된 "XBRL 전무 15개사(14+예별) — 전부 비상장
감사보고서(F형)만 제출, 사업보고서/반기보고서 자체를 안 냄" 집합과 **정확히 1:1 일치**한다
(`docs/changelog_downloader.md` 2026-08-13, FS API 온보딩 당시 `universe.py` 분류와도 일치
확인됨). alotMatter API는 반기·사업보고서(A유형)에서만 배당 데이터를 뽑는 구조라, 그 서류
자체를 안 내는 회사는 매번 라이브로 재확인해도 013만 나온다 — 우연이 아니라 두 독립 API가
같은 15개사에서 같은 이유로 막힌 것.

**4. 기존 2023~2025 분기 캐시는 손대지 않았다.** `refresh_year_reprt`는 `year`/`reprt` 인자로
스코프가 고정돼 있어 이번 호출(`2026 11012`)은 그 슬라이스만 건드렸다.

## 하류

parser raw-ready 통지: `inbox/parser/20260820T1720Z`(→ `20260820T1540Z` 재개 트리거).
