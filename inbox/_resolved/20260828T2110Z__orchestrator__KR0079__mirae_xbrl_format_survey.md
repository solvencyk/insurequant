---
from: orchestrator
to: parser
created: 20260828T2110Z
status: resolved
route: blind_spot
company: KR0079
period: MULTI
rule: PL_YESILCHA_ZERO_OTHER_PLUG
lane: ifrs17
iter: 1
---

## 미결 (orchestrator 작성 — 조사 전용, 수정 금지)

**미래에셋생명(KR0079)의 item6·item11 이 13개 분기 전부 `0` 이다. 왜 못 뽑는지 형식을
규명해라. 이 티켓은 조사다 — 마스터를 고치지 마라.**

### 지금까지 밝혀진 것

- `예상보험금`·`예상 보험금`·`발생보험금`·`발생 보험금` 한국어 라벨이 **전부 0회**다.
- `보험료배분접근법` 은 24회로 PAA 를 실제로 쓴다.
- **그런데 걸림돌은 LOB 도 PAA 도 아니라 공시 형식으로 보인다.** 이 회사는 한국어 라벨 표가
  아니라 **XBRL 구조화 공시**를 쓴다. 실측된 제목:

```
보험계약부채(자산)의 잔여보장부채 변동분과 발생사고부채 변동분의 차이조정 공시(배당여부별 구분)
보험계약부채(자산)의 잔여보장부채 변동분과 발생사고부채 변동분의 차이조정 공시(상품별 구분)
보험손익의 변동내역 > 보험수익  (계약 유형: 사망보험/건강보험/연금보험/저축보험/기타보험)
```

PAA 구분이 **별도 표가 아니라 컬럼 헤더**로 들어간다:
`보험료배분접근법을 적용한 보험계약 외의 보험계약`. 단위도 `원` 이다(다른 회사는 백만원).

### 조사할 것

1. **주석 목차부터 뽑아라.** 이 저장소는 목차를 안 보고 숫자부터 계산해서 같은 건에서 세 번
   틀린 이력이 있다(`_resolved/20260828T1000Z` 의 "실패 패턴" 절). 목차부터다.
2. `보험손익의 변동내역 > 보험수익` 구조화 표에 **예상 측 구성요소**(당기 서비스 관련 예상
   보험금·비용에 해당하는 태그)가 있는지 확인해라. XBRL 표준 롤포워드에는 보통
   `발생한 보험금 및 기타 보험서비스비용과 관련된 금액` 계열 태그가 있다.
3. 발생 측이 어디에 있는지, 그리고 **예상 측과 발생 측의 모집단이 같은지**(PAA 컬럼이 한쪽에만
   섞여 있지 않은지) 확인해라. NH 를 막았던 것이 정확히 그 비대칭이었다.
4. 4종(보험금·손해조사비·유지비·재산관리비) 경계가 이 형식에서 어떻게 그어지는지 판정해라.
   합쳐진 한 줄이면 그렇다고 보고해라.
5. **결론을 셋 중 하나로 내라.** (a) 뽑을 수 있다 — 어떻게 뽑는지 + 2026.2Q 실측값,
   (b) 원천에 없다 — 무엇을 확인해서 없다고 하는지, (c) 형식은 있으나 모집단 불일치로 불가 —
   NH 1회차와 같은 사유인지.

### 하지 말 것

- **마스터를 고치지 마라. 조사 전용이다.** 결론이 (a)면 별도 티켓으로 발주한다.
- **"키워드 0회 = 원천 없음" 으로 결론내지 마라.** 이 저장소에서 그 오판이 세 번 났다
  (악사 tier2, 흥국생명 스캔 PDF). 라벨이 다를 뿐일 수 있다. 판별식을 쓰기 전에 **예실차가 잘
  잡히는 회사에서 그 판별식이 참인지 먼저 대조**하고, 안 참이면 다른 라벨을 찾아라.
- `index.html`·`IFRS17.html` 금지. 브랜치 변경 금지. `git push` 금지.

원문: `data/dart/FY2026_Q2/raw/KR0079_미래에셋생명/20260814004054.xml`

## 답변 (recipient 작성 — 처리 후)

**결론: (a) 뽑을 수 있다.** item6(원수)은 고신뢰로 확정, item11(재보험)은 메커니즘은 같으나
population 검증 한 단계가 안 닫혀서 중간신뢰로 보고한다. **이 티켓에서는 구현하지 않았다** —
지시대로 조사만 하고 `PL_breakdown.json`·`pl_breakdown_master.json`·xlsx·`companies.py`는
전혀 건드리지 않았다(아래 "건드리지 않은 것" 참고).

### 1) 주석 목차 실측 (먼저 뽑았다)

`data/dart/FY2026_Q2/raw/KR0079_미래에셋생명/20260814004054.xml`은 DART XBRL 구조화 공시
템플릿이라 `<!-- ===== N: 제목 ===== -->` 주석이 목차 역할을 한다. 전체 131개 중 보험계약
관련 절만 발췌(연결/별도 두 벌 다 있음):

```
연결:  40: 18-1. 보험계약부채(자산) 변동분의 차이조정 공시 (연결)   [14841-20871]
       41: 18-2. 재보험계약자산(부채)의 변동, 보유 재보험계약 (연결) [20872-22405]
       42: 18-3. 보험계약의 정보 (연결)                          [22406-23016]
별도:  87: 18-1. 보험계약부채(자산) 변동분의 차이조정 공시          [43009-49039]
       88: 18-2. 재보험계약자산(부채)의 변동, 보유 재보험계약       [49040-50573]
       89: 18-3. 보험계약의 정보                                [50574-51184]
```

`보험손익` 이라는 별도 최상위 노트 번호는 **없다** — 이게 한국어 라벨 0회의 진짜 이유다.
"보험손익의 변동내역"은 18-1/18-2 **안의 하위 캡션**(`<TABLE class='nb'>` 한 줄짜리)으로만
존재한다. 목차부터 안 뽑았으면 "보험손익 노트가 아예 없다"로 오판했을 지점이다(요청하신
"실패 패턴" 그대로).

### 2) 형식 규명 — 티켓의 가설을 확인

- PAA 구분은 정말 **컬럼 헤더**다: `보험료배분접근법을 적용한 보험계약 외의 보험계약`. 이
  구문이 문서 전체에 **24회, 전부 "외의" 부정형**으로만 나온다(`grep -o` 로 전수 확인) —
  즉 원수 측 PAA 전용 분기표는 애초에 존재하지 않는다(§5에서 population 근거로 재확인).
- 단위는 `(단위 : 원)` 이 맞다. 표 바로 위 서브캡션에 `당반기`/`전반기` 로 붙는다.
- 18-1 안에 서로 다른 표가 최소 3종류 섞여 있다 (전부 5개 상품: 사망/건강/연금/저축/기타):
  1. **CSM/RA/PV 구성요소별 조정내역** (line 17124/45292 등, 25열=5상품×[PV,RA,CSM×3전환구분]) —
     `보험계약마진` 조정 전용, 예실차 관련 행 없음.
  2. **보험손익의 변동내역 > 보험수익** (line 20392/48560, 15열=5상품×3전환구분) — 예실차의
     **예상측**이 여기 있다.
  3. **(배당여부별 구분)/(상품별 구분) 잔여보장부채·발생사고부채 변동내역** (line 43051/43595 등,
     6~15열=상품(또는배당유형)×[LRC_손실요소외,LRC_손실요소,LIC]) — 예실차의 **발생측**이
     여기 있다.
  세 표 다 캡션이 전부 "18-1. 보험계약부채(자산) 변동분의 차이조정 공시" 로 동일해서(주석
  헤더만 보면 구분이 안 된다), 표 안 실제 헤더 행(`구성요소별 보험계약`/`계약 유형` 등)까지
  읽어야 서로 구분된다.

### 3) 방법 + 2026.2Q 실측값 (item6, 원수 예실차) — 고신뢰

표2("보험손익의 변동내역 > 보험수익", 별도, 당반기)의 첫 행이 정확히 4종 정본 정의와
같은 스코프다:

```
발생한 보험금 및 그 밖의 발생한 보험서비스비용을 통한 증가(감소), 보험계약부채(자산)
```

— "보험금 및 그 밖의(기타) 보험서비스비용" 한 줄로 합쳐져 있다(4종 개별 분리 아님 — 지시하신
4번 판정: **합쳐진 한 줄이다**, NH와 동일 패턴).

발생측은 표3("(상품별 구분)" 잔여보장부채·발생사고부채 변동, 별도, 당반기)의 같은 라벨을 쓰는
행에서 **발생사고부채(LIC) 열만** 취한다. 이 표는 NH와 달리 LRC_손실요소외/LRC_손실요소/LIC가
**애초에 별 컬럼**이라 NH가 3회차까지 겪은 "손실요소를 포함할지" 논쟁이 구조적으로 발생하지
않는다 — LIC 열만 골라 쓰면 자동으로 손실요소가 빠진다.

```
예상 4종 (표2 row1, 15열 합)                     322,653,643,456 원  =  322,653.643456 백만원
발생 4종 LIC-only (표3 row, LIC열만, 5열 합)      340,773,783,421 원  =  340,773.783421 백만원
item6 = 예상 − 발생 = -18,120.139965 백만원 (당반기 누계)
```

**검산 1 — population 일치, 3중 독립 확인(전부 원 단위 끝자리까지 정확히 일치):**

```
표2의 7개 구성요소(예상4종·RA변동·CSM상각·손실요소배분·경험조정·기타(0)·취득CF배분) 합
  = 594,378,172,139
표3의 "보험수익" 단일 합계행 (LRC_손실요소외 5열 합, 부호반대)
  = -594,378,172,139   (절대값 일치)
Tier-1 별도 포괄손익계산서 "일반보험서비스수익" 당반기누계 (line 33327)
  = 594,378,172,139    (원 단위까지 완전 일치)
```

세 번째(Tier-1 원수 보험수익 총계)와 완전히 일치한다는 것은 표2/표3이 커버하는 모집단이
**PAA 누락 없이 원수 보험수익 전체**라는 뜻이다 — NH의 "예상은 GMM만, 발생은 PAA+GMM 합산"
같은 비대칭이 여기는 없다. (§2에서 확인한 대로 PAA 전용 원수표 자체가 없으므로 애초에
갈라질 대상이 없다.)

**검산 2 — 손실요소배분 경계, 부호까지 정확히 일치:**

```
표2 "손실요소배분액" 행 합 (7개 구성요소 중 하나)     = -3,603,229,273
표3 row의 [풀로우합 − LIC-only]로 역산한 손실요소분   = -3,603,229,273   (원 단위 일치, 부호도 동일)
```

NH는 11개 분기 중 10개만 정확 일치(1개는 반올림 1백만 차)했는데 여기는 **정확히 일치**한다.
"같은 거래가 두 표에 다르게 찍혔다"는 근거가 NH보다 강하다.

**검산 3 — 기존 item7(잔차 plug)과의 정합:**

```
item7_old (기타) = -95,601.079834
item7_new (= item3 − item4 − item5 − item6_new) = -77,480.939869
차이 = -18,120.139965  (= item6_new와 정확히 일치 — 기존 잔차의 일부를 그대로 갈랐을 뿐,
                          새 값을 만들어낸 게 아니다)
```

### 4) 방법 + 실측값 (item11, 재보험 예실차) — 중간신뢰, 한 단계 미검증

구조는 동일하다 — 18-2 안에 "재보험비용의 변동내역"(예상측, line 50334) 과 잔여보장부채·
발생사고부채 변동내역(발생측, line 49079)이 각각 있고, 상품이 2종(사망보험/기타보험)뿐이다.

```
예상 4종 (재보험비용 변동내역 row1, 4열 합)         -12,754,036,173 원 = -12,754.036173 백만원
발생 4종 LIC-only (rollforward row, LIC열만)        -14,529,380,375 원 = -14,529.380375 백만원
item11 = 예상 − 발생 = +1,775.344202 백만원 (당반기 누계)
```

**주의 — 부호관례가 원수측과 반대다(추정 아니라 원문 셀 확인):** 원수측은 표3의 LRC_손실요소
열 합이 표2 손실요소배분액과 **부호까지 동일**(-3,603,229,273 = -3,603,229,273)했는데,
재보험측은 **부호가 뒤집힌다**: rollforward LRC_손실요소 열 합(원문 그대로 읽으면
+100,026,907, 괄호 없음)이 재보험비용 변동내역의 손실요소배분액(-100,026,907)과 **크기는
정확히 같고 부호만 반대**다. 두 표 다 원문 셀을 직접 읽어 확인했고(스크립트 주석 참고),
크기 일치 자체는 "같은 거래, 두 표현"이라는 population 근거로 유효하다.

**검증이 못 닫힌 부분:** item6과 달리, 이 예상/발생 어느 쪽도 Tier-1 별도 손익계산서의
"출재보험서비스수익"(당반기누계 19,415.252999백만원)과 일치하지 않는다 — 예상측 절대값
12,754.04도, 표2 6개 구성요소 전체 합(-8,039.830817)도 안 맞는다. 재보험은 "재보험비용"
한 노트만 있고 "재보험수익" 대응 노트가 없어서(§2 목차 확인), Tier-1의 "출재보험서비스수익"이
이 노트가 커버하지 않는 다른 구성요소(예: 재보험자에게 지급된 보험료 배분액과 상계되는
방식이 원수측과 다를 가능성)를 포함하는 것으로 보이는데, 정확한 원인은 이 티켓 범위에서
규명하지 못했다. **item6 수준의 3중 population 검증은 재보험 쪽에서 못 얻었다** — 손실요소
크기일치(검산2 상당)만 확보한 상태.

### 5) 4종 경계 판정

원수·재보험 둘 다 "발생한 보험금 및 그 밖의(기타) 보험서비스비용/재보험수익"으로 **4종이
한 줄에 합쳐져** 있다. 손해조사비/유지비/재산관리비를 개별로 가르는 열이나 행은 이 노트
어디에도 없다(NH와 동일 패턴).

### 6) 스코프 — 이 티켓이 확인하지 않은 것

- 이 XBRL 구조화 노트(18-1/18-2, `단위:원`) 자체는 **2023.2Q 파일에도 라벨이 존재**하지만
  ("발생한 보험금 및 기타 보험서비스비용" 20회 검출), 목차 마커(`<!-- ===== -->`)가 없고
  "22.6 보험손익의 변동내역"이라는 구식 캡션 + 상품별 5개 개별표(i~v, 단위:백만원) 구조라
  **2026.2Q와 표 레이아웃이 다르다.** 2023.2Q는 "보험수익"측(예상 4종)까지는 확인했지만,
  2026.2Q처럼 LRC_손실요소/LIC가 별 컬럼으로 갈라진 "발생측" 대응표가 있는지는 **확인하지
  않았다** — 후속 구현 티켓이 열어야 할 질문이다.
- `companies.py`의 기존 `extract_tier2_miraeasset` docstring이 구분한 "Era 1(백만원,
  ~2025.1Q)/Era 2(원-wide, 2025.2Q~2026.1Q)"과 이 18-1 XBRL 노트가 정확히 같은 경계에서
  갈리는지도 미확인.
- 반기보고서(2026.2Q)라 "당반기"/"전반기" 라벨을 썼다 — 분기보고서(Q1/Q3)는 "당분기"/
  "전분기"로 바뀔 가능성이 높고, 그 경우 라벨 매칭 문자열을 분기해야 한다(미확인, 후속
  구현에서 처리 필요).

### 7) 부수 관찰 (수정하지 않음, 후속 구현 시 참고)

기존 item4(CSM상각)·item5(RA변동) 추출(Era 2, `first_issue`)은 위 §2의 표1("CSM/RA/PV
구성요소별 조정내역", line 17124)에서 온다. item4(CSM=112,829.555147백만원)는 표2("보험손익의
변동내역 > 보험수익")로 독립 재계산한 값과 원 단위까지 정확히 일치하지만, item5(RA=
18,752.479685백만원)는 표2로 재계산한 값(22,640.014075백만원)과 **일치하지 않는다** —
CSM은 두 표가 같은 숫자를, RA는 다른 숫자를 준다는 뜻이다. 이건 item6/11과 무관한
기존 동작이라 **손대지 않았다** — 다만 이번에 표2를 새로 여니 우연히 발견된 것이라 기록만
남긴다. 마스터·코드 전혀 미수정.

### 8) 재현 명령

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/mirae_yesilcha_survey.py
```

오프라인, raw XML + 루트 `PL_breakdown.json`(읽기전용, 검산3용)만 읽는다. 위 §3/§4/§7의
모든 수치가 이 스크립트 출력과 정확히 일치한다(실행 확인 완료). 파일은 아직 미커밋 —
후속 구현 티켓이 같이 커밋하거나, 필요하면 지금 커밋해도 된다(마스터 변경 없음, 신규 파일
하나뿐).

### 건드리지 않은 것 (확인)

`git status`로 재확인: `PL_breakdown.json`·`data/dart/viz/pl_breakdown_master.json`·
`insurequant_master_tables.xlsx`에 다른(항목32) 에이전트의 진행중 diff가 이미 있었지만
(전부 신규 `항목번호:32` 행 추가, `git diff` 로 KR0079 라인 확인), **내 세션은 그 파일들에
아무것도 쓰지 않았다** — 전부 `json.load`로 읽기만 했다. `companies.py`·`index.html`·
`IFRS17.html`도 미수정. 브랜치는 `fix/csm-product-segmented-columns` 그대로, `git push`
없음, `git add` 없음.

status: answered (item6은 자기완결 수준 확신이나, item11의 Tier-1 population 미검증 건과
2023-2025 구간 스코프 미확인 건이 남아 있어 orchestrator 재확인을 요청한다. 결론이 (a)이므로
구현은 별도 티켓으로 발주 바란다.)


## 종결 확인 (orchestrator 2026-08-28)

결론 (a) 승인. population 3중 대사값 `594,378,172,139원` 을 orchestrator 가 독립 확인했다
(별도 포괄손익계산서의 `일반보험서비스수익` 누적과 원 단위 일치).

**단서 하나를 붙여 구현 티켓 `20260828T2300Z` 로 넘긴다.** 발생 4종을 `LIC열만` 으로 잡은 것이
NH 에서 owner 가 지적한 함정과 같은 형태다 — NH 는 합계 열에서 손실요소배분을 되돌린 값이
정답이었고 LIC 열 단독은 138 만큼 어긋났다. 미래에셋에서 그 차이가 0 인지 확인이 필요하다.

item11 은 Tier-1 `출재보험서비스수익` 대사가 안 닫혀 **구현 보류** 조건을 걸었다.
