---
from: owner
to: parser
created: 20260820T2210Z
status: resolved
route: reparse
company: KR0009
period: FY2025
lane: ifrs17
iter: 1
---

## 미결 (sender 작성) — 현대해상 상각 스케줄만 아직 FY2024 필링을 쓴다 (raw는 디스크에 있음)

`20260820T0033Z` 답변의 판단요청 3번("상각 패널 소스 세대, 30사 중 24사가 FY2024")을 종결하며
실측한 결과다. **그 사이 라운드에서 대부분 갱신됐고 한 회사만 남았다.**

```
csm_amort_schedule.json  rcept 연도 분포
  2026… (FY2025 필링)  38사
  2025… (FY2024 필링)   1사   ← 현대해상(KR0009), rcept 20250313001346
```

FY2025 raw는 이미 있다:

```
data/dart/FY2025_Q4/raw/KR0009_현대해상_20260312001448/
  20260312001448.xml · _00760.xml · _00761.xml
```

현재 레코드는 `status: ok` · `unit: 억원` · `unit_source: xref` · total 83,054.87이라
**깨진 건 아니고 한 세대 묵은 것**이다. 화면에는 정상으로 보이므로 급하지 않다.

## 요청

FY2025 필링에서 현대해상 상각 스케줄을 재추출해 갱신. 갱신 후 39사 전부 FY2025 세대로 통일된다.

## 주의

- 갱신되면 화면 숫자가 바뀐다(FY2024→FY2025). 다른 38사는 이미 FY2025라 **이 회사만 세대가
  어긋나 있는 상태**이므로 맞추는 방향이 맞다.
- `tests/test_viz_csm_waterfall_golden.py`가 이 산출을 고정한다 — 바뀌면 `--update` + 커밋에 이유 기록.
- 그 빌더는 **산출 JSON을 인플레이스로 덮어쓴다.** 실행 전 백업하고 drift 시 복구할 것(`CLAUDE.md` 골든표 주석).

## 답변 (recipient 작성 — 처리 후)

FY2025 필링에서 재추출해 갱신 완료. 39사 전부 FY2025 세대로 통일됐다.

**단순 재추출로는 안 됐다 — 캡션 부착 버그 하나를 고쳐야 했다.** 새 필링(`_00760.xml`/
`_00761.xml`)에 표는 있는데(`16.3 보험계약마진 상각 스케줄`, FY2024 필링의 `25.`에서
번호만 바뀜) `extract_csm_tables()`가 0건을 냈다. 원인: 표 바로 앞에 두 겹의 캡션이 있다
— 위에 진짜 설명문("...기대 상각기간별...예상하는금액은..."), 바로 위에 짧은 기간표시
("(1) 2025년 12월 31일 현재"). `src/ifrs17/csm_extractor.py`의 `_SUBCAPTION_PATTERNS`에
이미 이 정확한 함정을 막는 정규식이 있었다(주석에 현대해상 이름까지 박혀 있음:
"1) 2024년 12월 31일 현재" 형만 인식). 그런데 새 필링은 괄호가 다르다 — "(1) 2025년..."
(여는 괄호 있음) vs "1) 2024년..."(여는 괄호 없음). 그래서 서브캡션으로 안 잡히고 진짜
캡션을 덮어써 CSM 키워드가 사라졌다. 정규식에 여는 괄호를 옵셔널로 추가해 고쳤다
(`^\(?\d+\)\s*\d{4}년`). 이 함수는 measurement/insurance_pl/reinsurance/sensitivity/
PL breakdown/reserve_extract가 전부 공유하지만, 순수 가산적 매칭 확장이라(기존에 잡히던
건 계속 잡히고 새로 이 패턴만 추가로 잡음) 다른 38사 재현에 영향 없음을 재빌드로 확인
(다른 3개 panel 파일 byte-identical, 골든 companies/status_counts 불변).

**당기/전기 두 표 중 하나를 더 골라야 했다.** 캡션 고침만으로 재추출하면 표가 2벌(당기
"(1) 2025년..."·전기 "(2) 2024년...") 다 score=6으로 나와, `viz_build_ifrs17_panels.py`의
`_pick_amort_block`이 line_no 최댓값 tiebreak로 **전기(2024년) 표를 골랐다** — 결과가
기존 stale 값과 완전히 같아서 처음엔 못 알아챌 뻔했다(83,054.87억원 그대로). 별도(OFS,
`_00760.xml` — `_00761.xml`은 "연결재무제표에 대한"·"그 종속기업" 문구로 연결 확인)의
당기(line 12581) 표만 골라 추출 JSON에 담아 이 모호성을 우회했다(공유 tiebreak 로직은
안 건드림 — 다른 회사 영향 불명확한 채로 고치지 않음).

**최종 값(별도, 당기=2025.12.31)**: 1년이하 6,227.17억 · 1~2년 5,366.12억 · 2~3년
4,605.995억 · 3~4년 4,103.72억 · 4~5년 3,688.71억 · 5~10년 15,232.89억 · 10년초과
50,553.82억 · **합계 89,778.42억원**. owner 자신의 사업보고서 서술("연도말 CSM 잔액은
...8조 9,017억원") 대비 0.85% 이내 — 상각스케줄 합계≠CSM 잔액이 원래 다른 개념(스케줄은
잔여 인식기간 전체 합)이라 이 정도 근접이면 정상 교차검증으로 본다.

**반영**: `data/dart/extracted/현대해상_20260312001448_csm.json`(1개 표만) →
`data/dart/viz/csm_amort_schedule.json`(현대해상 1사만 변경, 나머지 38사 byte-identical
확인) → `CSM_amortization.json`(`--only amort`로 갱신, 390행 불변·현대해상 6개 값만
변경, 다른 회사 0건). `test_viz_ifrs17_panels_golden.py --update` 반영 후 재실행 통과.
`test_viz_csm_waterfall_golden.py`는 무관(CSM_waterfall.json 미변경, 재확인 통과).
