---
from: validation
to: parser
created: 20260814T0500Z
status: resolved
route: fix
company: MULTI
period: 2023.4Q-2026.1Q
lane: ifrs17
iter: 1
---

## 미결 (sender 작성)

게이트를 `equity_composition` → **`IFRS17_BS.json`** 으로 갈아끼웠다
(`inbox/validation/20260814T0232Z` 완료, RED 21 → 0). 새 게이트가 그 마스터에 대고 도는 룰은
**둘뿐**이다 — `BS_IDENTITY`(항목1 == 항목2+항목3) · `BS_CENSUS_MISSING_ITEM`(코어 1·2·3·4 결측).
항목 5·6·7(준비금 3종)은 optional 이라 **결측이어도 아무 신호도 안 낸다.**

라이브 실측 **40건**. 지금은 어떤 HTML 도 `IFRS17_BS.json` 을 fetch 하지 않아 전부 YELLOW 지만,
designer 가 `IFRS17.html:267` 을 갈아끼우는 순간(`inbox/designer/20260814T0232Z`) **코드 수정 없이
RED 로 승격 = push 차단**된다. 그 전에 정리되는 게 좋다.

기준: 표시 7분기(2023.4Q·2024.4Q·2025.1Q-2026.1Q)만 검사. 값 보정 요청은 0건 — 전부 소스 라우팅이다.

---

### B-1. Tier-2 본문 XML 파싱이 **부분 산출** — 26셀 (신규 지적)

`build_ifrs17_bs.py` 의 Tier-2 경로(`TIER2_ITEM_MAP = {40:1, 41:2, 1:3, 6:4, 10:5}`)가 회사에 따라
일부 항목만 채운다. 그 결과 **(회사,분기) 행은 존재하는데 자산/부채/자본이 통째로 비는** 상태가 된다.

| 회사 | 분기 | 실제 들어온 항목 | 결측 |
|---|---|---|---|
| AIG손해보험 (KR0029) | 2025.4Q | 5 만 | 1·2·3·4 |
| 메트라이프생명보험 (KR0095) | 2024.4Q · 2025.4Q | 5 만 | 1·2·3·4 |
| IBK연금보험 (KR1011) | 2024.4Q · 2025.4Q | 5 만 | 1·2·3·4 |
| 하나손해보험 (KR0050) | 2025.4Q | 4 만 | 1·2·3 |
| 신한이지손해보험 (KR0051) | 2025.4Q | 4 만 | 1·2·3 |

`parse_filing()` 이 40/41/1(자산·부채·자본)을 그 필링에서 못 잡은 것인지, 아니면 감사보고서 XML
선택(가장 큰 XML 1개)이 어긋난 것인지 확인해 달라. **판단 결과가 "본문에 없다" 면 그렇게 답신만
해 주면 된다** — 값 채우기를 요구하는 게 아니다. 다만 그 경우 이 26셀은 영구 결측이므로
owner 에게 census 처분(코어 예외 vs Tier-2 행 자체를 마스터에서 빼기)을 올려야 한다.

> 참고: Tier-2 15개사 예외 레지스트리(`data/_gold/equity_census_exceptions.json`)는 owner 지시로
> `archive/2026-08_equity_composition/` 로 내려갔다. 새 게이트엔 예외 기구가 **없다.**

### B-2. AOCI(항목4) 결측 12셀 — 이미 발주된 P-2 의 잔여

`inbox/parser/20260814T0216Z` **P-2(AOCI 태그 조건부 채택)** 대상. 0232Z 로 적용처가
`IFRS17_BS.json` 한 곳으로 줄었다.

| 회사 | 분기 |
|---|---|
| 한화생명 (KR0068) | 2023.4Q · 2024.4Q · 2025.1Q |
| 흥국생명보험 (KR0071) | 2025.1Q · 2025.2Q · 2025.3Q · 2025.4Q · 2026.1Q |
| 에이아이에이생명보험 (KR0080) | 2023.4Q · 2024.4Q · 2025.4Q |
| 아이엠라이프생명보험 (KR0076) | 2025.4Q |

한화생명·흥국생명은 `dart_ElementsOfOtherStockholdersEquity`(라벨 "기타자본구성요소") 태그로
갈아탄 게 확인된 건이고, **AIA생명·아이엠라이프 2사는 이번에 새로 걸린 것**이라 같은 원인인지
캐시에서 확인이 필요하다(다른 태그일 수 있음).

`build_ifrs17_bs.py:51` 의 `ACCOUNT_IDS[4]` 가 `ifrs-full_AccumulatedOtherComprehensiveIncome`
하나만 보고 있다. 대체 태그를 채택할 땐 0216Z P-2 의 단서 그대로 **채택 전 자체 검산**을 걸어라
(새 게이트에는 자본총계 폐쇄식이 없다 — 잘못 채택해도 게이트가 안 잡는다).

### B-3. 삼성생명 `BS_IDENTITY` 2건 — P-1(OFS 고정) 대기

| 회사 | 분기 | 자산총계 | 부채+자본 | 잔차 |
|---|---|---|---|---|
| 삼성생명 (KR0069) | 2025.2Q | 318,858,553 | 321,025,872 | △2,167,319 |
| 삼성생명 (KR0069) | 2025.3Q | 318,858,553 | 328,291,954 | △9,433,401 |

자산총계가 2025.1Q/2Q/3Q **세 분기 완전히 동일** = stale CFS. `inbox/parser/20260814T0216Z` P-1
(BS 기준 전사 OFS 고정) 적용 후 **저절로 소멸해야 정상**이다. owner V-3 지시대로 **예외 등재하지
않았다** — 재빌드 후에도 남으면 다시 올린다. (오케스트레이터 전수 대조: OFS 254건 위반 0 /
CFS 85건 중 이 2건.)

---

### 함정 1개 (아카이브 순서)

`scripts/build_equity_composition.py:169` 가 `data/_gold/equity_value_overrides.json` 을 읽는데,
owner 지시(`inbox/validation/20260814T0232Z` §3)로 그 파일을 `archive/2026-08_equity_composition/`
로 옮겼다. **빌더를 아카이브하기 전에 실행하면** KR0032 2024.4Q item30 부호 override 가 조용히
빠져 `tests/test_equity_composition_golden.py` 가 흔들린다. 순서상 **아카이브부터** 하면 된다.

### 참고 — 게이트가 안 보는 축 (지금 고칠 것 아님, 인지만)

- census 는 **마스터에 행이 있는 (회사,분기)** 안에서만 돈다 → `IFRS17_BS.json` 에 행이 0건인
  2사(KR0075 비엔피파리바카디프생명 · KR1098 카카오페이손해보험)는 아무 신호도 안 난다.
  회사축 census 를 살릴지는 owner 판단으로 올렸다.
- 마스터의 2022.4Q · 2023.3Q · 2026.2Q 행은 표시 분기 밖이라 검사 대상이 아니다.

## 답변 (recipient 작성)

**B-3(삼성생명) 확인 — 소멸.** `BS_IDENTITY` 전수 재검사 0건(`validate_data_contract.py`
재실행). P-1(OFS 고정)이 이미 코드에 있었고 재빌드만으로 해소됐다.

**B-2(AOCI) 확인 — 4셀 전부 고침, 12+4=16셀 재검사 0건.** 한화생명·흥국생명 12셀은
기존 `AOCI_FALLBACK_ID` 폴백으로 이미 해소돼 있었다(재검사로 확인, 코드 변경 없음).
AIA생명·아이엠라이프 2사 4셀은 **새 태그가 아니라 라벨 정규화 버그 2건**이었다:
① AIA는 `기타포괄손익누계액(주석29)`처럼 괄호 각주가 붙어 정확일치가 깨짐,
② 아이엠라이프는 `IV.` 같은 ASCII 로마숫자 prefix가 (섹션헤더 정규식엔 있는데) 행단위
lstrip엔 없어서 안 벗겨짐. `build_equity_composition_tier2.py::parse_filing()`에 둘 다
일반 수정(태그 추가 아님) — 3사·raw로 재확인. 채택 전 검산은 이 스키마에 자본 세부항목이
없어 폐쇄식으로는 불가 — 대신 재빌드 후 `BS_IDENTITY`(1==2+3) 전수 0건으로 간접 확인했다.

**B-1(Tier-2 부분산출) — 원인 3가지 특정, 미수정.** raw로 직접 확인:
- **AIG손해보험**: 각주가 꺾쇠 `<주석13,33,35>`(괄호 아님, B-2 수정이 안 잡음) + 총계행이
  `[label,'','',값,'',값]` 빈칸-스페이서 구조라 `_bs_row_value`의 `row[-2]` 가정과 안 맞음.
- **하나손해보험**: 같은 빈칸-스페이서 계열인데 **같은 표 안에서 행마다 오프셋이 다르다**
  (각주번호 셀이 끼는 행과 안 끼는 행 공존) — 단일 컬럼 규칙으로 못 묶음.
- **비엔피파리바카디프생명**(신규 발견, 원래 마스터에 행이 0건이라던 그 2사 중 하나 —
  지금은 행이 생겼지만 item7만 있고 나머지는 결측): "자산"으로 시작하는 첫 테이블을 BS로
  집는 현재 휴리스틱이, 본문 앞쪽 관계기업투자 주석 표(첫 셀이 우연히
  `['자산','<유의적인 영향력을 행사하는 기업>',...]`)에 낚여 진짜 BS 표까지 못 감.
- 메트라이프·IBK연금은 시간상 개별 raw 확인 안 함 — 위 세 가지 중 하나와 같은 계열로
  추정.
셋 다 서로 다른 표 구조 문제라 한 번에 안 고쳐지고, 이 스키마엔 폐쇄식이 없어 잘못
고치면 게이트가 못 잡는다 — "본문에 없으면 보고만"이라던 기준대로 이번엔 고치지 않았다.
재검사 RED **42건, 전부 이 6개사·11셀**(`BS_CENSUS_MISSING_ITEM`만, `BS_IDENTITY` 0건).

**참고 — 두 마스터 대조(§V-5)는 무의미해졌다.** `equity_composition.json` 자체가
`20260814T0232Z`로 archive됐다(별도 owner 지시, 파서 inbox 확인 요망). `IFRS17_BS.json`이
유일 마스터. `MASTER_FILES` 등록은 validation 소관.

---

## 재확인 (sender=validation, 2026-08-14T08:40Z) — RESOLVED, 잔여는 iter 2 로 이관

파서 답변을 마스터에 대고 독립 재측정했다(`IFRS17_BS.json` 16:35 재빌드본, 1,637행).

| 건 | 파서 주장 | 재측정 | 판정 |
|---|---|---|---|
| B-3 삼성생명 `BS_IDENTITY` | 소멸 | 전 분기 위반 **0건** | ✅ 확인 |
| B-2 AOCI(4) 16셀 | 소멸 | 코어 결측에 한화생명·흥국생명·AIA·아이엠라이프 **0건**, item4 265→282행 | ✅ 확인 |
| B-1 Tier-2 부분산출 | 미수정(원인 3종 특정) | 코어 결측 **42셀 / 6개사** 잔존 | ❌ → iter 2 |

부수 확인: 항목 6(비상위험준비금 91행)·7(대손준비금 247행) 신규 유입 = P-5 반영,
회사 37→38(비엔피파리바카디프 행 생성), `BS_IDENTITY` 전수 0.

**이 스레드는 종결**하고 B-1 잔여는
`inbox/parser/20260814T0620Z__validation__MULTI__ifrs17_bs_delta_after_ofs_rebuild.md` (iter 2)로 이관했다.
거기에 OpenDART API 2종(2019019 fnlttXbrl / 2019020 fnlttSinglAcntAll) 실측도 붙였다 —
**비상장사는 두 API 다 막혀 있어(014/013) 본문 XML 파싱이 유일 경로**라는 근거다.
