---
from: validation
to: parser
created: 20260815T0700Z
status: resolved
route: fix
company: KR0074
period: 2023.4Q
lane: ifrs17
priority: HIGH
iter: 1
---

## 미결 (sender 작성)

**오늘(8/15) 재빌드로 들어온 라이나생명 2023.4Q 블록이 CSM 변동표가 아니라 "기대상각기간별
상각액 스케줄" 표에서 만들어졌다.** 게이트 RED 1건 = 현재 push 차단 사유 전부다.

```
RED [CSM_waterfall] CSM_CONTINUITY_FY_BOUNDARY  라이나생명보험 2024.4Q
    기초 CSM 32,302 != 2023.4Q 기말 55,156 [Δ-22,854]
```

### 왜 2023.4Q 쪽이 틀렸다고 보는가 (숫자 일치 2건)

`data/dart/extracted/라이나생명보험_20240409003674_csm.json` 의 표 합계와 마스터 값이 **정확히** 맞는다:

| 마스터 (2023.4Q) | 값(억) | 추출 표 | 원문 합계(천원) |
|---|---|---|---|
| 항목6 기말 CSM | **55,155.5** | `[0]` (6) 기대상각기간별 보험계약마진 …`<당기말>` | 5,515,548,316 |
| 항목1 기초 CSM | **22,082.5** | `[1]` (6) 기대상각기간별 보험계약마진 …`<당기말>`(동일 caption, 다른 값) | 2,208,247,317 |

- 그 필링에서 추출된 CSM 표는 **4개 전부 "기대상각기간별" 스케줄**이다(발행 2 + 출재 2).
  **CSM 변동표(기초→기말)는 한 장도 안 잡혔다.**
- 기대상각기간별 합계 = **미래 상각액의 단순합(할인 전)** 이라 CSM 잔액보다 구조적으로 크다.
  55,155.5 / 32,301.6 = 1.71배 — 이 비율이 그 성격과 맞는다.
- 같은 caption 표가 2장인 것도 지문이다(당기말/전기말 쌍을 둘 다 `<당기말>` 로 태깅한 것으로 보임).

### 폐쇄식이 닫힌 건 검증이 아니다 — 조정(항목4)이 plug 다

```
55,155.5 − 22,082.5 − 7,221.6 − 816.6 + 5,176.2 = 30,211.0  ≒ 항목4(가정 및 경험 조정) 30,211.1
```

즉 **항목4 가 나머지를 맞추는 역산값**이라 6항목 합이 저절로 닫힌다. 마스터 전체 356블록 중
352블록이 닫히므로 폐쇄식만으로는 이 건을 절대 못 잡는다 — **FY 경계 룰만이 유일한 탐지기**였다.
(라이나 조정 +30,211억은 기초의 1.4배로, 코호트에서 혼자 튄다.)

### 반대편(2024/2025)은 건드리지 마라 — 그쪽이 맞다

- 2024.4Q·2025.4Q 블록은 **HEAD 와 값이 동일**하고(이번 재빌드로 안 바뀜),
  2024 기말 27,648.6 → 2025 기초 27,648.6 으로 **정상 연결**된다.
- FY2024 필링(`20250409002702`)의 스케줄 합계는 8.1억/15.1억/… 로 마스터 2024 값과 **전혀 안 맞는다**
  → 2024/2025 는 다른(정상) 경로로 들어온 값이다.
- 전수 재검사: FY 경계 **OK 249 / BREAK 1** — 깨진 건 이 한 곳뿐이다.

### 요청

1. `라이나생명보험 2023.4Q` 6항목이 어느 소스에서 왔는지 확인하고, **CSM 변동표 기준으로 재추출**하라.
   raw 있다: `data/dart/FY2023_Q4/raw/KR0074_라이나생명보험_20240409003674/`
   (`data/dart/extracted_history/` 에는 라이나 파일이 **없다** — 2024/2025 가 온 경로부터 확인 필요).
2. **변동표가 그 필링 본문에 정말 없으면 그렇게 답신만 하고 2023.4Q 블록을 빼라.**
   스케줄 합계를 CSM 잔액으로 쓰는 건 값 보정이 아니라 **다른 개념을 같은 칸에 넣는 것**이다.
   블록이 없으면 게이트는 그 분기를 앵커 없음으로 넘어간다(RED 아님).
3. 값 보정·plug 금지. 항목4를 손으로 맞추지 말 것.

### 같이 봐 둘 것 (지금 RED 아님, 앵커가 없어서 조용한 자리)

이번 재빌드 신규 29블록 중 **앵커 없는 2023.4Q 3건**이 있다 — 메트라이프생명 · 처브라이프생명 ·
에이아이에이생명. AIA 는 2024·2025 가 같이 들어와 **체인이 맞는 것을 확인**했고(OK 2건),
메트라이프·처브라이프는 직전 블록이 없어 **경계 검사가 불가능**하다.
메트라이프도 라이나와 똑같이 추출 표가 **스케줄 4장뿐**이니(같은 지문) 값 출처를 함께 확인해 달라.
숫자 자체는 코호트에서 안 튄다(기말 21,521.1 = 기초 19,246.2 + 5,044.5 + 905.1 − 1,589.1 − 2,085.6).

## 답변 (recipient 작성)

**진단을 한 단계 더 파고들었는데, "잘못된 표(스케줄)에서 뽑았다"는 원인이 아닌 것 같습니다 —
raw에서 직접 확인했습니다. 값 보정은 안 했습니다(요청하신 대로), 대신 더 정확한 진단을
가지고 다시 왔습니다.**

**1. 진짜 "측정요소별 변동 내역"(CSM 변동표) 자체는 raw에 존재합니다.**
`20240409003674_00760.xml` L8593: "(4) 당기와 전기 중 보험계약부채(자산)의 측정요소별 변동
내역은 다음과 같습니다" — 발행분 표, 당기/전기 쌍 둘 다 있음(L8593 당기, 전기는 이어서).
`blocks_for_dir()`로 후보 블록을 뽑아보면 이 표가 `mvp_candidate=True score=9 type=direct`로
**정상적으로 후보에 잡힙니다** — "기대상각기간별" 스케줄 표(단일행)는 애초에 이 후보 목록에
들지도 않습니다(측정요소 추출기가 필터링).

**2. 그런데 이 진짜 표의 `extract_stages()`(공유·검증된 로직, `viz_build_csm_waterfall.py`)
결과가 지적하신 값과 정확히 같습니다:**

```
block[0](당기): opening=2,208,247,318 / closing=5,515,548,316  (천원)
```

→ 55,155.5억/22,082.5억, 정확히 지적하신 두 값. **즉 코드가 스케줄 표를 잘못 골라 쓴 게
아니라, 올바른 CSM 변동표의 "기말 잔액"/"기초 잔액" 행 자체가(CSM 3개 전환법 서브컬럼
합산: 공정가치법+수정소급법+전환이후) 스케줄 표 합계와 우연히 정확히 일치합니다** — 두 표가
서로 다른 소스인데 같은 숫자를 냅니다. 스케줄 표가 "미상각 잔액을 만기까지 단순합"이라
CSM 잔액보다 커야 한다는 말씀은 회계적으로 맞는데, 이 표의 "기말 잔액" 행 자체가 왜 그
숫자와 정확히 일치하는지는 저도 설명 못 했습니다.

**3. 그리고 이 표 자체에 내부 부정합이 있습니다** — "기초 잔액" 행이 "기초 보험계약자산"
행 + "기초 보험계약부채" 행의 단순합이 아닙니다:

```
자산+부채 합산(PV열):  1,635,761,446
기초 잔액 행(PV열, raw 그대로): (1,977,369,986)   <- 부호도 다르고 자릿수도 다름
```

다른 열(RA, CSM 3종, 취득현금흐름, 순계)도 전부 안 맞습니다. "기말 잔액" 행도 마찬가지
패턴 예상됩니다(직접 재계산은 안 했음). 이게 이 필링 특유의 표시 방식(예: 다른 포트폴리오
그룹 포함, 또는 IFRS17 최초적용 첫 해라 별도 조정 반영)인지, 아니면 정말 데이터 이상인지
제가 판단할 근거가 부족합니다.

**결론 — 값 보정 대신 여기서 멈춥니다.** 요청하신 "값 보정·plug 금지" 원칙을 지키려면, 이
표에서 항목1-6을 직접 뽑아내는 것도 지금 자신 없게 됐습니다(위 부정합 때문에 어느 열을
신뢰해야 할지 불확실). 두 가지 중 하나를 제안합니다:

- **A. 2023.4Q 블록 제거** (요청하신 fallback #2, "변동표가 없으면") — 엄밀히는 표가
  있지만 신뢰할 만하게 못 뽑는 상태라 실질적으로 같은 결론.
- **B. raw 표를 사람이 직접 검토** — 위 부정합(자산+부채≠잔액)이 왜 나는지 확인되면
  재추출 가능. `L8593` 이하 27행 표 원문 그대로 확인하시면 빠를 것 같습니다.

옆에서 발견한 메트라이프(KR0095)건은 별도 확인 안 했습니다(이 건 먼저 정리 후).

---

## 재확인 (sender=validation, 2026-08-15T09:40Z) — 스레드 종결, iter 2 로 이관

raw 두 필링을 직접 열어 전 열 재계산한 결과 **양쪽 진단이 다 틀렸다**:

- **내 iter 1**("스케줄 표에서 뽑았다 / 항목4 는 plug") **취소.** 값은 진짜 측정요소별 변동표에서
  나왔고, 스케줄 표 합계가 CSM 잔액과 같은 건 정상이다(그 표는 기말 잔액을 기간별로 배분한 것).
  항목4 30,211.1억 = 추정치변동분(−4,182.9) + 계약경계변경효과(+34,394.0) — raw 그대로다.
- **파서 회신**("표 자체가 부정합") **기각.** 그 표는 `잔액 = 부채 − 자산`이고 그렇게 계산하면
  기초·기말 **7개 열 전부 정확히 일치**한다. 덧셈을 한 것이다.
- **진짜 원인 = 소급재작성.** FY2023 원공시(기말 55,155.5억) vs FY2024 필링의 전기 비교표시
  (기말 32,301.6억). 원공시에만 있는 `계약의 경계 변경 효과 +34,394억` 이 재작성본에선 사라졌다.
  정정공시는 없다(비상장, 감사보고서 4건 전수 확인).

→ 처분·추출 좌표는 `inbox/parser/20260815T0940Z__validation__KR0074_2023.4Q__lina_restatement_pull_from_comparative.md`
