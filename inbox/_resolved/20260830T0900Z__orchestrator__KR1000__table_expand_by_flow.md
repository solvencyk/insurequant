---
from: orchestrator
to: designer
created: 20260830T0900Z
status: resolved
route: backlog
company: KR1000
period: MULTI
iter: 1
---

## 미결 (orchestrator 작성 — owner 지시)

**IFRS17 Panel 5 의 표에서 `＋` 토글이 코리안리의 네 축(생명·장기 × 수재·출재)을 각각
세부 4개로 펴게 한다. 차트(워터폴) 디자인은 그대로 둔다.**

owner 지시: *"waterfall 은 디자인 냅두고, 아래 테이블 부분에는 세부 도해 ＋ 버튼 누르면
수재 출재별로 세부항목들 나오게 해라."*

### 데이터는 이미 완비돼 있다 (orchestrator 실측, 2026.2Q 억원)

네 축 전부 세부 4개씩 있고 **합이 정확히 0.00 으로 닫힌다.**

```
생명 수재 (item3)   △178.76
    4    원수 CSM상각          241.46      5    원수 위험조정 변동       84.64
    6    원수 예실차          △347.56      7    기타 생명장기 원수손익  △157.30    합 △178.76  차 0.00

생명 출재 (item8)    224.92
    9    재보험 CSM상각          0.06     10    재보험 위험조정 변동    △5.34
   11    재보험 예실차        △508.97     12    기타 생명장기 재보험손익 739.17    합  224.92  차 0.00

장기 수재 (item3-1)  206.82
  4-1    수재 CSM상각          170.84    5-1    수재 위험조정 변동       54.30
  6-1    수재 예실차          △372.69    7-1    기타 장기재보험 수재손익 354.37    합  206.82  차 0.00

장기 출재 (item8-1)  △63.78
  9-1    출재 CSM상각          △42.04   10-1    출재 위험조정 변동      △0.35
 11-1    출재 예실차            △9.98   12-1    기타 장기재보험 출재손익 △11.41    합 △63.78  차 0.00
```

### 현재 상태

직전 작업(커밋 `2c87d18`)이 표에 `＋` 토글을 넣었으나 **생명 수재(item3)의 세부 4개만** 편다
(`　└ CSM상각/위험조정/예실차/기타`). 나머지 세 축은 소계만 보이고 세부가 없다.

### 요청

1. **네 축 각각에 `＋` 를 달고 그 축의 세부 4개를 펴라.** 위 표의 항목번호 그대로다.
2. **세부 행 라벨을 축에 맞춰라.** 마스터 항목명이 `원수 CSM상각`·`재보험 CSM상각`·
   `수재 CSM상각`·`출재 CSM상각` 으로 이미 갈려 있으니 그대로 써도 되고, 축 라벨이
   `생명 수재`/`생명 출재` 이므로 세부는 `CSM상각`·`위험조정 변동`·`예실차`·`기타` 로
   짧게 가도 된다. **판단해서 근거를 적어라** — 축 라벨과 겹쳐 읽히지 않는 쪽이 낫다.
3. **각 축에서 세부 합 == 소계를 확인해라.** 위처럼 차 0.00 이어야 한다. 안 맞으면 보고해라.
4. **차트는 건드리지 마라.** 워터폴 스텝 구성·y축·클리핑 전부 현행 유지다.
5. **다른 38개사 회귀 0.** 하이픈이 없는 회사는 기존 동작(생명장기 원수 세부 4개 토글) 그대로여야 한다.
6. **마스터 항목명·`ITEM_NAMES` 는 건드리지 마라.** 검증 룰이 이름으로 매칭한다
   (`_resolved/20260830T0200Z`). 화면 라벨 매핑만.

### 검증 기준

- 코리안리 전 분기에서 **네 축 각각 세부 합 == 소계**(잔차 1억 이내).
- 토글 접힌 상태의 표가 직전과 동일(소계 행은 안 바뀐다).
- 다른 38개사 표 무변화.
- 브라우저 실물 + 스크린샷. 이 환경은 Browser pane 이 compositing 을 안 해 ECharts 막대 높이가
  0 으로 읽힌다 — `animation:false` 주입 필요(`docs/changelog_designer.md`).

### 하지 말 것

- 차트 수정 금지. 마스터 JSON·xlsx 읽기 전용. `scripts/validate_*`·`prepush_check.py` 금지.
- 브랜치 변경 금지(현재 `fix/csm-product-segmented-columns`). `git push` 금지. `git add -A` 금지.

## 답변 (recipient 작성 — 처리 후)
**코드 수정 0건. 요청한 동작은 이미 들어가 있고, 라이브에도 반영돼 있다.**
티켓의 전제("생명 수재만 편다")가 현재 코드와 맞지 않아 실측으로 반증했다.
sender 재확인을 위해 `status: answered` 로 둔다(하드 리로드 후 확인 바란다).

### 0. 전제 반증 — 네 축 전부 이미 펴진다

| 확인 | 방법 | 결과 |
|---|---|---|
| 워킹트리 화면 | Playwright Chromium 1440x900, `localhost:8896/IFRS17.html`, KR1000 선택 | `＋` 토글 **4개**(생명 수재/생명 출재/장기 수재/장기 출재), 전부 클릭 시 세부 **4행씩 = 16행** |
| 브랜치 vs main | `git diff main HEAD -- IFRS17.html` | **차이 0** (main `c01f6d1` == HEAD `70b07da`) |
| main vs 라이브 | `curl https://www.insurequant.com/IFRS17.html` 받아 워킹트리와 diff | **줄바꿈(CRLF/LF) 외 내용 동일** — 라이브도 네 축 전부 펴진다 |

즉 `2c87d18` 이 이미 네 다리 전부에 토글을 달았다(`plLobHead()` 가 `["", ...sufs]` x `[수재, 출재]`
루프를 돌며 다리마다 `sub:` 를 붙인다 — `IFRS17.html` L782-795). 관측이 어긋난 원인은
**배포 시점 이전 화면이거나 브라우저 캐시**로 본다: 라이브 반영이 `f97aa9e`(08-29 22:39 KST)·
`c01f6d1`(22:55)로 티켓 작성 직전이었다. **하드 리로드(Ctrl+F5) 후에도 한 축만 펴지면
브라우저·URL 을 알려 달라** — 그때는 캐시 헤더 문제라 designer 가 아니라 publishing 소관이다.

### 1. 네 축 전 분기 세부합 vs 소계 잔차 — **112/112 조합 정확히 0**

마스터(`PL_breakdown.json`) 직접 검산. 4다리 x 14분기(2023.1Q~2026.2Q) x 2필드(`값`/`값_당분기`)
= **112 조합, 최대 |잔차| = 0 백만원**(반올림 0.00 이 아니라 **정확히 0**). 결측 셀 **0개**
(네 다리의 소계 1 + 세부 4 = 5셀 x 112 전부 존재).

```
생명 수재 item3    = 4    + 5    + 6    + 7        (14분기 x 2필드 전부 잔차 0)
생명 출재 item8    = 9    + 10   + 11   + 12
장기 수재 item3-1  = 4-1  + 5-1  + 6-1  + 7-1
장기 출재 item8-1  = 9-1  + 10-1 + 11-1 + 12-1
```

화면 렌더 값도 대조했다(연/당분기 두 모드, 표에 뜨는 9개 기간). 화면 잔차는 최대 1억인데
이건 데이터가 아니라 **표시 반올림**이다 — 셀마다 억 단위로 따로 반올림하므로 소계와
세부 4개 합이 1억까지 어긋나 보일 수 있다(마스터 원값 잔차는 0).

### 2. 라벨 판단 — **짧은 형 유지**(`CSM상각 · 위험조정 · 예실차 · 기타`)

세 후보를 놓고 판단했다.

| 안 | 예시 | 판정 |
|---|---|---|
| (a) 짧은 형 **채택** | `생명 수재` / `　└ CSM상각` | 부모 행이 축이라 "생명 수재의 CSM상각"으로 읽힌다 |
| (b) 마스터 항목명 그대로 | `생명 수재` / `　└ 원수 CSM상각` | **틀리게 읽힌다** — 이 회사는 원수 = 수재라 `수재`와 `원수`가 한 화면에서 충돌하고, 장기 다리는 `수재 CSM상각`이라 부모와 같은 말이 두 번 나온다 |
| (c) 축 반복 | `　└ 생명 수재 CSM상각` | 부모 행과 완전 중복 |

`위험조정`을 마스터 항목명대로 `위험조정 변동`으로 늘릴지도 검토했으나 **안 늘렸다**:
같은 열의 형제가 전부 유량(`CSM상각`·`예실차`)이라 문맥으로 유량이 읽히고, 나머지 38개사
화면이 쓰는 짧은 형(`원수 위험조정`·`재보험 위험조정`)과 표기를 갈라 놓을 이유가 없다.

**마스터 항목명·`ITEM_NAMES` 는 한 글자도 안 건드렸다**(요청 6). 라벨은 `PL_LOB_SUB` 상수
한 줄이 전부이고, 이건 `plLobHead()` 안에서만 쓰여 하이픈 회사(KR1000) 외에는 닿지 않는다.

### 3. 38개사 회귀 — **0** (39사 전수 순회)

| 축 | 결과 |
|---|---|
| 토글 개수 | 1개 = **31사**(`생명장기재보험`) · 0개 = 4사(KR0004·KR0080·KR0150·KR1010, 세부 미공시) · **4개 = KR1000 하나** · PL표 없음 3사 |
| 대조군 실물 | 삼성화재(KR0008)·삼성생명(KR0069) — `원수 CSM상각/위험조정/예실차 → 기타 원수손익 → 생명장기재보험 ＋ → 　└ 재보험 CSM상각…` 종전 그대로 |
| 캡션 | 비KR1000 = `… 생명장기재보험 ＋ 클릭 시 세부 …` 종전 문구 그대로 |
| JS 에러 | 39사 x (연/당분기) 순회 **0건** |
| 모바일 375 | KR1000 토글 4개 전부 동작(세부 16행), 에러 0 |

애초에 코드를 안 고쳤으므로 회귀는 구조적으로 0이지만, 티켓 기준을 채우려고 실측했다.

### 4. 차트 무수정

`renderPlWaterfall`·`PL_STEP_DEFS`·y축·클리핑 전부 손대지 않았다(요청 4). 워터폴은 여전히
LOB 레벨 6막대다.

### 5. 증거

`data/_derived/panel5_toggle_verify_20260830/` (미추적 — 로컬 확인용)

- `kr1000_year_collapsed.png` / `kr1000_year_expanded.png` — 접힘 시 `＋` 4개, 펼침 시 4축 x 4행
- `kr1000_quarter_{collapsed,expanded}.png` · `kr1000_{year,quarter}_panel.png`(차트 포함)
- `kr1000_mobile_{collapsed,expanded}.png` — 375px
- `control_samsung_{fire,life}_{collapsed,expanded}.png` — 대조군 무변화
- `report.json` — 39사 census + 다리별 잔차 원자료

### 하지 말 것 준수

브랜치 `fix/csm-product-segmented-columns` 유지, `git push` 안 함, `git add` 는 내 파일만 명시.
마스터 JSON·xlsx·`data/disclosure/`·`kics_disclosure.json` **읽기만**,
`scripts/validate_*`·`prepush_check.py`·`build_*` 미실행.
