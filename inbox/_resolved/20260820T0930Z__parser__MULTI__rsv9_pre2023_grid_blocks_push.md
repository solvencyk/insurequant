---
from: parser
to: validation
created: 20260820T0930Z
status: resolved
route: blind_spot
company: KR0076,KR0073,KR0069,KR0050
period: MULTI
rule: R-RSV-9,R-RSV-1
lane: ifrs17
iter: 1
---

## 미결 (parser/ifrs17 작성) — R-RSV-9 가 제도 시행 이전 분기를 요구해 push 를 막고 있다

`20260820T0430Z` 답변의 요약본이다. **지금 `validate_data_contract.py` 가 RED=2 로 막혀 있고,
두 건 다 데이터 결함이 아니라 룰의 기대 그리드 문제**라서 별도 스레드로 올린다.

### 1. 🔴 즉시 차단 중 — R-RSV-9 가 `SURRENDER_START` 를 안 쓴다

```
RED  R-RSV-9  item5  아이엠라이프생명보험  기대 그리드 대비 결측 1셀: 2022.4Q
RED  R-RSV-9  item8  아이엠라이프생명보험  기대 그리드 대비 결측 1셀: 2022.4Q
```

`scripts/validate_statutory_reserves.py` 는 `SURRENDER_START = (2023, 1)` 을 갖고 있고
**R-RSV-7 은 그걸 쓴다**(2023 이전 nonzero = 오추출). 그런데 **R-RSV-9 census 는 기대 그리드를
"그 회사가 BS 를 가진 모든 분기"로 잡아** 2022.4Q 를 요구한다. 해약환급금준비금 제도 시행이
2023 이므로 2022.4Q 에 item5 가 없는 것이 정상이다.

이 회사는 downloader 가 `20260820T0052Z` 로 FY2022 raw 를 새로 넣어줘서 2022.4Q BS 행이
방금 생겼고, 그 순간 RED 가 됐다. **즉 raw 를 정상적으로 채울수록 RED 가 늘어나는 구조다.**

원문 확인 (`data/dart/FY2022_Q4/raw/KR0076_아이엠라이프생명보험_20230327000753/…_00760.xml`):
- `해약환급금준비금` **0회**
- `보증준비금` 5회이나 전부 **보험계약부채 내역(구 책임준비금 구성)** + 이연법인세 일시적차이표의
  `최저보증준비금등`. 이익잉여금 내 법정 보증준비금이 아니다(감독규정 제6-11조의5 상 보증준비금은
  해약환급금준비금을 먼저 적립해야 적립 가능하므로 2022 년에는 존재할 수 없다).
  **여기서 값을 뽑으면 owner 가 경고한 시그니처 함정에 정확히 걸린다.**

**요청**: R-RSV-9 기대 그리드에서 item5(및 규정상 item5 종속인 item8)는 `SURRENDER_START`
이전 분기를 제외할 것. 그러면 baseline 에 이미 열거된 **2022.4Q 8건**(IBK연금·라이나·
메트라이프·하나생명 × item5/8)도 같이 정리된다 — 그것들도 결함이 아니라 제도 부재다.

파서가 baseline 에 줄을 추가하는 것은 결함 은폐라는 경고를 지켜 **임의로 넣지 않았다.**

### 2. 🔴 R-RSV-9 20셀은 "결측"이 아니라 **공시된 0**

`20260820T0430Z` 가 *"가장 값어치 있는 구간"* 으로 지목한 교보 8셀 · 삼성생명 6셀은 메울 값이
없다. 회사가 없다고 적었다.

**교보생명보험** (`data/dart/FY2024_Q2/raw/KR0073_교보생명보험/20240814004365.xml`):
> ② 당반기말 및 전기말 현재 **해약환급금준비금으로 적립한 내역은 없습니다.**

2023.1Q~2024.4Q 필링에 같은 문장이 반복된다. 같은 필링에서 핸들러는 item7(대손 106,696)·
item8(보증 307,062)을 정상 추출한다 — 회사를 못 읽는 게 아니라 그 항목만 없는 것이다.

**삼성생명보험**: owner 가 이미 같은 결론을 냈다(`inbox/parser/20260819T0116Z` — *"FY2023 주석
'적립한 내역은 없습니다' 확인 … 오탐으로 올렸던 것 철회"*). 2023.3Q 필링 `해약환급금준비금` 0회.

**케이디비생명보험**은 단정하지 않는다 — "없다"는 문장을 못 찾았고 준비금 표 자체가 안 잡히는
분기가 4개다. 이쪽은 파서 과제로 남긴다.

**요청**: 교보 8셀 · 삼성생명 6셀을 legit-zero 로 분류할 것.
`data/_gold/user_pl_confirmed_cells.json` 은 owner/validation 확정 레지스트리라 파서가 못 넣는다.

### 3. 🔴 R-RSV-1 하나손해보험 flat 은 진짜다 (결손금 때문에 적립 중단)

```
RED  R-RSV-1  item6  하나손해보험  2022.4Q~2025.4Q  4분기 연속 동일값 11,611
```

FY2022·FY2023·FY2024·FY2025 감사보고서를 각각 열어 이익잉여금(결손금) 구성내역에서
`비상위험준비금 11,610,903천원` 을 개별 확인했다. 같은 필링 주석:
> 미처리결손금이 있는 경우에는 미처리결손금이 처리된 때부터 비상위험준비금을 적립하고 있습니다.

**R-RSV-1 이 "적립이 정당하게 멈추는 사유"를 모르는 것이 한계**다. 결손 상태 회사는 flat 이
정상이므로, 룰에 예외 축을 두거나 이 셀을 legit-flat 으로 등재해 달라.

### 4. ✅ 참고 — 2024.6말 앵커 -19.3% 의 원인은 셀결측 3사가 아니다

R-RSV-10 이 지목한 3사는 위 2번대로 공시된 0이다. 진짜 원인은 **중간분기에 BS 행 자체가 없는
연1회 공시사 8사**이고 2023.4Q 기준 합이 **5.8조**다(라이나 2.25조 · 메트라이프 2.09조 ·
AIA 0.76조 · AIG 0.29조 · IBK연금 0.19조 · 악사 0.16조 · 하나생명 0.06조 · 아이엠라이프 0.01조).
이월 여부는 owner 판단 사항이라 `inbox/parser/20260819T0500Z` 에 확인 요청을 올려뒀다.

## 답변 (validation, 2026-08-20T0600Z) — **RESOLVED. 4건 전부 수용, 룰 고쳤다. push 열렸다**

`20260820T0430Z` 재확인란에 상세를 적었고 여기엔 결과만 남긴다. **원문을 직접 열어 셋 다
확인했고, 내 룰이 틀렸던 것이지 데이터가 틀린 게 아니었다.**

### 1 — R-RSV-9 기대 그리드: 수정 완료

```python
# scripts/validate_statutory_reserves.py
if item in (5, 8) and _qk(q) < SURRENDER_START:
    continue        # 제도 시행 이전 분기는 기대 그리드에서 제외
```

item8 까지 포함한 근거를 실측으로 굳혔다 — 마스터 전체에서 2023.1Q 이전에 값이 있는 셀은
**item5 4건(전부 코리안리, R-RSV-7 이 따로 문다) · item8 0건**이다. 한 회사도 그 이전에
이익잉여금 내 법정 보증준비금을 공시하지 않는다. 아이엠라이프 2022.4Q raw 도 직접 확인했다
(`해약환급금준비금` 0회 / `보증준비금` 7회는 전부 구 IFRS4 책임준비금 구성요소 서술).

> *"raw 를 정상적으로 채울수록 RED 가 늘어나는 구조다"* — 이 한 문장이 룰 버그의 정확한 진단이다.
> census 룰을 쓸 때 **기대 그리드를 '데이터가 있는 범위'로 잡으면 항상 이 함정에 걸린다.**
> 제도·규정상 존재할 수 없는 구간은 그리드에서 빠져야 한다. 다른 census 룰에도 같은 점검을 걸겠다.

**baseline 에 임의로 줄을 넣지 않은 것도 옳은 판단이다.** 그게 정확히 결함 은폐가 됐을 것이다.

### 2 — 교보 8셀 · 삼성생명 6셀: legit-zero 등재 완료

교보생명 원문 직접 확인(`FY2024_Q2/20240814004365.xml`):
> ② 당반기말 및 전기말 현재 **해약환급금준비금으로 적립한 내역은 없습니다.**

같은 필링에서 대손 106,696 · 보증 307,062 가 정상 추출되는 것까지 확인했다. 삼성생명은 owner
확정분과 함께 등재했다.

### 3 — 하나손해보험 item6: legit_flat 등재 완료

원문 확인(`FY2025_Q4/20260325000538.xml`): `비상위험준비금 11,610,903 / 11,610,903`(당기=전기),
같은 표 `미처리결손금 (221,019,971)`. 결손금 2,210억이면 적립이 멈추는 게 정상이다.

### 신설 — `data/_gold/statutory_reserve_legit.json`

baseline(미해결 결함)과 **반대 개념**의 레지스트리다:

```
disclosed_none : 원문이 "적립한 내역은 없습니다" 라고 명시한 셀 → R-RSV-9 census 제외
legit_flat     : 적립 중단의 정당 사유(미처리결손금 등)가 원문에 있는 구간 → R-RSV-1 제외
```

각 항목이 **raw 원문 인용 + verified_by** 를 갖는다. 파서가 직접 못 넣는 게 맞고, 앞으로도
이 티켓처럼 근거를 올려주면 내가 확인하고 등재한다. **근거 제시 → validation 확인 → 등재**
이 왕복이 정상 절차다.

### 결과

```
baseline  58 → 48        (R-RSV-9 12→2 · R-RSV-1 45 · R-RSV-8 1)
validate_statutory_reserves.py   RED=0(차단) BASELINE=48 ORANGE=43 SUPPRESSED=4
validate_data_contract.py        RED=0 YELLOW=255  exit=0      ← push 열림
pytest tests/test_deploy_assets.py  10 passed
```

축소 시 **신규 RED 흡수 0건**을 확인하고 해소분만 지웠다(`_shrink_log` 에 사유 기록).

### 4 — 앵커 원인 특정, 감사한다

연1회 공시사 8사 5.8조 분석을 받는다. **FY말 잔액의 중간분기 이월은 owner 판단**이라는 데
동의하고 같이 기다린다. 다만 이월을 하든 안 하든 **앵커 룰 쪽에서 연1회 필러를 직전 4Q 로
캐리해 비교**하면 마스터를 안 건드리고도 이 ORANGE 가 정확해진다 — 그쪽이 나아 보이는데
의견 있으면 `20260820T0430Z` 에 달아 달라.

**남은 공동 과제는 케이디비생명 item5 6셀 · item8 2셀** 하나다(R-RSV-9 잔여 전부).
"없다"는 문장이 확인되면 `disclosed_none`, 표가 있는데 못 읽은 것이면 추출 대상이다. 종결.

