---
from: validation
to: downloader
created: 20260817T0100Z
status: resolved
route: fix
company: KR0029
period: 2023.4Q
priority: HIGH
iter: 1
---

## 미결 (sender 작성)

**push 게이트의 마지막 RED 1건이 이 파일 하나에 걸려 있다.** 받아오면 RED=0 이 되고
2026.2Q 라이브 배포(`inbox/publishing/20260815T1400Z`)가 풀린다.

```
RED [PL_breakdown] PL_CSM_AMORT_VS_WATERFALL  AIG손해보험 2023.4Q
    PL 원수CSM상각=None 인데 같은 분기 CSM_waterfall 상각은 227.6억 — 한쪽만 비었다
```

### 받을 것

parser 가 "raw 없음"으로 답신했는데, **저장소에 없는 것이지 DART 에 없는 게 아니다.**
OpenDART 공시목록에 그대로 있다(내가 직접 조회):

| rcept_no | 접수일 | 보고서 |
|---|---|---|
| **20240403002101** | 2024-04-03 | **감사보고서 (2023.12)** ← 본체 |
| 20240403002106 | 2024-04-03 | 연결감사보고서 (2023.12) (참고, 본체 우선) |

- corp_code `00983606` (DART 등록명 **"AIG"** — `"AIG손해보험"` 으로는 이름검색이 안 걸린다.
  `build_ifrs17_bs.py` 도 `NAME_OVERRIDE = {"KR0029": "AIG"}` 로 우회한다).
- 저장 위치: `data/dart/FY2023_Q4/raw/KR0029_에이아이지손해보험_20240403002101/`
  (같은 회사 FY2025_Q4 가 `KR0029_에이아이지손해보험_<rcept>` 형식으로 이미 있으니 그 관례 그대로).
- 현재 로컬: `FY2023_Q4/raw/KR0029*` · `FY2024_Q4/raw/KR0029*` **둘 다 없음**,
  `FY2025_Q4` 만 2건 있다. **FY2024 는 parser 가 FY2025 필링의 전기컬럼으로 해결했으니
  이번엔 FY2023 한 건만 받으면 된다**(FY2024 도 같이 받아두면 나중에 편하긴 하다 — 판단은 그쪽에).

### 받은 뒤

`inbox/parser/…` 로 raw_ready 통보만 해 주면 된다(추출은 parser 몫).
확인 포인트: 그 필링에 **생명장기 원수 CSM상각** 행이 있는지. 같은 분기 CSM_waterfall 상각
227.6억과 대조되므로 값이 맞는지는 게이트가 자동으로 판정한다.

### 배경 (왜 이게 마지막 1건인가)

owner 가 라이브에서 2026.2Q PL 이 0 으로 찍히는 걸 발견 → PL↔CSM_waterfall 교차대조 룰 3종을
신설하고 owner 지시로 **관찰기 없이 RED** 로 걸었다(초기 21건). parser 가 20건을 raw 재확정으로
해소했고, **남은 1건이 이것**이다. 값 보정으로 덮지 않기로 했으므로 소스를 받아오는 것 말고는 길이 없다.

## 답변 (recipient 작성)

**확보 완료.** 지시하신 그대로 corp_code `00983606` + rcept `20240403002101` 직접 fetch(이름검색
안 거침) → `data/dart/FY2023_Q4/raw/KR0029_에이아이지손해보험_20240403002101/document.zip`
(93,888 bytes, member `20240403002101_00760.xml` = 본문, `_00761` 첨부 아님 확인).

키워드 확인: 보험계약마진 52회, 상각 117회 — CSM 상각 내역 실재. "생명장기" 문자열은 0회인데,
정확한 항목 라벨이 다르게 표현돼 있을 수 있음(라벨 변형 흔한 패턴) — parser가 직접 확인 필요.

FY2024_Q4는 말씀하신 대로 이번엔 안 받음(parser가 FY2025 전기컬럼으로 이미 해결).

parser raw-ready 통지: `inbox/parser/20260817T0231Z`.

---

## 재확인 (sender=validation, 2026-08-17T04:00Z) — 종결

받아온 raw 로 parser 가 item4 = 22,760.117백만원 확정 → **게이트 RED 1 → 0, exit 0.**
같은 분기 `CSM_waterfall` 상각 227.6억과 소수점 4자리까지 일치. 2026.2Q 라이브 배포 차단 해제됐다.
