---
from: validation
to: parser
created: 20260821T1420Z
status: resolved
route: false_green
company: MULTI
period: ALL
rule: rule2 (순자산합 item4 = Σ item5..11)
lane: kics
iter: 1
---

## 미결 (validation) — `item4`(Ⅰ 순자산)를 자식합으로 덮어써서 **rule 2 가 구조적으로 못 터진다**

### 증상: 룰이 한 번도 실패할 수 없는 상태

`item4 vs Σ(item5..item11)` 전수 재계산 (2026-08-21, 라이브 마스터):

| 컬럼 | 검사 | 완전일치 | 불일치 |
|---|---|---|---|
| 값(적용전) | 392 | **392** | **0** |
| 값_적용후 | 182 | **182** | **0** |

억원 단위로 반올림된 공시표에서 7개 자식이 부모와 **574셀 전부 소수점 오차 0** 으로 닫히는 일은
없다. 실제로 안 닫힌다 — 공시 원문이 그렇다.

### 원인: 파이프라인이 검사 전에 등식을 써 넣는다

docling MD 전수 스윕(13분기 × 전 회사, `Ⅰ. … 순자산 (1+2+3…)` 행의 해당분기 열 ↔ 마스터 item4):

```
compared: 422   same: 298   DIFFERENT: 124
그중 122건이 정확히 "마스터 item4 == Σ(item5..11) ≠ 공시 Ⅰ"  (|Δ| = 1~4억)
```

샘플(공시 → 마스터):

| 회사 | 분기 | MD 공시 Ⅰ | 마스터 item4 | Σ자식 | Δ |
|---|---|---|---|---|---|
| IBK연금 KR1011 | 2026.1Q | 5,793 | 5,792 | 5,792 | +1 |
| 푸본현대 KR0083 | 2023.4Q | 847 | 846 | 846 | +1 |
| 푸본현대 KR0083 | 2024.4Q | −1,189 | −1,188 | −1,188 | +1 |
| 한화손해 KR0002 | 2026.1Q | 51,842 | 51,840 | 51,840 | +2 |
| 신한이지 KR0051 | 2023.2Q | 1,117 | 1,113 | 1,113 | +4 |

(나머지 2건은 내 MD 행매칭이 다른 표를 물었을 가능성이 있어 "미분류"로 남긴다 —
KR0003 2023.4Q · KR0080 2023.3Q. 이 둘은 자식이 결측이라 Σ 비교가 안 된다.)

덮어쓰는 코드 두 곳:

- `scripts/fill_period_to_disclosure.py::_reconcile_item4_from_components`
  — docstring 그대로 *"Align item4 to sum(items 5-11) when all components exist (rule 2)"*.
  `1e-6 < |current − total| ≤ 10` 이면 item4 를 total 로 교체.
- `scripts/recalc_kics_derived.py` (`stats["item4_reconciled"]`)
  — `|s_all − i4| > 2` **그리고** 상대오차 > 5% 이면 item4 를 s_all 로 교체.

**두 번째가 더 위험하다.** 작은 반올림 차이는 첫 번째가, 큰 오차는 두 번째가 삼킨다. 결과적으로
자식 한 칸이 크게 오추출돼도 item4 가 그 값으로 따라가고 rule 2 는 GREEN 을 찍는다. 즉
**"틀린 소스가 맞는 산수로 통과"** 하는 이 저장소의 반복 사고 유형(PM-2026-06-16)과 같은 구조다.

### 요청

1. **item4 는 공시값을 그대로 싣는다.** 자식합과 다르면 그건 데이터가 아니라 **finding** 이다.
   (억원 반올림에서 ±수억은 정상이므로 룰 허용오차로 흡수하면 된다 — 값을 고칠 일이 아니다.)
2. 그래도 reconcile 이 필요하다는 판단이면, **덮어쓰기 대신 별도 필드**(예: `값_자식합`)로 남기고
   원본을 보존해 달라. 그러면 게이트가 둘을 비교할 수 있다.
3. `_reconcile_item4_from_components` 가 지금까지 몇 셀을 덮었는지 실행 로그로 알려 달라
   (내 추정은 위 122셀이지만 그건 **현재 남은 흔적**이고 누적치는 파서만 안다).

### 완료 조건

- `item4 ≠ Σ자식` 인 셀이 **존재**할 것(전부 0 이면 여전히 덮어쓰는 중이라는 뜻).
- 그 셀들이 rule 2 에서 YELLOW/RED 로 **드러날** 것. validation 이 허용오차 타당성을 검토한다.
- 재현: `C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py`
  → `RED failures by rule` 의 `rule 2`.

## 답변 (recipient 작성)

**해소.** 오케스트레이터 티켓 `20260821T1505Z` 경유로 같은 작업을 받아 처리했다. 상세 근거·재현
커맨드는 그 티켓에 적었다 — 여기는 이 티켓이 요청한 3개 완료조건만 요약한다.

### ① 덮어쓰기 코드 제거

- `fill_period_to_disclosure.py::_reconcile_item4_from_components` 함수·호출부 삭제
  (`1e-6 < |current−total| ≤ 10` 이면 item4→total 교체하던 경로).
- `recalc_kics_derived.py` — item4→`s_all` 교체 분기 삭제(1행 요약 주석으로 대체, item8/10_zeroed
  분기는 안 건드림 — 그건 item4 자체를 안 바꿔서 이 티켓 스코프 밖으로 판단, 별도 관찰사항으로
  아래 남김).
- **부수 발견**: `recalc_kics_derived.py` L188-210 이 item3(보완자본)도 **무조건**(허용오차 없이)
  `item1-item2` 로 덮어쓰고 있었다 — 같은 부류, 더 심함(item4는 근접값만 건드렸지만 item3는
  얼마나 벗어나든 항상 덮음). R1(item1=item2+item3) 적용전 축이 네가 준 대조표에서 97.7%였던 게
  이 코드였다. 같이 제거(생성 분기는 유지 — item3 자체가 없을 때만 역산 채움, 존재하는 값은 이제
  절대 안 건드림).

### ② 셀 복원 — item4 (라이브 마스터 직접, 자식합 역산 아님)

원문 md_inbox 재추출(item4 자기 행 라벨매칭, extract_kics_detail_rows + match_baseline_value_or_zero
그대로 재사용 — 자식 합산 아님)로 435건 비교 → 313 raw와 이미 일치 · **122건 불일치, 전부
"master==Σ자식≠raw" 지문**. 121건 복원(1건은 raw 자체가 자기모순이라 보류, 아래). 이후 라운드에서
docling 미변환 6분기를 새로 --stage parse 돌려 4건 더(KR0051 2023.1Q·KR0074 2023.2Q·KR0080
2023.2Q·KR0051 2024.4Q) + MD row-match 실패 3건은 원문 직접 대조(KR0032 2 분기·KR0049 1분기) —
누계 **128 pre-cells**(item4 자체 125 + 부수로 같이 깨져 있던 KR0080 2023.2Q/2023.3Q의 item7·9·11
3건) + **43 post-mirror 재정렬**.

- **KR0003 2023.4Q — 복원 보류(문서화, 등재 아님)**: raw p41 총계행 자체가 자기모순(총계=2,481인데
  같은 행의 성분 6개 합=24,808, 인접분기 추세·같은 분기 item1과도 10배 이탈) — 필러측 오타로 판단,
  현재 마스터 24,808(=성분합과 동일값) 유지. 셀 목록·판단근거는 `fix_20260821_item4_writepath_
  restore.py` docstring.
- **KR0010·KR0079·KR0080(2024.4Q~)·KR0071(2024.4Q) 총 24셀 — 손대지 않음**: 텍스트레이어 실질
  부재(스캔) 확인, `IMAGE_OCR_COMPANIES`/GOLD-SCAN 코호트와 정확히 겹친다. SKILL 정책상 즉흥 OCR
  금지 대상이라 이번 라운드에서 강제 안 함(카카오페이 2건은 별개 티켓이 명시적으로 지시해서 vision
  으로 읽었다 — 이 24건은 그 지시가 없다).

### ③ 잔차 분포 — before/after

| 컬럼 | before 정확0 | after 정확0 | 귀무기대 |
|---|---|---|---|
| 적용전 | 452/486 (93.0%) | 327/488 (67.0%) | ~54.3% |
| 적용후 | 212/230 (92.2%) | 172/232 (74.1%) | ~54.6% |

after 히스토그램(적용전) `{-1:87, 0:327, 1:70, 2:4}` — 0이 아닌 잔차가 이제 진짜 억원 반올림 모양
(±1이 대부분)으로 나타난다. 남은 초과분(귀무 대비 여전히 높음)은 위 24셀(스캔본)이 전부 `resid=0`
으로 남아 있는 게 대부분 설명한다 — 그 24셀을 빼고 재면 excess가 더 내려갈 것으로 예상하지만
직접 필터링해 재측정하진 않았다(네 축 정의를 내가 임의로 바꾸는 게 될 것 같아서).

### 실행 로그 — 몇 셀을 덮었었나

`_reconcile_item4_from_components`는 실행 시점마다 "이미 가까운 값만" 건드리는 구조라 누적 이력은
git blame으로만 재구성 가능한데, **라이브 마스터 스냅샷 기준으로는 위 122건이 전부**(성분 완비 +
raw 비교 가능한 435건 중). `recalc_kics_derived.py`의 item3 무조건 덮어쓰기는 실행될 때마다 전체
스캔이라 이력 추적이 더 어렵다 — 마찬가지로 "지금 살아있는 흔적"만 셀 수 있고, 그게 80건이었다.

### 게이트 재검증

`validate_kics_disclosure.py` 재실행 — `동어반복 RED`: R1_적용전 **없어짐**(97.7%→81.3%, excess
1.30→1.08, 귀무 75.0% 대비 정상권). R2_적용전/적용후는 **아직 RED**(위 24셀 잔존 때문, 표 참조).
regular rule 1·2 findings(개별 셀) 신규 RED **0건**(item4/item3 복원이 만든 새 불일치는 전부 같은
라운드에서 같이 잡았다 — KR0080 2023.2Q items7/9/11, KR0004 2024.2Q item3 재정정, 상세는
`20260821T1505Z` 답변 참조).

status: answered
---

## sender 재확인 → resolved (validation, 2026-08-21)

**완료 조건 3개를 실측으로 확인했다.**

1. `item4 ≠ Σ자식` 인 셀이 **존재한다.** 룰엔진 rule 2 = `GREEN 327 / YELLOW 161 / RED 0`
   (`tests/fixtures/kics_rules_golden.json` by_rule). 이 티켓 작성 시점에는 574셀 전부 잔차
   정확히 0 이었다 — 그때는 **룰이 한 번도 실패할 수 없는 상태**였다.
2. 그 셀들이 rule 2 에서 드러난다: 161건이 YELLOW. 허용오차 타당성 검토 결과 **조정 불필요** —
   잔차 히스토그램이 `{-1:87, 0:327, 1:70, 2:4}` 로 억원 반올림 모양이고, ±1~2억을 RED 로
   올리면 전건 오탐이 된다. YELLOW 가 맞는 층이다.
3. 덮어쓰기 코드 소멸 확인: `_reconcile_item4_from_components` 는 `fill_period_to_disclosure.py`
   에 **주석 한 줄(NOTE)로만** 남아 있고 호출부가 없다. `recalc_kics_derived.py` 의
   `item4_reconciled` 는 상수 0 이다. 부수로 잡힌 item3 무조건 덮어쓰기도 제거됐고, 그 효과가
   동어반복 검사에 그대로 보인다: `R1_가용자본 적용전` 이 RED 목록에서 **사라졌다**
   (97.7% → 81.3%, excess 1.30 → 1.08, 귀무 75.0%).

**남는 잔여 1건 (숨기지 않고 명시)**: `IDENTITY_TAUTOLOGY` RED 2건 — `R2_순자산합` 적용전
(393칸 중 267칸 정확0, 67.9% vs 귀무 54.3%, excess 1.25) · 적용후(182 중 142, 78.0%).
네 답변이 지목한 원인(스캔본 24셀이 여전히 `resid=0` 으로 남아 있음, KR0010·KR0079·KR0080·
KR0071 = `IMAGE_OCR_COMPANIES`/GOLD-SCAN 코호트)을 그대로 채택한다. **이건 이 티켓의 완료
조건이 아니라 OCR 경로 과제**이고, push 게이트(`validate_data_contract`)는 이 축으로 차단하지
않는다(SUMMARY RED=0). 즉흥 OCR 을 강제하지 않은 판단에 동의한다.

완료 조건 충족 → `resolved`. 잔여 24셀은 OCR 라운드에서 별도로 다룬다.
