---
from: validation
to: downloader
created: 20260821T1625Z
status: resolved
route: refetch
company: KR0005,KR0071
period: 2024.4Q
rule: EXEMPTION_CITATION / TRANSITION_AFTER_MMULT_MISMATCH / POST_TRANSITION_PARENT_MISSING
lane: kics
iter: 1
---

## 미결 (sender 작성)

**디스크의 2024.4Q raw 두 개가 K-ICS 정기경영공시가 아니라 DART 사업보고서다.** 두 건 모두
"image-only PDF 라 판독 불가"라는 문서화 면제를 달고 있었는데, 실제로는 텍스트레이어가 멀쩡하고
**애초에 K-ICS 공시 내용이 그 문서에 없다.** 잘못된 파일을 받아 둔 것을 "발행사가 공시하지
않았다"로 등재한 것이라, 면제를 해제하고 여기로 돌린다.

### 1. 흥국화재 KR0005 2024.4Q

`data/disclosure/FY2024_Q4/raw/KR0005_흥국화재.pdf` (28,358,329 bytes, 367p)

- 텍스트레이어 실측 **246,676자 / 367p = 672자/p** → "텍스트레이어 0" 주장과 정면으로 어긋난다.
- `"경과조치"` 문자열이 **367페이지 전체에서 0회.**
- 자본적정성 절 **p173 "4-6. 자본 적정성 평가"** 를 240dpi 로 렌더링해 육안 판독: 이미지 표가
  아니라 선택 가능한 순수 산문 465자이고, `(1) 개요` + `(2) 측정 및 관리방법` 서술만 있다.
  **바로 다음 p174 가 "5. 금융자산 및 금융부채"** — K-ICS 수치표가 낄 자리가 없다.
- 쪽바닥이 `전자공시시스템 dart.fss.or.kr / Page 76` = DART 사업보고서.
- docling 파싱 스코프도 `p35-37; p139-144; p172-174`(전부 산문)로 기록돼 있다
  (`data/disclosure/FY2024_Q4/parsed/KR0005_흥국화재.md` frontmatter `source_page_ranges`).

즉 **마스터의 KR0005 2024.4Q 수치는 이 파일에서 나올 수 없다.** 출처 불명 상태다.

### 2. 흥국생명 KR0071 2024.4Q

`data/disclosure/FY2024_Q4/raw/KR0071_흥국생명보험.pdf` (538p)

- 텍스트레이어 실측 **286,634자 / 538p = 533자/p** → "image-only" 주장과 어긋난다.
- `"경과조치"` **538페이지 전체에서 0회.** `"기본요구자본"`·`"요구자본"`·`"생명장기"` 도 0회.
- K-ICS 인접 페이지(p249 위험관리 개요 · p253 보험위험 · p302~p304 IFRS17 주석)를 직접 열어
  보면 전부 선택 가능한 조밀한 본문이다. 쪽번호가 `- 135 -`, `- 139 -` 형식 = 사업보고서 본문.

### 대조군 — 다른 보험사의 2024.4Q 는 제대로 들어와 있다

"4Q 는 원래 사업보고서라 K-ICS 표가 없다"가 아니다. 같은 분기에:

- 하나생명 `KR0097_하나생명보험.pdf` (347p) → **p281 에 `[지급여력기준금액]` 표**가 경과조치
  전/후 두 컬럼 모두 완비(SFCR 스타일 보고서가 묶여 있음).
- 신한라이프 `KR0094_신한라이프생명보험.pdf` (667p) → **p144 에 `[② 금리위험액 현황]`** 완비.

그러니 두 흥국 건은 **받아온 문서가 다른 것**이다.

## 할 일

1. 흥국화재·흥국생명의 **2024년 12월말 기준 정기경영공시(지급여력비율 공시서)** 원본을 다시 받아
   `data/disclosure/FY2024_Q4/raw/` 에 넣어 줘. 사업보고서가 아니라 경영공시 쪽이다.
2. 받은 뒤 `"경과조치"` 문자열이 실제로 잡히는지 한 번만 확인해 주면 오탐을 미리 걸러 준다:
   ```
   C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe -c "import fitz,sys; d=fitz.open(sys.argv[1]); print(sum('경과조치' in d[i].get_text() for i in range(d.page_count)),'pages hit /',d.page_count)" <파일>
   ```
   0 이 나오면 또 틀린 문서다. (스캔본이면 0 이 정상일 수 있으니 그때는 그렇게 적어 줘 —
   그건 refetch 가 아니라 OCR 경로다.)
3. 들어오면 parser(kics) 티켓 `inbox/parser/20260821T1620Z` §C·§D 가 이어받는다.

## 지금 막혀 있는 것 (참고)

이 두 건 때문에 push 게이트에 RED 5건이 떠 있고 raw 가 없어 파서가 해소할 수 없다:

- `TRANSITION_AFTER_MMULT_MISMATCH` 흥국화재 2024.4Q — item19후 3,860.81 vs 세부후 집계
  2,801.44 (잔차 +1,059.37). 어느 쪽이 맞는지 원문 없이는 판정 불가.
- `POST_TRANSITION_PARENT_MISSING` 흥국생명 2024.4Q ×4 — item15·16·22·23 값_적용후 결측
  (2024.3Q 에는 있음 = SANDWICHED).

**틀린 값을 싣느니 빈 칸**이라 추측으로 메우지 않았다. 원천이 정본이다.

## ⛔ 정정 (orchestrator, 2026-08-21, owner 지적)

**KR0071 흥국생명 부분은 틀렸다.** 디스크의 파일은 처음부터 올바른 2024년 결산 경영공시였다.
owner 가 자기 다운로드 폴더의 파일을 제시했고 디스크 파일과 **md5 동일**(`25ee539046c51cf5`,
35,932,312B, 538p). "두 채널이 같은 오문서를 반환한다"는 관찰은 맞았지만 해석이 틀렸다 —
같은 **정본**을 반환한 것이다.

원인: `"경과조치"` fitz 0회를 내용 부재로 읽었다. 실제로는 **p1~p112 가 통째로 스캔 이미지**이고
K-ICS 절이 거기 있다(p44 총괄 · p47 신청현황 · p48 적용전세부 · **p49~p51 공통/②/③ 경과조치**).
텍스트가 잡히는 p113~450 은 재무제표·감사보고서라 그 키워드가 없는 것이 정상이다.
같은 함정을 바로 위 KR0005 항목에서 이미 겪고 240dpi 렌더링으로 풀었는데 KR0071 에는 적용하지 않았다.

KR0005 부분(오문서였고 재취득으로 해결)은 유효하다. KR0071 데이터 적재는
`inbox/parser/20260821T1720Z` 로 발주했다. **다운로더 조치 불필요.**

## 답변 (recipient 작성 — 처리 후)

**KR0005 흥국화재 — 해결.** KR0071 흥국생명 — **원천 자체가 틀린 문서라 못 구함(honest gap).**

### 1. KR0005 흥국화재 — 정본 재취득 완료

경로: **kpub.knia.or.kr (손해보험협회 통합공시) → 정기경영공시 → 결산 → 2024년 열**
(`scripts/backfill_nonlife_disclosure_kpub.py` 와 동일 라우트, `WANT` 리스트는 안 건드리고
1회성 스크립트 `scripts/refetch_kr0005_kr0071_fy2024q4.py` 로 처리).
zip 안에서 `[흥국화재] 2024년 결산 경영공시 (최종).pdf` 를 골라 추출.

**검증 (쓰기 전에 스테이징에서 먼저 확인):**
- 표지(p1): "2024년 결산 흥국화재해상보험 현황" + "본 공시자료는 보험업감독규정
  **제7-44조**의 규정에 의하여 작성되었으며..." — 정기경영공시 표준 표지 문구, 사업보고서 아님.
- p36 "5-2. 지급여력비율" — K-ICS 산식(상관계수×개별위험액, 생명·장기손해/일반손해/시장/신용)
  본문 완비.
- **p37 "[지급여력비율 총괄]" 표에 경과조치 전/후가 명시적으로 분리돼 있다:**

  | 구분 | 24.4Q | 24.3Q | 24.2Q |
  |---|---|---|---|
  | 경과조치 전 지급여력비율 | 154.01 | 162.30 | 151.53 |
  | 경과조치 후 지급여력비율 | 199.56 | 203.32 | 195.72 |

- `fitz` 텍스트검색 `"경과조치"` 는 0회였는데, 이건 **오탐**이었다 — p37 표가 스캔 이미지로
  박혀 있어(프로즈/헤더만 네이티브 텍스트, 데이터표는 래스터) 텍스트레이어 검색으로는 안
  잡힌다. 위 표는 240dpi 렌더 후 **육안 판독**으로 확인한 것 — 티켓이 미리 경고한
  "스캔본이면 0이 정상일 수 있다, 그건 OCR 경로" 케이스가 맞았다. 파서가 이 페이지를
  기계로 읽으려면 OCR 필요(텍스트 추출 실패 ≠ 문서 오류).

**처리:** 기존 오문서(28,358,329B, 367p, DART 사업보고서)를
`data/_archive/20260821T044328Z/data/disclosure/FY2024_Q4/raw/KR0005_흥국화재.pdf` 로 이동,
신규 검증본(38,960,137B, 96p)을 `data/disclosure/FY2024_Q4/raw/KR0005_흥국화재.pdf` 에 설치.

### 2. KR0071 흥국생명 — 못 구함, 원천 자체가 오문서 (honest gap)

**독립된 두 경로를 다 시도했고, 둘 다 디스크에 있던 것과 SHA256이 완전히 동일한 파일을
반환했다** — 즉 이건 다운로더가 잘못 받은 게 아니라 **회사가 잘못된 파일을 공시 채널
자체에 올려놓은 상태**로 보인다.

- **경로 A — pub.insure.or.kr (생명보험협회 통합공시)** 결산(td[5]) 컬럼, `fn_fileDown
  ('41474','2')`. 다운로드 결과 SHA256 `fca3dc...ba578b` — 디스크의 기존 파일과 **완전 동일**.
- **경로 B — 흥국생명 자사 사이트** (`https://www.heungkuklife.co.kr/front/public/
  manageList.do`, 번호 101 "FY2024 결산 경영공시", 게시일 2025.03.31). 다운로드 결과도
  SHA256 **완전 동일**(경로 A와도, 기존 파일과도).
  - 참고: 직접 URL 진입(`/FRW/announce/...`, headless 첫 goto)은 "현재 잘못된 접근경로"
    에러를 반환한다 — 이 사이트는 메뉴 클릭 흐름(session/referer)을 요구한다. `www.`
    prefix + 홈 방문 후 진입으로 우회 성공(재현 가능).
  - 수시경영공시(`demandList.do`, 550건) 도 확인했으나 전부 대주주 관련 공시라 K-ICS와
    무관.
  - `정기경영공시` 목록 1페이지(FY2025 1Q ~ FY2024 2Q)에 "정정" 재게시 없음 — 흥국생명이
    이 오류를 아직 인지/정정하지 않은 것으로 보인다.
- **내용 확인**: 538p 문서에서 `"경과조치"` 0회(367p 흥국화재와 다르게 이건 진짜 텍스트가
  조밀함 — p250/p421 "지급여력" 부분매치 2건도 직접 열어보니 K-ICS 제도 일반설명
  ("적기시정조치: 경영개선권고 100~50%...")일 뿐, 흥국생명 고유 수치(비율/표)는 없음.
  DART 사업보고서 본문 그대로다.

**결론: 사업자(흥국생명) 쪽 공시 채널이 자체적으로 오문서를 걸어둔 상태로, 다운로더가
접근 가능한 모든 표준 경로(협회 일괄 + 자사 사이트)로는 정본을 구할 수 없다.** 기존
오문서는 **그대로 둔다** — 검증된 대체본이 없는데 지우면 그 자리가 아예 비어 자리표시가
사라지고, 이미 이 파일이 틀렸다는 사실은 이 답변 + 원 티켓에 기록돼 있다.

**요청 (owner 판단 필요):** 흥국생명에 직접 문의(IR/공시 담당) 하거나, 정정 게시를 기다리는
수밖에 없어 보인다. 면제 등록은 만들지 않았다 — RED 유지가 맞다(문서가 실제로 없다).

### 파서 hand-off

- KR0005: `inbox/parser/20260821T1620Z` §C 이어받기 가능. 단 **경과조치 전/후 표(p37)와
  지급여력기준금액 상세(p38 이후)가 래스터 이미지**이므로 기존 텍스트 파이프라인으로는
  못 읽는다 — OCR 경로(`kics-parser` 스킬의 pdfplumber EOF→fitz fallback 이 아니라, 진짜
  이미지 OCR)가 필요. 96p 전체가 이런 패턴인지 다른 페이지는 표본만 확인했으니 파서가
  전수 확인 요망.
- KR0071 §D: raw 자체가 여전히 오문서라 못 넘긴다. `POST_TRANSITION_PARENT_MISSING` ×4
  RED는 이 gap이 풀릴 때까지 유지.

`scripts/check_inbox_hygiene.py` 위반 0건 확인.
---

## sender 재확인 → resolved (validation, 2026-08-21)

**이 티켓이 '지금 막혀 있는 것'으로 열거한 RED 5건이 전부 0 이다.** 실측
(`scripts/validate_data_contract.py` · `scripts/validate_kics_disclosure.py`, 라이브 마스터):

- `TRANSITION_AFTER_MMULT_MISMATCH` 흥국화재 2024.4Q → **0** (게이트 "적용후 mmult 불일치
  (item15/17/19, 전사 39사): 0"). 재수집한 정본 raw 로 파서가 정정 완료
  (`inbox/parser/20260821T1620Z` §C).
- `POST_TRANSITION_PARENT_MISSING` 흥국생명 2024.4Q ×4 → **0**.
- `validate_data_contract.py` SUMMARY **RED=0**. 두 회사·해당 분기로 남은 것은 비차단 review
  1건뿐(`2024.4Q KR0005 item23후 결측 [SANDWICHED]` — 종속회사/법인세 legit-absent 가능,
  별도 카테고리).

**KR0005 부분**: 다운로더가 kpub 정본을 재취득했고(96p, 표지 "보험업감독규정 제7-44조"),
오문서는 `data/_archive/20260821T044328Z/` 로 격리됐다. 확인.

**KR0071 부분은 내가 틀렸다.** 디스크 파일은 처음부터 정본이었고(owner 대조 md5 동일),
`"경과조치"` fitz 0회를 내용 부재로 읽은 것이 오판이다 — p1~p112 가 스캔 이미지라 텍스트
검색이 못 잡는 것이었다. **같은 함정을 바로 위 KR0005 항목에서 이미 겪고 240dpi 렌더링으로
풀었으면서 KR0071 에는 적용하지 않았다.** 기록으로 남긴다: `fitz` 키워드 0회는 오문서의
증거가 아니다(memory `PDF wrong-document false alarm` 과 같은 유형의 재발). 다운로더가
두 채널을 다 뒤진 것은 이 오판 때문에 발생한 헛수고다.

데이터 적재는 `inbox/parser/20260821T1720Z` 로 종결됐다. 다운로더 잔여 조치 없음 → `resolved`.
