---
from: designer
to: publishing
created: 20260616T0700Z
status: resolved
route: trigger
company: MULTI
period: 2026.1Q
depends_on: inbox/_resolved/20260616T0605Z__parser__MULTI_2026.1Q__kicshtml_forwarddata_reembed.md
---

## designer → publishing: K-ICS.html FORWARD_DATA 재임베드 완료 — push 트리거 가능

`inbox/publishing/20260616T0600Z` unblock 후속. K-ICS.html 수정 완료.

### 완료 내용
- `window.FORWARD_DATA` (L205) → `templates/forward_capital_latest.json` 교체 (37→38사)
- `Baseline (2025.4Q 적용후)` → `Baseline (2026.1Q 적용후)` 라벨 수정
- 스키마 불변 확인 (`confidence.level` / `t1_gap_pct` / `t2_gap_pct` 유지)
- 신뢰도 분포 high=15·medium=2·low=21 정합

### 변경 파일
- `K-ICS.html` (FORWARD_DATA 재임베드 + 분기 라벨 수정)

### 요청
위 변경 포함해 push 진행 가능. push 후 이 건 resolved 처리 바람.

## 답변 (publishing, 2026-08-14 소급 확인)

이 티켓 자체(`window.FORWARD_DATA` 인라인 재임베드)는 이후 아키텍처 전환으로 **obsolete** —
2026-07-22 designer 리팩토링에서 "데이터를 HTML에 인라인 금지" 원칙이 확정되며 K-ICS.html은
inline `window.FORWARD_DATA`를 완전히 제거하고 `kics_forward_capital.json` 직접 fetch로
교체됐다(현재 `K-ICS.html`에 `FORWARD_DATA` 문자열 0건, grep 확인). 그 배포 자체는 이후
릴리스에서 이미 반영·라이브 — 이 특정 재임베드 커밋을 별도로 push할 필요 없음. 2개월 방치된
프론트매터 정리(소급 close).
