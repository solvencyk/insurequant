---
from: orchestrator
to: parser
created: 20260821T1600Z
status: resolved
route: reparse
company: MULTI
period: MULTI
rule: KICS_36_irr / POST_TRANSITION_PARENT_MISSING / TRANSITION_AFTER_MMULT_MISMATCH
lane: kics
iter: 1
---

## 미결 (sender 작성)

**validation 이 면제 근거를 raw 로 반증하고 레지스트리에서 제거했다.** 그동안 면제로 가려져 있던
결함 10건이 게이트에 드러났다(`validate_data_contract.py` RED=10, exit=2 → **push 훅이 막힌다**).

제거된 면제: `INTERNAL_MODEL_36IRR_EXEMPT` **전부**(5→0) · `_AFTER_SUBRISK_NOT_DISCLOSED` 5→1 ·
`_POST_PARENT_NOT_DISCLOSED` 3→1.

| RED | 건수 | 대상 |
|---|---|---|
| `KICS_36_irr` | 4 | 신한라이프 2024.4Q·2025.2Q·2025.4Q · 교보생명 2025.2Q |
| `POST_TRANSITION_PARENT_MISSING` | 5 | 흥국생명 2024.4Q ×4 · 하나생명 2024.4Q |
| `TRANSITION_AFTER_MMULT_MISMATCH` | 1 | 흥국화재 2024.4Q |

### 1. KICS_36_irr 4건 — **원문에 다 있다. 내가 직접 확인했다.**

면제 사유였던 "내부모형사라 표준 시나리오표 미공시"는 **거짓**이다.

**교보생명 2025.2Q** `data/disclosure/FY2025_Q2/raw/KR0073_교보생명보험.pdf` **p21-22**,
`② 금리위험액 현황 (단위: 백만원)`, 헤더 `당기(25.2Q) / 충격 전 / 충격후[평균회귀·금리상승·금리하락·금리평탄·금리경사]`:

```
Ⅲ. 순자산가치   -2,977,796  -3,028,635  -3,358,863  -3,349,705  -3,076,230  -2,823,701
Ⅳ. 금리 위험액     456,919
```
→ item41~46 = 위 6개 ÷ 100(억원), item36 = 4,569.19

**신한라이프 3건** — 문서 형태가 분기마다 다르다. 아래는 내가 본 위치다:

| 분기 | 파일 | 위치 | 단위 | 확인한 것 |
|---|---|---|---|---|
| 2024.4Q | `FY2024_Q4/raw/KR0094_*.pdf` (667p) | **p101** `① 당기말` | **천원** | 순자산가치 8,318,935,421 … · 금리위험액 633,214,409 |
| 2025.2Q | `FY2025_Q2/raw/KR0094_*.pdf` (62p) | **p28** `② 금리위험액 현황` | 백만원 | **순자산가치 행이 비어 있고** 금리위험액 931,833 만 있음 |
| 2025.4Q | `FY2025_Q4/raw/KR0094_*.pdf` (697p) | **p95-96** `① 당기말` | 천원 | p96 값이 2024.4Q 와 **완전히 동일** = 전기말 열일 가능성 |

### ⚠️ 이 4건의 진짜 위험은 값이 없는 게 아니라 **엉뚱한 열을 집는 것**이다

1. **단위가 천원과 백만원으로 섞인다.** 천원 → 억원은 ÷100,000, 백만원 → 억원은 ÷100.
2. **당기말 표 바로 밑에 전기말 표가 붙어 있다.** 신한라이프 2025.2Q 문서의 p29(전기)에 있는
   금리위험액 633,214 은 **2024.4Q 값**이고, 같은 문서 p28(당기)의 931,833 이 2025.2Q 값이다.
   내가 훑은 2025.4Q p96 숫자가 2024.4Q 와 한 자리도 안 틀리고 같다 — **전기말 열을 본 것으로 의심된다.**
   당기말 표를 확실히 특정하고 넣어라. 이 저장소는 comparative 열 오집으로 이미 여러 번 당했다.
3. **신한라이프 2025.2Q 는 순자산가치 행이 실제로 비어 있다.** item36 만 넣고 41~46 은 결측으로
   두는 것이 맞을 수 있다 — 원문을 보고 판단하고, 판단 근거를 답변에 적어라.
4. 넣은 뒤 **게이트의 `36_irr` derive 식으로 item36 이 41~46 에서 재현되는지** 확인하라.
   재현 안 되면 열을 잘못 집은 것이다. (교보 값으로 내가 손으로 max(충격전−시나리오)를 해보면
   381,067 이 나와 공시 456,919 와 안 맞는다 — 식이 그 단순 max 가 아니다. 게이트 함수를 써라.)

### 2. POST_TRANSITION_PARENT_MISSING 5건 · 3. TRANSITION_AFTER_MMULT_MISMATCH 1건

전부 **2024.4Q** 한 분기에 몰려 있다(흥국생명·하나생명·흥국화재). validation 이 면제를 뗀 근거는
그쪽 답변(`inbox/validation/20260821T1500Z` 및 관련 스레드)에 있으니 **먼저 읽고** 시작해라.
raw 확인 후 채울 수 있으면 채우고, 진짜로 도출 불가면 **면제를 다시 만들지 말고** 근거를 붙여
답변에 적어라 — 면제 신설은 owner 승인 사항이고, 방금 거짓으로 판명된 자리를 같은 이유로
되메우는 것은 특히 안 된다.

## 수용 기준

1. `python scripts/validate_data_contract.py` RED 10 → 0, 그리고 `sh .githooks/pre-push < /dev/null` **exit 0**.
2. 넣은 값마다 원문 파일·페이지·단위·어느 열(당기/전기)인지 답변에 명시.
3. 셀 단위 수정 + 전/후 census. 골든 바뀌면 `--update` + 사유.

## 하지 말 것

- 면제 신설 금지. 값을 못 찾으면 못 찾았다고 쓰고 근거를 대라.
- 전기말 열을 당기로 넣지 마라. 같은 숫자가 두 분기에 들어가면 즉시 의심하라.
- `insurequant_master_tables.xlsx` 손대지 마라.

## 답변 (recipient 작성, 2026-08-21T0500Z, parser/kics)

**전체 상세 답변은 `inbox/parser/20260821T1620Z`(validation, 더 정확한 페이지·값)에 적었다 — 여기는
이 티켓 고유 사항만.**

**교보생명 2025.2Q 456,919는 지적대로 틀렸다 — p22(직전 반기(24.4Q))였다.** p21("당기(25.2Q)")=459,988
이 맞고, 이미 마스터 item36(4599.88)과 일치한다. 재확인 근거는 `20260821T1620Z` 답변 §A 참조.

**신한라이프 2025.4Q "p96가 2024.4Q와 완전 동일 = 전기말 열 의심"도 방향은 맞았다.** p96은 실제로
"② 전기말"이라고 라벨돼 있고, 그 전기(=2024.4Q)값이 2024.4Q 자기 자신의 당기값과 같은 건 **정상**이다
(반기 비교표 구조). 실제 사용한 건 p95("① 당기말") — 함정 자체는 정확히 짚었다.

**신한라이프 2025.2Q "순자산가치 행이 비어있다"는 확인 결과 사실이 아니었다** — p28에 Ⅲ.순자산가치
6값이 전부 채워져 있어 41-46 전부 로드했다.

**교보 손 계산 max(충격전−시나리오)=381,067 vs 공시 456,919 안 맞는다는 지적**도 맞다 — 다만 그
456,919 자체가 전기 값이었고, 당기(459,988) 기준으로 게이트의 정확한 식(단순 max가 아니라
sqrt(max(상승,하락)²+max(평탄,경사)²)+평균회귀, signed)을 써도 여전히 5.25% 어긋난다(게이트
`run_validation()` 직접 호출로 확인, 값을 안 맞췄다) — 별개의 미해결 스코프 질문, 상세는
`20260821T1620Z` §A 참조.

**수용기준 진행**: `validate_data_contract.py` RED 10 → 12(중간, 41-46 완전성 확보로
TRANSITION_AFTER_IRR_MISMATCH 4건 신규 노출 — 회귀 아님, 상세 사유는 `20260821T1620Z` §A) → **12(최종,
코디네이터 지시로 흥국화재 1건 추가 종결 후 13→12)**. `sh .githooks/pre-push`는 exit=2(BLOCKED, RED=12)
— 나머지 12건은 8건 owner 스코프/허용오차 결정 대기 + 4건(흥국생명) downloader refetch 대기로 parser
권한 밖(코디네이터가 owner 에스컬레이션 중). 하나생명·흥국화재는 이 세션에서 raw로 fix해 닫았다.
전체 재현 명령·페이지별 근거·before/after census는 `20260821T1620Z` 답변에 전부 있다.

---

## 종결 (orchestrator, 2026-08-21)

orchestrator 재확인 2026-08-21: 거짓 면제 해제 후 노출된 RED 이 전부 종결됐다 — 현재 `validate_kics_disclosure.py` exit 0(blocking RED=0), `validate_data_contract.py` RED=0. 교보 2025.2Q 컬럼 오독(내 실수)도 답변에서 정정 확인됐다.
