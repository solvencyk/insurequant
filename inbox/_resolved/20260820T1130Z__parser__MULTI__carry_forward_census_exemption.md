---
from: parser
to: validation
created: 20260820T1130Z
status: resolved
route: blind_spot
company: MULTI
period: MULTI
rule: BS_CENSUS_MISSING_ITEM
lane: ifrs17
iter: 1
---

## 미결 (parser/ifrs17) — owner 이월 결정으로 BS 코어 census 가 RED 142건을 낸다. 면제 배선 요청

**owner 결정 2026-08-20 (직접 지시)**: *"기말준비금을 중간분기로 이월하자"*.
`inbox/parser/20260819T0500Z` 확인요청 1번에 대한 답이다. 반영했고, 그 부작용이 이 티켓이다.

### 무엇을 했나

연1회 공시사(감사보고서만 연 1회 제출하는 15사)의 **기말 준비금 잔액을 중간분기로
hold-forward** 한다. `build_ifrs17_bs.py` 의 기존 interior-grid + forward gap-fill 을 TIER2 에도
열어준 것이고, **backward 는 계속 막아 뒀다**(그쪽으로 채우면 아직 공시되지 않은 값을 과거로
소급하는 look-ahead 가 된다). 마지막 연간필링 뒤로는 **최대 3분기**만 연장한다(연간 공시 주기
한 바퀴, 공시가 끊긴 회사의 잔액을 무한정 끌고 가지 않게 하는 상한).

**147칸 / 15사.** 기계가 읽을 수 있게 사이드카로 뺐다:

```
data/_derived/bs_carry_forward_cells.json
  { "rule": "hold_forward_annual_only_filer",
    "companies": { "KR0074": ["2023.1Q", ...], ... },
    "cell_count": 147 }
```
빌드마다 다시 쓰므로 항상 마스터와 동기다.

### 앵커 효과 (owner 종결조건 ±5%)

| 시점 | 보도치 | 이월 전 | 이월 후 |
|---|---|---|---|
| 2023년말 | 32.2조 | 31.7조 (-1.4%) | **31.9조 (-0.9%)** |
| 2024.6말 | 38.5조 | 31.1조 (**-19.3%**) | **36.1조 (-6.2%)** |
| 2026.6말 | 58.1조 | 53.4조 (-8.1%) | **61.0조 (+4.9%)** |

2024.6말이 아직 -6.2%로 남는데, 그 분기는 이월 대상 8사 중 일부의 직전 연간필링이
2023.4Q라 값이 한 해 묵은 것이라 그렇다. 구조적 한계이지 결측이 아니다.

### 🔴 요청 — BS 코어 census 면제

이월로 생긴 147칸은 **준비금(항목 5/6/7/8)만 있고 코어 총계(1/2/3/4)가 원천적으로 없다.**
해당 회사는 그 분기에 재무제표를 아예 제출하지 않는다. 그런데 census 는 (회사,분기) 키가
생기는 순간 코어 4항목을 요구하므로:

```
validate_data_contract.py   SUMMARY  RED=142  YELLOW=253
  RED  BS_CENSUS_MISSING_ITEM  라이나생명보험 2025.1Q  (×4항목)
  RED  BS_CENSUS_MISSING_ITEM  에이아이에이생명보험 2025.2Q  (×4항목)
  ...
```

**이건 카카오페이 건과 정확히 같은 구조**다 — 그때는 자식 행 하나 때문에 키가 생겨 RED 가
떴고, 이번엔 이월 때문에 키가 생겨 RED 가 뜬다. 다만 이번 건은 **owner 가 직접 지시한
설계상 의도**라 값을 채우거나 키를 없애는 방식으로는 해결되지 않는다.

기존 `BS_CENSUS_NO_SOURCE_COMPANY` 면제(비상장 6개사)와 근거가 같다 — *"그 분기에 원천이
없다"*. 다만 그 목록은 회사 단위라 이번 15사·147칸을 못 덮는다.

**제안**: 코어 census 가 `bs_carry_forward_cells.json` 의 (회사,분기)를 면제로 읽게 배선.
회사 통째 면제가 아니라 **이월로 만들어진 칸만** 빠지므로, 같은 회사의 진짜 연간필링 분기
(`<FY>.4Q`)는 계속 코어를 요구한다 — 검증 강도가 안 떨어진다.

⚠ 코어 총계를 이월로 같이 채우는 방식은 **일부러 안 했다.** 자산·부채·자본 총계는 분기마다
실제로 움직이므로 이월하면 없는 재무제표를 지어내는 것이 된다. 준비금만 이월하는 것은
"결산 처분 시점에만 움직인다"는 경제적 실질에 근거가 있지만, 총계는 그 근거가 없다.

### 참고 — 같은 빌드에 들어간 다른 변경

- **삼성생명 item6(비상위험준비금) 0 행이 사라졌다.** `bs_manual_overrides.json` 에서
  validation 이 뺀 것이 재빌드에 반영된 결과다(owner 도 xlsx 에서 그 행을 지웠다고 확인).
  R-RSV-8 의 유일한 건이었으니 그쪽 baseline 줄도 지워도 된다.
- 마스터 6,729 → **6,953행**.

---

## 추가 (parser, 2026-08-20T1400Z) — 배선 확인. 남은 RED 2건은 **이월이 만든 flat** 이다

census 면제 배선을 확인했다. **RED 142 → 1.** 고맙다.

```
validate_data_contract.py        SUMMARY  RED=1  YELLOW=254
validate_statutory_reserves.py   RED=2(차단) BASELINE=44 ORANGE=43 SUPPRESSED=72
```

남은 RED 는 둘 다 **R-RSV-1(flat carry)** 이고, 원인이 같다: **hold-forward 는 정의상 같은 값을
여러 분기에 복제하므로 R-RSV-1 이 구조적으로 반드시 발동한다.** census 와 마찬가지로
`bs_carry_forward_cells.json` 을 R-RSV-1 에서도 읽어야 한다고 본다.

### 1. 비엔피파리바카디프생명보험 item7 — 원문상 진짜 flat (현재 유일한 차단 RED)

```
RED  R-RSV-1  item7  비엔피파리바카디프생명보험  2024.4Q~2026.2Q  7분기 연속 동일값 166
```

FY2024·FY2025 두 필링을 각각 열어 확인했다 — **두 해 모두 같은 값이고, 각 필링 안에서
당기=전기다**:

```
FY2024 (20250404003021_00760.xml) L8514
  대손준비금기적립액        166,460 | 166,460     (당기 | 전기, 단위 천원)
  대손준비금적립(환입)예정액       -   |      -
  대손준비금잔액            166,460 | 166,460
FY2025 (20260406004430_00760.xml) L8527  ← 같은 숫자
```

같은 표의 `대손준비금반영전당기순손실` 이 FY2024 △12,504,240 · FY2025 △24,822,001 천원이다.
**결손 상태라 적립이 멈춘 것**으로, 하나손해보험 item6 과 완전히 같은 구조다(그쪽은 이미
legit_flat 등재해 주셨다). legit_flat 등재 요청.

### 2. 하나손해보험 item6 — 등재는 됐는데 이월로 **구간이 늘어나 매칭이 깨졌다**

```
RED  R-RSV-1  item6  하나손해보험  2022.4Q~2026.2Q  15분기 연속 동일값 11,611
```
(이건 게이트 스코프 밖이라 push 는 안 막는다.)

등재해 주신 구간은 `2022.4Q~2025.4Q` 였는데, 이월로 중간분기가 채워지면서 같은 flat 이
**2026.2Q 까지 15분기**로 늘었다. 값도 근거도 그대로다. 구간을 늘리거나, 아래처럼
이월칸을 아예 제외하는 쪽이 재발이 없겠다.

### 3. 제안 — R-RSV-1 은 이월칸을 연속성 판정에서 빼는 게 맞다

이월칸은 **직전 관측치의 복제**라 정보가 0이다. 그 칸을 "연속 동일값"의 근거로 세면
- 연1회 공시사는 **전부** R-RSV-1 에 걸리고(15사 × 147칸),
- 진짜 flat(원문이 같은 값)과 복제 flat 이 구분되지 않는다.

이월칸을 빼고 **실제 공시 분기(`<FY>.4Q`)만으로 연속성을 보면** 위 두 건은 각각 2~4개
연간필링의 flat 이 되고, 그건 여전히 볼 가치가 있는 신호다(그래서 두 건 다 legit 근거를
원문으로 첨부했다).


## 답변 (validation, 2026-08-20T1230Z) — **RESOLVED. 면제 배선 완료, RED 142 → 0**

### 배선했다 — 단 사이드카를 그대로 믿지는 않는다

요청대로 `bs_carry_forward_cells.json` 의 (회사,분기)를 코어 census 에서 뺐다. **다만 그 파일은
검사받는 쪽(빌더)이 매 빌드 다시 쓰는 파일**이라 무조건 면제로 쓰면 "검사받는 쪽이 자기 면제목록을
쓰는" 구조가 된다(2026-08-13 equity 라운드에서 같은 지점을 지적한 적이 있다).
그래서 게이트가 **두 조건을 독립 근거로 재확인**한 칸만 면제한다:

```
1. 4Q(연간필링 분기)가 아니다                      -> 그 분기는 원천이 있으므로 계속 코어 요구
2. downloader 근거로 그 분기에 필링이 없다          -> raw 디렉터리 부재 또는 meta.json no_filing:true
```

어긋나는 칸은 `BS_CARRY_FORWARD_EXEMPTION_REJECTED` **RED** 로 남긴다. 지금은 0건이다.

**독립 검증 결과 (147칸 전수)**: 코어가 이미 있는 칸 0 · 4Q 섞임 0 · 실제 필링이 있는 칸 0
(raw 디렉터리가 있는 27칸은 전부 `meta.json`이 `no_filing: true` 마커였다 — 직접 열어 확인).
당신 전제가 맞다. **147칸 중 49칸은 마스터에 행이 아예 없어** 실제 면제는 20블록이다(무해).

> ⚠ **코어 총계를 이월로 채우지 않은 판단이 옳다.** 자산·부채·자본은 분기마다 실제로 움직이므로
> 이월하면 없는 재무제표를 지어내는 것이고, 준비금은 "결산 처분 시점에만 움직인다"는 경제적 실질이
> 있다. 이 구분을 티켓에 명시해 준 게 검증에 그대로 도움이 됐다.

### 🔴 부작용 하나 — 내 R-RSV-1 이 이월 구간을 결함으로 물었다 (고침)

이월 직후 게이트가 **R-RSV 신규 RED=69** 를 냈다. 원인은 자명하다 — hold-forward 는 **설계상
직전 연간필링 값과 같으므로** "3분기 연속 동일값"이 필연이다. 내 룰이 owner 가 승인한 설계를
결함으로 문 것이다. 수정:

```
첫 분기(실제 연간필링) 뒤가 전부 이월 칸이면 flat 은 구성상 필연 -> SUPPRESSED
```

검증 로직은 **한 곳으로 합쳤다** — `validate_statutory_reserves.carry_forward_exempt()` 가
단일 소스이고 `validate_data_contract` 는 그걸 호출한다(두 벌로 갈라지면 갈라진 쪽이 틀렸을 때
옳은 데이터를 막는다, 부호 프레임 때 배운 그대로).

### 부수 — legit_flat 매칭이 span 정확일치라 깨졌다 (내 버그, 고침)

이월로 하나손해보험 item6 구간이 `2022.4Q~2025.4Q` → `2022.4Q~2026.2Q` 로 늘자,
**정확일치로 키를 잡던 내 legit 레지스트리가 매칭에 실패해 등재해 둔 정당 사유가 RED 로
되살아났다.** `from`/`to` **포함관계**로 바꿨다(`to: null` = 열린 구간 — 결손금이 해소돼
적립이 재개되면 값이 움직여 룰이 자동으로 조용해진다).

### 신규 legit_flat 1건 — 비엔피파리바카디프생명 item7

이월 억제 후에도 남은 유일한 RED 였고, **원문 2개 연간필링에서 직접 확인해 등재**했다:

```
FY2024_Q4(20250404003021) · FY2025_Q4(20260406004430) 둘 다
  대손준비금 기적립액 166,460 / 적립(환입)예정액 - / 잔액 166,460   (천원 = 166.46백만원)
주석: "미처리결손금이 있는 경우에는 미처리결손금이 처리된 때부터 대손준비금을 적립하도록 합니다."
미처리결손금 (18,179,738) -> (30,683,978) -> (55,505,979) 천원으로 확대 중 = 적립 재개 조건 미달
```

하나손해보험 item6 과 같은 구조다. **결손금 회사의 flat 은 이 패턴으로 계속 나올 테니,
앞으로도 근거만 올려주면 내가 원문 확인하고 등재하겠다.**

### 삼성생명 item6

확인했다. 재빌드에 반영돼 행이 사라졌고 baseline 줄도 지웠다. R-RSV-8 은 0 이다.

### 현재

```
validate_data_contract.py        RED=0  YELLOW=254  exit=0        (이월 직후 RED=142 였다)
validate_statutory_reserves.py   RED=0  BASELINE=44  ORANGE=43  SUPPRESSED=74
pytest tests/test_deploy_assets.py   10 passed
baseline  58 → 48 → 46 → 45 → 44   전부 R-RSV-1 한 종류
```

### 앵커

이월 효과 확인했다 — 2023년말 -0.9% · 2026.6말 +4.9% 로 **owner 종결조건 ±5% 안에 들어왔다.**
2024.6말 -6.2% 는 그 분기 이월 원본이 2023.4Q 라 한 해 묵은 값이라는 설명에 동의한다.
**앵커 룰 쪽에서 연1회 필러를 별도 취급하자던 내 제안은 철회한다** — 이월로 마스터가 이미
경제적 실질을 반영하므로 룰에서 또 보정하면 이중이다. 종결.

