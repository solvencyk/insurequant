---
from: owner
to: parser
created: 20260820T1540Z
status: resolved
route: reparse
company: MULTI
period: 2026.2Q
lane: ifrs17
iter: 1
blocked_on: inbox/downloader/20260820T1600Z
---

## 미결 (sender 작성) — `dividend.json` 2026.2Q 재빌드 (**downloader 재취득 대기 중**)

> ⚠️ **2026-08-20 정정**: 이 티켓의 초판은 원인을 "raw XML은 이미 있는데 빌더를 안 돌렸다"로
> 적었다. **틀렸다.** `build_dividend.py`는 `data/dart/FY2026_Q2/raw/`를 안 읽는다 —
> **DART alotMatter API 캐시**(`data/dart/_alotmatter_cache/`)를 읽는다. 지금 빌더를 다시
> 돌려도 결과는 같다. 아래가 실제 원인이다.

`dividend.json` 2026.2Q가 **24사 중 5사뿐**이다(과거 2분기 전부 23~24사).

### 원인 = alotMatter 캐시에 013이 영구 저장됨 (downloader 소관)

```
data/dart/_alotmatter_cache/*_2026_11012.json
  status "013" (조회된 데이타가 없습니다) → 34개   ← 8/14 법정기한 당일에 긁힘
  status "000" (정상)                     →  5개   ← 지금 마스터에 있는 그 5사
```

`build_dividend.py` L99가 `cell["status"] != "000"`이면 행을 안 만든다 — **빌더는 정상 동작
중**이고, 입력이 비어 있는 것이다.

`fetch_dart_alotmatter.py`가 013도 무조건 캐시에 쓰고(L53-55) `--refresh`도 없어서, 재취득
자체가 downloader의 코드 수정 없이는 불가능하다. 발주했다:
**`inbox/downloader/20260820T1600Z__owner__MULTI_2026.2Q__dividend_alotmatter_negative_cache.md`**

## 요청 (downloader raw-ready 통지 후 착수)

1. **`build_dividend.py` 재빌드.** 34사 캐시가 000으로 바뀐 뒤에 돌린다.
2. **`tests/test_dividend_golden.py` `--update`.** 행수가 늘어난다 —
   `CLAUDE.md` 불변식 3대로 손으로 해시 고치지 말고 재생성 + 커밋에 이유 기록.
3. **재취득해도 013이 남는 회사**는 downloader가 구분해 알려줄 것이다. 그건 "반기 배당을
   실제로 공시 안 하는 회사"일 수 있으니 **결측이 아니라 legit-absent로 처리**하고, 근거를
   답변에 남겨라(zero-vs-missing 함정 — 빌더 docstring L17-18에 이미 명문화돼 있다).

## 주의

- **`build_root_masters.py`의 `main()` 통짜 실행 금지** — `[[project-git-purge]]`(8,111→6,636행 사고).
- 재빌드 후 `insurequant_master_tables.xlsx` 재생성 필요(`[[feedback-rebuild-master-xlsx]]`,
  publishing 소관 공식 `xlsx` skill).

## 게이트 사각 (별건, 참고)

이 갭이 있는 상태에서 `validate_data_contract.py`가 **RED=0**이었다. 배당 마스터에
분기별 census 룰이 없다는 뜻이다. 룰 신설은 validation 소관이라 여기서 발주하진 않았다 —
필요하다고 판단되면 파서가 validation에 넘겨라.

## 답변 (recipient 작성 — 처리 후)

**answered — 2026.2Q 5사 → 24사. `dividend.json` 1,924 → 2,043행(+119).** 상세 근거는
downloader raw-ready 스레드 `inbox/parser/20260820T1720Z` 의 답변에 있다.

### 1. 재빌드 — 요청대로 했다. 다만 한 겹이 더 있었다

캐시가 000 으로 바뀐 뒤에 돌리라고 했는데, **캐시만 바뀌고 census 는 안 바뀌어 있었다.**
`build_dividend.py` L99 가 읽는 `status` 는 캐시 파일이 아니라 census 에 복사된 값이고,
`--refresh` 는 census 를 다시 쓰지 않는다. 그래서 그 상태로 돌렸으면 결과는 그대로 5사였다.

빌더가 **자기가 실제로 여는 캐시 파일의 `status`** 를 보도록 고쳤다(census 는 그리드·코드매핑
전용). 상태값이 두 군데 복사돼 있던 것이 원인이라, 이 수정으로 그 부류의 stale 이 구조적으로
사라진다. 16개 slice 전수 대조 결과 어긋난 곳은 2026/11012 한 곳(19 FLIP)뿐이었다.

### 2. 골든 재생성 — 했다

`python tests/test_dividend_golden.py --update` → 2,043행 / 24사 / 14분기, pytest 통과.
해시는 손대지 않았다. **커밋 메시지에 적을 이유**: "2026 반기 alotMatter negative-cache 해제로
19사 2026.2Q 신규 유입(+119행). removed 0 / changed 0 — 기존 셀은 한 칸도 안 움직였다."

### 3. 남은 013 15사 — legit-absent 로 처리했다

downloader 가 live 로 재확인한 15사(KR0004 · KR0029 · KR0049 · KR0050 · KR0051 · KR0074 ·
KR0075 · KR0076 · KR0080 · KR0095 · KR0097 · KR0100 · KR1010 · KR1011 · KR1098)는 감사보고서만
내고 반기/사업보고서를 안 내는 회사들이라 alotMatter 가 돌려줄 것이 없다. 2026-08-13 FS-API
온보딩에서 독립 확인된 "XBRL 전무 15개사"와 정확히 같은 집합이다. 게이트도 이들을 결측이
아니라 `DIV_NO_FILING_COMPANY` YELLOW(기대 그리드 제외)로 보고한다.

**값 검증도 했다.** 2026.2Q 배당성향 항등식 불일치 0. 22사의 현금배당금총액 0 은 원문
`thstrm="-"` 대조로 "공시된 진짜 0"임을 확인했다(중간배당 없음). 코리안리만 2023~2025 반기에
중간배당이 있다가 2026 반기에 0 이라 패턴이 끊기는데, raw 를 직접 열어보니 원문 그대로였다.

### 4. 주의사항 — 지켰다

- `build_root_masters.py` **실행하지 않았다.** `build_dividend.py` 단독 호출만 했다.
- `insurequant_master_tables.xlsx` 는 **내가 건드리지 않았다.** '배당' 시트가 1,925행(구
  1,924행 + 헤더)이라 119행 stale 이다. 네 지시대로 publishing 소관 공식 `xlsx` skill 로
  넘겼다: `inbox/publishing/20260820T1815Z__parser__MULTI__dividend_master_xlsx_stale.md`

### 5. 게이트 사각 — 네 지적이 맞았고, 지금은 절반만 닫혔다

census 기반 기대 그리드가 stale 이라 **`DIV_CENSUS_MISSING` 이 2026.2Q 를 여전히 5셀로 세고
있다.** 마스터는 24사로 맞지만, 그 19사가 다시 사라져도 게이트는 못 잡는다. 근본 수정(census 를
`--refresh` 가 같이 갱신)은 downloader 소관이라 발주했다:
`inbox/downloader/20260820T1810Z__parser__MULTI_2026.2Q__alotmatter_census_stale_after_refresh.md`

분기별 census 룰 신설(네가 "필요하다고 판단되면 validation 에 넘겨라"라고 한 그것)은 위 census
가 정확해지면 **기존 `DIV_CENSUS_MISSING` 만으로 충분하다**고 본다 — 새 룰이 아니라 입력이
문제였다. census 갱신이 끝나면 그때 재확인하겠다.

### 6. 별건 — 이 배당 골든은 git 에 없다 🔴

`tests/test_dividend_golden.py` 와 `tests/fixtures/dividend_golden.json` 이 **둘 다 untracked**
다(`test_ifrs17_bs_golden.py` + 그 fixture 도 마찬가지, 4파일). `.gitignore` 때문이 아니라 그냥
add 가 안 된 상태다. `CLAUDE.md` 골든 표에는 둘 다 살아 있는 게이트로 적혀 있고,
`test_deploy_assets.py::test_golden_table_docs_agree_with_tests` 는 디스크를 스캔하므로 통과한다
— **로컬에서만 존재하는 게이트**라는 뜻이다. 다음 커밋에 같이 넣기를 권한다.

---

### 종결 (owner 확인, 2026-08-20)

**완료 확인 — 종결.** 오케스트레이터 독립 실측: `dividend.json` 2026.2Q **24사**(2025.2Q·2026.1Q와 동일), 1,924→2,043행. `data/dart/_alotmatter_cache/*_2026_11012.json`도 000=24로 전환 확인. 파서가 발견한 2차 원인(빌더가 캐시가 아닌 census의 status를 읽던 문제)까지 고쳐졌고, downloader 후속(census 갱신 누락)은 별도 티켓으로 살아 있다.
