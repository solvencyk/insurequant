---
from: owner
to: parser
created: 20260814T0035Z
status: resolved
route: backlog
company: MULTI
period: 2023.1Q-2026.1Q
lane: ifrs17
iter: 1
---

## 미결 (sender 작성)

**Tier-2 중단. 착수하지 말 것.** 이전 발주(`20260813T0422Z`) 답변에 "남은 일(다음 세션):
Tier-2(15개사 본문 XML)"로 잡혀 있는데, **그 작업은 취소한다.**

### 이유

owner 원래 요구:

> high level의 17BS (자산/부채/자본/AOCI) 를 빠르게 OpenDart API로 가져오고,
> 가능하면 해약환급금준비금 정도까지 긁어보라고 (**안되면 pass**)

**"빠르게 OpenDART API로"** 가 조건이었다. 15개사 본문 XML 주석을 파싱하는 Tier-2는
그 반대편이다. 해약환급금준비금도 "안 되면 pass"였는데 이전 발주가 Tier-2 필수 항목으로
격상시켰다 — **오케스트레이터 발주 오류다. 파서 잘못 아니다.**

### 확정

- **Tier-1(FS API)만으로 종결.** 24개사 × 11분기가 최종 범위.
- XBRL FS 없는 15개사, 2023.1Q/2Q, KR0150 2023-2024 = **영구 결측 확정.** 채우지 말 것.
- 해약환급금준비금(항목 10/11)은 **BS 주기행에 있는 회사만.** 없으면 공란. 주석·이익잉여금
  처분계산서까지 파고들지 말 것.
- validation에도 census 코어 축소 발주 넣었다(`inbox/validation/20260814T0035Z__...`) —
  항목 10 결측 RED 160건이 그쪽에서 내려간다. 그 RED 때문에 재추출하지 말 것.

### 이전 답변에서 넘긴 결정 2건 — 여기서 종결

1. **항목 8(비지배지분) 신설** = 이미 마스터에 들어가 있음(32행) → 그대로 유지. 추가 작업 없음.
2. **KR0069 CFS 자산총계 2025.2Q/3Q 동일값 반복** = 원본 캐시(DART) 품질 이슈로 확인됨 →
   우리쪽 수정 대상 아님. provenance에 기록만 하고 종결.

### 이미 만든 것은 그대로 둔다

`equity_composition.json`(6,994행 / 36개 항목) · `build_equity_composition.py` ·
`tests/test_equity_composition_golden.py` 전부 유지. 같은 API 응답에 딸려온 항목이라
지우는 게 오히려 일이다. **롤백하지 말 것.**

### 하지 말 것

- Tier-2 착수. 본문 XML 접근. 별첨 페치.
- 결측 채우기. 재추출. 골든 재생성.
- 이 메시지를 계기로 새 작업 만들기. **줄이는 작업이다.**

## 답변 (recipient 작성)

**2026-08-14 이 지시는 owner의 후속 결정(`20260814T0232Z`)으로 뒤집혔다 — 기록만 남긴다.**
`IFRS17_BS.json`으로 통합하면서 owner가 "Tier-2(본문 XML) 아카이브하지 말 것 — `build_ifrs17_bs.py`가
`TIER2`/`parse_filing`을 문다"고 명시적으로 되살렸다. 15개사 본문 XML 파싱은 지금
`IFRS17_BS.json`의 Tier-2 경로로 살아 있고 계속 값을 채우는 중이다(예: 아이엠라이프·AIA생명
AOCI 버그를 이번 세션에 그 경로에서 고쳤다). "빠르게 API로"라는 원 취지 자체는 유지 —
Tier-1(FS API)이 여전히 1순위고 Tier-2는 FS API가 없는 회사에만 보조로 쓰인다. 상세는
`20260814T0232Z` 답변.
