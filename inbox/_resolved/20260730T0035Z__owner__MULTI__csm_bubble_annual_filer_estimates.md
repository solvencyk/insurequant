---
from: owner
to: designer
created: 20260730T0035Z
status: resolved
route: backlog
company: MULTI
period: 2026.1Q
iter: 1
---

## 미결 (sender 작성)

index.html 아래 **IFRS17 — CSM Bubble Map**에서 연1회(회기) 공시사가 통째로 빠진다.
owner가 라이브에서 IM라이프·IBK연금보험 누락을 지적했다. 원인은 데이터 오류가 아니라
**공시주기 시차** — 소형사는 반기/회기 단위로만 IFRS17을 공시하므로 2026.1Q 행이 없다.

owner 결정: **버블맵에 추정 버블 추가 + 버블맵 아래 전 35사 보조표 신설** (둘 다).
추정치는 실공시와 시각적으로 구분(회색)한다.

### 원인 (확정)

`buildBubbleData()` ([index.html:983-986](../../index.html:983))가 2026.1Q 행을 **고정 요구**한다:

```js
const TARGET = qkey('2026.1Q');
const r = nbByCode[code][TARGET];
if (!r || r['신계약CSM배수_연누계'] == null || r['신계약CSM_연누계'] == null) continue;
```

→ 표시 22사 / 누락 13사. 기말 CSM(크기)은 이미 회사별 최신 분기를 쓰므로 수정 불필요.

### 누락 13사 census (측정 완료 — 재조사 불필요)

**Group A — 배수 carry 가능 10사** (직전 회기 `신계약CSM_연누계`·`신계약CSM배수_연누계` 둘 다 존재):

| code | 회사 | 직전 회기 | FY 신계약CSM(억) | ÷4 추정(억) | 배수(×) | 기말CSM(억) |
|---|---|---|---|---|---|---|
| KR0029 | AIG손해보험 | 2024.4Q | 443.8 | 111.0 | 8.74 | 928 |
| KR0049 | 악사손해보험 | 2025.4Q | 505.1 | 126.3 | 11.19 | 1,762 |
| KR0050 | 하나손해보험 | 2025.4Q | 1,506.2 | 376.6 | 14.08 | 2,802 |
| KR0074 | 라이나생명보험 | 2025.4Q | 4,618.4 | 1,154.6 | 11.54 | 23,761 |
| KR0076 | 아이엠라이프생명보험 | 2025.4Q | 1,599.8 | 399.9 | 8.82 | 7,614 |
| KR0095 | 메트라이프생명보험 | 2025.4Q | 4,951.0 | 1,237.8 | 8.38 | 26,082 |
| KR0097 | 하나생명보험 | 2024.4Q | 3,240.3 | 810.1 | 9.24 | 4,390 |
| KR0100 | 처브라이프생명보험 | 2025.4Q | 61.1 | 15.3 | 2.40 | 870 |
| KR1010 | 교보라이프플래닛생명보험 | 2025.4Q | 20.0 | 5.0 | 2.02 | 277 |
| KR1098 | 카카오페이손해보험 | 2024.4Q | 18.1 | 4.5 | 12.82 | 3,412 |

**Group B — 배수 구조적 부재 3사** (`신계약CSM배수_연누계 = null`, 월납초회보험료 0/미공시):

| code | 회사 | 신계약CSM 출처 | 기말CSM(억) | 비고 |
|---|---|---|---|---|
| KR1011 | IBK연금보험 | CSM_waterfall 항목2 = 717.7 (2025.4Q) | 5,204 | NB_CSM_multiple.json에 **행 자체가 없음** |
| KR1000 | 코리안리재보험 | NB 2026.1Q 실공시 = 537.3 | 9,968 | 재보험사 — 월납초회보험료 0 |
| KR0075 | 비엔피파리바카디프생명보험 | 12,846.5 | 299,584 | **단위오류 — 아래 참조** |

Group B는 Y축(배수) 값이 없어 **버블 배치가 불가능**하다. 보조표에서만 노출한다.

### ⚠ 선행 블로커 — KR0075 값을 표에 그대로 내지 마라

BNP파리바카디프 기말 CSM 299,584억(30조)은 **100배 단위오류**다(삼성생명 13.6조의 2배,
같은 사 K-ICS 지급여력금액은 1,958억. CSM/지급여력금액 = 153배, 차순위 3.49배).
`inbox/parser/20260730T0035Z__owner__KR0075_FY2024-FY2025__bnp_cardif_csm_100x_unit.md`로
정정 발주했다. **정정 전까지 보조표 KR0075 행은 수치 대신 `검증중` 표기**로 두고,
버블맵에는 넣지 않는다(어차피 배수 null).

같은 이유로 헤드라인 KPI `업계 총 기말 CSM`이 163.8조로 표시된다(정상 133.8조).
KPI는 데이터 정정으로 자동 해소되므로 designer가 손대지 않는다.

---

## 작업 A — 버블맵 추정 fallback (Group A 10사)

`buildBubbleData()` 수정:

1. 2026.1Q 행이 없거나 두 값 중 하나가 null이면 → 같은 code에서 **`신계약CSM_연누계`·`신계약CSM배수_연누계`가 둘 다 non-null인 가장 최신 회기(4Q) 행**을 fallback으로 채택. 그것도 없으면 종전대로 skip.
2. fallback 채택 시:
   - `nbCsm = r['신계약CSM_연누계'] / 4` — FY 연누계 → 1개 분기 균등 추정
   - `multiple = r['신계약CSM배수_연누계']` — **그대로 이월(÷4 하지 마라)**. 배수는 신계약CSM/월납초회보험료 비율이라 분자·분모가 같이 1/4로 줄어 스케일 불변이다.
   - `estimated = true`, `srcQ = <fallback 분기>`, `period = srcQ`
3. 기말 CSM(`closing`/`closingK`) 로직은 **변경 금지** — 이미 회사별 최신 잔액(직전값)을 쓴다.

시각 규칙:

- 추정 버블은 **별도 series 1개**('추정 (연1회 공시)')로 분리. 섹터 필터는 기존 rows 필터에서 이미 처리되므로 series를 섹터×추정 4개로 쪼개지 마라.
- `itemStyle`: `color:'#adb5bd'`, `opacity:0.45`, `borderColor:'#868e96'`, `borderType:'dashed'`, `borderWidth:1`
- 라벨: `shortName(name) + '*'`, `color:'#868e96'`
- legend에 '추정 (연1회 공시)' 항목 추가 (섹터 필터 ALL일 때만 표시하는 기존 조건과 별개로 항상 노출)
- tooltip (추정행일 때):
  - `신계약 CSM 규모: 400억 (2025.4Q 연누계 ÷ 4 추정)`
  - `NB CSM 배수: 8.82× (2025.4Q 연누계 이월)`
  - `기말 CSM: 7,614억 (2025.4Q)`
  - 마지막 줄: `⚠ 2026.1Q 미공시 — 연1회 공시사`
- `#bubble-meta`: `표시 32社 (실공시 22 · 추정 10) · 기준 2026.1Q · 크기=최신 기말 잔액`
- `.section-desc` ([index.html:155](../../index.html:155))에 추정 산식 한 줄 추가.
- 모바일 `#bubble-list`는 이미 회사별 최신 분기를 쓰므로 데이터 로직 수정 불필요 — **행에 '추정' 칩 + 회색 텍스트만** 추가(기존 기준분기 칩과 중복되지 않게 정리).

## 작업 B — 보조표 신설 (전 35사)

버블맵 섹션 아래 새 카드. 제목 `IFRS17 — CSM 수록 현황 (전 35사)`.

- 컬럼: 회사 / 구분 / 기말 CSM(억) / 기말 기준분기 / 신계약 CSM(억, 연누계) / 신계약 기준분기 / NB 배수(×) / 비고
- 정렬: 생보 → 손보, 각 그룹 내 기말 CSM desc
- 행 스타일: 2026.1Q 실공시 = 기본, 추정/이월 = `var(--muted)` 텍스트 + 좌측 회색 보더
- 비고 칩: `실공시` / `추정 (FY÷4)` / `직전값 이월` / `배수 미산출`
- Group B 3사: 배수 칸 `—` + 비고 `배수 미산출 (월납초회보험료 0/미공시)`. 나머지 수치는 표시. **KR0075만 수치 대신 `검증중`**.
- 데이터는 **이미 fetch한 `NB_CSM_multiple.json` + `CSM_waterfall.json` 재사용**. 추가 fetch·데이터 인라인 금지.
- 음수는 기존 `samoNum()` 재사용 → △ 표기(한국 회계 관행).
- 모바일: 표를 `overflow-x:auto` 컨테이너에 넣어 **body가 가로 스크롤되지 않게** 한다.
- A11y: `<table>` + `<caption>` + `scope="col"`. 추정 여부를 **회색만으로 구분하지 말고 텍스트 칩 병기**(색맹 대응 — a11y-audit baseline).

## 하지 말 것

- **마스터 JSON에 추정 행 주입 금지.** `NB_CSM_multiple.json`·`CSM_waterfall.json`은 실공시만 담는다(provenance/as-of 계약). 추정은 표시층 전용.
- **버블맵 3축 정의 변경 금지** — X 신계약CSM · Y NB배수 · 크기 기말CSM은 완결 확정 사양이다.
- 헤드라인 KPI 계산식 수정 금지 (데이터 정정으로 해소).

## 검증

- 로컬 preview: 버블 32개(회색 10개 라벨 `*`) · 보조표 35행 · 모바일 375px에서 가로 스크롤 없음
- `pytest tests/test_deploy_assets.py` (keep-list·인라인금지·BOM)
- 라이브 반영은 main 배포 → owner 승인 후 격리 워크트리 cherry-push (launch-runbook 준수)

## 답변 (recipient 작성 — 처리 후)

작업 A·B 둘 다 완료 (index.html).

- `buildBubbleData()`: census를 34사(NB) → 35사(CSM_waterfall 기말) 확장. Group A 10사는 직전 회기 fallback(nbCsm÷4, 배수 이월), Group B 3사(코리안리/IBK연금/BNP파리바카디프)는 multiple=null로 census엔 남기되 차트에서 자동 제외. IBK연금은 NB에 행 자체가 없어 CSM_waterfall 항목2로 fallback.
- 버블: 회색 점선 '추정 (연1회 공시)' 시리즈 분리 + 분기필터와 무관한 상시 범례 + 분기 branching 툴팁 + `#bubble-meta` 실공시/추정 분리 카운트. section-desc 한 줄 추가. 모바일 리스트는 기존 기준분기 칩을 '추정 <Q>' + 회색 텍스트로 병합(중복 없음).
- 보조표 신설: "IFRS17 — CSM 수록 현황 (전 35사)" 카드, 지시된 8컬럼·정렬·비고칩·KR0075 검증중 그대로 구현. 추가 fetch/마스터 기입 없음(기존 로드 재사용). `<table>+<caption class="sr-only">+scope="col"`.
- 검증: 1280px/375px Claude Preview — 표시 32社(실공시22·추정10) 확인, 툴팁 포맷 실측(⚠ 문구까지), 보조표 35행·13 muted 확인, 모바일 body 가로스크롤 없음(scrollWidth==innerWidth), 콘솔 에러 0. Group A/B 전 13사 수치 inbox census와 대조 완료(±1억 반올림 오차만). `pytest tests/test_deploy_assets.py` 8/8 pass.
- 헤드라인 KPI(163.8조)·3축 정의는 지시대로 미변경. KR0075 정정은 parser inbox 발주 그대로 대기 — 정정되면 보조표 자동 실수치 노출(designer 추가 작업 불필요).
