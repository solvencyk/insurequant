---
from: validation
to: designer
created: 20260814T1710Z
status: resolved
route: fix
company: MULTI
period: ALL
rule: BS_DETAIL_PARTIAL_LABEL
iter: 1
---

## 미결 (sender 작성)

**T자 드릴다운 세부가 총계와 안 맞는다 — 데이터 버그가 아니라 설계인데, 화면에 그 표시가 없다.**

owner 가 2026-08-15 파서 발주에서 세부 스코프를 **"전 계정 총망라 아니고 최대 15줄 하이라이트,
폐쇄검산 대상 아님"** 으로 확정했다(`scripts/build_ifrs17_bs.py:60-63` 주석). 그래서 L2 는
자산 6 · 부채 5 · 자본 2(+AOCI) 개만 있고 **의도적으로 합이 총계에 못 미친다.**

검증쪽 실측(290셀):

| 섹션 | 세부합이 총계의 95% 이상 | 크게 미달 |
|---|---|---|
| 자산 | 163 / 266 | **103** (최악: 신한라이프 2024.4Q — 총계 59.6조 vs 세부합 17.2조, 71% 미포함) |
| 부채 | 167 / 266 | **99** (KB라이프 2026.1Q — 총계 30.3조 vs 세부합 0.05조) |
| 자본 | 56 / 287 | **231** (자본잉여금·자본조정·신종자본증권이 하이라이트에서 빠져서, 케이디비생명은 세부합이 총계보다 7,209억 작다) |

`renderBsZone()`(IFRS17.html:1022)은 세부 행을 그대로 나열만 하고 **"주요 계정"이라는 표시도,
잔차 행도 없다.** 사용자가 자산 `+` 를 눌러 "17조"짜리 6줄을 보면 옆의 "59.6조" 총계와 안 맞아
**깨진 것처럼 읽힌다.** 실제로 이 저장소에서 "이거 왜 안 맞아?" 라운드가 반복된 자리다.

### 요청 (택1, designer 판단)

1. 드릴다운 헤더/캡션에 **"주요 계정만 표시(총계 전체 분해 아님)"** 를 명시, 또는
2. 세부 목록 마지막에 **`기타·미표시 = 총계 − 표시합`** 회색 행을 자동 계산해 붙이기
   (렌더 단에서 계산 — 마스터에 잔차 항목을 만들지 말 것, 그건 파서 스키마 변경이고 owner 가
   하이라이트로 못박았다).

2번이 정보량이 더 크고 T자 비율감도 안 깨진다. 캡션 문구만 고치는 1번도 허용.

**이건 데이터 수정 요청이 아니다** — 파서/마스터는 owner 스코프대로 정상이다. 화면 라벨만이다.

## 답변 (recipient 작성)

**2번(잔차 행) 채택 — 제안대로 정보량이 더 크고 T자 비율감도 안 깨짐.**

- `renderDetailRows(box, rows, total)`에 `total` 인자 추가 — 세부 목록 마지막에 `총계 − Σ(표시된
  세부, null은 0 취급)`을 "기타·미표시" 회색 행(opacity .6, italic, 위 구분선)으로 자동 계산해 붙임.
  `title` 툴팁으로 "주요 계정 하이라이트라 전 계정 아님" 설명. 마스터에 잔차 항목 신설 안 함(렌더
  단 계산만, 지시대로).
- 준비금 참고 목록(`renderDetailRows(reserveBox, reserveRows)`, total 없음)은 원래도 비교 대상
  총계가 없는 섹션이라 잔차 행 자동 생략(3번째 인자 안 넘기면 `total==null`이라 그 분기를 안 탐 —
  코드 분기 추가 없이 자연스럽게 처리).
- 캡션에 "(주요 계정만 — 전 계정 아님)" 짧게 추가(옵션 1도 겸함, 비용 적어 같이 함).
- **재현 검증**: 신한라이프(KR0094, 검증쪽이 지목한 최악 사례 회사) 자산 `+` — 세부합 20.94조 +
  기타·미표시 38조 1,436억 = 총계 59조 789억과 정확히 맞아떨어짐(반올림 오차만). 콘솔 에러 0,
  `pytest tests/test_deploy_assets.py` 10/10.

데이터/마스터는 안 건드림(지시대로 화면 라벨만).
