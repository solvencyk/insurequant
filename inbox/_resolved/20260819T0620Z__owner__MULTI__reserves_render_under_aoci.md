---
from: owner
to: designer
created: 20260819T0620Z
status: resolved
route: backlog
company: KR0049
period: 2025.4Q
iter: 1
---

## 미결 (sender 작성)

owner: *"악사 같은 분기공시 미제공사를 IFRS17.html에서 조회했더니 **법정준비금들이 다 AOCI
밑으로 들어가있고 (not 이익잉여금)**"*

### 원인 (오케스트레이터 실측)

악사손해보험(KR0049)의 `IFRS17_BS.json` 셀은 **7개뿐**이다:
항목 1·2·3(총계) · 4(AOCI) · 5·6·7(준비금). **항목 31(이익잉여금)이 없다.**

`IFRS17.html` L1237의 폴백이 그대로 작동한 결과다:

```js
// 이익잉여금 행 자체가 없는 회사(비상장 등)도 준비금 공시는 있을 수 있음 — 누락 방지로 자본
// 세부 끝에 평행 배치(T자 D-4의 앵커-없음 폴백과 동일 취지).
if (secKey==="자본" && !reAttached){ ... level: 1, group: "g_"+secId ... }
```

이익잉여금 앵커가 없으니 준비금 3행이 **자본 세부 끝에 평행 배치**되는데, 그 섹션에 남은
L2 행이 AOCI 하나뿐이라 화면상 **AOCI 바로 아래 자식처럼 읽힌다.** 분류가 틀린 게 아니라
배치가 오해를 부른다.

## 요청

폴백 경로에도 소속을 명시할 것. T자 레이아웃 쪽(L1090)에는 이미
`"법정준비금 — 이익잉여금 내부 적립, 자본총계 합계에는 미포함"` 라벨이 있는데
**표 드릴다운 폴백에는 그 라벨이 없다.** 같은 문구의 구분 헤더 행을 폴백에도 넣어
AOCI와 시각적으로 분리해 달라.

**데이터 근본 해결은 parser 쪽**(항목31을 본문 XML로 채우면 폴백을 안 탄다) —
`inbox/parser/20260819T0500Z` 범위 B에 포함돼 있다. 다만 그쪽이 끝나도 앵커가
영영 없는 회사가 남을 수 있으니 폴백 라벨은 별도로 필요하다.

## 주의

`IFRS17_BS.json`은 지금 parser 에이전트가 재빌드 중이다(2026-08-19). 데이터는 읽기만 할 것.

## 답변 (recipient 작성 — 처리 후)

지적하신 그대로. 표 드릴다운의 앵커-없음 폴백(현재 L1237 부근)에 T자(L1090)와 동일 문구의
구분행("법정준비금 — 이익잉여금 내부 적립, 자본총계 합계에는 미포함")을 추가 — `colspan`으로
전체 폭을 차지하는 별도 행이라 AOCI와 시각적으로 확실히 분리됨. 준비금 항목이 하나도 없는
회사에는 이 행 자체가 안 붙게 가드했다(`reNames.length` 체크).

브라우저 실측(악사손해보험/KR0049, 연도모드): "기타포괄손익 누계액" → **"법정준비금 — …"
구분행** → 해약환급금준비금·비상위험준비금·대손준비금 순으로 정상 렌더 확인. 이익잉여금 앵커가
있는 회사(삼성화재/KR0008)는 기존과 동일하게 이 구분행 없이 "이익잉여금 +" 하위로만 표시,
회귀 없음. `IFRS17_BS.json`은 읽기만 함(재빌드 중이라던 경고 준수). 콘솔 에러 0,
`pytest tests/test_deploy_assets.py` 10/10. 상세: `docs/changelog_designer.md` 2026-08-20.
