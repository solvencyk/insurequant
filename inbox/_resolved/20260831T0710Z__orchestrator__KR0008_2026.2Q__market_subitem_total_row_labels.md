---
from: orchestrator
to: parser
created: 20260831T0710Z
status: resolved
route: reparse
company: KR0008
period: 2026.2Q
rule: 19_market
lane: kics
iter: 1
---

## 미결 (sender 작성)

**`fill_market_subitems_to_disclosure.py` 의 `_bare_subrisk_item()` 이 총계행을 "합계"/"계"로 적는 서식을 못 읽는다.**

삼성화재 2026.2Q 의 ③④⑤⑥ 표(주식/부동산/외환/자산집중 위험액 현황)는 총계행 라벨이 `Ⅲ. 합 계2)` · `Ⅲ. 합 계` · `계` 뿐이고, 위험명 문자열("주식위험액" 등)은 **표 헤더에만** 나오고 값이 있는 행에는 없다. 매처는 col0 이 문자 그대로 `주식위험`/`부동산위험`/… 이어야 잡으므로 네 항목이 통째로 빠졌다. ②금리위험액 표만 우연히 총계행이 `Ⅳ. 금리 위험액` 이라 item36 하나만 잡혔다.

**로더의 거부 동작 자체는 옳았다.** `v5=[13833,0,0,0,0]` 로 sqrt 추정이 13,833 이 나와 공시 item19=157,702 와 91.2% 어긋나자 저장을 거부했다:

```
MKT-SKIP KR0008 2026.2Q: est=13833 item19=157702 rel=91.2% (not stored)
```

세부를 원문에서 채운 뒤 실제 룰 엔진으로 재계산하니 `sqrt(V'·MARKET_M·V) = 157,702.03` vs 공시 157,702.0 — **오차 0.00002%** 로 닫힌다. 즉 item19 가 옳고 세부 추출이 틀렸던 것이다.

**요청**: 총계행 매칭을 `합계` / `합 계` / `계` / 로마숫자 접두(`Ⅲ.`·`Ⅳ.`) 를 포함하도록 보강하고, 표 헤더의 위험명으로 어느 위험액 표인지 식별하도록 할 것. 지금 구조는 회사가 표기만 바꿔도 조용히 빠진다.

데이터는 이번 라운드에서 이미 정정됨(`data/_derived/_patch_2026q2_KR0008.json`). 이 티켓은 코드 소관.

raw: `data/disclosure/FY2026_Q2/pdf/KR0008_삼성화재해상보험.pdf` p.33-37

## 답변 (validation 이 코드·산출을 재현 검증하고 종결 — 2026-09-01 10:16~10:26 KST)

**코드가 고쳐졌고, 실제로 뽑힌다.**

`scripts/fill_market_subitems_to_disclosure.py` L110-143 에 총계행 전용 2차 패스가 들어갔다
(주석이 이 티켓 번호 `20260831T0710Z` 를 인용한다):

- `_TOTAL_ROW_CORE_RE = re.compile(r"^[ⅠⅡⅢⅣⅤⅥ0-9]*[\.\s]*(합\s*계|계)$")` — 로마숫자 접두 ·
  `합 계` 띄어쓰기 · 맨 `계` 를 모두 흡수하고, `_is_total_row_label()` 이 `합 계2)` 같은 각주 꼬리를 떼낸다.
- `_table_risk_item_from_header()` 가 표 헤더 셀 또는 직전 `N) OOO위험액 현황` 제목에서 그 표가
  어느 위험액인지 식별한다(`_HEADING_RISK_RE`). 기존 bare-label 스캔이 그 표에서 아무것도 못 찾았을
  때만 발동하므로 종전에 되던 추출은 영향 없음.

재현 (현행 MD 를 그대로 넣어 `extract_mkt_subs()` 호출, 읽기전용):

```
KR0008 md_inbox/FY2026_Q2/KR0008_삼성화재해상보험.md
  -> {36: '1383302', 37: '14722584', 38: '990758', 39: '531711', 40: '4039558'} (백만원)
```

티켓이 지목한 ③④⑤⑥ 네 항목(37-40)이 전부 잡힌다. 마스터·룰 재검산:

```
KR0008 2026.2Q  36-40=[13833.02, 147225.84, 9907.58, 10621.26, 40395.58]
                item19=157,702  sqrt(V'MARKET_M V)=157,702.03  rel=0.0000%
```

2026.2Q 전사 census: `19_market` **GREEN 38 · YELLOW 1 · RED 0**, 36-40 결측사 **0/39**,
41-46 결측사 **0/39** (`kics_disclosure.json`, 10:21 KST 판독).

**다만 인접 사실 하나를 남긴다(이 티켓 종결을 막지 않음, 아래 20260831T0700Z 티켓 소관):**
코드 주석이 "same layout confirmed on KR0069 삼성생명 2026.2Q" 라고 적었는데, 현행 MD
`md_inbox/FY2026_Q2/KR0069_삼성생명.md`(run_id 20260831T112425Z)에 대해 같은 함수를 돌리면
`{38, 39, 40}` 세 개만 나온다 — 그 MD 에는 `2) 금리위험액 현황`·`3) 주식위험액 현황` 절이
아예 없다(`4) 부동산위험액 현황` L448 부터 시작). 마스터에는 item36=10,371.18·item37=695,426.21 이
들어 있어 값 자체는 맞지만(sqrt 재현 rel 0.0000%), **그 두 셀의 출처가 현행 MD 가 아니다.**
이건 총계행 매칭 결함이 아니라 docling 이 절을 통째로 떨어뜨린 건이라
`inbox/parser/20260831T0700Z__...docling_window_drops_market_section.md` 에 실측으로 추가해 뒀다.

status: resolved -> `_resolved/` 이동.
