---
from: validation
to: parser
created: 20260824T0400Z
status: resolved
iter: 1
route: reparse
company: MULTI
period: MULTI
rule: 50_tfi_tier_split · 50_tfi_tier_split_post · 53_tfi_memo_rows · 53_tfi_memo_rows_post
lane: kics
---

## 미결 (validation) — item52/53/54 적재분에서 **원문 대조로 확정된 결함 7건 (blocking RED 18)**

`inbox/parser/20260821T1425Z` iter-10 이 적재한 1,291셀에 룰을 배선했다. 배선 직후 **18칸이
RED** 로 떴고, 전부 raw PDF 를 직접 열어 확인했다. **7건 전부 우리 쪽 결함**이다(발행사
자기모순 1건은 따로 표시).

배선 전에는 이 18칸이 **전부 GREEN 이었다** — item52/53/54 를 보는 룰이 하나도 없었기 때문이다.
47/48/49 때 1,285칸이 조용히 통과한 것과 같은 형태다.

### 재현

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py
  -> exit 2, RED=56, blocking RED=29 (기존 11 + 이번 18)
```

---

### A. 카카오페이(KR1098) 5분기 — **item52 가 100배** (RED 10건, 최우선)

| 분기 | 마스터 item52 (전=후) | raw 인쇄값(백만원) | 정답(억원) | 같은 표 item50 |
|---|---|---|---|---|
| 2023.1Q | 38,147 | 38,147 | 381.47 | 381.47 ✅ |
| 2023.2Q | 31,350 | 31,350 | 313.50 | 313.50 ✅ |
| 2023.3Q | 119,870 | 119,870 | 1,198.70 | 1,198.70 ✅ |
| 2023.4Q | 97,416 | 97,416 | 974.16 | 974.16 ✅ |
| 2024.1Q | 86,725 | 86,725 | 867.25 | 867.25 ✅ |

raw `data/disclosure/FY2023_Q3/raw/KR1098_카카오페이손해보험_amended.pdf` p10:

```
지급여력금액   119,870   119,870
기본자본       119,870   119,870
보완자본             -         -
보완자본 한도 적용 전  -    -
보완자본 한도        -         -
해약환급금 … 초과분   -         -
(기발행 신종자본증권) -
(기발행 후순위채무)   -
지급여력기준금액   1,857     1,857
```

**추정 원인**: `data/_derived/tfi_capital_memo_rows_provenance.json` 의 `ALL_ZERO_TRIVIAL`
(5건) 이 정확히 이 5버킷이다. "47/48/49/51 이 전부 사실상 0 → 스케일 무관" 이라는 단축인데,
**같은 표의 item52 는 0 이 아니다.** 47/48/49/51 에만 유효한 판정을 같은 패스에서 함께 읽은
item52 에도 적용한 것으로 보인다. 같은 버킷의 item50 은 다른 스크립트
(`fix_20260822_tfi_tier_full_scan.py`, `50+51 ≈ item1` 밴드)가 실어서 정확하다 — **한 버킷 안에서
두 로더가 서로 다른 배율을 썼다.**

**조치 요청**: `ALL_ZERO_TRIVIAL` 분기에서 item52 를 제외하고, item52 자신의 앵커
(`item52 ≈ item1_적용전`, 또는 `item52 == item50 + item51`)로 배율을 다시 판정해 달라.
지금 배선된 룰이 그 식이므로 고치면 즉시 GREEN 이 된다.

---

### B. 처브라이프(KR0100) 2023.1Q item54 = **원문에 없는 값** (RED 1건)

마스터 `item54[값] = 840.06`. raw `data/disclosure/FY2023_Q1/raw/KR0100_처브라이프생명보험.pdf`
p6 (인쇄쪽수 11):

```
보완자본            70,465   70,465
보완자본 한도 적용 전    52       52
보완자본 한도       73,681   73,681
해약환급금 … 초과분  70,413   70,413
(기발행 신종자본증권)    -
(기발행 후순위채무)      -          <- 대시. 84,006 은 이 행에 없다
지급여력기준금액   147,361   75,674
```

`84,006` 이라는 문자열은 그 페이지에 **존재하긴 한다**(다른 행이다). 메모행 값 캡처가 대시를
건너뛰고 뒤쪽 숫자를 집은 것으로 보인다. **item53 은 0.0 으로 정확히 적재됐다** — 두 행의
처리가 다르다는 뜻이라 원인 특정에 도움이 될 것이다.

**정답**: `item54[값] = 0.0` (대시).

---

### C. 농협생명(KR0104) 2024.3Q — **적용후 컬럼에 없는 값이 실렸다** (RED 1건)

마스터: `item53[값]=2,500.00 · item53[값_적용후]=37,913.42` / `item54[값]=9,391.71 ·
item54[값_적용후]=19,008.63`.

raw `data/disclosure/FY2024_Q3/raw/KR0104_농협생명보험_amended.pdf` p8:

```
(기발행 신종자본증권)   250,000        <- 적용전 칸 하나뿐
(기발행 후순위채무)     939,171        <- 적용전 칸 하나뿐
지급여력기준금액      3,108,871   3,108,871
```

**적용전 두 값은 정확하다**(250,000÷100 = 2,500.00 · 939,171÷100 = 9,391.71).
**적용후 두 값은 원문에 존재하지 않는다.** 37,913.42 · 19,008.63 이 어디서 왔는지는 못
찾았다 — 컬럼 판별기가 다른 행의 값을 집은 것으로 보인다.

**정답**: `item53[값_적용후]` · `item54[값_적용후]` 둘 다 **삭제**(결측). 0 으로 채우지 말 것 —
발행사가 그 칸을 인쇄하지 않았다.

배선한 포함관계 룰이 이걸 잡았다: `53+54 = 56,922.05 > item51_적용후 45,656.48`.

---

### D. 푸본현대(KR0083) 2024.3Q — **컬럼 오배정** (RED 1건)

마스터: `item53[값]=결측 · item53[값_적용후]=400.00` / `item54[값]=결측 ·
item54[값_적용후]=5,051.85`.

raw `data/disclosure/FY2024_Q3/raw/KR0083_푸본현대생명보험_amended.pdf` p15:

```
보완자본 한도 적용 전  1,240,502   695,316
보완자본 한도            689,352   689,352
해약환급금 … 초과분            0         0
(기발행 신종자본증권)     40,000              <- 적용전 칸
(기발행 후순위채무)      505,185              <- 적용전 칸
```

**값 자체는 맞다**(40,000÷100=400.00 · 505,185÷100=5,051.85). **컬럼이 반대**다.

**정답**: 두 셀을 `[값]` 으로 옮기고 `[값_적용후]` 는 비운다.

---

### E. 행 유실 3건 (RED 4건) — 원문에 인쇄돼 있는데 마스터가 비었다

| 버킷 | 결측 | raw | 정답 | 추정 원인 |
|---|---|---|---|---|
| 롯데손해 KR0003 2026.1Q | item53 | p22 `(기발행 신〮자본증권) 45,370` | 453.70 | **라벨 깨짐** — `신종` 이 아니라 `신〮`(U+302E 혼입). 같은 표 item54(211,136→2,111.36)는 정상 적재됐다 |
| 동양생명 KR0087 2024.1Q | item53·54 | p14 `(기발행 신종자본증권) 344,567` / `(기발행 후순위채무) 0` | 3,445.67 / 0.0 | 미상. 같은 회사 2023.1Q(p12)는 두 컬럼 다 정상 적재됐다 |
| 하나생명 KR0097 2025.2Q | item53 | p20 `(기발행 신종자본증권) -` | 0.0 | 대시 미인식(B 와 같은 계열일 가능성). item54 는 0.0 으로 정상 |

---

### F. 동양생명(KR0087) 2024.3Q item54 — **확정 못 했다. 원문 재판독 요청** (RED 1건)

raw p13 의 페이지 텍스트가 `(기발행 신종자본증권) 344,567` 에서 끝난다. `(기발행 후순위채무)`
행이 다음 페이지로 넘어갔는지, 발행사가 아예 안 찍었는지, 값이 있는지를 **페이지 텍스트만으로
확정하지 못했다.** 좌표 기반으로 다시 봐 달라.

참고: 같은 회사 2024.1Q 는 `0`, 2024.4Q 는 **라벨만 있고 값 없음**(공란)이다. 2024.1Q 필링의
주요변동요인이 "기발행 후순위채 3,000억 상환(23년 9월 1,000억 + 24년 1월 2,000억)" 이라
잔액 0 이 개연성은 있지만, **개연성으로 값을 채우지 않는다.**

발행사 공란으로 확정되면 게이트 쪽 레지스트리
(`src/solvency/validation/kics_json_rules.py::_TFI_MEMO_ISSUER_BLANK`)에 등재하면 RED 가
풀린다 — 그 등재는 validation 이 한다. **raw 근거만 회신해 달라.**

---

### G. 삼성화재(KR0008) 2025.3Q item52_적용후 — **발행사 자기모순. 고치지 말 것** (RED 1건)

마스터 `item52[값]=286,501.95 · item52[값_적용후]=286,051.95`. **둘 다 raw 그대로다** —
`data/disclosure/FY2025_Q3/raw/KR0008_삼성화재해상보험.pdf` p16:

```
지급여력비율 (%)      275.92      275.92
지급여력금액      28,650,195  28,605,195     <- 자릿수 전치(650 <-> 605)
기본자본          17,928,794  17,928,794
보완자본          10,721,402  10,721,402
지급여력기준금액  10,383,339  10,383,339
주1) 당사는 기발행 신종자본증권 및 후순위채무가 없어 공통적용 경과조치 전·후 지급여력비율이 동일함
```

검산: `28,650,195 / 10,383,339 = 275.92%` (인쇄된 비율과 일치) ·
`28,605,195 / 10,383,339 = 275.49%` (인쇄된 비율과 불일치). 기본자본·보완자본이 두 컬럼
동일하므로 합계만 45,000백만원 다른 것도 모순이다.

**우리 추출은 정확하다. 파생값으로 갈아끼우지 말 것.** documented exception 후보로 owner 큐에
올린다(validation 소관). **회신 불필요** — 이 항목은 참고용으로만 적었다.

---

### H. 저우선 backlog — 텍스트 추출 불가 20버킷의 메모행

iter-10 §4 의 20버킷은 로더 텍스트 스캐너가 TFI 표를 못 읽어 53/54 가 전부 결측이다. 47-51 은
과거 vision 백필로 들어와 있어 "표를 읽은 것처럼" 보인다. 게이트는 이걸
`TFI_MEMO_TABLE_NOT_SCANNED` **SKIP 사유로 따로 세서** 매 실행 눈앞에 남긴다(RED 아님) —
발행사 공란과 같은 색으로 찍지 않기 위해서다.

목록은 `kics_json_rules.py::_TFI_MEMO_TABLE_NOT_SCANNED` 에 박아 뒀다. **백필하면 그
레지스트리에서 지워 달라** — 안 지우면 그 버킷의 census 가 영구 SKIP 이고,
`tests/test_tfi_memo_rows.py::test_not_scanned_registry_does_not_hide_readable_buckets` 가
값이 실렸는데 등재가 남아 있으면 실패한다.

---

### 완료 조건

- A~E 5건 수정 → `validate_kics_disclosure.py` blocking RED **29 → 12**
  (F 는 raw 회신 후 validation 이 등재 또는 parser 수정, G 는 owner 큐, H 는 backlog).
- `kics_disclosure.json` 수정 후 `sync_master_xlsx_sheet.py "K-ICS공시"` 재동기화.
- 골든 재생성은 **데이터가 바뀌므로 필요하다**: `python tests/test_kics_rules_golden.py --update`
  + 커밋 메시지에 사유. 룰 코드는 건드리지 말 것 — 이번 라운드에 배선을 마쳤다.


---

## 답변 (parser-kics)

오케스트레이터 재발주(A=본 A, B=owner 승인으로 재정의된 삼성화재[원 티켓 G], C~E=본 C~D~E)를
전부 raw 좌표 재확인 후 처리했다. **D는 raw 재확인 결과 티켓 주장과 반대 — 스왑하지 않았다**
(근거 아래). Chubb(원 티켓 B)과 동양생명 2024.3Q(원 티켓 F)도 같은 조사 과정에서 확정 근거를
확보해 opportunistic으로 같이 정정했다.

### 전수 요약

| 항목 | 회사·분기 | 판정 | 조치 |
|---|---|---|---|
| A | 카카오페이 KR1098 5분기 | **우리 결함**(로더 스케일추론 버그) | item52 값·값_적용후 ÷100 (5분기) + 근본원인 코드패치 |
| B | 삼성화재 KR0008 2025.3Q | **발행사 원문 자릿수 전치**(owner 예외승인) | item52_적용후 286051.95→286501.95 |
| C | 농협생명 KR0104 2024.3Q | **우리 결함**(2단 레이아웃 행클러스터링 오염) | item53·54_적용후 삭제(결측 복귀) |
| D | 푸본현대 KR0083 2024.3Q | **티켓 판정 오류 — 마스터가 이미 정확** | 변경 없음(근거는 아래) |
| E1 | 롯데손해 KR0003 2026.1Q | **우리 결함**(라벨손상 U+302E로 행 유실) | item53 INSERT (453.7, PRE단일) |
| E2 | 동양생명 KR0087 2024.1Q | **우리 결함**(행 유실) | item53·54 INSERT (3445.67 / 0, PRE단일) |
| E3 | 하나생명 KR0097 2025.2Q | **우리 결함**(행 유실) | item53 INSERT (0, PRE단일) |
| (원B) | 처브라이프 KR0100 2023.1Q | **우리 결함**(C와 동일 2단레이아웃 오염) | item54[값] 840.06→0 |
| (원F) | 동양생명 KR0087 2024.3Q | **판독 완료**(페이지 경계 너머에서 발견) | item54 INSERT (0, PRE단일) |

### A. 카카오페이(KR1098) — 근본원인 규명 + 코드패치 + 전수 무손상 확인

**원인**: `scripts/fix_20260824_tfi_capital_memo_rows.py::_infer_scale()`의 `ALL_ZERO_TRIVIAL`
숏컷 — "47/48/49/51이 전부 0이라 그 넷의 스케일은 무관하다"는 판정을, 같은 버킷의 item52
(지급여력금액, 채무성자본이 0인 회사도 대개 실값)에도 무비판적으로 적용했다. `scale=1.0`을
반환해버려서 그 뒤에 있던 더 나은 폴백(`ITEM52_VS_ITEM1_ANCHOR`, item52 자신을 마스터 item1과
대조)이 애초에 실행되지 못했다(그 폴백은 `if scale is None:` 게이트 안에 있는데 ALL_ZERO_TRIVIAL은
scale을 `1.0`으로 채워버려 조건이 거짓이 됨).

raw 5분기 전부 좌표 재확인(fitz `get_text("words")`, 이 세션에서 직접):

| 분기 | raw 인쇄값(전=후, 백만원) | 같은 표 item50(기존, 이미 정확) | 마스터 item52(수정 전) | 수정 후 |
|---|---|---|---|---|
| 2023.1Q | 38,147 | 381.47 | 38147 | **381.47** |
| 2023.2Q | 31,350 | 313.5 | 31350 | **313.5** |
| 2023.3Q | 119,870 | 1198.7 | 119870 | **1198.7** |
| 2023.4Q | 97,416 | 974.16 | 97416 | **974.16** |
| 2024.1Q | 86,725 | 867.25 | 86725 | **867.25** |

(각 분기 raw: 기본자본=지급여력금액=인쇄값, 보완자본="-" — 이 회사는 채무성자본이 없어
기본자본이 지급여력금액 전체를 차지한다. item50은 이미 ÷100 되어 있었는데 같은 패스의
item52만 안 됐다는 것 자체가 버그 시그니처였다.)

**조치**: `scripts/fix_20260824_tfi_capital_memo_rows.py`에 패치 — `scale_method ==
"ALL_ZERO_TRIVIAL"`이어도 memo가 있으면 item52 vs 마스터 item1 앵커로 재확인하고, 명확한
배율(0.98~1.02 또는 0.0098~0.0102 band)이 나오면 덮어쓴다(애매하면 그대로 둠, 억지로 안 바꿈).
이미 로드된 5셀은 idempotent guard(`existing` 체크) 때문에 재실행으로 자동 정정되지 않아
`scripts/fix_20260824_item52_54_load_defects.py`에서 직접 UPDATE.

**전후 전수 비교(460버킷)**: 패치 전/후 `_infer_scale` provenance를 전부 재계산해 diff —
`scripts/_probes/probe_20260824_scale_diff.py` 결과:
```
총 버킷: old=460 new=460 | scale/scale_method 변경 버킷 = 5
  KR1098 2023.1Q~2024.1Q (5분기): scale 1.0(ALL_ZERO_TRIVIAL) -> 0.01(ITEM52_OVERRIDES_ALL_ZERO_TRIVIAL)
```
**변경은 정확히 이 5버킷뿐 — 나머지 455버킷 scale_method 완전 동일.** 다른 버킷 무손상 확인.

**게이트 자체검증**: 2026-08-24 세션 중 validation이 `50_tfi_tier_split`(및 `_post`)를
"item50+item51==item52" 등식으로 승격했는데, 그 룰의 detail 문구가 "카카오페이 5버킷의 item52
100배(ALL_ZERO_TRIVIAL 스케일 단축이 만든 구멍)를 이 축이 처음 잡았다"고 스스로 기록하고
있다 — 내 진단과 독립적으로 일치.

### B. 삼성화재(KR0008) 2025.3Q — owner 예외 승인 반영

raw `data/disclosure/FY2025_Q3/raw/KR0008_삼성화재해상보험.pdf` PDF p16(인쇄쪽수 "-14-") 직접
재추출(fitz words):
```
구분                    경과조치 적용 전   경과조치 적용 후
지급여력비율 (%)            275.92            275.92
지급여력금액             28,650,195        28,605,195
기본자본                 17,928,794        17,928,794
보완자본                 10,721,402        10,721,402
보완자본 한도 적용 전         248,929           248,929
보완자본 한도             5,191,670         5,191,670
해약환급금...초과분        10,472,473        10,472,473
(기발행 신종자본증권)              -
(기발행 후순위채무)                -
지급여력기준금액          10,383,339        10,383,339
   주1) 당사는 기발행 신종자본증권 및 후순위채무가 없어 공통적용 경과조치 전·후
        지급여력비율이 동일함
```
검산(직접 계산): 28,650,195÷10,383,339=275.926%(인쇄비율 275.92와 일치) vs
28,605,195÷10,383,339=275.492%(불일치). 기본자본+보완자본=28,650,196(오차 1, 반올림)도
650,195쪽과만 맞는다. → `605,195`가 인쇄 자릿수 전치(`650`↔`605`) 오류라는 판정에 동의.

**조치**: item52_적용후만 286051.95→286501.95로 정정, item50·51·item1(모두 이미 정확)은
불변. 스크립트 docstring에 "owner 예외 승인 2026-08-24" 명시(`scripts/fix_20260824_item52_54_
load_defects.py` 상단 주석 "## B" 절).

**닫힘 확인**(요청사항 4): 정정 후 게이트 재실행 결과 —
```
50_tfi_tier_split      KR0008 2025.3Q: GREEN (expected=286501.96 actual=286501.95 diff=-0.01)
50_tfi_tier_split_post KR0008 2025.3Q: GREEN (expected=286501.96 actual=286501.95 diff=-0.01)
```
둘 다 GREEN으로 닫혔다.

### C. 농협생명(KR0104) 2024.3Q — 원인 규명(2단 레이아웃 행클러스터링 오염)

raw `data/disclosure/FY2024_Q3/raw/KR0104_농협생명보험_amended.pdf` p8 직접 재추출:
```
(기발행 신종자본증권)   250,000   [칸2는 전각공백뿐 — 숫자 없음]
(기발행 후순위채무)      939,171   [칸2는 전각공백뿐 — 숫자 없음]
```
PRE(2500.00 · 9391.71)는 정확. 마스터의 값_적용후(37,913.42 · 19,008.63)는 원문 어디에도 없다.

**원인 확정**: 이 페이지는 "(1)공통적용경과조치"(좌, x≈40-390)와 "②장수위험 경과조치"(우,
x≈464-800)를 좌우 2단으로 배치한다. y좌표만 보는 행클러스터링이 같은 높이의 서로 다른 표
값을 한 행으로 묶었다 — 우측 표 "기본요구자본" PRE=3,791,342(÷100=37,913.42, item53_적용후과
정확히 일치)와 "생명·장기손해보험위험액" PRE=1,900,863(÷100=19,008.63, item54_적용후과 정확히
일치)이 좌측 표의 신종자본증권/후순위채무 행에 섞여 들어왔다 — 원인 확정.

**조치**: item53·54의 값_적용후 필드 삭제(결측 복귀). 값(PRE)은 불변.

### D. 푸본현대(KR0083) 2024.3Q — **티켓 주장과 반대, 변경하지 않았다**

raw `data/disclosure/FY2024_Q3/raw/KR0083_푸본현대생명보험_amended.pdf` p15를 dpi=400으로
확대해 직접 시각 확인한 결과, 두 메모행(신종자본증권/후순위채무)의 **"적용전" 칸이 대각선
취소선으로 명시적으로 병합·공란 처리**되어 있고, 실값(40,000 · 505,185)은 **"적용후" 칸에**
인쇄돼 있다:

```
                          경과조치 적용 전 | 경과조치 적용 후
(기발행 신종자본증권)   [대각선으로 취소된 빈 칸]  |    40,000
(기발행 후순위채무)     [대각선으로 취소된 빈 칸]  |   505,185
```
(대각선 하나가 두 행에 걸쳐 "적용전" 칸만 가로지른다 — Excel류에서 "이 칸은 해당없음"을
표시하는 흔한 관행.)

좌표로도 재확인: 두 값의 우측정렬 x1=547.9(전각공백 후행문자 보정 시 ~537.9)가 **적용후
컬럼 우측 앵커(536.9~537.2)와 거의 정확히 일치**하고, 적용전 앵커(389.5~389.8)와는 148pt
이상 이격돼 있다 — 다른 정상행(지급여력금액·기본자본·보완자본 등) 6개로 두 컬럼 앵커를
교차검증한 결과다.

**결론**: 현재 마스터(값=결측, 값_적용후=400.00/5,051.85)가 **이미 원문과 일치**한다.
**컬럼 스왑을 수행하지 않았다.** 티켓의 아스키 표는 좌표 없는 평문 추출(`get_text()`)로
보이며, 대각선 취소선 정보가 텍스트 레이어에는 안 남기 때문에 "값이 하나뿐이니 첫 칸(전)"으로
읽었을 가능성이 있다 — validation 재확인 요청.

**부작용(중요)**: 이 판정 때문에 `53_tfi_memo_rows` 룰이 KR0083 2024.3Q를 새로 RED로 잡았다
(`TFI_MEMO_ROW_MISSING: item53/54 결측(본문 47/48/49는 있음)` — expected=2 actual=0). 이 룰은
`값`[PRE] 필드만 보고 `값_적용후`[POST]를 안 보는 것으로 보인다 — 내가 라벨A B위 D 근거로 보기엔
이 회사는 PRE가 원래 공란이라 룰의 전제("본문을 읽었으면 메모행도 PRE에 있어야 정상")와
어긋난다. 룰 코드가 금지 파일이라 못 고친다 — **validation 판단 필요**(발행사 공란 문서화
예외로 등재하거나, 룰이 값_적용후도 보게 확장).

### E. 행 유실 3건 — 좌표로 컬럼 확정 후 INSERT

| 버킷 | raw | 좌표 근거 | 조치 |
|---|---|---|---|
| 롯데손해 KR0003 2026.1Q item53 | p22 `(기발행 신〮자본증권) 45,370`(라벨에 U+302E 혼입 확인) | x1=397.9, PRE 앵커(397.9~398.0)와 일치 | INSERT 값=453.7 |
| 동양생명 KR0087 2024.1Q item53/54 | p14 `344,567` / `0` | x1=377.1, PRE 앵커(377.1)와 일치(둘 다) | INSERT 값=3445.67 / 0 |
| 하나생명 KR0097 2025.2Q item53 | p20 `-`(대시) | x1=389.4, PRE 앵커(389.4)와 일치 | INSERT 값=0 |

전부 POST 칸은 완전 공란(대시조차 없음) — item54(각 버킷 기존 적재분)와 동일 패턴이라
`값_적용후`는 아예 필드를 안 만들었다(None이 아니라 키 자체 부재, 기존 관행과 일치).

### 부수 확정 2건 (opportunistic)

**처브라이프(KR0100) 2023.1Q item54[값]** — C와 동일한 2단 레이아웃 오염. raw p6: 우측 표
"해지위험" PRE=84,006(÷100=840.06, 현재 마스터 item54[값]과 정확히 일치)이 섞여 들어왔다.
좌측 표 자신의 원문은 "-"(대시, 좌표상 PRE 앵커 x1=276.6~276.7에 위치, 우측 표 값과는 별개
행). item54[값]을 840.06→0으로 정정. item53은 우연히 이 페이지에서 오염을 안 타 이미 0/0으로
정확했다(불변).

**동양생명(KR0087) 2024.3Q item54** — 원 티켓 F("페이지 경계로 판독불가")를 raw 재확인으로
확정. item53(344,567, 기존 적재분)이 있는 페이지(p13) 바로 다음 페이지(p14) 맨 위에서 발견:
`(기발행 후순위채무) 0`(x1=377.1, PRE 앵커와 일치, POST 완전공란). INSERT 값=0.

### 게이트 결과

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py
  -> exit 2, RED=39 (기존 56에서 감소), blocking RED=2
```
**blocking RED가 예상(29→12)과 다르게 2까지 내려갔다** — 이유는 validation이 같은 시간대에
병행 작업했기 때문이다(공유 워킹트리, 2-lane 병렬):

1. **한화생명(KR0068) 2025.2Q [2_tier1_bridge]가 더 이상 단순 blocking RED가 아니다.**
   validation이 이 세션 중(`inbox/validation/20260824T0410Z__validation__KR0068_2025.2Q__
   tier1_bridge_residual_unexplained.md`, owner 확인 2026-08-24) `EXEMPTION_STANDS_ON_OWNER_
   JUDGEMENT`로 documented exception 등재했다(원인 미규명, 조사 티켓은 open 유지 — 면제는
   push만 푼 것). **내가 한 일이 아니다** — 티켓의 "한화생명 1건은 남아야 정상"은 이 상태
   변화 이전 기준이었던 것으로 보인다.
2. 대신 남은 blocking RED 2건:

| 룰 | 회사·분기 | diff | 성격 |
|---|---|---|---|
| `2_tier1_bridge` | NH농협손해 KR0032 2024.3Q | -522.0 | **내 작업과 무관한 기존 결함** — item2/4/12/13(core bridge), item52-54와 관계없음. 조사 안 함(스코프 밖) |
| `53_tfi_memo_rows` | 푸본현대 KR0083 2024.3Q | expected=2 actual=0 | **D 판정의 직접 결과**(위 참조) — 룰이 PRE만 보고 POST를 못 봐서 발생 |

문서화 면제(잔차 박제, 발행사 자기모순): 18버킷·36건 findings + 8_life 1건(KR0079) — 전부
불변(내가 안 건드림).

### pytest

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe -m pytest tests/ -q \
  --ignore=tests/test_pl_breakdown_golden.py --ignore=tests/test_ifrs17_bs_golden.py
  -> 317 passed, 1 skipped, 1 failed  (기존 302 대비 증가는 validation의 동시 세션이 신규
     테스트 4개를 추가했기 때문 — test_tier2_issuer_inconsistent_exemption.py·
     test_tfi_memo_rows.py·test_kics_item_registry.py·test_tier2_limit_rules.py, 전부 ??(untracked))
```
`test_kics_rules_golden.py::test_rule_engine_output_matches_golden`는 예상대로 실패했다가
`--update`로 해소(데이터가 실제로 바뀌었으므로 정상 — `tests/fixtures/kics_rules_golden.json`
재생성, 13,664 findings/488버킷, by_status RED=39 반영).

**남은 1개 실패는 내가 못 고치는 validation 소유 테스트다**(`tests/*` 금지 목록):
`tests/test_tier2_issuer_inconsistent_exemption.py::test_the_exemption_is_narrow_and_does_
not_touch_the_held_buckets`. 이 테스트 자신의 docstring이 정확히 이 상황을 예견하고 있다 —
"삼성화재 2025.3Q — 고칠 것이라 면제 대상이 아니다. owner 2026-08-24 결정으로 parser가 값을
정정 중이다(`inbox/parser/20260824T0400Z` §G). 여기 등재하면 고쳐진 뒤에도 죽은 핀이 남아
'그 축은 면제됐다'로 오독된다." `held` 셋에 `("KR0008", "2025.3Q")`가 아직 남아 있어 "이
버킷은 RED로 남아야 한다"를 단언하는데, 내가 방금 그 값을 고쳐서 GREEN이 됐다 —
**테스트가 의도대로 정확히 작동한 것**(내 수정이 반영됐다는 신호). validation이 다음
세션에서 `held`에서 `("KR0008", "2025.3Q")`를 빼면 해소된다.

### xlsx 동기화

```
sync_master_xlsx_sheet.py "K-ICS공시"
  -> 변경 셀 14 · 추가 행 5 · 삭제 행 0 (22653행 -> 22658행), 검증 OK
  재실행 -> 변경 셀 0 · 추가 행 0 · 삭제 행 0, "이미 동기 상태"(drift 0 확인)
```

### 파일

- `scripts/fix_20260824_tfi_capital_memo_rows.py` — A 근본원인 패치(ALL_ZERO_TRIVIAL이어도
  item52는 재확인하도록).
- `scripts/fix_20260824_item52_54_load_defects.py`(신규) — A~E + 부수 2건 전부 적용, dry-run
  지원, idempotent, 각 항목 raw 근거 docstring.
- `scripts/_probes/probe_20260824_item52_54_defects.py` · `probe_20260824_verify_raw_AE.py` ·
  `probe_20260824_scale_diff.py`(신규, 전부 read-only 진단용).
- `kics_disclosure.json`(22653→22658행), `insurequant_master_tables.xlsx`(동기화),
  `tests/fixtures/kics_rules_golden.json`(`--update` 재생성).
- `data/_derived/tfi_capital_memo_rows_provenance.json` — 패치 후 전수 재스캔으로 갱신(부수
  산출물, A 전후비교 근거).

### 후속 (spawn_task 발주함, task_66ee6d43)

C·처브라이프 사례가 보여준 "TFI 페이지 2단 좌우 레이아웃 → y좌표만 보는 행클러스터링이 다른
표 값을 섞는" 버그가 item47-54 전체 431버킷 중 다른 곳에도 더 있을 수 있다 — 이번 세션은
발견된 2건만 raw로 확정해 고쳤고, 전수 재감사는 별도 세션으로 스핀오프했다.

### owner/validation 판단 대기

1. D(푸본현대)의 새 blocking RED(`53_tfi_memo_rows`) — 발행사 공란(PRE) 문서화 예외로 등재할지,
   룰이 값_적용후도 인식하게 확장할지.
2. `test_tier2_issuer_inconsistent_exemption.py`의 `held` 셋에서 `("KR0008", "2025.3Q")` 제거
   (validation 소유 파일, 내가 못 고침).
3. `("KR0032", "2024.3Q")` `2_tier1_bridge` diff=-522 — 미조사 상태 그대로(스코프 밖, 이 티켓과
   무관).
4. spawn_task로 낸 TFI 2단 레이아웃 전수감사 착수 여부.

status: answered

## sender 재확인 (validation, 2026-08-24) — **owner 위임 면제 4건 등재. blocking RED 29 → 19.**
## **A~G 는 그대로 살아 있다.** §G 삼성화재만 지시가 뒤집혔다 — 읽고 가라.

**결론 먼저.**

| 항목 | 결과 |
|---|---|
| owner 위임 면제 등재 | **4건 완료** — 예별손해 2025.1Q · BNP카디프 2024.3Q · 롯데손해 2023.1Q · **한화생명 2025.2Q**(추가 위임) |
| blocking RED | **29 → 19** (면제 10건). 남은 19 = **이 티켓의 A~G 18건 + NH농협 2024.3Q 1건**(미조사) |
| `validate_data_contract.py` | RED **7 → 5** — **5건 전부 이 티켓 소관**이다(삼성화재 2025.3Q · 하나생명 2025.2Q · 롯데 2026.1Q · 카카오페이 2023.4Q ×2). 나머지는 display 분기 필터 밖 |
| pytest | **318 passed, 1 skipped** (직전 302). **골든 무변경** — 룰 엔진을 안 건드렸다 |
| 마스터·xlsx | **읽기만 했다.** `kics_disclosure.json` mtime 불변(네가 쓰는 중이라 손대지 않았다) |

---

### 🔴 §G 삼성화재 2025.3Q — **지시가 뒤집혔다. 고쳐라.**

이 티켓 §G 에 내가 "우리 추출은 정확하다. 파생값으로 갈아끼우지 말 것 / documented exception
후보로 owner 큐에 올린다 / 회신 불필요" 라고 썼다. **owner 2026-08-24 결정으로 그게 바뀌었다:**

> "삼성은 너무 자기모순이 자명해 보이는데 걔만 올바른 숫자로 우리가 고쳐서 올리고
> 나머지는 원수사 모순 그대로 가자."

**그래서 나는 삼성화재를 면제로 등재하지 않았다.** 등재하면 네가 고친 뒤에도 죽은 핀이 남아
다음 세션이 "그 축은 면제됐다" 로 오독한다(지난 라운드에 롯데 2026.1Q 죽은 핀을
`TIER2_EXEMPTION_INERT` review 로 잡아냈던 그 형태다). `test_the_exemption_is_narrow_and_does_
not_touch_the_held_buckets` 가 이 버킷이 면제로 새어 들어가는 것을 **기계로 막는다** —
정정이 끝나면 `50_tfi_tier_split_post` 가 저절로 닫히므로 그 테스트도 같이 통과한다.

정정 대상은 raw `FY2025_Q3` p16 의 적용후 `지급여력금액` 한 칸이다(`28,605,195` → `28,650,195`).
**근거는 이 티켓 §G 에 이미 다 있다** — 같은 표의 비율이 275.92/275.92 로 불변이고 각주가
전후 동일을 명시하며, `28,605,195 / 10,383,339 = 275.49%` 는 인쇄된 275.92% 와 안 맞는다.
`28,650,195 / 10,383,339 = 275.92%` 가 정확히 맞는다. **자릿수 전치(50↔05)** 다.

> 고칠 때 **`values_provenance` 나 그에 준하는 필드에 "발행사 오식 정정" 을 남겨 달라.** 원문과
> 다른 값을 싣는 것은 이 저장소에서 드문 일이라, 근거를 안 남기면 다음 라운드에 누가 "원문과
> 다르다" 로 되돌린다.

---

### A~F·H 는 변경 없다

면제 등재는 **이 티켓의 어떤 버킷도 건드리지 않았다.** 등재한 4버킷과 A~H 는 교집합이 0 이다
(유일한 접점이 §G 인데, 그건 위처럼 면제가 아니라 **정정**으로 갈렸다).

- A 카카오페이 item52 100배 5버킷(RED 10) — 그대로 살아 있다. **최우선.**
- B 처브라이프 2023.1Q item54(RED 1) · C 농협생명 2024.3Q 적용후(RED 1) ·
  D 푸본현대 2024.3Q 컬럼 오배정(RED 1) · E 행 유실 3버킷(RED 3) — 그대로.
- F 동양생명 2024.3Q — **raw 재판독 회신 여전히 대기 중.** 공란으로 확정되면 내가
  `TFI_MEMO_ISSUER_BLANK` 에 등재한다. 값이 있으면 적재.
- H 20버킷 backlog — 변경 없음.

**완료 조건 갱신: A~G 를 고치면 blocking RED 19 → 1.** 남는 1 은 NH농협 2024.3Q(다리 잔차 −522,
**미조사**)이고 **내 소관**이다 — 다음 라운드에 조사한다. 네가 신경 쓸 것 없다.

---

### 등재분 4건 — 무엇을 박았는지 (네 데이터가 움직이면 자동 RED 다)

아래 셀들이 **박제**돼 있다. 재파싱으로 한 칸이라도 값이 바뀌거나 결측이 되면
`TIER2_EXEMPTION_INPUT_DRIFT` / `..._INPUT_MISSING` **RED** 로 즉시 되살아난다. A~G 정정이
이 셀들을 건드리지 않는 것은 확인했지만, 혹시 걸리면 그건 버그가 아니라 **설계대로 작동한 것**
이니 나에게 알려 달라.

| 버킷 | 박제 셀 | 박제 축·잔차 |
|---|---|---|
| KR0004 예별손해 2025.1Q | 1·2·3·4·12·13·47·48·49·50·51·53·54 | `3_tier2_composition` **+997.00** |
| KR0003 롯데손해 2023.1Q | 1·2·3·4·12·13·47·48·49·50·51·52·53·54 | `2_tier1_bridge` **+19.00** · `3_tier2_composition` **−19.00** · `50_tfi_tier_split` **−18.00** |
| KR0075 BNP카디프 2024.3Q | 1·2·3·4·12·13·47·48·49·50·51·52 | `2_tier1_bridge` **+15.00** · `3_tier2_composition` **−220.98** · `47_tier2_census`(전·후) · `51_tfi_tier2_composition` **−221.31** |
| KR0068 한화생명 2025.2Q | 1·2·3·4·12·13·47·48·49·50·51·52 | `2_tier1_bridge` **−30,095.00** |

**롯데손해 2023.1Q 만 주의할 것이 하나 있다** — 이 발행사는 그 분기 TFI 표를 **억원**으로
인쇄한다(대부분 회사는 백만원). 스케일 로직을 손볼 일이 있으면 이 버킷이 반례다.

---

### 재현

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_kics_disclosure.py
  -> exit 2, RED=56, **blocking RED=19** (= 56 − 8_life 1 − tier2 36)
     면제 18버킷 36건 전부 "일치"(tol 0.01) · 면제 근거(provenance) 검사 RED=0 REVIEW=2

C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_data_contract.py
  -> SUMMARY RED=5 YELLOW=297   (등재 전 7)

C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe -m pytest tests/ -q \
    --ignore=tests/test_pl_breakdown_golden.py --ignore=tests/test_ifrs17_bs_golden.py
  -> 318 passed, 1 skipped
```

변경 파일: `scripts/validate_kics_disclosure.py`(레지스트리 4건 + `VERIFIED_BY_OWNER` status 처리) ·
`data/_gold/kics_exemption_provenance.json`(entries 34 → 38) ·
`tests/test_tier2_issuer_inconsistent_exemption.py`(28 → 47건) · `TODO.md` · `TODO_validation.md`.
**허용오차 무변경. 마스터·xlsx·룰엔진·골든 무변경. 커밋·push 안 했다.**

status 는 **open 유지** — A~G 가 살아 있다.

---

## 답변 추가 (parser-kics, 이 sender 재확인에 대한 응답)

위 "sender 재확인"이 내 작업과 같은 시간대에 붙어서(공유 워킹트리, 병렬 2세션) — 내가 이미
`## 답변 (parser-kics)`(이 파일 위쪽, line ~211)에서 A~E + 원B(처브라이프) + 원F(동양생명
2024.3Q)를 전부 처리했다. 요점만 다시 짚는다:

- **§G 삼성화재 — 이미 고쳤다.** item52_적용후 286051.95→286501.95, `50_tfi_tier_split`·
  `_post` 둘 다 GREEN 확인 완료. 면제 등재 안 함(맞는 판단 — 정정 대상이지 면제 대상 아님).
- **F 동양생명 2024.3Q — "회신 대기 중" 아니라 이미 확정·적재 완료.** raw p14 맨 위에서
  `(기발행 후순위채무) 0`(x1=377.1, PRE 앵커와 일치) 발견 — `TFI_MEMO_ISSUER_BLANK` 등재
  필요 없이 그냥 값이 있었다(공란이 아니라 0). item54 INSERT 완료.
- **"A~G 고치면 blocking RED 19→1"(NH농협 미조사분 1건만 남는다는 projection)과 실측이
  다르다 — 내 세션 종료 시점 blocking RED=2다.** NH농협 KR0032 2024.3Q(다리 -522, 니 소관,
  안 건드림)는 예상대로 남았는데, **푸본현대 KR0083 2024.3Q가 새로 RED로 떴다**
  (`53_tfi_memo_rows`, expected=2 actual=0). 원인은 위 "### D" 절 참조 — 그 회사는 원문 자체가
  PRE 칸을 대각선으로 지우고 POST 칸에만 값을 인쇄한다(zoom 스크린샷으로 확인, 좌표로도
  재확인). `53_tfi_memo_rows` 룰이 `값`[PRE]만 보고 `값_적용후`[POST]를 안 보는 것 같다 —
  **네 판단 필요**: 룰이 POST도 보게 고치거나, 이 버킷을 발행사 공란(PRE) 문서화 예외로
  등재해달라. 내가 손 못 대는 파일(`kics_json_rules.py`)이라 못 고친다.
- **`test_the_exemption_is_narrow_and_does_not_touch_the_held_buckets`도 이제 실패한다** —
  `held`에 아직 `("KR0008","2025.3Q")`가 남아 있는데 내가 그 값을 고쳐서 GREEN이 됐다. 그
  테스트 docstring이 정확히 이 순간을 예견하고 써놨다("여기 등재하면 고쳐진 뒤에도 죽은 핀이
  남는다") — 다음 라운드에 `held`에서 빼면 해소.

**최종 재현(내 세션 종료 시점, kics_disclosure.json 22658행)**:
```
validate_kics_disclosure.py -> exit 2, RED=39, blocking RED=2
  (KR0032 2024.3Q 2_tier1_bridge diff=-522 [니 소관, 미조사] +
   KR0083 2024.3Q 53_tfi_memo_rows [D 판정의 부작용, 위 참조])
pytest tests/ -q --ignore=...golden×2 -> 317 passed, 1 skipped, 1 failed
  (실패=held-set 테스트, 위 설명대로 기대된 실패)
```

status: answered (frontmatter 갱신 완료, 이 위 두 답변 절 다 반영)

## 재확인 (validation, 2026-08-24 3차) — 잔여 2건도 종결. 게이트 blocking RED=0

parser 답변 시점 잔여였던 2건 모두 그 다음 라운드에 직접 닫혔다:
- **KR0083 푸본현대 2024.3Q `53_tfi_memo_rows`** — 원문이 PRE 칸을 대각선으로 지우고 POST
  칸에만 인쇄하는 표 레이아웃임을 확인, `kics_json_rules.py` 에 `TFI_MEMO_COLUMN_VARIANT`
  SKIP 분기 추가(범위 사전검증: 정확히 이 1버킷만 해당).
- **KR0032 NH농협 2024.3Q `2_tier1_bridge`(다리 −522)** — raw p12 주2) 각주와 인쇄값이
  자기모순(23,478−0−8,867=14,611 vs 인쇄 기본자본 14,089)임을 확인, 잔차 박제 면제 등재 +
  근거 원장(`data/_gold/kics_exemption_provenance.json`) 마커 7개 추가.
- `held` 테스트의 예견된 죽은 핀도 제거, 레지스트리 크기 락(19) 추가.

오늘 재확인: `validate_kics_disclosure.py` baseline — RED=38 전부 tier2 면제(37)+8_life(1)로
설명됨, **미설명 blocking RED 0**. 카카오페이 item52 현재값이 item50 과 정확히 일치(억원
스케일) 재확인. 이 티켓의 결함 7건 전부 종결.
