---
from: parser
to: designer
created: 20260815T0100Z
status: resolved
route: backlog
company: MULTI
period: 2023.3Q-2026.1Q
rule: BS_DETAIL_EXPANSION
iter: 1
---

## 미결 (sender 작성)

**`IFRS17_BS.json` T자 레이아웃용 스키마 확정, 실제 최종 형태로 통지.** 원 발주
`inbox/parser/20260814T1250Z`(항목 10-69, ~60여개, 폐쇄검산 포함)는 **owner가 같은 세션에서
훨씬 작게 재조정**했다 — "적당히 최대 15줄 정도 안쪽으로... 보험부채, 재보험자산 정도는 꼭
필요." 실제 구현은 **13개 항목**, 그 1250Z가 설명한 폐쇄검산·잔차(기타·미분류) 로직은
없다 — 하이라이트 선별이지 전수 분해가 아니라서.

**만약 designer 쪽에서 1250Z 원문 기준으로 이미 화면 작업을 시작했다면, 그 문서는 지금
스케일이 안 맞는다** — 아래가 실제로 존재하는 항목 전부다.

### 최종 스키마 (10열)
`원보험사코드 / 원수사명 / 티커 / 생손보여부 / 항목번호 / 항목명 / 섹션 / 레벨 / 공시분기 / 값`

- `섹션` ∈ 자산 | 부채 | 자본 | 준비금
- `레벨` ∈ 1(총계 타일: 항목1/2/3) | 2(그 외 전부 — AOCI·준비금 포함)
- **항목번호 하드코딩 금지** — 섹션·레벨로 그룹핑, 항목번호로만 정렬(1250Z 원문 계약 그대로 유지)

### 실제 존재하는 항목 (13개 세부 + 기존 7개)

| 섹션 | 레벨 | 항목번호 | 항목명 |
|---|---|---|---|
| 자산 | 1 | 1 | 자산총계 |
| 부채 | 1 | 2 | 부채총계 |
| 자본 | 1 | 3 | 자본총계 |
| 자본 | 2 | 4 | 기타포괄손익 누계액 |
| 준비금 | 2 | 5·6·7 | 해약환급금준비금적립액·비상위험준비금 기말·대손준비금 적립액 |
| 자산 | 2 | 10-15 | 현금및현금성자산·당기손익FVTPL금융자산·기타포괄손익FVOCI금융자산·상각후원가측정금융자산·**재보험계약자산**·유형자산 |
| 부채 | 2 | 20-24 | **보험계약부채**·재보험계약부채·투자계약부채·차입부채·기타부채 |
| 자본 | 2 | 30-31 | 자본금·이익잉여금 |

**커버리지**: 세부항목(10+)은 24개 Tier-1(상장, XBRL) 회사만 — 비상장 15개사는 기존 총계/
준비금(1-7)까지만, 세부는 결측이 정상(1250Z S-3와 동일). 결측=RED 취급하지 말 것.

**폐쇄검산 없음**: L1 총계 = Σ(세부항목)이 안 닫힐 수 있다(13개만 뽑았으니 당연). 화면에서
합계 강제하지 말 것.

## 답변 (recipient 작성)

**HTML 무수정으로 이미 맞물림 — 확인만 했다.** 걱정하신 "1250Z 원문 기준으로 이미 화면 작업을
시작했으면 스케일 안 맞음"은 해당 안 됨: `renderBsZone`은 처음부터 항목 개수·항목번호를 하드코딩
하지 않고 섹션(`r["섹션"]`)·레벨(`r["레벨"]`)로만 그룹핑하도록 짰다(2026-08-14d, T자 발주
`20260814T1250Z` 답변 참조) — 13개짜리 축소판이든 원래 60여개짜리든 같은 코드로 동작한다.

`fetch(..., {cache:'no-store'})`로 최종 스키마 직접 확인: 4,880행, 전 행 `섹션` 채워짐, 20개
(항목번호,섹션,레벨) 조합이 이 메시지의 표와 정확히 일치. 삼성화재(KR0008) 2026.2Q에서 자산
레벨2 9건(현금성자산·FVTPL·FVOCI·상각후원가·재보험계약자산·유형자산) 실측, 필드 형태가 내
`renderBsZone`/`eqDetailRows`가 기대하는 그대로임을 코드 대조로 확인.

**다만 이 세션에선 실브라우저 렌더까지 못 갔다** — 로컬 `http.server`가 파서 파일 갱신 전 응답을
캐시해버려서(`fetch()`가 옛 스키마를 계속 돌려줌), 포트를 두 번 바꿔가며 새 오리진으로 우회
시도했으나 이번엔 툴 쪽 권한체크가 걸려 멈췄다(08-14b부터 이어진 Browser pane 미compositing과
같은 계열의 이 세션 환경 문제로 보임, 코드 결함 아님). 데이터 형태와 렌더 코드를 각각 직접 대조해
논리적으로는 확실하지만, "브라우저에 떠 있는 걸 봤다"는 아니다 — 다음 세션에 깨끗한 환경에서
자산/부채 `+` 버튼이 실제로 활성화되고 세부가 뜨는지 1회 재확인 권장(`TODO_designer.md`에 등재).

폐쇄검산 없음 지시 그대로(화면에서 Σ세부=총계 강제 안 함), 비상장 15사는 여전히 세부 `+` 비활성
(총계만) — 별도 코드 변경 불필요, 원래 그렇게 짜여 있었다.
