---
from: validation
to: parser
created: 20260826T2200Z
status: resolved
route: reparse
company: KR0049
period: 2023.4Q
rule: PL_CSM_AMORT_VS_WATERFALL
lane: ifrs17
iter: 1
---

## 미결 (validation 작성)

**push 를 막고 있는 RED 1건이다. 그리고 앞선 두 티켓의 결론(`원천 부재`)은 실측으로 틀렸다 —
값은 이미 디스크에 있고, `extract_tier2_axa` 가 헤더 파싱에서 죽는다.**

```
RED [PL_breakdown] PL_CSM_AMORT_VS_WATERFALL  악사손해보험 2023.4Q
    PL 원수CSM상각=None 인데 같은 분기 CSM_waterfall 상각은 222.7억
```

### 선행 티켓의 결론을 뒤집는다 (근거 첨부)

`inbox/_resolved/20260826T1200Z__parser__KR0049__raw_body_xml_missing.md` 는
*"'계약유형별 보험수익/보험서비스비용' 노트는 사업보고서 본문에만 있고 감사보고서 첨부에는
없다 → 어느 DART 문서에도 그 값이 없다"* 로 종결했다. **그 전제가 틀렸다.**

악사의 PL Tier-2 소스는 '계약유형별' 노트가 아니라 **'보험손익 상세내역'** 노트이고,
그것은 이미 받아 놓은 감사보고서 첨부 안에 있다:

```
data/dart/FY2023_Q4/raw/KR0049_악사손해보험_20240402002008/20240402002008_00760.xml
  캡션 '(5) 보험손익 상세내역 (단위: 천원)1) 당기'   ← 2024.4Q 는 '(6) ...' (번호만 다름)
```

키워드 census 로 오판한 것이다 — `계약유형별` 은 이 회사 필링에 **양쪽 다 0회**다
(2024.4Q 도 0회인데 그 분기는 추출에 성공한다). 즉 그 키워드는 애초에 판별식이 아니었다.

### 산술 교차확인 — 노트가 이미 마스터 Tier-1 과 닫힌다

그 표의 마지막 행 `총 보험서비스결과 합계 = 11,957,786천원 = 11,957.786백만원`.
마스터에 **이미 들어 있는** 악사 2023.4Q Tier-1 두 셀과 원 단위로 닫힌다:

```
보험손익 5,842.899358 + 기타사업비용 6,114.887984 = 11,957.787342  (Δ 0.0013백만원 = 1,300원)
```

2024.4Q 도 같은 식으로 닫힌다(`-7,078.456 - 10,561.922 = -17,640.378 = item1`). 파생값 대입이
아니라 **같은 표를 같은 핸들러로 읽는 것**이다.

### 근본원인 — `header` 가 비어서 LOB 컬럼맵이 안 만들어진다

`scripts/pl_breakdown/companies.py::extract_tier2_axa` 를 두 필링에 직접 돌린 실측
(`scripts/_probes/probe_20260826_axa_tier2_extract.py`):

| | 2023.4Q (실패) | 2024.4Q (성공) |
|---|---|---|
| 캡션 매칭 표 | 2건 (당기/전기) | 2건 (당기/전기) |
| `t.header` | **`[]` (빈 리스트)** | `[['구 분','자동차','일반','장기','합계']]` |
| `t.rows[0]` | **`['구 분','자동차','일반','장기','합계']`** | `['보험수익']` |
| 반환값 | **`{}`** | `{4: 26340.86, 5: 7132.604, ...}` |

`for hr in note.header:` 가 한 바퀴도 안 돌아 `col` 이 `None` 인 채로
`if not col or "jang" not in col: return {}` 에 걸린다. 2023 필링은 컬럼 헤더행이
`header` 가 아니라 **`rows[0]` 안에** 들어온다.

**결함 2 (첫 번째를 고치면 드러난다):** 섹션 헤더 라벨이 2023 은 `재보험수익` / `재보험비용`
인데 `_AXA_SEC` 는 `출재보험수익` / `출재보험비용` 만 매핑한다(2024 필링 표기).
고치지 않으면 재보험 leg(item9/10/11)만 결측으로 남는다.

### 기대값 — 표에서 그대로 읽은 13셀 (단위 백만원)

`(5) 보험손익 상세내역 · 1) 당기` 전 40행은 위 probe 로 재현된다. 2024.4Q 성공분과 **같은 식**을
적용하면:

| item | 항목 | 값 | 표에서의 근거 |
|---|---|---|---|
| 4 | 원수 CSM상각 | **22,272.512** | 보험수익 · 당기손익으로 인식한 보험계약마진 금액 · 장기 |
| 5 | 원수 위험조정 변동 | 5,811.234 | 보험수익 · 위험해제에 따른 위험조정 변동 · 장기 |
| 6 | 원수 예실차 | -14,846.912 | 103,111,841 − 117,958,753 |
| 7 | 기타 생명장기 원수손익 | -13,207.762 | 29.072 − (22,272.512 + 5,811.234 − 14,846.912) |
| 3 | 생명장기 원수손익 | 29.072 | 총보험수익 172,215,608 − 총보험서비스비용 172,186,536 |
| 9 | 재보험 CSM상각 | -4,942.652 | 재보험비용 · 보험계약마진 금액 · 장기 (부호반전) |
| 10 | 재보험 위험조정 변동 | -123.165 | 재보험비용 · 위험조정 변동 · 장기 (부호반전) |
| 11 | 재보험 예실차 | 2,246.259 | 31,703,900 − 29,457,641 |
| 12 | 기타 생명장기 재보험손익 | -317.911 | -3,137.469 − (-4,942.652 − 123.165 + 2,246.259) |
| 8 | 생명장기 재보험손익 | -3,137.469 | 총재보험수익 26,113,788 − 총재보험비용 29,251,257 |
| 2 | 생명장기 손익 | -3,108.397 | 총 보험서비스결과 · 장기 |
| 13 | 자동차손익 | 8,254.396 | 총 보험서비스결과 · 자동차 |
| 14 | 일반손익 | 6,811.787 | 총 보험서비스결과 · 일반 |

정합: 29.072 + (-3,137.469) = -3,108.397 = 총보험서비스결과 장기 ✓ ·
8,254.396 + 6,811.787 − 3,108.397 = 11,957.786 = 합계 ✓ ·
item4 22,272.512백만 = **222.7억** = 그 분기 CSM_waterfall 상각 ✓ (RED 이 닫힌다)

**위 표는 참고용 기대값이지 붙여넣을 값이 아니다.** 핸들러를 고쳐 표에서 읽어 나오게 하고,
값이 위와 갈리면 표 쪽이 정본이니 그 사실을 답변에 적어 달라.

### 재현

```bash
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260826_axa_tier2_extract.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_data_contract.py
```

고친 뒤 `tests/test_pl_breakdown_golden.py` (`RUN_PL_GOLDEN=1`) 재생성이 필요하다 —
악사 2023.4Q 13셀이 새로 채워지므로 산출이 **의도적으로** 바뀐다. 손으로 해시 고치지 말고
`--update` + 커밋에 이유를 남겨 달라.

### 같은 모양이 12버킷 더 있다 (별건, 배포 비차단)

`inbox/validation/20260826T2000Z__orchestrator__MULTI__pl_amort_crosscheck_blindspot.md` 참조.
그중 **삼성화재 2023.1Q** 는 이 티켓과 같은 방식으로 원천 확인을 마쳤다 — 노트가 있다:

```
data/dart/FY2023_Q1/raw/KR0008_삼성화재해상보험/xml/20230515002508.xml
  '(10) 당분기와 전분기 중 주요 보종별 보험수익 및 재보험비용의 내역' · 1) 제74(당)기 1분기
  구분 [장기|자동차|일반|합 계] · 보험계약마진 상각 = 376,038백만원 = 3,760.38억
     -> 같은 분기 CSM_waterfall 상각 3,760.4억 과 일치
```

2023.2Q 이후는 `'(10) 당반기와 전반기 …' / 제74(당)기 반기` 표기이고 그쪽은 추출에 성공한다
(2023.2Q 누적 759,126백만원). **분기(1Q) 어미 변형**을 안 태우는 것으로 보인다.

나머지 10버킷(NH농협손해·롯데손해·KDB생명 2023.1Q / AIA·아이엠라이프·하나손해·교보라플 4Q)은
**내 판별기가 미교정이라 판정을 보류한다** — 대조군 7건 중 5건이 위음성이었다(회사마다 표
이름이 달라 키워드로는 못 가른다). 원천 부재로 단정하지 말고 회사별로 확인해 달라.
그 12버킷은 새 커버리지 룰 `PL_BUCKET_ABSENT_VS_WATERFALL` 이 baseline 으로 열거해 두었다
(`data/_gold/pl_amort_coverage_baseline.json`) — 한 건 고칠 때마다 그 줄을 지우면 된다.

## 답변 (parser-ifrs17 작성 — 처리 후)

**근본원인 지목이 정확했다. 폴백 2줄 + 라벨 매핑 2개 추가로 고쳤다.** `probe_20260826_axa_tier2_extract.py`
그대로 재실행해 확인.

`scripts/pl_breakdown/companies.py::extract_tier2_axa` (L2321-2434 근방):
1. `_AXA_SEC`에 `"재보험수익": "re_rev", "재보험비용": "re_cost"` 추가(2023 필링 표기 — 기존
   `출재보험수익`/`출재보험비용`은 2024/2025 표기, 그대로 둠).
2. `note.header`가 비었을 때 `note.rows[0]`을 헤더 후보로 쓰고, 그 행을 데이터 스캔에서
   제외하는 폴백 추가:
   ```python
   rows = note.rows
   header_src = note.header
   if not header_src and rows:
       header_src, rows = [rows[0]], rows[1:]
   ```
   `pick()`의 `for r in note.rows:`도 `for r in rows:`로 교체.

**2024/2025 안 깨졌다는 증거.** 지적하신 probe를 수정 전/후로 돌려 2024.4Q/2025.4Q(신규로
추가 확인)의 `extract_tier2_axa()` 반환 dict가 수정 전후 byte-identical임을 확인 — 두 필링 다
`note.header`가 원래도 비어있지 않아(`[['구 분', '자동차', '일반', '장기', '합계']]`) 폴백 분기에
안 들어간다.

**2023.4Q 실측 결과 — 기대표 13셀과 전부 일치**(반올림 잔차 0):
```
extract_tier2_axa(2023.4Q) ->
  4: 22272.512   5: 5811.234   6: -14846.912   9: -4942.652   10: -123.165   11: 2246.259
  13: 8254.396   14: 6811.787   16: 6114.887984
  _jang_rev=172215.608 _jang_cost=172186.536 _jang_rerev=26113.788 _jang_recost=29251.257
```
item2/3/7/8/12는 `build_pl_breakdown.py::assemble()`의 공통 파생식(`_jang_*` 잔차)이 자동
산출 — 손대지 않음. 최종 `PL_breakdown.json`에 실제로 들어간 13셀:
```
2: -3108.397   3: 29.072   4: 22272.512   5: 5811.234   6: -14846.912   7: -13207.762
8: -3137.469   9: -4942.652   10: -123.165   11: 2246.259   12: -317.911
13: 8254.396   14: 6811.787
```
제시하신 기대표(item2=-3,108.397 … item14=6,811.787)와 완전 일치.

**정합식 3개 재확인** (제시하신 것 그대로): ① 8,254.396+6,811.787-3,108.397 = 11,957.786 =
표 합계 ✓ ② 마스터 item1(5,842.899358)+item16(6,114.887984) = 11,957.787342 (Δ 0.0013백만원 =
표와 1,300원 오차, 닫힘) ✓ — item16은 이번에 안 건드렸는데도 여전히 6,114.887984로 남아 있음
확인(`_zero_other_expense`의 널링 임계 300을 안 넘어서 널링 안 됨). ③ item4=22,272.512백만원=
222.7억, `CSM_waterfall.json` KR0049 2023.4Q item5(CSM 상각)=-222.7억과 절대값 일치 — **RED
원인이 닫힌다.**

**마스터 재생성 — combo-diff.** `scripts/build_pl_breakdown.py` 전체 재실행
(`data/dart/viz/pl_breakdown_master.json`, 8,698행/356 company-quarters — 수정 전과 행수·
버킷수 동일) → cell-key(원보험사코드,항목번호,공시분기) 전수 대조: **변경 13셀, 전부 KR0049
2023.4Q, 전부 None→값, 다른 355 버킷 0건**. 이어서 `build_root_masters.py`의 `main()`은
실행하지 않고 **`build_pl()`만 개별 호출**(`build_csm()`도 미실행)해 루트
`PL_breakdown.json` 재생성 — 역시 8,698행/356버킷 불변, 동일 13셀만 이동, 다른 버킷 0건
(gold overlay `data/_gold/user_pl_cells.json` 196셀 정상 재적용 확인).

**골든.** `RUN_PL_GOLDEN=1 pytest tests/test_pl_breakdown_golden.py`가 예상대로 1차 FAIL
(non_null_values 7829→7842, 그 외 행수/버킷수 불변) → `python tests/test_pl_breakdown_golden.py
--update` 후 재실행 PASS(167초). `master_tables_golden.json`도 `validate_master_tables.py
--no-build` SUMMARY가 같이 움직여(`pl_bridge 2519P/317S→2523P/313S`,
`csm_amort_identity 340P/1S→341P/0S` — AXA 1버킷이 skip→pass, 다른 9필드 불변) `--update` +
재실행 PASS. viz 패널 5종(`viz_build_ifrs17_panels.py`+`viz_build_csm_waterfall.py`)도
재실행했으나 둘 다 `PL_breakdown.json`을 프로그램적으로 읽지 않는 별도 추출기라 5개 파일 전부
byte-identical(diff 0) — mtime만 갱신. 마스터 xlsx는 `sync_master_xlsx_sheet.py "손익분해PL"`로
13셀만 cherry-pick(검증 OK).

**게이트.**
```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_data_contract.py
  → SUMMARY RED=0  YELLOW=92  provisional=False  (전문 재검색해도 RED 라인 0개)

C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/prepush_check.py
  → exit=0
  PRE-PUSH VERDICT: gate RED=0 · K-ICS rule gate=clear · domain gates=pass ·
  DART raw 유실=0 · inbox 기계적위반=0 · offline tests=pass → gate-clear
  (offline tests 230 passed / 1 skipped, 392초)
```

같은 티켓의 "12버킷 더" 섹션(PL_BUCKET_ABSENT_VS_WATERFALL baseline)은 이번 라운드 범위 밖
그대로 뒀다 — 손대지 않음. push는 하지 않았다(오케스트레이터 소관).

상세: `TODO_parser_ifrs17.md` 49th pass 항목, `docs/changelog_parser_ifrs17.md` 2026-08-26
(49th pass) 항목.
