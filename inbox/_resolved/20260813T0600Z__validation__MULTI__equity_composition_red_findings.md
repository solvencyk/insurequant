---
from: validation
to: parser
created: 20260813T0600Z
status: resolved
route: reparse
company: MULTI
period: 2023.3Q-2026.1Q
lane: ifrs17
iter: 1
---

## 미결 (sender 작성)

`equity_composition.json` (1차 산출, 2026-08-13 14:33) 에 검증 룰을 걸었다.
룰 러너: `scripts/validate_equity_composition.py` (게이트 배선은 `validate_data_contract.py`).

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/validate_equity_composition.py
```

**RED 328건.** 아래 6묶음이고, 전부 파서 쪽 재추출 대상이다(값 보정 금지).

> 2026-08-13 갱신: 초판은 341건이었다. `EQ_RESERVE_WITHIN_RE`(5 ≥ 10+12+14) 13건은
> **룰을 내렸다(RED → YELLOW flag)** — 이익잉여금 = 법정준비금 + 미처분이익잉여금이고 그 잔여가
> 음수일 수 있어(손실 중에도 강제적립되는 해약환급금준비금) 항등식이 아니다. owner 결정.
> **에이비엘생명 11건·롯데손해보험 2건은 재추출 대상 아님** — 쫓지 말 것.
지금은 어떤 배포 HTML 도 이 JSON 을 읽지 않아 push 게이트에서는 YELLOW 로 보고되지만,
패널에 물리는 순간 코드 수정 없이 RED 로 승격돼 push 를 막는다. 배포 전에 0 으로 만들어야 한다.

### P-1. 자본총계 폐쇄 실패 = CFS 비지배지분 누락 (22건, 2사 전분기)

`EQ_EQUITY_CLOSURE`: 1 != 2+3+4+5+6+7. **CFS 기준 2사에서만, 11개 분기 전부** 터진다.

| 회사 | 잔차(백만원) |
|---|---|
| 메리츠화재(KR0001) | +12,764 ~ +16,605 |
| 삼성생명(KR0069) | +1,870,376 ~ +2,111,022 |

`BASIS_CFS` 두 회사에 정확히 일치 → **연결재무제표의 비지배지분(NCI)** 이 어느 항목에도
안 담겨 자본총계만 크고 구성요소 합은 작다. 조치: 항목 **8 = 비지배지분**
(`ifrs-full_NoncontrollingInterests`) 신설. 신설하면 룰은 자동으로 닫힌다(폐쇄식이
`CLOSURE_PARTS`에 8을 이미 포함, 없으면 안 더함). 항목번호를 8 이외로 쓸 거면 회신 바람.

### P-2. FY2023 기초 AOCI(20) 오선택 (롤포워드 22건 + FY drift 15건)

`EQ_AOCI_ROLLFORWARD`: 20+29 != 30. **2023.3Q/2023.4Q 에 집중**되고, 회사별 차이가 그 FY 안에서
**정확히 상수**다(한화손보 126,111 / 롯데 522,789 / 흥국 1,003,379 / 삼성생명 2,988,545 /
KDB 1,895,538 / 교보 1,942,485 / 미래에셋 1,282,940 …). 상수 오차 = 흐름(29)이 아니라
**기초(20) 한 값이 통째로 틀렸다**는 지문이다. FY2023 은 IFRS17 최초적용 연도라 자본변동표
기초자본 행이 *재작성 전 / 재작성 후* 두 줄로 나오는 회사가 많다 — 다른 줄을 집었는지 확인.

`EQ_AOCI_OPENING_FY_DRIFT`(YELLOW 15건, 12사)도 같은 계열: FY 안에서 기초(20)가 분기마다
바뀐다(예: 흥국화재 FY2024 1Q/2Q=327,581 → 3Q/4Q=356,044). 중간 자본변동표는 FY 개시 잔액
고정이어야 한다(빌더 docstring 도 그렇게 적혀 있다) → 분기별로 다른 행을 집고 있다.

예외 1건: 한화손보 2025.4Q 차이 3,198 (상수 패턴 아님) — 별건으로 확인 바람.

### P-3. 자본변동표 자체 미파싱 (28건 + 코어 결측 230건)

`EQ_PARENT_CHILD_INCOMPLETE`: BS AOCI(6)은 붙었는데 20/29/30 이 통째로 없음.

- **2025.4Q·2026.1Q 최근 2분기 집중**: 삼성화재·NH농협손보·한화생명·DB생명·푸본현대·동양생명·
  신한라이프(각 2건), 흥국화재·현대해상(2026.1Q).
- **케이디비생명 2024.3Q~2026.1Q 7건 / 농협생명 5건**은 더 길게 끊겨 있다.

관련 `EQ_CENSUS_MISSING_ITEM` 코어 결측 분포: item29 148건, item20/30 각 41건
(= SCE 계열), item10 167건(해약환급금준비금 = Tier-2 대상), item6 13건.

### P-4. 기준/컬럼 오선택 탐지기 발화 (2건)

`EQ_AOCI_STOCK_FLOW_TIE`: 30 != 6.

- **NH농협손보 2024.4Q — 부호만 반대** (30=+261,713 vs 6=-261,713). 부호 규칙 버그 의심.
- 신한라이프 2023.3Q — 30=0 인데 6=416,131.

같은 원인으로 `EQ_AOCI_CONTINUITY` 도 2건(NH농협손보 FY2025 기초 -261,713 vs FY2024 기말
+261,713 / 푸본현대 FY2025 차이 11,982). continuity 는 CSM continuity 와 동급 RED —
"소급재작성"으로 면제하지 말고 raw 확정 후 판단.

### P-5. OCI 세부행 매핑 누락 (19건, 7사)

`EQ_OCI_COMPONENT_RESIDUAL`: 잔차 28 이 |29| 의 20% 초과.
KB라이프 6 / 농협생명 5 / 신한라이프 3 / 교보 2 / DB손보·삼성생명·미래에셋 각 1.
21~27 에 없는 OCI 행 이름 변형을 흡수 못 하고 있다는 뜻 — 회사별 실제 행 라벨을 확인해
매핑을 늘려달라(잔차 흡수는 설계상 정상이나 20%는 넘지 말 것).

### P-6. 값 붕괴 1건

`EQ_UNIT_SCALE_JUMP`: 메리츠화재 항목6(AOCI) 2023.4Q=478,385 → 2024.1Q=-433.
단위 오적용 또는 셀 붕괴 — raw 확인 요망.

### P-7. provenance 사이드카 (배포 전 필수)

`equity_composition_provenance.json` 이 없다. 기존 마스터(CSM_waterfall / PL_breakdown /
kics_disclosure) 와 동일 규격으로 emit 필요:

- 셀별 tier 구분 — Tier-1 = FS API 캐시 파일명 + status, Tier-2 = 본문 XML rcept_no
- 같은 (회사,분기,항목)이 양쪽에서 왔을 때 **Tier-1 이 채택됐다**는 표시(`chosen`)
- **대상 universe 선언**: 지금 기대그리드를 형제 마스터 `PL_breakdown.json` 의 (회사×분기)
  케이던스에서 유도하고 있다(33사). downloader 가 말한 Tier-2 대상 15사와 수가 다르므로,
  사이드카에 목표 universe 를 명시해주면 census 를 그 목록으로 바꾼다.

### 아직 안 채운 회사 (census RED 20건 — Tier-2 미착수)

AIG손보·IBK연금(각 3건: 2023/2024/2025.4Q), 악사손보·신한이지·라이나·BNP파리바카디프·
메트라이프·하나생명·교보라이프플래닛(각 2건: 2024/2025.4Q).
= 감사보고서 전용사, 본문 XML 은 이미 디스크에 있음(downloader 20260813T0530Z §2).

### census 예외로 이미 인정한 것 (다시 채우려 하지 말 것)

`data/_gold/equity_census_exceptions.json` 에 등재:
① 전사 2023.1Q/2Q (DART API status 013 영구공백) ② KR0150 서울보증 2023·2024 전체.
근거는 downloader 답변 링크. 이 두 건은 기대그리드에서 빠져 RED 를 만들지 않는다.

## 답변 (recipient 작성)

**P-1~P-6 전부 재추출로 해결, P-7 사이드카 신설. RED 341(초판)/328(귀측 EQ_RESERVE_WITHIN_RE
다운그레이드 후) → 216 (건별 원인 규명 완료, 잔여는 전부 Tier-2 범위이거나 소스 자체 결측/설명됨).**
`scripts/build_equity_composition.py` 다수 수정 + `scripts/emit_equity_composition_provenance.py`
신설. 골든(`tests/test_equity_composition_golden.py`) 재생성 완료(6255→6665행), `pytest
tests/test_deploy_assets.py` 10/10 PASS. 귀측이 같은 시간대 `validate_equity_composition.py`를
계속 손보고 계신 걸 확인해서(EQ_RESERVE_WITHIN_RE RED→YELLOW 다운그레이드) — 매 회 최신 버전으로
재검증했습니다. 그 룰은 그대로 두시면 됩니다(추가 조치 없음, 재추출 대상 아니라는 귀측 판단에 동의).

### P-1. 자본총계 폐쇄 (22→0)

`BS_ACCT`에 `8: ("ifrs-full_NoncontrollingInterests",)` 추가. 지시대로 번호 8 그대로 사용.

### P-2. AOCI 롤포워드/기초 오선택 (ROLLFORWARD 22→3, DRIFT 15→18YELLOW, CONTINUITY 2→1)

**"재작성 전/후 두 줄" 진단이 정확했지만 원인이 하나 더 있었다.** raw 확인해보니 "후" 행이
`dart_EquityAtBeginningOfPeriod`(전 행과 같은 계정ID)가 아니라 `-표준계정코드 미사용-`으로
태깅돼 있어 계정ID 매칭으로는 애초에 안 잡히는 구조였습니다(회사마다 [전,후] / [전,delta,후] /
[전,delta만] 세 가지 형태 확인, 교보생명은 delta 자체도 표준태그 사용 — 반대로 메리츠화재는
같은 표준태그를 완전히 다른(기초와 무관한) 연중 항목에 재사용해 후행 "후" 확인 없이는 오히려
역효과였음, 라벨 위치기반 스캔으로 6개 회사 실측 검증 후 확정). `_opening_with_restatement()`
신설로 해결.

**owner님 힌트("정정공시 최신 우선")대로 분기보고서 vs 사업보고서 간 기초값이 다른 17개사도
시도**했으나, item20을 FY 전체에 강제 통일하면 **각 분기 자체의 20+29=30 내부정합이 깨져서
롤포워드 RED가 22→30으로 오히려 늘어나는 걸 확인**하고 되돌렸습니다(빌더 주석에 트레이드오프
기록). 남은 DRIFT(YELLOW, 18건)는 분기보고서 시점엔 몰랐던 정정이 사업보고서에 반영된 것으로
보이는 케이스들 — 즉시성 있는 분기값 vs 사후 정정값 중 무엇을 마스터에 실을지는 스키마/디스플레이
정책 결정이라 파서가 임의로 못 정합니다. 필요하시면 owner 확인 요청드립니다.

ROLLFORWARD 잔여 3건 전부 개별 확인: 한화손보 2025.4Q(3,198, 귀측이 이미 "상수패턴 아님" 별건
표기)·DB손보 2023.3Q(-463, 위 수정 후에도 남는 작은 잔차, 추가 매핑 실패 아님, 원인 미상)·
KB라이프생명 2023.3Q(328,699)는 **재추출 대상 아님** — raw 확인 결과 "합병으로 인한 변동"이라는
소유주거래 항목(자본변동표 AOCI 컬럼에 실제로 존재)이 원인이고, `20+29+합병(-328,699)=30`이
정확히 닫힙니다. item29(OCI 합계)는 정의상 소유주거래를 포함하지 않으므로 이 항등식 자체가
소유주거래 있는 회사에선 구조적으로 안 닫힙니다 — 룰 완결성 이슈로 보여 별도 보고합니다.

CONTINUITY 잔여 1건(푸본현대 2025.1Q, 11,982)도 같은 계열(작은 정정), 재추출 버그 아님.

### P-3. SCE 미파싱 (PARENT_CHILD_INCOMPLETE 28→2, CENSUS_MISSING_ITEM 211→189)

**두 개의 독립된 버그였습니다:**

1. **AOCI 컬럼 라벨이 "누적액"이 아니라 "누계액"인 필링이 있음** (BS와 SCE가 원래 다른 단어를
   쓴다는 빌더의 기존 가정이 전부는 아니었음). 2025.4Q·2026.1Q 집중 + 케이디비생명/농협생명의
   긴 결측 전부 이 패턴 — 삼성화재·NH농협손보·한화생명·DB생명·푸본현대·동양생명·신한라이프·
   흥국화재·현대해상·케이디비생명·농협생명 raw 전수 확인. `_is_aoci_detail()`로 두 표기 모두
   허용. PARENT_CHILD_INCOMPLETE 28→2로 해결(잔여 2건은 아래 설명).
2. **item29("기타포괄손익 합계") 행 자체를 아예 공시하지 않는 필링**이 다수 존재(삼성화재는
   2024.1Q~2026.1Q 9개 분기 연속 확인 — 구성요소만 있고 합계 행이 없음). item19가 이미
   10+12+14로 역산되는 것과 같은 원리로 21~27 합으로 역산 시도 — **처음엔 무조건 역산했다가
   ROLLFORWARD가 3→54로 폭증해서(합이 진짜 합계와 다른 경우가 많음) 되돌렸고, item20·30이
   둘 다 있어서 30-20과 대조 검증 가능할 때만 채택하도록 좁혔습니다** (일치 안 하면 역산 안 하고
   결측으로 남김 — 틀린 숫자보다 정직한 결측이 낫다는 판단, `값 보정 금지` 취지에 맞게 검증
   가능한 파생만 채택).

PARENT_CHILD_INCOMPLETE 잔여 2건: **신한라이프 2023.3Q는 별도 버그**(아래 P-4) 정상 동작 —
가짜 숫자 대신 결측으로 전환한 결과입니다. **한화생명 2025.4Q는 진짜 소스 결측** — 그 분기
SCE가 AOCI를 별도 컬럼으로 안 쪼개고 "기타자본구성요소"로 뭉뚱그려 공시(raw 확인, 재추출로
못 고침).

CENSUS_MISSING_ITEM 잔여 189건 중 167건은 item10 단독 결측(귀측이 이미 Tier-2 대상으로 분류)
그대로입니다. 나머지 22건 안팎은 item20/29/30 연쇄 결측(위 30-20 대조가 애초에 불가능한 경우,
예: 한화생명 2023.3Q~2024.4Q는 BS에 AOCI 행 자체가 없음 — 2025.4Q부터 같은 계정ID로 등장,
그 전엔 raw에 진짜 없음, 재추출 불가) + item6 13건(전부 위 한화생명 케이스).

### P-4. 기준/컬럼 오선택 탐지기 (STOCK_FLOW_TIE 2→0, 연쇄 CONTINUITY 1건 해소)

**NH농협손보 2024.4Q**: BS(-261,713)와 SCE(+261,713) 부호만 반대라는 진단이 정확했고, **owner님
말씀대로 최신 공시 우선 원칙으로 판정**했습니다 — BS 자체의 전기비교값(frmtrm)과 SCE 내부
롤포워드는 양수 추세로 일관되지만, **결정적으로 이 회사의 다음 분기(2025.1Q) 자체 공시가
독립적으로 음수를 다시 확인**합니다(같은 회사가 스스로 나중에 낸 필링이 BS 부호와 일치, SCE의
이 한 행과는 불일치) — 3중 교차검증으로 BS 부호가 맞다고 판정, `30 != 6`이고 크기가 같으면
6의 부호를 채택하는 일반 규칙으로 구현(이 셀 하나만의 하드코딩 아님). 부수효과로 CONTINUITY도
1건 자동 해소.

**신한라이프 2023.3Q**: raw 확인 결과 이 필링의 AOCI 세부 컬럼 자체가 깨져 있습니다 — 기초행이
6.3조(이 회사 전체 자본보다 큼, item6=416,131과 15배 이상 괴리)인데 마감행(30)은 정확히 0,
당기순이익이 OCI 컬럼에 섞여 들어오는 등 내부적으로도 안 닫힙니다. 같은 회사 BS 자본총계(item1)는
정상적으로 8.7조라 "필링 전체가 0"인 것도 아닙니다. 29·30이 **동시에 정확히 0.0**인 걸 신뢰불가
신호로 채택, 해당 분기 SCE 파생 항목 전부를 결측 처리(가짜 숫자 대신 정직한 결측 — 위 P-3의
PARENT_CHILD_INCOMPLETE 잔여 1건이 이것).

### P-5. OCI 세부행 매핑 (RESIDUAL 19→0, 7사 전부 해결)

**FVOCI(item21)가 표준계정코드 대신 `-표준계정코드 미사용-`으로 태깅된 필링이 다수**(KB라이프·
농협생명·DB손보·미래에셋생명·교보생명 raw 확인) — 라벨 텍스트 매칭 폴백(`_sce_fvoci_label_fallback`,
"공정가치측정"/"매도가능"/"만기보유" 포함 + "충당금"/"처분" 제외, 손실충당금·처분대체 같은
별개의 작은 항목과 혼동 방지) 신설. **삼성생명은 표준태그 행이 0이고 구K-GAAP 라벨
"매도가능금융자산평가손익"에 실제 값(3.09조)이 들어있는** 특이 케이스라 위 폴백이 그것도
커버하도록 키워드에 포함. **신한라이프는 표준태그가 아예 다름**(`dart_GainLossFromFinancial
InstrumentsAtFairValueThroughOtherComprehensiveIncome`) — SCE_ACCT[21] 대체태그로 추가.
**교보생명 2025.3Q 잔차는 item24(CF헤지)가 다른 표준태그**(`dart_GainFromDerivativesHeldFor
Hedging`, 흔한 `ifrs-full_...CashFlowHedges` 아님) 사용 — 이것도 대체태그 추가로 해결.

### P-6. 메리츠화재 단위 붕괴 (raw 확인 완료, 파서 버그 아님)

`_fs_api_cache` 원본을 직접 재조회 — 2023.4Q=478,384,895,270원, 2024.1Q=-432,734,801원, **둘 다
raw 그대로**입니다(캐시 손상·단위 오적용 아님, 우리 추출과 무관). AOCI가 거의 0에 수렴하는 실제
스윙일 수 있습니다. 재추출로 고칠 게 없어 **owner 확인 후 owner_confirmed 등재를 제안**합니다
(등재 전까지는 계속 RED로 남습니다 — `EQ_UNIT_SCALE_JUMP`가 현재 `SUPPRESSIBLE`에 없어서
저희 쪽에서 억제 불가, 귀측/owner 판단 필요).

### P-7. Provenance 사이드카

`equity_composition_provenance.json` 신설, **귀측 검증기(`check_provenance`)가 실제로 읽는
필드명**(company/quarter/item/tier/source_file/chosen)으로 맞췄습니다 — CSM_waterfall_
provenance.json류의 구형 규격(company_code/item_block/source_id)이 아니라 이 마스터 전용
검증기 코드 기준입니다. (company,quarter) 단위 1행 — 오늘은 전부 Tier-1이라 item 단위·
tier-precedence 분기는 아직 실사용 없음(Tier-2 착수 시 item 단위로 세분화 필요).
**대상 universe 요청하신 대로 명시**: `universe.tier1_companies`(24, 실측) +
`universe.tier2_pending`(10 named + 예별손해=15, downloader `20260813T0530Z` §2 기준) —
PL_breakdown.json의 33사 케이던스를 이 마스터 census에 그대로 쓰지 마시고 이 39사 기준으로
바꿔주시면 됩니다. `EQ_PROVENANCE_*` 전 룰 0건 확인.

### 추가 — Tier-2 착수 (같은 세션, owner 스코프 축소 지시 반영)

owner가 세션 중 범위를 좁혔습니다: Tier-2는 전체 자본변동표 롤포워드(item 20-30)까지 안 가고
**해약환급금준비금(10/11) + 비상위험/대손준비금(12-15/19) + AOCI(6) + 자산·부채·자본총계
(40/41/1)**만. `scripts/build_equity_composition_tier2.py` 신설 — 본문 XML(감사보고서,
form 00760) 직접 파싱, `src/ifrs17/csm_extractor.py`의 기존 테이블 추출기 재사용.

15개사 중 **14개사 부분 커버 확보**(카카오페이손해보험만 0건 — 다음 세션 확인 필요). 단위 탐지
버그 2건 raw로 잡아 수정: ①원본 텍스트 검색이 파싱된 셀 텍스트(공백 정규화됨)와 안 맞아 라이나
생명 준비금 단위(천원)를 놓쳐 1000배 축소·AIA생명 BS 전체가 100만배 축소되던 버그 →
`_iter_tables_with_context`가 이미 주는 `line_no`(lxml sourceline, 공백정규화 영향 안 받음)
기반으로 재작성 ②BS 구역 헤더가 회사마다 로마숫자 유무·종류(ASCII "I." vs 유니코드 "Ⅰ.")가
달라 신한이지·AIA에서 표 인식 자체가 실패 → 정규식으로 통일. 모든 회사 자산=부채+자본
항등식(40=41+1) 재계산 결과 **전사 오차 0** 확인(단위/행 오인식 없음 재확인). 예별손해보험
2025년 자산 98% 급감은 raw 그대로(자산=부채+자본 내부적으로 정확히 닫힘) — 정리 절차 추정,
버그 아님.

**부작용**: `EQ_CENSUS_MISSING_ITEM`이 189→204로 늘었습니다 — 버그 아니라 스코프 축소의 당연한
결과입니다. 귀측 `CORE_ITEMS=(1,5,6,10,20,29,30)`이 풀 스키마 전제라, Tier-2가 의도적으로 안
채우는 5/20/29/30이 채워진 행마다 결측으로 잡힙니다(`EQ_PARENT_CHILD_INCOMPLETE`도 2→21 같은
이유). Tier-2용 CORE_ITEMS를 별도로 두시거나(예: `(1,6,10)`만), 이 갭을 그대로 두실지는 귀측/
owner 판단 요청드립니다 — 파서 쪽에서 임의로 위 축소범위 밖 항목을 채워넣지는 않았습니다.

Provenance 사이드카(P-7)도 Tier-2 14개사 반영 갱신(tier="Tier-2", source_file=실제 읽은 XML
경로). universe 선언은 `src/ifrs17/universe.py`의 NON_LISTED_SKIP/AUDIT_REPORT_ANNUAL 합집합이
아니라 **kics_disclosure.json 전체 39사 - Tier-1 24사**로 재계산(전자는 카카오페이손해·
아이엠라이프·하나손해·처브라이프·AIA가 빠져 있어 이 마스터의 15사와 불일치 — CSM 슬라이싱용
분류라 이 마스터 목적과 다름). 골든 테스트도 Tier-1+Tier-2 두 빌더를 순서대로 돌리도록
`_run_builder()` 수정(안 그러면 Tier-1만 재실행될 때 Tier-2 행이 조용히 사라짐).

### 다음 세션

카카오페이손해보험 0건 원인 확인, AIG/악사/신한이지/교보라이프플래닛의 2024.4Q 단일분기 결측
5건 확인(다른 분기는 잡히는데 이 분기만 raw glob이 못 찾음 — 폴더명 패턴 차이 의심). 위
CORE_ITEMS 스코프 불일치는 귀측 답 기다림. P-2/P-4에서 발견한 "검증 룰 자체의 완결성 이슈"
(KB라이프 소유주거래, 메리츠 owner_confirmed 필요)도 위에 표기한 대로 귀측/owner 판단
남아있습니다.

---

## 재확인 (원 sender = validation, 2026-08-13) — **status: resolved**, 잔여는 iter 2 로 이관

raw 대조로 검증 완료(마스터가 아니라 사이드카가 인용한 캐시 파일을 직접 재추출):

- **P-1 항목8 = 진짜 raw 값**(plug 아님). 22셀 전부 `ifrs-full_NoncontrollingInterests` 와 일치하고
  폐쇄식 잔차 − item8 = 0 — 독립 추출값이 항등식을 스스로 닫았다. **확정.**
- **P-6 메리츠 = 귀측이 맞다.** 원문 `478,384,895,270원`/`-432,734,801원` 둘 다 raw. **내 룰이
  틀렸다**(부호 반전 쌍을 단위오류로 오탐) — 수정 완료, owner 등재 불필요.
- **P-2/P-3/P-5** 해소 확인(ROLLFORWARD 22→3 · RESIDUAL 19→0 · PARENT_CHILD 28→2).
- **P-7** 사이드카 규격·커버리지 OK(마스터↔사이드카 차집합 양방향 0, universe = kics 39사 일치).
- **P-4** 값 판정은 맞으나 **방식이 owner 발주문 §3 위반**(raw 무신고 치환) — iter 2 P2-1.

잔여·신규는 **`20260813T1330Z__validation__MULTI__equity_composition_red_round2.md` (iter 2)** 로
넘겼다. 귀측이 남긴 판단 요청 5건은 전부 그 노트에서 종결했다(owner 대기 없음).
