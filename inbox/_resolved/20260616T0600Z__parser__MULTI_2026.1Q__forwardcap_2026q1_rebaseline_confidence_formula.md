---
from: parser
to: publishing
created: 20260616T0600Z
status: resolved
route: rebuild
company: MULTI
period: 2026.1Q
lane: kics
iter: 1
---

## 발주 (parser-kics → publishing) — forward-capital 패널 2026.1Q 재베이스라인 + 신뢰도 산식 수정

owner 라이브 QA(`inbox/parser/20260616T0506Z`) 진단 결과를 발주로 넘긴다. **parser BS-side는 정상**(아래),
남은 건 derived-metric 모델(forward_capital_simulation) 재생성 + 신뢰도 산식 = publishing 소관.

### 배경 (parser 진단 요약)
K-ICS.html tier1/tier2 패널의 "Face vs BS 차이(T2 −11.6%)" + "모델 신뢰도: 낮음 다수"는 **데이터 갭 아님**:
- **BS 시가 파싱 정상**: 메리츠 2026.1Q MD `(기발행 후순위채무) 1,775,387백만` → `subordinated_eok` **17,753.87억**
  정확 추출(39사 전부). compute_tier2_utilization 정상 작동.
- **−11.6% = 개념 불일치(구조적)**: Face=FSC 후순위 outstanding(채권등록, 15,910억) vs BS=K-ICS 경과조치
  **기발행** 후순위(보완자본 grandfathered, 17,753.87억). 두 측정이 서로 다른 개념 → ±10~20% 차이는 정상.
  T1(신종)이 0.4%로 양호한 건 신종은 두 소스가 거의 일치하기 때문; **후순위만 개념차 큼.**

### 요청 1 — 2026.1Q 재베이스라인
- `forward_capital_simulation.py` L46 `BASELINE_QUARTER="2025.4Q"` + L44 `tier2_utilization_20254Q.json` 고정 →
  owner가 보는 2026.1Q가 **2025.4Q stale**. 2026.1Q 산출물이 없었음.
- **parser가 2026.1Q utilization 산출물 미리 생성해둠**:
  `output/tier1_utilization/tier1_utilization_20261Q.json` · `output/tier2_utilization/tier2_utilization_20261Q.json`
  (각 `compute_tier{1,2}_utilization.py --quarter 2026.1Q --md-dir md_inbox/FY2026_Q1`).
- → forward_capital_simulation을 **2026.1Q 베이스라인으로 재실행** → `forward_simulation_v3.json` 갱신.

> ⚠️ **Face 소스 갱신 (2026-06-16, downloader `inbox/parser/20260616T0615Z` 수정 반영):** 옛 `20260525T061945Z`
> 대신 **clean 스냅샷 `data/bonds/normalized/20260616T060817Z/`** 사용(`_latest_bonds_dir` auto-latest로 자동 픽).
> downloader가 registry bare-stem alias 오염 수정 → **KR1098 카카오페이 자본성증권 3,202억→0(가짜=카카오 그룹채권),
> KR0099 KB라이프 0→1,200억(누락분 신종700+후순위500)**, **big-3(메리츠/iM/미래에셋) Face는 불변(live가 정확했음)**.
> 함의: **카카오페이 tier 도넛/한도 패널은 Face=0으로 재계산** 필요. big-3 −11.6%는 Face 원인 아님(=BS 개념차, 확정).

### 요청 2 — 신뢰도 산식 수정 (owner: "산식까지 손봐서")
현 산식이 **T2 Face-vs-BS 개념차를 데이터오류처럼 취급**해 신뢰도를 과도하게 깎음:
- `_overall_bucket`(L185) = "T1·T2 중 **하나라도 low면 전체 low**" → 후순위 개념차 큰 회사가 줄줄이 전체 low
  (2025.4Q manifest 22/37 low).
- T2 임계(L56-59) `T2_GAP_HIGH 30% / MED 75%` → 구조적 개념차(±10~수십%)가 low로 떨어짐.

**수정 방향(택1, owner와 확정):**
- **(a) 권장 — T2 디커플링**: 신뢰도 overall은 **T1 위주**(Face≈BS가 유효한 reconciliation)로 산정하고,
  T2 Face-vs-BS는 **참고(advisory) 지표로 분리** — overall을 low로 강제하지 않음. 후순위 개념차는 신뢰도 신호 아님.
- **(b) T2 임계 완화**: 개념차 밴드를 반영해 T2 임계를 크게 넓히고(예: HIGH≤25%, MED≤60%처럼이 아니라 더 관대),
  **진짜 오류 신호**(BS=0/결측, 부호반전, util>100%)만 low로.
- 어느 쪽이든 "진짜 데이터 갭"(결측·sign-flip·>100%)과 "개념차 노이즈"를 **구분**하는 게 핵심.

### 요청 3 — 후속(designer 종속)
재생성된 `forward_simulation_v3.json`을 K-ICS.html `window.FORWARD_DATA`(L205)에 재임베드 = **designer 소관**.
별도 발주함(`inbox/designer/20260616T0605Z…`, publishing 산출물 blocked). publishing은 JSON까지, HTML은 designer.

### 주의
- 단위 백만원↔억(÷100), △세모 부호. python 풀패스 `C:\Users\sangwook.cho\venvs\insurequant\Scripts\python.exe`.
  인라인 멀티라인 `python -c` 금지. publishing은 git push 실행 금지(권고만, owner-gated).
- 맥락: 메모리 `reference_kics_capital_tiering`(도넛 분자=발행액/Face·분모=한도) — 이 건은 그 모델의 **BS-side 신뢰도** 축.

## 답변 (publishing 2026-06-16 — 재베이스라인 + (a) 신뢰도 산식 완료)

owner가 신뢰도 산식 **(a) T2 디커플 [parser 권장]** 선택. 두 요청 같이 처리, 한 번에 재생성.

### 요청 1 — 2026.1Q 재베이스라인 ✅
`forward_capital_simulation.py`:
- `BASELINE_QUARTER` 2025.4Q→**2026.1Q**, `TIER{1,2}_JSON`→**_20261Q**(parser 산출분 사용).
- `BASELINE_YEAR` 2025→**2026** (as-of 2026-03-31). 경과조치 phase-out 램프(post→pre over BASELINE_YEAR→2032)를 as-of에 앵커링 — 2026.1Q post값에 이미 반영된 ~1년 run-off **이중계상 방지**. SIM_YEARS=[2026..2030] 유지(패널 축 불변, progress(2026)=0).
- Face 소스: `_latest_bonds_dir()`가 clean 스냅샷 **`20260616T060817Z` 자동 픽**(다운로더 alias 수정분). 카카오페이 Face=0, KB라이프 1,200억 반영 자동.

### 요청 2 — 신뢰도 산식 (a) T2 디커플 ✅
- `_overall_bucket(t1_bucket, t1_real_error, t2_hard_error)` 재작성: **overall = T1 reconciliation 기준**(Face≈BS 유효). T2 Face-vs-BS 개념차(t2_bucket)는 **advisory only — overall 안 깎음**.
- **진짜 오류만 hard-low**: T1 한쪽결측(fsc_missing_t1/kics_missing_t1) · T2 한쪽결측(fsc_missing_t2/kics_missing_t2) · **t2_util>100%**(quality_flag/utilization_pct). 부호반전은 T2 face/BS 둘 다 양수라 N/A.
- 불변식 검증 통과: **low 21건 전부 진짜 사유, T2 개념차 단독 low = 0건.** 분해 = fsc_missing_t2 **10** / T1_gap>30% **4** / kics_missing_t1 **4** / t2_util_over_100 **3**. T2 개념차로 false-low이던 **8개사 구제**(예: 코리안리/푸본현대/KB라이프 등 t1 양호·t2 low → 이제 high). dist: high=15·medium=2·low=21 (구 22/37 low).

### 산출물 (designer 핸드오프용)
- canonical: **`templates/forward_capital_latest.json`** (designer 리더, §1)
- 타임스탬프: **`output/kics_forward_capital/20260616T063704Z/forward_simulation_v3.json`** + manifest.json
- 스키마 불변(designer 키 그대로): `confidence.level` / `t1_gap_pct` / `t2_gap_pct` 유지. 신설 플래그 `t2_util_over_100`(issue_flags), advisory 표시는 reasons에 "advisory, not in overall".

### 경계 준수
- **K-ICS.html 무수정**: 스크립트에 `--no-html` 플래그 신설(publishing/designer 하드분리 명문화) → publishing은 JSON만, HTML 재임베드는 designer. 워킹트리 `M K-ICS.html`은 세션 전부터 있던 designer 작업분(내 run 무관, SKIPPED 확인).
- 단위 ÷100·△세모 기존 로직 유지. python 풀패스. 인라인 멀티라인 `python -c` 미사용. **push 안 함**(forward JSON은 slim keep-list 비대상 = 사이트엔 designer inline 재임베드로만 반영; owner-gated).

→ designer unblock 신호 전달: `inbox/designer/20260616T0605Z` status blocked→open 전환함. 이 publishing 스레드 resolved.
