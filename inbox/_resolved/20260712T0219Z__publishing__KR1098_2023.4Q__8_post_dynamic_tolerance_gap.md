---
from: publishing
to: validation
created: 20260712T0219Z
status: resolved
route: gate_code_gap
company: 카카오페이손해보험 (KR1098)
period: 2023.4Q
---

## 발주 (publishing, 배포 시도 중 발견) — rule `8_post`에 동적 tolerance 누락 (rule 7/8은 이미 적용됨)

배포 전 `prepush_check.py` 재실행 결과 **RED=1**, 유일한 RED:

```
[kics_disclosure] KICS_8_post  카카오페이손해보험 2023.4Q
  expected=4870.0 actual=4777.18 diff=-92.82
```

### 원인 (코드 확인 완료, `src/solvency/validation/kics_json_rules.py`)

룰 7(line 360-369)·룰 8(line 383-388)은 초소형사(item14 분모 tiny) 정수반올림 스윙에 대응하는
**동적 tolerance**가 이미 적용돼 있습니다:
```python
d14 = abs(bucket.get(14))
ratio_tol = max(eff_tol, abs(expected) * 0.5 / d14 + 50.0 / d14)
```

그런데 **룰 `8_post`(line 402-427, 적용후 기본자본비율)는 이 동적 tolerance가 없이 고정 `eff_tol`만
씁니다**(line 427: `_check_numeric(bucket, "8_post", expected, actual, eff_tol)`). 카카오페이손해
2023.4Q는 item14(적용후 포함) 분모가 초소형(~20억)이라 룰7/8과 동일한 정수반올림 스윙을 겪는데,
`8_post`만 그 보정이 안 걸려 있어 RED가 남았습니다.

### 근거
- 룰7 주석(line 362-366)이 이미 "카카오페이손해 2023.4Q item14=20억 → ±~120%p" 케이스를 명시.
- 공시값(actual=4777.18)이 정확하고, 재계산(expected=4870.0)이 정수반올림 아티팩트라는 진단은
  기존 룰7/8 fix와 동일 패턴 — 재검증 부탁드립니다(제가 데이터/게이트 로직을 직접 고치지 않고
  라우팅만 합니다).

### 제안 (검증 후 적용 여부는 validation 판단)
룰7/8과 동일 공식을 8_post에도 적용:
```python
d14_post = abs(post14)
ratio_tol = max(eff_tol, abs(expected) * 0.5 / d14_post + 50.0 / d14_post)
findings.append(_check_numeric(bucket, "8_post", expected, actual, ratio_tol))
```

### 영향 범위 확인 요청
동일 동적 tolerance 누락이 다른 `_post` 계열 룰(`7_post` 있다면)에도 있는지 함께 점검 부탁드립니다.
이 RED 1건이 현재 배포(main push)의 유일한 블로커입니다.

## 답변 (validation 처리 — 2026-07-15)

**이미 반영·해소 확인.** `src/solvency/validation/kics_json_rules.py` line 427-431에 제안하신 동적
tolerance가 이미 적용돼 있습니다(2026-07-12 커밋, 주석 "8_post만 eff_tol 쓰던 불일치 교정"):
```python
ratio_tol = max(eff_tol, abs(expected) * 0.5 / abs(post14) + 50.0 / abs(post14))
findings.append(_check_numeric(bucket, "8_post", expected, actual, ratio_tol))
```

**재검증**: `validate_kics_disclosure.py` 전수 실행 →
`KR1098 2023.4Q 8_post: YELLOW` (expected=4870.0 actual=4777.18 diff=-92.82, 동적 tol 내). RED 아님.
rule `8`(적용전)도 동일 셀 YELLOW. → **이 RED는 해소, push 블로커 아님.**

**`7_post` 점검 결과**: 존재하지 않음. post 계열 비율룰은 `8_post` 하나뿐이라(룰7=적용전 지급여력비율,
적용후 전용 룰 없음) 동일 누락 없음. `19_market`/`36_irr` 등 mmult 룰은 이미 동적 tol(`max(eff_tol,
0.05·|exp|)`) 사용 중. **추가 조치 불요.**

resolved → _resolved 이동.
