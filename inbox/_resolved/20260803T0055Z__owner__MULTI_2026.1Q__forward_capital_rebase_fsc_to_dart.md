---
from: owner
to: parser
created: 20260803T0055Z
status: resolved
route: reparse
company: MULTI
period: 2025.4Q~2026.1Q
lane: ifrs17
iter: 1
---

## 미결 (owner) — `kics_forward_capital.json`의 발행채권 소스를 FSC(data.go.kr) → DART per-bond로 갈아타기

**배경.** 다운로더 5대 소스 중 `bonds`(FSC data.go.kr 자본성증권 API `15059611`, `src/bonds/fsc_client.py`)를
폐지하려고 실사용을 전수 조사했다. 결과:

| 소비처 | 현재 소스 | 상태 |
|---|---|---|
| tier1/tier2 소진율 도넛 (K-ICS.html 라이브) | **DART 사업보고서 per-bond** | ✅ 2026-06-20 owner 결정으로 이미 교체 완료 (`scripts/wire_capital_securities_to_utilization.py:28`) |
| **`kics_forward_capital.json` (K-ICS.html 라이브)** | **FSC `bonds_by_insurer.json`** | ❌ **여기만 FSC에 남아있음 — 이 발주 대상** |

즉 **FSC 소스를 살려두는 이유가 이 파일 하나**다. 이걸 DART로 옮기면 `bonds` 소스를 폐지할 수 있다.

**현재 배선 (정확한 지점):**
- `scripts/forward_capital_simulation.py:116` — `load_outstanding_bonds()`가
  `data/bonds/normalized/<최신stamp>/bonds_by_insurer.json`(= FSC 정규화 산출물)을 직접 읽고
  `status == "outstanding"` 만 필터.
- 산출: `scripts/forward_capital_simulation.py:467` → 루트 `kics_forward_capital.json` (38사, 21분기 콜 roll-off).

## 요청

### 1. 소스 교체
`load_outstanding_bonds()`의 입력을 **DART per-bond**로 바꿀 것. 권장 입력은 집계본이 아니라 **per-bond 원본**:

- **권장**: `data/bonds/capital_securities_fy2025.json` (DART FY2025 사업보고서 per-bond, 24사).
  필드가 이미 시뮬레이션이 필요한 것 전부를 갖고 있음 —
  `issue_date` / `legal_maturity` / `call_date` / `call_source` / `face_amount_mn` / `outstanding_mn` /
  `past_call_outstanding` / `tier(hybrid|subordinated)`.
  → 기존 스키마(`status` / `effective_call_date` / `issue_amount_won`)로 어댑터 한 겹만 두면
  **시뮬레이션 로직(콜 roll-off·한도·경과조치)은 손대지 않아도 된다.**
- 참고(대안): `data/bonds/capital_securities_forward_outlook.json` — 이미 DART 기반으로 만들어둔
  21분기 시계열 집계본(23사, `assumption: "redeem at CALL(~5y); past_call_outstanding bonds held"`).
  다만 사별·회차별 per-bond 해상도가 없어 시뮬레이션 내부 로직을 바꿔야 함. **비권장.**

### 2. 커버리지 갭 2사 선(先)해소 — 이거 안 하면 조용히 2사가 빠진다
발행잔액 있는 회사 대조 결과:

- FSC 24사 / DART 23사
- **DART 미수록**: `KR0050` 하나손해보험, `KR0076` 아이엠라이프생명보험 ← **추출 갭. 반드시 메꿀 것.**
  (둘 다 FSC에서는 발행잔액이 잡히는 회사 = DART 쪽 추출 누락 의심)
- FSC 미수록 / DART만 있음: `KR0004` 예별(구 MG) ← DART 쪽이 더 정확. 교체 시 자동 이득.

→ 하나손보·iM라이프의 DART 사업보고서 "증권의 발행을 통한 자금조달에 관한 사항" /
사채발행내역 / 미상환잔액 주석에서 per-bond 추출 후 `capital_securities_fy2025.json`에 편입.
**추출 불가면 이유를 `## 답변`에 명시** (무발행 확인이면 그것도 결론 — 다만 FSC에 잡힌 근거와 대조해서 판정).

### 3. as-of 라벨 정합 (validation과 물려있음)
- 현 사이드카 `kics_forward_capital_provenance.json`은 `as_of_date: 2026-03-31`(=2026.1Q)로 선언.
- 그런데 산출 JSON의 baseline 키는 `baseline_2025_4Q`, DART per-bond는 `as_of: 2025-12-31`(=2025.4Q).
- `scripts/validate_data_contract.py:419-429`가 **as_of 분기 ≠ 셀 분기면 `STALE_AS_OF` RED**를 낸다.
  → 교체 후 어느 쪽이 정본인지 확정하고 사이드카를 그에 맞춰 재발행할 것.
  **판단이 갈리면 validation inbox의 동반 발주(`20260803T0056Z__owner__MULTI__capsec_provenance_source_id_dart.md`)와
  맞춰서 결정** — 두 발주는 같은 게이트를 건드린다.

## 제약 (하지 말 것)

- `kics_forward_capital.json`은 **라이브 배포 에셋**이다 (K-ICS.html이 fetch, keep-list는
  `tests/test_deploy_assets.py:189`). 스키마 필드 삭제·이름변경 금지 — HTML이 조용히 빈칸 된다.
- `data/bonds/` 디렉토리 자체는 지우지 말 것. FSC 산출물(`raw/`, `normalized/`)과
  DART 산출물(`capital_securities_*.json`, `disclosure/`)이 **같은 디렉토리에 섞여 있다.**
- `src/bonds/`·`scripts/ingest_fsc_bonds.py`·`scripts/normalize_bond_schedule.py` 정리는
  **downloader 발주(`20260803T0057Z`)** 소관 — 여기서 건드리지 말 것.

## 완료 조건 (verify)

1. `python scripts/forward_capital_simulation.py` 재실행 → `kics_forward_capital.json` 갱신,
   **발행잔액>0 회사 수 ≥ 24사** (하나손보·iM라이프 포함, 예별 추가).
2. `python scripts/validate_data_contract.py` → **RED = 0**.
3. `pytest tests/test_deploy_assets.py` 통과.
4. `data/bonds/normalized/**` 를 읽는 코드가 `forward_capital_simulation.py`에서 **완전히 사라짐**
   (grep으로 확인. 남아있으면 downloader 발주가 블록된다).
5. 교체 전/후 사별 `ratio_pct` 시계열 diff를 `## 답변`에 요약 — 큰 이동은 근거와 함께.

근거 메모리: `reference_capital_securities_utilization`, `reference_tier2_utilization_provenance`.
선행 결정 이력: `inbox/_resolved/20260620T0238Z__owner__MULTI__capital_securities_issuance_from_dart.md`.

## 답변 (parser/ifrs17 2026-08-03 — 소스 교체 완료·RED=0, 커버리지 2사는 downloader 발주)

### 1. 소스 교체 — ✅ 완료
`scripts/forward_capital_simulation.py::load_outstanding_bonds()`를 `data/bonds/capital_securities_fy2025.json`
(DART per-bond, 권장안 그대로) 직독으로 교체. 어댑터 한 겹만 추가, `simulate_one()`/`compute_confidence()`의
콜 roll-off·한도·경과조치 로직은 무변경:
- `tier`: `hybrid`/`subordinated` → `tier1_hybrid`/`tier2_subordinated`
- `issue_amount_won` ← **`outstanding_mn`**(×1e6), `face_amount_mn` 아님 — 부분상환분(FX 후순위채
  등 34/119건 face≠outstanding)까지 반영한 "실제 투자자 지급액 기준"(2026-05-26 owner directive)에
  더 정확히 부합. `outstanding_mn==0`(당기 중 전액상환, 예: 동양생명 구舊 신종자본증권)은 드롭 — FSC
  `status=='outstanding'` 필터와 동치.
- `effective_call_date` ← `call_date or legal_maturity`. `past_call_outstanding=true`(6/119건, 콜 도래
  후 미상환)는 콜일자를 그대로 둠 — FSC 시절 동일 실물(흥국화재 KR0005 신종자본증권1, 콜 2021-12-29 도래
  후에도 outstanding 유지)이 이미 이렇게 처리돼 있던 전례를 그대로 따름(로직 변경 아님).
- `bond_coverage` enum값 `fsc_listed`/`no_bonds_in_fsc` → `dart_listed`/`no_bonds_in_dart`로 정정(필드명
  유지, 값만 — 다른 소스인데 fsc 라벨을 다는 건 이 발주 자체가 문제삼은 것과 같은 종류의 거짓 라벨이라 같이 고침).
- `data/bonds/normalized/**` 참조(`BONDS_DIR`, `_latest_bonds_dir()`) 완전 제거 — grep 확인 완료.

### 2. 커버리지 갭 2사 — 🟠 raw 부재, downloader 발주함 (자력 해소 불가)
`KR0050`(하나손해보험)·`KR0076`(아이엠라이프생명보험) FY2025 사업보고서 raw가 **디스크에 없음**
(`data/dart/FY2025_Q4|FY2024_Q4/raw/`에 dir 자체가 없고, 유일한 흔적은 `FY2026_Q1/raw/.../meta.json`의
`{"no_filing": true}` 스텁뿐 — 이건 2026.1Q 분기보고서 부재 마커라 무관). CSM_waterfall.json엔 이미
두 회사 2024.4Q/2025.4Q 행이 있어 과거엔 raw가 있었던 것으로 보이나 현재는 [[project_git_purge]]로 소실
추정. **route by raw availability 원칙대로 downloader 발주**:
`inbox/downloader/20260803T0123Z__parser__KR0050_KR0076_FY2025__capital_securities_annual_raw_missing.md`.
raw 도착 후 재추출 → `capital_securities_fy2025.json`에 편입 → 재시뮬레이션 3줄이면 닫힘(발주에 verify 명시).

교체 전/후 실측 영향(두 회사 모두 `dart_listed`→**`no_bonds_in_dart`로 역행** — 이게 안 메워지면
지금 라이브가 보여주는 숫자보다 낙관적으로 보임, 요청 ②가 우려한 그대로):
| 회사 | bond_coverage 전→후 | 2030 ratio_pct 전→후 |
|---|---|---|
| KR0050 하나손해보험 | fsc_listed → no_bonds_in_dart | 124.47 → 146.09 |
| KR0076 아이엠라이프생명보험 | fsc_listed → no_bonds_in_dart | 93.65 → 152.12 |

`KR0004`(예별) 자동 이득은 확인: `dart_listed` 전환, `outstanding_bonds_total_eok: 680.0`으로 실채권
반영됨(단 baseline capital 자체가 이미 음수라 `capacity_exhausted=true`로 표시비율은 0.0% 클램프 불변 —
숫자는 안 바뀌어도 내부적으로는 이제 정확).

### 3. as-of 라벨 정합 — 사이드카는 이미 손 안 대도 되는 상태였음 (중요 발견)
작업 중 `scripts/emit_capsec_provenance.py`가 **이미 존재**함을 발견(미커밋, validation companion
발주 `20260803T0056Z` 처리분으로 보임) — `validate_data_contract.py`에도 `source_id_for_lineage()` +
`_SOURCE_LINEAGE` 계보 테이블(`data/bonds/capital_securities_` prefix → `DART`)이 이미 배선돼 있었음.
즉 **하드코딩 `FSC_BONDS` enum 요구 → 계보-기반 `SOURCE_ID_LINEAGE_MISMATCH` 검사로 이미 전환 완료** —
발주 §요청1이 우려한 "판단이 갈리면"의 판단이 이미 validation 쪽에서 내려져 있었다.

내 쪽에서 직접 사이드카를 쓰는 코드를 추가하려다 **철회**했다 — `emit_capsec_provenance.py`가 이미
"하드코딩 금지, 계보에서 derive"를 원칙으로 하는 단일 writer라 내가 또 하나 만들면 두 writer가 서로
다른 값을 쓸 위험(이 발주 체인이 고치려는 문제를 다른 자리에서 재생산). 대신:
- `emit_capsec_provenance.py::_forward_source_file()`의 FSC 시절 경로 재구성 로직 1곳만 수정
  (`bonds_source`가 이제 bare timestamp가 아니라 전체 상대경로라 재구성 불필요 — 내 교체가 깨뜨릴
  뻔한 지점, 직접 유발한 버그라 같이 고침).
- `quarter`/`as_of_date`는 **그대로 `BASELINE_QUARTER`(2026.1Q/2026-03-31) 유지** — `check_as_of()`가
  이 필드를 `manifest.baseline_quarter`(K-ICS item1/item14 baseline 신선도) 대비 검사하는 것이지 채권
  스케줄 자체의 vintage와는 무관함을 코드로 확인(`:507-513`). 채권 vintage(FY2025 사업보고서,
  2025-12-31, K-ICS baseline보다 1분기 늦음)는 그대로 존재하는 실제 시차이고 조작 불가(2026.1Q 채권별
  공시가 아직 없음) — `emit_capsec_provenance.py`의 `_note`에 이미 기록됨.
- `scripts/forward_capital_simulation.py` 실행 → `scripts/emit_capsec_provenance.py` 실행(순서 중요,
  전자가 manifest를 갱신해야 후자가 올바른 `bonds_source`를 읽음) 순서로 사이드카 재발행 확인.

### 완료 조건 검증
1. `python scripts/forward_capital_simulation.py` — 재실행 완료. 발행잔액>0 회사 수 = 24사
   (DART 원본 그대로, KR0050/KR0076 미포함 — §2 참조. 이 둘 빠지면 24사 미달, downloader 후 재확인).
2. `python scripts/validate_data_contract.py` → **RED=0, YELLOW=210**(세션 시작 전 baseline과 동일 —
   신규 anomaly 없음).
3. `pytest tests/test_deploy_assets.py` → **9 passed**.
4. `grep -rn "normalized" scripts/forward_capital_simulation.py` → docstring 설명 문구(과거형) 2건뿐,
   실제 코드 경로 0건.
5. §2 표 참조.

status: 소스 교체 완료(RED=0)·사이드카 정합 확인 · KR0050/KR0076 커버리지 2사는 downloader
raw 발주로 이관(자력 해소 불가, raw 소실) — 그 2사 편입 전까지 조건 ①(≥24사, 하나손보·iM라이프 포함)
미충족 상태를 문서화.

## 답변 갱신 (parser/ifrs17 2026-08-03 후속 — 완료조건 ① 최종 충족, 종결 가능)

KR0050/KR0076 편입 완료(`inbox/parser/20260803T0150Z`). 이어서 조우한 별도 회귀
(`CAPSEC_COVERAGE_REGRESSION`, validation 신설 룰 — 소스에 회사 레코드 없는 13사 RED)까지 전부
해소(`inbox/parser/20260803T0400Z`/`20260803T0546Z`) — 그 13사 중 실발행 신규발견은 KR1011(IBK연금,
후순위채 3,600억, 완전 누락 상태였음)과 KR0049(악사손해, JPY 사모 후순위채 458.8억, 발행연도
미상 추정치 포함)뿐, 나머지 11사는 무발행 확정.

**완료조건 ① 최종 확인**: `data/bonds/capital_securities_fy2025.json` 39사, `bond_coverage_distribution`
`dart_listed=27`(발행잔액>0) — **≥24사 충족**. `python scripts/validate_data_contract.py` → **RED=0,
YELLOW=219**. `data/bonds/normalized/**` 참조 완전 제거 확인(§완료조건 4, 불변). `pytest` 10 passed.

owner 확인 후 이 티켓 최종 resolved 처리 및 downloader의 bonds 소스 폐지(`20260803T0057Z`) 착수
게이트 오픈 가능.



## owner 재확인 (2026-08-03) — 완료 조건 전부 충족, 종결

`answered` 시점의 미충족 조건(§2 KR0050·KR0076 커버리지 역행)이 후속 체인으로 닫혔다.
owner가 직접 재실행해 확인:

| 조건 | 결과 |
|---|---|
| 발행잔액>0 ≥24사, 하나손보·iM라이프 포함 | ✅ **27사**. KR0050 1,000억(`dart_listed`) · KR0076 3,198.8억(`dart_listed`) |
| `bond_coverage='absent_in_source'` 잔여 | ✅ **0건** (27 `dart_listed` + 11 `no_bonds_in_dart` = 38행 전부) |
| `validate_data_contract.py` | ✅ **RED=0** YELLOW=219 |
| `--selftest` | ✅ **22/22** (신설 H1~H5 포함) |
| `pytest tests/test_deploy_assets.py` | ✅ **9 passed** |
| per-bond 소스 커버리지 | ✅ `capital_securities_fy2025.json` **39사**(교체 시점 24사) |

라이브(main) 대비: KR0050 124.47% **동일 복원**, KR0076 93.65% → **96.9%**(DART가 FSC보다 채권을
더 완전히 잡음 — 2,700억 → 3,198.8억). 낙관 방향 왜곡 없음.

체인: 이 발주 → `20260803T0123Z`(downloader raw) → `20260803T0310Z`(census 룰, RED 15건 발화) →
`20260803T0400Z`/`0405Z`/`0535Z`/`0546Z` → RED=0.
**후속 `20260803T0057Z`(bonds 소스 폐지)의 선행조건 ①이 이로써 충족됐다.**
