---
from: orchestrator
to: parser
created: 20260828T1000Z
status: resolved
route: escalate
company: KR0032
period: MULTI
rule: PL_YESILCHA_ZERO_OTHER_PLUG
lane: ifrs17
iter: 2
---

## 미결 (orchestrator 작성 — owner 지적으로 재개)

**`inbox/_resolved/20260826T2350Z__orchestrator__MULTI__yesilcha_zero_other_plug.md` §1 의
결론을 뒤집는다. NH 예실차는 분리 가능하다.**

그 티켓은 *"예상 측은 비PAA 분만, 발생 측은 PAA 포함 합산이라 모집단이 달라 대조 불가"* 로
종결했다. 전제는 맞았으나 **보험영업이익 노트 하나만 본 결론이었다.** 같은 보고서의
보험계약부채 변동 노트가 측정모형별로 갈라서 준다.

```
(3) 보험료배분접근법을 적용하지 않는 보험계약의 변동내역  → 1) 장기손해보험   ← GMM 전용
(4) 보험료배분접근법을 적용하는 보험계약의 변동내역        → 1) 일반보험
```

### 실측 (2026.2Q 당반기 누계, 백만원)

`data/dart/FY2026_Q2/raw/KR0032_NH농협손해보험/20260814003298.xml`, (3)-1) 장기손해보험
"가. 잔여보장부채(자산) 및 발생사고부채(자산)의 변동내역":

```
보험수익                                    510,001
보험서비스비용 > 발생보험금 및 기타보험서비스비용    351,114   (발생사고부채 열)
```

**모집단 일치 증거 — 보험수익이 두 노트에서 정확히 맞는다:**

```
보험영업이익 노트 소계            1,531,069
  − 보험료배분접근법 보험수익      1,021,068
  = 비PAA 보험수익                510,001   ==  (3)번 표의 보험수익 510,001  ✓
```

따라서 예상·발생이 같은 책이다.

```
예실차(장기) = 예상 보험금 및 기타서비스비용 341,009 − 발생 351,114 = △10,105
```

예상 대비 3% 로 상식적 크기다. 종전에 나왔던 △1,478,436(16배 어긋남)은 PAA 가 섞인 합산
발생액 1,051,620 을 붙였기 때문이다.

### 요청

1. **위 대조를 2026.2Q 외 전 분기에서 재현해라.** 한 분기만 맞는 우연이 아닌지 확인하는 게 먼저다.
   보험수익 일치(비PAA 보험수익 == (3)번 표 보험수익)를 **매 분기 판별식으로** 써라. 안 맞는
   분기가 있으면 그 분기는 뽑지 말고 보고해라.
2. 재현되면 `extract_tier2_nh`(`scripts/pl_breakdown/companies.py:1930`)를 고쳐 item6(원수 예실차)를
   채워라. item7(기타)은 잔차 plug 이므로 자동으로 줄어든다.
3. **4종 범위를 지켜라.** 정본 정의는 `예실차 = Σ예상(보험금·손해조사비·유지비·재산관리비) −
   Σ발생(같은 4종)` 이다. NH 는 양쪽 다 "및 기타(보험)서비스비용" 으로 묶은 한 줄이라 범위가
   대응한다. 그러나 `보험취득현금흐름 상각`·`손실요소 인식 및 환입`·`발생사고부채 관련 이행현금흐름
   변동`은 **4종 밖이다. 넣지 마라**(2026-06-05 owner 결정 계열).
4. 그 `_resolved` 티켓의 §1 정정 문구와 핸들러 docstring 을 이 결과로 갱신해라. 지금 docstring 은
   "LOB 컬럼이 없어 불가" 로 남아 있는데, 실제로는 **다른 노트에 있었다**는 것이 결론이다.

### 같은 각도를 ABL·미래에셋에도 적용할지

`_resolved` 티켓은 owner 의 *"예실차는 NH손보처럼 또 가정이 틀렸겠지. 헛수고하지마"* 로
종결됐다. **그 판단의 근거였던 NH 사례가 지금 뒤집혔으므로 전제가 바뀌었다.** 다만 확대 여부는
owner 결정 사항이다. NH 를 먼저 끝내고, ABL(KR0070)·미래에셋(KR0079)에 같은 측정모형별
변동 노트가 있는지 **존재 여부만 조사해서 보고해라.** 추출은 이 티켓 범위가 아니다.

### 하지 말 것

- 값을 지어내지 마라. 판별식이 안 맞는 분기는 비워 두고 보고한다.
- `index.html`·`IFRS17.html` 금지(다른 세션이 만지는 중). 브랜치 변경 금지. `git push` 금지.
- `build_root_masters.py` 의 `main()` 통짜 실행 금지. `validate_master_tables.py` 는 `--no-build`.
- 마스터 xlsx 는 `scripts/sync_master_xlsx_sheet.py` 로 바뀐 시트만. openpyxl 재저장 금지.
- 골든 `tests/test_pl_breakdown_golden.py` 가 막을 것이다. 산출이 의도적으로 바뀌므로 `--update`
  재생성 + 커밋에 이유 기록.

## 답변 (recipient 작성 — 처리 후)

## 답변 (orchestrator 작성 — 티켓 철회)

**이 티켓을 철회한다. 발주 근거가 틀렸다.** owner 지적(2026-08-28): *"발생사고부채는 그 예실차
산출할 때의 '발생' 항목이 아니다."* 맞는 지적이고, 확인 결과 이 티켓의 전제가 무너진다.

### 무엇이 틀렸나

이 티켓은 GMM 전용 변동표의 **발생사고부채 열** `발생보험금 및 기타보험서비스비용 351,114` 를
예실차의 발생 측으로 쓰자고 했다. 그건 부채 잔액의 증감이지 발생 4종이 아니다.

1. **4종 중 2종을 구조적으로 담을 수 없다.** 정본 정의의 발생 4종은 보험금·손해조사비·유지비·
   재산관리비인데, `직접유지비`·`직접재산관리비` 는 사고로 쌓이는 부채가 아니라 당기 비용이라
   발생사고부채를 통과하지 않는다. 실제로 보험영업이익 노트는 그 4종을 각각 따로 준다
   (발생 보험금 1,051,620 · 직접손해조사비 673,035 · 직접유지비 91,649 · 직접재산관리비 3,141).
2. **과거사고 발달분이 섞인다.** 발생사고부채 변동에는 `발생사고부채 관련 이행현금흐름 변동`
   (노트의 `발생사고요소 조정 △483,776`)이 들어가는데, 이건 예실차 항목이 아니라는 것이
   이미 `_resolved/20260826T2350Z` §1(a)의 owner 결정이다.

### 이 티켓의 근거가 실제로 증명한 것

보험수익 510,001 일치는 검산하면 닫힌다.

```
341,009 + 7,173(RA) + 101,133(CSM상각) + 79,626(취득CF상각) − 18,940(손실요소배분) = 510,001
```

**이건 예상 341,009 가 GMM 분만 담는다는 수익 측 사실만 확인해 준다.** 그것을 비용 측으로
옮겨 "발생도 GMM 분을 찾았다" 고 주장한 것이 비약이었다.

### 남는 결론

`_resolved/20260826T2350Z` §1 의 원래 결론이 유효하다. **NH 예실차는 분리 불가다.** 발생 4종은
회사 전체(GMM+PAA) 합산으로만 공시되고, 측정모형별로 가른 4종은 보고서 어디에도 없다.
부채 변동표 (3)·(4) 도 부채 롤포워드라 같은 한계를 갖는다. **item6 은 0 으로 유지한다.**

### 정정 2 — 위 "남는 결론" 도 틀렸다 (owner 재지적, 같은 날)

**이 철회 자체가 과잉이었다.** 열(발생사고부채)을 잘못 집은 것은 맞지만 **표는 유효하다.**
owner 가 "그럼 이건 뭔데" 라고 (3)번 표를 다시 가리켰고, 문서 목차를 뽑아보니 결정적 사실이 나왔다.

```
(3) 보험료배분접근법을 적용하지 않는 보험계약의 변동내역
      1) 장기손해보험          <- 이것뿐. 2) 가 없다.
(4) 보험료배분접근법을 적용하는 보험계약의 변동내역
      1) 일반보험  2) 자동차보험  3) 농작물보험
```

**NH 에서는 비PAA = GMM = 장기손해보험이 같은 계약 집합이다.** 자동차·일반·농작물은 전부 PAA다.
따라서 롤포워드의 보험수익 510,001 은 LOB 합계가 아니라 **장기 단독**이고, 예상 341,009 도 장기
단독이다(독립 경로 2개로 확인 — 티켓 `20260828T1400Z` 참조).

**장기 예실차는 분리 가능하다.** 올바른 발생 측은 발생사고부채 열이 아니라 `발생보험금 및
기타보험서비스비용` 의 **합계 열**이다. 후속은 `inbox/parser/20260828T1400Z__orchestrator__KR0032__
yesilcha_via_gmm_rollforward_total_column.md` 에서 이어간다(남은 쟁점은 손실요소배분 경계 하나).

### 다음 사람에게 — 실패 패턴 (같은 함정 방지)

이 건은 하루에 세 번 결론이 바뀌었다. 원인은 매번 같았다. **주석의 목차 구조를 먼저 확인하지
않고 숫자부터 계산했다.**

1. 1회차: 보험영업이익 노트 하나만 보고 "분리 불가" 단정. 측정모형별 변동표의 존재를 몰랐다.
2. 2회차: 표는 찾았으나 부채 잔액 열을 손익 발생액으로 착각.
3. 3회차: 열 오류를 표 전체의 무효로 과잉 일반화.

**LOB/측정모형 관련 판단은 주석 목차부터 뽑아라.** 명령 한 줄이면 (3)/(4) 아래 어느 LOB 가
걸려 있는지 바로 나온다. 그걸 세 번째에야 했다.
