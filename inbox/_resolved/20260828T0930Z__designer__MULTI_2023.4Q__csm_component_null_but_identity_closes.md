---
from: designer
to: parser
created: 20260828T0930Z
status: resolved
route: reparse
company: KR0050,KR0076
period: 2023.4Q
lane: ifrs17
iter: 1
---

## 미결 (sender 작성)

`IFRS17.html` 의 `Number(null)→0` 버그를 고치면서(designer 티켓 `20260828T0700Z`) 드러났다.
`CSM_waterfall.json` 의 `값`(연누계) 컬럼에 **null 이 정확히 2셀** 있는데, 둘 다 **0을 넣으면
항등식이 정확히 닫힌다.** 지금까지는 화면 코드가 null 을 0으로 바꿔 버려서 아무도 못 봤다.

| 회사 | 분기 | 항목 | 값 | 나머지 4항목 합 | 기말 | 차이 |
|---|---|---|---|---|---|---|
| KR0050 하나손해보험 | 2023.4Q | 3 이자 부리 | `null` | 1,837.3 | 1,837.2 | 0.1 |
| KR0076 아이엠라이프생명보험 | 2023.4Q | 5 CSM 상각 | `null` | 7,571.0 | 7,571.0 | 0.0 |

KR0050 (억원): 기초 1,186.3 + 신계약 623.6 + **이자 null** + 조정 184.4 + 상각 △157.0 = 기말 1,837.2
KR0076 (억원): 기초 6,757.9 + 신계약 2,489.3 + 이자 198.0 + 조정 △1,874.2 + **상각 null** = 기말 7,571.0

### 판단이 필요한 것

**"진짜 0" 인지 "원표에 값이 있는데 파서가 행을 떨어뜨린 것" 인지 원문으로 확정해 달라.**
특히 KR0076 은 CSM 잔액 7,571억인 생보사의 **연간 CSM 상각이 0** 이라는 뜻이 되어 실무적으로
믿기 어렵다(같은 회사 2024·2025 상각은 각각 △549.2·△537.9). 상각이 조정(△1,874.2) 안에
합산돼 있을 가능성을 먼저 보는 게 좋겠다. KR0050 이자 부리는 소액이라 진짜 0일 수 있다.

- 진짜 0이면 → 마스터에 `0` 으로 채워 달라(그래야 화면이 "미공시" 대신 "0"으로 정직해진다).
- 원표에 값이 있으면 → 그 값으로 채워 달라.
- 원표에 그 행 자체가 없으면 → `null` 유지가 맞다. 그 사실만 답변에 적어 주면 designer 는
  현행(회색 "미공시" 막대 + 표 `—`)을 유지한다.

### 화면 현황 (지금 상태)

designer 쪽 수정은 이미 반영됐다. 두 셀은 IFRS17 Panel 2 **연도 모드 2023 열**에서
표 `0` → `—`, 차트 초록 `+0.00` 막대 → 회색 "미공시" 막대로 바뀌었다. 브리지 누적합은
변하지 않아(결측을 0으로 더하나 건너뛰나 산술 동일) 기말 막대에 그대로 착지한다.

참고: `값_당분기` 의 null 200셀은 **연1회 공시 14사**(예별·AIG·악사·하나손보·라이나·아이엠라이프·
AIA·메트라이프·하나생명·처브·교보라이프플래닛·IBK연금·BNP카디프·카카오페이)의 것으로,
분기 미공시라 정상이다. 화면에도 도달하지 않는다(`IFRS17.html:1511` 이 분기 모드에서 이 회사들의
Panel 2 를 아예 안 그린다). **이 티켓의 대상이 아니다.**

## 답변 (recipient 작성 — 처리 후)

### 1. KR0050 재현 (orchestrator 실측을 직접 재현 — 그대로 믿지 않고 raw XML 재파싱)

`data/dart/FY2023_Q4/raw/KR0050_하나손해보험_20240325000106/20240325000107_00761.xml`
(주: 같은 접수번호 폴더 밑에 `...000106`/`...000107` 두 개가 있는데 실제 마스터에 반영되는
쪽은 `000106` 폴더 — `000107`은 재추출 시 통째로 `None` 을 반환해 파이프라인이 건너뛴다.
직접 `src.ifrs17.measurement_extractor.extract_measurement_tables()` 로 재파싱해 "측정요소별
변동내역" 원수 표(수정소급법/공정가치법/그 밖의 모든 계약 3열, 단위 천원)를 확인했다.

```
1. 기초 보험계약자산 및 부채                    118,625,827  = 1,186.26억   마스터 일치
(1) 보험계약마진을 조정하는 추정치의 변동분        12,240,870  =   122.41억   <- 티켓의 "조정" 184.4 아님(=진짜 조정)
(3) 최초 인식한 계약의 효과                      62,362,234  =   623.62억   마스터 일치(신계약)
(1) 서비스 이전…당기손익으로 인식한 보험계약마진 (15,704,909) =  △157.05억   마스터 일치(상각)
6. 보험금융손익 (1) 당기손익인식                   6,196,350  =    61.96억   <- 마스터는 null(이자 부리)
8. 기말 보험계약자산 및 부채                     183,720,372  = 1,837.20억   마스터 일치
```
CSM 열만(수정소급법+공정가치법+그밖의모든계약 3열 합) 뽑았고, 검산 122.41+61.96=184.37≈
마스터의 조정 184.4 — 티켓의 가설을 정확히 재현. **KR0050 2023.4Q: 이자 부리=62.0억(반올림),
조정=122.4억으로 확정.**

### 2. 왜 합쳐졌는가 — 코드 원인 2종 + census (이 티켓의 핵심)

**원인 A (KR0050 전용 구조).** `scripts/viz_build_csm_waterfall.py::extract_stages()` 의 행단위
라벨 매칭은 **부모 행 텍스트를 자식 행에 안 물려준다.** KR0050 원문은 `['6. 보험금융손익']`
(값 없는 순수 헤더 행) 바로 아래 `['(1) 당기손익인식', ...]` (실값 보유, "보험금융손익" 문자열
자체는 없음) 로만 표기 — 어떤 `STAGE_PATTERNS["interest"]` 패턴도 이 자식 행에 안 걸린다.
그 결과 `build_csm_waterfall_master.py::waterfall_for_dir()` L1198 의 `assum = clo - ((wf.get(1)
or 0) + (wf.get(2) or 0) + (wf.get(3) or 0) + (wf.get(5) or 0))` 에서 `wf.get(3)`(이자)이
`None`→`or 0`으로 조용히 0 취급되고, **항목4(조정)가 잔차라서 이자값을 통째로 흡수**한다.
item3 셀 자체는 `None`으로 남아 화면엔 "미공시"로 뜨는데, 항등식은 (구성상 참이라) 그대로
닫힌다 — 오늘 이 저장소에서 이미 확인된 "폐쇄식이 구성상 참이라 검증력이 없다"의 CSM 축
사례(`inbox/_resolved/20260829T2130Z__validation__MULTI__pl_eqs_constructive_tautology.md`
의 PL `pl_bridge` 축과 동일 패턴).

**원인 B (KR0076/KR0072 공통 구조, 별개 버그).** 원문 라벨이 줄바꿈 때문에 단어 중간에
공백이 끼는 렌더링 아티팩트 — 예: "보험계약마진"이 "보 험계약마진"으로 깨져서(아이엠라이프
2023.4Q) 어떤 `STAGE_PATTERNS["amortization"]` 패턴도 못 걸리고, `CSM_LABEL_REQUIRED` 게이트
(`"보험계약마진" not in stub_roll`)도 같은 이유로 막는다. 이자(원인 A)와 달리 상각(항목5)이
잔차 계산에서 빠지면 조정(항목4)이 상각값을 흡수한다.

**적용한 수정 (surgical, `scripts/viz_build_csm_waterfall.py`):**
- `_ns()` 헬퍼 신설(공백 전부 제거, `build_csm_waterfall_master._ns()`와 동일 개념) —
  `extract_stages()` 의 주 패턴매칭 + `CSM_LABEL_REQUIRED` 게이트를 공백무시 비교로 전환.
  원인 B를 일반적으로 고친다(이 회사만이 아니라 같은 아티팩트를 가진 모든 필링).
- "당기손익인식" 폴백을 **경쟁 패턴이 아니라 최후수단으로만** 추가 — `interest` 스테이지의
  1차 패턴이 **아무 후보도 못 찾았을 때만** 발동. 처음엔 일반 패턴으로 추가했다가 검증 중
  교보생명보험(KR0073) 2024.1Q~2025.1Q·코리안리(KR1000) 2025.1Q에서 **부모 집계행("6.
  보험금융손익", 실값 있음)과 경쟁해 잘못된 값(자식 단독, OCI분 제외)을 이기는 회귀**를
  발견 — KR0073 원문에서 "6."="(1)당기손익인식"+"(2)기타포괄손익인식" 임을 직접 확인했고
  (85,930≈85,990+(-58)), 부모값을 쓸 때만 폐쇄잔차가 원문의 "가정 추정치 변동" 행과
  거의 정확히 일치(오차 1 vs 자식단독일 때 오차 61) — **부모 집계가 CSM 잔액 롤포워드용
  정답, 자식은 P&L전용 하위개념**이라 폴백으로 재설계.

**census (원인 A+B 전부, 재파싱 가능한 raw 363 회사×분기 전수 — `build_root_masters.py`
`main()`/`build_csm_waterfall_master.py` `main()` 통짜 실행 없이 `waterfall_for_dir()` 만
직접 호출하는 read-only 스크립트로 확인, 마스터 미변경):**

수정 전 이 패턴(항목1/2/3/5 중 하나만 null, 나머지 3개+기말은 있음) 이 걸리는 버킷 = **7개**:
KR0050(2023.4Q·2024.4Q, 이자), KR0072 케이디비생명(2023.1Q, 상각), KR0076(2023.4Q, 상각),
KR0079 미래에셋생명(2025.2Q·2025.3Q·2026.1Q, 상각). 이 중 **5개는 이미 owner 가
`data/_gold/user_csm_cells.json` gold-overlay 로 수기 정정해 놓은 상태**였다(KR0050 2024.4Q:
이자 85.30805/`was:ROW_NULLED`, KR0072: 상각 -111.25/`was:None`, KR0079 3분기: 상각
-791.3/-1531.21/-537.11 — 전부 `was: None`) — 즉 **이 정확한 버그를 owner 가 이미 3번
따로 손으로 때웠던 이력**이 gold 파일에 남아 있었다. 코드 수정 후 재계산한 값이 이 기존
수기 정정값과 **소수점 첫째자리까지 거의 정확히 일치**(예: KR0050 2024.4Q 이자 85.3 vs
gold 85.30805, KR0072 상각 -111.2 vs gold -111.25) — 별개의 두 방법(raw 직접대조 vs
owner 수기)이 같은 답에 수렴해 수정의 정확성을 교차검증한다.

**KR0079 3분기(상각)는 이번 코드로 안 고쳐진다** — census 로 여전히 flag 남음, `src`가
`wide-product`/다른 경로라 원인 A/B와 다른 구조로 추정. 이미 gold-overlay 로 화면엔 정답이
떠 있어 이번 티켓 범위 밖으로 남겨둔다(후속 조사 필요하면 별도 티켓).

**전 363버킷 중 실제로 마스터가 바뀌는 건 최종 4셀뿐**(KR0050·KR0076 2023.4Q) — 나머지
5개는 gold-overlay 가 이미 이겨서 라이브 마스터엔 영향 없음, 359개 버킷은 완전 무변화
(before/after 전수 diff 로 확인, 아래 §5).

### 3. KR0076 판정 — "0 아님, 원표에 값 있음"

같은 회사 원문 "1) 보험계약의 구성요소에 따른 변동분석"(제37(당)기, 단위 백만원) 표에서
"(1) 서비스 제공으로 당기손익으로 인식된 보 험계약마진"(공백 아티팩트, §2 원인B) 행 =
CSM 2열(공정가치법/완전소급법+당기발행) 합 `(19,943)+(37,172) = (57,115)` 백만원 =
**△571.15억 → 상각 = -571.1억로 확정.** 티켓 가설("상각이 조정 안에 합산") 그대로 —
조정(항목4) 원래값 -1,874.2 = 진짜조정(-1,303.0) + 상각(-571.1)의 흡수합. 상각이 0이라는
결론은 틀렸다(원표에 명시적 행이 있고, 2024·2025 상각(-549.2/-537.9)과 동일한 규모대).

### 4. 적용 결과 — CSM_waterfall.json 4셀

| 회사 | 분기 | 항목 | 이전 | 이후 |
|---|---|---|---|---|
| KR0050 하나손해보험 | 2023.4Q | 3 이자 부리 | `null` | **62.0** |
| KR0050 하나손해보험 | 2023.4Q | 4 가정 및 경험 조정 | 184.4 | **122.4** |
| KR0076 아이엠라이프생명보험 | 2023.4Q | 4 가정 및 경험 조정 | -1874.2 | **-1303.0** |
| KR0076 아이엠라이프생명보험 | 2023.4Q | 5 CSM 상각 | `null` | **-571.1** |

값_당분기는 두 회사 모두 4개 항목 전부 변동 없음(둘 다 연1회 공시사라 2023.3Q YTD가 없어
`_flow_dangi` 가 여전히 `None` — 정상, 결측 아님).

**재현 명령:**
```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe -c "
import sys; sys.path.insert(0,'.'); sys.path.insert(0,'scripts')
import build_csm_waterfall_master as B
for kr,name,rd in [('KR0050','하나손해보험','data/dart/FY2023_Q4/raw/KR0050_하나손해보험_20240325000106'),
                    ('KR0076','아이엠라이프생명보험','data/dart/FY2023_Q4/raw/KR0076_아이엠라이프생명보험_20240405002220')]:
    print(kr, B.waterfall_for_dir(__import__('pathlib').Path(rd), name, None))
"
```
→ `KR0050 ({1: 1186.3, 2: 623.6, 3: 62.0, 4: 122.4, 5: -157.0, 6: 1837.2}, ...)` /
  `KR0076 ({1: 6757.9, 2: 2489.3, 3: 198.0, 4: -1303.0, 5: -571.1, 6: 7571.0}, ...)`

### 5. 전수 감사 + 게이트

- `CSM_waterfall.json` **2,172행 불변**(추가/삭제 0), 항목1~6 전부 362행/362 non-null(수정
  전엔 360/362 — null 2개가 이 둘). 37개사 불변.
- **전 363 (회사×분기) 버킷 before/after 전수 diff**(read-only, `waterfall_for_dir()` 직접
  호출 — `build_root_masters.py`/`build_csm_waterfall_master.py` 의 `main()` 미실행) —
  변경 4버킷은 위 표와 정확히 일치, **나머지 359버킷 완전 동일**(사라진/새로 생긴 버킷 0).
- `scripts/build_root_masters.py::build_csm()` **개별 함수만 호출**(overlay 276 set 그대로,
  0 added, 2 companies excluded — 기존과 동일), combo-diff: LOST 0 / NEW 0 / 변경 정확히 4셀.
- `data/dart/viz/csm_waterfall_master_diag.json` 도 같은 4셀 서지컬 패치(2,178행 불변) —
  실행 전 두 파일 다 `.bak_20260829_203919_interestabsorb` 백업.
- `insurequant_master_tables.xlsx` — `sync_master_xlsx_sheet.py "CSM워터폴"` cherry-pick,
  변경 4셀·추가/삭제 0행·"CSM워터폴 2172행×9열 마스터와 완전 일치, 나머지 시트 값 동일" 검증.
- `pytest tests/ -q`(heavy golden 3종 pl_breakdown/post_transition/ifrs17_bs 제외) —
  **501 passed, 1 skipped(무관), 3 deselected**. `test_viz_csm_waterfall_golden.py` PASS
  (코드는 바뀌었지만 `data/dart/viz/csm_waterfall.json` 산출 바이트 불변 — 이 프로토타입
  패널의 커밋된 47사 스냅샷엔 KR0050/KR0076이 없음). `test_viz_ifrs17_panels_golden.py` PASS
  (CSM_waterfall.json 을 입력으로 읽지만 4패널 산출 바이트 불변). `test_master_tables_golden.py`
  는 **의도된 이동**(closing:360P/0F/2S→362P/0F/0S — null 2개가 SKIP였다가 이제 계산 가능해져
  PASS, qoq_warn:235→236Y[리뷰용 경고 1건 추가, 차단 아님) → `--update` 로 재생성.
  `validate_golden_input_fingerprints.py` — CODE_MOVED(viz_csm_waterfall)·INPUTS_MOVED
  (viz_ifrs17_panels, CSM_waterfall.json 을 입력으로 읽음) 2건 RED → `--update` → RED=0.
  `validate_csm_continuity.py` — red=0(YELLOW 2건은 KR0001/KR0073 의 기존 문서화된 면제,
  이 수정과 무관).

**커밋**: `9a067dd`

status: resolved
