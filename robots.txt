# InsureQuant — robots.txt
# 검색 색인은 전체 허용. 이 사이트는 공시자료를 가공한 공개 데이터라 숨길 경로가 없다.
# 이용 조건: https://www.insurequant.com/privacy.html#terms
# (열람·출처표기 인용·사내 분석은 자유, 자동수집으로 통째 긁어가기·대량 재배포는 금지)
#
# 주의: 이 파일은 어떤 HTML 도 참조하지 않는다. 배포 keep-list 는 HTML 의
# fetch(/src=/href= 를 grep 해서 도출하므로 이 파일은 자동으로 도출되지 않는다.
# 상시 유지 대상이다 — docs/launch_runbook.md 의 "HTML 무참조 상시 유지 파일" 참조.

User-agent: *
Allow: /
# 다운로드 방명록용 스냅샷(JSON) — 화면 렌더링에는 안 쓰이므로 색인 불필요.
# 루트 *.json 은 페이지 렌더링 리소스라 일부러 막지 않는다(Googlebot 렌더링 색인 영향).
Disallow: /public_exports/

# AI 모델 "학습용" 크롤러만 차단. 사용자가 질문할 때 대신 페이지를 읽어 오는
# fetcher(ChatGPT-User, PerplexityBot 등)는 막지 않는다 — AI 검색 유입은 받는다.
User-agent: GPTBot
User-agent: CCBot
User-agent: ClaudeBot
User-agent: anthropic-ai
User-agent: Google-Extended
User-agent: Bytespider
User-agent: meta-externalagent
User-agent: Applebot-Extended
Disallow: /

Sitemap: https://www.insurequant.com/sitemap.xml
