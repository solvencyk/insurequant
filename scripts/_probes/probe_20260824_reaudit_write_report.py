# -*- coding: utf-8 -*-
"""Read-only helper: write the re-audit report as UTF-8 (no BOM)."""
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
OUT = ROOT / "artifacts/validation/reaudit_20260824_KR0075_KR0087_KR0073.md"

TEXT = r"""# 면제 재감사 — KR0075 · KR0087 · KR0073 (2026-08-24, validation)

읽기 전용 조사. 원장·룰코드·마스터·xlsx·게이트 어느 것도 수정하지 않았다.
기준 마스터: 루트 `kics_disclosure.json` (조사 시작·종료 시 `git status` 미변경 확인).

## 판정 집계

| 판정 | 건수 | 버킷 |
|---|---|---|
| `ISSUER_DEFECT_CONFIRMED` | **4** | KR0075 2024.3Q · 2024.4Q · 2025.1Q · KR0073 2025.2Q |
| `OUR_RULE_DEFECT` | **1** | **KR0087 2025.2Q** (뒤집힘) |
| `MASTER_DEFECT` | 0 | — |
| `UNDETERMINED` | 0 | — |
| `PIN_STALE` | 0 | 박제 잔차 5건 전부 현재 룰 출력과 일치 |

**뒤집힌 것은 1건뿐이다 — KR0087 동양생명 2025.2Q.** 나머지 4건은 등재 판정이 옳다.
5개 버킷 **전부 마스터 셀은 원문과 정확히 일치**한다(원문 좌표 판독 + 220dpi 렌더 육안).
어느 버킷에서도 마스터 값을 고칠 이유를 찾지 못했다.

## 한 줄 표

| 버킷 | 룰 · 실측 잔차 | 박제 | 판정 | 근거 한 줄 |
|---|---|---|---|---|
| KR0075 2024.3Q | `2_tier1_bridge` +15.00 · `3_tier2_composition` +14.86 · `51_tfi_tier2_composition` +14.53 | 15.0 / 14.86 / 14.53 | `ISSUER_DEFECT_CONFIRMED` | p16 TFI 가 `보완자본 33,067` > `보완자본 한도 적용 전 31,614` 을 인쇄. 어떤 스코프 읽기로도 33,067 이 (47,48,49) 에서 나오지 않는다. 더해 p15 헤드라인이 `지급여력금액 2,069 ≠ 순자산 2,098 − 불인정 44` 로 자기 안에서도 안 닫힌다 |
| KR0075 2024.4Q | 다리 +87.00 · 구성 +87.22 · TFI +86.75 | 87.0 / 87.22 / 86.75 | `ISSUER_DEFECT_CONFIRMED` | 위와 동형. p50 TFI `보완자본 43,353` > `한도 적용 전 34,678`. 헤드라인 p49 도 `2,091 ≠ 2,111 − 107` |
| KR0075 2025.1Q | 구성 +61.41 · TFI +61.19 (다리는 닫힌다) | 61.41 / 61.19 | `ISSUER_DEFECT_CONFIRMED` | p20 TFI `보완자본 40,878` > `한도 적용 전 34,759`. 헤드라인은 정상 — 깨지는 것은 TFI 한 표뿐 |
| **KR0087 2025.2Q** | `2_tier1_bridge` **+1188.00** · `47_tier2_census` RED(DUPLICATE_ROW) | 1188.0 / null | **`OUR_RULE_DEFECT`** | 등재 주장("헤드라인이 자기 각주 주1) 을 어긴다")이 **거짓**이다. 참 한도초과 1,188.04 를 쓰면 `33,001 − (1,188 − 1,188.04) − 18,883 = 14,118.04` 로 공시 14,118 과 **잔차 0.04** 에서 닫힌다. 그 1,188.04 는 헤드라인을 전혀 안 보는 TFI 적용후 컬럼에서 독립으로 나온다 |
| KR0073 2025.2Q | `36_irr` 적용전·적용후 각 +241.4374 (면제로 SKIP) | 241.4373504145833 | `ISSUER_DEFECT_CONFIRMED` | p21 [② 금리위험액 현황] 한 표 안에서 `Ⅳ. 금리 위험액 459,988` 이 자기 `Ⅲ. 순자산가치` 6열에서 표준식으로 재현되지 않는다(도출 435,845). 변이 탐색 전수에서 재현하는 산식이 **하나도 없다** |

---

## 1. KR0075 비엔피파리바카디프생명보험 2024.3Q — `ISSUER_DEFECT_CONFIRMED`

### 1.1 등재 당시 주장 (원장 `entries[35]` 그대로 인용)

- `claim`: "TFI 표(1) 공통적용 경과조치의 `보완자본` 행이 자기 구성행과 안 닫힌다 — min(한도적용전 31,614, 한도 31,614) + 초과분 23,584 = 55,198 인데 인쇄된 보완자본은 33,067 이다. 잔차를 메울 인쇄행이 존재하지 않는다: (기발행 신종자본증권)·(기발행 후순위채무)가 **둘 다 대시**다. `item47 == item48` 도 두 셀을 잘못 읽은 것이 아니라 발행사가 같은 숫자를 두 줄에 실제로 인쇄한 것이다."
- `claim_kind`: `ISSUER_INTERNAL_INCONSISTENCY` · `status`: `VERIFIED`
- `citation` / `verify`: `data/disclosure/FY2024_Q3/raw/KR0075_비엔피파리바카디프생명보험_amended.pdf` p15,16 · present_markers `31,614 · 23,584 · 33,067 · 206,824 · 173,757 · 2,069 · 2,098 · 331`
- `note` 요지: 다리만 +15 어긋난다(2,098 − 44 − 331 = 1,723 vs 인쇄 1,738), 2024.4Q 의 +87 과 같은 모양. 잔차/초과분 비율이 6.2% → 26.3% → 20.1% 로 흔들려 상수가 아니다.

### 1.2 실측 (룰엔진 재현)

```
2_tier1_bridge            RED  expected 1723.0  actual 1738.0  diff +15.00
   detail: branch=NEITHER scope=INCL 한도초과=0
3_tier2_composition       RED  expected  316.14 actual  331.0  diff +14.86  COMPOSITION_NEITHER
51_tfi_tier2_composition  RED  expected  316.14 actual  330.67 diff +14.53  TFI_COMPOSITION_NEITHER
47_tier2_census           RED  TIER2_DUPLICATE_ROW (item47 == item48 == 316.14)
```

마스터 입력: `item2 1738 · item3 331 · item4 2098 · item12 44 · item13 331 · item14 632`
· `item47 316.14 · item48 316.14 · item49 235.84 · item51 330.67 · item52 2068.24 · item53 0 · item54 0`

### 1.3 raw 대조 — 마스터 == 원문 (좌표 판독 + 220dpi 렌더 육안)

p16 [지급여력비율의 경과조치 적용에 관한 사항] 1) 공통적용 (단위: 백만원) — 적용전/적용후 두 컬럼 동일:

```
지급여력비율(%)          327.12 / 327.12
지급여력금액            206,824 / 206,824
 기본자본               173,757 / 173,757
 보완자본                33,067 /  33,067      <- item51
  보완자본 한도 적용 전   31,614 /  31,614      <- item47   (y276)
  보완자본 한도           31,614 /  31,614      <- item48   (y300)
  해약환급금...초과분      23,584 /  23,584      <- item49
  (기발행 신종자본증권)         -               <- item53 = 0
  (기발행 후순위채무)           -               <- item54 = 0
지급여력기준금액          63,227 /  63,227      -> x50% = 31,613.5 -> 31,614 = item48
```

`보완자본 한도 적용 전` 과 `보완자본 한도` 는 **서로 다른 y 좌표의 두 행**이고 각각 두 컬럼에
값을 가진다(총 4회 인쇄). 렌더 육안으로도 두 행이 같은 31,614 를 인쇄하는 것을 확인했다 —
텍스트레이어가 거짓말하는 경우가 아니다. **`MASTER_DEFECT` 는 배제된다.**

p15 헤드라인(단위: 억원, 25.2Q... 아니라 24.3Q 열): 순자산 2,098 = 2,763 + 0 − 210 + 0 − 123 + 0 − 332
(자기 구성행 7개 합과 정확 일치) · 불인정 44 · 재분류 331 · 기본자본 1,738 · 보완자본 331 ·
지급여력금액 2,069 · SCR 632.

### 1.4 반증 시도 — 우리 룰을 뒤집을 산식을 찾지 못했다

이 회사의 `item47` 스코프는 룰이 `INCL` 로 투표한다. 그 투표는 옳다 — 문제의 3분기를 뺀
**나머지 10분기 전부에서 `item51 == item47` 이고 `item49 > 0`** 이라, `min(47−49, 48) + 49 = 47`
(INCL) 만 재현되고 `min(47,48) + 49` (EXCL) 는 전부 틀린다. 예: 2023.4Q `min(181.94 − 68.09,
565.38) + 68.09 = 181.94` = 인쇄값, EXCL 이면 250.03 로 어긋난다.

문제의 분기에서 `item47 == item48` 이므로 한도가 무는지 여부와 무관하게 가능한 기대값은 두 개뿐이다:

```
INCL: min(47 − 49, 48) + 49 = 47              = 316.14
EXCL: min(47, 48) + 49      = 48 + 49         = 551.98
```

인쇄된 `보완자본 330.67` 은 **둘 다 아니다.** 인쇄된 세 수 (316.14, 316.14, 235.84) 의 어떤
합·차·min 조합으로도 330.67 이 나오지 않는다(316.14 / 551.98 / 80.30 / 235.84 가 전부다).
잔차 14.53 에 해당하는 **인쇄행이 존재하지 않는다** — 메모행 둘이 대시라는 등재 주장은 raw 로 재확인됐다.

다리쪽도 같다. 다리가 요구하는 한도초과는 15 인데, 한도가 물었다면 보완자본은 반드시
`48 + 49 = 551.98` 이어야 한다(인쇄 330.67). **즉 TFI 표는 "한도가 안 물었다(초과 0)" 고 말하고
헤드라인은 "초과 15 가 있었다" 고 말한다.** 두 표가 서로 다른 사실을 인쇄한다.

덧붙여 헤드라인은 자기 안에서도 안 닫힌다: `지급여력금액 2,069` vs `순자산 2,098 − 불인정 44 = 2,054`
(억원 반올림 밴드 ±1 을 다 줘도 최소 13 이 남는다). 같은 회사 나머지 11분기는 전부 닫힌다.

**판정 `ISSUER_DEFECT_CONFIRMED`.** 인용: 위 p16 의 `보완자본` 행과 `보완자본 한도 적용 전` 행,
그리고 p15 의 `가. 지급여력금액` 행과 `Ⅰ. 순자산`/`Ⅱ. 불인정` 행.

---

## 2. KR0075 2024.4Q — `ISSUER_DEFECT_CONFIRMED`

### 2.1 등재 당시 주장 (원장 `entries[27]`)

- `claim`: "TFI 표 자신의 `보완자본` 행이 자기 구성행(min(한도적용전,한도)+초과분)과 안 맞는다.
  표 전체를 덤프해도 더 읽을 행이 없다 — `(기발행 신종자본증권)`·`(기발행 후순위채무)` 두 메모행이
  둘 다 대시라 잔차를 메울 인쇄행이 존재하지 않는다. 숨은 공식도 아니다(잔차/초과분 비율이
  6.2%→26.3%→20.1% 로 흔들려 상수가 아니다)."
- `status`: `VERIFIED` · `citation`: `.../FY2024_Q4/raw/KR0075_비엔피파리바카디프생명보험.pdf` p49,50

### 2.2 실측

```
2_tier1_bridge            RED  1570.0 vs 1657.0   diff +87.00   branch=NEITHER scope=INCL
3_tier2_composition       RED   346.78 vs  434.0  diff +87.22
51_tfi_tier2_composition  RED   346.78 vs  433.53 diff +86.75
47_tier2_census           RED  TIER2_DUPLICATE_ROW (47 == 48 == 346.78)
```

### 2.3 raw 대조 — 마스터 == 원문

p50 TFI(백만원, 전/후 동일): 지급여력금액 209,061 · 기본자본 165,708 · **보완자본 43,353** ·
**한도 적용 전 34,678**(y291) · **한도 34,678**(y315) · 해약환급금 초과분 32,949 ·
(신종) `-` · (후순위) `-` · SCR 69,355 (×50% = 34,677.5 → 34,678).
p49 헤드라인(억원): 순자산 2,111 (= 2,763 − 266 − 107 − 279, 자기 구성행 합 일치) · 불인정 107 ·
재분류 434 · 기본자본 1,657 · 보완자본 434 · 지급여력금액 2,091 · SCR 694.

### 2.4 판정 근거

2024.3Q 와 완전히 동형이다. 가능한 기대값은 `47 = 346.78`(INCL) 또는 `48 + 49 = 676.27`(EXCL)
두 개뿐인데 인쇄된 보완자본은 433.53 이다. 잔차 86.75 를 메울 인쇄행이 없다.
헤드라인도 `2,091 ≠ 2,111 − 107 = 2,004` 로 87 어긋난다 — 다리 잔차와 같은 값이다.
그리고 한도가 물었다면 보완자본은 676.27 이어야 하므로 TFI 는 "초과 0" 을 말한다.
두 표가 모순이다. **`ISSUER_DEFECT_CONFIRMED`.**

> 참고 — 등재 note 의 문장 "채무성 자본이 0인 회사가 한도-체크 두 줄에 같은 숫자를 실제로
> 인쇄한다" 는 이미 2026-08-24 iter-3 에 정정됐다(나머지 9분기 채무성 자본은 53.06 · 71.51 ·
> 158.65 … 로 0 이 아니다). 이번 재감사도 같은 결론이다: 채무성 자본은 0 이 아니고,
> `47 == 48` 은 **발행사가 `한도 적용 전` 자리에 `한도` 값을 인쇄한 것**으로 읽는 편이 자연스럽다.
> 다만 그 읽기를 채택해도(참 47 = 51) 2024.3Q·2024.4Q 의 헤드라인 잔차 15·87 은 그대로 남는다.

---

## 3. KR0075 2025.1Q — `ISSUER_DEFECT_CONFIRMED`

### 3.1 등재 당시 주장 (원장 `entries[28]`)

`claim` 은 2024.4Q 와 동일 문구. `note` 에 "이 필링은 페이지 전체가 두 번 찍히지만 라벨-값 짝은
안 깨진다(중복 제거 후 대조). **이 분기는 다리가 닫히므로 `2_tier1_bridge` 는 박제하지 않았다.**"
`citation`: `.../FY2025_Q1/raw/KR0075_비엔피파리바카디프생명보험.pdf` p19,20.

### 3.2 실측

```
3_tier2_composition       RED  347.59 vs 409.0   diff +61.41
51_tfi_tier2_composition  RED  347.59 vs 408.78  diff +61.19
47_tier2_census           RED  TIER2_DUPLICATE_ROW (47 == 48 == 347.59)
2_tier1_bridge            (finding 없음 = GREEN)  2,187 − 69 − 409 = 1,709 = 공시 item2
```

### 3.3 raw 대조 — 마스터 == 원문 (220dpi 렌더 육안 확인)

p20 TFI(백만원): 지급여력금액 211,756 · 기본자본 170,878 · **보완자본 40,878** ·
**한도 적용 전 34,759** · **한도 34,759** · 해약환급금 초과분 30,450 · 메모행 둘 다 `-` ·
SCR 69,517 (×50% = 34,758.5 → 34,759). 텍스트레이어가 페이지를 두 번 찍지만 값은 동일하다.

### 3.4 판정 근거 + 등재 주장에 붙일 정정

가능한 기대값 `47 = 347.59`(INCL) / `48 + 49 = 652.09`(EXCL) 중 어느 것도 408.78 이 아니다.
**한 표가 자기 구성행과 안 닫힌다 = 등재 주장 그대로.** `ISSUER_DEFECT_CONFIRMED`.

다만 이 버킷에 대해서는 **범위를 좁혀 적는 편이 정확하다.** 이 분기는 헤드라인이 완전히 닫히고
다리도 닫힌다. 깨지는 것은 TFI 표의 `한도 적용 전` 행 하나뿐이다 — 그 행을 이 회사의 자기 관행
(나머지 10분기 전부 `보완자본 == 한도 적용 전`)에 맞춰 40,878 로 놓으면 채무성 자본
`40,878 − 30,450 = 10,428 < 한도 34,759` 라 한도가 안 물고 구성·다리가 **둘 다** 닫힌다.
즉 이 버킷의 잔차 61.41/61.19 는 **인쇄행 한 칸으로 전부 설명된다.** 발행사 결함이라는 판정은
바뀌지 않지만, "표가 통째로 이상하다" 가 아니라 "한 행에 한도값이 들어갔다" 로 적어야
다음 세션이 과잉 일반화하지 않는다.

---

## 4. KR0087 동양생명 2025.2Q — **`OUR_RULE_DEFECT` (뒤집힘)**

### 4.1 등재 당시 주장 (원장 `entries[29]`) — **이 주장이 거짓이다**

- `claim`: "헤드라인표가 **자기 각주 주1) 을 어긴다.** 주1) 은 '기본자본은 … 순자산에서 지급여력금액
  불인정 항목(단, 보완자본 한도를 초과한 금액을 제외) 및 보완자본으로 재분류하는 항목을 차감한 금액'
  이라고 써 놓고, **인쇄된 기본자본은 불인정항목 1,188 을 빼지 않았다.**"
- `claim_kind`: `ISSUER_INTERNAL_INCONSISTENCY` · `status`: `VERIFIED`
- `citation`/`verify`: `data/disclosure/FY2025_Q2/raw/KR0087_동양생명.pdf` p15,16
- `note` 중: "33,001 − (1,188 − 0) − 18,883 = 12,930 ≠ 14,118 (잔차 +1,188 = 정확히 item12).
  33,001 − 0 − 18,883 = 14,118 = 공시 기본자본(정확 일치)."
- `expected_residual`: `{"2_tier1_bridge|적용전": 1188.0, "47_tier2_census|적용전": null}`

**등재는 `한도초과 = 0` 을 전제로 "그러니 발행사가 불인정을 안 뺐다" 고 읽었다.
전제가 틀렸다. 참 한도초과는 1,188.04 이고, 발행사는 주1) 을 정확히 지켰다.**

### 4.2 실측

```
2_tier1_bridge   RED  expected 12930.0  actual 14118.0  diff +1188.00
   detail: branch=CAPPED scope=EXCL 한도초과=0
47_tier2_census  RED  TIER2_DUPLICATE_ROW (item47 == item48 == 12107)
3_tier2_composition_post / 51_tfi_..._post  YELLOW  diff +1188.04   <- 같은 숫자가 적용후에 나온다
```

### 4.3 raw 대조 — 마스터 == 원문 (좌표 판독 + 220dpi 렌더 육안)

p16 TFI (단위: 백만원):

```
                          경과조치 적용 전    경과조치 적용 후
지급여력비율(%)                 172.1            177.0
지급여력금액                4,166,224        4,285,029
 기본자본                   1,411,796        1,756,363
 보완자본                   2,754,428        2,528,665
  보완자본 한도 적용 전       1,210,705          866,138     <- 적용전/적용후가 다르다
  보완자본 한도              1,210,705        1,210,705
  해약환급금...초과분         1,543,723        1,543,723
  (기발행 신종자본증권)         344,567            (사선)
  (기발행 후순위채무)                 0            (사선)
지급여력기준금액             2,421,410        2,421,410     -> x50% = 1,210,705 = 한도
```

p15 헤드라인(억원): 순자산 33,001 (= 12,705 + 3,446 + 15,883 − 595 − 14,943 + 0 + 16,505, 자기
구성행 합 정확 일치) · 불인정 1,188 · 재분류 18,883 · 기본자본 14,118 · 보완자본 27,544 ·
지급여력금액 41,662 · SCR 24,214. 주1) 문구는 등재가 인용한 그대로 인쇄돼 있다.

### 4.4 인과 — 룰의 `한도초과` 가 0 인 것이 틀렸다

`item47` 로 적재된 `한도 적용 전 1,210,705` 는 **한도를 이미 적용한 값**이다(= `한도` 와 같은 값).
참 한도적용전(채무성 보완자본)은 인쇄돼 있지 않지만, **같은 표의 적용후 컬럼에서 되짚어진다.**

**독립 도출 D1 — TFI 적용후 컬럼만 사용(헤드라인을 전혀 안 본다):**

```
적용후 인정 채무성 보완자본 = 보완자본_후 − 해약환급금_후
                            = 2,528,665 − 1,543,723 = 984,942
경과조치 = (기발행 신종자본증권) 344,567 의 기본자본 승격
   확증: 기본자본_후 − 기본자본_전 = 1,756,363 − 1,411,796 = 344,567  (item53 과 정확 일치)
참 한도적용전(적용전) = 984,942 + 344,567 = 1,329,509      (인쇄값 1,210,705)
한도초과 = 1,329,509 − 1,210,705 = 118,804 백만 = 1,188.04 억
   되짚기: min(1,329,509, 1,210,705) + 1,543,723 = 2,754,428 = 인쇄 보완자본_전  (닫힌다)
```

**독립 도출 D2 — 헤드라인 표만 사용(TFI 를 전혀 안 본다):**

```
item2 − (item4 − item12 − item13) = 14,118 − (33,001 − 1,188 − 18,883) = 1,188 억
```

**보강 D3 — 경과조치 순증:** `지급여력금액_후 − 지급여력금액_전 = 4,285,029 − 4,166,224 = 118,805`
백만 = 1,188.05 억. 신종자본증권이 기본자본으로 승격되면서 **한도로 잘려 있던 초과분이 그대로
풀린다** — 경과조치가 자본을 늘리는 정확한 크기가 적용전 한도초과액이다.

세 도출이 **1,188.04 / 1,188.00 / 1,188.05** 로 일치한다. D1 과 D2 는 공유하는 셀이 하나도 없다.

### 4.5 고친 산식이 공시값을 재현한다

```
item2 == item4 − (item12 − 한도초과) − item13
      = 33,001 − (1,188 − 1,188.04) − 18,883 = 14,118.04
공시 item2 = 14,118            잔차 = +0.04   (게이트 tol 2.0)
현행 룰(한도초과 = 0)          = 12,930        잔차 = +1,188.00
```

적용후 구성도 같은 참값으로 닫힌다:
`min(1,329,509 − 344,567, 1,210,705) + 1,543,723 = 2,528,665` = 인쇄 보완자본_후 **정확 일치**
(인쇄된 `한도적용전_후 866,138` 을 그대로 쓰면 2,409,861 로 118,804 어긋난다 — 지금 YELLOW 로
찍히는 그 숫자다).

### 4.6 반증 시도 — 같은 발행사 13분기 전수 (가장 중요한 검사)

"불인정항목 = 보완자본 한도초과액" 이 이 회사의 관행인지, 2025.2Q 만 끼워 맞춘 것인지를
13분기 전수로 확인했다(`req_exc` = 헤드라인이 요구하는 한도초과, `exc_tfi` = TFI 에서 되짚은 한도초과):

| 분기 | item12 | req_exc | exc_tfi | 현행 다리 |
|---|---|---|---|---|
| 2023.1Q ~ 2025.1Q (9분기) | 0 또는 624 | 0 ~ ±1 | 전부 음수(= 초과 없음) | 닫힘 |
| **2025.2Q** | **1,188** | **1,188.00** | **+1,188.04** | **RED +1,188** |
| 2025.3Q | 0 | 0 | 음수 | 닫힘 |
| 2025.4Q | 996 | 937.00 | — | 닫힘 (인쇄 47 12,223.25 > 48 11,286.49 → 룰이 936.76 을 산출) |
| 2026.1Q | 943 | 943.00 | — | 닫힘 (인쇄 47 12,405.78 > 48 11,463.16 → 룰이 942.62 를 산출) |

**2025.4Q·2026.1Q 가 결정적이다.** 그 두 분기에는 발행사가 `한도 적용 전 > 한도` 를 정상적으로
인쇄하고, **현행 룰이 계산한 한도초과로 다리가 그대로 닫힌다**(잔차 0.24 · 0.38). 즉 이 회사는
보완자본 한도초과액을 `Ⅱ. 지급여력금액으로 불인정하는 항목` 에 담고 주1) 대로 다시 제외한다 —
그것이 이 발행사의 일관된 관행이고, 룰은 47 > 48 이 인쇄된 분기에서는 그 관행을 이미 올바로 처리한다.
**2025.2Q 만 `한도 적용 전` 자리에 한도값이 인쇄돼 룰의 한도초과가 0 이 됐다.**
2025.4Q 는 `item12 996 = 한도초과 937 + 그 밖의 불인정 59` 로, "불인정 = 한도초과" 가 항등식이
아니라는 것까지 보여 준다(그래도 주1) 은 정확히 성립한다).

### 4.7 판정과 그 경계

**`OUR_RULE_DEFECT`.** 등재된 `2_tier1_bridge|적용전 = 1188.0` 의 **원인 귀속이 틀렸다.**
발행사는 자기 각주를 어기지 않았다 — 각주대로 계산했고, 우리 룰이 `한도초과 = max(0, item47 − item48)`
을 인쇄값 그대로 적용해 0 을 얻은 것이다. KR0068 한화생명과 같은 형태다(마스터 셀은 한 칸도
안 틀렸고, 틀린 것은 `item47` 이 무엇을 담고 있는지에 대한 룰의 가정이다).

**뒤집히지 않는 부분을 분명히 한다:**

- `47_tier2_census` 의 `TIER2_DUPLICATE_ROW` RED 는 **여전히 발행사쪽 사실**이다. 발행사가
  `한도 적용 전` 행에 한도 적용 **후** 값을 인쇄한 것이 맞고, 그 축의 면제는 그대로 유효하다.
  다만 그 사유를 "발행사가 우연히 같은 값을 인쇄했다" 가 아니라 **"참 한도적용전 = 1,329,509
  인데 한도값 1,210,705 가 인쇄됐다"** 로 적어야 한다.
- 적용후 컬럼의 `한도적용전_후 866,138` 은 자기 표의 `보완자본_후` 와 118,804 어긋난다 —
  이건 진짜 발행사 결함이고 지금 YELLOW(`POST_IDENTITY_UNESTABLISHED`)로 나간다.
- 마스터 셀은 **한 칸도 바꿀 것이 없다.**

**룰을 고치기 전 반드시 할 것:** 이 되짚기(`debt_pre = item51_후 − item49_후 + item53 + item54`)는
경과조치로 증권이 승격되는 회사에서만 성립한다. 전 버킷 양방향 시뮬(닫힘/깨짐) 없이 배선하면
안 된다 — 이 저장소가 1건 고치려다 129건 깨뜨릴 뻔한 전례가 있다.

---

## 5. KR0073 교보생명보험 2025.2Q — `ISSUER_DEFECT_CONFIRMED`

> 이 버킷은 tier2 축이 아니라 `36_irr`(금리위험액 도출) 축이다. 같은 (회사,분기)에 원장 항목이
> **두 개** 있는데, `entries[9]`(`INTERNAL_MODEL_36IRR_EXEMPT`)는 이미 `status: CONTRADICTED` 로
> 해제됐고 살아 있는 것은 `entries[15]` 다.

### 5.1 등재 당시 주장 (원장 `entries[15]`)

- `registry`: `IRR_DERIVE_ISSUER_INCONSISTENT` · `claim_kind`: `DERIVATION_NOT_REPRODUCIBLE` ·
  `status`: `VERIFIED` · `cause`: `UNEXPLAINED`
- `claim`: "표준 도출식이 공시 금리위험액을 재현하지 못한다 — item36 +5.25% 잔차 241.4374억.
  item36 도 41-46 도 원문 그대로이며 어느 쪽도 추출 오류가 아니다. item36 은 시장위험 축
  (item19 = sqrt(item36~40·MARKET_M))에서 |rel| ≤ 0.0022% 로 닫히므로 값 자체는 다른 축에서 검산된다."
- `citation`/`verify`: `data/disclosure/FY2025_Q2/raw/KR0073_교보생명보험.pdf` p21, present_markers
  `② 금리위험액 현황 · 당기(25.2Q) · Ⅲ. 순자산가치 · Ⅳ. 금리 위험액 · -5,667,711 · -5,414,904 ·
  -6,352,338 · -5,586,899 · -5,463,138 · -5,742,051 · 459,988`
- `expected_residual`: 적용전·적용후 각 `241.4373504145833` · `pin_tolerance` 0.01

### 5.2 실측

```
36_irr  SKIP(면제)  expected 4358.442649585417  actual 4599.88  diff 241.4373504145833
   -> 박제 241.4373504145833 과 소수점 13자리까지 일치. PIN_STALE 아님.
19_market  GREEN  (item36 이 시장위험 축에서 닫힌다 — 등재 corroboration 그대로 유효)
```

적용후 컬럼: `item36 · item41~46` 전부 적용전과 동일 값이 실려 있어 적용후 박제도 같은 값으로 성립한다.

### 5.3 raw 대조 — 마스터 == 원문

p21 [② 금리위험액 현황] 당기(25.2Q), 단위 백만원:

```
Ⅲ. 순자산가치   충격전 -5,667,711 | 평균회귀 -5,414,904 | 금리상승 -6,352,338
                 금리하락 -5,586,899 | 금리평탄 -5,463,138 | 금리경사 -5,742,051
Ⅳ. 금리 위험액   459,988   (표 폭 전체를 덮는 병합셀)
```

마스터 `item41~46 = -56,677.11 / -54,149.04 / -63,523.38 / -55,868.99 / -54,631.38 / -57,420.51`,
`item36 = 4,599.88` — 백만원↔억원 환산까지 정확 일치. **`MASTER_DEFECT` 배제.**

### 5.4 반증 시도 — 재현하는 산식이 하나도 없다

충격량(= 충격전 − 시나리오): 평균회귀 **-252,807** · 금리상승 **+684,627** · 금리하락 -80,812 ·
금리평탄 -204,573 · 금리경사 +74,340.

`(다리/평탄·경사 짝 조합) × (0절단 on/off) × (평균회귀 가산·감산·제곱합·제외)` 전수 탐색 결과
서로 다른 값이 4개뿐이고, 목표 459,988 에 가장 가까운 것이 **현행식 435,844**(Δ 24,144 백만 = 241.44억)다.
나머지는 688,651 · 733,588 · 941,458 로 훨씬 멀다. 다섯 충격량 중 두 개의 제곱합 제곱근에
나머지 하나를 ±로 붙이는 모든 조합에서도 ±1,000 백만 안에 들어오는 것이 **0건**이다.

**"고친 산식이 공시값을 재현한다" 는 조건을 만족하는 후보가 없다 → 뒤집을 수 없다.**

### 5.5 판정 근거 — 그리고 등재 note 에 붙일 정정 2건

같은 표 안에서 `Ⅳ. 금리 위험액` 이 자기 `Ⅲ. 순자산가치` 6열에서 재현되지 않는다.
닫는 규칙이 표에 인쇄돼 있지는 않지만(감독규정 표준식이다), 그 식은 **면제 5건을 뺀 나머지
221 버킷 전부에서 통과**한다(현재 게이트 실측: `36_irr` GREEN 172 · YELLOW 49 · **RED 0**).
입력 6칸은 원문 그대로이고 `item36` 은 `19_market` 축에서 따로 검산된다.
**`ISSUER_DEFECT_CONFIRMED`** — 인용은 p21 [② 금리위험액 현황] 당기(25.2Q) 열의
`Ⅲ. 순자산가치` 행과 `Ⅳ. 금리 위험액` 행.

**정정 1 — 등재 note 의 하한 논증은 성립하지 않는다.** note 는 "금리상승 시나리오 단일 충격량
684,627 이 공시 금리위험액 459,988 보다 크다. K-ICS 금리위험액은 최악 단일 시나리오 충격량보다
작아질 수 없으므로 …" 라고 적었다. 그러나 **우리 자신의 표준식 도출값도 435,845 로 684,627 보다
작다** — 이 필링의 평균회귀 충격량이 음수(-252,807)라 도출값을 끌어내리기 때문이다.
따라서 그 문장은 이 버킷의 근거가 될 수 없다. 면제 자체는 §5.4 의 전수 탐색(재현 산식 부재)과
221/221 대조군으로 충분히 선다 — **근거를 그쪽으로 갈아야 한다.**

**정정 2 — 이 잔차는 이 분기만의 사건이 아니다(신규 사실).** 같은 발행사 6개 짝수분기를 전수로 재면
공시값이 **매 분기 도출값보다 크고**, 절대 잔차가 계속 커진다:

| 분기 | 공시 item36 | 도출 | 잔차 | 상대 | 5% tol | 판정 |
|---|---|---|---|---|---|---|
| 2023.2Q | 17,560.36 | 17,537.36 | +23.00 | 0.13% | 876.87 | ok |
| 2023.4Q | 12,247.50 | 12,155.62 | +91.88 | 0.76% | 607.78 | ok |
| 2024.2Q | 8,293.09 | 8,182.33 | +110.76 | 1.35% | 409.12 | ok |
| 2024.4Q | 4,569.19 | 4,444.14 | +125.05 | 2.81% | 222.21 | ok |
| **2025.2Q** | **4,599.88** | **4,358.44** | **+241.44** | **5.54%** | **217.92** | **RED** |
| 2025.4Q | 8,208.41 | 8,130.40 | +78.01 | 0.96% | 406.52 | ok |

`36_irr` 의 허용오차가 `max(2.0, 5% × 기대값)` 이라, 같은 성질의 편차가 6분기 중 5분기는
조용히 통과한다. 2025.2Q 가 RED 인 것은 편차가 유별나서가 아니라 **기준금액이 작아져
상대비가 5% 를 넘었기 때문**이다(임계 대비 10.8% 초과). 면제 사유를 "이 한 분기가 이상하다"
로 적어 두면 사실과 다르다 — **발행사 상시 편의(systematic bias)** 로 적어야 한다.
이건 게이트 사각으로도 따로 볼 값어치가 있다(상대 tol 이 상시 편의를 흡수한다).

---

## 6. 무엇을 확인하지 못했나 (정직한 잔여)

- KR0075 3분기에서 **왜** 발행사가 `한도 적용 전` 자리에 한도값을 인쇄했는지는 확정하지 못했다.
  나머지 10분기 관행(`보완자본 == 한도 적용 전`)과 어긋난다는 사실만 말할 수 있다.
- KR0075 2024.3Q·2024.4Q 의 헤드라인 잔차(15·87)는 TFI 를 어떻게 읽어도 메워지지 않는다.
  두 표가 서로 다른 사실을 말하는 것까지가 확정이고, 어느 쪽이 옳은지는 원문에서 못 정한다.
- KR0073 의 공시 금리위험액이 어떤 모수로 계산됐는지는 이 필링에서 확정 불가다. p21 [② 금리위험액]
  절에는 작성기준 각주가 없다. (같은 필링 p22 [③ 주식위험액] 에는
  "주) 주식위험액은 주식위험에 직·간접적으로 노출된 자산과 부채를 대상으로 측정" 이 인쇄돼 있는데,
  금리 쪽에는 대응 문장이 **없다**. 있었으면 근거가 됐을 것이므로 그 부재를 기록해 둔다.)

## 7. 재현 명령

```
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_extract_ledger.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_findings_KR0075_KR0087_KR0073.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_master_items.py KR0075 2,3,4,12,13,14,47,48,49,51,52,53,54
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_kr0087_excess_recovery.py KR0087
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_kr0087_verify.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_kr0073_irr_search.py
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_kr0073_irr_allq.py KR0073
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_rowdump.py "data/disclosure/FY2024_Q3/raw/KR0075_비엔피파리바카디프생명보험_amended.pdf" 15,16
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_rowdump.py "data/disclosure/FY2024_Q4/raw/KR0075_비엔피파리바카디프생명보험.pdf" 49,50
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_rowdump.py "data/disclosure/FY2025_Q1/raw/KR0075_비엔피파리바카디프생명보험.pdf" 19,20
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_rowdump.py "data/disclosure/FY2025_Q2/raw/KR0087_동양생명.pdf" 15,16
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_rowdump.py "data/disclosure/FY2025_Q2/raw/KR0073_교보생명보험.pdf" 21,22
C:/Users/sangwook.cho/venvs/insurequant/Scripts/python.exe scripts/_probes/probe_20260824_reaudit_render.py <pdf> <page> <outprefix> 220
```

전부 읽기 전용이다. 이 재감사는 원장·룰·마스터·xlsx·게이트를 **하나도 수정하지 않았다.**
"""

OUT.parent.mkdir(parents=True, exist_ok=True)
OUT.write_text(TEXT, encoding="utf-8")
print("wrote", OUT, OUT.stat().st_size, "bytes")
print("first line:", TEXT.splitlines()[0])
