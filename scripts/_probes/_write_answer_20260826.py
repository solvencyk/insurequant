import sys
from pathlib import Path
sys.stdout.reconfigure(encoding="utf-8")
ROOT = Path(__file__).resolve().parents[2]
p = ROOT / "inbox/parser/20260826T0500Z__validation__MULTI__separate_restore_residuals_3items.md"
txt = p.read_text(encoding="utf-8")
txt = txt.replace("status: open", "status: answered", 1)
ANS = """## 답변 (parser-ifrs17, 2026-08-26)

**3건 중 ①은 반려, ②③은 확증(단 각각 정정 1~2건).** 마스터 파일은 하나도 건드리지 않았다.

---

### ① 삼성생명 10셀 — **반려. "어느 원천으로도 확인 안 됨"이 아니다. 현재 마스터가 맞다.**

세 가지를 실측했다.

**(1) 차이의 정체는 `환율변동효과 등` 행이다.** 인쇄된 raw 행이고, 찾을 수 있다.

블록에서 `환율변동` 행만 제거한 사본으로 `block_stages`를 돌려(다른 경로 전부 동일)
`waterfall_for_dir`을 재실행하면 **5분기 10셀이 이전 값과 정확히 재현된다**:

| 분기 | item3 이전 | item3 현재 | 환율제거 재현 | item4 이전 | item4 현재 | 환율제거 재현 |
|---|---:|---:|---:|---:|---:|---:|
| 2024.1Q | 1096.8 | 1118.0 | **1096.8** | -3588.9 | -3610.1 | **-3588.9** |
| 2024.2Q | 2239.7 | 2276.4 | **2239.7** | -7117.2 | -7153.9 | **-7117.2** |
| 2024.3Q | 3420.1 | 3431.9 | **3420.1** | -10378.1 | -10390.0 | **-10378.1** |
| 2024.4Q | 4613.4 | 4683.5 | **4613.4** | -16977.6 | -17047.6 | **-16977.6** |
| 2025.1Q | 1161.7 | 1160.5 | **1161.7** | -58.4 | -57.2 | **-58.4** |

즉 이전 vintage = `순 금융손익`만, 현재 = `순 금융손익 + 환율변동효과 등`이다.
2024.1Q 실물 예시(FY2024_Q1 본문, 단위 백만원, CSM 3개 열 = 수정소급/공정가치/완전소급):

```
보험계약의 순 금융손익   6,541 + 9,181 + 29,708 = 45,430
환율변동효과 등          1,772 +     0 +    350 =  2,122   <- 이 행이 차이 전부
```

선택된 3개 상품라인 블록 합: 이자 111,798백만 = 1,117.98억 → **1,118.0** (현재값),
2,122 빼면 109,676백만 = 1,096.76억 → **1,096.8** (이전값). 잔차 0.

**(2) 현재 마스터는 raw 에서 그대로 재현된다 — 6항목 × 5분기 전부.**
`waterfall_for_dir` 재추출값이 `CSM_waterfall.json`과 전 항목 일치(±0.05억).
`+u:lit-conf`(표 리터럴로 단위 확정)까지 붙는다. **vintage 혼재가 아니다.**
같은 코드경로가 IR 팩트시트와도 맞는다:

| | 기초 | 신계약 | 이자부리 | 가정조정 | 상각 | 기말 |
|---|---:|---:|---:|---:|---:|---:|
| 2025.4Q raw 재추출 | 129020.2 | 30594.7 | 4937.5 | -17577.8 | -14796.0 | 132178.7 |
| 2025.4Q IR 팩트시트 | 129020.2 | 30594.7 | 4937.5 | -17577.8 | -14796.0 | 132178.7 |
| 2026.2Q raw 재추출(anchor) | 132178.7 | 17174.6 | 2584.6 | -7188.4 | -7336.6 | 137413.0 |
| 2026.2Q IR 팩트시트 | 132178.7 | 17174.6 | 2584.6 | -7188.4 | -7336.6 | 137413.0 |

**(3) 규약은 발행사 자신이 정의한다.** `SLI 1HFY26 Factsheet_KOR.xlsx` 시트 `Ⅰ-3`
(= 티켓이 인용한 그 파일) BEL 절이 이자부리의 구성을 명시적으로 쪼갠다:

```
이자부리
 - 부담이자
 - 공시이율차
 - 외환차 등        <- 삼성생명 IR 이 외환을 '이자부리' 밑에 둔다
```

가정변경등 조정은 별도 행이다. 즉 환율을 item3 에 넣는 현재 처리가 **발행사 분류와 같고**,
빼면 그만큼이 잔차인 item4(가정 및 경험 조정)로 흘러가 발행사 분류와 어긋난다.

**곁들여 — 티켓이 이상징후로 든 "두 항이 정확히 상쇄된다"는 구조상 필연이다.**
`waterfall_for_dir` 은 item4 를 `기말 - (기초+item2+item3+item5)` 잔차로 만든다
(L1183). item3 을 무엇으로 잡든 item4 가 같은 크기로 반대로 움직이고 closing 은 항상 닫힌다.
이건 정보가 아니라 정의다 — 다음 세션이 이걸 근거로 삼지 않도록 적어 둔다.

**"raw 에서 468,350·461,340 이 0회"는 아무것도 증명하지 않는다.** 이 값들은 3개 상품라인
블록의 합이고 raw 에는 개별 블록만 인쇄된다. 키워드 부재를 원천 부재로 읽지 말 것
(같은 함정이 이 저장소에서 3회 반복됐다).

**결론: 정정 불필요. 현재 마스터(=환율 가산)를 유지한다.** 티켓 ① 종결.
`build_csm_waterfall_master.py` L233 주석의 "2025엔 없음(0)"만 부정확하다 —
2025.1Q 환율 행은 존재하고 -120백만이다(그래서 유일하게 현재값이 더 작다). 서술만 부정확하고
처리는 맞다.

재현: `scripts/_probes/probe_20260826_kr0069_fx_hypothesis.py <pre.json>` ·
`probe_20260826_kr0069_ir_fx_crosscheck.py` · `probe_20260826_kr0069_rowdump.py FY2024_Q1 6`

---

### ② PL 5분기 — **확증. 방향이 맞다. 다만 패턴 서술은 틀렸고, 손대지 않았다.**

측정은 전부 재현됐다(원수 상각 item4 기준, 잔차 5분기 = 2024.1Q/2Q/3Q · 2025.2Q · 2026.2Q).

**raw 증거를 하나 더 확보했다 — FY2024_Q1 필링 안에 같은 표가 두 번 들어 있다:**

```
"...당사는 ..."      22.(1) 보험서비스수익  제공된 서비스의 보험계약마진  350,879   (전분기 333,688)
"...연결실체는 ..."   22.(1) 보험서비스수익  제공된 서비스의 보험계약마진  356,574   (전분기 333,688)
```

현재 PL 이 든 값은 356,574(연결)이고, 별도 350,879 는 워터폴 별도 상각 350,878백만
(=160,256+138,008+52,614)과 1백만 차이(라인별 반올림)로 맞는다. **별도로 갈면 닫힌다.**

**단 "반기(2Q) 필링 전부 + FY2024 분기 필링" 패턴은 반증된다.** 2023.2Q 는 반기 필링인데
닫혀 있다. 닫히는 진짜 이유는 필링 종류가 아니라 **그 시기엔 연결과 별도가 같은 값**이라는
것이다 — 위 두 표의 전분기(2023.1Q) 열이 둘 다 333,688 로 동일하다. 연결효과는 2024 부터
갈라진다. 추출기가 별도를 고르는 데 성공해서 닫힌 게 아니다. `_prefer_ofs` 가설을 이 패턴
위에 세우면 안 된다.

**미조치**: 티켓 본문이 "PL 은 병렬 세션 작업 중"이라 적었고 `scripts/pl_breakdown/` 은
미접촉 대상이다. 위 raw 좌표(같은 필링 내 "당사" vs "연결실체" 두 표)를 근거로만 넘긴다.
`csm_amort_identity_ledger.json` 도 안 건드렸다.

재현: `scripts/_probes/probe_20260826_kr0069_pl_context.py <FY2024_Q1 xml> "350,879" "356,574"`

---

### ③ diag stale — **확증. gold 30셀 유지가 맞다. 정정 2건.**

diag(2026-08-17)와 현재 코드 재추출을 직접 대조했다. 주장대로다:

| | diag 기말 | 현재 코드 재추출 | 배포본 |
|---|---:|---:|---:|
| KR0029 2025.4Q | 928,075.0 | **928.1** | 928.07497 |
| KR0075 2025.4Q | 299,583.9 | **299.6** | 299.584 |
| KR1098 2025.4Q | 3,411.9 | **3.4** | 3.41188 |

**정정 1 — KR0029 는 "1000배로 되돌아가지" 않는다.** 928,075 > `CSM_ABS_CAP`(5e5) 이라
`build_root_masters.build_csm` 이 그 company-quarter 를 통째 `None` 으로 만든다
(L219-228). 즉 gold 를 지우면 화면에서 **AIG 2025.4Q 가 사라진다**(틀린 값이 실리는 게 아니라
빈칸). 실제로 1000배 값이 실리는 건 cap 밑인 **KR0075·KR1098** 이다. 영향 버킷 전수 = 5개:
KR0029 2025.4Q · KR0075 2024.4Q/2025.4Q · KR1098 2024.4Q/2025.4Q.

**정정 2 — KR0051 은 이 목록에 넣을 대상이 아니다.** `exclude_companies` 등재사라
마스터에 행이 **0개**다(화면 무영향). 덧붙여 그 제외 사유문에 적힌 "waterfall_for_dir
단위휴리스틱 미수정 → 여전히 1693.2억" 서술은 이제 **stale** 이다 — 현재 코드로 돌리면
`+u:lit-near` 로 **1.7억**이 나온다. 2026-08-25 단위 리터럴 수정이 이 회사도 고쳤다.
(제외 유지 결론 자체는 그대로 유효하다. 사유문만 갱신 대상.)

**순서 지시는 그대로 수용한다**: diag 재생성(owner 승인 필요) → gold 30셀 삭제 검증 →
그 전까지 `why` 에 사유 부기. **이 세션은 diag 를 재생성하지 않았다**(실행금지 관례).

`_LXML_LINE_NO_SENTINEL`(L902): 전 트리 미참조 확인. 다만 L293-299 주석이 같은 함정의
근거를 담고 있어 **사유 없는 삭제는 하지 않았다**(무관 죽은코드는 보고만 하는 관례).

---

### 곁들여 — `20260825T2200Z` 의 gold census 가 stale

그 티켓의 `gold entries = 381` 은 측정 시점(2026-08-25 22:00)이 `b2293c8` 이전이다.
현재는 **277건**(b2293c8 이 104건 삭제). 공란 44건과 그 분포
(KR0003 12 · KR0072 5 · KR0079 27)는 현재 트리에서도 그대로 성립한다 — 요청 자체는 유효하다.

### 이 세션이 건드린 것

`scripts/_probes/probe_20260826_*.py` 7개(신규, read-only 프로브) + 이 답변.
`CSM_waterfall.json` · `PL_breakdown.json` · `data/_gold/*` · 빌더 · diag **전부 미접촉**.
"""
txt = txt.replace("## 답변 (parser 작성 — 처리 후)\n\n<처리 결과. 못 했으면 왜 못 했는지.>", ANS)
p.write_text(txt, encoding="utf-8")
print("wrote", p, len(txt))
print(txt.splitlines()[3])
